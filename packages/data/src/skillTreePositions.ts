/**
 * **技能树坐标覆盖表**（**2026-09-22 船长令**：「**能否将测试中的技能页面放入EXCEL中，将其位置转换成坐标。
 * 我来手动调整图标位置？**」）。
 *
 * 它是什么：**节点中心点的像素坐标**（**每本技能书自己的局部坐标系**，原点 = 该图左上角）——
 * 只填**你手动挪过**的节点；**没填的节点照 `ui/skillTreeLayout.ts` 的算法自动排**（层压紧 ＋ 树列 ＋
 * 孤立点垫后）。空表 = 全部走算法（当前状态）。
 *
 * 怎么产生（三轮：导出 → 你改 → 我回写）：
 * 1. **导出**：`npm run skilltree:export` → 生成 `content-csv/skilltree-workbench.xlsx`
 *    （每行一个技能：`x`/`y` 是当前坐标，另有 `行`/`列` 反推读数与前置读数）；
 * 2. **你改**：只改 `x`/`y` 两列（整数、每本书局部坐标）；空单元格 = 该节点不动；
 * 3. **我回写**：按 id 填进本表，然后跑 `npm run skilltree:layout-check`（不越界 / 同行不重叠 /
 *    子在前置正下方）把冲突点出来。
 *
 * 网格参考（不知道填多少时按这个走）：一格 = **116 × 102** 像素；第 0 行/列的格子中心 = **`(84, 52)`**
 * ⇒ 第 n 列中心 `84 + 116n`、第 m 行中心 `52 + 102m`（树列与"孤立点列"之间另有一个 14px 的组间距；
 * 拿不准就以 `npm run skilltree:export` 导出的表里那一行的 `x`/`y` 为基准加减）。
 */
export const SKILL_TREE_POSITIONS: Readonly<Record<string, { readonly x: number; readonly y: number }>> = {
  // 例：'mining': { x: 152, y: 154 },
}
