# AGENTS.md — 大鲸鱼-深空放置 · 仓库级 AI 工作约定(开工必读)

> 本文件会随会话自动进入上下文,是**可执行的精简版约定**;权威细则与完整章节见
> `docs/development-conventions.md`(仓库唯一权威)。两者冲突时以该文档为准,并回报船长同步本文件。
> 所有会话(一号/二号/三号/新助手)开工先读本文件。
> **🔴 本文件仅船长明确指示时修改(2026-09-15 船长定)**;改后必在 `docs/development-conventions-changelog.md` 顶部记一条。

## 0. 开工顺序
1. 读本文件 → 2. **读 `docs/catalog.md`（文档目录）**，只读本次任务需要的那几份 → 3. 看代码现状,不凭记忆/猜测写。
   (例外:首次接手/大工程开工/压缩后续接,才通读 `docs/development-conventions.md`。)

## 0.1 文档目录（指路）
- 按任务类型该读哪几份 · 权威文档清单 · 检索查证顺序 ⇒ **`docs/catalog.md`**（开工按它指路）。
- 压缩/续接后上下文中没有它 ⇒ 先重读这份目录，不凭记忆猜该读哪份。

## 1. 语言与汇报
- 开发侧一律中文:沟通、思考呈现、总结、文档、代码注释、日志(2026-09-09 船长:中文优先、少用非单位英文缩写;单位类与词典专名可直接用)。
- **玩家可见文案中英双语(2026-09-19 船长特批改判)**:游戏内文案(界面 / 内容数据 / 说明文字)做**中文 + English 双语**;
  开发侧(文档 / 注释 / 汇报 / 思考呈现 / 日志)仍全文中文。英文口径(术语与专名译名)以 `docs/glossary-en.md` 为准(船长审后冻结);
  工程做法见工作文档 `docs/design/l10n-en-20260919.md`。⚠ 本条为**船长明确指示**下修改,已在 changelog 顶部记账。
- **实现 = id 映射制(2026-09-20 船长定)**:新写的玩家可见文案**先取 id、再写文案**——① 造 id(形态 `<域>.<短名>.<三位序号>`)
  ② 调用点写 `t('id')`(core 文案用 `textId` / `errorId`) ③ 在唯一表 `packages/data/src/l10n/table.ts` 补 `zh` + `en`;
  **不得内联中文字面量**。这样以后加别的语言只需**按同一批 id 补一列译文**、实现零改动。
  细则与例外(声明制 `l10n-keep` / 合入前必跑 `l10n:check`+`l10n:list`)见约定 **§十一之三**。
  ⚠ 本条同属**船长明确指示**下修改,已在 changelog 顶部记账。
- 汇报结论先行,列清楚:**改了哪些文件 + 行为变化 + 验证结果 + 已知取舍**,不藏改动。
- **验证证据标注分寸(2026-09-11 船长定「乙」)**:只在与结论易被误读的场合(桌面外壳 / 真档只读取证 / 性能耗时 / 跨窗口几何)才标"验证目标与工具";纯文案/数据/逻辑类用"实测""已核""全绿"即可——标注是为讲清证据边界,不是显得严谨。
- **船长的验收视图默认 = 本地(2026-09-12 船长定)**:报障/读数默认看的是本地构建;先按"本地产物是不是最新"查并提醒**重建/重启**,别先猜"你看的是线上旧产物";要区分就问一句"本地还是线上"。

## 2. 四步闸门(最高优先级,未走完不得动工)
触发:新的大功能/系统级/存档结构改动 · 涉及设计取舍或数值体系 · 需求模糊、有多个方案、或没说"开始做"。
流程:1 **集中提问**(一次问完) → 2 **中文设计总结**(方案+取舍+涉及模块/数值+边界,结尾写"请确认") → 3 **等显式确认**("没有反对"≠确认) → 4 **确认后实现**,并把已确认决策落进工作文档 `docs/design/…`(标注状态)。
红线:未确认不动工;不把"随便/你看着办/都行"当确认(再追问一次明确范围);总结必须列清所有改动(新增/修改/删除),不许藏私货;实现中发现需推翻已确认设计 → 停下重走提问+总结+确认。

## 3. 验证闭环
- `npm run typecheck`(core/data/ui/desktop 全绿) · `npm run test -w @whale/core` 全绿;
- 改行为必须同步改/加测试;数值/经济改动跑对应 `npm run …` 工具;
- `npm run content:check`(内容契约体检) · `npm run ui:rot-check`(第五道闸门,2026-09-14 船长定:UI/样式改动必跑;口径细则见约定 §九/§十四);
- 结构改动后跑构建 + 必要时真档迁移检查(真档只读或先备份);
- 临时调试文件按工具纪律处置(见 §6),不留工作区垃圾。

## 4. 工作区与合入纪律(多 agent 并行)
- 主树 `main`(一号与船长独占);二号 `H:\大鲸鱼\Deepseek-EVE-d2`(同仓 worktree,分支 `d2/*`);三号 `H:\大鲸鱼\Deepseek-EVE-verify`(同仓 worktree,分支 `verify`,**不叫 d3**)——核验与收尾。
- **二号/三号永不直接写主树文件**,改动一律在各自工作树提交;开工先核对基线。
- **完成即合入**:一件安排的工作自测全绿后即合并回 main——先解冲突,**确认主树干净(无未提交改动)才 merge**;合完追平 main。
- **推送闸门(2026-09-08 船长定,与公告同批)**:开发/推进中只本地 commit 不推送 origin;只有该工作**确实完成**(自测全绿+船长验收口径满足;大更时公告稿已过船长审核写入)后,才把**代码与公告一起推送一次**;未完成的本地提交标"待验收/待发布"。
- docs 唯一权威在主树;真档只读/走备份,绝不覆写船长正在玩的档。
- **合入与批量文档写入的编码纪律(2026-09-12 船长定)**:取历史版本一律 `git checkout <rev> -- <path>`(**不经变量中转**);必须写中文盘时用 .NET 显式 UTF-8(`WriteAllText`+`UTF8Encoding`,禁 `Set-Content`/`Out-File` 裸用);合并或批量写文档后必做三条自检(BOM/行尾与改前一致 · 全仓往返解码乱码 0 · 与两侧父提交比"父提交有的行还在")。细则见约定 §三;背景 = 2026-09-12 roadmap 转码事故。
- 疑似对方(一号)在途改动:先停手、报告证据/影响面/判断/交接建议,船长协调前只读观察。

## 5. 检索、术语与文案
- **文档类查询先查 `docs/catalog.md` §三 + `docs/INDEX.md`**(2026-09-15 立);改条目按 **id/唯一键** 检索改写(禁长文本近似匹配);纯文案无 id 才小上下文判断。
- 术语以 `docs/glossary.md` 为权威,新术语先登记再用;压缩遗忘先查词典,不猜不问。
- 导航/功能/系统官方命名以代码与船长口径为准;历史旧称不得混入新文案。
- **游戏内可见文案必须符合游戏设定**;开发/商讨阶段文本(版本/日期/复核/待定/占位/口径等)禁止进入玩家可见范围(UI/日志/公告/任务/教程/手册),交付前自查,发现残留即清理并报告。
- **说明文案硬规矩(2026-09-17 船长定;2026-09-18 废止其中一条)**:**不写原因解释**(尤其括号里——括号只许放规格)。护栏 = `content:check` 的「说明文案原因解释契约」(命中即红,无宽限,细则见约定 §十三)。**⚠「≤30 字」那条已于 2026-09-18 由船长废止**(原话:「**「说明文案 ≤30 字」的计划已经废止,清档所有相关内容**」)⇒ 长度上限、`copyLen`/`COPY_LEN_MAX`、基线文件与生成器、「存量待改写」清单已全部删除。

## 5.1 禁止启动子代理(2026-09-11 船长定,底层规则)
- **任何会话、任何任务,一律不得启动子代理**(subagent / 会话分叉子代理 / worktree 隔离会话 / 多代理编排 workflow / Ralph 式循环,**一个都不许开**):该读的自己读、该查的自己查、该验的自己验,不许把"读文档/搜代码/写实现/跑验证/做审查"分包出去。
- 为什么:子代理拿不到本会话上下文与船长口径,容易跑偏、违铁律、重复耗资源。多 agent 只指 §4 里船长安排的一号/二号/三号工作区。
- 事情多时:分阶段自己做完并如实汇报进度,或请船长排期;不得靠派生代理"并行提速"。

## 5.2 新提议与旧规则冲突时,先提醒船长(2026-09-11 船长定,底层规则)
- 新提议与**既有已确认规则/契约/测试**冲突(按新提议做会让其失效) ⇒ **先停下,把冲突摆出来请他裁决**——不许默默按新的做,也不许拿旧规则当挡箭牌拒绝执行。
- 提醒格式(一次讲清,别挤牙膏):1 **新提议原话**(照抄) · 2 **冲突旧规则**(条款原文+出处文件行号+日期裁定语) · 3 **二者不可同时成立的后果**(数值/机制/文案/存档影响面) · 4 **可选做法+推荐与理由** · 5 结尾问「按哪个来?」。
- 裁决后按其实现并把**新裁定写回文档**(旧条款标作废或改写),不让两套口径并存;等不到答复 → 先做与冲突无关的部分,汇报里明确哪处等他定。
- 实证(2026-09-11 稀有残骸彩头案):未先提醒就动工,白改两轮、6 用例失败——详情已封存 roadmap 卷。

## 6. UI 与工具纪律
- **任何位置新增内容,样式一律优先参考同级相似项 UI**(同组相似项 → 同页其它区块 → 同模块其它页面 → 全仓 `app-*` 家族),先复刻既有结构与类名再最小差异,不自造相似新样式;容器/卡片尽量固定尺寸防内容变化引起跳动。
- **一级页不滚(2026-09-08 船长定)**:导航直系页(星图/舰船/装配/物品/市场/工业/技能)整页无滚动条,滚动只进二级窗口;**前提红线:内容必须完整装下,禁止硬裁内容去滚**,放不下先与船长讨论方案再动。
- **视觉物件一律 SVG 线稿(2026-09-10 船长定)**:细描边/currentColor/viewBox,与舰船资产同语言;**禁止 CSS 拼形状**(clip-path/伪元素/渐变裁形/圆角拼图);背景氛围光效可用 CSS。细则见约定 §九。
- **悬停说明统一走 `ui/Tooltip.tsx` 自绘接管层**(2026-09-13 定,09-14/09-15 补):HTML 写 `title`、SVG 写 `data-tip`;限宽 300px;停 `TIP_DELAY_MS=500ms` 才弹;富内容用 `hoverTipProps(content)` 或 `HoverTip`(同延迟、同"内层优先");**禁 SVG `<title>` 子元素**;**同一元素不许同时带 `title` 与 `hoverTipProps`**;不许另起悬停机制。细则见约定 §九。
- 正式工具(tools/xxx.ts)入库 + 注释 + 必要时挂 npm script;临时探针一律 `_` 前缀,收尾二选一:有复用价值转正式入库,一次性即删除。
- **🔴 一律不许关闭浏览器进程(2026-09-13 船长定,底层规则)**:以任何形式结束浏览器进程都不行(`Stop-Process`/`taskkill` 打在 chrome/msedge/firefox/brave 上,不管按进程名批量还是按命令行特征点名);也不许对"不确定是不是自己起的"实例发 CDP `Browser.close`。**为什么**:船长的浏览器自己也带 `--remote-debugging-port`,按特征过滤会命中**他的**实例。**正确做法**:挂后台任务用 `job_kill` 收尾;自己 `Start-Process` 时留 PID、只对这一个 PID 收尾;拿不准就让它跑着、汇报里写明。**宁可留个进程,也不许误关船长的浏览器。** 配套:探针端口用非默认端口并先探占用。细则见约定 §十。
- **🔴 观感审查权在船长(2026-09-13 船长定)**:UI/观感类改动,实现+四闸门跑完直接交船长审——**不许自己起无头浏览器截图当"改好了"的依据**;好不好看只有船长能判。例外仅两类:① **读数型问题**(溢出不溢出/滚动条/跨窗口尺寸几何/性能帧数这类"看代码判不了"的事实) ② **船长明确要求**;要跑就挂后台任务,汇报写明"这是读数、不是观感结论"。细则见约定 §九。
- **后台任务巡检(2026-09-13 船长定)**:任何后台任务每 2 分钟无新输出就主动查一次(读日志尾/进程是否还在/是否卡等);确认正常再继续等,不许挂上不管。探针日志落 `tools/_ui-artifacts/*.log` 以便随时看尾。

## 7. 游戏内更新公告(大更必发;发布需船长审核)
- 每个"大的系统添加或更新"并入主树时,经办人准备一条玩家公告(标题/日期/分类/3~5 条要点);判定与写法见约定 §十二。
- **2026-09-08 发布审核(船长定)**:开发交流/设计讨论内容不得直接写入公告数据;公告文案先整理成待审稿递送船长审核,**明确批准**后才合入 `packages/data/src/announcements.ts`;被驳回按意见修改后再审。
- **2026-09-08 人称与话术(船长补定)**:公告禁用第一/第二人称代词,一律以「玩家」第三人称表述;开发/验收话语(复测/校准/对齐/目标带等)不进公告。

## 8. 文档工作流：工作文档 → 归档（2026-09-15 船长改口径）
- **工作期间不许改旧文档**(船长原话:「工作流没有进行收尾归档事,不允许直接修改旧文档。应该新建一个该工作的临时文档记录工作相关。等到归档时,再合入旧文档。」+「归档后当前工作文档直接删除,只将关键内容归档。」)
- **开工先建工作文档** `docs/design/<主题>-<YYYYMMDD>.md`(头部写:状态:进行中 + 船长原话照抄 + 范围/不做 + 待裁决点);工作期间**只改它**(＋代码/数据/测试),不往 roadmap 追加批次条目、不动词典现行词条。
- **归档三步**(船长验收 + 合入 main 后当批做):① 关键内容**并入**该去的老文档(roadmap 一条精简条目 · 该更新的词典词条 · 必要时 architecture/本约定) ② **删掉工作文档**(细节以 git 历史兜底 ⇒ 结论/数值必须已并入) ③ 重跑 `npm run docs:index`。
- **例外(这几份随时可改)**:**本文件仅船长特批**(见页首) · `docs/development-conventions.md` · 那份 changelog(规则类当场生效) · 词典**新术语登记** · **`docs/catalog.md`(指路目录)** · 生成件 `docs/INDEX.md` 与 `docs/archive/` 封存卷。
- 大工程开工前提示船长压缩点并给续接卡;船长随时可要求「先固化状态/整理续接卡」——立即照做再继续。
- 细则见约定 §十五。

## 9. 测试门槛存档
- 大功能交付按需配可复现脚本与测试存档(`tools/make-test-save.ts` + `docs/test-saves/`,见约定 §八),让船长能立刻实测,不手工改档。

---
_维护:本文件或 `docs/development-conventions.md` 的内容变更,一律在 `docs/development-conventions-changelog.md`
顶部追加一条;细则冲突以 `docs/development-conventions.md` 为准。_
