# AGENTS.md — 大鲸鱼-深空放置 · 仓库级 AI 工作约定(开工必读)

> 本文件会随会话自动进入上下文,是**可执行的精简版约定**;权威细则与完整章节见
> `docs/development-conventions.md`(仓库唯一权威)。两者冲突时以该文档为准,
> 并回报船长同步本文件。所有会话(一号/二号/三号/新助手)开工先读本文件。

## 0. 开工顺序
1. 读本文件 → 2. **读 `docs/catalog.md`（文档目录）**，只读本次任务需要的那几份 → 3. 看代码现状,不凭记忆/猜测写。
   (例外:首次接手/大工程开工/压缩后续接,才通读 `docs/development-conventions.md`。)

## 0.1 文档目录（指路）
- 按任务类型该读哪几份 · 权威文档清单 · 检索查证顺序 ⇒ **docs/catalog.md**（开工按它指路）。
- 压缩/续接后上下文中没有它 ⇒ 先重读这份目录，不凭记忆猜该读哪份。

## 1. 语言与汇报
- 一律中文:面向船长的沟通、思考呈现、总结、文档、代码注释、游戏文案、日志。
- **中文优先、少用非单位英文缩写(2026-09-09 船长定)**:单位类(ISK、秒/分/时、m³ 等)与词典专名可直接用;其余英文缩写/代码标识符(civ、mil、蓝图、制造耗时等)须先给中文全称或纯中文表达,不靠缩写与极度简略省字;代码与 commit 标识符不受限。
- 汇报结论先行,列清楚:**改了哪些文件 + 行为变化 + 验证结果 + 已知取舍**,不藏改动。
- **验证证据的标注粒度(2026-09-11 船长定「乙」)**:报告验证结果时,**只在与结论容易被误读的场合标注"验证目标与工具"**——
  ① 涉及**桌面外壳**(窗口/菜单/IPC 存档读写/打包产物)② **真档只读**取证 ③ **性能与耗时** ④ **跨窗口尺寸几何**(溢出不溢出这类);
  **纯文案 / 纯数据 / 纯逻辑类默认不标注**(也不必每次写"无头 Chrome + 网页版"),用"实测""已核""全绿"这类结论性表述即可。
  分寸:**标注是为了讲清证据边界,不是为了显得严谨**;边界不清会让人误读时才写,其余省略。
- **船长的验收视图默认 = 本地(2026-09-12 船长定)**:「**没有特意说明的情况下,都是本地查看的效果**」——
  船长报障/报读数时**默认他看的是本地构建**(桌面端本地跑 / 本地 dev),**不是**线上网页版;
  排查时先按"本地那份产物是不是最新"查(改完代码要提醒他**重建/重启**本地那份),
  **不要**一上来就推断"你看的是线上旧产物"或让他去刷新网页——线上(推送后的 Pages)只是另一条通道。
  举证时如需区分,先问一句"你用的是本地还是线上",不要替他假定。

## 2. 四步闸门(最高优先级,未走完不得动工)
触发场景(命中其一):
- 新的大功能 / 系统级 / 存档结构(版本迁移)改动;
- 涉及设计取舍、数值体系的改动;
- 船长需求模糊、有多个可选方案、或没有明确说“开始做”。

流程:
1. **集中提问**(一次问完,不边做边问);
2. **中文设计总结**(方案 + 取舍 + 涉及模块/数值 + 边界),结尾写明“请确认”;
3. **等待显式确认**——“没有反对”≠确认,必须听到“可以/没问题/确认”;
4. **确认后实现**,并把已确认决策落进 `docs/design/…`(标注状态:已确认)。

红线:
- 未确认不动工;不把“随便 / 你看着办 / 都行”当确认(再追问一次明确范围);
- 总结必须列清所有改动(新增/修改/删除),不许藏私货;
- 实现中发现需推翻已确认设计 → 停下重走提问+总结+确认。

## 3. 验证闭环
- `npm run typecheck`(core/data/ui/desktop 全绿);`npm run test -w @whale/core` 全绿;
- 改行为必须同步改/加测试;数值/经济改动跑对应 `npm run …` 工具;
- `npm run content:check`(内容契约体检) 与 `npm run ui:rot-check`(旋转口径体检;**2026-09-14 船长定为第五道闸门**——
  UI/样式改动必跑:手机竖屏自动横屏活在"逻辑空间 1200×`--mob-h`",凡用 `vw`/`vh` 定尺寸的覆盖层必须按逻辑空间同轴覆盖,
  窄窗媒体查询在旋转下不许误触发,侧栏让位只许收内边距;只读 `styles.css`、不用浏览器);
- 结构改动后跑构建 + 必要时真档迁移检查(真档只读或先备份);
- 临时调试文件按工具纪律处置(见第 6 节),不留工作区垃圾。

## 4. 工作区与合入纪律(多 agent 并行)
- 主树 `main`(一号与船长独占);二号工作区 = `H:\大鲸鱼\Deepseek-EVE-d2`(同仓 worktree,分支 `d2/*`);
  三号工作区 = `H:\大鲸鱼\Deepseek-EVE-verify`(同仓 worktree,分支 `verify`,**不叫 d3**)——核验与收尾。
- **二号/三号永不直接写主树文件**,改动一律在各自工作树提交;开工先核对基线。
- **完成即合入**:一件安排的工作自测全绿后即合并回 main——先在 d2 侧解冲突,
  确认主树干净(无未提交改动)才 merge;合完 d2 追平 main。
- **推送闸门(2026-09-08 船长定,与公告同批)**:开发/推进中只做**本地 commit,不推送 origin**;
  只有在该工作**确实完成**(自测全绿+船长验收口径满足;大更时公告稿已过船长审核写入)后,
  才把**代码与公告一起推送一次**;未完成的本地提交标注"待验收/待发布"。
- docs 唯一权威在主树;真档只读/走备份,绝不覆写船长正在玩的档。
- **合入与批量文档写入的编码纪律(船长 2026-09-12 定)**:取历史版本一律 `git checkout <rev> -- <path>`
  (**不经变量中转**);必须写中文盘时用 .NET 显式 UTF-8(`WriteAllText` + `UTF8Encoding`,禁 `Set-Content`/`Out-File` 裸用);
  **合并或批量写文档后必做三条自检**——BOM/行尾与改前一致 · 全仓**往返解码**乱码 0 · 与**两侧父提交**比"父提交有的行还在"。
  细则见 `development-conventions.md` §三「合入与批量文档写入的编码纪律」;背景 = 2026-09-12 `roadmap.md` 转码事故(449 行乱码 + 丢 19 行,四连全绿也查不出)。
- 疑似对方(一号)在途改动:先停手、报告证据/影响面/判断/交接建议,船长协调前只读观察。

## 5. 检索、术语与文案
- 按 **id / 唯一键**检索改写内容(禁长文本近似匹配);纯文案无 id 才小上下文判断。
- 术语以 `docs/glossary.md` 为权威,新术语先登记再用;压缩遗忘先查词典,不猜不问。
- 导航/功能/系统官方命名以代码与船长口径为准;历史旧称不得混入新文案。
- **游戏内可见文案必须符合游戏设定**;开发/商讨阶段文本(版本/日期/复核/待定/占位/口径等)
  禁止进入玩家可见范围(UI/日志/公告/任务/教程/手册),交付前自查,发现残留即清理并报告。

## 5.1 禁止启动子代理(2026-09-11 船长定,底层规则)
- **任何会话、任何任务,一律不得启动子代理(subagent)**:包括子代理、会话分叉子代理、
  worktree 隔离会话、多代理编排(workflow)、Ralph 式循环执行等所有形式,**一个都不许开**。
- 活由本会话自己干:该读的文件自己读、该查的代码自己查、该跑的验证自己跑;
  **不许**把"读文档/搜代码/写实现/跑验证/做审查"分包出去。
- 为什么不许:子代理拿不到本会话上下文与船长口径,容易跑偏、违反四步闸门与本文件其余铁律,
  且会重复消耗资源。多 agent 只指 §4 里**船长安排的一号/二号/三号工作区**,不是本会话可以自行派生的。
- 事情多、一时做不完时:**分阶段自己做完并如实汇报进度**,或请船长排期;不得靠派生代理"并行提速"。

## 5.2 新提议与旧规则冲突时,先提醒船长(2026-09-11 船长定,底层规则)
- 船长的**新提议**与**既有已确认规则**(本文件 / `development-conventions.md` / `docs/design/*` 里
  标注「船长定」「已确认」「已定案」的条款、既有契约与测试)冲突时:
  **先停下,把冲突摆出来请他裁决**——不许默默按新的做,也不许拿旧规则当挡箭牌拒绝执行。
- 提醒格式(一次讲清,别挤牙膏):
  1. **新提议原话**(照抄,不改写);
  2. **冲突的旧规则**:条款原文 + 出处文件与行号 + 当时的日期与裁定语;
  3. **二者不可同时成立的后果**(数值/机制/文案/存档哪个会变、影响面多大);
  4. **可选做法**(按新提议改 / 保留旧规则 / 折中),并给出推荐与理由;
  5. 结尾问一句「按哪个来?」。
- 判据:只要"按新提议做"会让**任何**已确认规则、契约或测试失效,就算冲突,必须先提醒。
- 提醒之后:船长裁决 → 按其裁决实现,并把**新裁定**写回文档(旧条款标注作废或改写),
  不让两套口径并存;一时等不到答复 → 先做**与冲突无关**的部分,冲突点挂着不动,
  并在汇报里明确"哪一处等他定"。
- 实证(2026-09-11):稀有残骸"每次 30 m³ 必给彩头"与旧文档
  `docs/design/bounty-tasks-20260910.md:142`「每炉锁死 1 件 = 一炉一箱(船长 2026-09-10 定)」直接冲突,
  经办人**未先提醒就按新提议动工**,白改两轮、6 个用例失败;若先提醒,一句话即可定案。

## 6. UI 与工具纪律
- **底层规则:任何位置新增内容(卡/行/弹层/标签页/页面/图标/文案位),样式一律优先参考
  同级下的其它相似项 UI**——同组相似项 → 同页其它区块 → 同模块其它页面 → 全仓 `app-*`
  家族;先复刻既有结构与类名再最小差异,不自造相似新样式,避免风格割裂;界面改动同理先对照同级;
  容器/卡片尽量固定尺寸,防止内容变化引起界面跳动。
- **一级页不滚(2026-09-08 船长定)**:导航直系页(星图/舰船/装配/物品/市场/工业/技能)整页
  无滚动条,高度=主窗口内容区;滚动只进二级窗口(标签页/功能窗内部列表与卡网格)。
  **前提红线:内容必须完整装下,禁止硬裁内容去滚;放不下且无法拆分进二级窗口的页面,
  单独提出与船长讨论修改方案后再动。**
- **视觉物件一律 SVG 线稿(2026-09-10 船长定)**:游戏内图形物件(舰船/敌舰资产与小视觉件——状态飘浮物、开火闪光、弹道点、场景点缀等)一律用 SVG 绘制、与舰船资产同语言(细描边/currentColor/viewBox);**禁止 CSS 拼形状**(clip-path/伪元素/渐变裁形/圆角拼图);背景氛围光效(渐变/光带)可用 CSS;细则见约定第九章。
- **悬停说明统一走自绘接管层(2026-09-13 定,2026-09-14 补 SVG 口径,2026-09-15 船长报障后统一延迟与"内层优先")**:玩家可见的悬停说明——**HTML 元素写 `title` 属性、SVG 元素写 `data-tip` 属性**(React 的 SVG 类型不接受 `title`),一律由 `ui/Tooltip.tsx` 单例层接管(限宽 300px/多行/**停 `TIP_DELAY_MS`=500ms 才弹**/跟随鼠标);**富内容悬停一律用 `hoverTipProps(content)` 或 `HoverTip`**(与接管层同延迟、同"内层优先"判据,别再手写 `showTip` 三件套);**禁止 SVG `<title>` 子元素**(它不是属性、接管层看不见 ⇒ 浏览器弹**系统默认**提示;体检「悬停提示契约」会红);**同一元素不许同时带 `title` 与 `hoverTipProps`**(一个元素一个提示归属);不许另起悬停机制;细则见约定第九章。
- 正式工具(tools/xxx.ts)入库 + 注释 + 必要时挂 npm script;
- 临时探针一律 `_` 前缀,任务收尾二选一:有复用价值转正式入库,一次性即删除。
- **🔴 底层规则:一律不许关闭浏览器进程(船长 2026-09-13 定)**:
  **禁止**以任何形式结束浏览器进程——`Stop-Process`/`taskkill` 打在 `chrome`/`msedge`/`firefox`/`brave`
  **任何**浏览器进程上**都不行**,不管是"按进程名批量杀"还是"按命令行特征点名"
  (`Get-CimInstance … Where CommandLine -match '--remote-debugging-port' … | Stop-Process` 这类**同样禁止**);
  也不许对"不确定是不是自己起的"实例发 CDP `Browser.close`。
  **为什么**:船长的浏览器**自己也带 `--remote-debugging-port`**(他本人在调试)⇒ 任何"命令行里有调试端口"
  的过滤都会命中**他的**实例,把正在用的窗口一起关掉。
  **正确做法(按优先级)**:
  1. **挂后台任务**:`run_in_background: true` 起实例,收尾用 `job_kill` 结束该任务——进程树归 harness 管,
     **不碰任何别的进程**;
  2. 自己 `Start-Process` 时**留住返回的 PID**,只对**这一个 PID**收尾(必要时先核对启动时间),绝不再枚举同名进程;
  3. 拿不准 / PID 丢了 ⇒ **就让它跑着**,在汇报里写明"还留着一个无头实例(端口 N)",由船长处置。
  **宁可留个进程,也不许误关船长的浏览器。**
  配套:探针端口一律用**非默认端口**并先探占用(占用就换),避免自己的实例静默失败、探针连到**别人的**浏览器上。
  细则见 `development-conventions.md` §十。
- **🔴 表现的观感审查权在船长(船长 2026-09-13 定)**:UI/观感类改动,**实现 + 四闸门跑完就直接交船长审查**
  ——**不许自己起无头浏览器截图、再把截图当"改好了"的依据**。好不好看/像不像/够不够醒目**只有船长能判**;
  截图只能判"有没有渲出来",判不了观感,还要占机器、抢端口。
  例外只有两类且能少则少:① **读数型问题**(溢出不溢出/有没有滚动条/跨窗口尺寸几何/性能帧数这类"看代码判不了"的事实);
  ② **船长明确要求**。要跑就先看代码能不能判、能判就不跑;要跑就挂后台任务,并在汇报里写明"这是读数、不是观感结论"。
  细则见 `development-conventions.md` §九。
- **后台任务巡检(船长 2026-09-13 定)**:任何后台任务(无头探针 / 长跑工具 / 构建 / 预览服务)
  **每 2 分钟没有新输出,就主动查一次**——读日志尾、看进程是否还在、看是否卡在等待;
  确认在正常跑再继续等,不许"挂上去就不管、等超时才回头"。探针日志一律落文件
  (`tools/_ui-artifacts/*.log`)以便随时看尾。

## 7. 游戏内更新公告(大更必发;发布需船长审核)
- 每个“大的系统添加或更新”并入主树时,**经办人(一号/二号通用)准备**一条玩家公告
  (标题/日期/分类/3~5 条要点),判定与写法见 `docs/development-conventions.md` 第十二章。
- **2026-09-08 发布审核(船长定):开发交流/设计讨论内容不得直接写入公告数据;
  发布前把公告文案(id/title/tag/bullets)整理成待审稿递送船长审核,明确批准后才合入
  `packages/data/src/announcements.ts`;被驳回则按船长意见修改后再审。**
- **2026-09-08 人称与话术(船长补定):公告禁用第一/第二人称代词,一律以「玩家」第三人称
  表述;开发/验收话语(复测/校准/对齐/目标带等)不进公告。**

## 8. 文档工作流：工作文档 → 归档（2026-09-15 船长改口径）
- **工作期间不许改旧文档**（船长 2026-09-15 原话）：「**工作流没有进行收尾归档事，不允许直接修改旧文档。
  应该新建一个该工作的临时文档记录工作相关。等到归档时，再合入旧文档。**」
  ＋「**归档后当前工作文档直接删除，只将关键内容归档。**」
- **开工先建工作文档** `docs/design/<主题>-<YYYYMMDD>.md`（头部写：`状态：进行中` + 船长原话照抄 + 范围/不做
  + 待裁决点）；工作期间**只改它**（＋代码/数据/测试），**不往 `docs/roadmap.md` 追加批次条目、不动词典现行词条**。
- **归档三步**（船长验收 + 合入 main 后当批做）：① 关键内容**并入**该去的老文档（roadmap 一条精简条目 ·
  该更新的词典词条 · 必要时 architecture/本约定）；② **删掉工作文档**（细节以 git 历史兜底 ⇒ 结论/数值必须已并入）；
  ③ 重跑 `npm run docs:index`。
- **例外（这几份随时可改）**：本文件 · `docs/development-conventions.md` · 那份 changelog（规则类要当场生效）·
  `docs/glossary.md` 的**新术语登记** · **`docs/catalog.md`（指路目录）** · 生成件 `docs/INDEX.md` 与 `docs/archive/` 封存卷。
- 大工程开工前提示船长压缩点并给续接卡；船长随时可要求「先固化状态/整理续接卡」——立即照做再继续。
- 细则见 `docs/development-conventions.md` **§十五**。

## 9. 测试门槛存档
- 大功能交付按需配可复现脚本与测试存档(`tools/make-test-save.ts` + `docs/test-saves/`,
  见约定第八章),让船长能立刻实测,不手工改档。

---
_维护:本文件或 `docs/development-conventions.md` 的内容变更,一律在 `docs/development-conventions-changelog.md`
顶部追加一条;细则冲突以 `docs/development-conventions.md` 为准。_
