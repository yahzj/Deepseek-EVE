# 依赖与构建产物
node_modules/
dist/
out/
coverage/

# 日志与临时文件
*.log
*.tmp
.DS_Store
Thumbs.db

# 编辑器
.vscode/
.idea/
.npm-cache/
node_modules/

# 网页版（GitHub Pages）构建产物
dist-web/
apps/desktop/dist-web/

# Electron builder output (green zip + setup exe)
apps/desktop/release/

# 内容工作台中间产物（导出 CSV；源数据以 git 为准）
content-csv/

# 敌人明细导出产物（xlsx + csv；源数据以 git 为准，随时 `npm run foe:export` 重建）
foe-csv/

# 工具区可重建产物（无头核对用的注入档 / 截图 / 读数 JSON；见 tools/README.md §三）
tools/_ui-artifacts/

# 提交消息暂存文件（三号收尾时用，别入库）
_cm.txt
_commitmsg.txt

# 🚫 **船长的真档**（2026-09-22 加：`docs/test-saves/` 里除仓库自带的门槛档 `test-save-*.json` 外，
#   还有船长从游戏里导出的真档 —— 文件名是 `save-<时间戳>.json`（迁移检查跑过会多一层 `.json`），
#   **一律不进库**：真档只读、只在本机用于 save:migrate 取证。此前一次 `git add -A docs` 误把它们
#   带进过提交（已在同一提交里撤回），加这条规则兜底）
docs/test-saves/save-*.json
docs/test-saves/save-*.json.json
