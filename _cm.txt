chore(l10n): 第五批（industry.ts）诚实回退 + 交接卡记录范围与三个坑

## 本轮发生了什么
`industry.ts` 开工后发现比预估大（错误串**两种引号形态**，我第一次只扫了单引号 ⇒ 漏 20+ 条），
做到一半上下文告急 ⇒ **按纪律不留半成品**：`git checkout` 整文件回退，表里多造的 44 条一并清掉。
工作区回到第四批的干净状态（typecheck 0 错 · l10n:check 通过 · 闸门全绿）。

## 交接卡已记下「下次从这里继续」（§6.1.1 内）
1. **两种引号都要扫**：`git grep -n 'error: \`' -- packages/core/src/industry.ts`（反引号那批我第一次漏了）；
2. 「需停靠空间站…」三条（精炼炉 / 货柜拆解 / 残骸回收炉）已定为共用条目 `core.state.010`，
   句式 `{p1}随协会基地网络运转：需停靠空间站（母港或已建成副站）才能启动（AI 核心驱动不受此限）。`，
   `{p1}` = `精炼炉` / `精炼炉的「货柜拆解」` / `残骸回收炉`（⚠ 占位符必须与句式一致，否则占位符契约报红）；
3. 「采矿作业中：先停止开采。」已出现在 3 处 ⇒ 用共用 `core.state.013`（打捞/远征/巡逻/返航同理）；
4. **多段拼接**日志（精炼炉/回收炉停炉三条、AI 战报）**先别接**，等「多段文案」形状统一。

## 进度（不变）
`mining` ✅ · `salvaging` ✅ · `shipyard` ✅ · `ai` ✅（33 条 + 2 条多段待方案）⇒ 剩 ≈25 文件 / ≈450 处，
下一个就是 `industry.ts`（材料已备齐，上手即改）。