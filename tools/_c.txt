feat(industry): 工业界面优化批（读数合并 · 缺料徽标 · 组装机↔书架双向跳转）

船长令：「按你推荐来」。前一批已交付「仅看可造」筛选；本批做剩下三条。

一、删1：文案 id `ui.IndustryPage.118`（「前往章鱼人兑换」）
- 已由 ui.IndustryPage.126「前往声望商店兑换」取代，全仓源码零引用（实测：只有表里那一行）
- 删除后 l10n:check 通过（无死引用）

二、优1：卡头两条读数合并成一条（原本要连读两遍才知道哪个是筛后的）
- 新增 filterActive 判据（搜索 / 学会 / 子类 / 图纸类型 / 仅看可造；**门类不算**——那是分区不是收窄）
- 筛选生效 ⇒ 只报「当前 N 张」；没筛选 ⇒ 只报分项汇总（制造线/装备/零件/消耗品/已学会）

三、优2：组装机卡片加缺料徽标
- 原先"造不了"只体现在底部按钮置灰，171 张卡的列表里扫不出来
- 现在卡头补一枚「缺料 N 项」（悬停给完整清单），走既有 .app-chip，不新增行
- 口径与 app-bp-mat.is-short 一致：只在**没在跑**的时候标

四、优3：组装机 → 蓝图书架的反向入口（书架 → 组装机早就有了）
- 组装机卡「书架有书未学」那一支补次级按钮「去书架」（主行动仍是「学习」）
- 工业页新增 bookFocus 状态 ＋ 与 craftFocus **同一个**滚动/自选 effect（3.5 秒自清）
- 书架卡接 focusBookId ⇒ 高亮 .app-belt-card.is-goto ＋ 居中滚动（与正向同一套）

五、如实说明：删3 撤回、优4 延期
- 删3（那句旧注释）读下来是**有用的防踩坑说明**（就是我自己踩过的 id 误用坑）⇒ 不删，保留
- 优4（精炼炉批量加开）要动精炼炉开工链路 ⇒ 留到下一轮单独做、单独验，本批不硬塞

验证：typecheck 四包 0 错 · core 234 文件 / 2568 用例全绿 · content:check ✅ ·
l10n:check ✅（无死引用）· ui:rot-check ✅ · ui:layout-css:check ✅ ·
桌面 build ✅（assets/index-B_B1njEF.js）