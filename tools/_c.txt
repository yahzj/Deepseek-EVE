fix(plug): 插件图纸被 unreleased 闸门整批滤掉（组装机那一档空的真根因）

船长报障（照抄）：「组装机处也没有舰船插件的的门类筛选，反而是多出一个错误的图纸筛选。」

真根因（两条，都得修）：
1. **`unreleased: true` 是施工期闸门**，而 `engine.blueprints` 用 `itemReleased()` 过滤
   （engine.ts:581/827）⇒ 12 张插件图纸**根本进不了组装机的卡片流** ⇒ 那一档必然空。
   我把它误当成"市场不可见"用了（市场不可见由市场行的 unreleased 负责，与蓝图无关）。
   ⇒ 12 张图去掉 unreleased（兑换来的书就该能在组装机造）；市场占位行照旧 unreleased。
   实测：BLUEPRINTS 171 张全部进卡片流，其中插件图 **12 张**，kindLabel=舰船插件、subKey 空。
2. 一级门类判定 `inTab` 原是按中文 key 的字符串三元链、**没有 plug 分支** ⇒ 上一批已改成
   与 bpFilterKeysOf 同源的 cardTabOf（本条修复的前提）；本轮补上 pushEquip 的 kindLabel。

验证：typecheck 四包 0 错 · core 234 文件 / 2566 用例全绿 · content:check ✅ ·
l10n:check ✅ · ui:rot-check ✅ · 桌面 build ✅（assets/index-Bdra3Kq7.js 17:29:08；
产物含 bp-plug-shield-plate 4 处 · 舰船插件 50 处 · plug-exchange 4 处）