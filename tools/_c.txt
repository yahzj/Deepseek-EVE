fix(comms): 首匣那封信的「前往」点不动 —— commsInbox 漏把 hint.action 提到顶层

船长报障（照抄）：「通讯也是，前往章鱼人兑换，而且点击后还不是跳转到声望商店。」

真根因（读数据查出来的，不是猜）：
- 界面侧 CommsEave 的「前往」**只认顶层 entry.action**：有它走 onAction（开窗口），
  没有就退回 onGoto(entry.hint.page)（只切页）
- `commsInbox` 的**表消息**分支只带了 `hint`，**没把 `hint.action` 提到顶层**
  （实例通讯那条分支一直是对的）⇒ 首匣那封信永远只会切到工业页
- 实测（用真 ctx 跑一遍）：entry.action = undefined；修后 = "plug-exchange"

修：
- packages/core/src/comms.ts：表消息分支补 `...(msg.hint?.action !== undefined ? { action } : {})`
- 通讯提示文案改「前往声望商店兑换」（船长同日改口），id = ui.Expedition.442
- ui/commsText.ts 的 COMMS_HINT_ID 登记这句 ⇒ 英文侧不露中文
- 组装机那张卡的按钮改「前往声望商店兑换」（新 id ui.IndustryPage.126，与通讯同口径）

回归护栏（新用例 2 条，反向验证过）：
- 「表消息：hint.action 提到顶层 action」——摘掉补丁即红，装回即绿
- 对照条：没写 action 的信仍走切页分支（不误伤别的通讯）

验证：typecheck 四包 0 错 · core 234 文件 / 2568 用例全绿 · content:check ✅ ·
l10n:check ✅ · ui:rot-check ✅ · 桌面 build ✅（assets/index-DxCiB28n.js）