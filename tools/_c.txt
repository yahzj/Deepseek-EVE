feat(industry): 组装机加「仅看可造」筛选（船长令：优化工业界面）

船长令：「你顺便用技能对整个工业界面进行下优化。看下是否有优化空间。」

一、先量读数（结论：性能与已做的信息架构**没有空间**）
- 引擎侧：仓内自带探针实测（tools/industry-perf-probe.ts）——渲染每帧 0.01~0.03 ms、
  合计 < 0.1 ms；切页卡顿已在 2026-09-22 用 content-visibility 修过（138→59 ms）
- 「筛选后补读数 当前 N 张」已有（ui.Industry.127/108）
- 筛选为空时的空态已有（ui.Industry.093）
- 「学会」与「门类」已同排一行（2026-09-19 船长令）
- 搜索框在头部、DOM 上排在筛选行**之前**（tab 序也靠前）
⇒ 上面几条**不重复造**，没有改动

二、真缺的一条：**「仅看可造」**（已加）
- 组装机是唯一长列表（后期 171 张图纸），玩家最常问的不是"我有哪些图纸"而是
  "**现在这批料能造什么**"；原先 canStartNow() 只喂卡片按钮的可用态，**做成筛选项的入口一个都没有**
- 新增并列布尔开关（与「学会」同排、贴在门类之后）：`可造：[仅看可造]`
  · 判据 = 卡片按钮**同一把尺**（同一个 canStartNow）⇒ 筛出来的就是真能点的
  · 关着不过滤（默认）；开着收窄，且**算进"当前 N 张"读数**
  · 落款复用既有胶囊样式（app-tasktab + is-active），不新增行高（.app-fleet-row 自带 flex-wrap）
- 真档读数（船长当前档，实测）：图纸 171 · 已学会 75 · **仅看可造 115（67.3%）**
  ⇒ 开着能一次性收掉 56 张当前造不了的，不用一张张往下找

三、skill 依据（ui-ux-pro-max）
- 查了 "filter overload progressive disclosure" 与 "chip collection reflow"：
  后者的 High 级要求 = **筛选胶囊必须能换行、不许挤成一行被裁**——
  本页 .app-fleet-row 自带 flex-wrap，新加的那枚胶囊沿用同一容器 ⇒ 天然满足；
- 未采用其移动端条目（本项目是桌面 Electron）；
- 观感审查权在船长：本版未自行截图。

验证：typecheck 四包 0 错 · core 234 文件 / 2568 用例全绿 · content:check ✅ ·
l10n:check ✅ · ui:rot-check ✅ · ui:layout-css:check ✅ ·
桌面 build ✅（assets/index-BQd3XPfJ.js）