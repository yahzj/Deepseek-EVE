l10n(ui): 虫洞面板整批接线（51 条 JSX 文本断片 → 0），表 +96 条

界面批第一块（交接卡 §3② 的重灾区之一）。Wormhole.tsx 原读数 51 条未译，全部是
**被 {} 表达式切碎的 JSX 文本断片**（"第"/"层"、"格"、"堆"这类）——正是工具不碰的那一类，
按卡的做法「读源码 → 整段替换成 tr(id, {参数}) → 补表项」。

做法要点：
- 断片合并回整句再翻：`第 {n} 层`、`共 {n} · 抛弃 {x}`、`· 第 {n} 列`、`（去过…）`、
  自动探索说明那一长句（含"但"连接词也做成条目，避免英文语序错位）；
- 可复用的同中文条目一律复用（货柜 = ui.MarketPage.006 · 件 = ui.MarketPage.117 等），不新造重条；
- 纯标点（"。"、"」"）也登记条目——中文全角标点在英文侧要换成 "." 与引号；
- 错误兜底串（无法跃入/派队失败/无法前往/扫描/激活/深入/撤离/拾取/整理/抛弃/换不了位置…）
  一并接线：这些是 `r.error ?? '中文'` 的**界面兜底**，与 core 的 error 全量翻译（待船长定口径）分开处理。

行为变化：仅文案取用路径改为 id 引用；渲染文本与改动前逐字一致（中文侧同字，英文侧首次可显示）。
不改机制、不改数值。

验证（实测）：typecheck 四包 0 错 · l10n:check 通过（死引用 0 · 旧写法 0 · 占位符对齐 · 无残留中日韩）·
content:check 全绿 · ui:rot-check 全绿 · core 用例全绿 · build 成功 · docs:index 最新。
表内 Wormhole 段新增 96 条并做了一次去重自检（重复键 0 · BOM 无 · 乱码 0）。

已知取舍：同批清掉了 5 条确无引用的旧条目（原先的"未接线"读数从 15 降到 12）；
其余未接线条目留待 ⑤ 收尾核对一起处置。