# 后续工作流备忘（Roadmap / Backlog）

> **2026-09-15 改版**：本文件只留三块 —— **① 待办活面**（置顶单 / 待办段 / 交接开放项）· **② 最近 20 条批次条目（滚动窗口）** · **③ 封存卷索引**。
> 更早的批次条目已**原文封存**进 `docs/archive/roadmap-YYYY-MM-DD.md`（一字未改，含船长原话照抄）——查老决策先看索引表、再 grep 对应卷。
> 封存动作 = `npm run docs:seal`（滚动窗口之外自动进卷，幂等；`--dry-run` 先看计划）· 封存区规则见 `docs/archive/README.md` · 全仓文档总索引 = `docs/INDEX.md`。

﻿ 后续工作流备忘（Roadmap / Backlog）

---

## 置顶：船长测试反馈改动单（2026-09-04 加入，按列序逐条推进）
**执行规则**：批次 A = 无歧义可直接实现；批次 B = 需先给设计总结等确认（大系统/结构级/规则数值）。
- **T1~T11 全部 [✓] 办结**（2026-09-15 逐条核对后原文移出）⇒ 全文见 `docs/archive/roadmap-todo-done-20260915.md`；本节留段头，新的船长反馈单另起一段。

## 未完成功能清单（2026-09-20 建 · 船长令「标注未完成」）

> **这份清单是什么**：登记**已开工但未完成**或**只有预留接口**的功能。进本清单的功能，
> 其文案**不计入本地化工作排队**（规则见 `docs/development-conventions.md` §十一之二）——
> 改动处统一留记号 **`⟪未完成 YYYY-MM-DD⟫`**，`grep -rn '⟪未完成'` 一次列全。
> **收口**：功能完成并合入 main 时，从本清单删条 ＋ 删掉代码/文档里的记号。

| 功能 | 状态 | 挂起原因 | 恢复入口 |
| --- | --- | --- | --- |
| （暂无） | — | 成就系统两批均已于 2026-09-20 完成并合入 main（见下方 D 段条目） | — |

## 2026-09-20 一号交接开放项（原工作文档里的「待你定」· **✅ 已全部结清 2026-09-22**）

> ✅ **本节已销账（船长 2026-09-22：「全部可以消档，都已经完成。」＋同日逐条裁定「1甲 2甲 3甲 4甲 5甲 6甲 7不动 8不动 9保持 10不动 11不动 12不动 13不动」）**：
> **1 / 4 / 5 三条已落码**（详见 `docs/roadmap.md` 最近批次的 2026-09-22 条目）；**其余 10 条按船长裁定结清**（不动 / 保持）。
> 下列原文只作过程留档，**不再有未决项**。

> 2026-09-20 批的 22 份工作文档已按 §8 归档（关键内容并入本文件与 `docs/glossary.md`）。
> 下列各条**当时上报但未获答复**，原文要点留在这里；你要动哪条，点名即可（多数是一句话改一处）。

- **「寻找人类」发布的日志口吻**：`core.onboarding.001` 仍写「先在这座城市活下去，再慢慢打听」——
  那是按"序章刚结束"写的；发布点已挪到「第一次」顺序段（前 11 条）完成之后。换不换口吻（要动中英两列）？
- **老公告追改**：`packages/data/src/announcements.ts` 里那条历史公告写着「长期目标「寻找人类」已发布」
  （属历史记录，未追改）；要不要另发一条说明发布时机的公告（公告需船长审核后才入库）？
- **「重要任务」页未发布态**：现在什么都不显示（只有当前那条「第一次」卡与副站卡）。要不要加一句占位提示？
- **老档补采集器**：2026-09-20 奖励前移后，**已完成「第一次扫描」但还没做「第一次采集原矿」**的老档
  拿不到采集器 MK1（市场 31,200 ISK 有售 / `bp-miner-1` 可造）——要不要给这类档补发一次？
- **AI 打捞被拒文案**：`core.ai.008`「该副船没有打捞器…」要不要也补一句"去「装配」页"？
  （主控那条 `core.salvaging.003` 已改，副船这条未改。）
- **进洞 vs 长途运输**：现在进洞**自动停运** ＋ 两段确认；要不要改成像其它主控活动那样**直接拒绝**（先停止再进）？
- **「第一次采集原矿」的跳转按钮**：仍指「星图 · 矿带开采」；任务正文已引导"先去舰船页换采矿艇"
  ⇒ 要不要把按钮改成「舰船」优先？
- **虫洞深层盘重建**：`wh-grid-deep-save` 那批修好了"读档丢盘"，但**已经中招的那一层不会自动回来**。
  可选路径 = 读档时若 `run.grid` 缺失而 `run.seed` 在（盘面按 `(seed, depth)` 确定性生成）⇒ 用
  `wormholeMakeGrid` 重建同盘；代价 = 该层已探索进度重置、且当时 `scanBonus` 未随档 ⇒ 可能与原盘差一圈。
- **谜质科技 T4「工业多核调度」定价**：现值 30/60/90/120/150 谜质（合计 450 谜质 ＋ 18 亿信用点）是本树最贵的一件。
  三个可选：① 降步长到 20/30/40/50/60（200 谜质）② 改 3 级（与"最高 5 级"不符，需改口径）③ 保持现值。
- **`tools/` 纳入 typecheck**：存量清单已出（134 行 / 19 文件，其中 ~40 条真错、~46 条 strict 噪音）。
  三条路：全纳入（先清存量）／白名单纳入（常跑的十几个）／只加运行期护栏。要不要开工？
- **死工具 `tools/content-validate.ts`**：import 了不存在的 `ITEM_KIND_ORDER`，且**没挂任何 npm script、全仓无人引用**
  （已被 `content-check` 取代）⇒ 建议删除。删不删？
- **里程碑"彻底追溯"**：`aiCoreKinds`（某类核心被花光后现算会少）与 `whBossClears`（跨趟累计无留痕）
  目前只做到**下界**追溯；要精确就得**新加存档字段**（属改存档结构）。
- **成就那条"里程碑"的指代**：船长令「里程碑都加入追溯检查」已按**18 枚里程碑成就的六个计数键**落地；
  若本意是**里程碑任务链（次数链）**，说一句我按同一张推导表接上去（可推导的只有 `scan` / `skills` 两条）。
- **待迎战期间"再捞一次"被禁**（`wormholeActivateAt` ＋ 按钮双向）：若想保留"先把剩下几堆捞完再打"的自由，一句话放开。
- **链条耗时比 4:2:1 方案**（见下方同日条目）：**未实行**——方案全文要点已并入该条；要落就说一句"落 4:2:1（锚采矿）"。

## A. 体验走查 + 打磨（建议优先，收口"内容感"）
- [ ] A1 船长通读悬赏文案与界面观感（2026-09-05 注：需船长走查后按条关闭；2026-09-04 首轮已走查并清理过一批）
- [ ] A2 信息密度检查：星图（安全色 + ⚔悬赏徽标 + ′标称 + 名称是否拥挤）、悬赏列表排序/卡片密度、矿带卡布局细节
- [ ] A3 定版存档策略（2026-09-05 注：需船长确认正式档基线后关闭）

## B. 玩法闭环延伸（需确认流程的大方向）
- **本段 4 项全部 [✓] 办结**（2026-09-15 核对后原文移出）⇒ 见 `docs/archive/roadmap-todo-done-20260915.md`。

## C. 战斗与舰船系统余项
- [ ] **C3 V18 立项：EVE 式高/中/低槽重组**（2026-09 船长拍板"V17 收尾、高低中槽立项 V18"；
      **2026-09-04 交接：实施由大鲸鱼二号负责**，一号不再触碰；**2026-09-04 设计总结已确认**
      （docs/design/v18-slots.md：复数安装/数量制/槽位映射/Q4 布局草案 19 船表 + 高槽无人机
      家族 6 件——甲板扩展/战术导控，装备 48→54）——**V18A 实施已完成（2026-09-04，core 301 tests）**；
      **C4 校准轮（多炮/多矿枪/无人机件复核）与本里程碑联动，另行收尾**）：
      船体槽位布局按 role 族差异化（武装多高槽/装甲多低槽等）+ 模块归属高/中/低 +
      槽位数量/布局制（**炮尺寸制已随 V18 口径取消移除，无尺寸位**——变更记录见下）；
      fitted 结构迁移一次做干净（存档 17→18；旧六槽原位映射 high=[炮,矿] mid=[盾,推] low=[甲,货]）；
      V17 参数（乘入抗性/CPU 档位/推进代价）与槽制正交、届时零返工
      （见 docs/design/v17-modules.md §预留；v10b-combat-data.md 原"留战斗阶段"的旧账在本里程碑清偿；
      2026-09 设计问答已收：Q1 复数安装 / Q2-final 数量制不加尺寸位 / Q3 槽位映射 / Q4 布局草案）
- [ ] **C6 无人机 vs 炮位 DPS 比例评估**（2026-09-04 船长确认 V18 时追加：**延后**——武器体系
      全面后（V18B 炮族差异化 + 导弹架之后）再评估无人机流与炮击流的高槽伤害比例；
      无人机件数值（+15/35/70 m³、+12/25/40%）为暂定初值，届时可调）
      **2026-09-10 评估完成（二号；`docs/design/c6-drone-vs-gun-20260910.md` + 正式工具
      `tools/drone-vs-gun.ts`，`npm run battle:drone-vs-gun`）**：王鲭级满配同船对比——
      名义 DPS 无人机流 = 炮流 **81%**（每高槽名义反而 121%），但有效 DPS（命中修正）**48%**、
      每高槽有效 **73%**；成因 = ①命中链缺口（无人机固定命中 0.6，火控/索敌对无人机无效；
      炮台 ≈0.98）②支援链缺口（稳定器/射速/索敌只作用炮台，炮流有效 +33%）③CPU 双计
      （装置 + 每架，D3 占 310/320）④高槽不产生本体火力。实战：威胁 96 穹顶守卫
      纯炮 100%/76s/58% vs 无人机 D3 0%/74s（被击毁）；加目标锁定 MK3 后 D3 升到 60%。
      情景推演：单靠"导控 +60% + 无人机 CPU×0.65"有效仅到 68%、硬卡 40%——
      **须动命中链/支援链才能拉平**。
      **2026-09-10 第一轮调整已实施（船长拍板）**：无人机命中/衰减下放机型本体
      （`ItemDef.hitRate/falloff`）——**蜂鸟/赤鸢/猎鹰：命中 75% 且不随距离衰减**（falloff 1）；
      **雷鸥哨戒：命中 110%（用意 = 抵掉部分衰减）+ 保留正常衰减**（falloff 0.35）；
      combat 读本体（旧档兜底 0.6/0.35）、命中公式零改动；UI 无人机详情/手册新增「放飞命中」行；
      新增 `tests/drone-hit.test.ts`（6 项）；glossary 增词条「无人机命中与衰减」。
      **改后实测（王鲭级）**：有效 DPS@2km 48% → **70%**（D3 184 vs 炮流 263）、
      每高槽有效 −27% → **+5%**；实战 D3 反超炮流（穹顶 **100%/65s** vs 76s、坟场 27s vs 45s、
      噬口 41s vs 50s），但无人流行残血偏低（散火击杀节奏慢；装目标锁定 MK3 后 35%→57%）。
      **待船长判断是否回调**（旋钮：导控 +40%→+25% / 三型命中 0.75→0.70 / 无人机每架 CPU 上调）。
      **2026-09-10 船长裁定：先保持现状**（回调未实施，数值维持本行口径），由船长实机实测后再定。
      **2026-09-10 船长追加：无人机专用舰高槽 −2**（梭鱼级 5→**3**、王鲭级 6→**4**；其余带机巢
      舰船不动）——`ships.ts` 两行 + 旧档由 repair 链尾件退库（不丢装备、清单保留）+ 新增
      `tests/drone-ship-slots.test.ts` 钉布局与旧档兼容；校准行 D1/D2 改三槽、新增 D2b
      （槽位吃紧后单件导控升 MK3）、D3 四槽恰好占满；工具与测试存档同步。
      **复跑结论（重要）**：削槽对**相对关系是净利好无人机流**——王鲭炮流 6 门→4 门
      名义 272→**183**（−33%），而无人机流 D3 本体只占 4 槽、名义 220 **不变**；于是
      无人机流/炮流名义比 81%→**120%**、有效 DPS@2km 比 70%→**105%**；
      实战王鲭 4 槽对比：无人机 D3 穹顶 **65s** vs 纯炮 104s、坟场 27s vs 68s、噬口 41s vs 76s。
      梭鱼 3 槽：无人机 D2b 有效 107 vs 纯炮 3×MK3 120，实战仍更快（穹顶 85s vs 157s，但 0% 胜）。
      原因 = 无人机流火力来自机群（受舱容/CPU 约束，不吃槽位），炮流火力随槽位线性下降。
      **若目标是削弱无人机流，杠杆是 CPU / 舱容 / 战术导控，而非高槽数量**（详见设计稿 §九）。
- [x] **无人机四型定位 + 「无人机可被击落」**（2026-09-10 船长拍板，**本批已完成**）：
      **① 四型定位契约已确立（已实施）**——侦察机闪避最高装甲最薄 / 战斗机全属性平均 /
      攻坚机血量最厚·单发高·闪避最低 / 哨戒机射程最远·单发最高（狙击）·血量与侦察机相仿；
      现有四型按此调整（**蜂鸟闪避不变 0.45**；赤鸢 0.40→**0.25**、血 34→40；猎鹰 0.32→**0.10**、
      血 66→**97**、抗性装甲向；雷鸥 0.25→**0.18**、血 105→**23**、抗性削到单条）。
      契约**可执行化**：新增 `data/droneRoles.ts`（`DRONE_ROLE_SPECS` 四档区间 + 逐机/跨类阶梯校验），
      `content:check` 与 `tests/drone-roles.test.ts`（7 项）双重守卫——**新无人机越档即被拦下**；
      glossary 增词条「无人机四型定位」；设计稿 `docs/design/drone-roles-20260910.md`。
      注：`DroneDefense` 目前**不进战斗**（只有详情/图鉴/手册显示），故本批**不改胜率、校准矩阵不变**。
      **②「无人机可被击落」已实施（船长选乙：永久损失/耗材流）**：敌方**近防炮**——**威胁 ≥ 60**
      的敌舰才装，每舰**每 0.5 秒判定一次**，随机挑一架**正在攻击的放飞无人机**（**哨戒机不被打**），
      按 `pdAcc − 机型闪避` 掷命中、命中按 `pdDmg` 走机型三层抗性，打空即击落并停火；
      **不看距离**（"毕竟你要飞过去"）、**不参与**敌舰对玩家的常规攻击、单场最多击落 50%；
      **战后从无人机舱清单永久扣除**（主控远征胜/败/撤退 + 遭遇战 + AI 副船全接线），
      事件日志 + 在线 toast 提示，重复清剿在本场战损过半时自动停环提示补货；
      手册/物品说明/装配页删除"不消耗、不被击落"并改为近防炮口径；测试 `tests/drone-loss.test.ts`（5 项）；
      新增调参工具 `npm run battle:pd-tune`（支持 `--acc/--dmg/--cap/--period` 传参试档）、
      `drone-vs-gun` 表 3 增战损列。
      **实测（满技能 5 种子，王鲭 D3 满编；合法且符合流派的配装）**：软卡交火短（27~41s）战损近 0；
      硬卡穹顶（96）约 **3.2~3.6 架且全是攻坚机**（哨戒机免疫）；硬卡仍 **100% 胜**——
      因编队火力核心（6 架雷鸥）不受近防炮威胁，压力在"打得更久 + 要补货"。
      战损估值占该卡奖励约 1.1~1.2%。
      **数值已确认（2026-09-10 船长）**：「如果没有特意说明，默认按 0.5/5 来算」——
      未特意说明的敌舰一律 **0.5 秒/舰判定 · 命中 0.5 · 每发伤害 5**（逐舰独立、上限 50% 保留）。
      **默认档实测（王鲭 D3 满编）**：噬口(80)/坟场(88) **0 战损**；穹顶(96) **2.2 架（全为攻坚机）**、
      战损估值 1.1 万 ISK ≈ 该卡奖励 **0.7%**；三卡仍 100% 胜——因哨戒机免疫且编队火力核心是雷鸥，
      压力落在"打得更久 + 攻坚机要补货"。调参参照档（0.45/7 → 3.6 架；0.30/12 → 3.2 架；
      0.55/3 → 0 架）见设计稿 §七。
      **2026-09-10 船长追加**：**非哨戒无人机全部被摧毁后，近防炮转而攻击哨戒无人机**——
      哨戒机的"豁免"变成有条件的（靠同队其它机型当肉盾）；已实施 + 测试覆盖
      （`tests/drone-loss.test.ts` 新增"非哨戒机全灭后转火"用例），重档实测可见效果
      （acc 0.5 / dmg 10：噬口与穹顶在攻坚机全灭后哨戒机开始被击落）。
      **2026-09-10 船长追加（无人机舰专属加成）**：无人机舰的 `powerBonus` 只喂炮台、对机群无效
      （王鲭 0.6 与长尾鲨同级却吃不到机群）→ **削火力 + 加专属**：新增船体字段
      `ShipDef.droneDmgBonus`（只乘入放飞无人机单发，与战术导控/作战学乘算）；**王鲭 powerBonus
      0.6→0.3 且 +12%**、**梭鱼 0.45→0.25 且 +8%**。实测：王鲭 D3 名义 220→**245**、
      同船炮流 183→**149**（−19%，实战 104s→120s）；梭鱼 D2b 132→142、炮流 126→109；
      近防炮战损 2.2→**1.4 架**（机群更强→交火更短）。测试 `tests/drone-ship-bonus.test.ts`。
      **2026-09-10 船长追加（机舱联动）**：**卸下「无人机甲板扩展」后，超出机舱的无人机也要卸下**——
      新增 `trimDroneLoadToBay()`（按清单逆序卸下、退回仓库、写日志 + 一次性提示），
      接在 `unfitAt`（UI 传 ctx）与 `repairDeprecatedModules`（旧档槽位对齐裁掉甲板件）之后；
      测试 `tests/drone-bay-trim.test.ts`（5 项）；手册「装配」条目补口径。
      **2026-09-10 船长追加（损坏与回收 + 无人机线技能）**：①**取消单场 50% 击落上限**——
      战斗内机群可 **100% 损坏**；②**战后回收**：损坏机体按 `10% + 8%/级（回收学）`、满级 **50%**
      **四舍五入**找回并**回无人机舱清单**，未回收部分才永久损失（日志/toast 写明 损坏/回收/净损）；
      ③新增**五条无人机线技能**（战斗组）：无人机打击学 rank4 单发 +4%/级、无人机耐久学 rank2
      三层血 +4%/级（满级 **+20%**）、**无人机强化学 rank4 三层血再 +6%/级（满级再 +30%，与耐久学乘算，双满 ×1.56）**、
      无人机回收学 rank3 回收率 +8%/级、无人机规避学 rank4 闪避 +2%/级（满级 +10%）；
      数值常量集中在 `combat.ts` 的 `DRONE_SKILL`。测试 `tests/drone-skills.test.ts`（6 项）+
      `drone-loss.test.ts` 改口径；词典增词条「无人机线技能」。**实测**：默认近防炮档不变
      （软卡 0、穹顶 1.4 架）；重档（0.5/10、0.5/20）可见**整队 10/10 损坏**且胜率掉到 0~40%——
      取消上限后的真实风险与"耐久/强化/回收"技能的价值都在这里体现。
      **2026-09-10 船长二次修订**：耐久学**拆成两条**（rank2 基础档 + rank4 强化学进阶档，
      **乘算**叠加）；**其中一条满级改为 20%**——现行 = **rank2 耐久学 +20%（+4%/级）/
      rank4 强化学 +30%（+6%/级），双满 ×1.56**（越难练收益越高）；**规避学满级由 +20% 降为 +10%**（+2%/级）。
      **2026-09-10 玩家反馈修复（悬赏奖金口径）**：反馈"卡片赏金与实际到账不一样"——根因
      ① 结算带 ±15% 浮动（`rewardJitter`）而卡片展示名义值；② **赏金任务（窝点）卡漏乘
      "赏金猎手学"系数**。船长按甲案拍板：**取消奖金浮动**（rewardJitter 0.15→0）——展示=到账，
      唯一随机奖励 = 情报彩蛋 +10%（触发即写日志）；并修掉赏金任务卡展示漏项。
      测试 `tests/bounty-reward-display.test.ts`（4 项）；词典增词条「悬赏奖金口径（展示=到账）」。
      **口径修正（自检）**：早期工具给王鲭配了 3 中+3 低支援件（超出 2/2 布局）导致结论失真，
      已修正并统一为合法配装。
      表现层（击落演出/战报行）待三号对接，引擎侧字段契约见设计稿 §八。
- [ ] **C5 炮族差异化 + 导弹架**（2026-09 船长审核批注：同 MK 三系炮"只差弹药、无性格差异"——暂定稿可玩，**与导弹版统一做**；
      **2026-09-04 船长重定向：按伤害类型设计真武器形态（质量炮=动能 / 导弹=爆炸 / 激光=能量），
      旧三系同参炮 = 临时填充数据，逐批替换——V18B-1 导弹架 ✅ 已完成**（docs/design/v18b-missiles.md：
      追踪命中不随距离衰减的远程爆破 + 近盲安全射距；原高爆炮 1:1 迁移；爆破导弹弹药归位）；
      **V18B-2 激光炮 ✅ 已完成**（docs/design/v18b2-lasers.md：必中光束 + 距离衰减作用于威力
      （幅度减半 50%）+ 消耗能量弹药；能量炮/异星原型 1:1 迁移激光；per-gun 多键预载完成
      ——混装异型不再饿死；core 345 tests）：
      **V18B-3 ✅（2026-09-05 船长确认完成）** = 动能炮 → 质量炮形态（届时确认是否改名），并联动 V18 槽制
      （炮尺寸制已随口径取消移除，见变更记录）；
      v17b-weapons.md §已知限制已记录
- [ ] **C7 每族一艘新敌舰 ＋ 层 6「深渊档」深层卡（船长 2026-09-17「**先挂起吧**」· 一号 · **建议稿已随本条归档**）**——起因 = 船长「**打算给每个种族添加一个新敌舰，并添加一张新的深层卡（出现层数6），你有什么推荐（不用特意补齐全舰种，按照族设定推荐）**」。
      **①建议（六族各一艘 · 全部零引擎改动、只复用既有字段）**：A **劫掠跳帮舰 T2**（捕获网 ＋ 冲锋双持，把"钉住"从血 182 的电子舰搬到真突击位）· B **拾荒拆解舰 T2**（`repairPct 0.3`＋低速厚甲，**需加进 `repairPct` 白名单**）· C **螯颚撕咬者 T2**（射程 1~1,800m ＋ `blindDmgMul: 1` ＋ 2.0× 速度 ＋ 冲锋，反"距离门"）· D **静滞方尖舰 T4**（首选；`web` 字段参数改写＝周期性压制最近我方：机动 ×＋射程 −2,000）或 **陵寝主宰舰 T5**（撞"旗舰留给 E 族核心舱段"那句既有口径 ⇒ **首选 T4**，要开旗舰位另说）· E **无人机母舱段 T4**（纯机群 ＋ 警戒机 ×10 ＋ 后备机库 ＋ 机群射程 ×4 ＋ **速度 0**，必须带防空贴上去拆）· G **孵巢舰 T3**（蜂群 ×6 ＋ 鱼雷、无后备，守 G 族口径）；**F 族（遭遇巡逻）跳过**——数据里 0 舰级、只是"遭遇战模板族"、没有种族设定。
      **②层 6 三读法（推荐甲）**：**甲** 新增**「深渊档」（层 6 起）每族一张（共 5 张）**——与现有结构对称，上面 5 艘新舰正好做各自深渊卡主体（否则新舰无出场处；代价 = core 层档池加一条规则 ＋ 契约/用例）· **乙** 只加一张**层末守卫专用卡**（推荐 G 族「深渊封锁线」，代价最小）· **丙** 跨族动态编成（卡面是静态引用，要新机制 ⇒ 不推荐）。**层 6 的收益必须一起定**（否则"更深"只有难度）：推荐 **(b) 新增"深渊货柜"**（最高档一次性图纸 ＋ 原型件）＋ **(c) 层 6 起上调深档卡/货柜权重** 的轻量版；纯难度 (a) 不推荐。
      **③待船长裁定 5 件**：六舰收不收（可逐条改）· D 要不要开 T5 旗舰位 · 层 6 走甲还是乙 · 层 6 收益选 (a)/(b)/(c) · F 族跳过还是先给一句设定（它是谁 / 什么美学 / 什么威胁级）。
      **④节奏（定了之后）**：① 六艘新舰（纯数据 ＋ 契约/用例同步，`npm run foe:export` 的敌舰表自动跟随）→ ② 深渊档 ＋ 层 6 卡（含"深渊货柜"若要做）→ ③ 数值标定（跑 `npm run wormhole:econ` / `bounty:stats`，把逐层威胁 / 总血 / 总 DPS 读数摆给船长核）。
      **⑤全文**：原稿按 §8 归档删除，需要原文看 `git show f88ac64a:docs/design/foe-new-ships-deep-cards-20260917.md`（含六族现役 **25 舰级**的档位覆盖与**契约硬上限**表：A 只 1~3 · B 不配 T3 及以上 · C ≤4 且 T4 须登记进 `ALIEN_BEAST_SHIP_IDS` · G ≤4 · E 持 T5；以及"层档池 / 半径封顶 R=4 / 深档图纸货柜 = 层 5+ / 威胁 ×1.10"这些层档现状）。

## D. 经济/数值精调
- [ ] D1 balance（2026-09-05 甲案已做：体检工具扩"总资产口径"——钱包+舰队/已装模块+仓库/书架按市场常驻 base 折算，重跑 24h 三策略：钱包/总资产 A 934k/2067k、B 410k/1589k、C 312k/1499k；B/C 钱包低估约 1.7~2.2×，总资产差收窄至 A 的 73~77%，缺口主要来自技能训练时间与蓝图书沉没而非倒挂。是否需数值拉平待船长定目标）
  **2026-09-08 大改批后重跑（平衡-check，24h 30s 步长，同口径）**：钱包/总资产 A 原矿流 1,127k/**2,278k**（鲸吞级）、B 精炼流 577k/**801k**（掘洞级）、C 装备流 607k/**844k**（掘洞级；造 MK1/MK2）；B/C 总资产仅 A 的 **35~37%**（旧 73~77% 反转收窄）；双修贸易变体 24h 内技能未练成、与基础线同值。**状态（2026-09-08 船长定）：后续定位/拉平挂起——船长将调整技能体系（技能直接影响收益），待技能改完重跑再定目标；本行 09-08 数字保留为改前基线**。
  **2026-09-09 技能/工业大改后复跑（O3；工具修正后）**：总资产 A 原矿流 **2,293k**、B 精炼流 **2,338k**、C 装备流 **2,663k**（三线均鲸吞级；钱包 1,128/1,150/1,433k）——B/C 从 A 的 35~37% → **102%/116%**（技能投入有回报、装备流最优，A 原矿流与基线持平 +0.7% 未受损）；双修贸易变体仍 24h 内练不成、与基础线同值。**工具修正（balance-check.ts）**：船分级后沙猫/掘洞同 T1，原"目标 tier > 当前 tier"升级门槛卡死 12 万换掘洞（A 流伪影性卡在 89 万买不到鲸吞、总资产仅 91 万）——改按升级梯子内位置比较后恢复真实路径（三线均按 沙猫→掘洞→鲸吞 正常滚雪球）；里程碑/每小时曲线干净。**待船长定目标带**（B/C 已反超 A 是否到位，或需微调）。

## E. 基础设施
- [ ] E2 云存档 / 在线化：架构预留（决策 1/2/5），未启动；Phase 2/3 见 architecture.md

## 2026-09-08 一号交接开放项（来自临时交接卡 docs/design/handoff-2026-09-08-current.md，已按约定迁移后删除）
- [ ] **O1 组装机卡「手动 vs AI」两档净/h**：现为单一手动口径（二号 A 项）；手动/AI 基础核心档分列估算待船长拍板。
- [ ] **O2 奇货卖出侧溢价巡游**：挂价高于收购价线的奇货卖单仍可能被巡游采购按挂价成交（买入侧已 20L 收口，卖出侧未动）；处理前先问口径。
- [ ] **O3 balance-check 24h 策略复跑**：工业扩容（工位数大增）后未复跑；范围先问船长。
- [ ] **O4 顶部 AI 徽标计数与「执行中 N」偏差**：早期玩家反馈，根因未实证。
- [ ] **O6 二号船分级批次验收（已在 main）**：舰船尺寸分级 + 掠食者巡洋线 A1–A9；公告待审稿在 6b79a61（id 2026-09-09-ship-size-tier）未经船长批准不得写入 announcements.ts；巡洋 T3 数值/渠道声望待船长终审（涉二号分工，一号只读 ship-size-tier-rework.md）。
- 说明：O5 数值文档分层 = 现数值以 docs/design/manufacture-refine-worker-econ.md 与 docs/glossary.md 为准（refine-balance-50.md 为历史锚）；O7 真档验收状态 = 2026-09-08 引擎级实测探针 20/20 通过（临时探针已删），UI 目测项待船长开游戏确认（市场蓝图 chip/奇货低挂提示/AI 指挥中心 +N 工业/交付航线按钮与弹窗/工业页数值行/公告三条）。

## 2026-09-09 一号收尾开放项（收尾盘点后登记，待办照旧先读本段）
- 备忘（2026-09-09 自交接卡迁移）：**精炼净收益差异问题 = 船长延后**——差异来源 = 吞吐桶 ×8.7 × 矿价 ×37.7 ≈ 卖原矿差 328×，精炼净再 ×净率差；重开先回看 refine-cycles.md 头注与 content-csv 工作簿 items sheet。
- [ ] **P0-2「开战距离 / 先手」提案 + 预演（船长 2026-09-11 选定本批 =「先出提案 + 预演，审核后再落码」；二号）**——**本批零引擎、零数值改动，产出 = 提案稿 `docs/design/p0-2-engagement-20260911.md`（含全部实测读数）**。**①三条决定性事实**：(a)「够不着」= **距离拔河的胜负**——同一张蜃影、同一把 4×动能MK2，**带推进器 A0 = 100%/65s 闭合到 3.3km**，**仅去掉推进器 = 0%/140s、全程停在 9.99km、我开火 0.0**；(b) **教学卡「敌人一炮未放」与开战距离无关**——开局从 6314m 缩到敌人带内（1980/1500m）敌人**仍只放 0.2 炮、战斗缩到 1s**，敌人血抬到 **×12（264）** 也**依然 0 炮**（根因 = 交战距离停在玩家期望位 3.2km 而敌人带只有 2.2km ＋ 玩家先手一轮清场）；(c) **全局改法「开战距离 = 双方射程重叠区上沿」不成立**——全卡清点 **78 格中 77 格**被改动（含旧路径 20 张）⇒ 打穿「旧路径逐字不变」判据。**②病灶 A 实测（kite 卡，5 播种 1/7/13/29/51）**：蜃影 T48 的 A1/⑤/③ 三行现状 **0%｜140s｜我 0.0｜敌 136**；候选甲（可及性钳制：敌期望交距 ≤ 我射程×0.95）⇒ **20%/100%/80%**、我开火 28~40 次，但**连带把 A0 行从残 32% 拉到 58%**（= 船长刚裁定「保留」的那一行）；灰霾 T28 三行 **0%/600s ⚠触顶 5/5｜敌 584** ⇒ 甲后 **100%/36~59s**；赤潮 T34 三行 **0%/373s｜敌 364** ⇒ 甲后 **100%/41~64s**；**旧路径对照卡「幽灵舰信号」四行全 100%**（从未够不着 ⇒ 差异只来自舰级路径按"自身带×0.85"算站位）。**③候选四选一（甲乙丙丁）**：甲 全局可及性钳制 / **乙 逐卡覆写（复用既有 `rangeMinM/rangeMaxM` 旋钮，不新增字段；建议先做灰霾+赤潮、蜃影单独议）** / 丙 只做可读性（提前脱战 + 战报写明"够不着" + 卡面预告交距）/ 丁 不动；戊（全局改开战距离）已实测淘汰。**④顺带发现（建议先修）**：`battle-calibrate` 的**敌开火(头目/杂鱼)**列用 `b.fx.slice(seen)` 计数，而 `fx` 是 **48 条环**（`pushBattleFx` 超长丢最旧）⇒ **环满即停、长战全漏**：同格真值 **584.0**（头 145/杂 438）↔ 工具法 **48.0**（漏 92%）、蜃影 A1 136 ↔ 48、蜃影 A0 54.0 ↔ 43.2（= 蜃影稿 §2.1 记的 10.8/32.4 正是此口径）；**我开火列不受影响**（工具用 `stats.meShots` 权威累计）；修法 = 按 `BattleFx.seq` 续读（一行级）。**⑤四问待裁**：病灶 A 走哪条路 / 若走乙点名哪几张卡 / 要不要一并修工具漏计 / 教学卡「一炮未放」是否单列。**⑥验证**：本批**不改代码**，故只跑读数工具（`npx tsx _probe-p02*.ts`，两个探针**用完即删**，`_` 0 个）；提案落码时再走四连 + 重跑 A0~A3 与六组。**改动全在 d2 工作树，未合入 main、未推送。****【提案 · 未落码 · 待船长裁决】**
- [ ] **★★ 挂账①「低威胁卡敌人一炮未放」+ ②「名义火力 / 实际火力脱节」（2026-09-11 船长要求处理；二号）**——**①实测形态**：边境/碎晶（威胁 12/20）在 A0/A3 参考行**敌人全程 0 开火**（我开火 11~12 次、18~25 秒清场）——根因 = 敌军自带带太短（快艇 1~1883、护卫舰 326~4275），而玩家参考行站 3.2 km；**候选**：甲 逐卡给敌军**加宽射程带**（让其进入自己的交战带）· 乙 逐卡**开战距离覆写**（把开局放进敌军带内，实测 1500~1980m 时敌人会开火 1.0~4.2 次）· 丙 给低威胁卡**先手**（开场即开火）。**②实测形态**：纸面单发与实际输出脱节（例：赤潮头目 beam 必中单发 21 ↔ 灰霾头目 fixed 单点 59，两者实收相近）；**候选**：甲 单发按「名义 DPS × 装填 × 实收命中」**反推**（纸面 = 实收，须重标全部 26 卡）· 乙 只做**登记口径**（在词典写明"纸面 ≠ 实收"，不重标）。**两份提案待船长选型后再落码。****【提案 · 未落码】**

## 2026-09-20 三号交接开放项（本地化尾巴 · 船长令「本地化交给三号」）

> 交接口径：**一号不再动本地化**；下面两条是 `npm run l10n:list` / `l10n:check` 实测出来的尾巴，
> 处理时按约定 **§十一之三「实现 = id 映射制」**（先取 id、再写文案、中英都进唯一表），合入前跑 `l10n:check` + `l10n:list`。

- [ ] **L1 谜质科技节点说明（`note`）英文缺失**：`EN_MATTER_TECH`（`packages/data/src/l10n.ts`）目前**只覆盖 `name`**，
  24 个节点的 `note` 仍是中文 ⇒ 英文界面下说明显示中文（**既有缺口，非 2026-09-20 新增**；新增的 T4 节点
  `mt-industry-ai`「工业多核调度」同样只有中文说明）。要做成整树补齐，得先定口径：
  ① 数据侧补 `noteEn`（改 `MatterTechNodeDef`）；② 或改走 id 映射制（节点 id → 表里补 en，与 §十一之三 对齐，推荐）。
- [ ] **L2 一号批次的界面字符串未接 id**（`npm run l10n:list` 实测条数）：
  `panels/Shipyard.tsx` **13** · `panels/Industry.tsx` **19** · `ui/itemSubs.ts` **28** · `ui/Glyphs.tsx` **10** ·
  `pages/FitPage.tsx` **9**（Shipyard 目前只有搜索框接了 id）。`l10n:check` 是绿的（无中文源串调用点），
  这些是**尚未接线的字面量** ⇒ 英文界面下会显示中文。
- [ ] **L3 通讯消息整类没有英文**（**2026-09-22 一号查出 · 船长令「英文本地化依旧交给三号」**）：
  `ctx.messages`（**30 条**：13 封「第一次」情报信 ＋ 开场简报 ＋ NPC 来信）的 **主题 / 正文 / 前往提示**
  只有中文 ⇒ 英文界面下通讯页整封显示中文。判据：`packages/data/src/l10n.ts` 的 `localizeCtx` 覆盖了
  ships / modules / items / skills / blueprints / shipBlueprints / galaxies / belts / stations / commsFactions /
  matterTech / travelEvents，**没有 `messages`**。两条做法：**甲（推荐）**新增 `EN_COMMS_MESSAGES`
  （`Record<messageId, { subject; body: string[]; hintText? }>`）＋ 一个 `overlayMessages()`，在 `localizeCtx`
  加一行（改动集中数据层）；**乙**走 id 映射表（`msg.<id>.subject` / `msg.<id>.body.N`），更贴现行 id 制，
  但要改 `core/comms.ts` 与 `CommsPage` 的读取方式。⚠ 13 封情报信的**中文已于 2026-09-22 重写**
  （提交 `da8764ce`）⇒ 英文请照新版中文译，术语以 `docs/glossary-en.md` 为准。

## 2026-09-16 二号交接开放项（虫洞战利品扩充批的尾巴 · 已推送 · 公告已裁定不发）

- [x] **W1 虫洞战利品扩充 · 公告（船长 2026-09-20 裁定：「都进行归档，不发送」⇒ 销账）**：
  待审稿 = `docs/design/archive/announcement-draft-20260915-wormhole-loot.md`（4 条要点已成稿 · 第三人称 · 无开发话语 ·
  口径来源照抄船长七段原话）；**本稿不发布**（改动已随代码上线，不另发公告；稿已归档留档）。
  原挂账口径（2026-09-15 船长「公告我让一号发」）随本裁定作废。
  数值口径仍以词典八之二/八之三为准（谜质 1 台 = 1 枚 · 70,000/枚 · 只收不卖 · 奇货档；
  奢侈品三档 10 万 / 40 万 / 200 万；贵重品货柜 2 格 ⇒ 5~30 件 · 箱价 365 万；军用备货柜 4 格 ⇒ MK3 1~3 件 ·
  箱价 280 万；遗迹出货固定 70% · 贵重品柜 50%；残骸堆 0.75%/堆；安全货柜 3000 m³ / 6 格 / 价 ×1.5）。
- [x] **W2 验收用测试存档（船长 2026-09-22 销账：不再配）**：`wh-loot` 一档——仓库预置 2× 贵重品柜 + 2× 军用柜 + 1× 安全柜（可立刻拆解），
  洞内一趟带 2 台谜质装置并站在残骸堆旁（一撤离即可看谜质折算与结算单新格）。配在 `tools/make-test-save.ts` + `docs/test-saves/`。

## 最近批次（滚动窗口 · 最新 20 条）

> 越窗即封存（`npm run docs:seal`）；窗口大小 = 20 条，需要临时调大用 `--window=N`。

- 2026-09-24：**文档事故还原：两处一级标题的 `#` 被 BOM 顶掉（一号 · main · 归档自检时发现）**——**①现象**：`docs/glossary.md`（**术语权威**）与 `docs/design/handoff-verify-20260924.md` 的**第 1 行**都成了「`U+FEFF` ＋ 空格 ＋ 正文」，一级标题的 `#` 不见了 ⇒ `docs/INDEX.md` 里这两份文档的标题列**都读作「(无一级标题)」**。**②溯源（按干净父提交逐字节比对，未凭记忆重写）**：`glossary.md` 在 `origin/main` 上第 1 行 = `# 术语词典（Glossary）`（首 6 字节 `23 20 E6 9C AF E8`），到 `HEAD` 变成 `EF BB BF 20 E6 9C …`（**BOM 替换掉了 `#`**）；`git log origin/main..HEAD -- docs/glossary.md` 只命中一个提交 = `6a753b86`「归档（§8 三步）＋ 交接卡」（同日 18:18）。`handoff-verify-20260924.md` 是同轮新建件、同一形态（**全仓扫描确认只有这两份中招**）。**③影响面**：**纯文档**，不碰代码 / 数值 / 存档 / 玩家可见内容；唯一可观测影响 = 文档清册标题列少了两条名字。**④还原法**：`.NET ReadAllText → 第 1 行 TrimStart(U+FEFF) → 正则补回 `# ` → WriteAllText(UTF8Encoding($false))`，**其余行一字未动**。**⑤自检读数**：两份文件除第 1 行外与父提交**逐行相同** · **无 BOM** · 纯 LF **0** · 乱码命中 **0** · `docs:index --check` 通过 · 清册里两份文档的标题恢复为「术语词典（Glossary）」/「交接卡：三号本轮收尾…」。**⑥教训（写给所有会话）**：本仓 `edit` 工具有**吞 BOM** 的已知毛病（2026-09-12 记过一次），而这次是**反向**的 —— **BOM 被写了进去、把首字符顶掉**；两者同源 ⇒ **批量改中文盘后必须验「第 1 行内容 ＋ 文件字节头」**，只扫乱码特征查不出来。
- 2026-09-24：**敌方挂载件两件：A 族「姿态陀螺仪」＋ G 族「船体修理装置」（船长令 · 二号 d2 落码 ＋ 一号合入并复核 · ✅ 船长实测通过）**——**①船长原话（照抄）**：「**我调整了A族的闪避，并且希望在虫洞内，A族添加一个挂载件：姿态陀螺仪：增加10%闪避。给G族添加挂载件：船体修理装置。每5秒恢复5装甲和5结构，会吃威胁的加成。**」＋「**我的改动是A给A族除电子舰外的其他敌人加10%闪避。**」＋「**你先提高A族闪避，提高后再挂载，电子舰也要挂。**」；三条追问取**乙/甲/甲**：**只挂洞内卡条目** · 陀螺仪**加算 +10 个百分点** · 修理量**乘层威胁倍率**（归一基准「不改动」＝ 层 1 ×1.00；**基数以实交付为准 = 15/15**——船长同日二次令「船体修理装置（G 族）基础数值上调至15装甲15结构」）。**②落地**：a) **闪避**——A 族四艘（海盗快艇 / 劫掠护卫舰 / 劫掠狙击舰 / 海盗头目舰）`evasion` 0.12 → **0.22**（显式），**劫掠电子舰保持 0.30**（刻意排除）；b) **两件挂载件**（`core/foeMounts.ts` 目录 **10 → 12 件**）＝姿态陀螺仪（`FoeMountDef.evasionBonus.add`，消费端 `min(0.9, 舰级 + 加数)` 夹紧；**多件相撞取加和**）＋船体修理装置（`repairPulse.everyMs/armor/hull` **只给基数**，实数 = 基数 × **k**，`k = 该层威胁 ÷ 45`）；c) **战斗消费端**（`core/combat.ts`）＝建档处加闪避 ＋ 逐单位修理脉冲（`initFoeRepairPulses` / `pulseFoeMountRepair`，开战与换波各调一次、**幂等**），运行态账本 `battle.foeRepairPulses` **登记进 `save.ts`**（漏登记＝战中重载白赚一跳）；d) **数据**（`data/wormholeFoes.ts`）——A 族三卡每条编成挂陀螺仪（**含劫掠电子舰那条**）、G 族三卡每条编成挂修理装置（共 10 条编成），**星图悬赏 / 低安遭遇一律不动**；e) **契约**（`content-check` 挂载件判据⑤）＝洞内 A 族每条编成必有陀螺仪 · G 族必有修理装置 · 洞外与舰级**零这两件**（按卡 id 前缀 `wh-` 认"洞内"，不维护第二份清单）；f) **文案**＝两件名与说明中英双语（`Attitude Gyro` / `Hull Repair Unit`，走 `namePairs` 一路到敌舰悬停与战报，**显示层按当前语言取列**）。⚠ **过程中两次自我纠错（留档）**：①`FOE_REPAIR_THREAT_REF` 起初 import `wormholeFoes` 的 `WORMHOLE_THREAT_BASE`，而后者反向 import `combat` ⇒ **循环依赖在 `content:check`（CJS 转译）下顶层求值 TDZ**（`ReferenceError: Cannot access … before initialization`；**typecheck 与 vitest 都发现不了**）⇒ 改**字面量 45 ＋ 一行类型级一致性校验**（两处不等 ⇒ typecheck 当场红）；②双语名对第一版做在 `data/l10n.ts` 的卡条目上，写完发现**界面没有任何地方读卡内编成**（界面拿的是 `battle.foeMounts` 那份运行时快照）⇒ 那是**死代码**，改为"运行时带名对、显示层按语言取"。**③验证**：typecheck 四包 0 错 · core **201 文件 / 2271 用例**（新增 15 条 ＋ 同步改写既有 3 条）· `content:check` ✅ · `l10n:check` ✅ · `ui:tip-check` ✅ · `ui:rot-check` ✅ · `save:roundtrip-audit` 丢键 0 ✅ · `save:migrate` ✅ · 桌面 `build` ✅（1.82 秒）；**合入后由一号在 main 上复跑同一套闸门复核，读数一致**。**④验收**：船长 2026-09-24「**实测没问题**」。**⑤工作文档**：`docs/design/foe-mounts-20260924.md` 与交接卡 `docs/design/handoff-foe-mounts-20260924.md` 已归档（内容并入本条目 ＋ `docs/glossary.md`「敌方挂载件」词条，原文件删除）。⛔ 遗留：**存量八件仍无英文名**（缺 `en` ⇒ 英文界面回落中文名，与改动前一致），待船长拍板译名后逐件补；`foe:export` 的"两列读数"**有意未加**（敌舰明细表是**舰级粒度**，而这两件挂在**卡条目**上 ⇒ 加列会写出误导性空值）。⚠ **验收要盯的一处**：A 族**洞内**闪避 = 数据 0.22 ＋ 陀螺仪 0.10 = **0.32**（劫掠电子舰 0.40），我方面向 A 族命中率降约 10pp。
- 2026-09-24：**拆除「打捞器全员补发」临时补丁（船长令 · 一号 · main）**——**①船长原话（照抄）**：「**打捞器全员补发可以拆除。**」（补丁本体＝船长 2026-09-22 令「这次补发直接所有人无条件发，**发放完成后下次更新删除补发**」）。**②落地（实际连带 12 处，比 roadmap「未完成功能清单」登记的 5 步多）**：① `firstRewards.backfillSalvagerIfMissing` 函数本体 ＋ `SALVAGER_MODULE_ID` 常量 ＋ 整段注释 ②③ `engine.ts` 的 import 与每拍调用（含注释块）④ `state.ts` 的 `ImportantTaskState.salvagerGift` 字段 ⑤ **`save.ts` 的 `normalizeState` 白名单那一行**（**漏删＝白名单留死键**）⑥⑦ `first-tasks.test.ts` **两块**用例（`it('全员补发打捞器 MK1…')` 整例 ＋ 「奖励真的按新口径发」里**另一块**补发断言）⑧⑨ `save.test.ts` 两行（含类型穷尽表 `Record<keyof ImportantTaskState, true>` —— **不删即 typecheck 红**）⑩ `simulation.test.ts` 的"补发噪声"规避行 ⑪ 孤儿文案 id `core.firstRewards.008`（补发日志，拆除后再无调用点）⑫ roadmap「未完成功能清单」那一行（该清单自带收口规则：完成即删条）。另加三处注释收尾：`save.ts` / `save.test.ts` 里以该字段为实例讲「随档字段两处落笔」规则的注释，各补一行说明补丁已拆、字段已删，**不留悬空引用**。**③用例净变化**：删 1 整例 ＋ 3 块断言，**新增 1 条负向断言**（`'补发已拆除 ⇒ 不再无条件发打捞器'` = 0）⇒ 谁把补发加回来会当场红。**④验证**：typecheck 四包 0 错 · core **201 文件 / 2270 用例** · `content:check` ✅ · `l10n:check` ✅ · `ui:rot-check` ✅ · `save:roundtrip-audit` 丢键 0 ✅ · `build` ✅。**⑤口径**：**已发出的打捞器不回收**（沿用船长 2026-09-22 口径）；不动「第一次打捞残骸」任务的起手道具链路（`firstTasks.ts` 的 `startReward`，照常发）；删字段**零迁移**（原字段本就"只在 true 时落键"）。**⑥工作文档**：`docs/design/salvager-backfill-removal-20260924.md` 已归档（内容并入本条目，原文件删除）。⛔ 遗留：当年那笔「**已被刷新多领的那几台要不要回收**」（2026-09-22 挂账）本次按"不回收"结；要收另开一单（扣回装备库 `mod-salvager-1`）。
- 2026-09-24：**铁人模式两件 UX：关闭警告弹窗 ＋ 未选择旧档的模式选择弹窗（船长令 · 一号 · main）**——**①船长原话（照抄）**：「**新增玩家关闭铁人模式时弹出的警告，要告诉玩家关闭后无法再开启。**」＋「**并对至今未选择的旧档进行模式选择弹窗。**」；四条选定（答一号集中提问）：「**Q1 进游戏后要二选一。Q2 甲 Q3 乙 Q4 给**」⇒ **进游戏后**弹且**必须二选一** · **复用序章那两张模式卡** · **选完普通即用掉那次机会**（存档页「开启铁人模式」入口撤除）· 旧档转铁人**照给**「铁人」隐藏徽章。**②落地**：a) **关闭警告**——`panels/SaveManager.tsx` 的「关闭铁人模式」由"按钮变红再点一次"（提示只藏在按钮 `title` 里）改为**弹一次警告弹窗**（与站内其它确认弹窗同一套结构，对齐 `WormholeScan` 的"放弃确认"写法），正文两条：**关闭后无法再次开启铁人模式** ＋ 福利同时失效（代次与已获得的徽章保留）；b) **模式选择框**——新增 `panels/ModeChoice.tsx`，由 `App.tsx` 在 `onboarding.step !== 0`（**进游戏之后**）挂载，判据 = `engine.modeChoiceNeeded()`；遮罩不可点、无关闭键 ⇒ **强制二选一**；选铁人走既有 `enterIronmanNow`（**失败不静默放行**，框内显示理由、可改选普通）；c) **存档面**——新增 `state.modeChosen?: boolean`（**只在 true 时落键、可选字段、零迁移、不升版本**）；**为什么必须有** ＝ 存档里原本区分不出「选过普通」「序章跳过」「老档」（**三者都没有 `ironman` 字段**）⇒ 不加标记就会对已经选过的人反复弹；**只有"选普通"那一刻写它**（选铁人**不写** —— `ironman.sinceWallMs` 本身就是记录，`ironmanEver` 判得出来），按约定 §二 **写入点 `engine.chooseStandardMode()` 与 `save.ts` 白名单两处落笔**；d) **连带**——序章 `chooseStandard` 也落一次账（新档在序章选过普通，进游戏就不会再被问）· 存档页开启入口按 `!im.on && !im.modeChosen` 隐藏（弹窗强制二选一之后实际不再出现 —— 正是"用掉唯一机会"）；e) 新增 5 条文案 id（`ui.Ironman.039~043`，中英双语）。**③口径（一号落的三条，船长未逐条点选，已在汇报里报备）**：弹窗**替代**原两段确认（不留双保险）· 正文写**两条** · **「跳过序章」不算已选**（若把跳过算作已选，这批玩家将**永久失去**转铁人的可能 —— 那是"入口撤除"的必要条件）。**④验证**：typecheck 四包 0 错 · core **201 文件 / 2272 用例**（新增 2 条）· `content:check` ✅ · `l10n:check` ✅ · `ui:rot-check` ✅ · `save:roundtrip-audit` 丢键 0 ✅ · 桌面 `build` ✅（本地产物已重建）；**负向验证** ＝ 临时摘掉 `save.ts` 白名单里 `modeChosen` 那行 ⇒ 新用例「随档往返：`modeChosen` 读档后还在」**如实变红**（`expected undefined to be true`），还原即绿。**⑤验收**：船长 2026-09-24「**实测没问题**」。**⑥工作文档**：`docs/design/ironman-mode-ux-20260924.md` 已归档（内容并入本条目 ＋ `docs/glossary.md` 铁人词条，原文件删除）。⛔ 待船长定：①已发布公告 `ann-ironman-20260923` 那句「**现有存档也允许切换一次铁人模式**」的"一次机会"现在**落在本弹窗上**（选普通＝把机会用在普通上），要不要另发公告说明 ②`ui.Ironman.023`（"再点确认关闭…"）随两段确认退役成**孤儿 id**，待归档清理。
- 2026-09-24：**战斗显示三件（伤害飘字 / 伤害类型配色 / 武器弹种徽标）＋ 一次黑屏事故（三号 · verify-activity-win → main · 本地领先 origin 9 个提交，一律未推送）**——**①船长原话（照抄）**：「**战斗界面，我希望添加战斗伤害的数值动画（包括 MISS）**」＋四答甲（目标旁向上飘 · 同拍同目标累加成一个数字 · 类型色数字 + 灰色 MISS · 只在战斗画面且跟随倍速/暂停）；「**我发现新问题关于伤害类型颜色，不知道什么时候改错位了，原先动能是黄色，能量是蓝色的。**」＋（转述玩家）「**他的动能武器和高爆打能量弹药**」；「**战斗画面，玩家的截图中的攻坚炮台 MK3 动能型后面写着的是能量弹药，还有巡航导弹架 MK3 也写着。**」；报障「**现在进入战斗会黑屏**」。**②落地**：a) **飘字**——`BattleFx.dmg`（可选字段、零迁移；口径 = `applyDamage().dealt` 实收，含附伤段，与 `stats.meDmg` 同源）在六个开火支逐发写入；未命中/无伤事件（捕获网连线、机群击落）**不写** ⇒ 表现层不出数字、不飘「-0」；`BattleScreen` 按目标 tag 同拍累加、生命期走**战斗时钟**（倍速/暂停同步）。b) **配色**——`app-d-*`（伤害类型徽标）原先按**层位**配色（动能→盾蓝、能量→结构黄），与 `app-a-*`/`DMG_COLOR` 的**类型色**（动能金/爆破橙/能量青）正好相反；现**两族合并成同一条 CSS 规则**（结构上不可能再漂移），层位色只留 `.app-p-*` 与血条。c) **弹种徽标**——`battleArcsFor` 原先给**每一门炮**取 `nextAmmoType(battle.ammo)`（**全船剩余最多那一型**，V18 遗留）⇒ 装激光就会把动能炮、导弹架全写成「能量弹药」（截图那一幕）；现按**武器自己的 `shotsByType`** 报，该型打光 ⇒ `null`（显示"无弹/虚线弧"）。d) **黑屏**——`BattleScreen` 把 `const POPUP_LIFE` 写在组件体后段，而前段的 `.filter((p) => … POPUP_LIFE)` 同一次渲染里先执行 ⇒ `ReferenceError`（TDZ）⇒ 战斗页整棵树崩掉；常量上提到 `battleViewCore` 模块级并导出。**③验证**：core **2255/2255（200 文件）** · typecheck 四包 0 错 · `l10n:check` · `ui:rot-check`（含新入库的 TDZ 体检）· `ui:theme-check`（含 token 契约 + 伤害类型配色契约）· `build` 全绿；弹种那条用 `git stash` 反证（旧口径 ⇒ 用例报 `expected 'plasma' to be 'kinetic'`，与截图同形）。⛔ 遗留：无（飘字的**观感**仍待船长在真机上看一眼）。
- 2026-09-24：**任务板与工业侧四件（快递 120 分钟 / 蓝图卡利润率与整批 / 拆解日志报数量 / 一次性图纸被吞）＋ 停机善后收口（三号 · verify-activity-win → main · 本地未推送）**——**①船长原话（照抄）**：「**快递任务和资源任务的刷新周期延长到 120 分钟。**」→「**那只改快递任务。**」→「**1 快递任务的周期和持续时间都为 120 分钟，资源任务不变。2甲。**」；「**快递任务现在是 2 小时刷新周期，但是卡片上和快递任务页面写的还是 20 分钟。**」；「**组装机零件的利润率不对，组装机是一次性生产 10 个的，现在的利润只计算一个**」→「**也显示整批**」；「**拆解货柜的事件日志内，要显示获得的物品数量（比如奢侈品）**」；「**刚刚在造的锤头鲨级一次性蓝图…离线后…船不见了，蓝图显示已消耗**」＋「**玩家的存档我已经导入本地，你可以检查下制造记录**」＋「**1 肯定要材料一起退。**」＋「**你直接将 2 艘舰船成品给予玩家**」。**②落地**：a) **快递 120 分钟**——引擎（`COURIER_BOARD_PERIOD_MS` / `courierDueAtWindow` / `courierDeadlineMs` / `courierRemainingMs`）＋**界面与文案同源**（周期取常量、倒计时 `h:mm:ss`、新增快递专用 id `.434/.435`）；⚠ **根因不止文案**：出发到期护栏原先用资源那 20 分钟 ⇒ **抽到手超 20 分钟的单子被判"已到期"拒发**。b) **利润率按整批**——`marginPctOf(price, cost, unitsPerRun)`（零件 10 件 / 消耗品 120 发 / 修理组件 3~5 件；装备与舰船 1 件逐值不变）＋卡面新增「整批 = 单价 × 件数」（`ui.Yield.006`）。c) **拆解日志报数量**——按 id 归并成 `名称 ×N`（军用柜同型多件合并；奢侈品整叠 5~30 件）。d) **一次性图纸被吞**——三条路一起修：重复清剿**抢主控**（等待表补「亲自开炉/亲自开线」）· 停机**不退书**（现退书退名额，停掉的线从表里摘掉防二次退）· `bookSpent` **不随档**（现随档）；**存档实证**（只读核对玩家档）：三张一次性舰船图全"已消耗"、舰船仓库一艘没有、跑线表空、清剿开着。e) **停机退料收口**——制造线按线上记的账（`spentMaterials`）**整批退料**；炉子（精炼/回收/货柜拆解）"每批到点实时扣"⇒ 停机本就不吃料，只补**老档炉内预占账**退回仓库。f) **玩家补账**——按船长令用 `tools/save-grant-ships.ts`（双备份 + 外科式改写 + 自检）给导入档补 **锤头鲨 / 牛鲨各 1 艘**进舰船仓库，只有 `shipStore` 变了（其余逐字节未动）。**③验证**：core 用例（`courier-board-period` / `sideTasks` / `manufacturing` / `halt-material-refund` / `one-time-blueprint` / `wormhole-unbox` 等新增 20 余例）· 闸门全绿 · 读数样张 `npm run courier:clock` / `unbox:log-sample` / `bp:margin` / `once:repro`。**④工作文档**：`docs/design/side-tasks-courier-and-damage-popup-20260924.md` 与 `docs/design/one-time-blueprint-loss-20260924.md` **已归档（内容并入本条目与本文件 ＋ `docs/glossary.md` 十四/十五两节，原文件删除）**。⛔ 待船长定：①本批 9 个提交**推不推**（船长 2026-09-24 明令「不推送」）②玩家反馈里的"动能武器打能量弹药"若确因配色买错/装错弹，要不要补偿。
- 2026-09-24：**8 个临时探针转正式工具入库 ＋ 两道新契约（三号 · verify-activity-win → main · 船长令「入库」）**——**①船长原话（照抄）**：「**入库。**」（答"8 个 `_` 前缀临时工具要不要转正"）。**②入库件与 npm script**：`ui-tdz-check.ts`（`ui:tdz`，**已挂进 `ui:rot-check` 链**）· `dmg-color-check.ts`（`ui:dmg-color`，**已挂进 `ui:theme-check` 链**）· `bp-margin-readout.ts`（`bp:margin`）· `blueprint-ship-ids.ts`（`bp:ship-ids`）· `once-blueprint-repro.ts`（`once:repro`）· `unbox-log-sample.ts`（`unbox:log-sample`）· `courier-clock-sample.ts`（`courier:clock`）· `save-diff-audit.ts`（`save:diff`，只读）· `save-grant-ships.ts`（**刻意不挂 script**：会写真档，双备份＋外科式改写＋自检，仅船长下令时手动敲）。**③两道新契约的起因与判据**：a) **TDZ 体检**——起因是"进入战斗黑屏"（`const POPUP_LIFE` 写在组件体后段，前段的 `.filter(cb)` 同一次渲染里先执行）⇒ 这类错**typecheck 查不出、测试不渲染 UI、配色/旋转契约无关**；判据 = 同一声明列表里"靠前语句（含闭包）读靠后块级声明"，其中传给**同步数组方法**的回调标「**直接执行**」（必崩 ⇒ **报红 exit 1**）、其余（`useEffect`/事件回调）标「闭包内」（只作读数；本仓现存 20 来处）。b) **伤害类型配色契约**——两族必须解析到**同一个 token**、逐主题三系底色两两不同、且**动能不比能量更蓝**（"错位"的形状）。**④验证**：两道新契约接进闸门后 `ui:rot-check` / `ui:theme-check` 均通过（六套主题逐套打印读数）· typecheck · core 2255/2255 · build 全绿。
- 2026-09-23/24：**铁人模式上线（二号 · d2 · 已合入 main ＋ 已推送 `aac08f62` 一线 · 公告已发）**——**①船长原话（照抄）**：「**和玩家讨论了下，发现好像搞一个铁人模式更受欢迎。玩家的导出的存档会带版本号。如果导入一个铁人存档的版本号比当前存档的版本号更靠前则会导入失败。玩家重置档案并不会清空这个版本号。**」＋七条追加（新档可选/旧档一次性转换 · 每次落盘 +1 · 可关闭且关闭后不再上升 · 救援 ≥ 两天 · 不要自动备份 · 只有关闭后才出现的隐藏成就 · 只能关闭无法再打开）＋福利三档（A 离线上限 +8h · B 市场刷单 ×3 / 稀有 ×2 / 奇货 ×2 且每窗上限 +2 · C 奖励 ×1.1 / 稀有掉率 ×1.2 / 训练 ×0.9；**贸易税不做优惠**）。**②落地**：`core/ironman.ts`（代次 · 单向门 · 闸门判据 `ironmanLoadVerdict` · 福利乘区）＋ `state.ironman` 存档字段（零迁移）＋ 主进程**存档之外的账本**（`ironman-ledger.json` 主+影子双写、读 max、重置不清）＋ 装载闸门（导入/恢复两条路径都判 `T = max(当前档, 账本)`，**救援 = 档龄 ≥48h**）＋ 两枚隐藏徽章（铁人 / 中途关闭铁人）＋ 存档页铁人区块与**序章两张模式卡**（含"跳过"也出卡，船长指定）+ 新增工具 `npm run ironman:check`（账本与闸门 15 项回归）。**③关键裁定（照抄）**：「**铁人战斗…**」无需——「**贸易税不做优惠，可以开工了**」·「**奇货订单每窗最大数量+2**」·「**入侵…**」另批；**闸门"静默放行"缺陷**（账本读不到时整条闸门被 catch 跳过）由船长实机报障后修掉（拆开 try：账本缺失只降级、判据照走）。**④验证**：typecheck ×4 · core 全绿（收官 198 文件 / 2226 用例）· content/l10n/ui:rot/build 全绿 · `ironman:check` 17 项。⛔ 遗留：①「直接替换 save.json 后重启」目前不过闸门（启动不判，船长未裁）②奇货权重实测只到 ×1.47（样本少，登记待复核）。
- 2026-09-23/24：**周末入侵活动 M1 机制骨架完成（二号 · d2 · 已合入 main ＋ 已推送 · 公告未写）**——**①船长原话（照抄）**：「**我打算做一个周末入侵的活动…**」（设计冻结稿见 `docs/design/weekend-invasion.md`，已转常设设计文档 `docs/design/weekend-invasion.md`）＋七条裁决（「**Q1，威胁降低，依旧是单舰。其他按你推荐。**」）＋两条实现口径（「**入侵战斗采用独立设计的卡（之后设计），我们暂时先试用虫洞的**」·「**目前入侵只有调试模式可见**」）＋三条实机反馈（「**入侵的窗口放在活动窗口的右侧**」→「**干脆放在事件日志的底部，和现有事件日志同级。点击后自动跳转到星图界面**」·「**入侵信息和窗口用红色色系比较好**」）。**②落地**：`core/weekendEvent.ts`（时间轴 · 占领 · 进度 = NPC 铺底 + 玩家台账 · 遇袭概率 · 旗舰倒计时与离线保护 · 贡献四档）＋ `core/weekendBattle.ts`（三类战斗规格 · 结果结算 · 旗舰击毁 · 结束结算）＋ `core/weekendBounty.ts`（悬赏替换派生卡 · 遇袭判定 · 占领区中安/高安破例）＋ `core/weekendLaunch.ts`（旗舰 4 波 120 威胁 · 4 艘小队战）＋ **战斗层新增敌群强度覆写口** `FoeOverride`／`applyFoeOverride`（不传=一字不变）＋ 遭遇系统接线 + 引擎 tick + 日志底部红色活动框（点击跳星图）+ 星图被占红环与旗标（`--wui-danger`）。**③验证**：入侵相关用例 41 条全绿（weekend-event 23 / weekend-battle 11 / weekend-bounty 5 / 其余）· typecheck ×4 · content/l10n/ui:rot/build 全绿。⛔ **M2（三族舰队＋各自 T5 旗舰，含改 C 族 T5 契约）/ M3（两个新族）/ M4（黑匣入库定价 · 通讯两封 · 公告待审稿）未开工**；解除"仅调试可见"待船长令。
- 2026-09-23：**护盾充能装置 冷却与效果值减半（船长令 · 二号 · d2 · 已合入 main ＋ 已推送）**——**①船长原话（照抄）**：「**将护盾充能装置的冷却时间和效果值降低到50%**」＋复核确认「**护盾回充的平均恢复量不变**」。**②落地**：`SHIELD_PULSE_MS` 30 秒→**15 秒**（含 `save.ts` 旧档兜底常量同改）· MK1/2/3 每跳比例 24/40/64% → **12/20/32%** · 说明文案同步；注释与 4 个用例标题一并改口径。**③口径**：间隔与比例**同比减半 ⇒ 每秒回盾量不变**，实际变化 = 破盾后起盾更快、过充浪费更少（船长已确认这是预期）。工作文档 `docs/roadmap.md` 已归档。
- 2026-09-23/24：**虫洞货柜出货率提到 5% ＋ 契约修复（船长令 · 二号 · d2 · 已合入 main ＋ 已推送）**——**①船长原话（照抄）**：「**将虫洞敌人掉落货柜的概率提高到5%**」→ 追加「**一起改成5%**」。**②落地**：新增 `WORMHOLE_LOOT_BOX_CHANCE = 0.05`（**战果/敌人掉落**那条）· `WORMHOLE_SALVAGE_BOX_CHANCE` 0.0075 → **0.05**（**残骸打捞点**那条）；`content:check` 的「战利品扩充契约③」期望值同步改（**我上一批漏改过、主树一度转红，已修**）。**③验证**：残骸格命中率统计用例（1 万个掷骰键落在 2%~10% 带内）＋ core 全绿。⛔ 遗留：期望收益约抬 6.7 倍，跑一阵如觉过肥再调。
- 2026-09-24：**货柜拆解「无料即停」修复（玩家报障 · 二号 · d2 · 已合入 main ＋ 已推送）**——**①玩家原话（照抄）**：「**货柜拆除时，没有货柜了精炼炉还是会空转**」＋「**没有东西了还在拆。取消就消失**」＋补充「**当时"可用"是显示 0**」，货柜为**图纸货柜·深层**。**②根因**：「料尽」原先**只在批到点那一刻**判 ⇒ 料在**批中途**消失（卖掉/搬走/**被另一台炉抢走同一种货柜**）时，那一台要转到本批结束（拆解线 **90 秒/件**）才停 ⇒ 面板「可用 0」却仍在"拆"，而按取消（`stopRefineRun` 直接 splice）当然就消失。**③修法**：`advanceRefining` **每拍先算余量**，不足一批即把到点时间拨到"现在"，交回既有停炉分支（退料账/记「货柜已拆完」/还核心，口径不变）⇒ 窗口从**最长 90 秒**压到**下一拍**。**④用例**：`tests/unbox-idle.test.ts` **7 条**（含两台炉抢料复现、`box-bp-deep` 原样复现、批中途没料、没货柜不许开工、反向保护）。
- 2026-09-22：**界面配色（皮肤）六套可切换（船长令 · 一号 · main · 已推送 `fd409dd9` · 公告已发）**——**①船长原话（照抄）**：「**玩家反应，现在的界面看着太吃力，我打算添加几套配色，用于让玩家切换（至少有一个是白色皮肤）。**」；后续三轮口径：「按你推荐来进行」→「**继续做剩余的颜色**」→「**都做**」→「**亮白的亮度依旧有点刺眼**」；两条报障：「**字都没办法辨认**」「**各种 svg 图标都失效了**」；一条裁示：「**面板透明度的没问题了，可以消档**」。**②做法（单一色表）**：颜色字面量收进 `packages/ui/src/index.css` 的 `:root` ＋ `[data-theme='…']` 块（**481 个色 token × 六套，键集完全一致**）；语义色阶（族色/物品/导航/图标/作业/伤害/角色）单点化到新文件 `ui/tones.ts`（值 = `rgb(var(--wui-tone-*))`）；`styles.css` **974 处字面量改写 944 处**（余 85 处在 `box-shadow`/`text-shadow`/`filter` 里**有意保留** —— 影子在亮底下仍是黑的），渲染层 30 处内联色全部收口。六套 = 深空（默认）· 亮白 · 高对比 · 暖色护眼 · 纯黑夜间 · 色盲友好；入口 = 设置面板新增「界面配色」一行（本机 `whale-idle:ui-theme` ＋ 跟随系统，**不进存档**），首帧前写 `data-theme`（不闪）；字号上限 125% → 150%。**③色板来源（按参考图实测取色）**：船长给的四张参考图用探针解 PNG 按面积数主色 —— 深空 ← SpaceIdle（面板 `#182232` 51% · 宇宙底近黑 · 白字 `#f3f3f3`）· 亮白 ← Kittens Game 亮色（内容 `#f2f2f2` 66% · 页面灰 `#cbcbcb`/`#d9d9d9` · 近黑字）。**④三次事故真因（各配一条可复跑契约）**：a) **裸 `var()` 当色值用** —— 色板 token 是**空格三元组**、必须包 `rgb(...)`，而仓里原有 **481 处**是旧写法 ⇒ 那些声明**整条失效、`color` 回落黑**（深色皮肤上黑字压深底 = 「字都没办法辨认」；亮色皮肤上碰巧正常 ⇒ 只在深空暴露）⇒ 修 470 处，守门人 = `ui:theme-check` **⑥**；b) **`ui/tones.ts` 的 `toneVar()` 漏包 `rgb()`** ⇒ 图标描边整条失效（「各种 svg 图标都失效了」；填充型 `currentColor` 图标不受影响 ⇒ "各种"而非"全部"）⇒ 守门人 = **⑥-b**（CSS 与渲染层 TS/TSX 一起查，并点名"拼接式色值串缺 rgb() 包装"）；c) **长尾"面"色被判成"字"** —— 角色判据只看 token 名前缀，而长尾 token 叫 `x24`/`x74` 看不出语义 ⇒ 把**导航栏底色提亮**、其上文字掉到 1:1（三套新主题各约 400 处不合格）⇒ 判据改为**看它在深空里亮不亮**（亮 ⇒ 字/图标；暗 ⇒ 面/槽/边，一律不许提亮）。**⑤顺带修掉的历史问题**：3 个 token 引用了却从未定义（`--wui-dim` ×10 / `--wui-green` ×1 / `--wui-warn` ×2，那些声明一直在静默失效）· 亮底"凹槽"用纯黑半透明会变近白（新设 `--wui-well`）· 导航角标 10px 黑字压亮红底仅 **2.4:1**（新设 `--wui-badge-bg`/`--wui-badge-ink`）· 日志时间列（2.2~2.8:1 · 每页 ×140）与 `app-dim` 系列抬到 ≥4.5:1 · 通讯页发件人色调**写死在数据侧**（亮白下 1.7:1）改为引用色阶 token ＋ `content:check` 势力色调形参放宽。**⑥读数**（新工具 `npm run ui:contrast`：真实底像素，9 个一级页 ＋ 设置面板 × 六套）：不达 AA —— 深空 **1** · 亮白 **7** · 高对比 **0** · 暖色 **2** · 纯黑 **1** · 色盲 **2**（余下个位数皆是 13px 导航角标的**取样误差**，色板层实测该对 5.87:1）；色板层最差正文：深空 5.09 / 亮白 4.86 / 高对比 16.4 … **六套全部 ≥4.5:1**。**⑦工具（均正式入库）**：`ui:contrast`（对比度体检，含小元素 3×3 中位采样修正）· `ui:theme-check`（**六条契约**：引用齐全 / 各主题键集一致 / 深空基线冻结 / 无残留字面量 / 前景×底色对比度 / 无裸 var()＋渲染层版）。**⑧验证**：typecheck 四包 0 错 · core **185 文件 / 2110 例** · `content:check` ✅ · `l10n:check` ✅ · `ui:rot-check` ✅ · `ui:tip-check` ✅ · `ui:theme-check` ✅ · `build` ✅。**⑨发布**：公告 `ann-theme-box-skills-20260922`「界面配色、货柜拆解与技能扩充」（船长批准，一条覆盖三件事）。**⑩面板透明度一条销档**：星云底图每次启动随机抽 ⇒ 面板 0.88 下对比度会随"抽到哪张图"漂移（实测极端 1.2:1 ↔ 9:1）；我一度改到 0.94/蒙层 0.72，但那是船长 2026-09-10 选定的「方案A」，按 §5.2 已改回原值，船长裁示**维持现状、销档**。
- 2026-09-22：**货柜拆解改用「存档种子 ＋ 第 N 次拆解」的独立子流（船长令 · 一号 · main · 已推送 `fd409dd9` · 公告已发）**——**①船长原话（照抄）**：「**所有货柜的开启，能否采用随机数种子固定？**」→ 四答（目的 **防刷** · 种子按存档种子＋序号现算 · 范围 **只这四种货柜** · 老档首次拆解补种写回）→ 关键裁定：「**允许玩家通过改变开箱顺序来微调。**」→ 定案「**照此开工 ＋ 全局混算**（四种货柜共用一条拆解计数）」。**②最终口径**：种子 = `hash(存档种子, 第 N 次货柜拆解)`，`N` = 全局货柜拆解计数（安全/图纸/贵重品/军用**混算**，存存档、老档缺省 0）；`wormholeUnboxRoll` **不再消费 `state.rng`** ⇒ 开箱前去跑商/打架/触发事件都**不影响**本箱结果（防刷成立），读档重开 `N` 回到同一值 ⇒ 同一箱结果相同（**刷不了**），而**先开哪一箱由玩家决定** ⇒ 同一箱落在不同 `N` 上开出不同东西 ⇒ **顺序仍可微调**（船长要的）；一箱多件（军用 1~3 件 / 贵重品 10~20 件）由同一条子流派生 ⇒ **整箱固定**。**③落码**：`core/state.ts` 的 `RngState.box?: number`（**可选 ⇒ 零迁移**）· `core/save.ts` 按可选字段读写（**老档不带就不写回**，保持"存档往返逐字节一致"）· `core/wormholeSalvage.ts` 的 `wormholeUnboxRoll` 改走本地子流并把计数推进放在函数内（与既有 `nextRandom` 改 `rng.count` 同风格 ⇒ `industry` 调用点**零改动**）。**④相比初版作废两条**（因船长裁定而简化、记录防再犯）：~~序号落在"那一件货柜"上~~（不需要货柜实例身份）· ~~老档懒迁移~~（计数器从 0 起即可）⇒ **存档结构零改动、堆叠语义不变**。**⑤用例 5 条**（`packages/core/tests/box-seed.test.ts`）：同 (种子,N) 逐字同结果 · 不同 N 结果会变（60 个序位 >3 种）· 拆解**不推进全局流**（且"先干别的 100 次随机"后结果仍一致）· 四种货柜混算一条序列 · 结果仍落在该族池内（池与权重未动）。**⑥被既有契约逮到一次**：第一版让存档读回时**总是写 `box: 0`**，破坏"存档往返逐字节一致"⇒ 改可选字段后通过（老契约有效，记一笔）。**⑦验证**：typecheck 0 错 · core **185 文件 / 2098 例**（含新 5 例）· `content:check` ✅ · `salvage:econ` 读数未漂移 · **`save:migrate` 77/77 份真档通过**（不倒退 · 不凭空完成 · 资产不变）· `build` ✅。
- 2026-09-22：**新增六条技能（工业 T4/T5 ＋ 战斗舰种操作 T4/T5）（船长令 · 二号 · d2 · 已合入 main ＋ 公告已发 `fd409dd9`）**——**①船长原话（照抄）**：「添加一个精炼系T4的精炼炉周期缩短技能，每级4%。添加一个制造系T5技能，缩短所有零件生产周期每级4%。添加战舰操作系T4技能，护卫舰操作…驱逐舰操作…添加战舰操作系T5技能，巡洋舰操作…战列操作…」；逐条裁定：闪避/命中加**百分点**（各 +2pp/级）· 单发伤害与容量**相对乘算** · 抗性只对**已有条目**相对乘算（上限 90%、没有抗性的层不动）· 舰种操作**只对主控在驾的那艘**生效 · 两条工业技能名 =「**炉温精调学**」（前置 炉心熔炼学）/「**零件流水线**」（前置 批量生产学）· 战舰四技能前置 = **武装舰操作 ＋ 装甲舰操作**。**②落值**：`furnace-precision` 炉温精调学（工业 `b-refine` r4，精炼炉周期 −4%/级 ⇒ 满级 ×0.8，**吞吐 +25%**）· `parts-line` 零件流水线（`b-craft` r5，零件类蓝图周期 −4%/级）· `frigate-ops` 护卫舰操作（r4，闪避 +10pp）· `destroyer-ops` 驱逐舰操作（r4，单发 ×1.25 ＋ 命中 +10pp）· `cruiser-ops` 巡洋舰操作（r5，单发 ×1.15 ＋ 已有抗性 ×1.1）· `battleship-ops` 战列操作（r5，护盾/装甲/结构 ×1.15 ＋ 已有抗性 ×1.1）。**判据 = 舰种 `tier`**（T1 护卫舰 / T2 驱逐舰 / T3 巡洋舰 / T4 战列舰），与按**类别**判的「武装舰/装甲舰操作」互不冲突、可叠加 ⇒ 技能总数 **82 → 88**，存档结构不变（v31）。**③接线点**：`core/industry.ts`（炉温精调学进周期乘区，与炉心熔炼学同乘区）· `core/manufacturing.ts`（`spec.partTier !== undefined` 才吃）· `core/combat.ts` 五处（容量乘链 / 已有抗性相对乘 / 闪避加百分点 / 单发乘链 / 命中加百分点）。**④技能树排布的新结构问题与裁决**：四条舰种技能是**全仓第一批"同书两条前置"**的节点 ⇒ 排布体检「每个前置都要挂得住 ＋ 子不回头」对两个父**结构上不可能**同时成立（一列只能承一个父：设父列 pA < pB，子要挂 pA 须 x=pA，而"不回头"要求每个子 x ≥ pB ⇒ pA ≥ pB，矛盾）⇒ 船长选「**乙**」：**数据与排布算法都不动，只给体检加"多前置合流豁免"**（`tools/skilltree-layout-check.ts`：子全是共享子的父跳过这两条，但**整族必须有一条向下生长的边**；独占子照旧严格判 ⇒ 单前置的书读数逐字未变）。**⑤验证**：单测"先红后绿"（抽掉三处引擎改动 ⇒ 6 条引擎用例全红）· typecheck 四包 0 错 · core **2105 用例** · `content:check` ✅ · `l10n:check` ✅ · `ui:rot-check` ✅ · `skilltree:layout-check` ✅（23 本书 / 88 格 · 全 0 处）· `save:migrate` 76/76 ✅ · `build` ✅。**⑥发布**：公告 `fd409dd9`「界面配色、货柜拆解与技能扩充」（船长批准）已写入 `announcements.ts`。
- 2026-09-22：**全流程模拟 renovate ＋ 三条目标收尾（船长令 · 二号 · d2 · ✅ 船长 2026-09-22 消档）**——**①船长原话（照抄）**：「**更新下全流程工具，进行一次全量跑通测试，设置几个目标：完成所有悬赏，虫洞的成就完成，赚到10亿。**」＋追问两答（虫洞成就 = **六枚里程碑**：层深 2/3/4/5 ＋ 击破层末守卫 2/4；跑法 = 默认快进 `debugQuick`）。**②三条目标最终读数（30 天档 · 真流程不直接改状态）**：`bounties` **20/23**（⬜）· `whach` **全拿**（层深 5 · 守卫 47 · 第 20.03 天）· `isk1b` **第 16.00 天**（✅）。**③口径核实（别再改错）**：目标文案写"27 张卡"，数据实测 `ctx.anomalies` 共 **42 张 = 可见 23 ＋ 隐藏 19**；"27" = 23 张悬赏 ＋ 4 张隐藏遭遇卡（`enc-pirate-1..4`）——**遭遇卡不走 `completedBounties`** ⇒ 工具判据 = **23 张可见卡**是对的。**④本轮累计修掉 21 处缺陷**（每处都有实测证据）：策略层 4 处（`ensureFlagship` 换驾不配装 ⇒ 炮全装在采矿艇上；战力打分口径错 ⇒ 改用 `tier×100 + 槽位×2`；升级流程跑不到；只装驾驶船）· 虫洞专项 8 处（**最要紧 = 引擎 `buildEvalState` 只复制一艘船** ⇒ 拿它跑整队 `startFleetBattleFor` 时另外 3 艘不存在、4×T3 满配也"第 1 层打不过"，新写 `buildFleetEvalState()`；另新增 `ensureWhFleet()` 凑齐 4 艘 T3 等）· 编队质量与政策 7 处 · 关掉整条目标的两个盲区 2 处。**⑤工具**：`npx tsx tools/playthrough-sim.ts --goal bounties,whach,isk1b`（默认快进；`--max-days 30` 等档位照旧；旧档 `boss`/`tril`/`collect`/`all` 保留）——报告含三目标逐项读数、未达成时**还差哪几张卡**（威胁/声望门槛/实时预估胜率）、虫洞逐层读数、编队战力明细、进洞门未过原因、悬赏战报节选。**⑥验证边界**：本轮只改 `tools/playthrough-sim.ts` 与 `docs/design/**`（未碰 `packages/**`、`apps/**`）；全程走真流程。**⑦第 3 条目标（悬赏收全）已定性为配平问题**并成文上报 ⇒ 船长 2026-09-22 裁定**消档**（见下条）。
- 2026-09-22：**悬赏最后 3 张卡 = 配平问题（不是 bug）· ✅ 船长 2026-09-22 消档 = 接受 20/23 现状**——**结论**：虫洞「完成所有悬赏（23 张可见卡）」与「虫洞六枚里程碑（层深 5＋守卫 4）」在"单主控出征 ＋ 编队上限 4 艘"前提下**互斥**；三轮共**五种策略方案**实测**无一能同时达标**（A 全炮塔装：20/23 ＋ 虫洞 6/6 ＋ 10 亿第 16.0 天 ✅；B 主控全换导弹：22/23 但虫洞掉到 2/6；C/D/E 更差）。**机制根因（已定位到代码，非工具缺陷）**：① **射程墙**——剩余 3 张里两张是 `kite` 战术，引擎的敌方期望距离 = 「跟随我方最大射程」（`combat.ts` 的 `tactic === 'kite'` 分支 `mainMax × 0.95`）⇒ 只装动能炮塔 MK3（7,350 m）时敌停在约 **10,102 m**，引擎写「⚔ 无法交战：一炮未发…已脱离交火并返航」（脱战不写胜负 ⇒ 报告里"出征 N 次、零结局行"）；够得着须上导弹 MK3（14,880 m）或激光 MK3（10,500 m）。② **火力墙**——虫洞层末守卫硬拼伤害，而远射武器"伤害/秒/槽"更低（**炮塔 MK3 1.745 ＞ 激光 1.05 ＞ 导弹 0.915**）⇒ 换远射后虫洞立刻 6/6 → 2/6。③ 两个目标**共用同一批船**（虫洞编队上限 `WORMHOLE_MAX_SHIPS = 4`，本模拟能稳定养出的满配就是 4 艘 T3）⇒ 单主控没有余力再养"远射专用队"。**船长裁决**：三条方向（提编队上限 / 给 kite 留条够得着的路 / 接受现状）中选**接受现状**（按 20/23 交付），**不调平衡**；「想全拿就得取舍」视为设计意图。
- 2026-09-22：**采矿 vs 精炼 速度配平 · 读数入库（船长问题 · 二号 · d2 · ✅ 船长 2026-09-22 消档 = 不改数值）**——**船长原话（照抄）**：「我打算添加新技能，用于平衡采矿和精炼的速度。以鲸王+3MK3采集器满技能作为满配标准。现有的精炼炉应该加强多少才能达到 2 精炼炉搭配 1 鲸王的挖掘速度？」。**工具**：`npm run balance:mine-refine`（`tools/mine-refine-balance.ts`，正式入库；读数存档 `tools/_ui-artifacts/mine-refine-balance.txt`）。**核心读数**：**没有一个单一倍数能同时配平所有矿**——采矿速度**几乎不分矿种**（满配**纯挖掘** 普通 **195,353** / 低品级 **253,588** / 气冰 **244,059** 单位/时），而**往返把有效到货压到 ~1/3 且三档趋同**（**79,071 / 87,724 / 88,277**）；精炼侧按矿种**差 6 倍**（周期 10s~60s、批容恒 100）⇒ 同样"2 台 AI 炉（alpha）"：低品级**已过剩 1.51×**（132,597 vs 87,724）、虚空母矿**还差 ≈4 倍**（22,100 vs ~88,000）。**三条可选路线**（①一条全局技能/参数 ②收窄精炼周期的矿种跨度＋一条小幅全局技能 ③让采矿速度也分矿种）＋ 一条口径提醒（**配平基准算不算往返**：按纯挖掘要 ×1.9~4.4、按有效到货只要 ×0.66~1.2，差 2~4 倍；建议按**有效到货**）。**船长裁决**：**消档**（不改数值、不加新技能；读数与工具留档备查）。
- 2026-09-22：**通讯本地化（三号 · verify-activity-win → main · 船长令「你继续本地化」）**——
**①读数**（`npm run l10n:scan` · 英文语境真档）：通讯页 **49 → 0 处**（三批做完：发件人/主题行/送达时间 → 立场片/类型片/跳转说明 → 正文）。
**②病根**：通讯文案在**数据侧**——`CommsMessageDef.subject`（`firstTaskMessages.ts` 13 封 + `messages.ts` 17 封）与 `commsFactions.ts` 的势力/部门 `name` 都是中文，core 还把发件人拼成「势力名 · 部门名」（`comms.ts` 的 `resolveCommsSender`）再交给界面。
**③做法（data/core 一个字节没动）**：新增**渲染层单点** `ui/commsText.ts`——`commsSenderText()`（按**部门中文名**映射，13 条）、`commsSubjectText()`（按**消息稳定 id** 映射，30 条）、`commsClockText()`（用 core 导出的 `COMMS_DAY_MS` 自己算"第几天 + 时:分"，句子走 `ui.comms.044`）；查不到一律原样回落（新剧本漏登记看得见）。词条 **44 条**落 `ui.comms.001~044`，专名以游戏既有英文为准（Cinder Sector / Redring Corridor / Auro Waste Ring / Deadarmy / Low-sec，均取自 `packages/data/src/l10n.ts` 既译）。接线两处：`panels/CommsReader.tsx`（右栏 + 送达弹窗同源）与 `pages/CommsPage.tsx`（左栏列表）。
**④正文（第三批）**：「第一次」13 封任务信的 **39 行正文**英译（`ui/commsText.ts` 的 `COMMS_BODY_EN`，按 `isEn()` 取用；**行数与中文逐行对齐**，不符则整段回落中文、绝不错行）；**物品/技能/舰船名一律用游戏既有官方英文**——用临时脚本从「内容 id → `packages/data/src/l10n.ts` 英文表」导出对照后逐条核对（Peridotite / Tritanium Alloy / Silvervein Supermetal / Reinforced Mining Laser MK1 / Basic AI core / Skipjack-class Frigate / Flyingfish-class Courier / Sandcat-class Mining Corvette / Civilian Hull Repair Unit / Civilian Repair Kit / Accelerated Learning / AI Core Operation / Reprocessing / Refining）；强调段落的高亮判定改为**比对中文原文**（英译整段替换后不能拿译文比对，否则高亮丢）。
**④之补（第四批）**：协会侧**8 封短札正文**（welcome / survey-memo / industry-shift / refinery-note / salvage-crew / site-thanks / redring-outpost / first-ship，共 27 行）＋ **13 条部门与势力简介**（悬停说明 `fromBrief`，`commsBriefText()`，中文即键、已声明 `l10n-keep`）；探针 `l10n:scan` 新增**通讯逐封点开扫**（列表页只显示主题/发件人/时间，正文要选中才渲染——只扫默认那封会漏其余信件）。**逐封读数**：28 封已投递信件里 **21 封正文全英文**；剩 **7 封长设定文**（Archive reboot 12 行 · Low-sec 5 行 · wormhole unlock 5 行 · ambush-retreat 4 行 · Auro 巨构 4 行 · Deadarmy 蜂群 4 行 · Cinder 3 行 = **37 行**）留待下一批。
**⑤闸门**：typecheck 四包 0 错 · `l10n:check` 未译读数仍 **0** · core 184 文件 / 2093 例全绿 · `content:check` ✅ · `ui:rot-check` ✅ · `build` ✅ · `docs:index` ✅。
- 2026-09-22：**手册本地化（三号 · verify-activity-win → main · 船长令「先进行手册的本地化」）**——
**①读数**（`npm run l10n:scan` 的手册专项 · 英文语境真档）：**314 → 6 处**（物品图鉴 56→6 · 装备图鉴 140→**0** · 舰船图鉴 43→**0** · 蓝图图鉴 35→**0** · 技能速查 40→**0**）；**玩法说明（指南正文）本来就是 0**。
**②病根全是「接线」，不是数据翻译**（故不用成批造译名）：a) `panels/Handbook.tsx` 的 `kindName` / `slotName` / `roleName` 原先**直读 core 的中文名表**（`ITEM_KIND_LABELS` / `SLOT_LABELS` / `SHIP_ROLE_LABELS`）⇒ 卡片副标题、槽位/角色 chip、详情行、筛选档一起漏；b) 分组顺序表 `orderOf()` 把单点表的 **`id` 丢掉了**（`{ key, label }`）⇒ 渲染处只能回落中文 label；c) 蓝图按舰级的档是**拼接标签**（``label: `${s.label}蓝图` ``，无 id——静态扫描看不见）；d) `ui/shipInfo.tsx` 的推进器周期后缀**原样吃 core 的中文整句**（`thrusterCycleText` / `thrusterCycleFullText`）。
**③修法**：新增 `ui/labelsText.ts` 的 **`slotText`**（槽位→id 映射 15 项：采集器/打捞器/推进器/目标锁定**复用既有 id**，其余 11 项新登记 `ui.labelsText.049~059`）与 **`shipTierText`**（舰级整档模板，复用 `ui.labelsText.014~018`）；`Handbook` 的 kind/role/slot/技能分类一律改走 `kindText` / `shipRoleText` / `slotText` / `skillGroupText`；`orderOf()` 原样带上 `id`/`idParam`、渲染处走 `subText`；补 5 条**舰级蓝图整档模板** `ui.labelsText.060~064`（给拼接档补 id）；推进器周期改为**取 core 的秒数 ＋ 渲染层模板**（新登记 `ui.shipInfo.182/183`，core 一个字节没动）。
**④剩下 6 处**：物品图鉴里「Cargo Expander MK2**蓝图碎片**」这类**碎片物品名**——名字在数据侧拼出来（`ItemDef` 无 `nameId`），与 AI 核心等物品名同属"**数据侧命名**"批，本批未动（已记在挂账里）。
**⑤闸门**：typecheck 四包 0 错 · `l10n:check` 未译读数仍 **0** · core 184 文件 / 2093 例全绿 · `content:check` ✅ · `ui:rot-check` ✅ · `build` ✅ · `docs:index` ✅。

## 封存卷索引（更早批次已移入 `docs/archive/`，点开即读）

| 卷 | 收录日期 | 条目 | 体量 |
|---|---|---|---|
| `docs/archive/roadmap-2026-09-04.md` | 2026-09-04 | 28 条 | 13 KB |
| `docs/archive/roadmap-2026-09-05.md` | 2026-09-05 | 12 条 | 12 KB |
| `docs/archive/roadmap-2026-09-06.md` | 2026-09-06 | 1 条 | 2 KB |
| `docs/archive/roadmap-2026-09-08.md` | 2026-09-08 | 18 条 | 15 KB |
| `docs/archive/roadmap-2026-09-09.md` | 2026-09-09 | 45 条 | 46 KB |
| `docs/archive/roadmap-2026-09-10.md` | 2026-09-10 | 71 条 | 169 KB |
| `docs/archive/roadmap-2026-09-11.md` | 2026-09-11 | 81 条 | 237 KB |
| `docs/archive/roadmap-2026-09-12.md` | 2026-09-12 | 76 条 | 173 KB |
| `docs/archive/roadmap-2026-09-13.md` | 2026-09-13 | 95 条 | 265 KB |
| `docs/archive/roadmap-2026-09-14.md` | 2026-09-14 | 124 条 | 397 KB |
| `docs/archive/roadmap-2026-09-15.md` | 2026-09-15 | 38 条 | 132 KB |
| `docs/archive/roadmap-2026-09-16.md` | 2026-09-16 | 40 条 | 107 KB |
| `docs/archive/roadmap-2026-09-17.md` | 2026-09-17 | 16 条 | 36 KB |
| `docs/archive/roadmap-2026-09-18.md` | 2026-09-18 | 4 条 | 11 KB |
| `docs/archive/roadmap-2026-09-19.md` | 2026-09-19 | 1 条 | 2 KB |
| `docs/archive/roadmap-2026-09-20.md` | 2026-09-20 | 32 条 | 73 KB |
| `docs/archive/roadmap-2026-09-21.md` | 2026-09-21 | 5 条 | 19 KB |
| `docs/archive/roadmap-2026-09-22.md` | 2026-09-22 | 11 条 | 24 KB |

> 上面每卷都是**原文冻结件**：不重写、不摘要、不追加。`docs/archive/README.md` 另有"已办结待办单"卷。
