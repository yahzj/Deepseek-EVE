# 后续工作流备忘（Roadmap / Backlog）

> **2026-09-15 改版**：本文件只留三块 —— **① 待办活面**（置顶单 / 待办段 / 交接开放项）· **② 最近 20 条批次条目（滚动窗口）** · **③ 封存卷索引**。
> 更早的批次条目已**原文封存**进 `docs/archive/roadmap-YYYY-MM-DD.md`（一字未改，含船长原话照抄）——查老决策先看索引表、再 grep 对应卷。
> 封存动作 = `npm run docs:seal`（滚动窗口之外自动进卷，幂等；`--dry-run` 先看计划）· 封存区规则见 `docs/archive/README.md` · 全仓文档总索引 = `docs/INDEX.md`。

﻿ 后续工作流备忘（Roadmap / Backlog）

---

## 置顶：船长测试反馈改动单（2026-09-04 加入，按列序逐条推进）
**执行规则**：批次 A = 无歧义可直接实现；批次 B = 需先给设计总结等确认（大系统/结构级/规则数值）。
- **T1~T11 全部 [✓] 办结**（2026-09-15 逐条核对后原文移出）⇒ 全文见 `docs/archive/roadmap-todo-done-20260915.md`；本节留段头，新的船长反馈单另起一段。

## 未完成功能清单（2026-09-20 建 · 船长令「标注未完成」）

> **这份清单是什么**：登记**已开工但未完成**或**只有预留接口**的功能。进本清单的功能，
> 其文案**不计入本地化工作排队**（规则见 `docs/development-conventions.md` §十一之二）——
> 改动处统一留记号 **`⟪未完成 YYYY-MM-DD⟫`**，`grep -rn '⟪未完成'` 一次列全。
> **收口**：功能完成并合入 main 时，从本清单删条 ＋ 删掉代码/文档里的记号。

| 功能 | 状态 | 挂起原因 | 恢复入口 |
| --- | --- | --- | --- |
| （暂无） | — | 成就系统两批均已于 2026-09-20 完成并合入 main（见下方 D 段条目） | — |

## 2026-09-20 一号交接开放项（原工作文档里的「待你定」· **✅ 已全部结清 2026-09-22**）

> ✅ **本节已销账（船长 2026-09-22：「全部可以消档，都已经完成。」＋同日逐条裁定「1甲 2甲 3甲 4甲 5甲 6甲 7不动 8不动 9保持 10不动 11不动 12不动 13不动」）**：
> **1 / 4 / 5 三条已落码**（详见 `docs/roadmap.md` 最近批次的 2026-09-22 条目）；**其余 10 条按船长裁定结清**（不动 / 保持）。
> 下列原文只作过程留档，**不再有未决项**。

> 2026-09-20 批的 22 份工作文档已按 §8 归档（关键内容并入本文件与 `docs/glossary.md`）。
> 下列各条**当时上报但未获答复**，原文要点留在这里；你要动哪条，点名即可（多数是一句话改一处）。

- **「寻找人类」发布的日志口吻**：`core.onboarding.001` 仍写「先在这座城市活下去，再慢慢打听」——
  那是按"序章刚结束"写的；发布点已挪到「第一次」顺序段（前 11 条）完成之后。换不换口吻（要动中英两列）？
- **老公告追改**：`packages/data/src/announcements.ts` 里那条历史公告写着「长期目标「寻找人类」已发布」
  （属历史记录，未追改）；要不要另发一条说明发布时机的公告（公告需船长审核后才入库）？
- **「重要任务」页未发布态**：现在什么都不显示（只有当前那条「第一次」卡与副站卡）。要不要加一句占位提示？
- **老档补采集器**：2026-09-20 奖励前移后，**已完成「第一次扫描」但还没做「第一次采集原矿」**的老档
  拿不到采集器 MK1（市场 31,200 ISK 有售 / `bp-miner-1` 可造）——要不要给这类档补发一次？
- **AI 打捞被拒文案**：`core.ai.008`「该副船没有打捞器…」要不要也补一句"去「装配」页"？
  （主控那条 `core.salvaging.003` 已改，副船这条未改。）
- **进洞 vs 长途运输**：现在进洞**自动停运** ＋ 两段确认；要不要改成像其它主控活动那样**直接拒绝**（先停止再进）？
- **「第一次采集原矿」的跳转按钮**：仍指「星图 · 矿带开采」；任务正文已引导"先去舰船页换采矿艇"
  ⇒ 要不要把按钮改成「舰船」优先？
- **虫洞深层盘重建**：`wh-grid-deep-save` 那批修好了"读档丢盘"，但**已经中招的那一层不会自动回来**。
  可选路径 = 读档时若 `run.grid` 缺失而 `run.seed` 在（盘面按 `(seed, depth)` 确定性生成）⇒ 用
  `wormholeMakeGrid` 重建同盘；代价 = 该层已探索进度重置、且当时 `scanBonus` 未随档 ⇒ 可能与原盘差一圈。
- **谜质科技 T4「工业多核调度」定价**：现值 30/60/90/120/150 谜质（合计 450 谜质 ＋ 18 亿信用点）是本树最贵的一件。
  三个可选：① 降步长到 20/30/40/50/60（200 谜质）② 改 3 级（与"最高 5 级"不符，需改口径）③ 保持现值。
- **`tools/` 纳入 typecheck**：存量清单已出（134 行 / 19 文件，其中 ~40 条真错、~46 条 strict 噪音）。
  三条路：全纳入（先清存量）／白名单纳入（常跑的十几个）／只加运行期护栏。要不要开工？
- **死工具 `tools/content-validate.ts`**：import 了不存在的 `ITEM_KIND_ORDER`，且**没挂任何 npm script、全仓无人引用**
  （已被 `content-check` 取代）⇒ 建议删除。删不删？
- **里程碑"彻底追溯"**：`aiCoreKinds`（某类核心被花光后现算会少）与 `whBossClears`（跨趟累计无留痕）
  目前只做到**下界**追溯；要精确就得**新加存档字段**（属改存档结构）。
- **成就那条"里程碑"的指代**：船长令「里程碑都加入追溯检查」已按**18 枚里程碑成就的六个计数键**落地；
  若本意是**里程碑任务链（次数链）**，说一句我按同一张推导表接上去（可推导的只有 `scan` / `skills` 两条）。
- **待迎战期间"再捞一次"被禁**（`wormholeActivateAt` ＋ 按钮双向）：若想保留"先把剩下几堆捞完再打"的自由，一句话放开。
- **链条耗时比 4:2:1 方案**（见下方同日条目）：**未实行**——方案全文要点已并入该条；要落就说一句"落 4:2:1（锚采矿）"。

## A. 体验走查 + 打磨（建议优先，收口"内容感"）
- [ ] A1 船长通读悬赏文案与界面观感（2026-09-05 注：需船长走查后按条关闭；2026-09-04 首轮已走查并清理过一批）
- [ ] A2 信息密度检查：星图（安全色 + ⚔悬赏徽标 + ′标称 + 名称是否拥挤）、悬赏列表排序/卡片密度、矿带卡布局细节
- [ ] A3 定版存档策略（2026-09-05 注：需船长确认正式档基线后关闭）

## B. 玩法闭环延伸（需确认流程的大方向）
- **本段 4 项全部 [✓] 办结**（2026-09-15 核对后原文移出）⇒ 见 `docs/archive/roadmap-todo-done-20260915.md`。

## C. 战斗与舰船系统余项
- [ ] **C3 V18 立项：EVE 式高/中/低槽重组**（2026-09 船长拍板"V17 收尾、高低中槽立项 V18"；
      **2026-09-04 交接：实施由大鲸鱼二号负责**，一号不再触碰；**2026-09-04 设计总结已确认**
      （docs/design/v18-slots.md：复数安装/数量制/槽位映射/Q4 布局草案 19 船表 + 高槽无人机
      家族 6 件——甲板扩展/战术导控，装备 48→54）——**V18A 实施已完成（2026-09-04，core 301 tests）**；
      **C4 校准轮（多炮/多矿枪/无人机件复核）与本里程碑联动，另行收尾**）：
      船体槽位布局按 role 族差异化（武装多高槽/装甲多低槽等）+ 模块归属高/中/低 +
      槽位数量/布局制（**炮尺寸制已随 V18 口径取消移除，无尺寸位**——变更记录见下）；
      fitted 结构迁移一次做干净（存档 17→18；旧六槽原位映射 high=[炮,矿] mid=[盾,推] low=[甲,货]）；
      V17 参数（乘入抗性/CPU 档位/推进代价）与槽制正交、届时零返工
      （见 docs/design/v17-modules.md §预留；v10b-combat-data.md 原"留战斗阶段"的旧账在本里程碑清偿；
      2026-09 设计问答已收：Q1 复数安装 / Q2-final 数量制不加尺寸位 / Q3 槽位映射 / Q4 布局草案）
- [ ] **C6 无人机 vs 炮位 DPS 比例评估**（2026-09-04 船长确认 V18 时追加：**延后**——武器体系
      全面后（V18B 炮族差异化 + 导弹架之后）再评估无人机流与炮击流的高槽伤害比例；
      无人机件数值（+15/35/70 m³、+12/25/40%）为暂定初值，届时可调）
      **2026-09-10 评估完成（二号；`docs/design/c6-drone-vs-gun-20260910.md` + 正式工具
      `tools/drone-vs-gun.ts`，`npm run battle:drone-vs-gun`）**：王鲭级满配同船对比——
      名义 DPS 无人机流 = 炮流 **81%**（每高槽名义反而 121%），但有效 DPS（命中修正）**48%**、
      每高槽有效 **73%**；成因 = ①命中链缺口（无人机固定命中 0.6，火控/索敌对无人机无效；
      炮台 ≈0.98）②支援链缺口（稳定器/射速/索敌只作用炮台，炮流有效 +33%）③CPU 双计
      （装置 + 每架，D3 占 310/320）④高槽不产生本体火力。实战：威胁 96 穹顶守卫
      纯炮 100%/76s/58% vs 无人机 D3 0%/74s（被击毁）；加目标锁定 MK3 后 D3 升到 60%。
      情景推演：单靠"导控 +60% + 无人机 CPU×0.65"有效仅到 68%、硬卡 40%——
      **须动命中链/支援链才能拉平**。
      **2026-09-10 第一轮调整已实施（船长拍板）**：无人机命中/衰减下放机型本体
      （`ItemDef.hitRate/falloff`）——**蜂鸟/赤鸢/猎鹰：命中 75% 且不随距离衰减**（falloff 1）；
      **雷鸥哨戒：命中 110%（用意 = 抵掉部分衰减）+ 保留正常衰减**（falloff 0.35）；
      combat 读本体（旧档兜底 0.6/0.35）、命中公式零改动；UI 无人机详情/手册新增「放飞命中」行；
      新增 `tests/drone-hit.test.ts`（6 项）；glossary 增词条「无人机命中与衰减」。
      **改后实测（王鲭级）**：有效 DPS@2km 48% → **70%**（D3 184 vs 炮流 263）、
      每高槽有效 −27% → **+5%**；实战 D3 反超炮流（穹顶 **100%/65s** vs 76s、坟场 27s vs 45s、
      噬口 41s vs 50s），但无人流行残血偏低（散火击杀节奏慢；装目标锁定 MK3 后 35%→57%）。
      **待船长判断是否回调**（旋钮：导控 +40%→+25% / 三型命中 0.75→0.70 / 无人机每架 CPU 上调）。
      **2026-09-10 船长裁定：先保持现状**（回调未实施，数值维持本行口径），由船长实机实测后再定。
      **2026-09-10 船长追加：无人机专用舰高槽 −2**（梭鱼级 5→**3**、王鲭级 6→**4**；其余带机巢
      舰船不动）——`ships.ts` 两行 + 旧档由 repair 链尾件退库（不丢装备、清单保留）+ 新增
      `tests/drone-ship-slots.test.ts` 钉布局与旧档兼容；校准行 D1/D2 改三槽、新增 D2b
      （槽位吃紧后单件导控升 MK3）、D3 四槽恰好占满；工具与测试存档同步。
      **复跑结论（重要）**：削槽对**相对关系是净利好无人机流**——王鲭炮流 6 门→4 门
      名义 272→**183**（−33%），而无人机流 D3 本体只占 4 槽、名义 220 **不变**；于是
      无人机流/炮流名义比 81%→**120%**、有效 DPS@2km 比 70%→**105%**；
      实战王鲭 4 槽对比：无人机 D3 穹顶 **65s** vs 纯炮 104s、坟场 27s vs 68s、噬口 41s vs 76s。
      梭鱼 3 槽：无人机 D2b 有效 107 vs 纯炮 3×MK3 120，实战仍更快（穹顶 85s vs 157s，但 0% 胜）。
      原因 = 无人机流火力来自机群（受舱容/CPU 约束，不吃槽位），炮流火力随槽位线性下降。
      **若目标是削弱无人机流，杠杆是 CPU / 舱容 / 战术导控，而非高槽数量**（详见设计稿 §九）。
- [x] **无人机四型定位 + 「无人机可被击落」**（2026-09-10 船长拍板，**本批已完成**）：
      **① 四型定位契约已确立（已实施）**——侦察机闪避最高装甲最薄 / 战斗机全属性平均 /
      攻坚机血量最厚·单发高·闪避最低 / 哨戒机射程最远·单发最高（狙击）·血量与侦察机相仿；
      现有四型按此调整（**蜂鸟闪避不变 0.45**；赤鸢 0.40→**0.25**、血 34→40；猎鹰 0.32→**0.10**、
      血 66→**97**、抗性装甲向；雷鸥 0.25→**0.18**、血 105→**23**、抗性削到单条）。
      契约**可执行化**：新增 `data/droneRoles.ts`（`DRONE_ROLE_SPECS` 四档区间 + 逐机/跨类阶梯校验），
      `content:check` 与 `tests/drone-roles.test.ts`（7 项）双重守卫——**新无人机越档即被拦下**；
      glossary 增词条「无人机四型定位」；设计稿 `docs/design/drone-roles-20260910.md`。
      注：`DroneDefense` 目前**不进战斗**（只有详情/图鉴/手册显示），故本批**不改胜率、校准矩阵不变**。
      **②「无人机可被击落」已实施（船长选乙：永久损失/耗材流）**：敌方**近防炮**——**威胁 ≥ 60**
      的敌舰才装，每舰**每 0.5 秒判定一次**，随机挑一架**正在攻击的放飞无人机**（**哨戒机不被打**），
      按 `pdAcc − 机型闪避` 掷命中、命中按 `pdDmg` 走机型三层抗性，打空即击落并停火；
      **不看距离**（"毕竟你要飞过去"）、**不参与**敌舰对玩家的常规攻击、单场最多击落 50%；
      **战后从无人机舱清单永久扣除**（主控远征胜/败/撤退 + 遭遇战 + AI 副船全接线），
      事件日志 + 在线 toast 提示，重复清剿在本场战损过半时自动停环提示补货；
      手册/物品说明/装配页删除"不消耗、不被击落"并改为近防炮口径；测试 `tests/drone-loss.test.ts`（5 项）；
      新增调参工具 `npm run battle:pd-tune`（支持 `--acc/--dmg/--cap/--period` 传参试档）、
      `drone-vs-gun` 表 3 增战损列。
      **实测（满技能 5 种子，王鲭 D3 满编；合法且符合流派的配装）**：软卡交火短（27~41s）战损近 0；
      硬卡穹顶（96）约 **3.2~3.6 架且全是攻坚机**（哨戒机免疫）；硬卡仍 **100% 胜**——
      因编队火力核心（6 架雷鸥）不受近防炮威胁，压力在"打得更久 + 要补货"。
      战损估值占该卡奖励约 1.1~1.2%。
      **数值已确认（2026-09-10 船长）**：「如果没有特意说明，默认按 0.5/5 来算」——
      未特意说明的敌舰一律 **0.5 秒/舰判定 · 命中 0.5 · 每发伤害 5**（逐舰独立、上限 50% 保留）。
      **默认档实测（王鲭 D3 满编）**：噬口(80)/坟场(88) **0 战损**；穹顶(96) **2.2 架（全为攻坚机）**、
      战损估值 1.1 万 ISK ≈ 该卡奖励 **0.7%**；三卡仍 100% 胜——因哨戒机免疫且编队火力核心是雷鸥，
      压力落在"打得更久 + 攻坚机要补货"。调参参照档（0.45/7 → 3.6 架；0.30/12 → 3.2 架；
      0.55/3 → 0 架）见设计稿 §七。
      **2026-09-10 船长追加**：**非哨戒无人机全部被摧毁后，近防炮转而攻击哨戒无人机**——
      哨戒机的"豁免"变成有条件的（靠同队其它机型当肉盾）；已实施 + 测试覆盖
      （`tests/drone-loss.test.ts` 新增"非哨戒机全灭后转火"用例），重档实测可见效果
      （acc 0.5 / dmg 10：噬口与穹顶在攻坚机全灭后哨戒机开始被击落）。
      **2026-09-10 船长追加（无人机舰专属加成）**：无人机舰的 `powerBonus` 只喂炮台、对机群无效
      （王鲭 0.6 与长尾鲨同级却吃不到机群）→ **削火力 + 加专属**：新增船体字段
      `ShipDef.droneDmgBonus`（只乘入放飞无人机单发，与战术导控/作战学乘算）；**王鲭 powerBonus
      0.6→0.3 且 +12%**、**梭鱼 0.45→0.25 且 +8%**。实测：王鲭 D3 名义 220→**245**、
      同船炮流 183→**149**（−19%，实战 104s→120s）；梭鱼 D2b 132→142、炮流 126→109；
      近防炮战损 2.2→**1.4 架**（机群更强→交火更短）。测试 `tests/drone-ship-bonus.test.ts`。
      **2026-09-10 船长追加（机舱联动）**：**卸下「无人机甲板扩展」后，超出机舱的无人机也要卸下**——
      新增 `trimDroneLoadToBay()`（按清单逆序卸下、退回仓库、写日志 + 一次性提示），
      接在 `unfitAt`（UI 传 ctx）与 `repairDeprecatedModules`（旧档槽位对齐裁掉甲板件）之后；
      测试 `tests/drone-bay-trim.test.ts`（5 项）；手册「装配」条目补口径。
      **2026-09-10 船长追加（损坏与回收 + 无人机线技能）**：①**取消单场 50% 击落上限**——
      战斗内机群可 **100% 损坏**；②**战后回收**：损坏机体按 `10% + 8%/级（回收学）`、满级 **50%**
      **四舍五入**找回并**回无人机舱清单**，未回收部分才永久损失（日志/toast 写明 损坏/回收/净损）；
      ③新增**五条无人机线技能**（战斗组）：无人机打击学 rank4 单发 +4%/级、无人机耐久学 rank2
      三层血 +4%/级（满级 **+20%**）、**无人机强化学 rank4 三层血再 +6%/级（满级再 +30%，与耐久学乘算，双满 ×1.56）**、
      无人机回收学 rank3 回收率 +8%/级、无人机规避学 rank4 闪避 +2%/级（满级 +10%）；
      数值常量集中在 `combat.ts` 的 `DRONE_SKILL`。测试 `tests/drone-skills.test.ts`（6 项）+
      `drone-loss.test.ts` 改口径；词典增词条「无人机线技能」。**实测**：默认近防炮档不变
      （软卡 0、穹顶 1.4 架）；重档（0.5/10、0.5/20）可见**整队 10/10 损坏**且胜率掉到 0~40%——
      取消上限后的真实风险与"耐久/强化/回收"技能的价值都在这里体现。
      **2026-09-10 船长二次修订**：耐久学**拆成两条**（rank2 基础档 + rank4 强化学进阶档，
      **乘算**叠加）；**其中一条满级改为 20%**——现行 = **rank2 耐久学 +20%（+4%/级）/
      rank4 强化学 +30%（+6%/级），双满 ×1.56**（越难练收益越高）；**规避学满级由 +20% 降为 +10%**（+2%/级）。
      **2026-09-10 玩家反馈修复（悬赏奖金口径）**：反馈"卡片赏金与实际到账不一样"——根因
      ① 结算带 ±15% 浮动（`rewardJitter`）而卡片展示名义值；② **赏金任务（窝点）卡漏乘
      "赏金猎手学"系数**。船长按甲案拍板：**取消奖金浮动**（rewardJitter 0.15→0）——展示=到账，
      唯一随机奖励 = 情报彩蛋 +10%（触发即写日志）；并修掉赏金任务卡展示漏项。
      测试 `tests/bounty-reward-display.test.ts`（4 项）；词典增词条「悬赏奖金口径（展示=到账）」。
      **口径修正（自检）**：早期工具给王鲭配了 3 中+3 低支援件（超出 2/2 布局）导致结论失真，
      已修正并统一为合法配装。
      表现层（击落演出/战报行）待三号对接，引擎侧字段契约见设计稿 §八。
- [ ] **C5 炮族差异化 + 导弹架**（2026-09 船长审核批注：同 MK 三系炮"只差弹药、无性格差异"——暂定稿可玩，**与导弹版统一做**；
      **2026-09-04 船长重定向：按伤害类型设计真武器形态（质量炮=动能 / 导弹=爆炸 / 激光=能量），
      旧三系同参炮 = 临时填充数据，逐批替换——V18B-1 导弹架 ✅ 已完成**（docs/design/v18b-missiles.md：
      追踪命中不随距离衰减的远程爆破 + 近盲安全射距；原高爆炮 1:1 迁移；爆破导弹弹药归位）；
      **V18B-2 激光炮 ✅ 已完成**（docs/design/v18b2-lasers.md：必中光束 + 距离衰减作用于威力
      （幅度减半 50%）+ 消耗能量弹药；能量炮/异星原型 1:1 迁移激光；per-gun 多键预载完成
      ——混装异型不再饿死；core 345 tests）：
      **V18B-3 ✅（2026-09-05 船长确认完成）** = 动能炮 → 质量炮形态（届时确认是否改名），并联动 V18 槽制
      （炮尺寸制已随口径取消移除，见变更记录）；
      v17b-weapons.md §已知限制已记录
- [ ] **C7 每族一艘新敌舰 ＋ 层 6「深渊档」深层卡（船长 2026-09-17「**先挂起吧**」· 一号 · **建议稿已随本条归档**）**——起因 = 船长「**打算给每个种族添加一个新敌舰，并添加一张新的深层卡（出现层数6），你有什么推荐（不用特意补齐全舰种，按照族设定推荐）**」。
      **①建议（六族各一艘 · 全部零引擎改动、只复用既有字段）**：A **劫掠跳帮舰 T2**（捕获网 ＋ 冲锋双持，把"钉住"从血 182 的电子舰搬到真突击位）· B **拾荒拆解舰 T2**（`repairPct 0.3`＋低速厚甲，**需加进 `repairPct` 白名单**）· C **螯颚撕咬者 T2**（射程 1~1,800m ＋ `blindDmgMul: 1` ＋ 2.0× 速度 ＋ 冲锋，反"距离门"）· D **静滞方尖舰 T4**（首选；`web` 字段参数改写＝周期性压制最近我方：机动 ×＋射程 −2,000）或 **陵寝主宰舰 T5**（撞"旗舰留给 E 族核心舱段"那句既有口径 ⇒ **首选 T4**，要开旗舰位另说）· E **无人机母舱段 T4**（纯机群 ＋ 警戒机 ×10 ＋ 后备机库 ＋ 机群射程 ×4 ＋ **速度 0**，必须带防空贴上去拆）· G **孵巢舰 T3**（蜂群 ×6 ＋ 鱼雷、无后备，守 G 族口径）；**F 族（遭遇巡逻）跳过**——数据里 0 舰级、只是"遭遇战模板族"、没有种族设定。
      **②层 6 三读法（推荐甲）**：**甲** 新增**「深渊档」（层 6 起）每族一张（共 5 张）**——与现有结构对称，上面 5 艘新舰正好做各自深渊卡主体（否则新舰无出场处；代价 = core 层档池加一条规则 ＋ 契约/用例）· **乙** 只加一张**层末守卫专用卡**（推荐 G 族「深渊封锁线」，代价最小）· **丙** 跨族动态编成（卡面是静态引用，要新机制 ⇒ 不推荐）。**层 6 的收益必须一起定**（否则"更深"只有难度）：推荐 **(b) 新增"深渊货柜"**（最高档一次性图纸 ＋ 原型件）＋ **(c) 层 6 起上调深档卡/货柜权重** 的轻量版；纯难度 (a) 不推荐。
      **③待船长裁定 5 件**：六舰收不收（可逐条改）· D 要不要开 T5 旗舰位 · 层 6 走甲还是乙 · 层 6 收益选 (a)/(b)/(c) · F 族跳过还是先给一句设定（它是谁 / 什么美学 / 什么威胁级）。
      **④节奏（定了之后）**：① 六艘新舰（纯数据 ＋ 契约/用例同步，`npm run foe:export` 的敌舰表自动跟随）→ ② 深渊档 ＋ 层 6 卡（含"深渊货柜"若要做）→ ③ 数值标定（跑 `npm run wormhole:econ` / `bounty:stats`，把逐层威胁 / 总血 / 总 DPS 读数摆给船长核）。
      **⑤全文**：原稿按 §8 归档删除，需要原文看 `git show f88ac64a:docs/design/foe-new-ships-deep-cards-20260917.md`（含六族现役 **25 舰级**的档位覆盖与**契约硬上限**表：A 只 1~3 · B 不配 T3 及以上 · C ≤4 且 T4 须登记进 `ALIEN_BEAST_SHIP_IDS` · G ≤4 · E 持 T5；以及"层档池 / 半径封顶 R=4 / 深档图纸货柜 = 层 5+ / 威胁 ×1.10"这些层档现状）。

## D. 经济/数值精调
- [ ] D1 balance（2026-09-05 甲案已做：体检工具扩"总资产口径"——钱包+舰队/已装模块+仓库/书架按市场常驻 base 折算，重跑 24h 三策略：钱包/总资产 A 934k/2067k、B 410k/1589k、C 312k/1499k；B/C 钱包低估约 1.7~2.2×，总资产差收窄至 A 的 73~77%，缺口主要来自技能训练时间与蓝图书沉没而非倒挂。是否需数值拉平待船长定目标）
  **2026-09-08 大改批后重跑（平衡-check，24h 30s 步长，同口径）**：钱包/总资产 A 原矿流 1,127k/**2,278k**（鲸吞级）、B 精炼流 577k/**801k**（掘洞级）、C 装备流 607k/**844k**（掘洞级；造 MK1/MK2）；B/C 总资产仅 A 的 **35~37%**（旧 73~77% 反转收窄）；双修贸易变体 24h 内技能未练成、与基础线同值。**状态（2026-09-08 船长定）：后续定位/拉平挂起——船长将调整技能体系（技能直接影响收益），待技能改完重跑再定目标；本行 09-08 数字保留为改前基线**。
  **2026-09-09 技能/工业大改后复跑（O3；工具修正后）**：总资产 A 原矿流 **2,293k**、B 精炼流 **2,338k**、C 装备流 **2,663k**（三线均鲸吞级；钱包 1,128/1,150/1,433k）——B/C 从 A 的 35~37% → **102%/116%**（技能投入有回报、装备流最优，A 原矿流与基线持平 +0.7% 未受损）；双修贸易变体仍 24h 内练不成、与基础线同值。**工具修正（balance-check.ts）**：船分级后沙猫/掘洞同 T1，原"目标 tier > 当前 tier"升级门槛卡死 12 万换掘洞（A 流伪影性卡在 89 万买不到鲸吞、总资产仅 91 万）——改按升级梯子内位置比较后恢复真实路径（三线均按 沙猫→掘洞→鲸吞 正常滚雪球）；里程碑/每小时曲线干净。**待船长定目标带**（B/C 已反超 A 是否到位，或需微调）。

## E. 基础设施
- [ ] E2 云存档 / 在线化：架构预留（决策 1/2/5），未启动；Phase 2/3 见 architecture.md

## 2026-09-08 一号交接开放项（来自临时交接卡 docs/design/handoff-2026-09-08-current.md，已按约定迁移后删除）
- [ ] **O1 组装机卡「手动 vs AI」两档净/h**：现为单一手动口径（二号 A 项）；手动/AI 基础核心档分列估算待船长拍板。
- [ ] **O2 奇货卖出侧溢价巡游**：挂价高于收购价线的奇货卖单仍可能被巡游采购按挂价成交（买入侧已 20L 收口，卖出侧未动）；处理前先问口径。
- [ ] **O3 balance-check 24h 策略复跑**：工业扩容（工位数大增）后未复跑；范围先问船长。
- [ ] **O4 顶部 AI 徽标计数与「执行中 N」偏差**：早期玩家反馈，根因未实证。
- [ ] **O6 二号船分级批次验收（已在 main）**：舰船尺寸分级 + 掠食者巡洋线 A1–A9；公告待审稿在 6b79a61（id 2026-09-09-ship-size-tier）未经船长批准不得写入 announcements.ts；巡洋 T3 数值/渠道声望待船长终审（涉二号分工，一号只读 ship-size-tier-rework.md）。
- 说明：O5 数值文档分层 = 现数值以 docs/design/manufacture-refine-worker-econ.md 与 docs/glossary.md 为准（refine-balance-50.md 为历史锚）；O7 真档验收状态 = 2026-09-08 引擎级实测探针 20/20 通过（临时探针已删），UI 目测项待船长开游戏确认（市场蓝图 chip/奇货低挂提示/AI 指挥中心 +N 工业/交付航线按钮与弹窗/工业页数值行/公告三条）。

## 2026-09-09 一号收尾开放项（收尾盘点后登记，待办照旧先读本段）
- 备忘（2026-09-09 自交接卡迁移）：**精炼净收益差异问题 = 船长延后**——差异来源 = 吞吐桶 ×8.7 × 矿价 ×37.7 ≈ 卖原矿差 328×，精炼净再 ×净率差；重开先回看 refine-cycles.md 头注与 content-csv 工作簿 items sheet。
- [ ] **P0-2「开战距离 / 先手」提案 + 预演（船长 2026-09-11 选定本批 =「先出提案 + 预演，审核后再落码」；二号）**——**本批零引擎、零数值改动，产出 = 提案稿 `docs/design/p0-2-engagement-20260911.md`（含全部实测读数）**。**①三条决定性事实**：(a)「够不着」= **距离拔河的胜负**——同一张蜃影、同一把 4×动能MK2，**带推进器 A0 = 100%/65s 闭合到 3.3km**，**仅去掉推进器 = 0%/140s、全程停在 9.99km、我开火 0.0**；(b) **教学卡「敌人一炮未放」与开战距离无关**——开局从 6314m 缩到敌人带内（1980/1500m）敌人**仍只放 0.2 炮、战斗缩到 1s**，敌人血抬到 **×12（264）** 也**依然 0 炮**（根因 = 交战距离停在玩家期望位 3.2km 而敌人带只有 2.2km ＋ 玩家先手一轮清场）；(c) **全局改法「开战距离 = 双方射程重叠区上沿」不成立**——全卡清点 **78 格中 77 格**被改动（含旧路径 20 张）⇒ 打穿「旧路径逐字不变」判据。**②病灶 A 实测（kite 卡，5 播种 1/7/13/29/51）**：蜃影 T48 的 A1/⑤/③ 三行现状 **0%｜140s｜我 0.0｜敌 136**；候选甲（可及性钳制：敌期望交距 ≤ 我射程×0.95）⇒ **20%/100%/80%**、我开火 28~40 次，但**连带把 A0 行从残 32% 拉到 58%**（= 船长刚裁定「保留」的那一行）；灰霾 T28 三行 **0%/600s ⚠触顶 5/5｜敌 584** ⇒ 甲后 **100%/36~59s**；赤潮 T34 三行 **0%/373s｜敌 364** ⇒ 甲后 **100%/41~64s**；**旧路径对照卡「幽灵舰信号」四行全 100%**（从未够不着 ⇒ 差异只来自舰级路径按"自身带×0.85"算站位）。**③候选四选一（甲乙丙丁）**：甲 全局可及性钳制 / **乙 逐卡覆写（复用既有 `rangeMinM/rangeMaxM` 旋钮，不新增字段；建议先做灰霾+赤潮、蜃影单独议）** / 丙 只做可读性（提前脱战 + 战报写明"够不着" + 卡面预告交距）/ 丁 不动；戊（全局改开战距离）已实测淘汰。**④顺带发现（建议先修）**：`battle-calibrate` 的**敌开火(头目/杂鱼)**列用 `b.fx.slice(seen)` 计数，而 `fx` 是 **48 条环**（`pushBattleFx` 超长丢最旧）⇒ **环满即停、长战全漏**：同格真值 **584.0**（头 145/杂 438）↔ 工具法 **48.0**（漏 92%）、蜃影 A1 136 ↔ 48、蜃影 A0 54.0 ↔ 43.2（= 蜃影稿 §2.1 记的 10.8/32.4 正是此口径）；**我开火列不受影响**（工具用 `stats.meShots` 权威累计）；修法 = 按 `BattleFx.seq` 续读（一行级）。**⑤四问待裁**：病灶 A 走哪条路 / 若走乙点名哪几张卡 / 要不要一并修工具漏计 / 教学卡「一炮未放」是否单列。**⑥验证**：本批**不改代码**，故只跑读数工具（`npx tsx _probe-p02*.ts`，两个探针**用完即删**，`_` 0 个）；提案落码时再走四连 + 重跑 A0~A3 与六组。**改动全在 d2 工作树，未合入 main、未推送。****【提案 · 未落码 · 待船长裁决】**
- [ ] **★★ 挂账①「低威胁卡敌人一炮未放」+ ②「名义火力 / 实际火力脱节」（2026-09-11 船长要求处理；二号）**——**①实测形态**：边境/碎晶（威胁 12/20）在 A0/A3 参考行**敌人全程 0 开火**（我开火 11~12 次、18~25 秒清场）——根因 = 敌军自带带太短（快艇 1~1883、护卫舰 326~4275），而玩家参考行站 3.2 km；**候选**：甲 逐卡给敌军**加宽射程带**（让其进入自己的交战带）· 乙 逐卡**开战距离覆写**（把开局放进敌军带内，实测 1500~1980m 时敌人会开火 1.0~4.2 次）· 丙 给低威胁卡**先手**（开场即开火）。**②实测形态**：纸面单发与实际输出脱节（例：赤潮头目 beam 必中单发 21 ↔ 灰霾头目 fixed 单点 59，两者实收相近）；**候选**：甲 单发按「名义 DPS × 装填 × 实收命中」**反推**（纸面 = 实收，须重标全部 26 卡）· 乙 只做**登记口径**（在词典写明"纸面 ≠ 实收"，不重标）。**两份提案待船长选型后再落码。****【提案 · 未落码】**

## 2026-09-20 三号交接开放项（本地化尾巴 · 船长令「本地化交给三号」）

> 交接口径：**一号不再动本地化**；下面两条是 `npm run l10n:list` / `l10n:check` 实测出来的尾巴，
> 处理时按约定 **§十一之三「实现 = id 映射制」**（先取 id、再写文案、中英都进唯一表），合入前跑 `l10n:check` + `l10n:list`。

- [ ] **L1 谜质科技节点说明（`note`）英文缺失**（⬜ **仍未做，2026-09-24 复核**：`EN_MATTER_TECH` 仍只有 `name`，24 个节点的 `note` 在英文界面下显示中文）：`EN_MATTER_TECH`（`packages/data/src/l10n.ts`）目前**只覆盖 `name`**，
  24 个节点的 `note` 仍是中文 ⇒ 英文界面下说明显示中文（**既有缺口，非 2026-09-20 新增**；新增的 T4 节点
  `mt-industry-ai`「工业多核调度」同样只有中文说明）。要做成整树补齐，得先定口径：
  ① 数据侧补 `noteEn`（改 `MatterTechNodeDef`）；② 或改走 id 映射制（节点 id → 表里补 en，与 §十一之三 对齐，推荐）。
- [x] **L2 一号批次的界面字符串未接 id · ✅ 2026-09-22 已清场（2026-09-24 复核）**：下列快照条数当时实测（`npm run l10n:list`）：
  `panels/Shipyard.tsx` **13** · `panels/Industry.tsx` **19** · `ui/itemSubs.ts` **28** · `ui/Glyphs.tsx` **10** ·
  `pages/FitPage.tsx` **9**（Shipyard 目前只有搜索框接了 id）。**当批读数：`l10n:check` 未译 58 → 0**。
  **2026-09-24 复核读数**：渲染层含中日韩字符串字面量 **0** 条 · 已声明不译 `l10n-keep` **113 条**（全部是数据联合 key／形状槽键／探针／i18n 实现自身，逐条见 `npm run l10n:list`）⇒ **L2 已清场**，
  本行只作过程留档（详见「最近批次」2026-09-22 那条「筛选与标签页文案补齐」）。
- [ ] **L3 通讯消息整类没有英文 · 2026-09-22/23 已做完前三批，剩 9 条长设定文未做**（**2026-09-24 复核读数见行尾**）：
  `ctx.messages`（**31 条**：13 封「第一次」情报信 ＋ 开场简报 ＋ 协会侧短札）的 **主题 / 正文 / 前往提示**
  只有中文 ⇒ 英文界面下通讯页整封显示中文。判据：`packages/data/src/l10n.ts` 的 `localizeCtx` 覆盖了
  ships / modules / items / skills / blueprints / shipBlueprints / galaxies / belts / stations / commsFactions /
  matterTech / travelEvents，**没有 `messages`**。两条做法：**甲（推荐）**新增 `EN_COMMS_MESSAGES`
  （`Record<messageId, { subject; body: string[]; hintText? }>`）＋ 一个 `overlayMessages()`，在 `localizeCtx`
  加一行（改动集中数据层）；**乙**走 id 映射表（`msg.<id>.subject` / `msg.<id>.body.N`），更贴现行 id 制，
  但要改 `core/comms.ts` 与 `CommsPage` 的读取方式。⚠ 13 封情报信的**中文已于 2026-09-22 重写**
  （提交 `da8764ce`）⇒ 英文请照新版中文译，术语以 `docs/glossary-en.md` 为准。
  - **✅ 已做（2026-09-22/23 · 三号）**：发件人 13 条 ＋ **主题 31 条全齐**（`ui/comms.001~044`）· **前往提示 18 条**（`COMMS_HINT_ID`）·
    部门与势力简介 13 条（`COMMS_BRIEF_EN`）· **正文 22 条**（`COMMS_BODY_EN`）。做法走的是**渲染层单点** `ui/commsText.ts`
    （数据层与 core 一字未动；查不到一律回落中文）——即原「乙」思路的渲染层变体，**已在役，不必再改数据层**。
  - **⬜ 还剩 9 条长设定文 · 合计 45 行**（**2026-09-24 接手复核**，脚本比对 `COMMS_BODY_EN` 的 key 与中文数据）：
    `msg-briefing`(12) · `msg-lowsec-rules`(5) · `msg-wormhole-unlock`(5) · `msg-auro-megastructure`(4) · `msg-exile-swarm`(4) ·
    `msg-wormhole-nebula`(4) · `msg-ambush-retreat`(4) · `msg-pirate-capture-web`(4) · `msg-cinder-warning`(3)。
    **口径**：仍按 `COMMS_BODY_EN` 补段（行数与中文逐行对齐，不符整段回落中文）；**这 9 条是"整类没有英文"的最后一截**，
    做完通讯页即全英文。⚠ 另有一批**数据侧命名**未做（手册「××蓝图碎片」6 条碎片物品名，见下方 2026-09-22 手册条 ④）。

## 2026-09-16 二号交接开放项（虫洞战利品扩充批的尾巴 · 已推送 · 公告已裁定不发）

- [x] **W1 虫洞战利品扩充 · 公告（船长 2026-09-20 裁定：「都进行归档，不发送」⇒ 销账）**：
  待审稿 = `docs/design/archive/announcement-draft-20260915-wormhole-loot.md`（4 条要点已成稿 · 第三人称 · 无开发话语 ·
  口径来源照抄船长七段原话）；**本稿不发布**（改动已随代码上线，不另发公告；稿已归档留档）。
  原挂账口径（2026-09-15 船长「公告我让一号发」）随本裁定作废。
  数值口径仍以词典八之二/八之三为准（谜质 1 台 = 1 枚 · 70,000/枚 · 只收不卖 · 奇货档；
  奢侈品三档 10 万 / 40 万 / 200 万；贵重品货柜 2 格 ⇒ 5~30 件 · 箱价 365 万；军用备货柜 4 格 ⇒ MK3 1~3 件 ·
  箱价 280 万；遗迹出货固定 70% · 贵重品柜 50%；残骸堆 0.75%/堆；安全货柜 3000 m³ / 6 格 / 价 ×1.5）。
- [x] **W2 验收用测试存档（船长 2026-09-22 销账：不再配）**：`wh-loot` 一档——仓库预置 2× 贵重品柜 + 2× 军用柜 + 1× 安全柜（可立刻拆解），
  洞内一趟带 2 台谜质装置并站在残骸堆旁（一撤离即可看谜质折算与结算单新格）。配在 `tools/make-test-save.ts` + `docs/test-saves/`。
- 2026-09-26：**归档批：十一件已验收批次并入本窗口（船长「实测验收的都没问题了，可以归档」· 三号 · verify）**——**①船长原话（照抄）**：「**实测验收的都没问题了，可以归档。先把工作区追平**」。**②本批口径**：下列 11 条工作文档（原 `docs/design/*-20260924/20260925.md`，件件**已落码并过闸门**、状态栏一律「进行中 · 待船长验收」）按约定 §8 三步归档 —— 关键内容并入本窗口 → 删工作文档 → 重跑 `docs:index`。**③并入条目**（按下条起 11 条，逐条可追到原稿）：盔甲线降速 · 伤害飘字停留/字重 · 捕获网保留 · 重复清剿内联 · 奢侈品出售读数 · 派系活跃掉落读数 · 力场施法者口径（＋CPU 上调）· 星图扫描曲线 · 装备库排序 · 胜率预估重做 · 舰船工作台回写 · 市场蓝图「未学会」 · 稀有供给单件。**④顺带处置**：`docs/design/win-estimate-mc.md`（2026-09-09 立稿）与 `docs/design/shield-field-cpu-20260924.md` 两份旧稿**只就地标注「已被取代」**（按船长选定：文件保留，不删）。**⑤本次未并**：`docs/design/ui-refresh-20260925.md` / `ui-shell-layout2-20260925.md`（外壳与 UI 重置线）**保持隔离不动**——状态仍是「进行中」（④逐页校准进行中 · ⑤契约与构建未开），等船长单独议。**⑥本批为纯文档归档**：零代码、零数值、零存档、零玩家可见内容。
- 2026-09-25：**甲壳装甲线三艘降速（船长令 · 三号 · main）**——**①船长原话（照抄）**：「**陆龟级玳瑁级玄武级，三艘舰船的最大速度减半，移除一个低槽。**」→（本人撤回槽位那半句）「**不移除低槽了，陆龟级速度减半，玳瑁级速度降低到80，玄武级速度降低到100。**」。**②落值**（`packages/data/src/ships.ts`，三处各带 `⟪2026-09-25 船长令⟫` 留痕）：陆龟 T2 **240 → 120**（减半）· 玳瑁 T3 **230 → 80** · 玄武 T4 **170 → 100**；`git diff --stat` = 1 file / 3 增 3 删，**槽位 / 货舱 / 甲血 / 抗性 / 价格 / 工期一律未动**。**③连带面逐条已核**：「敌速口径契约」（`content:check`）基准船是**固定锚定**（长尾鲨 272 / 敏捷 0.54）⇒ 本次降速**不带偏**判据；舰船字段校验只要求有限正数；`armored-line.test.ts` 等**无速度断言**；三艘 `description` 是定性描述、不含速度数字 ⇒ 蓝图说明契约不涉；测试档与工具按装配/角色取材 ⇒ 不需重生成。**④⛔ 已报备船长的两点**：a) 改后**速度不单调** —— 陆龟 T2 120 ＞ 玄武 T4 100 ＞ 玳瑁 T3 80（同线 T3 反而最慢）；若原意是"越重越慢"，玳瑁与玄武两个数**疑似对调**（待船长一句话）；b) 三艘成**全游戏最慢**，与第四慢的 155 之间出现 35~75 断层（本批按船长给的数原样落地、未自行打折）。**⑤验证**：七道闸门全绿（typecheck 四包 0 错 · core 全量 · content/l10n/ui:rot/ui:theme · 桌面与网页构建）。**⑥归档**：工作文档 `docs/design/armored-line-speed-20260925.md` 已按 §8 删除（本条即其并入落点）。
- 2026-09-25：**战斗页伤害飘字：停留 900ms → 1500ms ＋ 字重 700 → 900（船长令「修复隐患」· 三号 · main）**——**①船长原话（照抄）**：「**修复隐患。战斗页面中，伤害数字的停留时间延长，使用粗体字。**」（前半句"修复隐患"= 上一批汇报点出的 `makeTestCtx` 共享 `balance` 隐患，已单列处理，并入下条"稀有供给单件"）。**②改动**：`POPUP_LIFE` **900 → 1500 ms**（`panels/battleViewCore.tsx`）——**一个常量驱动两处**（存活过滤 ＋ 元素内联 `animationDuration`）⇒ 飘得更慢也留得更久，且仍走**战斗时钟**（倍速跟着快、暂停即冻结，2026-09-24 口径未动）；`.app-bts-pop` 的 `font-weight` **700 → 900**（`styles.css`）。动画本体（`@keyframes bts-pop-rise`）未动，只是被拉长 ⇒ 淡出段同步变长约 1.7 倍。**③⚠ 一条实话（已报船长）**：字体面 `--wui-mono`（Cascadia Mono / Consolas / Courier New）**最重只有 Bold(700)** ⇒ **900 会回落到 700、视觉上与改前一致**；要"看起来更粗"得换带 Black 字重的字体面 / 加 `-webkit-text-stroke` / 抬字号（三选一，一句话即可）。**④验证**：typecheck 四包 0 错 · core 210 文件 / 2365 用例全绿 · `ui:rot-check` · `ui:theme-check` · `l10n:check` · `content:check` · `build` 全绿。**⑤归档**：`docs/design/battle-dmg-popup-20260925.md` 已删（本条即并入落点）。
- 2026-09-25：**劫掠捕获网：「没钉到人的网不算用掉」（船长报障 · 三号 · main）**——**①船长原话（照抄）**：「**装备劫掠捕获网的船攻击时，如果命中已经被捕获的船时，并不会触发，而是保留直到攻击了没有被捕获的船**」。**②取证（先查真相再改）**：引用捕获网的是 A 族「劫掠电子舰」（每场 1 艘）＋ **H 族「墨潮突击舰」舰级**，后者被 `ink-harass` 骚扰舰队 **×4** 等卡同时上场 ⇒ **一队 2~4 张网同场**，船长要的场景是常态。真引擎实测（`ink-harass` ＋ 我方 1 艘）：改前 **4 艘都记了"网已发放"**（`foeWebFired`），而实际被钉住的只有 **1 个**目标、蓝线只 1 条 ⇒ **现行行为不是"保留"，是"静默作废"**：`fireFoeCaptureWeb` **无条件先记 `foeWebFired` 再判"目标已被钉住"**，第 2 艘起第一发打到同一目标就早早 return（不推特效、不写日志、此后整场不再发放）；`git log -S` 证这个顺序**自 2026-09-16 落码起没变过**。**③改法**：把 `foeWebFired` 记账**挪到"目标已被钉住"那道判断之后** ⇒ 只有**真正钉住一个未被捕获的目标**才算发放，打空（目标已被别的网钉住）时本舰的网**留着**、下次开火再试；三条口径写进函数头注（整场只发一次 / 多艘不叠加 / **打空不算用掉**），`state.ts` 字段注释与调用点注释同步。**效果一字未动**（机动 ×0.1 · 关推进器 · 闪避归零 · 射程 −500m · 不看命中 · 击杀发动者即解除 · 不叠加）。**④验证**：`tests/foe-capture-web.test.ts` 新增两条 —— ① 4 艘网船打同一目标 ⇒ 改前 4 艘记发放、改后 **1 艘**（蓝线 1 条）；② **保留的网后来会发**（打沉首张网的发动者 ⇒ 目标重新"未被捕获" ⇒ 留着的网补发，`foeWebFired` 1 → 2）；真引擎复跑 `foeWebFired` = **1** 艘、与钉住数一致。**⑤归档**：`docs/design/capture-web-retain-20260925.md` 已删（本条即并入落点；口径亦属词典「劫掠电子舰 ＋ 劫掠捕获网」词条范围）。
- 2026-09-24：**重复清剿并入「④ 悬赏」每一行（删掉独立容器）（船长令 · 三号 · main）**——**①船长原话（照抄）**：「**星系详细的悬浮窗中，重复清剿明明在现有的悬赏处添加一个重复清缴的按钮就行了，为啥还要单开一个容器。**」＋方案回话「**按你推荐来**」（推荐 = 环做成**纯图标按钮**）。**②我上一版错在哪（如实记）**：早先按船长令落码时把「乙：列出候选卡」理解成"得有一个完整候选清单"，于是另开了容器 `BountyLoopBlock` ⇒ 同一个悬浮窗里**同一批卡被列了两遍**（上面 ④ 悬赏一份、下面重复清剿一份），违反约定 §九「新增内容优先参考同级相似项 UI」。**③改法**：a) **删掉整个容器**（组件 · 调用点 · 四条 `.app-map-loop*` 死样式）；b) **环按钮并入 ④ 悬赏的每一行**（`ico-loop`；环已开时转 `is-warn` 琥珀态、点击即停），列内容补 **冷却剩余秒 / 征战中 / 循环中**（新增 `ui.Expedition.436/437`，其余复用卡面既有文案）；c) **顶替确认保留**：给 A 开环再点 B ⇒ **行内**出警示条（`app-ga-switch-confirm`，照抄同文件"采矿中转战"的内联写法，**不新增弹窗机制**）；d) **列表可见性必须一起改（关键）**——原 ④ 悬赏把**冷却中/进行中**的卡过滤掉了，而"开环"恰恰最需要它们可见（core `setAutoLoopBounty` 只受 `autoLoopReopenBlockReason` 管、**与卡片冷却无关**）⇒ 改为**列出该星系全部悬赏**、只保留声望门槛过滤，空态分两句。不改这条就会出现"打完那张卡从列表消失、环还在跑却看不到也停不掉"。**④状态与开关唯一性没变**：仍只有 T8 那条唯一入口 `engine.bountyLoopAt()`，**不新增状态、不改存档结构**；停用 id `ui.Expedition.427~433` **不删留档**并就地写明缘由（计入"未接线条目"、非死引用）。**⑤验证**：typecheck 四包 0 错 · core 203 文件 / 2309 用例全绿 · content/l10n/ui:rot/ui:theme 全绿 · 残留自查 `BountyLoopBlock` 与 `.app-map-loop*` 代码内**已无引用**。**⑥归档**：`docs/design/bounty-loop-inline-20260924.md` 已删（本条即并入落点）。
- 2026-09-25：**奢侈品"无法出售"误报：把「收购簿为空」误判成「不在市场目录」（船长报障 · 三号 · main）**——**①船长原话（照抄）**：报障「**部分奢侈品玩家反馈显示的是不在市场目录导致无法出售**」＋「**按你推进来，查一下**」。**②结论先行（两件事性质完全不同）**：a) **界面误报 = 真缺陷、已修** —— `itemBuyQuote()` 把「**收购簿为空**」与「**不在市场目录**」返回同一个 `undefined`，两处「出售」按钮拿它当判据 ⇒ 把"暂无人收购"显示成「不在市场目录（无法出售）」，**还挡掉了一条本来能走通的路**（空簿时仍可挂限价卖单等收购单浮现）。b) **"簿为什么会空" = 设计使然、不是缺陷** —— 奢侈品买簿**天生薄**（总量 ~40 件），补单走「单件平价品」路（每窗 85% 概率补 1 张、qty ≈ 3）⇒ 十款各自 4%~33% 时间处于空簿。**③取证**：数据层先排除 —— 探针在真 `buildSimContext()` 上核对**物品表 / 市场目录 / 产出池三方完全一致**、十款一款不缺；真档 `save-20260924-181852`（130.7 游戏小时）逐款实测 ⇒「部分」= **lux-5 异域织物 / lux-7 宫廷乐谱 / lux-8 古法香膏** 三款买簿恰好为 0，其余七款一切正常。**④修法（甲）**：**判"能不能卖"看「在不在市场目录」（`marketGoodOf`）；看"卖多少价"才用 `itemBuyQuote`** —— `pages/CargoPage.tsx` 与 `pages/ItemsPage.tsx` 各两处（行级 ＋ 详情弹层）新增 `sellable` 判据；**价格显示一律不动**（空簿照旧显示 `—`）；「不在市场目录」这句只留给真的不在目录的物件。⚠ 缺陷形态值得记：真正判"在不在目录"的 `marketGoodOf` **本来就在同一页、同一行数据上**，同一份数据两个判据并存、一个对一个错。**⑤验证**：八道闸门全绿（typecheck 四包 · core 全量 · content/l10n/ui:rot/ui:theme · 构建）。**⑥归档**：`docs/design/luxury-sell-readout-20260925.md` 已删（本条即并入落点）。
- 2026-09-24：**派系活跃掉落率读数：从"写死裸常量"收口到"随档位现算"（船长报障 · 三号 · main）**——**①船长原话（照抄）**：报障「**敌对派系活跃的卡牌上，铁人模式的残骸掉率加成似乎没应用到？**」＋选定方案「**甲**」（① 工具读数参数化、打印普通/铁人/限时三档；② 卡面读数随状态现算）。**②查证结论：机制层一直是对的，病在读数** —— 「敌对派系活跃」的掷骰全仓只有一处（`expedition.ts`），且**乘了**乘区 `FACTION_RARE_DROP_CHANCE × rareDropRateMulOf(state)`（限时倍率 × 铁人 ×1.2）。真引擎实测（各 3000 趟、同一批档只切铁人开关）：普通档 **29.70%**（891/3000）· 铁人档 **35.10%**（1053/3000）· 比值 **×1.182** ↔ 解析式 **×1.180** ⇒ 加成确实生效（自然 p 30% → 36%，被保底轻微稀释）。**病根 = 两处读数写死裸常量**：① 卡面 `Math.round(FACTION_RARE_DROP_CHANCE * 100)` ⇒ **恒印 30%**；② `npm run faction:audit` 内部写死 `const p = FACTION_RARE_DROP_CHANCE` ⇒ **恒印 30.9%/趟** ⇒ 铁人档在卡面上"看上去没加上"。**③落码**：`core/lairs.ts` 新增 `factionRareDropChanceOf(state)`（当前档位单趟自然概率、含全部乘区）与 `factionRareDropRateOf(state)`（界面/工具读数、再过保底折算），`factionRareDropEffectiveRate(rate = …)` **参数化**（夹 0~1，`p=0 ⇒ 1/N`、`p=1 ⇒ 1` 边界补用例）；`expedition.ts` **掷骰改调同一函数**（读数与结算同一把尺）；`core/index.ts` 导出；卡面改读新函数。**依赖方向已核**（`lairs → tuning → ironman` 无环，且新函数都在函数体内调用 ⇒ 不会重蹈 2026-09-24 那次 `combat ↔ wormholeFoes` 循环依赖的 CJS 转译 TDZ）。**④验证**：七道闸门全绿。**⑤归档**：`docs/design/faction-rare-drop-readout-20260924.md` 已删（本条即并入落点）。
- 2026-09-25/24：**护盾充能力场：回盾量改按「装件舰（施放者）满盾」＋ MK2/MK3 的 CPU 上调到 63/91（船长报障与两令 · 三号 · main）**——**①船长原话（照抄）**：「**护盾充能力场装置 MK2和MK3的CPU占用提高到63/91**」；报障「**护盾充能力场的文案描述有歧义，容易让玩家以为恢复量是根据每艘船变化的，而不是固定使用的船的护盾量**」＋改判确认「**原本设想的就是乙啊，说明之前的文案歧义确实大**」（乙 = 改机制 ＋ 改文案）＋文案口径「**为队内所有舰船恢复护盾，恢复量为本舰护盾量的10%。**」；Q2（多舰各带一件要不要加收敛）/ Q3（发不发公告）/ Q4（CPU 透支那条待办）**一律「不管」**。**②根因**：2026-09-20 三答之三记作「10% 按**携带者自己的满盾**」，同句括号里被补注成「（即每艘被治疗舰按它自己那本账算）」——「携带者 = 装件舰还是受益舰」没当场澄清 ⇒ 实现与 5 处文案一起按"每艘受益舰各按自身满盾"落地。⚠ **为什么一直没被发现**：`tests/shield-field.test.ts` 的测试世界里两艘**同一船型**（1000 对 1000）⇒ 新旧口径数值完全相同、**旧口径下没有任何用例能分辨**。**③新口径（现行）**：每跳给全队回的护盾量 = **施放者（装力场那艘船）满盾 × 该路比例**，全队**存活**单位拿**同一个绝对量**；每人仍**夹在自己满盾内**（溢出丢弃）；**逐路**（MK2 一路 10 秒 / MK3 一路 8 秒）各按**各自施放者**满盾算，多舰各带一件时**各按自己满盾各跳一路、全队叠加**（Q2 不加收敛）。**边界沿用不动**：施放者阵亡 ⇒ 本跳不跳 · 死亡单位不回盾 · 按**满盾**不按当前盾 · 仍能从 0 盾点起来 · 同舰多件仍走 EVE 曲线 `stackWeight` 收敛。**④数值影响（实测例）**：装件舰 = 邓氏鱼级旗舰（满盾 1248）、僚舰 = 甲壳截击舰（满盾 30）⇒ 旧口径「装件舰 +125 · 僚舰 **+3**」↔ 新口径「两舰**各 +125**（僚舰顶满并溢出 95）」；反向（装件舰是小盾舰）则大盾舰收益缩水。**⑤CPU 上调**：MK2 **55 → 63** · MK3 **80 → 91**（`data/modules.ts`，各带 `⟪2026-09-24 船长令⟫`）——纯数据数值改动，零接口/零存档/零文案结构；横向对照同族**中槽**「护盾充能装置」MK1/2/3 = **25/45/70**，本件高槽、治全队、冷却自带 ⇒ 落"明显更贵但非装不上"一档。⚠ **连带面**：内容工作台（第三份派生件）**必须重导**——`content-csv/modules.csv` 与 `content-workbench.xlsx` 原写 55/80，**且下次 `content:import` 会把 55/80 写回来**（已跑 `content:export` 核对两行 = 63/91 ✅）。**⑥涉及文件**：`core/combat.ts`（`pulseShieldFieldFor` 增第 4 参＝施放者规格、`capS` 改取施放者满盾、调用点传施放者、头注记改判与旧记录误记原因）· `data/modules.ts` MK2/MK3 描述 · `data/blueprints.ts` 两张图纸描述 · `data/l10n.ts` 英文 4 条 · `l10n/table.ts` `ui.shipInfo.181` · `data/modules.ts` 两处 `cpuUse` · 用例 `shield-field.test.ts`（头注改判 ＋ **新增一条能分辨口径的用例**＝两艘满盾不同的船）。**⑦验证**：typecheck 四包 0 错 · core 全量全绿 · `content:check`（仅既有警告：两条 `bp-shieldfield` 书价偏离，**与 CPU 无关、改动前就在**）· `l10n:check` · `ui:rot-check` · `save:roundtrip-audit`（84 份往返、丢键 0）· 桌面 `build` · `balance`（三策略 24h 对照照跑）。**⑧公告**：**四件一律不发**（与同日"敌方挂载件 / 铁人一次机会 / 派系活跃读数修正"一并，**船长 2026-09-25 裁定「不发公告」**；口径出处见词典「公告发布口径」词条内的"不发清单"）。**⑨不在本批**：CPU 透支 ⇒ 无人机静默少放（Q4「不管」）· 多舰各带一件的收敛（Q2）· 公告（Q3）。**⑩归档**：`docs/design/shield-field-caster-20260925.md` 已删；**前一阶段稿 `docs/design/shield-field-cpu-20260924.md` 按船长选定只就地标注「已被取代」（文件保留、不删）**。
- 2026-09-25：**星图扫描时长改幂次曲线：最深 24 小时 → 12 小时（船长令 · 三号 · main）**——**①船长原话（照抄）**：需求「**打算平衡下星图扫描星系的时机，原先最后一个星系是24小时，削减到12小时，并且采用曲线形式，让玩家高安地区扫的更快一些。**」＋四答「**Q1 乙，Q2按照①Q3接受Q4确定**」（① 高安**保持 10 分钟不动**、"更快"指比线性快 ② **幂次曲线** ③ **接受中低安大幅压缩** ④ 母港 10 秒／技能乘区／上限钳制照旧）＋「**确认**」。**②新公式**：删 `SCAN_LOWSEC_PENALTY`（线性系数）、新增 `SCAN_CURVE_EXP`；端点校验 `2.5^4.667368 = 72.0000` ⇒ 10 分 × 72 = **12 小时 0 分**。**③逐档读数（旧制线性 ÷ 新制）**：高安 1.0/0.6/0.5 **10 分 → 10 分（不变）** · 0.4 中安 1 时 45 分 → **16 分（−85%）** · 0.2 中安 4 时 56 分 → **34 分（−89%）** · 0.1 中安 6 时 31 分 → **48 分（−88%）** · `sec=0` 低安边界 8 时 07 分 → **1 时 06 分（−86%）** · −0.2 低安 11 时 17 分 → **1 时 59 分** · −0.5 低安 16 时 03 分 → **4 时 14 分** · −0.7 低安 19 时 14 分 → **6 时 36 分** · −0.9 低安 22 时 25 分 → **9 时 55 分** · **−1.0 最深 24 时 0 分 → 12 时 0 分**。**全 19 个星系扫描窗口合计：224.2 时 → 77.6 时（降到 35%）**；`npm run balance:chains` 实测该链 **77.4 小时**（与逐档求和对得上）。**④取舍（按船长四条）**：高安逐字不变 · **上凸**（`u` 小时涨得慢、`u` 大时涨得快 ⇒ 中安先快、深低安才逐步变慢）· 中低安大幅压缩是"10 分~12 时之间放上凸曲线"的必然结果 · 母港固定 10 秒／技能乘区不变／上限钳制跟随。**⑤改动清单**：`core/explore.ts`（删线性系数 · 新增曲线指数含整段口径注释并把 2026-09-23 那条「10 分钟 ~ 24 小时」令**就地标作废** · `scanWindowMsFor` 换曲线 · `maxScanWindowMs()` 按指数推导）· `core/index.ts`（导出换名）· `core/save.ts`（**只改注释**，逻辑不动）· `tests/t25.test.ts`（断言改曲线且**读常量不写死指数** ＋ 新增"端点 + 单调 + 上凸"用例 ＋ 订正文件头过期注释）· `tests/exploration.test.ts`（读档兜底上限 24 时 → 12 时）· `tools/chain-balance.ts`（口径说明改"危险度曲线"）。**不动**：`save.ts` 逻辑 · `Expedition.tsx`（读数读 `scanWindowMsFor` 自动跟随）· `skills-effects.test.ts`（基准未变）· **虫洞扫描**（另一套 12 小时基准，与本批无关）· 无新增文案 id。**⑥存档影响（如实报）**：`normalizeState` 没有 ctx、拿不到目标星系安全等级，只能按**全游戏最大有效窗口**兜底钳制；上限降到 12 小时后，**老档里若存有 > 12 小时的续扫进度**，读档被钳到 12 小时、再按该星系新窗口钳一次 ⇒ **净效果 = 那些"扫到一半的深空星系"直接算扫完**（方向对玩家有利、不报错、不涉存档版本）。**⑦验证**：七道闸门全绿（typecheck 四包 0 错 · core 203 文件 / 2309 用例全绿 · content/l10n/ui:rot · `save:roundtrip-audit` 丢键 0 · 桌面 build）；新用例覆盖 ① 高安（≥0.5 含边界）**恒等基准** ② 端点 `2.5^p = 72` ⇒ 最深 = 12 时 = 合法上限 ③ 中低安 16 档**严格单调递增** ④ **上凸**（二阶差分 > 0 —— 线性时恒为 0，这是"曲线"的可测特征）。**⑧公告**：**不发**（船长 2026-09-25 裁定；依据 = 既有功能数值重平衡，且**星图扫描时长从无已发布公告**）。**⑨归档**：`docs/design/starmap-scan-curve-20260925.md` 已删（本条即并入落点；口径亦属词典「扫描探索」词条范围）。
- 2026-09-24：**装备库排序：按武器攻击类别 ＋ 档位 rank 从低到高（船长令 · 三号 · main）**——**①船长原话（照抄）**：「**物品仓库页面的装备（装备库）排序有问题，现在的排列很混乱，希望按照武器攻击类别，rank 从低到高排序。**」＋方案回话「**按你推荐来**」。**②病根（真档实证）**：装备库原先直接 `Object.entries(state.moduleBay)` 渲染 ⇒ **顺序 = 获得/存盘顺序**，既不分类别也不分档位（船长真档 41 种排出来头几行就是"跳跃"的：动能稳定器 MK3 → 射速计算机 MK3 → 巡航导弹架 MK3 → 目标锁定阵列 MK3 → 力场装置 MK3 …）。**③现行口径**：主键 **① 攻击类别：动能 → 爆破 → 能量**（与战斗画面 `DMG_ORDER` 同序）；次键 **② 档位从低到高** —— 基础型号走 **民用(-1) → 基础件(0) → MK1 → MK2 → MK3 → 异星原型(3.5)**，**档位取自 id 的单点规则**（`civ` / 无后缀 / `-1` / `-2` / `-3` / `proto`）**不靠中文名正则**（改名不影响排序）；**势力/虫洞专属件**没有 MK 档 ⇒ 统一排在该类别**末尾**、内部按**名义火力**（单发 × 门数 ÷ 装填秒）由低到高（同值再按 id）；**非武器件**排在武器之后、按核心既有槽位展示顺序 `MODULE_SLOTS` 分组、组内按名称。**④实现（单点 + 一行接线）**：新增 `apps/desktop/src/renderer/src/ui/modOrder.ts`（`modFrameOf` / `nominalDpsOf` / `modOrderKey` / `compareModOrder` / `sortModEntries`）；`pages/ItemsPage.tsx` 只改一行取数 `modRows = sortModEntries(...)`；**只决定展示顺序**，不动数据/数值/存档，也不动筛选（筛选仍归 `itemSubs.ts` 单点）。**⑤新契约 `npm run mod:order`**（`tools/mod-order-check.ts`，用**真排序函数**排全目录）四条：武器段在前 · 同类别档位单调不减且专属件一律在基础型号之后 · **MK1/MK2/MK3 每类齐备**（民用档只有动能系有，故不强制）· 非武器件槽位顺序不回退；`--all` 连非武器件一起印成**验收表**。**工具第一次跑就抓出两个真错（留档）**：① 虫洞专属「近防炮 MK1」（`mod-wh-e-pd`）被纯 id 规则判成基础档 MK1 插进基础段 ⇒ 改为**武器件优先判专属**（`mod-lair-*` / `mod-wh-*` 且带 `damageType`）；② 契约第一版要求每类都有"民用"档 ⇒ 被自己的工具判红（爆破/能量系**本来就没有**民用件）⇒ 改为只强制 MK1~MK3。**⑥真档实测**（一次性探针、用完即删）：船长档 41 种排序后 = 攻坚炮台 MK3·动能型 → 近防炮 MK3 → 守墓者长炮 → 劫掠者转管炮（动能）｜巡航导弹架 MK3 → 掠袭导弹巢（爆破）｜攻坚激光炮 MK3 → 酸液喷吐器（能量）｜精密采集器 MK3 → …（非武器件按槽位）——**条目数 41 → 41 守恒、集合一致**（只换顺序，不丢不增）。**⑦验证**：typecheck 四包 0 错（过程中修一处：`sortModEntries` 形参原写 `Map`，而 `SimContext.modules` 是 `ReadonlyMap`）· core 2272/2272 · content/l10n/ui:rot/ui:theme 全绿 · `npm run mod:order` ✅。**⑧归档**：`docs/design/mod-order-20260924.md` 已删（本条即并入落点）。
- 2026-09-24：**悬赏胜率预估重做：三点距离 ＋ 去种子化 ＋ 实战胜利记录（船长令与报障 · 三号 · main）**——**①船长原话（照抄）**：报障「**胜率过于极端，98 胜率打噬口猎杀令连续失败**」＋「**武器的射程 → 期望距离，那就按照武器最大射程，最小射程，中距离各跑数次。不加入新参数。偏移也取消了。**」＋「**去种子化，有胜利记录时取代，三点各10局就够了，"残血"口径按照剩余比例。**」＋「**③按敌卡。记录残血最多的一次，如果都是满血则不覆盖。夹回当前射程内。**」。**②旧口径的四个洞**：① **固定种子表**（`20260909 + i×常量`）⇒ 所有卡所有玩家跑**同一批时间线**＝"同一条路线跑 N 遍"不是抽样（系统性偏差，"显示必胜却翻车"的头号嫌疑）；② **单点距离**（只有该星系设定/中距档）⇒ 玩家在射程带边缘接战的表现根本没被采样；③ **评估与实战不同尺**（不复制弹药档位 ⇒ 评估吃基础弹、实战吃 MK2/MK3）；④ **无实战反馈**（打过的卡与没打过的卡用同一套采样）。**③方案（逐条对照原话）**：a) **三点距离采样** —— 按该船**主武器**的 `maxRangeM` / `minRangeM` / 中距档各跑 `BOUNTY_MC_RUNS` 局，三处复用引擎自己的 `mainWeaponOf` + `desiredRangeFor` ⇒ **不新增参数**，开战仍走 `startBattleFor`（与实战同一入口、同一夹紧），**旧"偏移"取消**；b) **去种子化** —— 每局种子 = `散列(敌卡 id ＋ 舰船装配/技能指纹 ＋ 距离点 ＋ 局号)` ⇒ 同输入逐位可复现（预热缓存稳定、用例可断言）、不同卡/不同点互不相关（真抽样）；c) **每点 10 局**（三点共 30 局/卡，旧口径单点 63 局）；d) **实战胜利记录取代采样** —— 新增 `state.winRecord[敌卡 id] = { desireM, remainPct }`，**只由实战胜利写入**（模拟评估是只读克隆、绝不写 ⇒ 不会自证）、**只在剩余比例更高时覆盖**（等值不写 ⇒"两次都满血则不覆盖"）、消费时**夹回当前射程带**（换过武器仍能吃记录）；e) **「残血」口径 = 剩余比例** `(装甲 + 结构) ÷ 满值`（护盾每场满值重建、不进分子）；f) **同一场仗的四项对齐**（船长 2026-09-24 追问确认）：弹药 = **玩家当前档位**（快照复制 `ammoPref`）· 补给 = **完全充足**（料放进取用开关真正读的那个池）· **享受全部战斗加成**（会战判定/窝点档位/派系活跃都带进快照）· 存进快照的每星系目标距离照旧生效；g) **输出新增读数**：`worstWinRate`（三点最低者＝"运气最差的距离上也赢得下来吗"）· `points`（逐点）· `recorded`。**④落码清单**：`core/winEstimate.ts`（重写）· `core/state.ts`（新增可选字段 `winRecord?`，**老档零迁移**）· `core/save.ts`（清洗器**登记**该字段，只收合法行、**空表不落键**）· `core/expedition.ts`（`resolveBattleOutcome` 胜利支新增**唯一写入点**：键 = 本场目标卡 id · 距离 = `battle.myDesireM` · 比例取到万分位）· `game/engine.ts`（预热指纹**补四项**：弹药档位 · 取用开关 · 胜利记录 · 窝点档位/派系活跃，否则吃完工那一场仍看旧缓存）· `panels/Expedition.tsx`（缓存未就绪显示**「计算中」**不再回退另一套尺子 · 悬停附最差距离胜率 · **三处胜率统一去掉 `Math.min(98, …)`**）· `l10n/table.ts`（`ui.Expedition.425/426`）· `panels/Wormhole.tsx`（顺带修：压着围剿者的格悬停曾落到「没有信号：空信息地点」⇒ 围剿者提到标题链最高优先）· 用例（`winEstimate.test.ts` 重写 8 条 · `win-record.test.ts` 新增 5 条 · `desire-per-galaxy.test.ts` 局数随常量改 10）。**⑤验证（实测）**：闸门全绿（typecheck · core **196 文件 / 2215 用例** · content/l10n/ui:rot/ui:theme · build）；**单卡耗时**（真内容 + T1 护卫舰裸装）10 局/点均 **28ms**（最大 71ms）↔ 旧口径 63 局/点均 **138ms**（最大 368ms）⇒ 预热**便宜约 5 倍**，引擎"每批 ≤2 张、预算 8ms"的自限为每批 1 张；**三点读数的实际意义**：`ano-abandoned-platform` 三点 = **2500m 0% · 200m 30% · 2000m 0%** ⇒ 平均 10%、最差 0% —— 同一条船同一张卡，**拉开距离就输、贴上去才有三成**（旧口径只给一个数，正是"看着还行、实战翻车"的来源）；存档侧无记录的档**不落 `winRecord` 键**（与改动前逐字一致）、有记录则随档往返一致、非法行读档即清洗。**⑥已知取舍与待裁决点**：a) **派系活跃卡 / 赏金任务（窝点）卡仍走解析口径**（MC 缓存按**未加成卡**建键，而这两类卡的展示必须与实战一致）——要不要把这两类也纳入 MC（引擎按"派生卡"另建键、快照临时置 `factionActive`/`lairTier`）**等船长定**；b) **「最差距离胜率」目前只在悬停里** —— 要不要提到主读数旁（新增一行文案）**等船长定**；c) **「计算中」是过渡态**（战力指纹一变全板重算，头 1~3 秒部分卡显示「计算中」，不再显示旧口径的数）；d) **记录按基础卡 id 记账**（窝点/派系是本卡的派生强化，共用同一行 ⇒ 打窝点赢下的距离也会被普通悬赏读到）；e) **编队口径**：本模块服务**星图悬赏**（单舰入口 `startBattleFor`），虫洞编队战斗走 `startFleetBattleFor`、本模块不覆盖。**⑦归档**：`docs/design/win-estimate-redesign-20260924.md` 已删（本条即并入落点）；**前一阶段立稿 `docs/design/win-estimate-mc.md`（2026-09-09 定稿）按船长选定只就地标注「已被取代」（文件保留、不删）**。
- 2026-09-24：**舰船工作台回写：我方 8 艘虫洞舰船属性 ＋ 鱼雷舰豁免裁决（船长令 · 三号 · main）**——**①船长原话（照抄）**：「**我调整了我方舰船的一些属性。将敌族的excel属性更新。**」＋（追问"更新敌族 excel"要哪种做法 ⇒ 选）**乙：先回写我方那 10 处，再重出敌族表**；关于 `构件鱼雷舰.powerBonus` 撞契约 ⇒ 裁决「**鱼雷舰排除出装甲舰不得带档位单发加成的范围内。**」。**②改动（10 处 · 8 艘，走既有回路）**：船长在 `content-csv/ships.csv`（GBK + Tab 工作台文件）里改 ⇒ `npm run content:import ships content-csv/ships.csv` 按主键**字段级回写** `packages/data/src/ships.ts`（AST 区间最小替换 ⇒ 注释/分组/排版不动）。**逐处**：掠袭电子舰 回避 0.216 → **0.316** · 掠袭炮舰 回避 0.12 → **0.22** · 掠袭重型突击巡洋舰 速度 225 → **265** · 幼虫截击舰 回避 0.1 → **0.38** · 甲壳截击舰 回避 0.06 → **0.31** · 巢群重型突击巡洋舰 速度 166 → **206** · 构件鱼雷舰 单发加成 0.2 → **0.35**、族系高爆 0.15 → **0.2** · 亡军鱼雷舰 族系高爆 0.15 → **0.2**、速度 285 → **275**。**③唯一一处口径冲突与裁决（值得记）**：新提议把 `sh-wh-e-frigate` 的 `powerBonus` 改成 **0.35**（= 武装舰 T1 档位 0.15 ＋ 鱼雷舰子分类 0.2），与「**加成与抗性新口径**（船长 2026-09-17）」那条「装甲舰**不得有档位单发加成**，唯一例外 = 子分类给的」冲突 —— 0.35 恰好等于档位值 ⇒ 等于装甲舰又吃回档位伤害、那条设计界线（装甲舰换的是甲层抗性）被抹掉，且 `content:check` **会持续报红、别的会话无法提交**。按 §5.2 **先做与冲突无关的部分**（留 0.2 让闸门变绿）后摆给船长，**船长裁决「鱼雷舰排除出该范围」**⇒ 落码三处：① `ships.ts` 该舰 `powerBonus` = **0.35**；② 契约改为：装甲舰照旧只允许子分类那笔，**`subClass === '鱼雷舰'` 的装甲舰按武装舰口径校验**（档位阶梯 ＋ 子分类 0.2）——现存唯一一艘就是构件鱼雷舰（T1 ⇒ 0.35），亡军鱼雷舰本身是武装舰、不受影响；③ 用例 `wh-ship-baseline.test.ts` 三条断言同步（0.2 → 0.35 · 基础舰炮 10 → 11 · 火力指数 ×1.2 → ×1.35）。**④产物**：`npm run foe:export` ⇒ `foe-csv/enemy-ships.xlsx`（25 舰级 · 56 列 · A5/B2/C5/D4/E4/G5 · 最高总血 核心舱段 3,920 · 最高名义 DPS 136.5 · 带机群 6 条 · 带挂载件 11 条）。⚠ **敌舰数值本身没有变**：导出器读敌舰数据与平衡常量、**不读我方舰船**（两者无自动联动），本次只是把表刷成当前代码的版本。**⑤⛔ 挂账两条**：a) **工作台待重导** —— `content-csv/ships.csv` 当时被 Excel 占用（EBUSY）⇒ `content:export` 没跑成，该文件里「构件鱼雷舰 powerBonus」仍是旧的 **0.2**（其余 9 处已最新）⇒ **关掉 Excel 后要重导一次**，否则下次从这份工作台编辑会把旧值带回来（已备份整份工作台到 `%TEMP%\content-csv-bak-123110`）；b) **敌族数值是否随新基线调整** —— 等船长给方向（当前敌我对照：我方掠袭电子舰闪避 0.316 ↔ 敌方劫掠电子舰 0.30；我方 C 族截击舰 0.38/0.31 ↔ 敌方 C 族全 0.12；我方 A/C/G 巡洋速度 265/206/275 ↔ 敌方同族实速 374/300/271）。**⑥验证**：`content:check` 全绿（含改后的加成与抗性新口径）· typecheck 全绿 · core **196 文件 / 2216 用例全绿** · 敌舰明细导出成功（工作台重导未跑，原因见上）。**⑦归档**：`docs/design/ships-workbench-20260924.md` 已删（本条即并入落点）。
- 2026-09-24：**市场「蓝图」二级筛选加入「未学会」（船长令 · 三号 · main）**——**①船长原话（照抄）**：「**市场蓝图筛选的子筛选里，加入一个未学习蓝图的子筛选。**」。**②做了什么**：市场页类型选「蓝图」后的**二级下拉**在原九档（高/中/低槽装备蓝图 · T1~T5 舰船蓝图 · 消耗品蓝图 · 零件蓝图）之后**新增两档**：**已学会** · **未学会**（复用组装机那一行 `BLUEPRINT_LEARN_TABS` 的**同一套键与同一套文案 id** `ui.itemSubs.031/032` ⇒ 中英双语现成、不新造词条）。**为什么船长只点名「未学会」却补了「已学会」**：与组装机那一行**对称**（那边是"全部 / 已学会 / 未学会"），且筛完未学会的人下一个动作通常就是回看"我学过哪些"——**若只要一档，说一句我删掉「已学会」**（一处删除、零连带）。**③判定口径（与组装机同一把尺）**：走市场与组装机共用的判定入口 `subPasses(ctx, good, 'blueprint', sub, state)`（`ui/itemSubs.ts`）——**已学会** = `ownsBlueprint(state, refId)`（core 单点、读 `state.learnedRecipes`）；**未学会** = **永久图纸**且尚未学会。⚠ **一次性图纸不进「未学会」**：它按定义"不上市场、不能学、只能造一次"（`BlueprintDef.singleUse`），放进去就是一张**永远学不会**的清单（组装机那边也是这么排除的）。**④两个实现取舍（防后人重踩）**：a) **另起一张市场专用表 `MARKET_BLUEPRINT_SUBS`，没有直接往 `BLUEPRINT_SUBS` 里加** —— 后者**同时是手册「蓝图图鉴」的分组顺序表**（`Handbook.orderOf('blueprints')`），而 `groupCells` 对"顺序表里有、但没卡片"的键**会原样留下一组** ⇒ 直接加会让图鉴多出两个空组标题；b) **「学没学会」两档在下拉里常显**（不走"本档没商品就隐藏"那条既有尺）：它按**玩家存档**实时判定、不是内容分类，否则"一张都还没学会"的玩家看不到「已学会」这一档；另：下拉选中后的结果栏标题改走 `subText(...)`（原先直接读 `label`，英文界面下会漏中文）。**⑤实测读数**（一次性探针、用完即删；真内容目录 ＋ 真引擎、调**市场同一个判定入口**）：市场蓝图行 **200** 条 = 永久 **167** ＋ 一次性 **33**；「**未学会**」命中 **167** 条（**正好等于永久图纸数** ⇒ 33 张一次性一张都没漏进来）·「**已学会**」命中 **0** 条（新档未学任何蓝图）；**反向验证**：拿一张永久图纸（`bp-pd-e`）——学之前 `未学会=true / 已学会=false`，`learnedRecipes` 里推入后反转，与 `ownsBlueprint` 复核一致；**边界**：一次性图纸两档都不命中、不传 `state`（老调用方）两档恒不命中（宁可不筛也不猜）；**既有档零漂移**：高 47 · 中 37 · 低 42 · 消耗品 11 · 零件 7。**⑥验证**：typecheck 四包 0 错 · `ui:rot-check` ✅（含 TDZ：**直接执行 0**）· `l10n:check` ✅ · `content:check` ✅。**⑦归档**：`docs/design/market-bp-learn-filter-20260924.md` 已删（本条即并入落点）。
- 2026-09-25：**稀有供给单「每次恒 1 件」＋ 奇货两旋钮 ＋ 稀有订单四档权重（船长三令 · 三号 · main）**——**①船长原话（照抄）**：报障「**玩家经常会出现，某一个奇货商品一次性刷出两三件的情况，是怎么回事**」→「**单件相同的商品，一次性刷出了3件，这才是问题**」→ 裁决「**每次恒 1 件，奇货允许最大件数上调。**」；⟪同日第二令⟫「**奇货订单每窗上限再+2。0.8%的概率上调到1%稀有和常驻不做修改。**」；⟪同日第三令⟫「**稀有单的权重有问题吗？之前我记得修复了，没实装吗？2~5稀有度的稀有订单，权重调整为1/0.5/0.2/0.05**」；⟪同日第四令⟫「**修复隐患**」。**②恒 1 件**：`core/market.ts` `spawnRareSupply` 原**一次抽中随机给 1、2 或 3 件**（真档推进 3 小时实测：供给侧 `rare · module` 最大 **3 件**（货舱扩展 MK2）· `rare · blueprint` 最大 **3 件**（强化采集器 MK2 蓝图）· `exotic` 全部恒 1 件 · `rare · item` 消耗品 600 件）⇒ 单张件数改**恒 1 件**（船仍恒 1 艘）；**消耗品批量档 `rareQtyMul` 保留**（2026-09-20 船长令：让弹药这类在簿上"看得见在收"）⇒ 弹药 MK2 单张由「200~600 发」变为「**200 发/张**」（**倍率本身一字未动**）。⚠ **仍掷一次骰但丢弃结果**（`void nextInt(state.rng, 3)`）：少掷会**整体推移 rng 序列**（市场慢噪声/价线轨迹跟着变，实测 `market.test.ts` 的"站内让利吸收额度"当场红：吸收量 1,232 ≪ 21,600）——与 2026-09-11 稀有残骸保底的「**恒掷一次随机数**」是同一款时序纪律。**③奇货两旋钮**：每窗上限 **2 → 4**（铁人 +2 ⇒ 6）· 出单概率 `balance.market.exoticWindowChance` **0.8% → 1%**/抽取窗（≈每件每 20.8h → **16.7h**）；**稀有与常驻一律未动**。读数（合成档 24h 同逻辑前后对照）：24h 奇货现货推送 **15 → 18 张**（+20%）· 在场现货 **2 种 → 3 种**有单 · 每件每窗期望命中 **0.33 → 0.41**（满现货抢购学 0.61 · 铁人 1.23）。⚠ **上限 4 仍几乎不触发**（铁人满技能档期望 1.23 ≪ 4）——本批真正起作用的是概率 +25%，上限这条是为"偶发/离线补单爆量"留的护栏。**④稀有订单四档权重**（查证回答船长那两问）：**有问题，而且不是"没实装"——是当初就没做档 5**（档 2 ×1 隐含 · 档 3 由 2026-09-10 定的 0.15 · 档 4 由 2026-09-16 补的 0.05 · **档 5 从未有过系数**，`git log -S'rareTier5'` 零命中；2026-09-20 放宽渠道带时**明确记账**"档 5 按缺省 ×1，要系数另立条目"——但一直没立）⇒ 船长记的"修复"应是 **2026-09-16 补档 4** 那次。实测后果（`market-rarity-sim`，1440 窗 × 5 种子，改前）：`bp-shieldfield-3`（档 5）单行间隔 **68 窗 ≈11.4h**，反而比 `bp-shieldfield-2`（档 4，**214 窗 ≈35.7h**）**常见 3 倍** ⇒ 档位越高越常见、与"稀有度"语义相反。**改动**：`rareTier3Weight 0.15 → 0.5` · `rareTier4Weight 0.05 → 0.2` · **新增 `rareTier5Weight = 0.05`**（`types.ts` 补字段 ＋ `market.rareTierWeight` 加档 5 分支 ＋ `data/rarityTier.ts` 与各处注释同步 ＋ `tools/market-rarity-sim.ts` 支持 `--tier5-weight` 并打印档 5 权重）。读数（前 → 后）：稀有供给里 **tier3 占比 12% → 30%**（tier2 88% → 68%）· **每窗总张数 19.10 → 19.17**（不变，船长 2026-09-09 定的"总量不收敛"未破）· **12 张一次性舰船图纸（档 3）35~44 窗（5.8~7.3h）→ 14~16 窗（2.3~2.7h）** · `bp-shieldfield-2`（档 4）214 → 176 窗（sim 受簿上限压制，解析净份额 ×2.9 ⇒ 真实约 12h）· `bp-shieldfield-3`（档 5）68 → **399 窗（66.5h）**（解析 ×27.6 更慢）⇒ 它**第一次成为最稀的那档**。⚠ 为什么 12 张只快约 2.4 倍（权重明明 ×3.33）：**90 行档 3 同时从 0.15 抬到 0.5 ⇒ 稀有池总权重 ×1.38**，把所有行的份额一起稀释（tier2 单行间隔 4.9 → 6.0 窗即此）。**⑤顺带修掉一个测试基建隐患**（船长令「修复隐患」）：`makeTestCtx` 的 `balance` 原本直接取 `opts?.balance ?? DEFAULT_BALANCE`——**共享对象、没有深拷贝** ⇒ 用例里改 `ctx.balance.market.rareTier3Weight` 改的是**模块级常量**、会**跨用例泄漏**（第一版新用例写进 `market.test.ts` 时读到的就是被上个用例改成 1 的权重：命中数 150 vs 162，与 0.5 > 0.2 相反）。**修法**：`tests/helpers.ts` 增加纯数据深拷贝 `clonePlain`（递归复制纯对象/数组；函数、类实例、Map/Set 等**非纯对象按引用原样返回**），`makeTestCtx` 的 `balance` 走它、`quietEvents` 的 events 覆盖改在副本上做；**回归守卫** `market-tier-weights.test.ts` 第三条钉「改 `ctx.balance` 不再污染 `DEFAULT_BALANCE`」；修复后全量用例仍全绿（2364 → 2365，多的那条就是守卫）⇒ **没有任何用例曾依赖那个泄漏**（确认是纯隐患）。**⑥验证**：`market.test.ts` 66/66 绿（含批量档两条按新口径重写、奇货单次上限一条改 4）· `ironman.test.ts` 20/20 绿（普通档 4 张 / 铁人档 6 张，夹具由 5 件改 8 件让上限成为约束）· **`market-tier-weights.test.ts` 2/2 绿**（新：默认值 1/0.5/0.2/0.05 ＋ 四档单调性，含"档 5 必须出得来"这条此前完全不存在的判据）· 真档复跑口径：供给侧 `rare · module/blueprint` 的单张件数应为 **1**（改前最大 3）。**⑦归档**：`docs/design/rare-supply-single-piece-20260925.md` 已删（本条即并入落点）。

## 最近批次（滚动窗口 · 最新 20 条）

> 越窗即封存（`npm run docs:seal`）；窗口大小 = 20 条，需要临时调大用 `--window=N`。

- 2026-09-26：**损伤管制装置上线（MK1~MK3 ＋「损管修理组件」＋ 免死「丁案」1 秒锁定 ＋ 同舰唯一 ＋ 归支援件·修理装置档）（船长令 · 一号 · main · 已推送 `d09fc585`）**——**①船长原话（照抄）**：「**添加新装备，舰船损伤管理MK1~MK3，低槽，效果是大幅增加结构的全伤害抗性，分别+30/40/50，且当舰船第一次结构低于1时，将结构恢复到1（避免一次死亡）。损管只能装备一件**」＋「**全名为损伤管制装置**」＋「**入侵「重复出击」也挂 0.5 撤退保险。损害管制好像什么都不消耗也不太合适吧？维修组件新增新的消耗品，损管修理组件，触发损管效果时需要消耗一份。**」＋「**改为1秒内结构锁定1会不会更复杂？**」→（裁定）「**丁**」＋「**损管修理组件应该消耗更高级的资源，价值提高到 50 万每件，蓝图基础 100 万。**」＋「**做完后给我一个存档 我要测试**」＋「**损管装置和修理装置是同一类型装备分类，不是装甲。三号正在修改，等他改完你修改损管装置的分类**」。**②落地**：a) **三件**（低槽 `slot:'support'` ＋ `rack:'low'`）：结构层三系 `hullResistAdd` **+0.30/0.40/0.50** · CPU 30/38/46 · 稀有档现货 62 万/280 万/950 万 · 蓝图 124 万/700 万/2,850 万 · 无声望门槛；b) **免死＝丁案**：致死那一刻起**结构锁定 1、持续 1 秒**，窗口内**逐段夹伤**（二分反解到"结算后结构 ≥ 1"）⇒ **同拍/同秒多段破不了**；**每场一次** · **不强制撤退** · **谁装谁有** · 判据挂在**敌方→我方的唯一入伤口**（敌机群/敌光束/敌炮台三处共用 `cappedFoeDamage` 之后再过 `applyDcGuard`）；c) **消耗品** `repairkit-dc`「损管修理组件」：**50 万/件**（市场 55 万）· 蓝图 **100 万**（登记覆盖表，不走组件旧式）×2.2）· 材料**清一色高阶矿**（冥铁/星髓/重钨/同位 ＝ 82.5 万/批）；启动消耗 1 枚（仓库优先，关掉"从母港仓库取用"走本舰货仓）· **没组件不启动**；d) **同舰唯一** `ModuleDef.unique`（V18.1 取消同类唯一后**首次**）＋ `fitModule` 拒装（`core.equipment.028`）＋ 登记进 `SUPPORT_MODULE_KEYS['support-repair']`（配合三号「支援件三分」，修理装置档 **4 → 7 件**）；e) **连带修复**：入侵「重复出击」也挂 `hullEscapeFrac = 0.5`（原判据两头不成立 ⇒ 会打到结构归零、可能丢船）；f) **观感**：日志 `core.combat.002/003` ＋ 战报尾巴（远征胜/败/弃船三处）＋ 战斗内提示走既有 `battle.notices`。**③读数**（`tests/damage-control.test.ts` 8 例）：致死发 999 → 夹到 99（结构 1）· 组件 3→2 · 窗口 1000 ms · 窗口内第二发仍锁 1 且不再扣组件 · 出窗后原样放行 · 组件 0 不启动 · 战报尾巴「损伤管制装置启动 ×1（消耗损管修理组件 ×1）」· 随档往返 `dc` 与 `dcKitsUsed` 保住。**④验证**：typecheck 四包 0 错 · core 全绿 · `content:check`（新增**支援件第十类「损管」**判据 ＋ 跨族白名单 `mod-dc-1/2/3:hullResistAdd`）· `l10n:check` · `ui:rot-check` · `ui:layout-css:check` 全绿 · 验收档 `docs/test-saves/test-save-dc-20260926-092228.json`（注入器 `npx tsx tools/make-test-save.ts dc`；主控低槽就地换 MK3，原件退回装备库）。**⛔ 遗留**：① AI 副船战报那一行未接（ai 战报是链式 id 拼装）；② 装配预设/读档"裁掉多余件"未做（本件是新物品 ⇒ 旧档不可能有两件，装配层已从源头拦住）；③ 未另做"锁定中"状态灯（走 `battle.notices`）。**⛔ 待船长批**：公告待审稿 `docs/design/announcement-draft-20260926-damage-control.md`（`ann-damage-control-20260926` · 五条 ＋ 备选第 6 条"重复出击自动脱离"）。
- 2026-09-26：**玩家报障两件：装填条上的无人机条目按存活架数显示 ＋ 撤退按钮文案去括号（一号 · main · 已推送 `d09fc585`）**——**①玩家原话（照抄）**：「**玩家战斗中如果无人机被摧毁，中间下方的各种装备条的内容也要更新，玩家今天反应就是无人机显示就绪不会开火，但是无人机实际上已经被打掉了。**」＋「**战斗中撤退按钮文本过多，删除括号内的文本。**」。**②无人机条的真因与修法**：那条「装填冷却平铺」只看冷却剩余（`arcs.meReload`）⇒ **机群被打光后冷却照样归零**，那一格继续写「就绪」；现改为**再看一眼存活架数**（取视图锚那条舰的 `myUnits[].drones`，与画面上的机体同一份读数、按机型归并）：**打光** ⇒ 名字加删除线 ＋ 色点/进度条转灰 ＋ 状态文字改「**机群已损失**」，悬停「◯◯：机群已全部被击落，不会再开火」；**部分损失** ⇒ 悬停「◯◯：存活 N/M 架」；非无人机条目一字未动。新增文案 `ui.BattleScreen.109/110/111` ＋ 样式 `.app-bts-reload.is-lost`（布局生成件已重跑入库）。**③撤退文案**（船长选的「乙：连带一起删」）：悬停说明 `ui.BattleScreen.040` / `ui.ActivityBar.023` →「撤退：轻损脱离战斗并即刻回港」· 首点提示 `.085` / `.053` →「撤退 = 轻损脱离——再点一次确认。」· 洞内替代文本 `.039` →「洞内：本场必须打完」；完整口径仍在手册 `ui.Handbook.085`。**④验证**：typecheck 0 错 · core **225 文件 / 2,480 例**全绿（新增 `tests/drone-strip-status.test.ts`：读数「机型 drone-scout：开战 2 架 → 全灭后 0 架」＝界面依赖的那份读数）· content/l10n/ui:rot/ui:layout-css 全绿。
- 2026-09-26：**我方无人机战斗演出与近防炮档位（玩家报障「构件哨戒无人机不会出现在战斗场景中」· 三号 · main · 未推送）**——**①玩家原话（照抄）**：「**玩家的构件哨戒无人机不会出现在战斗场景中。**」（船长为「守墓者长袍」纠名时另确认：玩家会把它叫成"守墓者长袍"，实物名 = **守墓者长炮** `mod-lair-turret-d`）。**②真因（三款族专属机从没进过战斗演出表）**：`ui/droneArt.tsx` 的 `DRONE_MODELS` 只登记**制式四型**，而玩家能造/能掉的 **E 构件哨戒 `drone-wh-e-sentry` · C 巢卫攻坚 `drone-wh-c-heavy` · G 鱿蜂 `drone-exile-bee`** 全部漏登记 ⇒ 演出层四处走 `droneModelOf()`（查不到即 `return`）：机体层有 `droneModelOrFallback` 兜底（画成**中性灰方块**），而**弹道层（原 1260 行 `if (dm)`）· 击落演出（2580）· 坠落登记（1045）三处直接跳过** ⇒ 表现为「**只有一个灰块、不开火、不爆炸**」。2026-09-12 蜂群机踩过同一个坑，当时只补了机体那一处兜底。**③修法（四件）**：a) **三款机体登记**（船长选定「**用本族舰体色 ＋ 同档机体形**」：E 残铁棕哨戒碟盘 · C 磷光绿攻坚菱体 · G 聚落紫侦察箭形；弹点仍守"颜色按弹型、机型只决定形制与密度"）；b) **四处取机型统一走兜底**（机体/弹道/击落/坠落/战报机名）⇒ 以后任何漏登记**不再静默消失**；c) **近防炮档位补专属机**：`PD_PRIORITY_BY_ART` 加 `drone-wh-e-sentry`（哨戒档 0）· `drone-wh-c-heavy` ＋ `drone-ink-heavy`（攻坚档 1）——该表的 `role` 兜底只覆盖**敌方**机型，玩家的专属机原先掉进"其余等权"，与船长 2026-09-12 口径不符（`pdPriorityOf` 因此改为导出、供用例逐型断言）；d) 顺带修一处撞名：`ui.droneArt.005` 英文原 "Sentry"（E 警戒机）与哨戒档重复 ⇒ 改 "Construct Alert"。**④新增防复发闸门**：`npm run art:ships:check` 现在**同时核对无人机机体表**（数据侧每个 `kind:'drone'` 物品必须在 `DRONE_MODELS` 有键，缺即退出码 1）——**上线当天就抓到真问题**：H 族「墨潮重袭无人机」（势力装备批 `5a6075c2`）同样漏登记 ⇒ 补机体 ＋ 攻坚档 ＋ 机型文案，读数 **7/8 → 8/8**。**⑤负向验证**：把 `drone-wh-e-sentry` 的键改名 ⇒ 闸门报「6/7 型已登记；无人机缺：drone-wh-e-sentry」且退出码 1；还原后逐字节一致、复绿。**⑥验证**：typecheck 四包 0 错 · core **224 文件 / 2,479 例**全绿 · l10n/content/ui:rot/ui:theme/art 契约全绿 · 桌面与网页产物已重建。⛔ 三款新机体的**观感**归船长真机验收（本次只做"看得见/打得出来"）。
- 2026-09-26：**撤退的修理损伤：那一口 10 秒 → 5 秒（船长裁定「甲：单纯削减时间」· 三号 · main · 未推送）**——**①船长原话（照抄）**：「**目前撤退的修理损伤有些大，我记得之前是敌人10秒的DPS输出吧？**」→ 两条追问后裁定：「**甲**」（10 → 5 秒，与低安遇袭那口同口径）＋「**之前没有抗性就算了**」＋「**单纯削减时间**」。**②核对结论（先证"不是被谁改高了"）**：现值**确实**是 2026-09-14 船长自己改判的 **10 秒**（原 1 秒作废），变重的原因是**卡面威胁已按新定价式整体上调** ⇒「10 秒 × 已上调威胁」双重放大。**③落地（单点常量）**：`combat.retreatHitFirepowerSec` **10 → 5**，四档同一 K 不变（主动撤退 / 结构<50% 自动脱离 / 超时 / 无法交战；**虫洞撤离战照旧不吃本值**，走真实战斗损伤）。读数：一口 = **威胁 × 4 HP**（原 ×8）——威胁 42 的入门低安卡 ⇒ **168 HP**（原 336）· 威胁 115 ⇒ 460（原 920）；参照船体（沙猫级 甲 40 ＋ 结构 95 = 135）对威胁 42 的那一口**仍会触底 5%**（要小船不触底得换口径，非本次范围）。**④不计玩家抗性（有意为之）**：本口伤害**不吃**盾/甲/壳三层抗性——船长同批明令「之前没有抗性就算了」，已在 `balance.ts` / `hullDamage.ts` / `types.ts` / `expedition.ts` **四处注释标明**，防止日后被顺手"补全"。**⑤用例**：`tests/retreat.test.ts` 按 K=5 重算三处读数（32 / 160 / 96）；窝点档那条用例的基准卡威胁 **1 → 2**（派生后 4 ⇒ 那一口 16 HP 才够打穿测试船甲池 10，否则它的反向断言会失效）。**⑥文档**：`docs/glossary.md`「承伤口径单点」词条 ② 撤离战斗那一格改 5 秒并记下本次裁定（低安遇袭同为 5 秒 ⇒ **两处口径现已一致**）。**⑦验证**：typecheck 四包 0 错 · core **223 文件 / 2,474 例**全绿 · l10n/content/ui:rot/ui:theme 全绿 · `build` ＋ `web` ✓。
- 2026-09-26：**支援件筛选拆成三个同级（战斗支援件 · 辅助支援件 · 修理装置）＋ 装备「槽位 / 类型」同行更新（船长令 · 三号 · main · 未推送）**——**①船长原话（照抄）**：「**将装备细分类的支援件筛选拆分成三个同级：战斗支援件，辅助支援件，修理装置。**」＋（追加）「**装备的'槽位 / 类型'这个也要更新**」。集中提问三条裁定：「**按效果族分**」·「**都跟着拆**」·「**直接替掉旧键**」。**②为什么不能只改槽位**：这一族 **34 件共用同一个 `slot: 'support'`**（横跨高/中/低三槽），而「战斗/辅助/修理」是**按件**分的（例：跃迁计算机是低槽、姿态陀螺是中槽，却分属辅助与战斗）⇒ 原「槽位 → 键」一对一映射（`MODULE_SUB_SLOTS`）装不下，改为**逐件清单** `SUPPORT_MODULE_KEYS`（单点表）。**③分档（34 件）**：**战斗支援件 27**（三系稳定器 MK1~3 · 射速计算机 MK1~3 · 索敌阵列 MK1~3 · 姿态陀螺 MK1~3 · 赃物扫描阵 · 亡军火控 · 陵墓弹道铭文 · 守墓者速装填机 · 幽灵弹道校正器 · 墨潮捕获网 · 墨潮电子舱 · 隐秘行动装置 MK2/MK3）· **辅助支援件 3**（跃迁计算机 MK2/MK3 · 生体脉搏加速器）· **修理装置 4**（船体维修装置 民用/MK1/MK2 · 生体损管腔）。**④落地**：`MODULE_SUBS` 的 `support` 一档 → `support-combat / support-aux / support-repair` 三个同级；`moduleSubKeyOf(slot, moduleId?)` 增第二参（支援件必须传 id 才能精确归属）；`itemSubPasses` 的 module/fragment 两条判定补传 `mod.id`；**手册装备图鉴的筛选判定收敛到唯一入口**（原先自己写 `moduleSubKeyOf(slot) === sub`，与市场/碎片那条单点各一份 ⇒ 拆三档后必须有同一把尺）；**四处同时生效**（同一张表）：市场二级筛选 · 手册装备图鉴分组与筛选 · 组装机装备蓝图子筛选 · 碎片子类。**⑤「槽位 / 类型」那一行**（`shipInfo.moduleInfoLines`）：支援件改报**功能子类名**——原先 34 件都显示「支援系统」、看不出属哪一档；实测读数：索敌阵列 MK3 →「战斗支援件（中槽）」· 跃迁计算机 MK3 →「辅助支援件（低槽）」· 墨潮捕获网 →「战斗支援件（高槽）」；其余装备族**逐字未变**。**⑥新增体检「支援件三分契约」**（`content:check`）：`slot === 'support'` 的每一件必须**恰好**落在三档之一——**漏登记 / 重复登记 / 空档 / 登记了非 support 件 四类错全部报红**（漏登记属"静默消失"型：玩家在那个筛选里永远看不到它）。读数：34 件 → **27 / 3 / 4**。**⑦文案**：新增 `ui.itemSubs.038/039/040`（战斗支援件 / 辅助支援件 / 修理装置，中英双语）。**⑧验证**：typecheck 四包 0 错 · core **224 文件 / 2,479 例**全绿 · l10n/content/ui:rot/ui:theme/art 契约全绿 · 桌面与网页产物已重建（生产串自检三档键名与文案在位）。⛔ **票在途**：与该批同线的「损管装置 `mod-dc-1/2/3` 归入修理装置（船长令『损管装置和修理装置是同一类型装备分类，不是装甲』）」由**二号**在 `docs/design/damage-control-20260926.md` 的「待办 1」里落（工作区在途；本条只出分类框架）。
- 2026-09-26：**五个导航页的「子页签 ＋ 滚动位置」会话级记忆（船长令 · 三号 · main · 未推送）**——**①船长原话（照抄）**：「**我希望舰船、技能、工业、任务中心、通讯这几个导航栏页面，能记住玩家上次选择的子页面和滚动条位置。在本次游戏启动期间记忆，不入存档。**」集中提问四条裁定：①任务中心标签「**改成会话制，与新规矩统一**」（它原先存 `localStorage`、**跨启动**也记得）②「**两级都记，但是不记搜索**」③滚动「**只记主要列表那一条**」④范围「**就按点名的五页**」。**②根因**：六个一级页都是条件渲染（`key={page}`）⇒ **切走即卸载**，页面自己的 `useState` 与 DOM 的 `scrollTop` 一起消失，切回来必然回默认值 ⇒ 状态必须活在页面之外。**③落地**：新增会话级存储单点 **`ui/sessionView.ts`**（两份模块级 `Map`，活到本进程结束；**不写 localStorage、不进存档**）＋「挂载恢复（布局阶段，防闪）＋ 滚动即记 ＋ 卸载收尾」的钩子契约。逐页落点：**舰船**＝舰队签与仓库签各自的 `.app-fleet-scroll`（舰队那支与"量列宽"的 ref 共用同一节点）· **技能**＝大类 ＋ 技能书**两级**，主列表 = 目录 Panel 的 `.wui-panel-body`，图标视图另接树画布 · **工业**＝四个子页 ＋ 炉子两级筛选，滚动体形态不一（精炼/造船厂在面板内 `.app-win-body`，组装/货架是 Panel 自己的体）⇒ 统一由 `useSessionScrollFrom` 从 `.ind-pane` 那层**自→祖先→后代**反查 · **任务中心**＝标签由 localStorage 改会话制（旧键 `whale-idle:task-tab` **彻底退役**）＋ 任务列表内滚体 · **通讯**＝选中哪封信 ＋ 信件列表（正文右栏不记）。**④新增能力**：`packages/ui` 的 `Panel` 开一个**可选** `bodyRef` 出口（把 `div.wui-panel-body` 交给调用方——`overflow:auto` 写死在共用件里、外面拿不到那支滚动容器）；**不传 = 与旧版逐像素一致**。**⑤边界**：每个子页签**各记各的**位置；内容变短时浏览器自己夹紧（不报错、不跳顶）；**程序化跳转**（通讯定位 / 任务跳转 / 技能大类跳转）**优先**于记忆并同样写进记忆；**不记**搜索框与排序。**⑥验证**：typecheck 四包 0 错 · core 全绿 · l10n/content/ui:rot/ui:theme 全绿 · 桌面与网页产物已重建（生产串自检十个会话键在位）。⛔ 恢复得准不准属**真机/观感项**，交船长实测；⚠ 口径提醒：记忆挂在**本次 App 运行**上 ⇒ 同一次启动内换档/重置后页面位置仍保留（"本次启动"的字面口径）。
- 2026-09-26：**修玩家报障「打捞残骸捞不到 H 族残骸，只能捞到该星系默认的」（二号 · d2）**——**①玩家原话（照抄）**：「**玩家反应打捞残骸捞不到H族残骸，只能捞到该星系默认的。**」**②真因（真档实测 + 只读探针）**：**残骸场比"占领"活得久** —— 入侵残骸（`state.weekendWrecks`）48 小时自然衰减、活动结束也不清空，而打捞型号池原先**只在"此刻仍在占领名单里"时**才并入入侵舰队 ⇒ 三种星系的读数都是「残骸条写着入侵残骸、捞出来全是默认残骸」：a) **旗舰期的核心**（核心夺回后旗舰才现身，玩家正是在这里打旗舰、注入最大一笔残骸）；b) **夺回后的外围**（打赢即夺回，残骸留在那儿慢慢衰减）；c) **上一场的遗留场**。船长真档实测：**回音荒区**（不在本场占领名单）残骸密度 136、池内只有 `ano-echo-haunt` ⇒ 只出默认残骸；而仍被占的**暗星坟场**池内有 `ink-raid` ⇒ 正常出 H 残骸。**③修法（两件）**：a) **残骸场记来源族** —— `WeekendWreckRecord.family?`（兼容字段 · 零迁移），两个注入点（遭遇 `encounters.ts` / 远征 `expedition.ts`）带上"击败的是哪一族"，存档清洗器保留、打捞写回不丢；b) **有场就并入那一族的独立入侵卡** —— `salvaging.wreckPoolOf` 补第二刀：只要该星系残骸场有效密度 > 0，就把 `weekendWreckFamilyOf`（记录里的族 ?? 回落当前事件族）那一族的 `hidden` 独立卡并进池。⚠ **池子只决定"能捞出什么型号"，不额外造残骸**：能捞的总量仍是残骸场那一份，扣减口径一字未改。**④真档复验（内存里跑真 `pullOneWreck`）**：回音荒区 40 轮 ⇒ **`wreck-h-hi`**（改前只有默认残骸）· 暗星坟场 40 轮 ⇒ `wreck-h-hi` ＋ 原卡残骸。**⑤验证**：typecheck 四包 0 错 · core **2,479 例全绿（224 文件，+5 例新用例）** · `content:check` ✓ · `l10n:check` ✓ · **`save:roundtrip-audit` ✓（新字段一个键不少）** · `build` ✓。⚠ **一处口径说明**：极老的场**没记来源族**（字段是今天才加的）⇒ 回落**当前事件族**：船长那笔 A 族时代的遗留场，现在会捞出 H 族残骸（玩家侧看到的就是"入侵残骸 ⇒ 入侵者的残骸"）。要按"当初那一族"出，得再给旧档补一次族账（无从考据）——现按回落处理。
- 2026-09-26：**墨潮帮（H 族）势力装备三件套上线：墨潮电子舱 · 墨潮捕获网 · 墨潮重袭无人机（船长令 · 二号 · d2）**——**①船长原话（照抄 · 四轮）**：「**墨潮帮势力装备定为射程压制、捕获网、重袭机，分别占据高槽，高槽，攻坚机。**」＋「**敌人的射程压制挂载件好像也还没有命名？墨潮干扰舱改名墨潮电子舱。CPU使用提高到150**」＋「**1按你推荐来2，只电子舱。3，玩家的捕获网和武器一样有冷却周期，独立瞄准，不看命中 ，击沉携带者才解除，或者对面被击沉，不选取重复目标。对方被击沉后进入冷却，冷却结束选择新目标。重袭机单发16的动能伤害，40/80/100血量，4500射程，0.05闪避其他不变**」＋「**我方捕获网移除武器射程下降的效果，其他没问题了**」。**②三件（掉 H 族稀有残骸的高级箱 · 5%／箱 · 集齐前不重复）**：a) **墨潮电子舱** `mod-lair-ecm-h`（高槽 support）——**敌舰武器射程 −15%**（与电子舰那套**乘法叠加**：一件 ＋ 一艘 = 27.75%；地板 3,000m 等既有口径继承）· **CPU 150**；b) **墨潮捕获网** `mod-lair-web-h`（高槽 support）——**周期装置**（20 秒），**独立瞄准 · 不看命中 · 不选重复目标**，开战即钉一艘未捕获敌舰；效果 = **机动 ×0.1 · 推进器全关 · 闪避归零**（⚠ **不含"射程下降"**，船长明令移除）；**目标被击沉** ⇒ 记冷却、冷却结束选新目标；**携带者被击沉** ⇒ 解除；无目标可选 ⇒ 保持待发不空转 · CPU 44；c) **墨潮重袭无人机** `drone-ink-heavy`（**攻坚机** · 消耗品 ×10 架／箱）——单发 **16 动能** · 三层血 **40/80/100** · 射程 **4,500** · 闪避 **0.05** · 命中 0.85 · CPU 13 · `exclusive`（无蓝图、不上市场）。**③连带三件**：a) **敌方射程压制迁成具名挂载件「墨潮干扰阵列」**（`foe-mount-ink-range-debuff`，pct 0.5 数值一字不变）——从此它进敌舰悬停/战报的挂载件清单与 `foe:export` 的「H 族 · 挂载件」表，与我方那件成为敌我同源的一对；b) **H 族残骸回收打开**（删 `wreckRecycleClosedOf` 的 H 判据 ＋ 退役拒因 `core.industry.084` ＋ 工业页摘卡过滤退役）——这正是当初关闭它的理由；c) 计数连带：无人机 7→**8** 种 · 物品 101→**102** · 挂载件 13→**14** · 新增 3 条市场收购行（两件模块按"同槽位最高档 ×4"= 40,000,000 · 无人机 48,000）。**④验证**：typecheck 四包 0 错 · core **2,465 例全绿（222 文件，+14 例新用例）** · `content:check`（新增两类效果字段校验 · 挂载件白名单 · 计数三处同步）· `l10n:check` · `ui:rot-check` · `ui:theme-check` · `build` ✓。**⑤顺带修好的既有用例三处**：`ink-tide` 的舰级字段断言改为"挂载件供给、运行时读数不变"；`recycle` 的"暂不可回收"改为"已打开且能起炉"；`market` 的收购额度用例（**种子敏感**：新增 3 条收购行让相位再次平移，旧 seed 1 从 50,000 掉到 1,200 ⇒ 改用 **seed 5**，逐种子读数 1200/50000/22000/400/50000/400/22000/400/22402/22401 已写进用例注释）。**⑥工作文档**：`docs/design/ink-faction-gear-20260926.md`（归档时并入本条 ＋ 词典，原文件删除）。⛔ 待船长复核：两件模块的市场收购价按"同槽位最高档 ×4"取到 **40,000,000**（同族其余专属件是 7.6~12M 一档）——若嫌高，改 `marketCatalog.ts` 那两行的一个数即可。
- 2026-09-26：**章鱼人削血窗口 2 小时 → 24 小时（船长令 · 二号 · d2）**——**①船长原话（照抄）**：「**当入侵的旗舰出现后，章鱼人的削血速度降低，延长到默认最多24小时才能削完**」。**②落地（单点常量）**：`WEEKEND_FLAGSHIP_DEADLINE_MS` **2h → 24h**（`weekendEvent.ts`）⇒ 削血速率 = `池子总量 ÷ 窗口` 随之变慢，满血在线 1 小时削 **6,250** 点、**最多 24 小时削空**；窗口四处同源（倒计时读数 · 削血速率 · 池子读数 · 收口）⇒ 一处改全动。**调试档不动**（仍 10 分钟，调试便利）。**③离线保护的连带（重要）**：`WEEKEND_OFFLINE_SHIELD_MS` 仍 24h ⇒ "离线 >24h 即保护失效"那一档的收口 = `自离线满 24h 起算 ＋ 一个 24h 窗口`= **离线满 48 小时才被章鱼人摧毁**（改前：24h ＋ 2h = 26h）——离线 30 小时回来的玩家会发现旗舰还活着。**④验证**：`weekend-event` 新增 1 例（正常档窗口 = 24h · 倒计时读数同源 · 1 小时削 6,250 点 · 调试档仍 10 分钟）＋ 离线保护用例与旧档迁移用例按新窗口重写（旧档换算分母 2h → 24h：半小时 = 3,125 点）· typecheck 四包 0 错 ·core 全绿 · content:check ✓ · l10n:check ✓ · build ✓。
- 2026-09-25/26：**周末星域入侵上线：旗舰战与战前准备 · 入侵经济改版 · 演出与开放（二号 · d2 → main · 已推送 `4f180e17` 一线 · 公告已发）**——**①船长原话（照抄要点）**：「**旗舰战入口和准备界面也要做一下，玩家完成核心区域的收复后，在入侵窗口内显示发现敌方旗舰，点击后进入战前准备界面，和进入虫洞类似的舰船选取界面。**」＋「**1，星图核心星系的星系详细里要添加入口。其他按你推荐。**」＋「**入侵舰队不应该有赏金**，而且**添加的残骸是原星系的残骸密度，需要独立的残骸条**（**入侵残骸没有星系的残骸保底，因为随时间消减到最后会消失**），因为**击败入侵舰队就能获取进度，在结算时候直接按进度获取收入**。」＋四答（「**每 1% 固定 20 万 ISK（与星系无关）**」「**按照击败卡的威胁注入**」「**入侵残骸不算当地星系密度，因为是独立的。48 小时线性衰减**」「**打捞界面置顶**」）＋「**母舰哪怕残血，在战斗中血上限依旧保持不变。**」＋「**母舰当前血条不要按照三个等比扣除，应该按照护盾-装甲-结构的顺序扣除。**」＋「**章鱼人 = 真实削减血量所以并不需要显示章鱼人削减进度和倒计时。**」＋「**开战准备界面没有收藏舰船置顶**，**旗舰战无法进入战斗画面**…**建议当出现入侵时，关闭敌方势力活跃。**」＋「**给入侵触发加一个前提，需要拥有至少40声望，才会触发入侵。**」＋「**打算给入侵发生添加一个开场动画，当入侵发生时，游戏屏幕的正上方和正下方出现警告式的红灯闪烁，然后这里预留一个声效的接口**…**且入侵结束后的通讯发送给玩家时，屏幕上出现类似庆祝的烟火动画**」＋「**我希望增强H族敌人的近防炮强度。其近防炮伤害增加50%，命中提高5%**」＋「**先关闭对应的精炼炉，等势力装备出来再说**」（H 族残骸）＋（开放）「**现在可以解除限制，并在一会22点开始第一次入侵活动。**」。**②落地（十余批）**：a) **旗舰战与战前准备**——`core/weekendLaunch.ts` 新 `WEEKEND_FLAGSHIP_MAX_SHIPS=4` · `weekendFlagshipPrepView` · `weekendPrepSquadOf`（落盘优先、失效回落）· `weekendBestFlagshipSquad`（按战力自动选）· 缺口判据（未装武器 / 未带弹药 / 装甲·结构低于 60%）＋ 新面板 `panels/WeekendFlagshipPrep.tsx`（虫洞同款船位卡 · 上限 4 · 收藏置顶 · 软提醒不拦人）＋ **两处入口**（事件日志底部红色活动框 · 星图核心星系详细）；b) **共享血条**——`剩余 = 150,000 −（玩家已伤 ＋ 章鱼人已削）`（`octopusHpDone` 改血量口径 ＋ 旧档就地迁移）· 三层按 **护盾→装甲→结构** 从最后一层往回灌（`weekendFlagshipLayersOf` ＋ `FoeOverride.bossHpLayers/bossMaxLayers`，**不只是显示**：残血母舰开打时护盾已为 0）· 战斗内血条分母恒 = 池子总量（`bossHpMax`）· 章鱼人"独立进度/倒计时"两格撤掉 · 削血窗口单点 `weekendFlagshipWindowMs`（**正常 24h** / 调试 10 分钟）；c) **入侵经济改版**——入侵场次**赏金恒 0**（远征与遭遇两条路）＋ **进度收入** `WEEKEND_PROGRESS_ISK_PER_PCT = 200,000`（按 `ev.contributed` 台账 · NPC 铺底不计 ⇒ 外围一场 +10% = 200 万）· 夺回奖励改**记账 → 活动结束统一发**（`reclaimPending`）· **独立残骸池 `state.weekendWrecks`**（注入 = 击败卡威胁式 · 遇袭 ×0.5 · **不进星系密度、无保底、48h 真线性衰减到 0 即销账**）· 打捞合并计量且**先扣入侵池**· 星图与星系详细「入侵残骸」条置顶；d) **抽签与区域池**——主动出击**每场重抽**（`assaultDraws` 计数 · 价钱恒 = 该星系原卡 ×1.4 · 板面仍列驻留卡）· 遇袭每场重抽（盐 = 5 分钟窗）· 撤退 / 自动脱离 / 超时**照记对母舰的伤害**；e) **一批报障修复**——换波刷新期望距离与玩家侧距离上界（"按第 0 波算"第三次复发）· 干扰舰阵亡后压制与射程恢复 · 卡面编成主体排前（`ink-flagship` W3 / `ink-main` W2）· 快进后入侵不刷新（`weekendClockOf` 取真实与模拟墙钟较大者 ＋ 快进按 1 小时补跑入侵拍）· 存档墙钟账只进不退 · 旗舰战补**第三宿主**（战斗屏挂载 / 距离条 / 撤退 / 逐舰承伤与机群战损）· 结算通讯被战场盖住且被随手点掉（弹窗让位战场 ＋ 450ms 上膛）· 第二排敌舰血条压数字（DOM 列盒 == 锚点）· 结算面板「未击沉」判据 · 星图入侵标记悬停可达（读数挂圆点 · 条与 ★ `pointer-events:auto` · 光晕保持 `none`）；f) **演出与开放**——警报红灯（上下灯带 · 闪 3×0.8s · 演完再弹通讯）＋ 结束烟火（4.2s · 按场次号只放一次）＋ 声效接口 `game/sfx.ts`（默认空操作、绝不抛错）· **声望前提 40**（协会 DSI · 调试不受限 · 只判开新场 · 不足静默不开）· **锁 H 族**（`WEEKEND_LOCKED_FAMILY='H'`）＋ 调试模式**两场收复**（`WEEKEND_DEBUG_WIN_GAIN=0.5`）· **解除调试限定**（`WEEKEND_DEBUG_ONLY=false`）＋ 首场 `WEEKEND_FIRST_T0_WALL_MS` = 2026-09-25 22:00 ＋ 公告《星域入侵玩法上线》发布 ＋ 修"同周重开"（窗口内不再开新场）；g) **H 族数值与强化**——船长两轮 Excel 改值同步（命中 ＋0.25~0.4 · 突击舰闪避 0.47 · 重袭机 180→**600** 血 / 60→**120** 单发 / 命中 **1.3** / 射程 7,000→**12,000**）＋ 主力卡威胁 129→**132**（残骸侧按 `wreckThreat` 冻结仍 124）＋ **近防炮按族覆写** `pdFamilyOverride.H = { dmgMul: 1.5, accAdd: 0.05 }`（期望 ×1.61）＋ 干扰舰挂**捕获网**（不加冲锋）；h) **H 族残骸暂不开放回收**——`wreckRecycleClosedOf`（按组族 H）＋ 起炉拒绝 `core.industry.084` ＋ 工业页摘卡（打捞产出 / 市场收购行 / 回收画像一字未改）；i) **奖励口径修正**——周末稀有残骸按"件"补足（×`RARE_WRECK_VOLUME_M3`=30 ⇒ 旗舰 ×3 件 = 90 m³ · 夺回 ×8 件 = 240 m³ · 贡献 ×12/8/4/1 件 = 360/240/120/30 m³）；j) **测试档两份**（`test-save-weekend-*`：旗舰已现身待开打 / 血池剩 1,000 一场见击沉）；k) **工具**——`foe:export` 加 H 族三张 sheet 与「无人机（敌用机型）」sheet（试算 = 实算自检 ＋ `assertCleanCells/assertCleanCsv` 脏值即红退出）· `battle:layout` 加两节（敌排 DOM 盒中心 == 锚点 · 同排血条互不遮挡）· `weekend:board` 巡检工具入库。**③验证**：typecheck 四包 0 错 · core **2,436 例全绿（217 文件）** · `content:check`（新增五条契约：入侵演出 · 入侵声望前提 · 入侵标记悬停可达 · 入侵行红框 · 入侵开放前必须发公告）· `l10n:check` · `ui:rot-check` · `ui:theme-check` · `ui:layout-css:check` · `build` · `save:migrate` 88/88 · `docs:index`；多数批次配**反向验证**（先确认无修复时用例红）。**④归档**：工作文档 `docs/design/weekend-flagship-prep-20260925.md`（777 行 / 38 节）与公告待审稿 `docs/design/announcement-draft-20260925-weekend-invasion.md` 按 §8 归档（结论并入本条 ＋ `docs/glossary.md` 周末入侵词条 ＋ `docs/design/weekend-invasion.md` §十，原文件删除）。⛔ **遗留**：① A 变种 / C / G 三族仍是占位派生卡（只换名字 / 威胁 / 奖励，敌人编成照旧）⇒ M2 未完；② M3 两个新族未开工；③ **H 势力装备 3 件未做** ⇒ H 残骸回收关着；④ 黑匣只是"壳"（用途随改装件 / 特殊装备那批）；⑤ 「旗舰现身」日志每场各一条、**不加场次号**（船长 2026-09-25「①不用」）；⑥ 一号另批（悬赏板面去重 · 出发归属串星系 · 锁区域池边界）已落地并归档（`e6151096` / `75c93421` / `9c7fab32` / `bf68a3b6` 一线），本条目只登记不重复。
- 2026-09-26：**虫洞自动探索改「真进洞跑一趟」＋每次占 1 枚 AI 核心（船长令 · 三号 · ui-redesign-2 → main）**——**①船长原话（照抄）**：「**玩家翻译自动探索虫洞要占用4个核心，且收益太低了，我打算降低为只占用1个核心，且优化自动探索**」＋**关键澄清**「**我的优化逻辑指的是，自动探索舰船进入洞后获得的收益逻辑**」＋四答（「**1 保持 5 分钟**」「**2，要**（货舱当真上限）」「**3，快速对判**」「**4 允许**（不追深度只求稳）」）＋「**允许多趟并行，文案改为每次自动探索占用1枚AI核心**」＋「**收益按照80%折扣**」。**②核心占用**：`ai.ts` 的 `aiCoreShipUsed` 里自动探索那段由 `Σ shipIds.length` 改 **`Σ 1`**（副船任务仍 1 枚/条，两段口径分写）；`wormholeAutoCandidates` 的编队预算由 `min(4, 可派核心)` 改 **4**（改前只有 1 枚核心时只能派 1 条 ⇒ 收益掉到 1/4）；守卫按 `need=1`、结算 `coresReleased=1`、`save.ts` 读档按新口径重算（不迁移数值）。**③收益重做**：新增 `core/wormholeAutoSim.ts`（**按手动进洞规则真走一遍**：网格 / 边走边扫 / 贪心访产出格（遗迹优先）/ 每动作 1 堆 / 层末守卫快速对判 / 货舱上限 / 层收益曲线），`settleRun` 四条产出线改读模拟器结果，**删掉「手动期望 × 40%」折算**（`WORMHOLE_AUTO_YIELD_MUL` 退出结算、UI 上 40% 读数一并撤）；损伤改**独立随机流**（同种子同序列 ⇒「科技减伤」可复现）；战斗按船长口径做 **DPS×有效血量快速对判**（留 15% 余量，打不过就绕开/留层多捞）。**④三条口径修正（都是船长追问逼出来的，记牢）**：a) **扫描一次 = 1 回合揭 7 格**（`gridScanTargets` + `WORMHOLE_SCAN_RADIUS_BASE=1`），**不是"每格 1 回合"**——按每格算会把扫描成本放大 7 倍、4×T3（45 回合）连层 3 都上不去；b) **扫描半径 = 1 + Σ 编队各船 `wormholeScanRadiusBonus`**（鹦鹉螺 `sh-nautilus` / 鲸盟护卫 `sh-wh-{a,d,g}-frigate` 各 +1 可叠加 ⇒ 4×鹦鹉螺 = 半径 5、一次 91 格；**科技树里没有半径加成**，`whScanCut` 是"扫描间隔"另一回事；装置「深空测绘仪」每台 +1 圈）；c) 对手（层末守卫）威胁用**游戏真值** `wormholeLayerThreat(depth)`（层 1 = 45、每层 ×1.10），自造量级会让 4×T1 连层 2 守卫都过不去。**⑤满科技读数**（`mt-explore-turn` +100 回合 · `hold` +12 格 · `whSalvageEff`/`whCollectEff` 各 +60%；编队 4×扫描船；200 颗种子）：**平均层深 7.1 · 残骸 3410 m³（8 折后）· 稀有残骸 11.14 件 · 安全货柜 5.16 件**；对照旧口径（残骸 600 m³ / 稀有 0.50 件 / 货柜 9%）≈ 5.7 倍。**⑥稀有/货柜曾恒为 0 的真因**：不是遗迹层规则（真实网格层 4 平均 2.6 个 · 层 6 4.2 个 · 层 8 6.5 个），而是**策略把遗迹挡在"浅层留手额度"之外** ⇒ 改为「留手额度只管占货舱的东西（矿脉/坟场）；遗迹出稀有残骸与安全货柜、不占货舱 m³，不受额度限制」。**⑦验证**：typecheck 四包 0 错 · core **214 文件 / 2415 用例**（含新增"1 枚/趟""核心不够开不了第二趟"两例）· content/l10n/ui:rot/ui:theme/art:ships 契约全绿 · `build web` ✅。⛔ **未推送**（推送闸门）。
- 2026-09-26：**敌舰 30 条逐舰线稿 ＋ 逐舰取形/挂点接线（船长令「乙案」· 三号 · ui-redesign-2 → main）**——**①船长原话（照抄）**：「**我需要给新的敌族SVG绘制舰船外形，且敌方的旗舰需要一个独立形象。你可以问一些族群设定**」→「**既然都H族画了，那旧版敌人能否按照敌舰不同做出些许区分？**」→（裁定）乙案：**旧版敌人 30 张逐一手绘**、H 族直接采用已交的 v3 六张；后续报障「**现在的问题只有C组敌人没有封闭外形**」。**②落地**：新增 `ui/shipArtFoe.tsx`（30 条逐舰线稿，键 = 数据侧舰级 id；含 `nozzle/turret/box/cyl/bridge/hex/pod/blob/edge` 基元）· `ui/shipMounts.ts` 新增 `FOE_SHIP_MOUNTS`（30 条逐舰挂点，逐条注明取自哪条路径端点；`mountsOf` 逐舰优先于族级）· `ui/ShipSprite.tsx` **修三元短路硬伤**（原 `shipId ? SHIP_ART : foeKey ? FOE_ART : …` ⇒ 敌舰一带 shipId 族形分支永远走不到）改三级回退 · `BattleScreen` 逐单位传舰级 id、`Expedition` 传**代表舰** · core 新增只读反查 `foeShipIdOfTag` / `foeCardShipIdOf`（与舰名/档位/体积同一份编成枚举，界面不另推）· 新闸门 `npm run art:ships:check`（美术键 ↔ 舰级表一一对应；键写错只静默落族形）。**③写形口径（踩过的坑）**：船体纵带 `y36~96`（防"细横条"）· 不在一条 path 里交叉画纵横面板线（防"网格铁笼"）· C 族走 `blob()` 圆角有机轮廓（防机械感）· **`blob()` 控制点必须落在原顶点上**（早先"两端内缩 r + 补线"会漏 `2r` 缺口 ⇒ 轮廓裂成碎线段，正是船长报的"C 组没有封闭外形"）· **开放折线不当骨架、独立闭环不当附肢**（`fill:none` 下线就是线、环就是空心环）⇒ 突出部件一律**并进船体轮廓**（尾部开叉/背脊凸起）。**④读数**：闭合性审计 30 条 **全部有闭合轮廓、无未闭合折线、无出画布**（改前 C 族 5 条各有 5/1/1/3/2 段散折线）；界面取证（探针 `_probe-foe-art.mjs`）悬赏窗 **23 张卡 / 23 个舰影 / 22 张逐字命中**美术表的舰级形，**同族不同卡命中不同舰形**。**⑤验证**：typecheck 四包 · core 全绿 · content/l10n/ui:rot/ui:theme/art 契约全绿 · `build web` ✅。⛔ **未推送**；战斗画面未做真机取证（测试档里没有"正在打"的存档）。
- 2026-09-25：**常驻悬赏·入侵板面四件（出发归属串星系 · 板面同星系只出一条 · 每场进度反馈与活动栏读数 · 外围/核心区池边界）（玩家报障 · 一号 · main · 已推送 `46373613`）**——**①船长原话（照抄）**：「**本地存档，在常驻悬赏内，有2个红环航道的入侵悬赏**」＋「**而且玩家反应，从常驻悬赏进行重复清缴，不会增加进度条**」＋（**纠正一号的错判**）「**② 是对应的星系，玩家打的红环常驻悬赏，不加红环的进度条。**」＋「**玩家反应，他在外围遇见联动H族主力舰队卡**」＋「**我怀疑和之前的错位有关**」＋裁决「**甲：同星系只出一条（不分族）**」＋批「**甲＋乙＋丙**」（进度反馈／活动栏读数／核心门禁提示）＋「**修复后就推送**」。**②四件其实同源**：a) **出发归属串星系（真因 · 三件报障都由它生）**——H 族"每星系抽一支驻留舰队"只在自家那几张独立卡里抽 ⇒ **多个被占星系抽到同一张**（真档：深渊之门/暗星坟场/红环航道都是 `ink-raid`），而出发是按卡 id 反查星系（`anomalies.find(a => a.id === id)`）⇒ **永远命中列表第一个** ⇒ 威胁覆写/进度记账/残骸注入/结算星系名/遇袭选卡**全串**。真档读数（新工具 `npm run weekend:board` ④/⑤ 段）：点核心或坟场的行都归到红环（若"第一个"是核心，则点红环一分进度不给 = 玩家报障原文）；**"在外围打到主力舰队"也是它**（错位到核心 ⇒ 用核心池 {袭击, 主力} 抽签）。修法 = core 新 `weekendLaunchGalaxyOf(state, hint, cardGalaxy, now)`：**以界面上那一行的星系为准**；引擎 `foeGalaxyOf(id, hint?)`／`startExpeditionAt(id, foeGalaxyId?)`／`startExpeditionFromMiningAt(id, foeGalaxyId?)`；界面三处出击调用点传 `anomaly.galaxyId`；**没传 hint ⇒ 老口径逐字不变**。b) **板面同星系只出一条**（船长裁决甲）——core 新 `weekendBoardRowsOf`（只对被占星系生效；A/C/G 派生卡保留各自原卡 id ⇒ 不误删）＋ 引擎 `refreshAnomaliesView` 接上 ⇒ 真档板面 **5 条 → 4 条**。c) **每场胜利给进度反馈**——`weekendResolveBattle` 新增 `core.weekend.035`「✦ <星系>：夺回进度 a% → b%」与 `core.weekend.036`「✦ <星系>：外围未清完，本次不计夺回进度」（夺回那场不重复、战败不写）；活动栏补「外围平均 N%（`ui.weekend.109`）· 外围进度最高 N%（`ui.weekend.108`）」——**为什么两条都要**：实测红环已 74% 时连清坟场 3 场，"最高"一个数都没动。d) **核心门禁提示上板面**（船长批丙）＋ **区池边界回归锁** `weekend-pool-region.test.ts`：**2880 组（场次×位序×盐）扫描 外围抽到主力 0 次 / 核心 1396 次**；三个抽签口（驻留／主动出击重抽／遇袭 12 档）逐口断言，核心必须能抽到主力作反向对照（防断言空转）。**③验证**：typecheck 四包 0 错 · core **221 文件 / 2448 用例** · content/l10n/ui:rot/tools:audit 全绿 · 新正式工具 `npm run weekend:board`（只读真档巡检：逐槽位替换／去重后板面／台账与门禁／**出发归属并排打印修后 vs 旧口径**／区池×重抽）· 远端 `main` = `46373613`，Pages 已发布新产物 `index-RHgdTZlL.js`。⛔ 遗留：无。
- 2026-09-25：**玩家报障两件：激光打 H 族"会 MISS"（复核＝不是 BUG）· 稀有残骸轮扣了普通池却不给普通残骸（已修 · 甲案）（一号 · main · 已推送 `bf6e1c74`/`a02565a7`）**——**①船长原话（照抄）**：「**玩家反应，激光武器在打H族敌人时，会出现MISS的情况**」＋「**前面激光MISS的船，他同时携带了陵卫连装炮，劫掠转管炮，和2个激光MK3**」＋「**玩家反应，当星系的残骸里有稀有残骸时，打捞稀有残骸会同时消耗普通残骸，但是没有回收普通残骸**」。**②复核与修法**：a) **激光没有 MISS 通路**——`slot==='laser'` 的件一律 `kind:'beam'`（`hitRate` 硬写 1 · `dmg = max(1,…)` · `autoHit=true`），命中判定 `hit = dmg>0 && (autoHit || …)` 恒真；玩家那套配装的 MISS **全来自两门动能炮**（陵卫连装炮 0.76 · 劫掠者转管炮 0.84）对上墨潮突击舰闪避 **0.59**（0.22→0.47→0.59 同日三档）。实测 `laser-no-miss.test.ts`（真实数据 vs `ink-harass`）：沙猫 激光 4 发/MISS 0 · 动能 3 发/MISS 2；**玩家原配装 激光 6 发/MISS 0 · 动能 11 发/MISS 10（命中 9.1%）**。b) **稀有轮扣池**——`pullOneWreck` 稀有分支照样调 `salvageRoundPull`（契约 = "扣减 ↔ 本轮按 mul 出普通残骸"）却把产出换成固定 30 m³ 的稀有残骸 ⇒ 扣了不给；修法 = 新增**只读**口 `salvageRoundMulOf`（与 `salvageRoundPull` 共用算式），稀有轮只取读数、**星系池与入侵池都不扣**。读数 `rare-wreck-round.test.ts`（3 例）：稀有轮 星系池 190 → **190**（旧实现白掉 4.60）· 入侵池 50 → 50；对照（稀尽回常规池）190 → **186.40** ⇒ 放干本身没被一起关掉。**③验证**：typecheck 四包 0 错 · core 全绿 · `content:check` ✅。⛔ 备选未做（等船长定）：**乙案**"稀有轮照扣、同时给 1 份普通残骸"（高密度星系一轮约 +94 m³ ⇒ 相当于变相加量）。
- 2026-09-25：**存档三件：丢失防线（浏览器不给写必须让玩家看见）· 网页版可把存档写到本地文件 · 公告已发（玩家报障 · 一号 · main · 已推送 `84e6f808`/`1b6993d1`/`c16fd944`）**——**①船长原话（照抄）**：「**玩家反应，他的macbook关掉游戏后存档就丢失了**」→「**是Safari ，下午玩的，这会发现没存档了。**」→「**按你推荐来**」（＝ 甲＋乙＋丙＋丁 全做）；「**存档能否除了浏览器默认存档，优先保存到本地？就很我本地运行一样**」→（两点追问后）「**按你推荐来**」⇒ ① 做；② 冲突判据 **甲案：按档内 `savedAtWallMs` 取较新**；「**存档相关的公告没有一并推送吗**」→「**按推荐**」。**②落地**：a) **防线**（`game/saveGuard.ts`）——存档探测（网页版写/读/删探针键；桌面版只读 `listBackups`）＋ 写失败与不可用通知 ＋ 持久化存储申请；`boot.ts` 在启动引擎**之前**先探 ⇒ 不能写就**明说**，不再静默丢。b) **网页版本地文件**（`game/saveFileHandle.ts`）——File System Access 绑定（文件名 `大鲸鱼-深空放置-save.json` · IndexedDB 记绑定），`storage.ts` 的 `load()` 取"文件与 localStorage 里按 `savedAtWallMs` 较新者"，`save()` **双写**（先文件后 localStorage；文件失败即报警）。c) **公告**——`ann-save-local-20260925`「存档更稳：网页版可把存档写到本地文件」（分类 系统 · 五条 · 第三人称），**已过船长审核并写入** `packages/data/src/announcements.ts`，与代码同批推送。**③验证**：typecheck 四包 · core 全绿 · 两条新工具读数（`save:guard-check` / `save:webfile-check`）· Pages 产物核对。⛔ 遗留：那位 Safari 玩家的**老档**需他自己按指引找回（同源 404 页上取 `localStorage` 值 → 导入）。
- 2026-09-25：**调试模式「只有本地开启，发布版关闭且隐藏」（船长令 · 一号 · main · 已推送 `eb0f9a17`）**——**①船长原话（照抄）**：「**推送前关闭设置里的调试模式开关**」→（追问后）「**是否可以设置只有本地开启，上传后的版本都是关闭隐藏的**」＋「**你先完成设置的改动**」。**②口径**：`@whale/core` 新 `isLocalDebugOrigin(protocol, hostname)`（http(s) ＋ `localhost`/`127.0.0.1`/`::1`/`*.local`/私有网段 ⇒ 本地；`file:` 与公网 ⇒ 发布版）；设置页那一行**只在本地渲染**，顶栏「⇄ 调试」「⏱ 性能」在发布版**永不出现**（手动置标志也不生效）；`game/perf.ts` 同判据。**③验证**：`debug-gate.test.ts` 四种协议×多主机逐条断言 · typecheck/core 全绿 · 本地构建上实测"设置里看不到那一行"（读数：正确隐藏，非 BUG）。⛔ 遗留：无。
- 2026-09-25：**手机竖屏战场不再「被压成一条」（船长报障 · 甲＋乙 · 一号 · main · 已推送 `66122299`）**——**①船长原话（照抄）**：报障「**战斗画面遮挡住了所有战斗画面**」（附截图）＋「**之前的解决办法是手机端缩放，为什么现在消失了？**」＋裁定「**甲+乙，适当缩放和适当压底栏**」＋（两处留白是否收紧）「**按你推荐来**」。**②落地**：a) **甲 整块等比缩放**——新 `ui/battleFit.ts`（`fitScale` ＋ `useBattleFit`）：先量车道自身内容高（临时 `flex:0 0 auto` ＋ `minHeight:0` 读 `scrollHeight`，避免与最小高度互推振荡），再按 `k = min(1, 可用 ÷ 需要)` 缩放；`k = 1` ⇒ `height:100%` 且**不加 transform**（桌面零变化）。b) **乙 压底栏**——移动端旋转布局下图例/装填条 `nowrap` ＋ 横滚、`.app-bts-chip.is-foe { order:-1 }`、`> * { flex:0 0 auto }`（漏这条时 chip 被挤到换行，实测图例条 112 → 178 的回归就是它）。⚠ 上述规则必须放在 2026-09-06 那批移动端块**之后**（同特异度靠顺序决胜，放前面会被顶掉）。**③验证**：`npm run ui:battle-fit` 读数（390×844：缩放后内容高 ≤ 车道高 · 无裁切）· typecheck/core/ui:rot 全绿 · 桌面端 `k=1` 无变化。⛔ 遗留：观感以船长真机为准。
- 2026-09-26：**入侵悬赏板面三件：同星系去重 · 进度可见（日志＋活动栏读数）· 出发归属修「串星系」（船长裁决甲 ＋ 批甲乙丙 · 三号 · main · 已推送 `c7f4c5a9`）**——**①船长原话（照抄）**：「**本地存档，在常驻悬赏内，有2个红环航道的入侵悬赏**」·「**而且玩家反应，从常驻悬赏进行重复清缴，不会增加进度条**」·（追问后）「**玩家打的是常驻悬赏的红环入侵悬赏卡，但是不加进度，是不是因为那2张悬赏其中一张是错误的原因？**」·（裁决）「**甲：同星系只出一条（不分族）**」·（纠错）「**② 是对应的星系，玩家打的红环常驻悬赏，不加红环的进度条。**」·（追问）「**我怀疑和之前的错位有关**」＋报障「**玩家反应，他在外围遇见联动H族主力舰队卡**」。**②报障①＝板面重复（甲案已落码）**：替换是**按槽位逐个做**（`weekendBountyCardsOf` = `cards.map(...)`），而 H 族是"每星系抽一支驻留舰队" ⇒ 同星系多槽位换出**同一张卡**（A/C/G 不会：派生卡保留各自原卡 id）；新增 **`weekendBoardRowsOf(rows, isOccupiedLive)`** —— 同一星系的多条**同一张**入侵卡只留第一条、**只对被占星系生效**（非占领区原卡一行不动）、**不动** `weekendBountyCardsOf` 的"同序整池"契约（遇袭敌群池/打捞池/冷却判据照旧读整池）；接线 = `engine.refreshAnomaliesView` 末尾（`weekendOccupiedLiveAt` 绑 now）。**真档读数（`npm run weekend:board`）：板面 5 条 → 4 条**（红环航道 2 槽 → 1 条，其余各 1）；用例 `weekend-board-dedupe.test.ts` 3 例（H 2 槽→1 条 · A/C/G 2 槽→仍 2 条 · 边界不误删）。**③报障②真因＝出发归属按卡 id 反查 ⇒ 串星系（`75c93421`）**：真因**不是**"进度没记"——实测主动出击**每场确实 +10%**（红环台账 41% → 51% → 61% …）；真因是 `anomalies.find(a => a.id === id)` **永远命中列表第一个**同 id 行（H 族抽签只在本族那几张独立卡里抽 ⇒ 真档三处都是 `ink-raid`）⇒ 串的是**整条链**：出发落盘的 `expedition.foeGalaxyId` → 威胁覆写 · **进度记账星系** · 结算日志星系名 · 残骸注入星系 · 遇袭敌群池选卡；船长档里"第一个"恰是红环 ⇒ 点核心/坟场的行进度全记到红环，**若"第一个"是核心**（门禁锁死）⇒ 点红环**一分进度都不给**（正是报障原话）。修法 = core 单点 **`weekendLaunchGalaxyOf(state, hintGalaxyId, cardGalaxyId, nowWallMs)`**：**界面那一行的星系**（hint）是活的占领区就采信它，否则**逐字回落卡面星系**（结算/打捞/遭遇这些**没有界面行**的路径零变化）；引擎 `foeGalaxyOf(id, hint?)` / `weekendDispatchOf(id, hint?)` / `startExpeditionAt(id, foeGalaxyId?)` / `startExpeditionFromMiningAt(id, foeGalaxyId?)` 逐层传 hint，界面三处（出击 / 确认转战 / 星系出击栏）传 `anomaly.galaxyId`（`autoPerf` 传目录卡的星系）；用例 `weekend-launch-galaxy.test.ts` 2 例；**巡检工具 ④ 段现在并排打印"修后归属 / 旧口径反查"**，真档四条读数 = 深渊之门→`galaxy-abyss` ✅｜暗星坟场→`galaxy-grave` ✅｜红环→`galaxy-redring` ✅｜虚海边缘→`galaxy-voidedge` ✅（**旧口径前两条都会串到红环**）。**④进度可见两件（船长批甲＋乙 · `1ecc72d9`）**：问题是玩家**看不到**、不是没记 ⇒ a) **每场入侵战斗写日志**——外围/核心打赢「✦ <星系>：夺回进度 a% → b%」（`core.weekend.035`）、被**核心门禁**挡下「✦ <星系>：外围未清完，本次不计夺回进度」（`core.weekend.036`；**夺回那一场不补本行**（已有「✦ 夺回…」）·**战败不写**（进度没动，别放噪声））；b) **活动栏补外围读数**——「外围平均 N%」（`ui.weekend.109`）＋「外围进度最高 N%（<星系>）」（`ui.weekend.108`），外围未全清时显示、全清后整行隐藏；⚠ **两个都要**：实测红环已 74% 时连清暗星坟场 3 场「最高」**一动没动** ⇒ 补"平均"（任何一处前进都响应）——一个证明在动、一个指明打到哪了；真档现读 **外围平均 32% · 最高 86%（红环航道）**。c) **丙＝核心门禁提示上常驻悬赏行**（`9c7fab32`）：「星系详细」那张卡先有（`ui.weekend.103/104/105`），本次补上**常驻悬赏列表那一行**（外围未清完时显示「至少需要夺回一个外围星系」／「核心进度暂不推进：还需夺回 N 个外围星系」），**读数与星系详细同源**（`weekendCoreGateView` = `weekendCoreProgressAt` ＋ `weekendPeripheryClearedAt`）。**⑤报障③＝同一条错位的下游（已锁边界 · `9c7fab32`）**：池定义 **外围 {骚扰 `ink-harass`, 袭击 `ink-raid`} · 核心 {袭击 `ink-raid`, 主力 `ink-main`}** ⇒ 站在外围打到主力，只能是**这一场被归到了核心**（旧口径按 id 反查 ⇒ "第一个"是核心时点外围的行就用**核心池**抽签）；回归锁 `weekend-pool-region.test.ts` 3 例 = **2880 组 (seq×位序×盐) 扫描里外围抽到主力 0 次、核心 1396 次**，三个实际抽签口（驻留／主动出击重抽／遇袭 12 档）逐个断言，且**核心 12 场重抽里必须出现主力**（防断言空转）；真档对照读数 = 核心 深渊之门 前 6 场重抽里 **4 场是主力**、三个外围的池**不含主力**。**⑥工具**：探针 `_weekend-board-probe.ts` / `_weekend-repeat-probe.ts`（真档**只读**）已转正式入库 **`npm run weekend:board`**（`tools/weekend-board-inspect.ts`：①逐槽位替换 ②去重后板面 ③进度账与活动栏读数 ③-a核心层连打两场 ③-b**引擎真实结算路径** ＋ 活动栏同源算式 ④**修后归属 / 旧口径反查并排** ⑤区域池 × 每场重抽 ＋ 旧错位会打到什么）。**⑦验证**：typecheck 四包 0 错 · core 全绿（本轮新增 `weekend-board-dedupe` / `weekend-launch-galaxy` / `weekend-pool-region` / `weekend-autoloop` 四份用例）· content:check / l10n:check / ui:rot-check / ui:theme-check / ui:layout-css:check 全绿 · `build web` ＋ 桌面 build ✅ · 船长本地实测。**⑧文档**：关键内容已并入 `docs/glossary.md`「周末入侵」词条（板面去重 · 出发归属 · 区域池边界 · 进度条读数两处修正 ＋ 核心门禁两处提示），工作文档 `docs/design/weekend-board-fix-20260925.md` 已删（细节以 git 历史兜底）。
- 2026-09-26：**入侵悬赏卡四件：敌情徽标 · 敌舰悬停一览 · 装置效果明文 · 星系详细补回「重复出击」（船长四令两报障 · 三号 · main · 已推送 `c7f4c5a9`）**——**①船长原话（照抄）**：「**我希望给星系详细内的悬赏卡片，像常驻悬赏那样，添加一个敌人火力类型，敌人血量类型。且如果是入侵活动的悬赏卡片，玩家鼠标悬停还能看见敌人舰船的大概介绍（一句话介绍敌人舰船的特点）**」·「**特殊装置的效果最好直接解释。比如核心舱段：携带大量无人机的泰坦核心残骸端，受到攻击后会启动反击模式，增加无人机射程。**」·（报障）「**你限定了只能悬停在标题上显示。为什么要限制在标题上？**」＋「**而且敌人有多个种类时，你只显示了其中一种**」·「**建议将敌人舰船的名称和特殊装置染色下。并且因为入侵卡是随机抽取的，你应该显示所有抽取的卡可能出现的敌人。**」·（二次令）「**特殊装置颜色不要和舰船名称颜色一样。建议就特殊装置这四个字染色**」·「**每艘船的介绍不能新起一行吗。不要全放一个段落内**」·（报障）「**现在好了，但是我发现星系详细里的入侵悬赏重复清缴的按钮不见了**」。**②两枚徽标**（`65ab0ac0`）：入侵悬赏卡（板面 ＋ 星系详细）按**常驻悬赏同款**补「敌火力类型」＋「敌血量类型」两行 —— 复用既有 `FoeDamageMix`（`ui/shipInfo`，与战斗结算同源）＋ `ProfileChip`（`defProfile`：护盾/装甲/均衡 ＋ `foeLayerSplit` 百分比读数），**不自造徽标**；`card` 缺省（没抽签）时两行都不渲染。**③敌舰一览（悬停）**：新增 **`ui/foeBrief.ts`** —— `FoeBriefLine{id,name,hull,count,bits,mounts?}` · `briefShipsOf`（按编成折算每型舰的数量）· **`briefsOfPool(anomalies, poolIds, drawn)`**（范围 = **该族该区域整池**（`weekendFoePoolOf` 与抽取同源）**∪ 本卡编成** ⇒ 不再只报"这一仗抽到的那一种"，正是船长第二条报障）· `toLine` · `lineText` · `foeBriefsOfCard` / `foeShipBriefOf`（板面与星系详细共用同一取数口）。**④装置效果明文**（`2fea1f83`）：`mountEffectText(id)` 把 `FOE_MOUNTS` 的字段翻成「**触发条件 ＋ 效果**」白话说清（`droneRangeOnHit` / `gunRangeOnHit` / `web` / `supportCall` / `reviveEscort` / `repairPulse` / `evasionBonus` / `charge`）——例：核心舱段 = **受到攻击后会启动反击模式、增加无人机射程**；**不写原因解释**、括号只放规格（守 §5 说明文案硬规矩）。**⑤三处报障的修法**：a) **悬停范围**——由"只挂在标题上"改为**挂在整张卡**（`hoverTipProps` 挂 `.app-ga-row.app-ga-invasion`），行内不再有自己的悬停；b) **多舰种**——`bitsOf()` 逐型舰都出（一个编成里有几种舰就报几种），范围再扩到**整池**；c) **排版**——每艘船**各占一行**：新增 `.app-ano-foebrief`（块级）／`.app-ano-foebrief-row`（`display:block` ＋ `line-height:1.45` ＋ `overflow-wrap:anywhere`），`.app-ano-foebrief-row + .app-ano-foebrief-row` 给 `var(--wui-sp-4)` 行距；⚠ **这处改动一开始"没生效"，真因＝`styles.css` 的改动没被拆进运行时的两份样式表**（`ui/layout-css/styles-modern.css` / `styles-classic.css` 是 `npm run ui:layout-css` 的生成件、运行时只读它们）⇒ 船长报「**仍挤成一团**」，补生成件后正常（`57c55501`）；**教训：改 `styles.css` 后必须重跑拆分并重建**。**⑥染色（两次校正）**：舰名用**战场同款** `UI_TONES.foeNameMain`（本卡出现的舰 / 有数量）与 `foeName`（池内其它可能出现的舰），不自造颜色；**「特殊装置」只染这四个字**、用 **`UI_TONES.matBattle`（琥珀 224,168,106）**——与舰名那条敌族红（216,160,143）是**不同 token**（深空主题实测两值不同），效果说明保持正文色。**⑦星系详细补回「重复出击」**（`c7f4c5a9` · 船长报障）：那枚环按钮原先只加在**板面**的入侵卡（`AnomalyCard`）上，而**星系详细的被占分支只有「出击」** ⇒ 同一张卡换个地方看就少一枚按钮；现按板面那一套补齐 —— 开关走 `engine.invasionLoopAt(galaxyId | null)`（与 `bountyLoopAt` **互斥**、同一把尺），禁用沿用本组件已有的 `goBlocked` / `reopenBlock`，文案 `ui.weekend.106`（重复出击）/ `ui.weekend.107`（开启说明）/ `ui.Expedition.043`（关闭）；core 侧 `weekendEvent` 记 `autoLoopGalaxyId`（每场从该星系重抽一支 · 胜利自动返航再出发 · 该星系被夺回或活动结束时自动停止），用例 `weekend-autoloop.test.ts`。**⑧验证**：typecheck 四包 0 错 · core 全绿 · l10n:check（新增 id 全有中英两列）· ui:rot-check / ui:theme-check / ui:layout-css:check 全绿 · `build web` ＋ 桌面 build ✅ · 船长本地实测通过（含"挤成一团"复验）。**⑨文档**：关键内容已并入 `docs/glossary.md`「周末入侵」词条与 `docs/roadmap.md` 上一条（板面那批）；本批无独立工作文档（改动点在 `Expedition.tsx` / `foeBrief.ts` / `styles.css` 与 `weekendEvent.ts`，途中与另一会话同改 `Expedition.tsx`，提交前均确认对方工作区干净、且我的提交只含自己的改动）。
- 2026-09-25：**H 族入侵按新规重做（威胁重定价 · 抽签 · 遇袭 ×0.75 · 旗舰轮 · 引擎接线与结算入账 · 两封通讯与结算面板）（二号 · d2 · 已合入 main · 未推送）**——**①船长原话（照抄要点）**：「**我们回到H族上，按照新规对H族进行调整**」＋「**Q1，非旗舰威胁按照90/108/129定，旗舰因为是小队战，之后在讨论。Q2按照敌舰设定的属性比例重定…Q3伏击 ×0.5移除。旗舰卡单独。**」（后改口：「**伏击恢复×0.5，没问题。（伏击并不是袭击舰队）这边的残骸就不冻结了。**」）＋「**能否让入侵遭遇的敌人卡是随机抽取的**」＋「**外围玩家主动出击和被动遇袭都是从骚扰和袭击舰队中抽取。核心区，则是抽取袭击和主力舰队。遇袭的时候遭遇的敌人按强度\*0.75算。**」＋「**血: 3920 →39200**」→「**感觉整体威胁可以上调了**」→「**取170威胁**」→「**BOSS血条15万（约2.5个母舰）来算**」→「**只有不落才会有撤离动画…选甲吧…黑匣先做壳。火力钳制先按照现有的不做改变。**」＋「**那你将工作做完再汇报。活动开关仍是"仅调试模式"，这条不变。**」＋（通讯两封）「**每场都发，但是覆盖上一次的**」·「通讯的跳转**显示详细奖励**，**点击后弹出类似虫洞撤离的结算界面**，显示玩家在**各个被入侵星系的贡献**和**旗舰战的输出**，并列出玩家**获得的物品**（包括旗舰战的掉落物）」·「**通讯中不要使用这种备注的样式**」·「用全称」·「你可以进行润色修改」·（验收）「**可以了，依旧仅调试可见**」。**②落地（五批）**：a) **重定价**——H 三张非旗舰卡按定价式（单舰 ×3）落 90/108/129，属性 = 每艘船血与单发**同乘一个 K**（血÷DPS = 舰级自然比）；旗舰卡 170（**小队 ×10**）、母舰舰级血 ×10（3,920 → 39,200）⇒ 全波 94,439 血 / 峰值 357.7 DPS / 达成预算 2,906 = F(170)；**BOSS 池固定 150,000**（取代两条自适应公式），母舰血条 = **池子剩余**（新 `FoeOverride.bossHp/bossShipId`，随档逐拍套用）。b) **抽签与遇袭**——新 `FoeOverride.strengthMul`（真缩放）＋ `foeThreatOfAnomaly`（定价反解）；外围池 {骚扰, 袭击}、核心池 {袭击, 主力}，`hash32` 独立子流**不消费主随机序列**；板面/主动 = 每场每星系定一支，**遇袭每场重抽**且强度 **×0.75**、标签 = 缩放后实测价（76/91/109）。c) **残骸与黑匣**——残骸组 `h-wh` 退役 ⇒ **`h-hi`（高安组）**（旧组从无产出路径 ⇒ 零迁移）；新物品 **`blackbox-h`「墨潮旗舰黑匣」**（`kind:'kit'` · 稀有度 4 · 只收不卖 · 中英双语）＋ 市场收购行第 9 行。d) **引擎接线与结算入账**——悬赏板/星图改用 `weekendBountyCardsOf`（H = 抽到的**独立卡** · 真实 id · 威胁 = 卡面 · 星系覆写 · 奖励 ×1.4）· 远征落盘 `ExpeditionState.foeGalaxyId`（战后归属与残骸注入不再落母港）· **占领区破例补全**（原只破概率侧、暴露收集侧把中安/高安丢掉 ⇒ 现 `collectExposures(…, includeOccupiedAt)` 两半齐：收得进 · 掷得出）· **贡献奖入账**（新 `weekendSettleAndGrant` ＋ 引擎每拍在 `weekendTick` 前补发；幂等靠 `prizePaidAtWallMs`；**占比按 `endedAtWallMs` 评估**——NPC 铺底是时间函数，晚结会少发）· 夺回奖励（×8 ＋ 2M，全清再 +5M）真入账 · **三条里程碑日志**（`core.weekend.001~003`：夺回 / 全清 / 旗舰击沉；原先只入账不吭声）。e) **两封通讯 ＋ 结算面板**——新**实例通讯**机制（`state.commsInstance` · 固定 id `msg-weekend-warn`/`msg-weekend-settle` ⇒ **每场整条覆盖**；与表消息共用收件箱/已读/未读/弹窗/记账）＋ `weekendSyncComms`（按场次号比 ⇒ 引擎每拍调一次即自愈）＋ **战果快照** `state.weekendLastResult`（每场覆盖）＋**到手台账** `ev.rewardLedger`（夺回逐星系 · 贡献奖 · 旗舰掉落三处累加 ⇒ 清单与实发逐值一致）；文案走 id 制（`core.weekend.010~015` 正文 · `020~023` 族名全称：墨潮帮/海盗/异形生物/鱿烬亡军 ↔ Ink Tide/Pirate/Alien/Deadarmy），**奖励不写括号备注**、改为清单里的一项，由界面按语言拼串；新面板 `panels/WeekendSummary.tsx`（骨相全照虫洞撤离结算 `SettleView`）由结算信跳转按钮弹出，列清缴贡献/夺回星系/旗舰输出/稀有残骸/信用点/旗舰黑匣 ＋ **各星系贡献明细** ＋ 旗舰战一行。**③同批修掉的七处旧账**：`save` 丢 `market.npcBuy[].bm`（布尔门槛标记，engine 写 `bm:true`）· `market` 消化队列排空后残留 `price` ⇒ 往返丢键 · **`weekendEvent` 清洗器漏掉旗舰 BOSS 那 7 个字段**（读档母舰血条回满 / 章鱼削血清零 / 同场幂等键丢失 ⇒ 同一场可能被重复记账）· **遇袭进度两头都反**（打赢记成主动胜利 +10% 而不是击退 +3%、打输记成击退 +3% 而不是 0）· **迎战遇袭一分不给 ＋ 文字结算路径没接**（`settleFight` 先清遭遇槽 ⇒ 归属反推落空；现显式传 `WeekendOutcomeHint{kind,galaxyId,source}`，旗舰那条不套"活的占领区"闸门，否则复发"打完旗舰不结算"）· `weekend-bounty` 里一行断言被字面 `` `n `` 吞进注释（等于没测）· `market.test` 弹药上限混了两条通道。**④验证**：typecheck ✓ · core **2,353 例全绿**（209 文件；新增 `h-reprice` 17 例 · `weekend-wiring` 13 例 · 存档往返 2 例）· content:check ✓ · l10n:check ✓ · `ui:rot-check` ✓ · `ui:theme-check` ✓ · build ✓ · docs:index ✓ · **真档体检** `save:migrate` 84/84 · `save:roundtrip-audit` 83 份丢键 0 / 类型变化 0 · **普通玩法 30 天回归**（`playthrough-sim`：20/20 星系 · 现金 16.8 亿 · 策略黑洞 0 次；⚠ 该 sim 只跑 core、不含入侵，它证的是"普通玩法不受影响"）· 一次性探针全部删除。**⑤工作文档**：`docs/design/h-family-reprice-20260925.md` 已归档（内容并入本条目 ＋ `docs/glossary.md` 周末入侵词条，原文件删除；**验机清单**见该批汇报）。⛔ 遗留：① **A/C/G 三族仍是占位口径**（派生卡保留原卡 id ⇒ 标签 78/120 与真实属性不符；击沉旗舰会发 H 的黑匣 `blackbox-h`）· ② **M3 两个新族未开工** · ③ 死门面两处登记不擅自删（`weekendBounty.weekendEncounterRollOf` 与 `weekendTick().encounterRolls` 只有用例/读数在跑，生产路径走 `rollLowSecAmbush`） · ④ **「仅调试可见」保持不变**（船长 2026-09-25 复述「**依旧仅调试可见**」；上线闸门与公告都不在本批范围） · ⑤ **旗舰战入口与战前准备界面**（船长同日令：核心收复后在入侵窗口内显示「发现敌方旗舰」⇒ 点击进入与虫洞同款的选船准备界面）**另批进行中**。

## 封存卷索引（更早批次已移入 `docs/archive/`，点开即读）

| 卷 | 收录日期 | 条目 | 体量 |
|---|---|---|---|
| `docs/archive/roadmap-2026-09-04.md` | 2026-09-04 | 28 条 | 13 KB |
| `docs/archive/roadmap-2026-09-05.md` | 2026-09-05 | 12 条 | 12 KB |
| `docs/archive/roadmap-2026-09-06.md` | 2026-09-06 | 1 条 | 2 KB |
| `docs/archive/roadmap-2026-09-08.md` | 2026-09-08 | 18 条 | 15 KB |
| `docs/archive/roadmap-2026-09-09.md` | 2026-09-09 | 45 条 | 46 KB |
| `docs/archive/roadmap-2026-09-10.md` | 2026-09-10 | 71 条 | 169 KB |
| `docs/archive/roadmap-2026-09-11.md` | 2026-09-11 | 81 条 | 237 KB |
| `docs/archive/roadmap-2026-09-12.md` | 2026-09-12 | 76 条 | 173 KB |
| `docs/archive/roadmap-2026-09-13.md` | 2026-09-13 | 95 条 | 265 KB |
| `docs/archive/roadmap-2026-09-14.md` | 2026-09-14 | 124 条 | 397 KB |
| `docs/archive/roadmap-2026-09-15.md` | 2026-09-15 | 38 条 | 132 KB |
| `docs/archive/roadmap-2026-09-16.md` | 2026-09-16 | 40 条 | 107 KB |
| `docs/archive/roadmap-2026-09-17.md` | 2026-09-17 | 16 条 | 36 KB |
| `docs/archive/roadmap-2026-09-18.md` | 2026-09-18 | 4 条 | 11 KB |
| `docs/archive/roadmap-2026-09-19.md` | 2026-09-19 | 1 条 | 2 KB |
| `docs/archive/roadmap-2026-09-20.md` | 2026-09-20 | 32 条 | 73 KB |
| `docs/archive/roadmap-2026-09-21.md` | 2026-09-21 | 5 条 | 19 KB |
| `docs/archive/roadmap-2026-09-22.md` | 2026-09-22 | 20 条 | 48 KB |
| `docs/archive/roadmap-2026-09-23.md` | 2026-09-23 | 1 条 | 1 KB |
| `docs/archive/roadmap-2026-09-24.md` | 2026-09-24 | 7 条 | 20 KB |

> 上面每卷都是**原文冻结件**：不重写、不摘要、不追加。`docs/archive/README.md` 另有"已办结待办单"卷。
