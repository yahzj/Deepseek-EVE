# 后续工作流备忘（Roadmap / Backlog）

> **2026-09-15 改版**：本文件只留三块 —— **① 待办活面**（置顶单 / 待办段 / 交接开放项）· **② 最近 20 条批次条目（滚动窗口）** · **③ 封存卷索引**。
> 更早的批次条目已**原文封存**进 `docs/archive/roadmap-YYYY-MM-DD.md`（一字未改，含船长原话照抄）——查老决策先看索引表、再 grep 对应卷。
> 封存动作 = `npm run docs:seal`（滚动窗口之外自动进卷，幂等；`--dry-run` 先看计划）· 封存区规则见 `docs/archive/README.md` · 全仓文档总索引 = `docs/INDEX.md`。

---

## 置顶：船长测试反馈改动单（2026-09-04 加入，按列序逐条推进）
**执行规则**：批次 A = 无歧义可直接实现；批次 B = 需先给设计总结等确认（大系统/结构级/规则数值）。
- **T1~T11 全部 [✓] 办结**（2026-09-15 逐条核对后原文移出）⇒ 全文见 `docs/archive/roadmap-todo-done-20260915.md`；本节留段头，新的船长反馈单另起一段。

## A. 体验走查 + 打磨（建议优先，收口"内容感"）
- [ ] A1 船长通读悬赏文案与界面观感（2026-09-05 注：需船长走查后按条关闭；2026-09-04 首轮已走查并清理过一批）
- [ ] A2 信息密度检查：星图（安全色 + ⚔悬赏徽标 + ′标称 + 名称是否拥挤）、悬赏列表排序/卡片密度、矿带卡布局细节
- [ ] A3 定版存档策略（2026-09-05 注：需船长确认正式档基线后关闭）

## B. 玩法闭环延伸（需确认流程的大方向）
- **本段 4 项全部 [✓] 办结**（2026-09-15 核对后原文移出）⇒ 见 `docs/archive/roadmap-todo-done-20260915.md`。

## C. 战斗与舰船系统余项
- [ ] **C3 V18 立项：EVE 式高/中/低槽重组**（2026-09 船长拍板"V17 收尾、高低中槽立项 V18"；
      **2026-09-04 交接：实施由大鲸鱼二号负责**，一号不再触碰；**2026-09-04 设计总结已确认**
      （docs/design/v18-slots.md：复数安装/数量制/槽位映射/Q4 布局草案 19 船表 + 高槽无人机
      家族 6 件——甲板扩展/战术导控，装备 48→54）——**V18A 实施已完成（2026-09-04，core 301 tests）**；
      **C4 校准轮（多炮/多矿枪/无人机件复核）与本里程碑联动，另行收尾**）：
      船体槽位布局按 role 族差异化（武装多高槽/装甲多低槽等）+ 模块归属高/中/低 +
      槽位数量/布局制（**炮尺寸制已随 V18 口径取消移除，无尺寸位**——变更记录见下）；
      fitted 结构迁移一次做干净（存档 17→18；旧六槽原位映射 high=[炮,矿] mid=[盾,推] low=[甲,货]）；
      V17 参数（乘入抗性/CPU 档位/推进代价）与槽制正交、届时零返工
      （见 docs/design/v17-modules.md §预留；v10b-combat-data.md 原"留战斗阶段"的旧账在本里程碑清偿；
      2026-09 设计问答已收：Q1 复数安装 / Q2-final 数量制不加尺寸位 / Q3 槽位映射 / Q4 布局草案）
- [ ] **C6 无人机 vs 炮位 DPS 比例评估**（2026-09-04 船长确认 V18 时追加：**延后**——武器体系
      全面后（V18B 炮族差异化 + 导弹架之后）再评估无人机流与炮击流的高槽伤害比例；
      无人机件数值（+15/35/70 m³、+12/25/40%）为暂定初值，届时可调）
      **2026-09-10 评估完成（二号；`docs/design/c6-drone-vs-gun-20260910.md` + 正式工具
      `tools/drone-vs-gun.ts`，`npm run battle:drone-vs-gun`）**：王鲭级满配同船对比——
      名义 DPS 无人机流 = 炮流 **81%**（每高槽名义反而 121%），但有效 DPS（命中修正）**48%**、
      每高槽有效 **73%**；成因 = ①命中链缺口（无人机固定命中 0.6，火控/索敌对无人机无效；
      炮台 ≈0.98）②支援链缺口（稳定器/射速/索敌只作用炮台，炮流有效 +33%）③CPU 双计
      （装置 + 每架，D3 占 310/320）④高槽不产生本体火力。实战：威胁 96 穹顶守卫
      纯炮 100%/76s/58% vs 无人机 D3 0%/74s（被击毁）；加目标锁定 MK3 后 D3 升到 60%。
      情景推演：单靠"导控 +60% + 无人机 CPU×0.65"有效仅到 68%、硬卡 40%——
      **须动命中链/支援链才能拉平**。
      **2026-09-10 第一轮调整已实施（船长拍板）**：无人机命中/衰减下放机型本体
      （`ItemDef.hitRate/falloff`）——**蜂鸟/赤鸢/猎鹰：命中 75% 且不随距离衰减**（falloff 1）；
      **雷鸥哨戒：命中 110%（用意 = 抵掉部分衰减）+ 保留正常衰减**（falloff 0.35）；
      combat 读本体（旧档兜底 0.6/0.35）、命中公式零改动；UI 无人机详情/手册新增「放飞命中」行；
      新增 `tests/drone-hit.test.ts`（6 项）；glossary 增词条「无人机命中与衰减」。
      **改后实测（王鲭级）**：有效 DPS@2km 48% → **70%**（D3 184 vs 炮流 263）、
      每高槽有效 −27% → **+5%**；实战 D3 反超炮流（穹顶 **100%/65s** vs 76s、坟场 27s vs 45s、
      噬口 41s vs 50s），但无人流行残血偏低（散火击杀节奏慢；装目标锁定 MK3 后 35%→57%）。
      **待船长判断是否回调**（旋钮：导控 +40%→+25% / 三型命中 0.75→0.70 / 无人机每架 CPU 上调）。
      **2026-09-10 船长裁定：先保持现状**（回调未实施，数值维持本行口径），由船长实机实测后再定。
      **2026-09-10 船长追加：无人机专用舰高槽 −2**（梭鱼级 5→**3**、王鲭级 6→**4**；其余带机巢
      舰船不动）——`ships.ts` 两行 + 旧档由 repair 链尾件退库（不丢装备、清单保留）+ 新增
      `tests/drone-ship-slots.test.ts` 钉布局与旧档兼容；校准行 D1/D2 改三槽、新增 D2b
      （槽位吃紧后单件导控升 MK3）、D3 四槽恰好占满；工具与测试存档同步。
      **复跑结论（重要）**：削槽对**相对关系是净利好无人机流**——王鲭炮流 6 门→4 门
      名义 272→**183**（−33%），而无人机流 D3 本体只占 4 槽、名义 220 **不变**；于是
      无人机流/炮流名义比 81%→**120%**、有效 DPS@2km 比 70%→**105%**；
      实战王鲭 4 槽对比：无人机 D3 穹顶 **65s** vs 纯炮 104s、坟场 27s vs 68s、噬口 41s vs 76s。
      梭鱼 3 槽：无人机 D2b 有效 107 vs 纯炮 3×MK3 120，实战仍更快（穹顶 85s vs 157s，但 0% 胜）。
      原因 = 无人机流火力来自机群（受舱容/CPU 约束，不吃槽位），炮流火力随槽位线性下降。
      **若目标是削弱无人机流，杠杆是 CPU / 舱容 / 战术导控，而非高槽数量**（详见设计稿 §九）。
- [x] **无人机四型定位 + 「无人机可被击落」**（2026-09-10 船长拍板，**本批已完成**）：
      **① 四型定位契约已确立（已实施）**——侦察机闪避最高装甲最薄 / 战斗机全属性平均 /
      攻坚机血量最厚·单发高·闪避最低 / 哨戒机射程最远·单发最高（狙击）·血量与侦察机相仿；
      现有四型按此调整（**蜂鸟闪避不变 0.45**；赤鸢 0.40→**0.25**、血 34→40；猎鹰 0.32→**0.10**、
      血 66→**97**、抗性装甲向；雷鸥 0.25→**0.18**、血 105→**23**、抗性削到单条）。
      契约**可执行化**：新增 `data/droneRoles.ts`（`DRONE_ROLE_SPECS` 四档区间 + 逐机/跨类阶梯校验），
      `content:check` 与 `tests/drone-roles.test.ts`（7 项）双重守卫——**新无人机越档即被拦下**；
      glossary 增词条「无人机四型定位」；设计稿 `docs/design/drone-roles-20260910.md`。
      注：`DroneDefense` 目前**不进战斗**（只有详情/图鉴/手册显示），故本批**不改胜率、校准矩阵不变**。
      **②「无人机可被击落」已实施（船长选乙：永久损失/耗材流）**：敌方**近防炮**——**威胁 ≥ 60**
      的敌舰才装，每舰**每 0.5 秒判定一次**，随机挑一架**正在攻击的放飞无人机**（**哨戒机不被打**），
      按 `pdAcc − 机型闪避` 掷命中、命中按 `pdDmg` 走机型三层抗性，打空即击落并停火；
      **不看距离**（"毕竟你要飞过去"）、**不参与**敌舰对玩家的常规攻击、单场最多击落 50%；
      **战后从无人机舱清单永久扣除**（主控远征胜/败/撤退 + 遭遇战 + AI 副船全接线），
      事件日志 + 在线 toast 提示，重复清剿在本场战损过半时自动停环提示补货；
      手册/物品说明/装配页删除"不消耗、不被击落"并改为近防炮口径；测试 `tests/drone-loss.test.ts`（5 项）；
      新增调参工具 `npm run battle:pd-tune`（支持 `--acc/--dmg/--cap/--period` 传参试档）、
      `drone-vs-gun` 表 3 增战损列。
      **实测（满技能 5 种子，王鲭 D3 满编；合法且符合流派的配装）**：软卡交火短（27~41s）战损近 0；
      硬卡穹顶（96）约 **3.2~3.6 架且全是攻坚机**（哨戒机免疫）；硬卡仍 **100% 胜**——
      因编队火力核心（6 架雷鸥）不受近防炮威胁，压力在"打得更久 + 要补货"。
      战损估值占该卡奖励约 1.1~1.2%。
      **数值已确认（2026-09-10 船长）**：「如果没有特意说明，默认按 0.5/5 来算」——
      未特意说明的敌舰一律 **0.5 秒/舰判定 · 命中 0.5 · 每发伤害 5**（逐舰独立、上限 50% 保留）。
      **默认档实测（王鲭 D3 满编）**：噬口(80)/坟场(88) **0 战损**；穹顶(96) **2.2 架（全为攻坚机）**、
      战损估值 1.1 万 ISK ≈ 该卡奖励 **0.7%**；三卡仍 100% 胜——因哨戒机免疫且编队火力核心是雷鸥，
      压力落在"打得更久 + 攻坚机要补货"。调参参照档（0.45/7 → 3.6 架；0.30/12 → 3.2 架；
      0.55/3 → 0 架）见设计稿 §七。
      **2026-09-10 船长追加**：**非哨戒无人机全部被摧毁后，近防炮转而攻击哨戒无人机**——
      哨戒机的"豁免"变成有条件的（靠同队其它机型当肉盾）；已实施 + 测试覆盖
      （`tests/drone-loss.test.ts` 新增"非哨戒机全灭后转火"用例），重档实测可见效果
      （acc 0.5 / dmg 10：噬口与穹顶在攻坚机全灭后哨戒机开始被击落）。
      **2026-09-10 船长追加（无人机舰专属加成）**：无人机舰的 `powerBonus` 只喂炮台、对机群无效
      （王鲭 0.6 与长尾鲨同级却吃不到机群）→ **削火力 + 加专属**：新增船体字段
      `ShipDef.droneDmgBonus`（只乘入放飞无人机单发，与战术导控/作战学乘算）；**王鲭 powerBonus
      0.6→0.3 且 +12%**、**梭鱼 0.45→0.25 且 +8%**。实测：王鲭 D3 名义 220→**245**、
      同船炮流 183→**149**（−19%，实战 104s→120s）；梭鱼 D2b 132→142、炮流 126→109；
      近防炮战损 2.2→**1.4 架**（机群更强→交火更短）。测试 `tests/drone-ship-bonus.test.ts`。
      **2026-09-10 船长追加（机舱联动）**：**卸下「无人机甲板扩展」后，超出机舱的无人机也要卸下**——
      新增 `trimDroneLoadToBay()`（按清单逆序卸下、退回仓库、写日志 + 一次性提示），
      接在 `unfitAt`（UI 传 ctx）与 `repairDeprecatedModules`（旧档槽位对齐裁掉甲板件）之后；
      测试 `tests/drone-bay-trim.test.ts`（5 项）；手册「装配」条目补口径。
      **2026-09-10 船长追加（损坏与回收 + 无人机线技能）**：①**取消单场 50% 击落上限**——
      战斗内机群可 **100% 损坏**；②**战后回收**：损坏机体按 `10% + 8%/级（回收学）`、满级 **50%**
      **四舍五入**找回并**回无人机舱清单**，未回收部分才永久损失（日志/toast 写明 损坏/回收/净损）；
      ③新增**五条无人机线技能**（战斗组）：无人机打击学 rank4 单发 +4%/级、无人机耐久学 rank2
      三层血 +4%/级（满级 **+20%**）、**无人机强化学 rank4 三层血再 +6%/级（满级再 +30%，与耐久学乘算，双满 ×1.56）**、
      无人机回收学 rank3 回收率 +8%/级、无人机规避学 rank4 闪避 +2%/级（满级 +10%）；
      数值常量集中在 `combat.ts` 的 `DRONE_SKILL`。测试 `tests/drone-skills.test.ts`（6 项）+
      `drone-loss.test.ts` 改口径；词典增词条「无人机线技能」。**实测**：默认近防炮档不变
      （软卡 0、穹顶 1.4 架）；重档（0.5/10、0.5/20）可见**整队 10/10 损坏**且胜率掉到 0~40%——
      取消上限后的真实风险与"耐久/强化/回收"技能的价值都在这里体现。
      **2026-09-10 船长二次修订**：耐久学**拆成两条**（rank2 基础档 + rank4 强化学进阶档，
      **乘算**叠加）；**其中一条满级改为 20%**——现行 = **rank2 耐久学 +20%（+4%/级）/
      rank4 强化学 +30%（+6%/级），双满 ×1.56**（越难练收益越高）；**规避学满级由 +20% 降为 +10%**（+2%/级）。
      **2026-09-10 玩家反馈修复（悬赏奖金口径）**：反馈"卡片赏金与实际到账不一样"——根因
      ① 结算带 ±15% 浮动（`rewardJitter`）而卡片展示名义值；② **赏金任务（窝点）卡漏乘
      "赏金猎手学"系数**。船长按甲案拍板：**取消奖金浮动**（rewardJitter 0.15→0）——展示=到账，
      唯一随机奖励 = 情报彩蛋 +10%（触发即写日志）；并修掉赏金任务卡展示漏项。
      测试 `tests/bounty-reward-display.test.ts`（4 项）；词典增词条「悬赏奖金口径（展示=到账）」。
      **口径修正（自检）**：早期工具给王鲭配了 3 中+3 低支援件（超出 2/2 布局）导致结论失真，
      已修正并统一为合法配装。
      表现层（击落演出/战报行）待三号对接，引擎侧字段契约见设计稿 §八。
- [ ] **C5 炮族差异化 + 导弹架**（2026-09 船长审核批注：同 MK 三系炮"只差弹药、无性格差异"——暂定稿可玩，**与导弹版统一做**；
      **2026-09-04 船长重定向：按伤害类型设计真武器形态（质量炮=动能 / 导弹=爆炸 / 激光=能量），
      旧三系同参炮 = 临时填充数据，逐批替换——V18B-1 导弹架 ✅ 已完成**（docs/design/v18b-missiles.md：
      追踪命中不随距离衰减的远程爆破 + 近盲安全射距；原高爆炮 1:1 迁移；爆破导弹弹药归位）；
      **V18B-2 激光炮 ✅ 已完成**（docs/design/v18b2-lasers.md：必中光束 + 距离衰减作用于威力
      （幅度减半 50%）+ 消耗能量弹药；能量炮/异星原型 1:1 迁移激光；per-gun 多键预载完成
      ——混装异型不再饿死；core 345 tests）：
      **V18B-3 ✅（2026-09-05 船长确认完成）** = 动能炮 → 质量炮形态（届时确认是否改名），并联动 V18 槽制
      （炮尺寸制已随口径取消移除，见变更记录）；
      v17b-weapons.md §已知限制已记录
- [ ] **C7 每族一艘新敌舰 ＋ 层 6「深渊档」深层卡（船长 2026-09-17「**先挂起吧**」· 一号 · **建议稿已随本条归档**）**——起因 = 船长「**打算给每个种族添加一个新敌舰，并添加一张新的深层卡（出现层数6），你有什么推荐（不用特意补齐全舰种，按照族设定推荐）**」。
      **①建议（六族各一艘 · 全部零引擎改动、只复用既有字段）**：A **劫掠跳帮舰 T2**（捕获网 ＋ 冲锋双持，把"钉住"从血 182 的电子舰搬到真突击位）· B **拾荒拆解舰 T2**（`repairPct 0.3`＋低速厚甲，**需加进 `repairPct` 白名单**）· C **螯颚撕咬者 T2**（射程 1~1,800m ＋ `blindDmgMul: 1` ＋ 2.0× 速度 ＋ 冲锋，反"距离门"）· D **静滞方尖舰 T4**（首选；`web` 字段参数改写＝周期性压制最近我方：机动 ×＋射程 −2,000）或 **陵寝主宰舰 T5**（撞"旗舰留给 E 族核心舱段"那句既有口径 ⇒ **首选 T4**，要开旗舰位另说）· E **无人机母舱段 T4**（纯机群 ＋ 警戒机 ×10 ＋ 后备机库 ＋ 机群射程 ×4 ＋ **速度 0**，必须带防空贴上去拆）· G **孵巢舰 T3**（蜂群 ×6 ＋ 鱼雷、无后备，守 G 族口径）；**F 族（遭遇巡逻）跳过**——数据里 0 舰级、只是"遭遇战模板族"、没有种族设定。
      **②层 6 三读法（推荐甲）**：**甲** 新增**「深渊档」（层 6 起）每族一张（共 5 张）**——与现有结构对称，上面 5 艘新舰正好做各自深渊卡主体（否则新舰无出场处；代价 = core 层档池加一条规则 ＋ 契约/用例）· **乙** 只加一张**层末守卫专用卡**（推荐 G 族「深渊封锁线」，代价最小）· **丙** 跨族动态编成（卡面是静态引用，要新机制 ⇒ 不推荐）。**层 6 的收益必须一起定**（否则"更深"只有难度）：推荐 **(b) 新增"深渊货柜"**（最高档一次性图纸 ＋ 原型件）＋ **(c) 层 6 起上调深档卡/货柜权重** 的轻量版；纯难度 (a) 不推荐。
      **③待船长裁定 5 件**：六舰收不收（可逐条改）· D 要不要开 T5 旗舰位 · 层 6 走甲还是乙 · 层 6 收益选 (a)/(b)/(c) · F 族跳过还是先给一句设定（它是谁 / 什么美学 / 什么威胁级）。
      **④节奏（定了之后）**：① 六艘新舰（纯数据 ＋ 契约/用例同步，`npm run foe:export` 的敌舰表自动跟随）→ ② 深渊档 ＋ 层 6 卡（含"深渊货柜"若要做）→ ③ 数值标定（跑 `npm run wormhole:econ` / `bounty:stats`，把逐层威胁 / 总血 / 总 DPS 读数摆给船长核）。
      **⑤全文**：原稿按 §8 归档删除，需要原文看 `git show f88ac64a:docs/design/foe-new-ships-deep-cards-20260917.md`（含六族现役 **25 舰级**的档位覆盖与**契约硬上限**表：A 只 1~3 · B 不配 T3 及以上 · C ≤4 且 T4 须登记进 `ALIEN_BEAST_SHIP_IDS` · G ≤4 · E 持 T5；以及"层档池 / 半径封顶 R=4 / 深档图纸货柜 = 层 5+ / 威胁 ×1.10"这些层档现状）。

## D. 经济/数值精调
- [ ] D1 balance（2026-09-05 甲案已做：体检工具扩"总资产口径"——钱包+舰队/已装模块+仓库/书架按市场常驻 base 折算，重跑 24h 三策略：钱包/总资产 A 934k/2067k、B 410k/1589k、C 312k/1499k；B/C 钱包低估约 1.7~2.2×，总资产差收窄至 A 的 73~77%，缺口主要来自技能训练时间与蓝图书沉没而非倒挂。是否需数值拉平待船长定目标）
  **2026-09-08 大改批后重跑（平衡-check，24h 30s 步长，同口径）**：钱包/总资产 A 原矿流 1,127k/**2,278k**（鲸吞级）、B 精炼流 577k/**801k**（掘洞级）、C 装备流 607k/**844k**（掘洞级；造 MK1/MK2）；B/C 总资产仅 A 的 **35~37%**（旧 73~77% 反转收窄）；双修贸易变体 24h 内技能未练成、与基础线同值。**状态（2026-09-08 船长定）：后续定位/拉平挂起——船长将调整技能体系（技能直接影响收益），待技能改完重跑再定目标；本行 09-08 数字保留为改前基线**。
  **2026-09-09 技能/工业大改后复跑（O3；工具修正后）**：总资产 A 原矿流 **2,293k**、B 精炼流 **2,338k**、C 装备流 **2,663k**（三线均鲸吞级；钱包 1,128/1,150/1,433k）——B/C 从 A 的 35~37% → **102%/116%**（技能投入有回报、装备流最优，A 原矿流与基线持平 +0.7% 未受损）；双修贸易变体仍 24h 内练不成、与基础线同值。**工具修正（balance-check.ts）**：船分级后沙猫/掘洞同 T1，原"目标 tier > 当前 tier"升级门槛卡死 12 万换掘洞（A 流伪影性卡在 89 万买不到鲸吞、总资产仅 91 万）——改按升级梯子内位置比较后恢复真实路径（三线均按 沙猫→掘洞→鲸吞 正常滚雪球）；里程碑/每小时曲线干净。**待船长定目标带**（B/C 已反超 A 是否到位，或需微调）。

## E. 基础设施
- [ ] E2 云存档 / 在线化：架构预留（决策 1/2/5），未启动；Phase 2/3 见 architecture.md

## 2026-09-08 一号交接开放项（来自临时交接卡 docs/design/handoff-2026-09-08-current.md，已按约定迁移后删除）
- [ ] **O1 组装机卡「手动 vs AI」两档净/h**：现为单一手动口径（二号 A 项）；手动/AI 基础核心档分列估算待船长拍板。
- [ ] **O2 奇货卖出侧溢价巡游**：挂价高于收购价线的奇货卖单仍可能被巡游采购按挂价成交（买入侧已 20L 收口，卖出侧未动）；处理前先问口径。
- [ ] **O3 balance-check 24h 策略复跑**：工业扩容（工位数大增）后未复跑；范围先问船长。
- [ ] **O4 顶部 AI 徽标计数与「执行中 N」偏差**：早期玩家反馈，根因未实证。
- [ ] **O6 二号船分级批次验收（已在 main）**：舰船尺寸分级 + 掠食者巡洋线 A1–A9；公告待审稿在 6b79a61（id 2026-09-09-ship-size-tier）未经船长批准不得写入 announcements.ts；巡洋 T3 数值/渠道声望待船长终审（涉二号分工，一号只读 ship-size-tier-rework.md）。
- 说明：O5 数值文档分层 = 现数值以 docs/design/manufacture-refine-worker-econ.md 与 docs/glossary.md 为准（refine-balance-50.md 为历史锚）；O7 真档验收状态 = 2026-09-08 引擎级实测探针 20/20 通过（临时探针已删），UI 目测项待船长开游戏确认（市场蓝图 chip/奇货低挂提示/AI 指挥中心 +N 工业/交付航线按钮与弹窗/工业页数值行/公告三条）。

## 2026-09-09 一号收尾开放项（收尾盘点后登记，待办照旧先读本段）
- 备忘（2026-09-09 自交接卡迁移）：**精炼净收益差异问题 = 船长延后**——差异来源 = 吞吐桶 ×8.7 × 矿价 ×37.7 ≈ 卖原矿差 328×，精炼净再 ×净率差；重开先回看 refine-cycles.md 头注与 content-csv 工作簿 items sheet。
- [ ] **P0-2「开战距离 / 先手」提案 + 预演（船长 2026-09-11 选定本批 =「先出提案 + 预演，审核后再落码」；二号）**——**本批零引擎、零数值改动，产出 = 提案稿 `docs/design/p0-2-engagement-20260911.md`（含全部实测读数）**。**①三条决定性事实**：(a)「够不着」= **距离拔河的胜负**——同一张蜃影、同一把 4×动能MK2，**带推进器 A0 = 100%/65s 闭合到 3.3km**，**仅去掉推进器 = 0%/140s、全程停在 9.99km、我开火 0.0**；(b) **教学卡「敌人一炮未放」与开战距离无关**——开局从 6314m 缩到敌人带内（1980/1500m）敌人**仍只放 0.2 炮、战斗缩到 1s**，敌人血抬到 **×12（264）** 也**依然 0 炮**（根因 = 交战距离停在玩家期望位 3.2km 而敌人带只有 2.2km ＋ 玩家先手一轮清场）；(c) **全局改法「开战距离 = 双方射程重叠区上沿」不成立**——全卡清点 **78 格中 77 格**被改动（含旧路径 20 张）⇒ 打穿「旧路径逐字不变」判据。**②病灶 A 实测（kite 卡，5 播种 1/7/13/29/51）**：蜃影 T48 的 A1/⑤/③ 三行现状 **0%｜140s｜我 0.0｜敌 136**；候选甲（可及性钳制：敌期望交距 ≤ 我射程×0.95）⇒ **20%/100%/80%**、我开火 28~40 次，但**连带把 A0 行从残 32% 拉到 58%**（= 船长刚裁定「保留」的那一行）；灰霾 T28 三行 **0%/600s ⚠触顶 5/5｜敌 584** ⇒ 甲后 **100%/36~59s**；赤潮 T34 三行 **0%/373s｜敌 364** ⇒ 甲后 **100%/41~64s**；**旧路径对照卡「幽灵舰信号」四行全 100%**（从未够不着 ⇒ 差异只来自舰级路径按"自身带×0.85"算站位）。**③候选四选一（甲乙丙丁）**：甲 全局可及性钳制 / **乙 逐卡覆写（复用既有 `rangeMinM/rangeMaxM` 旋钮，不新增字段；建议先做灰霾+赤潮、蜃影单独议）** / 丙 只做可读性（提前脱战 + 战报写明"够不着" + 卡面预告交距）/ 丁 不动；戊（全局改开战距离）已实测淘汰。**④顺带发现（建议先修）**：`battle-calibrate` 的**敌开火(头目/杂鱼)**列用 `b.fx.slice(seen)` 计数，而 `fx` 是 **48 条环**（`pushBattleFx` 超长丢最旧）⇒ **环满即停、长战全漏**：同格真值 **584.0**（头 145/杂 438）↔ 工具法 **48.0**（漏 92%）、蜃影 A1 136 ↔ 48、蜃影 A0 54.0 ↔ 43.2（= 蜃影稿 §2.1 记的 10.8/32.4 正是此口径）；**我开火列不受影响**（工具用 `stats.meShots` 权威累计）；修法 = 按 `BattleFx.seq` 续读（一行级）。**⑤四问待裁**：病灶 A 走哪条路 / 若走乙点名哪几张卡 / 要不要一并修工具漏计 / 教学卡「一炮未放」是否单列。**⑥验证**：本批**不改代码**，故只跑读数工具（`npx tsx _probe-p02*.ts`，两个探针**用完即删**，`_` 0 个）；提案落码时再走四连 + 重跑 A0~A3 与六组。**改动全在 d2 工作树，未合入 main、未推送。****【提案 · 未落码 · 待船长裁决】**
- [ ] **★★ 挂账①「低威胁卡敌人一炮未放」+ ②「名义火力 / 实际火力脱节」（2026-09-11 船长要求处理；二号）**——**①实测形态**：边境/碎晶（威胁 12/20）在 A0/A3 参考行**敌人全程 0 开火**（我开火 11~12 次、18~25 秒清场）——根因 = 敌军自带带太短（快艇 1~1883、护卫舰 326~4275），而玩家参考行站 3.2 km；**候选**：甲 逐卡给敌军**加宽射程带**（让其进入自己的交战带）· 乙 逐卡**开战距离覆写**（把开局放进敌军带内，实测 1500~1980m 时敌人会开火 1.0~4.2 次）· 丙 给低威胁卡**先手**（开场即开火）。**②实测形态**：纸面单发与实际输出脱节（例：赤潮头目 beam 必中单发 21 ↔ 灰霾头目 fixed 单点 59，两者实收相近）；**候选**：甲 单发按「名义 DPS × 装填 × 实收命中」**反推**（纸面 = 实收，须重标全部 26 卡）· 乙 只做**登记口径**（在词典写明"纸面 ≠ 实收"，不重标）。**两份提案待船长选型后再落码。****【提案 · 未落码】**

## 2026-09-16 二号交接开放项（虫洞战利品扩充批的尾巴 · 已推送 · 公告待一号发布）

- [ ] **W1 虫洞战利品扩充 · 公告待发（船长 2026-09-15：「公告我让一号发」⇒ 经办 = 一号）**：
  待审稿 = `docs/design/announcement-draft-20260915-wormhole-loot.md`（4 条要点已成稿 · 第三人称 · 无开发话语 ·
  口径来源照抄船长七段原话）；批准后写入 `packages/data/src/announcements.ts`
  （`ann-wormhole-loot-20260915` ·「虫洞战利品扩充」· 2026-09-15 · 内容更新）并推送。
  **要落进文案的数值（口径权威 = 词典八之二/八之三）**：谜质 **1 台 = 1 枚** · 70,000/枚 · 只收不卖 · **奇货档**；
  **奢侈品三档 10 万 / 40 万 / 200 万**（可买可卖）；贵重品货柜 2 格 ⇒ 奢侈品 **5~30 件** · 箱价 **365 万**（= 期望 1,458 万 ×**0.25**）；
  军用备货柜 4 格 ⇒ MK3 **1~3 件**（含武器、不含专属）· 箱价 280 万（= 期望 ×0.6）；
  **遗迹出货固定 70%**（层 2 起）· 池 10 种 ⇒ **贵重品柜 50%**；残骸堆 **0.75%/堆**（单次上限 1）；安全货柜 3000 m³ / 6 格 / 价 ×1.5。
- [ ] **W2 验收用测试存档未配（船长点单再做）**：`wh-loot` 一档——仓库预置 2× 贵重品柜 + 2× 军用柜 + 1× 安全柜（可立刻拆解），
  洞内一趟带 2 台谜质装置并站在残骸堆旁（一撤离即可看谜质折算与结算单新格）。配在 `tools/make-test-save.ts` + `docs/test-saves/`。

## 最近批次（滚动窗口 · 最新 20 条）

> 越窗即封存（`npm run docs:seal`）；窗口大小 = 20 条，需要临时调大用 `--window=N`。

- 2026-09-17：**带括号文案逐类审核（三批）＋ 按新硬规矩重写 17 条（船长逐类裁决 · 三号 · verify）**——**①船长原话（照抄）**：
「**将目前所有带括号的列出，我们一起按类别挨个审核**」＋同日立的硬规矩「**不要在任何说明文案内写入原因解释（特别是使用括号进行解释的这种）。
文案要保证在30个字以内。**」。**②清单**：**223 段括号 / 127 类**（装备 142 · 物品 86 · 技能 79 · 舰船 43 · 蓝图 135 的 description）；
工作文档 `parens-review-20260917.md` 已按 §8 归档删除（细节以 git 历史兜底）。**③裁「删」29 段**：槽位标签 `（低槽）`17 · `（中槽）`6 · 
`（高槽）`3 · `（高槽，无伤害）`3；**裁「留」**：`（上限 90%）`18（富卡抗性行本来就印 · `shipInfo.tsx:99`）· `（不可制造）`1
（§十三 第 1 条明写"能不能造是有用信息"，我在交船长前自查时**自纠**了原判）· 谜质储存器尾注 24（随存量清档保持现状）；
**延后项**：目标锁定阵列——已由二号按「全队生效」改判改写（`全队集火编队首位，全队伤害 +8%`）。**④口径统一（船长裁「照做」）**：
① 稳定器三族改按「**系**」表述（依据 `damageTypeBonusPct` 按系加算 · `combat.ts:1108→1229`；**基础舰炮与无人机不吃** `:1226`）
② 克制写法统一到唯一源 `typeLayerMult`（`combat.ts:294`）、只列非 ×1 的层，**修 3 处真值错**（「打甲减半」「打盾减半」真值 ×0.75；
「装甲镀层·高爆」写「双倍」真值 ×1.5）＋ 蓝图弹药图纸补齐另一层（`content:check`「弹药克制声明」自动对账）③ 系名统一到 
`DAMAGE_TYPE_LABELS`（动能 / 高爆 / 能量）④ 叠加口径固定三词（`多装递减` / `多件不叠加` / `同类叠加，多来源收益递减`）。
**⑤新硬规矩落地 17 条**（护栏「改过文本即报红」）：装备 12 · 物品 1 · 蓝图 4 ⇒ 全部压到 **12–28 字**、括号只剩规格；
删原因解释（「贴太近发射会炸到自己」）· 删跨件对比（「破盾后的重启能力比 MK1 强得多」）· 删界面已显示读数（CPU 数 / 占格 / 船体建议）；
「与任何类型推进器一起使用时失效」受 2026-09-16 **指定措辞契约**约束 ⇒ 保留；侦察舰例外移交装配页「本船特性」（`FitPage.tsx:581`）。
**⑥收口裁定**：**存量 248 条超线说明「清档，不执行」** · 装配卡片「装置限制」行去掉「（船一动就藏不住）」（§十三 反例，全仓残留 0）· 
鹦鹉螺「机巢与分辨率同级最高」删半句 · 装甲镀层 MK1·动能型「动能破甲弹」→「动能弹药的克制镀层」（中途撞旧称契约后修正）· 
`modules.ts:16/:148` 旧注释「能量打盾 ×0.75」订正 ×1.25。**⑦闸门**：typecheck 四包 0 错 · core **164 文件 / 1,775 例** 全绿 · 
`content:check` ✅（长度契约报红 0 · 括号原因 0 处 · 存量待改写 247）· `ui:rot-check` ✅ · `docs:index` ✅。**【已合入 main · 未推送】**
- 2026-09-17：**技能「满级才生效」清零：星际奇遇学的虫洞扫描窗口改「每级 −4%」（船长三答确认 · 一号 · main）**——**①船长原话（照抄）**：「**将一些只有满级后才有效果的技能，拆分成每个等级效果（比如满级获得20%，那么就每个等级拆分成4%）**」＋三答（**全取推荐**）：**范围 = 只改星际奇遇学** · **数学 = 线性相加 `1 − 0.04 × 等级`**（满级恰 −20%）· **防阶跃体检 = 不加**。**②§5.2 冲突与改判（旧裁定作废）**：旧口径 = 船长 2026-09-14「**星际奇遇学，满级后缩减虫洞扫描周期20%**」＋回答"线性每级 −4% vs 满级阶跃"之问的「**星际奇遇学一起改。**」⇒ 当日按**阶跃**落码（`lv >= 5 ? 1 - 0.2 : 1`）并如实登记取舍「**阶跃 ⇒ Lv1~4 练奇遇学对虫洞毫无收益（只有满级那一下）**」（出处 `docs/archive/roadmap-2026-09-14.md` · 设计稿 `wormhole-discovery-scan-20260914.md` §三·补）；本日按新指示改回线性，**满级总量一字不动（仍 −20%）**，旧条款与那句取舍**作废**（设计稿 §三·补 与词典「怎么发现」条已就地标改判）。**③全仓盘点（为什么只剩这一条 · 判据可复用）**：判据①按 `；/。` 切子句找"含『满级』不含『每级』"⇒ 命中 2 条（`refining｜精炼学`「与高级回收处理双修满级合计 165%」为**误报**——那是跨技能合计、本技能自身每级齐全；`galactic-happenings｜星际奇遇学` 为**真阶跃**）；判据② core 里按技能等级做比较的分支全仓只有 `wormholeScan.ts` 一处（`balance.ts` 旋钮一律 `*PerLevel`）；同类早已拆过的两条 = **星图记录学**（2026-09-16 船长「**效果错误，应该为每级+2，满级+10**」）· **事件玄学**（09-14 落码即取每级 −4%）。**④落码（7 处）**：`core/wormholeScan.ts`（`happeningsScanFactor` ⇒ `Math.max(0, 1 - 0.04 * lv)`，函数头注与文件头注按新裁定重写并记旧裁定作废）· `data/skills.ts`（说明末句 ⇒「虫洞扫描窗口**每级缩短 ⟦4%⟧**（满级 −⟦20%⟧）」＋条内注释追记改判）· `tools/content-check.ts`（技能说明契约登记值 `0.2 → 0.04`，现场复核命中）· `core/tests/wormhole-scan.test.ts`（阶跃断言 ⇒ 线性：Lv5 ×0.8 · **Lv4 ×0.84** · **Lv1 ×0.96**；文件头与用例名同步）· 界面文案两处（`WormholeScan.tsx` ⓘ 悬停、`Handbook.tsx`「怎么扫」词条 ⇒「每级 −4%，满级 −20%」）。**⑤读数（真引擎逐级实测）**：系数 **1.00 / 0.96 / 0.92 / 0.88 / 0.84 / 0.80** ⇒ 窗口 **12.00 / 11.52 / 11.04 / 10.56 / 10.08 / 9.60 小时**（Lv0→Lv5）；**满级读数逐字不变**（三技能全满 + 奇遇学满级仍 ≈169.3 分钟/个）。**⑥闸门**：typecheck 四包 **0 错** · core **162 文件 / 1,758 例** 全绿 · `content:check` ✅（技能说明契约：**79 个技能登记来源 · 核对 89 个每级值 · 51 条引擎现场复核 · 未接线 0**）· `ui:rot-check` ✅ · 桌面＋网页 build ✅。**⑦未做（登记备查）**：不加"防阶跃"常驻体检（船长本轮选「不加」）⇒ 日后新技能若又写成「满级后…」体检查不出，靠人查；`refining` 那条误报不动。**【已推送 origin/main】**
- 2026-09-17：**复核玩家报障「虫洞遗迹每次打捞都会触发战斗」⇒ 取证判为"已确认口径的必然体感、非缺陷"，船长裁定「不改（登记为已确认口径）」（船长 · 一号 · main）**——**①船长原话（照抄）**：「**玩家反应，虫洞遗迹每次打捞都会触发战斗**」。**②取证（真引擎一次性探针 · `wormholeMakeGrid` ＋ `wormholeSalvageAt` · 跑完即删）**：**同格重复触发实测 0/171 格**（8 种子 × 层 2~7 · 324 次真打捞 · 每次清标记＝模拟打赢）⇒ 一格至多一场、`grid.ruinsRolled` 记账与代码读法一致（**不是重复触发的 bug**）。真实成因 = **70%/格 × 深层格数多**：逐层遗迹格均值（30 种子，层 1→8）**0 / 0.6 / 2.5 / 2.6 / 4.7 / 5.0 / 5.2 / 4.9**、逐格掷中率实测 **62.8%~77.1%**（与 0.7 相符 ⇒ 不是分布坑）；**一趟（层 1~7 全清）＝ 15.0 场**（逐格掷点算）／**16.3 场**（真打捞实测，区间 11~24），层 5 / 6 / 7 各 **3.3 / 3.9 / 4.0 场**；守军强度 = **本层威胁 ×1.3**（`WORMHOLE_RUINS_THREAT_MUL` ⇒ 层内除层末守卫外最硬的一场）。**③冲突的旧口径两条（按 §5.2 先摆出来再动）**：① **70% 是你确认过的值**——设计稿 `wormhole-extraction-endgame-20260912.md:702`「遗迹收尾战 | 概率 **70%**、强度 **本层威胁 ×1.3**……✅ **船长 2026-09-13 确认**（「**除了 5 其他没问题**」）」（同稿 F3b 行原话是「**结束时**大概率高难战」）；② **"首捞即掷"也是你选的**——`wh-ruins-roll-timing-20260916.md`（已归档 · `git show a7322be4:docs/design/wh-ruins-roll-timing-20260916.md`）「船长裁定：『**时间点改为遗迹第一次打捞**』」＋「**惊扰守卫 = 乙**（**也提前**到第一次打捞）」，该稿自己就登记过风险「**危险节奏**：惊扰守卫提前 ⇒ 第一次打捞就可能当场开战」。**④裁定与登记**：三个备选都摆了读数——**甲 降概率**（0.4⇒8.4 · 0.3⇒6.6 · **0.25⇒5.3** · 0.2⇒4.3 场/趟）· **乙 每层封顶一场**（0.7⇒5.2 · 0.5⇒4.6 · 0.3⇒3.6）· **丙 时机回到"打空该格"**（＝设计稿原话「结束时」；频率不变、但不再打断）⇒ 船长选「**不改（登记为已确认口径）**」⇒ **70% 与首捞时机一字不动**；词典新增「**惊扰守卫 / 遗迹守备战**」条（含本次读数与"甲/乙/丙 未采用、留档备查"）。**⑤本批零代码 / 零数值改动**（唯一新增物是临时探针，按 §6 用完即删；方法与读数已写进词典与本节）。**【已推送 origin/main】**
- 2026-09-16：**侦察舰特性：隐秘行动装置 CPU 减半（向上取整）＋ 免推进器失效（船长四答逐条落码 · 一号 · main）**——**①船长原话（照抄）**：「**侦查舰添加特性，隐秘行动装置所需CPU降低50%，且移除推进器失效惩罚。装置限制说明改为：与任何类型推进器一起使用时失效。**」＋澄清「**修改的是隐秘行动装置的限制说明**」（⇒ 那句只是**装置说明文案**的措辞，不改引擎规则）＋四答：**范围 = 只有「侦察舰」子分类那两艘** · **取整 = 向上取整** · **推进器惩罚 = 完全移除** · **展示 = 特性栏 ＋ 装配页标注都要**。**②现状（动手前查的）**：「侦察舰」是**子分类**（`ShipDef.subClass`），全仓恰好两艘 = **鹦鹉螺级测绘巡洋舰**（`sh-nautilus` · T3 · CPU 340）与**幽影侦察舰**（`sh-wh-g-frigate` · T1 · CPU 175）；舰船特性的现行做法 = **数据字段驱动**（照同日「后勤舰」把"代码硬判子分类"改成字段的先例）；`fittedCpuUsed` 当时只逐件相加、没有"逐船折算"的口子。**③落码（十处）**：`types.ts` 新增 `ShipDef.stealthCpuMul?` 与 `stealthIgnoresPropulsion?`（注释写明船长原话与取整口径）· `data/ships.ts` **只有那两艘**写 `0.5` / `true`（其余 41 艘不写）· `equipment.ts` 新增单点 **`cpuUseOf(def, ship?)`**（`ceil(cpuUse × 倍率)`，**只作用于带 `stealthMs` 的件**）＋ `fittedCpuUsed` 增第三个可选参数，`cpuOverloadText` / `fitModuleTo` / `adjustDroneLoad` 三处调用点带上本船 · `combat.ts` 推进器禁令改为 `propDefs.length > 0 && ship.stealthIgnoresPropulsion !== true`（**不在引擎里判子分类**）· `modules.ts` 两档限制说明按船长指定措辞改写 · `shipInfo.tsx`「船体特性」栏补两条 · `FitPage.tsx` 空位候选与已装槽 tooltip 追加「← 本船特性：CPU 28（原 55）· 装推进器也能保持隐身」、三处 CPU 读数带上本船（**界面写的数 = 引擎算的数**）· `content-check.ts` 新增「**侦察舰特性契约**」· `tests/stealth-device.test.ts` 新增「侦察舰特性」5 条。**④闸门与读数**：typecheck 四包 0 错 · core **157 文件 / 1,714 例** 全绿 · `content:check` ✅（读数行：那两艘 ⇒ ×0.5 向上取整 **55→28 · 80→40**；装推进器亦可隐身；**其余 41 艘一律不带，实测多带 0 件**；限制说明已改写）· `ui:rot-check` ✅ · 双构建 ✅；**负向验证两次各自精准**（倍率改 0.4 ⇒ 契约红；说明改回旧措辞 ⇒ 契约逐条点名；复原即绿）。**⑤遗留（待船长一句话）**：装置说明里我多加了半句「**——侦察舰不受此限**」（**不是船长原话**；不加则这句说明对侦察舰就是错的）——若认为例外只该走舰船特性栏，说一声即删（`modules.ts` 两处）。**⑥归档同步**：词典「隐秘行动装置」条补该例外 ＋ 新增「侦察舰（子分类 · 特性）」词条。**【已推送 origin/main（`3f969eeb`，经 `9cb605de` 合入）】**
- 2026-09-16：**修报障「仓库有 553 发 MK2、进战斗却提示无弹」⇒ 弹药取档口径改判：同族取"能装得最多"的那一档（船长两答取推荐 · 一号 · main）**——**①船长原话（照抄）**：「**玩家反应，爆破导弹MK2的仓库有553个，，舰船携带重型导弹架MK2进入战斗后提示无弹**」。**②根因（真引擎探针三情形对照 · 探针跑完即删）**：**不是漏接线**，是 2026-09-09「选档制」在实战里露出的两个缺口——**A 没在装配页选档（默认）**：只装基础弹、仓库 553 发 MK2 **一发未动**，基础弹 0 ⇒ 界面「（无弹）」（**玩家遇到的正是这个**）· **C 选了 MK2 但持有量 < 本场需求 207**：旧规则**整族回退基础弹** ⇒ 那 30 发 MK2 一发不用、基础弹 0 ⇒ 仍无弹。**③§5.2 冲突上报与裁定**：旧口径原文（船长 2026-09-09，封存卷 `docs/archive/roadmap-2026-09-09.md`）「配置档库存不足**整族回退基础弹 + 日志**（不卡远征）」与新需求不可同时成立 ⇒ 船长**两问两答都取推荐**：**①回退改为"同族取能装得最多的那一档"（允许装不满）②装配页两侧都显示库存 ＋ 0 库存时预警** ⇒ 旧口径**作废**（`docs/design/ammo-mk2-20260909.md` 就地标注作废并指向本条）。**④现行口径（单点 `combat.resolveAmmoTier`，引擎装载与装配页共用）**：候选 = 基础弹恒在 ∪ 同族全部档 ⇒ 各算可装量 `min(该档库存, 本场需求)` ⇒ **取可装量最大者**；**平局按期望档优先**（`ammoPref` ＞ 基础弹 ＞ id 序）⇒ **没选档且两档都够时仍走基础弹**（不偷偷烧玩家没选的高级弹）；改档即写日志「⚙ 基础弹可用量不足，本场改用爆破导弹 MK2（预载 207 发）」；**`battle.ammoIds` 只要有实装就记实际档**（含实际档 = 基础弹）——⚠ 这条是**正确性修复**（旧口径只在设了档时记 ⇒ 推进重建会按 MK2 结算伤害、退还却退基础弹，**串档**）。界面：两侧库存都显示 ＋ 一行「本场预载：〈档名〉可装 C/D」（**界面显示 = 实战结果**，同一把尺 `ammoLoadTotals`）＋ 两条红字预警。**⑤读数与闸门**：typecheck 四包 0 错 · core **156 文件 / 1,691 例**（+4）· `content:check` ✅ · `ui:rot-check` ✅ · 双构建 ✅；探针复现对照（3× 重型导弹架 MK2 · 本场需求 = `⌈4 分钟 × 1.5 余量 ÷ 装填 5280ms⌉ × 3 门` = **207**）：A 情形 553→346（装 207 发 MK2）· C 情形 0 发 → **基础弹 60 发 ＋ 日志**。**⑥公告**：船长同日定「**不用公告**」⇒ 不发公告、不另出待审稿；2026-09-10 已上线公告里那句「库存不足整族回退基础弹」按纪律**不回改**。**⑦未做**：不加"没选档就自动选最高档"的默认偏好。**【已推送 origin/main（`191c294e`）】**
- 2026-09-16：**敌舰体火力「越线折扣」：超过 150 的部分打 85 折（船长两句改判本机制形态 · 一号 · main）**——**①船长原话（照抄）**：先「**超过150的火力，按比例衰减**」⇒ 改判**形态**「**不是钳制到150，而是超过150的部分进行一个约15%的折扣**」⇒ 生效范围「**是，都生效**」（洞外与虫洞一律）。**②前史（本机制的来源，已被本次取代）**：2026-09-12「按照 DPS 上限 150 算」的**硬钳制**在同日深夜「**火力钳制也暂时关闭**」⇒ `foeDpsCap` 置 0；本次改为折扣形态重新开启（**不会退回旧硬钳制**）。**③判线口径（沿用 2026-09-12 已实测确认的三条，不动）**：按**整卡每波舰体总 DPS** 判线（逐波独立建档）· D **含多舰补偿 `2N/(N+1)`** · **不含机群**与**被 `droneFireShare` / `firepowerAnchor` 拆分的条目**（那条链自带总火力锚定，再打折会让锚点失准）。**公式**：`D ≤ 150 ⇒ ×1`；`D > 150 ⇒ 目标 D′ = 150 + (D−150)×0.85`、系数**等比例**施加于全卡舰体单发（条目相对权重不变）⇒ **不封顶**（斜率 1 → 0.85，`D → ∞` 时 D′ ≈ 0.85D）。**旋钮** = `foeDpsCap`（阈值 150）＋新增 `foeDpsOverCapDiscount`（0.15），**两者同时为正才生效**，任一未写 / 0 ⇒ 零行为变化。**④读数（`npm run battle:overcap` · 新正式工具）**：洞外 27 张基础卡**逐字不变**（最高 = 虚海守望者 131.3 ≤ 150）；派生档越线者打折——穹顶守卫 L1/L2/L3 **164.8 / 203.1 / 253.2 → 162.7 / 195.2 / 237.8**（−1.3% ~ −6.1%）、虚海守望者 L1/L2 170.3 / 210.0 → 167.3 / 201.0、噬口猎杀令 L2/L3 172.3 / 215.5 → 169.3 / 205.5（⚠ 噬口是 **3 波卡**、头目在第 3 波 ⇒ 判线必须逐波看，波 0 只有 4 只小虫 ≈ 20 DPS 会误判成"未越线"；旧注释只登记了穹顶 / 虚海，本次已更正）；虫洞一并吃折扣 ⇒ 层 1 −1% ~ −8%、**深层峰值 node 1,234.5 → 1,071.8 · boss 1,665.8 → 1,438.5（−13%）** ⇒ 属"轻收敛"，**虫洞既有难度口径（×1.10 增长 / 激活免费 / 撤离战取消）不需要重校**。**⑤落码**：`types.ts`（新字段）· `combat.ts`（`foeDpsCapScaleOf` 由硬钳公式改折扣公式）· `balance.ts`（`foeDpsCap: 0 → 150` ＋ `foeDpsOverCapDiscount: 0.15`，注释按改判重写并更正过期读数）· `tests/foe-dps-cap.test.ts`（现值由"关闭"改"开启"＋折扣六类断言）· 新工具 `tools/foe-dps-overcap.ts`（`npm run battle:overcap`，两旋钮可 `--cap=… --disc=…` 覆写）；读数存档 `tools/_ui-artifacts/dps-overcap-20260915.log`。**⑥闸门**：typecheck 四包 0 错 · core **149 文件 / 1,598 例** 全绿 · `content:check` ✅（两条既有预警 `bp-shieldchg-2/3`，与本批无关）· **负向验证**：折扣率临时置 0 ⇒ `foe-dps-cap` **5 条如实红**（533.5 / 952 / 1655 / 159 四处定值 ＋ 现值断言），还原即绿 · `tools:audit` 登记 **v25 · 2026-09-15** · 双构建 ✅（不涉 UI ⇒ `ui:rot-check` 免跑）。**⑦不做**：机群单发 / 多舰补偿口径 / 舰级路径绝对值 / 任何卡数据（`anomalies.ts`、`foe-ships.ts` 零改动）/ UI 与卡面 / 血量曲线 / 速度射程钳制。**【已推送 origin/main（`23d473a5`，经 `fd91b12d` 合入）】**
- 2026-09-15：**新功能「限时倍率表」：按现实日期给指定数值上倍率（船长六答 · 一号 · main）**——**①船长原话（照抄）**：「**添加一个新功能，允许我快速设置在指定的现实日期之前，给特定数值调整一个倍率（比如残骸量，虫洞扫描周期等）。**」＋六问六答：**①生效范围 = 写进正式数据（全员生效）· ②可调面 = 白名单注册表 · ③清单 = 残骸密度 / 稀有残骸掉率 / 稀有残骸体积 / 虫洞扫描周期 / 采矿速度 / 悬赏任务奖励 / 技能训练 · ④时间 = 支持区间（开始可省）＋ 截止日期 · ⑤叠加 = 相乘 ＋ 界面可见 · ⑥入口 = 数据文件**（改完重载生效），并且「同时拥有限时加成时，还会在**活动无人机的右侧**（扫描进度条的右侧）显示当前加成项是什么和剩余时间」。**②落码**：**`packages/core/src/tuning.ts`（新）** = `TUNABLE_KNOBS`（8 个开关的白名单：名称 / 乘在哪 / 方向）· `TUNING_RULES`（**当前为空**，头注给了写法示例）· `localDayStartMs` / `ruleActiveAt` / `tuningMulAt` / **`tuningMul(state, key)`** / `activeTunings(now)`；`state.wallMs?`（**不落盘、不参与迁移**）只在显式传 `nowWallMs` 时写入 ⇒ **工具与用例不传 ⇒ 恒 1×**（标定读数不被日历污染）；**8 个读取点**（残骸密度只乘读取值 · 派系掉落与墓场判定掷点、遗迹 / 墓场每件单位数 · 打捞彩头 · 自动探索发放 · 扫描窗口 · 采矿周期与产量 · 悬赏结算基底与三类任务生成 · 技能训练时长）；`ActivityBar` 在**扫描条右侧**加「限时加成」徽标（多条横排 · 名称 × 倍率 · 剩余时间 · 悬停写明备注与截止日，照 `.app-activitybar-scan` 同族复刻、定宽 190px 防头部跳动）；`content:check` 新增「**限时倍率表契约**」4 条（未知开关 / 倍率必须 > 0 且有限 / 日期合法且 `from ≤ until` / **每个白名单开关都必须有读取点**——防"登记了没接线"的静默失效）；`tests/tuning.test.ts` **12 条**（表本身 5 ＋ 八个开关逐个"乘在正确的地方" 7）。**③口径**：`YYYY-MM-DD` 按**本地时区**解析 · `from`（可省）= 当地 00:00 起 · `until` = **当天整天仍生效**、次日 00:00 失效 · 同一开关多条命中 ⇒ **相乘** · **不落存档生效状态**（命中现算 ⇒ 到期自动回落，无需回收 / 迁移）· `rareWreckRate` 夹在 ≤ 1 · `rareWreckVolume` 只乘**发放时的单位数**（物品说明里"单件 30 m³"是基础值文案、活动期不随动）· 技能系统没有经验点 ⇒ `skillTrainMs` 按**训练时长**倍数（×0.5 = 练得更快）。**④怎么用（船长操作）**：在 `TUNING_RULES` 里加一行即生效（例 `{ key: 'wreckDensity', mul: 2, from: '2026-09-20', until: '2026-09-22', note: '周末双倍残骸' }`）。**⑤闸门**：typecheck 四包 0 错 · core 全量（含新增 12 条）· `content:check` ✅ · `ui:rot-check` ✅ · 双构建 ✅；**负向验证**：摘掉任一开关的读取点 ⇒ 契约点名该开关、去掉区间上界判断 ⇒ 区间边界用例红。**⑥与促销的关系**：限时促销（`PROMOS`，如「虫洞大量生成」）与限时加成**共用日期判据**（`dayWindowActiveAt` / `dayWindowEndMs`）；促销是**全时段环境乘子** ⇒ 任何"给扫描窗口定读数"的用例必须避开活动窗口（2026-09-16 踩过一次日历重叠）。**⑦遗留（待船长）**：①要不要发一条「限时活动」公告（待审稿可随时补写）②未接入候选（精炼产出 / 市场价 / 战斗伤害血量 / 虫洞威胁回合）③悬赏**卡面**奖金显示目前仍读原始 `rewardIsk`（结算已吃倍率，界面另有徽标解释差异）——要卡面也 × 倍率说一声 ④表放 core 的理由（读取点全在 core，且 core 不能反向 import data，`balance.ts` 是同款先例）。**【已推送 origin/main（`0672ff9a`，经 `d5241f40` 合入）】**
- 2026-09-17：**修报障「虫洞地图悬停时同屏两个提示」——取消地图框的滚轮缩放 title（船长 · 一号 · main）**——**①船长原话（照抄）**：「**虫洞探索的地图，鼠标在地点上悬停时，会同时出现滚轮缩放的title提示和格子信息，建议取消滚轮缩放的title**」。**②根因（不是"两个 title 都弹"，是原生 title 走祖先回退）**：格子上的提示写的是 **SVG 的 `data-tip`**（由 `ui/Tooltip.tsx` 自绘层接管 ⇒ 一个自绘提示），而**悬停元素自身没有 `title` 时，浏览器会沿祖先链找最近的 `title` 并照原生弹出** —— 地图框 `.app-wh-mapbox` 那层写着 `title="滚轮缩放地图（…）"` ⇒ **自绘提示（格子信息）＋ 浏览器原生提示（滚轮那句话）同屏**，与船长看到的现象逐字对上。**③落码**：删掉 `.app-wh-mapbox` 的 `title`（并在原处留注释写死机制与"此处不许再写 title"）；那句话**一字不改**搬进图例行末尾的 **ⓘ `HintIcon`**（"常驻说明进 ⓘ"的既有口径，与货仓说明 2026-09-14 那次同款处置）。**④复核（穷举祖先链）**：`Wormhole.tsx` 里剩下的 `title=` 全在**叶子**上（缩放 ＋/－/适应 三个按钮、扫描/打捞/深入/装舱等按钮、地点读数 span、准备页卡片），**没有一处落在格子祖先链上**；`Panel` 组件的 `title` 是 **prop 渲染成 `<h2>` 文本**、不落 HTML 属性；地图子树内**无 SVG `<title>` 子元素**（另一条原生提示路径）⇒ 原生提示已无来源。**⑤闸门**：typecheck 四包 **0 错** · core **162 文件 / 1,753 例** 全绿 · `content:check` ✅ · `ui:rot-check` ✅。（按 `tools/README.md` §三口径：界面改动**不强制**交无头实测，这里以"祖先链上无 title"的穷举复核 ＋ 四闸门为准；观感由船长过目。）**【已推送 origin/main】**
- 2026-09-17：**异星原型件抬到「同槽 MK3 行价 ×15」（船长定价 · 一号 · main）**——**①船长问 → 裁定**：先问「**异星装备的价格是否太低了？**」⇒ 我跑读数（同槽 MK3 对照）后他定「**按照 MK3 的十五倍价格估算**」。**②取证读数（原价，现跑）**：三件奇货原型件里**两件工业件比它们取代的 MK3 还便宜**——`mod-miner-proto` **+110%** 卖 1,600,000 而 `mod-miner-3` **+80%** 卖 2,330,000（每 +1% 效果 **14,545 vs 29,125**）· `mod-cargo-proto` **+180%** 卖 1,500,000 而 `mod-cargo-3` **+140%** 卖 2,400,000（**8,333 vs 17,143**）；**激光炮本就大致合理**（`mod-laser-proto` 2,395,000 → 3,000,000＝价 +25% ≈ 射程 10.5→13 km +24%，名义 DPS 只 +5.6%，每 km 射程单价 228k vs 231k 基本持平）。渠道位置：39 条可买奇货里原型货舱排**第 7 便宜**、采集器**第 8**、激光**第 13**——而三件名义上是"**超档收藏**"、不可制造无蓝图、只走奇货（声望 10、0.8%/10 分钟独立掷骰），玩家唯一代价只是 **CPU 高 50%**（60/60/70 vs 40/40/52）。**③落码**：`marketCatalog.ts` 三行 `basePrice` → **34,950,000**（=2,330,000×15）· **36,000,000**（=2,400,000×15）· **35,925,000**（=2,395,000×15），行内注释写死推导与缘由；`demandMultiplier` 仍 **1.0**（奇货全价回收）⇒ **抬价 1:1 抬收购价、不产生套利**（供应价恒 ≈1.0~1.06L 高于全价回收）。**④护栏（两处常驻）**：`tools/price-audit.ts` 新增「**奇货原型件：行价 = 同槽 MK3 ×15**」段（逐件打 现价/目标/偏差，偏离 ±1% 即红并置退出码 1；该工具头注原写"奇货原型件不在本表内"已改写）＋ 新用例 `packages/core/tests/proto-price.test.ts`（3 条：×15（±1%）· 不连带改动（仍奇货/声望 10/全价回收/无蓝图）· 抬价后已显著贵过 MK3）。**⑤连带面排查（都核过）**：模块行价的读取点只有 `manufacturing.ts` 的 AI 制造收益估值（原型件**不可制造** ⇒ 不受影响）· 战斗保险是"**单次齐射伤害封顶**"型、与价值无关 ⇒ 无连带 · 图鉴/装配页价格走同一张市场卡 ⇒ 自动跟随。**⑥闸门**：typecheck 四包 **0 错** · core **163 文件 / 1,753 例** 全绿 · `content:check` ✅ · `npm run price:audit` ✅（三件偏差 0.0%）。**⑦归档**：词典「价格权威关系（真源 = marketCatalog）」条补该裁定。**【已推送 origin/main】**
- 2026-09-17：**新增 `foe:export`：敌人「敌舰明细」导出（xlsx 单 sheet ＋ 同列 CSV）（船长 · 一号 · main）**——**①船长原话（照抄）**：「**能否将所有敌人的详细数据单独输出一个excel表格（之前的工具输出都太过粗糙，数据不详细）**」⇒ 追问后收窄「**只需要敌舰明细。**」。**②现状（动手前查的）**：`exceljs` 早已是 devDependency（`content-export.ts` 的多 sheet 工作台先例）⇒ **不需新装依赖**；而现有敌人工具只各切一面（`bounty:stats` 仅整卡总血/总DPS · `foe-hp-table` 按威胁档参考表 · `counter-audit`/`faction-audit`/`foe-dps-overcap`/`pd-vs-drone`），**逐舰级的三层三系抗性、射程带/期望交距/衰减/近盲、闪避与实速、挂载件与冲锋/捕获网、后勤修理、机群**没有任何一处汇总。**③产出**：`tools/foe-export.ts`（npm script **`foe:export`**）⇒ `foe-csv/enemy-ships.xlsx`（**一张「敌舰明细」sheet：25 个敌舰级 × 56 列**，冻结前两列＋表头、开自动筛选、表头带单元格批注写口径、按列数字格式）＋ `foe-csv/enemy-ships.csv`（**同列同序**、试算列在 CSV 里由工具算成数值、UTF-8 带 BOM）；`foe-csv/` 已入 `.gitignore`（与 `content-csv/` 同款）。列分组 = ①标识（id/名/族/舰种档与名/精锐/战术）②血量（总血＋结构/装甲/护盾值与占比）③**三层×三系抗性 9 列** ④火力（单发/装填 ms 与秒/命中率/**命中模型**/伤害构成/**名义 DPS 合计与分系**/射程带/期望交距与来源/远端衰减/近盲倍率/闪避/**实速与倍率**）⑤特性（挂载件/冲锋与倍率与冷却/捕获网/挨打后机群与炮台射程倍率/后勤修理/机群明细与机群名义 DPS）⑥**试算列 4 列（Excel 公式：DPS、三层血）**。**④口径**：全部是**舰级裸值**——登记原值取 data 表；派生走引擎同一把尺（三层血 = 总血×占比 · 名义 DPS = 单发×1000÷装填**不含命中与距离衰减** · 实速 = `hullClassBaseSpeedMps`×`speedRatio` 取整 · 期望交距 = `desireRangeM` ?? 射程下限+战术系数×带宽 夹 `minDistanceM` · 挂载件走 `resolveFoeMounts`）；**刻意不含卡级覆写**（条目级 `hpMul`/`dmgMul`/`rangeMul`/`split`/`tactic`/`mounts`/`droneFireShare`、多舰补偿、越线折扣）⇒ 卡级实算值仍走 `npm run bounty:stats`，避免两套口径混表。**⑤交叉核对（表里的数是不是引擎的数）**：劫掠电子舰 **182 / 0.30 / 374**（与他批提交记录「血 182 / 闪避 0.30 / 速 374」逐字一致）· 劫掠护卫舰实速 **374**（「337 → 374」）· 海盗头目舰 **900 / 135**（`foe-ships.ts` 登记注与设计表）。**⑥闸门**：`npm run foe:export` 跑通（自检读数：A5/B2/C5/D4/E4/G5 · 最高总血 核心舱段 3,920 · 最高名义 DPS 136.5 · 带机群 6 条 · 带挂载件 10 条）· `npm run tools:audit` 登记为 **v25 · 核对 2026-09-17** · typecheck 四包 0 错 · `content:check` ✅；`tools/README.md` 台账 **29 → 30**。**【已推送 origin/main】**
- 2026-09-17：**修报障「组装机目标件数被切页吞掉 → 变成无限生产」（船长转述玩家 · 一号 · main）**——**①船长原话（照抄）**：「**玩家反应，组装机生产弹药时，如果指定了生产数量，切换其他页面再切回来，有时候会移除目标数量变成无限生产。**」**②复现（真浏览器 · 真产物 · 自己的无头 Chrome）**：一次性探针（只读托管 `web/dist` 4179 ＋ 自起无头 Chrome/CDP 9223，独立 profile、收尾只杀自己那一个进程）种入测试档（已学 `bp-ammo-plasma`＝能量弹药），进「工业→组装机→能量弹药卡」开循环、往「目标」打 500，四种导航各跑一遍读**输入框＋落盘真值**：**A 回车＋鼠标点导航 = 500/500 ✓ · B 不回车＋鼠标点导航 = 500/500 ✓（mousedown 先失焦，侥幸提交） · C 不回车＋程序化跳页（合成点击，不失焦）= 空/null ✗ ⇒ ∞ · D 已设 500 再聚焦后点导航 = 500/500 ✓**。**③根因**：目标件数**只在 `onBlur`/`Enter` 提交**（`goalDraft` 是组件局部 state），而**程序化跳页**（通讯「前往」、教程跳转、任务卡跳转）**不产生 blur** ⇒ 数字从未提交、循环开关仍开 ⇒ 目标空 = 无限生产；鼠标点左侧导航会先 `mousedown→blur` ⇒ "有时候"丢、有时候不丢，与玩家措辞完全对上。**同时排除**：存档清洗/离线结算/存档往返都不丢 `goal`（测试档 10 张卡的 goal 全在）、core 只在达成目标/缺料时置 `on=false` 且**保留 goal**。**④修法（`panels/Industry.tsx`）**：草稿进 ref ＋「玩家打过字」标记（`onChange` 置真、**任何正式提交即清账**）；**卡片卸载兜底补提交**（切页/切子页签都会卸载卡片）⇒ 打过字就一定生效；「没打过字」与 StrictMode「挂载即卸载」**都不误提交**（堵死"静默清空"风险）；**清空照旧如实生效**（E 变体：设 500→清空→跳页 ⇒ 落盘 null ✓）；玩家可见改动只有一句 tooltip（回车/失焦生效 → **回车、点空白处、或离开本页都会记住**）。**⑤修复后复跑**（重建产物）：A/B/D 500/500 ✓ · **C 由 空/null 变 500/500 ✓** · E 空/null ✓。**⑥闸门**：typecheck 四包 0 错 · core **161 文件 / 1,748 例** · `content:check` ✅ · `ui:rot-check` ✅；探针按纪律删除（全程未碰船长浏览器）。**【已推送 origin/main】**
- 2026-09-16：**稀有订单档位权重复核：档 4 补单独系数 0.05（船长问「2/3/4 的权重」→ 选「选项 B」· 一号 · main）**——**①船长原话（照抄）**：「**稀有订单的2/3/4的权重分别是多少**」⇒（答：档 2 **×1** · 档 3 **×0.15** · 档 4 **×1（与大众档同频）**，并摆出两个已实测的旋钮 A/B）⇒「**按选项B**」。**②由来**：`market.ts` 的 `rareTierWeight` 在 2026-09-09 定这条带时**只特判档 3**（当时档 4 ＝"奇货层"、从不进稀有渠道）；同日「甲＋乙」把 5 艘官方巡洋舰**保 4** 挪进稀有订单后，它们落 `else` 拿 ×1 ⇒ 实测单行 **3.2 窗 ≈32 分钟**（改动前在奇货 ≈20.8 小时/件）。**③两个旋钮都实测过**（**临时**改权重函数跑 `market-rarity-sim` 再复原，`git diff` 已核为空）：现状 = 档2 5.0 窗≈0.8h / 档3 25.1 窗≈4.2h / 档4 3.2 窗≈0.5h；**A**「档 ≥3 都吃 0.15」⇒ 档4 16.2 窗≈2.7h；**B**「档 4 = 0.05」⇒ 档4 **57.2 窗≈9.5h**（船长选 B）。**④落码**：`balance.ts` 新增 `rareTier4Weight: 0.05`（带由来与读数）· `market.ts` `rareTierWeight` 加档 4 分支 · `types.ts` 的 `MarketBalance` 与 `rarityTier` 注释 · `rarityTier.ts`/`content-check.ts`/`marketCatalog.ts` 三处口径注释同步 · `market-rarity-sim.ts` 新增 **`--tier4-weight`** 覆写（读数行改打印三档系数）· 用例 `market.test.ts`「数字稀有度分层」增档 4 一条（2/3/4 三行同抽 + 覆写 0/1 两端）。**⑤闸门**：typecheck 四包 **0 错** · core **161 文件 / 1,748 例** · `content:check` ✅ · `ui:rot-check` ✅；**负向验证**：撤掉档 4 分支 ⇒ 新用例精准报「档 4 应显著低于档 2（系数 0.05 ⇒ 约 1/20）: expected 372 to be less than 131」，复原即绿。**⑥新默认读数**（窗 720 × 5 种子 · 声望 24）：档 2 ≈0.8h · 档 3 ≈3.9h · **5 艘巡洋舰 ≈6.8~9.5 小时/件**（长尾鲨 9.5 · 电鳐 9.3 · 锤头鲨 8.4 · 牛鲨 6.8 · 鹦鹉螺 8.6）。**⑦遗留**：档 **5** 仍无自己的系数（走 `else` ⇒ ×1；契约已允许 exotic 带 3~5，将来启用档 5 时一并定）；那 5 行 `demandMultiplier` 仍 **1.0**（奇货口径全价回收）按「其他不变」保留。**⑧踩坑留档**：注释里写 `**≈9.5 小时**/件` ⇒ `**` 紧跟 `/` 把块注释提前闭合，tsc 报 `TS1127 Invalid character`；已改写为「≈9.5 小时一件」并全仓扫过 `**/` 组合。**【已推送 origin/main】**
- 2026-09-16：**官方五艘巡洋舰进「稀有订单」（船长「甲＋乙」两裁）＋ 稀有度契约改区间（一号 · main）**——**①船长原话（照抄）**：「**将目前官方的几艘巡洋舰舰船和一次性蓝图在稀有度不变的前提下，放到稀有订单里，所需声望降低为8。其他不变。**」⇒（摆出现状与 §5.2 冲突＋甲/乙/丙三案后）「**1甲2乙**」＝范围**甲**（名字带「巡洋舰」的 5 艘：长尾鲨/电鳐/锤头鲨/牛鲨/鹦鹉螺）· 数字档**乙**（**保持 4 不动**，只挪渠道）；再一条「**那么修正契约，rate现在允许2~4，exotic拓展到3~5**」。**②冲突的旧口径四条（照抄出处）**：`marketCatalog.ts:465`（2026-09-09「**T3 巡洋入奇货**」）· `:452/:445`（2026-09-13「玳瑁/蝠鲼**渠道升奇货**」）· `:484-485`（2026-09-13「**T3 现货 12 / 蓝图 15**」）· 词典「**武装巡洋级内容 = 奇货渠道**」（2026-09-09）——四条**作废**（行内与词典均已改写/标注）。**③落码**：`marketCatalog.ts` 5 艘现货 `exotic → rare`、`standingReq 12 → 8`、**数字档保 4**（行体从「限定奇货」段挪进「稀有订单」段并留指路注释）；5 张**一次性**图纸 `standingReq 15 → 8`；**价格与 `demandMultiplier` 逐值保留**（9/15/11/13/9 M · 全 1.0）；`content-check.ts` 契约改**区间**（common `1` · rare `2~4` · exotic `3~5`，值域上沿 4 → **5**）；`rarityTier.ts` 头注与 `types.ts` 的档位语义同步松开。**④"其他不变"的逐值锚**（新用例钉住）：T3 里没被点名的现货/图纸（座头鲸矿舰·旗鱼货舰·鲸王采矿艇·王鲭母舰·玳瑁重装巡舰；一次性图纸其余五张）、**非一次性**图纸（仍奇货 15）、T4/T5 舰体与图纸、洞内族专属 15 艘、MK3 等其他奇货商品、引擎权重口径**一律未动**。**⑤读数（`market-rarity-sim` 窗 720 × 5 种子 · 声望 24）**：rare 供给行 **161**（tier2 66 · tier3 90）· 每窗总张数均值 **18.69**；**5 艘各自单行命中 ≈3.2 窗（≈32 分钟）**，对照改动前在奇货是**每件每窗 0.8% ⇒ ≈20.8 小时/件**——⚠ 这是「乙（保 4）」的**必然结果**（`rareTierWeight` 只认档 3，档 4 走 rare 即 ×1 大众档），选项里已写明、船长已知并接受；想压回去的旋钮 = "给档 4 单独权重"或"档 ≥3 都吃 ×0.15"（本批按「其他不变」未动，登记备查）。**⑥验证**：typecheck 四包 **0 错** · core **161 文件 / 1,747 例** 全绿 · `content:check` ✅ · `ui:rot-check` ✅；**契约负向验证三条各自精准**（rare 行改 5 ⇒「rare 渠道应为 2~4，实际 5」· exotic 行改 2 ⇒「exotic 渠道应为 3~5，实际 2」· 值改 6 ⇒「值非法：6（应为 1~5）」＋带越界，复原即绿）。**⑦遗留登记**：这 5 行 `demandMultiplier` 仍 **1.0**（奇货口径全价回收，稀有其余行 0.65）——按「其他不变」保留，要对齐说一声；档 **5** 暂无商品使用。**⑧玩家可见文案零改动**。**【已推送 origin/main】**
- 2026-09-16：**装配界面悬停改挂仓库同款富卡 ＋ 提示层被换装浮层遮挡修复（船长两条 · 三号 · verify）**——**①船长原话（照抄）**：「**装配界面，鼠标悬停槽位上的装备时，显示出的装备详细过于简陋，参考仓库界面的物品详细。**」＋「**且装入和更换界面中，鼠标悬停的悬浮窗会被界面遮挡。**」**②两条根因（都查实）**：**(a)** 装配台三处（已装槽位 / 换装候选卡 / 无人机舱单元格）只挂了一行 `title=`，而仓库侧早已统一用富卡 `InfoHover`/`ModuleHover`（`ui/shipInfo.tsx` 注释里写着"已装槽位行"却**漏接**）；**(b)** `.app-tip`（全局悬停提示层）`z-index: 1000` **低于**换装浮层 `.app-fit-overlay` 的 **1100** ⇒ 提示层虽画在 root 下 `fixed` 单例里，仍被浮层整片盖住。**③落码**：`ui/shipInfo.tsx` 抽出**唯一一份富卡皮肤** `infoCardContent`，新增两个"不带包裹元素"的出口 **`moduleHoverContent(mod, hint?)`** / **`itemHoverContent(item, nameOf?, hint?)`**（供 `<button>` 这类容器标签不能换的场合，用 `{...hoverTipProps(...)}` 直接挂）；`FitPage` 三处改挂富卡并**删掉原 `title`**（行动提示与"第 N 件衰减"降为末行注脚；± 按钮与候选卡内 chip 保留自身提示 = 内层优先，行为不变）；`styles.css` 的 `.app-tip` **z-index 1000 → 3000** 并写明规矩「**提示层必须压过全站所有浮层（现行最高 1100），今后新加浮层不得超过它**」。**④验证**：typecheck 四包 0 错 · core **158 文件 / 1,728 例** · `content:check` ✅（新注脚文案也过三道文案契约）· `ui:rot-check` ✅ · 双 build ✅；**静态审计（几何读数）**：全站 `z-index` 排行 **3000 > 1100 > 1001 > 980**、三处升级元素逐个核过不再带 `title`；产物 CSS 里 `.app-tip` = `z-index: 3000`。**⑤取舍（如实登记）**：候选卡"卡上摘要 + 富卡参数表"并存（待船长目测定是否收窄）；**禁用态候选卡也挂富卡**（Chromium 对 `disabled` 照派发鼠标事件，真机若不弹则改回 `title`）。⚠ **观感审查权在船长**（实现 + 闸门已完，不自评、不以自截图作依据）。**【已合入 main · 未推送】**
- 2026-09-16：**带括号文案全量重审：6 条跨件对比清零 ＋ 新增「跨件对比契约」（船长二次报障 · 三号 · verify）**——**①船长原话（照抄）**：「**文案中依旧还有类（总输出 ≈ 攻坚炮台的八成）这种文案，建议重新审查一遍所有带有括号的文案。**」**②审查口径（可复现）**：临时探针用编译器 API 抽**全部"带括号的玩家可见串"**（口径同 `content-check`：`apps/desktop/src/*` 与 `packages/{core,data,ui}/src` 的**文本节点**、注释不算）⇒ **1,676 条 / 86 文件**，再按三类筛：跨件对比 / 开发词 / 极值（**自比与跨玩法对比判为合规**）。**③为什么第一轮漏**：那轮清的是**常规装备**，这几条在 **2026-09-10 之后新增的窝点/虫洞专属装备**里，且都塞在括号或破折号后面。**④改掉 8 处**：`modules.ts` 6 条装备说明（掠袭近程导弹巢「射程只有巡航导弹架的三成」· 酸液喷吐器「单发略高于攻坚激光炮…总输出相当」· 陵寝守备炮「射速只有攻坚炮台的四成…（总输出 ≈ 攻坚炮台的八成）」· 掠袭破片炮「单发是攻坚炮台 MK3 的 1.37 倍」· 孢子导弹巢「单发是导弹架 MK3 的 1.6 倍」＋「（**已落码**：逐目标独立结算）」· 陵卫连装炮「单发是攻坚炮台 MK3 的 1.79 倍」）⇒ 一律改**本件自述**（射速/单发/射程各自定性；绝对值界面卡上已有）；`ui/shipInfo.tsx` 无人机信息卡行标「生存（**契约**）」→「生存（**三层血**）」；`panels/TutorialGuide.tsx` 悬停「（历史上每一步都**留档**）」→「（每一步都能回看）」。**⑤新哨**：`content-check` 新增「**跨件对比契约**」——只认**点名了别的装备/舰种**的对比（类名词后 12 字内出现"的 N 成 / 的 N 倍 / 相当 / 同档"，或"它/该件/原型"＋N 成/N 倍），**刻意放过**自比 / 跨玩法对比 / 数值读数（三类在第一轮已判合规）。**⑥验证**：存量 **0 命中**（147 文件）· **反证**（把"单发是导弹架 MK3 的 1.6 倍"塞回 `modules.ts:1543` ⇒ 体检当场点名）· 四闸门 + 双 build 全绿 · 探针跑完即删。**⑦船长同批裁「甲/甲」的边界项**：**删净舰船说明里的渠道标签**（「（限定奇货）」×5 ·「（限定奇货，需高声望）」×1，并同批清掉同类残留「（仅可制造）」×2 ·「（稀有现货或蓝图自造）」×1 ·「市场限量现货」半句）＋ **改净开发语**（「护盾占比**全批**最高」×2 → 自述「**盾厚甲薄**」，含同处版本对比「（不再靠总血厚）」；「火控与回避**双高**」「机巢与无人机战力**双高**」改自述）；**保留**「（与推进器点火加成乘算）」＝叠加规则 ·「收益约为亲自探索的四成」＝跨玩法对比。**【已合入 main · 未推送】**
- 2026-09-15 / 09-16：**首艘自造船通讯（`msg-first-ship`）＋ 触发口径收窄「只认造过」（船长两次裁定 · 三号 · verify）**——**①船长原话（照抄）**：**建**「**新增通讯发送的节点：当玩家造好第一条船后，弹出通讯祝贺玩家，并告诉玩家新建造的舰船在舰船仓库页面。**」（三问裁决「**丙，甲，甲**」；文案指示「**删除③和④。添加一条告诉玩家，想销售舰船也是在舰船仓库进行，只有没有装配的完好舰船才能入库。**」）→ **修**「**购买舰船也会触发第一艘自造船的通讯，这不对**」（二选一取「**甲：造过才发**」）。**②建批落码**：`CommsTrigger` 增 `{ kind: 'shipBuilt' }` · 随档标记 `state.firstShipBuilt`（**置位点唯一** = `manufacturing.settlePiece()` 造船分支：主控亲手开线与 AI 核心代造**同算**）· 弹窗走"除教程外默认弹"· 跳转 = 舰船页「舰船仓库」· `data/messages.ts` 追加该条（深空工业协会 · 工业部 · 提示）。**③修正的根因（先查证，不猜）**：**买船并没有触发它**——两条独立证据：**(a)** 探针四现场（新档买船 不送达 / 新档什么都不做 不送达 / **老档删字段买船 送达** / **老档删字段什么都不做 也送达**）；**(b)** 船长**真档只读**实测：`firstShipBuilt` **缺字段** · `shipStore` **空** · 机库 19 艘**全是买来的** ⇒ 一份"从没造过船的老档"被旧口径（2026-09-15 三问裁决「**丙：老档缺字段 ⇒ 读档即补发**」）当成了"造过船"；而买船走 `buyAtMarket → depositGood → addShipToFleet`，**根本不碰该字段**。**④§5.2 冲突与裁决**：新报障 vs 旧规则「丙」（`types.ts` / `comms.ts` / `messages.ts` / `comms.test.ts` 四处明钉）⇒ 裁决「甲」= 判定改 **`firstShipBuilt === true`**（缺失与 `false` 一律不发）；备选「乙：按痕迹判」**查证不可行**（`shipStore` 也能由"把机库船转进仓库"产生、组装机无历史账本 ⇒ 会漏也会误）。**⑤落码与用例**：判定分支改写 + 注释留档（报障/裁决/取舍）· 四处口径同步 · 2026-09-15 旧工作文档头部标 ⛔ 且 Q1 行标作废 · 用例**新增「买船不算造过」**（照抄船长现场：`addShipToFleet` = 买船落点 ⇒ 不置位、不送达、不弹窗）＋ 原「老档补发」用例改「**老档缺字段也不发**」并补「老档真造一艘后照常送达」；探针前后对照：**修前 ③④ 送达 → 修后 ③④ 均不送达**。**⑥取舍（如实登记）**：早就造过船的老档，这封"指路信"晚到（下次造船时）；**已误发的副本不回撤**（真档只读、删玩家已收到的信属越权）。**【已合入 main · 未推送】**
- 2026-09-16：**18 艘新增舰船补独立 SVG 形与挂点 ＋ 「舰船图形契约」防回潮（船长 · 三号 · verify）**——**①船长原话（照抄）**：「**新增的舰船没有SVG图形，按照之前的规则每艘需要单独的SVG图形**」（两问两答取甲：**18 艘全补** ＋ 同批**补挂点 + 加哨**）。**②缺口（逐艘核过）**：全仓 **43 艘**里 **25 艘**有独立形（2026-09-09 手工全量接入），**18 艘没有**（15 艘虫洞族专属 · 2026-09-13 批 ＋ 鹦鹉螺 / 巨齿鲨 / 邓氏鱼 · 2026-09-12 批）⇒ 一律静默回退 `ShipSprite` 的 role 兜底剪影；**挂点表同覆盖（25）** ⇒ 这 18 艘同时缺引擎喷口 / 真实炮口。**根因 = 此后没有任何哨要求"新船必须配形"**（四道闸门全绿也照样漏）。**③落码**：新文件 `ui/shipArtWh.tsx`（虫洞族 15 艘、按族分节）＋ `ui/shipArt.tsx`（另 3 艘）并表；语言沿用 2026-09-09 规格（**240×110 · 舰首朝右 · 主轮廓 currentColor 2.2 · 面板线 1px/40% · 族色件**），**族血统**取 `ship-battle-art/README` §三 分型表：**A 掠袭**（粗短破旧 / 角旗天线 / 斜排劫掠炮）· **C 巢群**（有机曲线 / 发光裂隙 / 固定磷光绿）· **D 陵墓**（残破古典舰体 / 长舰首 / 冷青磷光）· **E 巨构**（厚重装甲层 / 断裂截面 / 外露桁架）· **G 亡军**（蜂窝舱段 / 补丁帆 / 吊臂），同族三档做**体量阶梯**；另 3 艘按个体特征（鹦鹉螺螺旋装甲环 / 巨齿鲨舰首巨齿 / 邓氏鱼头盾）。**④挂点**：`ui/shipMounts.ts` 逐艘补 `engines`/`muzzles`（坐标取自本批几何；C 族有机舰无喷口 ⇒ 不画尾焰 · 哨戒电子舰/后勤舰无原生炮 ⇒ 回退舰艏锚）⇒ **形表与挂点表 43/43 齐备**。**⑤新哨**：`content:check` 新增「**舰船图形契约**」——每艘船必须在**形表**与**挂点表**各一条，缺哪边**点名报错并写明后果**（落兜底剪影 / 尾焰回落单焰）；**反证两处各自精准**。**⑥复用作**：`tools/shipart-preview.ts` + **`npm run art:ships`** ⇒ 全 43 艘渲成一张静态总览页（按舰种分节、★ 标本批），供船长目测或新人比对（虫洞族舰在存档里不易凑齐）；做法 = 从源码抽 JSX 转 SVG、**与真源同源**。**⑦验证**：typecheck 四包 0 错 · core **156 文件 / 1,696 例** · `content:check` ✅ · `ui:rot-check` ✅ · 双 build ✅（桌面包含量 2,675 → **2,708 KB** = 18 艘形的体量）；总览页 43 艘**缺形 0**。⚠ 观感验收归船长。**【已合入 main · 未推送】**
- 2026-09-16：**弹药定名批：全族统一到《系+弹药》（船长 · 三号 · verify）**——**①船长原话（照抄）**：「**将弹药 爆破导弹改名为爆破弹药，其他的弹药也进行类似的改名**」；追问范围取「**甲：全改**」（物品名 + 蓝图名 + 武器/技能/手册/教程/界面里的旧名引用，并加黑名单防回潮）。**②映射（`id` 与市场 key 一律不变 ⇒ 市场零改动、存档零迁移）**：动能弹 → **动能弹药** · 爆破导弹 → **爆破弹药** · 能量弹药不动（含 MK2 两档与 6 张生产线蓝图）；**「导弹架」这个武器名不动**（被打出去的东西才叫弹药）。**③落码 21 处**：6 件物品（名+描述）· 6 张蓝图 · 武器/技能/舰船/教学说明 · 消耗品池读数注释 · **已发布公告 2 处**（沿用同日"一起改"裁定；「爆破导弹**类**敌舰」改「**导弹类**敌舰」以免与旧名同形）· 任务②奖励日志与重要任务卡 · 炮台配弹文本与三处弹种 chip（后缀"弹"→"**弹药**"）· 伤害克制矩阵三行改以**伤害系名**做功名、弹名进正文（顺带清掉散装旧称「爆炸弹」「等离子弹」）· 掠袭破片炮「所耗的爆炸弹」→「所耗的爆破弹药」· 测试 fixture 名。**④契约**：陈旧术语契约新增 3 组黑名单 `动能弹(?!药)` · `爆破导弹` · `等离子弹`——⚠ `动能弹` **必须带 `(?!药)`**（它是新名的前缀，裸写会把新名一起判红；反证已验证）。**⑤验证**：typecheck 四包 0 错 · core **156 文件 / 1,684 例** · `content:check` ✅（146 文件无旧称 · 黑名单 16 组）· **反证**（物品名退回"动能弹" ⇒ 只报该处 1 条、全仓"动能弹药"未误报）· 双 build ✅；**产物实证**：网页包旧名 0、新名齐备；桌面包残留只在源码注释里。**【已合入 main · 未推送】**
- 2026-09-16：**清掉玩家可见文案里的施工期黑话（高级箱 / 开箱 / 奖池 / 抽奖）＋ 陈旧术语契约收口（船长 · 三号 · verify）**——**①船长原话（照抄）**：「**游戏内依旧有高级箱，开箱等不符合游戏的名词使用，进行检查。**」→ 追问后定性：「**我并没有设定是游戏内文案，将不符合的文案全部修改**」；两问另裁：**开箱族现行文案全清** · **已发布公告一起改**。**②§5.2 冲突先摆清**：**「高级箱」是船长自己 2026-09-10 定的名字**（词典当时"预留口子"、工业页 2026-09-14 选甲写进档名）⇒ 本轮按"**施工期工作名**"作废；而**「开箱」是补执行**词典 2026-09-14 已定的口径（「开箱那一步一律写『货柜拆解』，玩家可见文案里不出现内部简称」）——当时只改了引擎错误文案，散在各处的日志/手册/悬停漏了。**③词表**：高级箱 → 档名 **稀有残骸** / 叙述 **额外战利品**；开箱（货柜）→ **拆解**、（残骸）→ **解体/回收**；开出 → 产出/得到/缴获；奖池 → 产出；抽奖券 → **旧式配给券**；回收箱 → 残骸。**④落码 19 处玩家可见**：工业页二级筛选标签「稀有 · 高级箱」→「**稀有残骸**」＋ 徽标悬停 ＋ 卡面说明 · 星图/远征悬停 · 一次性图纸提示 4 处 · 手册「在哪拆」· 回收日志 4 条 · **稀有残骸物品说明** · 派系/赏金日志 · 随机事件 · 通讯正文 · **已发布公告 4 处**；用例同步 4 处断言（不改就是红的）。**⑤契约（顺手修它自己三处毛病）**：新增 5 组黑名单（`高级箱` / `开箱` / `奖池|卡池` / `抽奖|抽卡|盲盒|礼包` / `开出(后接战利品名词)`）；① `announcements.ts` 原先**整份豁免** ⇒ 新公告写旧词也拦不住，改「**按文件 + 按词**豁免」；② 豁免比对用"正则源码全等" ⇒ 词组豁免失效（当场被 `announcements.ts:54` 的"撤离战"报出来才发现），改 `exemptRe.test(termRe.source)`；③ 裸 `/开出/` 误伤「协会…**开出**了长期悬赏」（正当用法）⇒ 收窄成"后接战利品名词才算"。**⑥验证**：存量 **0 命中**（146 文件）· **反证**（往已发布公告塞回"开箱" ⇒ 报红，且**同文件"撤离战"仍被正确豁免**）· 四闸门 + 双 build 全绿 · 产物实证（网页包旧词 0）。**⑦保留（登记）**：内部注释与内部标识符（`rareBoxesOpened` / `rollRareBoxExtra` 等）保留旧简称——不是文案、契约也扫不到；将来抄进界面会被拦。**【已合入 main · 未推送】**
- 2026-09-16：**奢侈品（贵重品）货柜占格改 2×2 = 4 格（船长 · 三号 · verify）**——**①船长原话（照抄）**：「**奢侈品货柜调整为2*2**」；**释义核对**：船长 2026-09-15 也用"奢侈品货柜"指 `box-valuables`（数据名"**贵重品货柜**"）⇒ 本条 = 该箱**货仓占格 2×1 = 2 格 → 2×2 = 4 格**。**②落值（两处必须同改）**：`core/wormholeHold` 形状表 `box-valuables` 2×1 → **2×2**（与军用备货柜同形）；`data/items.ts` 体积 **1000 → 2000 m³**（= 500 m³/格 × 4 格，与 AI 核心 / 图纸柜 / 军用柜 / 安全柜同一把尺）。**③明确不动**：箱价 **382.8 万**（= 内容期望 1,531.25 万 ×0.25）· 遗迹掉落占比 50% · 拆解产出 5~30 件十款等权 · 描述文案 · 市场侧。**④老档零迁移**：placement 自带 `w`/`h`、读档**不按形状表重算** ⇒ 改规格前已装进仓的那件仍是 2×1（不凭空超载），只有新装舱才按 2×2；用例照抄 2026-09-15「安全货柜 4→6 格」那条行为钉子。**⑤顺手补的空白**：`content:check` 此前**没给这两个 2026-09-15 新增货柜设"形状 ↔ 体积"哨**（安全柜/图纸柜/AI 核心/谜质都有）⇒ 而**形状在 core、体积在 data**，只改一处**四道闸门都不会报错**；本批补「**贵重品 / 军用货柜契约**」（kind / 形状 2×2 / 体积 = 500×w×h / 形状表登记）。**⑥验证**：typecheck 0 错 · core 156 文件 1,684 例 · `content:check` ✅ · **负向验证两处各自精准**（形状退回 ⇒ 报"形状 = 2×1，应为 2×2"；体积退回 ⇒ 报"体积 = 1000 m³，应为 2000"）· 双 build ✅；产物实证（桌面/网页包 `unitM3: 2e3` ＋ 形状常量 2×2）。**【已合入 main · 未推送】**
- 2026-09-16：**虫洞 C 族三卡改编成 ＋ 15 卡火力密度普查（船长 · 三号 · verify）**——**①船长原话（照抄）**：「**C族，浅层改为3只星髓成虫，中层为孢群异虫 ×1+星髓成虫 ×2，深层为噬口巨兽+2星髓成虫**」＋四答「**1，卡名更改，2，不做变化。3乙，4你进行落码**」＋（本人查错后）「**对虫洞内所有卡进行校准。检查下DPS和血量比**」→ 收口「**C族先折中吧，其他保持不变**」。**②落值**：浅 `wh-alien-swarm` **星髓游猎群**（星髓成虫 ×3 · `dmgMul` **1.40**）· 中 `wh-alien-brood` **孢群兵潮**（孢群异虫 ×1 ＋ 星髓成虫 ×2 · **1.15**）· 深 `wh-alien-hive` **噬口深巢**（噬口巨兽 ×1 ＋ 星髓成虫 ×2 · **0.94**）；**不动**：族定选靶 `smallest` 0.4 · 混伤 8:2 · 威胁/层档 · 隐蔽与零赏金 · 另 12 张卡（船长"其他保持不变"）。**③连带**：孢群异虫**（连同孢群机 ×3 · 池 264 血）由深档挪到中档** ⇒ **层 2~3 首次出现敌机**；畸变幼虫从三张洞内卡**全部退场**（仍在悬赏线：裂谷畸变体 / 噬口猎杀令 ⇒ 不成孤儿）；⚠ **孢群异虫没有受击增程**（`droneRangeMulOnHit` 只有 E 族三舰——本人先前那句"×4"**是错的**，已更正并写进注释）。**④普查（引擎真实口径）**：`wormholeAnomalyOf` 按卡归一**血量**的同时把 `scaleDmg = 层血预算 ÷ 卡自然总血` **同乘在单发上** ⇒ **卡的实际输出 ∝ 火力密度**（`ΣDPS ÷ 自然总血 × 多舰补偿`），**编成形状本身就是难度旋钮**；15 卡密度实测（浅 A 0.0467 / C 0.0615 / D 0.0185 / G 0.0673 / E 0.0540 · 层内极差 ×3.63；中 ×3.42；深 ×1.90）⇒ **密度与实测难度不成正比**（C 密度第二却 boss 5%：全族唯一"全员 `brawl` 贴脸 ＋ 快虫冲锋 ＋ 能量掷命中"把射程优势归零）。**⑤结论（决定了"不能只按密度校准"）**：两张卡的问题**不是火力**（`dmgMul` 修不了）——**A 深**（node/boss 双 100% · **敌开火 0** = 长臂白刷 ⇒ 期望交距/射程问题）· **E 深**（boss 0% 但敌开火 102~111 = 火力已过剩）⇒ 校准以**实测读数为准、密度只作结构诊断**；**全族 15 卡校准按船长"其他保持不变"挂账**。**⑥深卡（噬口深巢）后裁「保持不变」**：折中口径读数 **0%/0%**（超我提的目标带），根因 = **巨兽独占该卡归一血预算约 56%**、20 播种内打不穿 ⇒ **单靠 `dmgMul` 到不了 boss 带**（≈0.50 预估 node ≈85% / boss ≈10%；要 boss ≥20% 需 ≈0.30~0.35 或动几何/编成）；留档供日后省一轮摸底。**⑦验证**：typecheck 四包 0 错 · core **156 文件 / 1,683 例** · `content:check` ✅ · 双 build ✅；`tools/pd-vs-foe-drone.ts` 的 C 族卡位**同步改指中卡**（孢群机随孢群异虫挪档）；旧卡名在源码/工具/产物内 **0 处**。**【已合入 main · 未推送】**

## 封存卷索引（更早批次已移入 `docs/archive/`，点开即读）

| 卷 | 收录日期 | 条目 | 体量 |
|---|---|---|---|
| `docs/archive/roadmap-2026-09-04.md` | 2026-09-04 | 28 条 | 13 KB |
| `docs/archive/roadmap-2026-09-05.md` | 2026-09-05 | 12 条 | 12 KB |
| `docs/archive/roadmap-2026-09-06.md` | 2026-09-06 | 1 条 | 2 KB |
| `docs/archive/roadmap-2026-09-08.md` | 2026-09-08 | 18 条 | 15 KB |
| `docs/archive/roadmap-2026-09-09.md` | 2026-09-09 | 45 条 | 46 KB |
| `docs/archive/roadmap-2026-09-10.md` | 2026-09-10 | 71 条 | 169 KB |
| `docs/archive/roadmap-2026-09-11.md` | 2026-09-11 | 81 条 | 237 KB |
| `docs/archive/roadmap-2026-09-12.md` | 2026-09-12 | 76 条 | 173 KB |
| `docs/archive/roadmap-2026-09-13.md` | 2026-09-13 | 95 条 | 265 KB |
| `docs/archive/roadmap-2026-09-14.md` | 2026-09-14 | 124 条 | 397 KB |
| `docs/archive/roadmap-2026-09-15.md` | 2026-09-15 | 37 条 | 128 KB |
| `docs/archive/roadmap-2026-09-16.md` | 2026-09-16 | 28 条 | 73 KB |
| `docs/archive/roadmap-2026-09-17.md` | 2026-09-17 | 4 条 | 7 KB |

> 上面每卷都是**原文冻结件**：不重写、不摘要、不追加。`docs/archive/README.md` 另有"已办结待办单"卷。
