# 后续工作流备忘（Roadmap / Backlog）

> **2026-09-15 改版**：本文件只留三块 —— **① 待办活面**（置顶单 / 待办段 / 交接开放项）· **② 最近 20 条批次条目（滚动窗口）** · **③ 封存卷索引**。
> 更早的批次条目已**原文封存**进 `docs/archive/roadmap-YYYY-MM-DD.md`（一字未改，含船长原话照抄）——查老决策先看索引表、再 grep 对应卷。
> 封存动作 = `npm run docs:seal`（滚动窗口之外自动进卷，幂等；`--dry-run` 先看计划）· 封存区规则见 `docs/archive/README.md` · 全仓文档总索引 = `docs/INDEX.md`。

---

## 置顶：船长测试反馈改动单（2026-09-04 加入，按列序逐条推进）
**执行规则**：批次 A = 无歧义可直接实现；批次 B = 需先给设计总结等确认（大系统/结构级/规则数值）。
- **T1~T11 全部 [✓] 办结**（2026-09-15 逐条核对后原文移出）⇒ 全文见 `docs/archive/roadmap-todo-done-20260915.md`；本节留段头，新的船长反馈单另起一段。

## A. 体验走查 + 打磨（建议优先，收口"内容感"）
- [ ] A1 船长通读悬赏文案与界面观感（2026-09-05 注：需船长走查后按条关闭；2026-09-04 首轮已走查并清理过一批）
- [ ] A2 信息密度检查：星图（安全色 + ⚔悬赏徽标 + ′标称 + 名称是否拥挤）、悬赏列表排序/卡片密度、矿带卡布局细节
- [ ] A3 定版存档策略（2026-09-05 注：需船长确认正式档基线后关闭）

## B. 玩法闭环延伸（需确认流程的大方向）
- **本段 4 项全部 [✓] 办结**（2026-09-15 核对后原文移出）⇒ 见 `docs/archive/roadmap-todo-done-20260915.md`。

## C. 战斗与舰船系统余项
- [ ] **C3 V18 立项：EVE 式高/中/低槽重组**（2026-09 船长拍板"V17 收尾、高低中槽立项 V18"；
      **2026-09-04 交接：实施由大鲸鱼二号负责**，一号不再触碰；**2026-09-04 设计总结已确认**
      （docs/design/v18-slots.md：复数安装/数量制/槽位映射/Q4 布局草案 19 船表 + 高槽无人机
      家族 6 件——甲板扩展/战术导控，装备 48→54）——**V18A 实施已完成（2026-09-04，core 301 tests）**；
      **C4 校准轮（多炮/多矿枪/无人机件复核）与本里程碑联动，另行收尾**）：
      船体槽位布局按 role 族差异化（武装多高槽/装甲多低槽等）+ 模块归属高/中/低 +
      槽位数量/布局制（**炮尺寸制已随 V18 口径取消移除，无尺寸位**——变更记录见下）；
      fitted 结构迁移一次做干净（存档 17→18；旧六槽原位映射 high=[炮,矿] mid=[盾,推] low=[甲,货]）；
      V17 参数（乘入抗性/CPU 档位/推进代价）与槽制正交、届时零返工
      （见 docs/design/v17-modules.md §预留；v10b-combat-data.md 原"留战斗阶段"的旧账在本里程碑清偿；
      2026-09 设计问答已收：Q1 复数安装 / Q2-final 数量制不加尺寸位 / Q3 槽位映射 / Q4 布局草案）
- [ ] **C6 无人机 vs 炮位 DPS 比例评估**（2026-09-04 船长确认 V18 时追加：**延后**——武器体系
      全面后（V18B 炮族差异化 + 导弹架之后）再评估无人机流与炮击流的高槽伤害比例；
      无人机件数值（+15/35/70 m³、+12/25/40%）为暂定初值，届时可调）
      **2026-09-10 评估完成（二号；`docs/design/c6-drone-vs-gun-20260910.md` + 正式工具
      `tools/drone-vs-gun.ts`，`npm run battle:drone-vs-gun`）**：王鲭级满配同船对比——
      名义 DPS 无人机流 = 炮流 **81%**（每高槽名义反而 121%），但有效 DPS（命中修正）**48%**、
      每高槽有效 **73%**；成因 = ①命中链缺口（无人机固定命中 0.6，火控/索敌对无人机无效；
      炮台 ≈0.98）②支援链缺口（稳定器/射速/索敌只作用炮台，炮流有效 +33%）③CPU 双计
      （装置 + 每架，D3 占 310/320）④高槽不产生本体火力。实战：威胁 96 穹顶守卫
      纯炮 100%/76s/58% vs 无人机 D3 0%/74s（被击毁）；加目标锁定 MK3 后 D3 升到 60%。
      情景推演：单靠"导控 +60% + 无人机 CPU×0.65"有效仅到 68%、硬卡 40%——
      **须动命中链/支援链才能拉平**。
      **2026-09-10 第一轮调整已实施（船长拍板）**：无人机命中/衰减下放机型本体
      （`ItemDef.hitRate/falloff`）——**蜂鸟/赤鸢/猎鹰：命中 75% 且不随距离衰减**（falloff 1）；
      **雷鸥哨戒：命中 110%（用意 = 抵掉部分衰减）+ 保留正常衰减**（falloff 0.35）；
      combat 读本体（旧档兜底 0.6/0.35）、命中公式零改动；UI 无人机详情/手册新增「放飞命中」行；
      新增 `tests/drone-hit.test.ts`（6 项）；glossary 增词条「无人机命中与衰减」。
      **改后实测（王鲭级）**：有效 DPS@2km 48% → **70%**（D3 184 vs 炮流 263）、
      每高槽有效 −27% → **+5%**；实战 D3 反超炮流（穹顶 **100%/65s** vs 76s、坟场 27s vs 45s、
      噬口 41s vs 50s），但无人流行残血偏低（散火击杀节奏慢；装目标锁定 MK3 后 35%→57%）。
      **待船长判断是否回调**（旋钮：导控 +40%→+25% / 三型命中 0.75→0.70 / 无人机每架 CPU 上调）。
      **2026-09-10 船长裁定：先保持现状**（回调未实施，数值维持本行口径），由船长实机实测后再定。
      **2026-09-10 船长追加：无人机专用舰高槽 −2**（梭鱼级 5→**3**、王鲭级 6→**4**；其余带机巢
      舰船不动）——`ships.ts` 两行 + 旧档由 repair 链尾件退库（不丢装备、清单保留）+ 新增
      `tests/drone-ship-slots.test.ts` 钉布局与旧档兼容；校准行 D1/D2 改三槽、新增 D2b
      （槽位吃紧后单件导控升 MK3）、D3 四槽恰好占满；工具与测试存档同步。
      **复跑结论（重要）**：削槽对**相对关系是净利好无人机流**——王鲭炮流 6 门→4 门
      名义 272→**183**（−33%），而无人机流 D3 本体只占 4 槽、名义 220 **不变**；于是
      无人机流/炮流名义比 81%→**120%**、有效 DPS@2km 比 70%→**105%**；
      实战王鲭 4 槽对比：无人机 D3 穹顶 **65s** vs 纯炮 104s、坟场 27s vs 68s、噬口 41s vs 76s。
      梭鱼 3 槽：无人机 D2b 有效 107 vs 纯炮 3×MK3 120，实战仍更快（穹顶 85s vs 157s，但 0% 胜）。
      原因 = 无人机流火力来自机群（受舱容/CPU 约束，不吃槽位），炮流火力随槽位线性下降。
      **若目标是削弱无人机流，杠杆是 CPU / 舱容 / 战术导控，而非高槽数量**（详见设计稿 §九）。
- [x] **无人机四型定位 + 「无人机可被击落」**（2026-09-10 船长拍板，**本批已完成**）：
      **① 四型定位契约已确立（已实施）**——侦察机闪避最高装甲最薄 / 战斗机全属性平均 /
      攻坚机血量最厚·单发高·闪避最低 / 哨戒机射程最远·单发最高（狙击）·血量与侦察机相仿；
      现有四型按此调整（**蜂鸟闪避不变 0.45**；赤鸢 0.40→**0.25**、血 34→40；猎鹰 0.32→**0.10**、
      血 66→**97**、抗性装甲向；雷鸥 0.25→**0.18**、血 105→**23**、抗性削到单条）。
      契约**可执行化**：新增 `data/droneRoles.ts`（`DRONE_ROLE_SPECS` 四档区间 + 逐机/跨类阶梯校验），
      `content:check` 与 `tests/drone-roles.test.ts`（7 项）双重守卫——**新无人机越档即被拦下**；
      glossary 增词条「无人机四型定位」；设计稿 `docs/design/drone-roles-20260910.md`。
      注：`DroneDefense` 目前**不进战斗**（只有详情/图鉴/手册显示），故本批**不改胜率、校准矩阵不变**。
      **②「无人机可被击落」已实施（船长选乙：永久损失/耗材流）**：敌方**近防炮**——**威胁 ≥ 60**
      的敌舰才装，每舰**每 0.5 秒判定一次**，随机挑一架**正在攻击的放飞无人机**（**哨戒机不被打**），
      按 `pdAcc − 机型闪避` 掷命中、命中按 `pdDmg` 走机型三层抗性，打空即击落并停火；
      **不看距离**（"毕竟你要飞过去"）、**不参与**敌舰对玩家的常规攻击、单场最多击落 50%；
      **战后从无人机舱清单永久扣除**（主控远征胜/败/撤退 + 遭遇战 + AI 副船全接线），
      事件日志 + 在线 toast 提示，重复清剿在本场战损过半时自动停环提示补货；
      手册/物品说明/装配页删除"不消耗、不被击落"并改为近防炮口径；测试 `tests/drone-loss.test.ts`（5 项）；
      新增调参工具 `npm run battle:pd-tune`（支持 `--acc/--dmg/--cap/--period` 传参试档）、
      `drone-vs-gun` 表 3 增战损列。
      **实测（满技能 5 种子，王鲭 D3 满编；合法且符合流派的配装）**：软卡交火短（27~41s）战损近 0；
      硬卡穹顶（96）约 **3.2~3.6 架且全是攻坚机**（哨戒机免疫）；硬卡仍 **100% 胜**——
      因编队火力核心（6 架雷鸥）不受近防炮威胁，压力在"打得更久 + 要补货"。
      战损估值占该卡奖励约 1.1~1.2%。
      **数值已确认（2026-09-10 船长）**：「如果没有特意说明，默认按 0.5/5 来算」——
      未特意说明的敌舰一律 **0.5 秒/舰判定 · 命中 0.5 · 每发伤害 5**（逐舰独立、上限 50% 保留）。
      **默认档实测（王鲭 D3 满编）**：噬口(80)/坟场(88) **0 战损**；穹顶(96) **2.2 架（全为攻坚机）**、
      战损估值 1.1 万 ISK ≈ 该卡奖励 **0.7%**；三卡仍 100% 胜——因哨戒机免疫且编队火力核心是雷鸥，
      压力落在"打得更久 + 攻坚机要补货"。调参参照档（0.45/7 → 3.6 架；0.30/12 → 3.2 架；
      0.55/3 → 0 架）见设计稿 §七。
      **2026-09-10 船长追加**：**非哨戒无人机全部被摧毁后，近防炮转而攻击哨戒无人机**——
      哨戒机的"豁免"变成有条件的（靠同队其它机型当肉盾）；已实施 + 测试覆盖
      （`tests/drone-loss.test.ts` 新增"非哨戒机全灭后转火"用例），重档实测可见效果
      （acc 0.5 / dmg 10：噬口与穹顶在攻坚机全灭后哨戒机开始被击落）。
      **2026-09-10 船长追加（无人机舰专属加成）**：无人机舰的 `powerBonus` 只喂炮台、对机群无效
      （王鲭 0.6 与长尾鲨同级却吃不到机群）→ **削火力 + 加专属**：新增船体字段
      `ShipDef.droneDmgBonus`（只乘入放飞无人机单发，与战术导控/作战学乘算）；**王鲭 powerBonus
      0.6→0.3 且 +12%**、**梭鱼 0.45→0.25 且 +8%**。实测：王鲭 D3 名义 220→**245**、
      同船炮流 183→**149**（−19%，实战 104s→120s）；梭鱼 D2b 132→142、炮流 126→109；
      近防炮战损 2.2→**1.4 架**（机群更强→交火更短）。测试 `tests/drone-ship-bonus.test.ts`。
      **2026-09-10 船长追加（机舱联动）**：**卸下「无人机甲板扩展」后，超出机舱的无人机也要卸下**——
      新增 `trimDroneLoadToBay()`（按清单逆序卸下、退回仓库、写日志 + 一次性提示），
      接在 `unfitAt`（UI 传 ctx）与 `repairDeprecatedModules`（旧档槽位对齐裁掉甲板件）之后；
      测试 `tests/drone-bay-trim.test.ts`（5 项）；手册「装配」条目补口径。
      **2026-09-10 船长追加（损坏与回收 + 无人机线技能）**：①**取消单场 50% 击落上限**——
      战斗内机群可 **100% 损坏**；②**战后回收**：损坏机体按 `10% + 8%/级（回收学）`、满级 **50%**
      **四舍五入**找回并**回无人机舱清单**，未回收部分才永久损失（日志/toast 写明 损坏/回收/净损）；
      ③新增**五条无人机线技能**（战斗组）：无人机打击学 rank4 单发 +4%/级、无人机耐久学 rank2
      三层血 +4%/级（满级 **+20%**）、**无人机强化学 rank4 三层血再 +6%/级（满级再 +30%，与耐久学乘算，双满 ×1.56）**、
      无人机回收学 rank3 回收率 +8%/级、无人机规避学 rank4 闪避 +2%/级（满级 +10%）；
      数值常量集中在 `combat.ts` 的 `DRONE_SKILL`。测试 `tests/drone-skills.test.ts`（6 项）+
      `drone-loss.test.ts` 改口径；词典增词条「无人机线技能」。**实测**：默认近防炮档不变
      （软卡 0、穹顶 1.4 架）；重档（0.5/10、0.5/20）可见**整队 10/10 损坏**且胜率掉到 0~40%——
      取消上限后的真实风险与"耐久/强化/回收"技能的价值都在这里体现。
      **2026-09-10 船长二次修订**：耐久学**拆成两条**（rank2 基础档 + rank4 强化学进阶档，
      **乘算**叠加）；**其中一条满级改为 20%**——现行 = **rank2 耐久学 +20%（+4%/级）/
      rank4 强化学 +30%（+6%/级），双满 ×1.56**（越难练收益越高）；**规避学满级由 +20% 降为 +10%**（+2%/级）。
      **2026-09-10 玩家反馈修复（悬赏奖金口径）**：反馈"卡片赏金与实际到账不一样"——根因
      ① 结算带 ±15% 浮动（`rewardJitter`）而卡片展示名义值；② **赏金任务（窝点）卡漏乘
      "赏金猎手学"系数**。船长按甲案拍板：**取消奖金浮动**（rewardJitter 0.15→0）——展示=到账，
      唯一随机奖励 = 情报彩蛋 +10%（触发即写日志）；并修掉赏金任务卡展示漏项。
      测试 `tests/bounty-reward-display.test.ts`（4 项）；词典增词条「悬赏奖金口径（展示=到账）」。
      **口径修正（自检）**：早期工具给王鲭配了 3 中+3 低支援件（超出 2/2 布局）导致结论失真，
      已修正并统一为合法配装。
      表现层（击落演出/战报行）待三号对接，引擎侧字段契约见设计稿 §八。
- [ ] **C5 炮族差异化 + 导弹架**（2026-09 船长审核批注：同 MK 三系炮"只差弹药、无性格差异"——暂定稿可玩，**与导弹版统一做**；
      **2026-09-04 船长重定向：按伤害类型设计真武器形态（质量炮=动能 / 导弹=爆炸 / 激光=能量），
      旧三系同参炮 = 临时填充数据，逐批替换——V18B-1 导弹架 ✅ 已完成**（docs/design/v18b-missiles.md：
      追踪命中不随距离衰减的远程爆破 + 近盲安全射距；原高爆炮 1:1 迁移；爆破导弹弹药归位）；
      **V18B-2 激光炮 ✅ 已完成**（docs/design/v18b2-lasers.md：必中光束 + 距离衰减作用于威力
      （幅度减半 50%）+ 消耗能量弹药；能量炮/异星原型 1:1 迁移激光；per-gun 多键预载完成
      ——混装异型不再饿死；core 345 tests）：
      **V18B-3 ✅（2026-09-05 船长确认完成）** = 动能炮 → 质量炮形态（届时确认是否改名），并联动 V18 槽制
      （炮尺寸制已随口径取消移除，见变更记录）；
      v17b-weapons.md §已知限制已记录

## D. 经济/数值精调
- [ ] D1 balance（2026-09-05 甲案已做：体检工具扩"总资产口径"——钱包+舰队/已装模块+仓库/书架按市场常驻 base 折算，重跑 24h 三策略：钱包/总资产 A 934k/2067k、B 410k/1589k、C 312k/1499k；B/C 钱包低估约 1.7~2.2×，总资产差收窄至 A 的 73~77%，缺口主要来自技能训练时间与蓝图书沉没而非倒挂。是否需数值拉平待船长定目标）
  **2026-09-08 大改批后重跑（平衡-check，24h 30s 步长，同口径）**：钱包/总资产 A 原矿流 1,127k/**2,278k**（鲸吞级）、B 精炼流 577k/**801k**（掘洞级）、C 装备流 607k/**844k**（掘洞级；造 MK1/MK2）；B/C 总资产仅 A 的 **35~37%**（旧 73~77% 反转收窄）；双修贸易变体 24h 内技能未练成、与基础线同值。**状态（2026-09-08 船长定）：后续定位/拉平挂起——船长将调整技能体系（技能直接影响收益），待技能改完重跑再定目标；本行 09-08 数字保留为改前基线**。
  **2026-09-09 技能/工业大改后复跑（O3；工具修正后）**：总资产 A 原矿流 **2,293k**、B 精炼流 **2,338k**、C 装备流 **2,663k**（三线均鲸吞级；钱包 1,128/1,150/1,433k）——B/C 从 A 的 35~37% → **102%/116%**（技能投入有回报、装备流最优，A 原矿流与基线持平 +0.7% 未受损）；双修贸易变体仍 24h 内练不成、与基础线同值。**工具修正（balance-check.ts）**：船分级后沙猫/掘洞同 T1，原"目标 tier > 当前 tier"升级门槛卡死 12 万换掘洞（A 流伪影性卡在 89 万买不到鲸吞、总资产仅 91 万）——改按升级梯子内位置比较后恢复真实路径（三线均按 沙猫→掘洞→鲸吞 正常滚雪球）；里程碑/每小时曲线干净。**待船长定目标带**（B/C 已反超 A 是否到位，或需微调）。

## E. 基础设施
- [ ] E2 云存档 / 在线化：架构预留（决策 1/2/5），未启动；Phase 2/3 见 architecture.md

## 2026-09-08 一号交接开放项（来自临时交接卡 docs/design/handoff-2026-09-08-current.md，已按约定迁移后删除）
- [ ] **O1 组装机卡「手动 vs AI」两档净/h**：现为单一手动口径（二号 A 项）；手动/AI 基础核心档分列估算待船长拍板。
- [ ] **O2 奇货卖出侧溢价巡游**：挂价高于收购价线的奇货卖单仍可能被巡游采购按挂价成交（买入侧已 20L 收口，卖出侧未动）；处理前先问口径。
- [ ] **O3 balance-check 24h 策略复跑**：工业扩容（工位数大增）后未复跑；范围先问船长。
- [ ] **O4 顶部 AI 徽标计数与「执行中 N」偏差**：早期玩家反馈，根因未实证。
- [ ] **O6 二号船分级批次验收（已在 main）**：舰船尺寸分级 + 掠食者巡洋线 A1–A9；公告待审稿在 6b79a61（id 2026-09-09-ship-size-tier）未经船长批准不得写入 announcements.ts；巡洋 T3 数值/渠道声望待船长终审（涉二号分工，一号只读 ship-size-tier-rework.md）。
- 说明：O5 数值文档分层 = 现数值以 docs/design/manufacture-refine-worker-econ.md 与 docs/glossary.md 为准（refine-balance-50.md 为历史锚）；O7 真档验收状态 = 2026-09-08 引擎级实测探针 20/20 通过（临时探针已删），UI 目测项待船长开游戏确认（市场蓝图 chip/奇货低挂提示/AI 指挥中心 +N 工业/交付航线按钮与弹窗/工业页数值行/公告三条）。

## 2026-09-09 一号收尾开放项（收尾盘点后登记，待办照旧先读本段）
- 备忘（2026-09-09 自交接卡迁移）：**精炼净收益差异问题 = 船长延后**——差异来源 = 吞吐桶 ×8.7 × 矿价 ×37.7 ≈ 卖原矿差 328×，精炼净再 ×净率差；重开先回看 refine-cycles.md 头注与 content-csv 工作簿 items sheet。
- [ ] **P0-2「开战距离 / 先手」提案 + 预演（船长 2026-09-11 选定本批 =「先出提案 + 预演，审核后再落码」；二号）**——**本批零引擎、零数值改动，产出 = 提案稿 `docs/design/p0-2-engagement-20260911.md`（含全部实测读数）**。**①三条决定性事实**：(a)「够不着」= **距离拔河的胜负**——同一张蜃影、同一把 4×动能MK2，**带推进器 A0 = 100%/65s 闭合到 3.3km**，**仅去掉推进器 = 0%/140s、全程停在 9.99km、我开火 0.0**；(b) **教学卡「敌人一炮未放」与开战距离无关**——开局从 6314m 缩到敌人带内（1980/1500m）敌人**仍只放 0.2 炮、战斗缩到 1s**，敌人血抬到 **×12（264）** 也**依然 0 炮**（根因 = 交战距离停在玩家期望位 3.2km 而敌人带只有 2.2km ＋ 玩家先手一轮清场）；(c) **全局改法「开战距离 = 双方射程重叠区上沿」不成立**——全卡清点 **78 格中 77 格**被改动（含旧路径 20 张）⇒ 打穿「旧路径逐字不变」判据。**②病灶 A 实测（kite 卡，5 播种 1/7/13/29/51）**：蜃影 T48 的 A1/⑤/③ 三行现状 **0%｜140s｜我 0.0｜敌 136**；候选甲（可及性钳制：敌期望交距 ≤ 我射程×0.95）⇒ **20%/100%/80%**、我开火 28~40 次，但**连带把 A0 行从残 32% 拉到 58%**（= 船长刚裁定「保留」的那一行）；灰霾 T28 三行 **0%/600s ⚠触顶 5/5｜敌 584** ⇒ 甲后 **100%/36~59s**；赤潮 T34 三行 **0%/373s｜敌 364** ⇒ 甲后 **100%/41~64s**；**旧路径对照卡「幽灵舰信号」四行全 100%**（从未够不着 ⇒ 差异只来自舰级路径按"自身带×0.85"算站位）。**③候选四选一（甲乙丙丁）**：甲 全局可及性钳制 / **乙 逐卡覆写（复用既有 `rangeMinM/rangeMaxM` 旋钮，不新增字段；建议先做灰霾+赤潮、蜃影单独议）** / 丙 只做可读性（提前脱战 + 战报写明"够不着" + 卡面预告交距）/ 丁 不动；戊（全局改开战距离）已实测淘汰。**④顺带发现（建议先修）**：`battle-calibrate` 的**敌开火(头目/杂鱼)**列用 `b.fx.slice(seen)` 计数，而 `fx` 是 **48 条环**（`pushBattleFx` 超长丢最旧）⇒ **环满即停、长战全漏**：同格真值 **584.0**（头 145/杂 438）↔ 工具法 **48.0**（漏 92%）、蜃影 A1 136 ↔ 48、蜃影 A0 54.0 ↔ 43.2（= 蜃影稿 §2.1 记的 10.8/32.4 正是此口径）；**我开火列不受影响**（工具用 `stats.meShots` 权威累计）；修法 = 按 `BattleFx.seq` 续读（一行级）。**⑤四问待裁**：病灶 A 走哪条路 / 若走乙点名哪几张卡 / 要不要一并修工具漏计 / 教学卡「一炮未放」是否单列。**⑥验证**：本批**不改代码**，故只跑读数工具（`npx tsx _probe-p02*.ts`，两个探针**用完即删**，`_` 0 个）；提案落码时再走四连 + 重跑 A0~A3 与六组。**改动全在 d2 工作树，未合入 main、未推送。****【提案 · 未落码 · 待船长裁决】**
- [ ] **★★ 挂账①「低威胁卡敌人一炮未放」+ ②「名义火力 / 实际火力脱节」（2026-09-11 船长要求处理；二号）**——**①实测形态**：边境/碎晶（威胁 12/20）在 A0/A3 参考行**敌人全程 0 开火**（我开火 11~12 次、18~25 秒清场）——根因 = 敌军自带带太短（快艇 1~1883、护卫舰 326~4275），而玩家参考行站 3.2 km；**候选**：甲 逐卡给敌军**加宽射程带**（让其进入自己的交战带）· 乙 逐卡**开战距离覆写**（把开局放进敌军带内，实测 1500~1980m 时敌人会开火 1.0~4.2 次）· 丙 给低威胁卡**先手**（开场即开火）。**②实测形态**：纸面单发与实际输出脱节（例：赤潮头目 beam 必中单发 21 ↔ 灰霾头目 fixed 单点 59，两者实收相近）；**候选**：甲 单发按「名义 DPS × 装填 × 实收命中」**反推**（纸面 = 实收，须重标全部 26 卡）· 乙 只做**登记口径**（在词典写明"纸面 ≠ 实收"，不重标）。**两份提案待船长选型后再落码。****【提案 · 未落码】**

## 2026-09-16 二号交接开放项（虫洞战利品扩充批的尾巴 · 已推送 · 公告待一号发布）

- [ ] **W1 虫洞战利品扩充 · 公告待发（船长 2026-09-15：「公告我让一号发」⇒ 经办 = 一号）**：
  待审稿 = `docs/design/announcement-draft-20260915-wormhole-loot.md`（4 条要点已成稿 · 第三人称 · 无开发话语 ·
  口径来源照抄船长七段原话）；批准后写入 `packages/data/src/announcements.ts`
  （`ann-wormhole-loot-20260915` ·「虫洞战利品扩充」· 2026-09-15 · 内容更新）并推送。
  **要落进文案的数值（口径权威 = 词典八之二/八之三）**：谜质 **1 台 = 1 枚** · 70,000/枚 · 只收不卖 · **奇货档**；
  **奢侈品三档 10 万 / 40 万 / 200 万**（可买可卖）；贵重品货柜 2 格 ⇒ 奢侈品 **5~30 件** · 箱价 **365 万**（= 期望 1,458 万 ×**0.25**）；
  军用备货柜 4 格 ⇒ MK3 **1~3 件**（含武器、不含专属）· 箱价 280 万（= 期望 ×0.6）；
  **遗迹出货固定 70%**（层 2 起）· 池 10 种 ⇒ **贵重品柜 50%**；残骸堆 **0.75%/堆**（单次上限 1）；安全货柜 3000 m³ / 6 格 / 价 ×1.5。
- [ ] **W2 验收用测试存档未配（船长点单再做）**：`wh-loot` 一档——仓库预置 2× 贵重品柜 + 2× 军用柜 + 1× 安全柜（可立刻拆解），
  洞内一趟带 2 台谜质装置并站在残骸堆旁（一撤离即可看谜质折算与结算单新格）。配在 `tools/make-test-save.ts` + `docs/test-saves/`。

## 最近批次（滚动窗口 · 最新 20 条）

> 越窗即封存（`npm run docs:seal`）；窗口大小 = 20 条，需要临时调大用 `--window=N`。

- 2026-09-16：**虫洞「扫描扫出下一层入口」（船长裁定甲案 · 一号 · main）**——**①船长原话（照抄）**：「**发现一个问题，玩家扫描无法直接扫出下一层入口**」⇒（摆出 §5.2 冲突与甲/乙/丙三案后）裁定「**甲**」＝**扫到即标**。**②取证（探针 8 盘实测 · 跑完即删）**：扫描**本来就**把出口格记进 `scanned`（`gridScanTargets` 从不排除它）、**扫完前往也不再要「未知地点」确认**；但出口格不参与信号分配 ⇒ `place` 恒为 `empty` ⇒ `revealOf` 只给 `signal(null)`，界面 `isExit` 判据是 `visited || exitKnown` ⇒ **扫过也画成「没有信号：空信息地点」**（与真·空地无法区分）——玩家扫到了入口位置却认不出来，只能撞上去或读信标。顺带查实一处**失实注释**（`wormhole.ts` 旧注「出口格**永远不在 `scanned` 里**」，被实测证伪）。**③冲突与旧口径作废**：旧口径 = 「入口标记的条件从"到达过"放宽为**到达过 or 被信标标出**」（设计稿 `wormhole-extraction-endgame-20260912.md:579`，2026-09-13）＋ 用例注释「默认为假：入口不标在地图上」＋ 工具口径「不偷看 `grid.exit`，出口只由信标给出」——甲案后**入口多一条来源**（扫描），信标降为**迄今唯一的远程途径**（不废）。**④落码（单一收口）**：新增 `wormholeGrid.markExitKnown`（置 `grid.exitKnown` ＋ 把入口格并入 `scanned`），**信标与扫描共用它**；`wormholeGridScan` 比格键判"这一扫有没有扫到出口格" ⇒ 命中即标，并在**日志**补一句「扫到下一层入口（Q? · R?，已标在地图上）」；返回值新增 `exitScanned`（core `CommandResult` ＋ 界面 engine 各一处）；界面**扫描提示**补一句同义提示（与星云提示并列，不改原有四句）；**UI 地图判据一行未改**（`isExit` 早已读 `exitKnown`）。工具侧：`wormhole-econ` 的"不许偷看"口径与 trace 标签（`信标已读/未读` → `入口已标/未标`）同步、`make-test-save` 备注同步；**机制不需改**（工具调的就是真 `wormholeGridScan`，自动跟随）。**⑤闸门**：typecheck 四包 0 错 · core **158 文件 / 1,728 例** · `content:check` ✅ · `ui:rot-check` ✅（UI 文件改了扫描提示）。**⑥负向验证**：临时把新判定改成 `exitScanned && false` ⇒ 新用例精准报「扫到出口格 ⇒ 入口已知: expected false to be true」，复原即绿。**⑦读数（探索模拟 300 盘：能扫就扫、扫不动就走向最近未扫描格）**：**57.3%** 的盘能在回合耗尽前靠扫描标出入口（中位**第 5 回合**、四分位 3/7）、**42.7%** 仍要靠信标或撞上去；同一批样本里玩家**真的走到过出口格**的只有 37/300 盘 ⇒ **信标仍有价值**（唯一"不靠近就知道"的途径）。**⑧存档/文案**：`exitKnown`/`scanned` 本就是随档字段 ⇒ **零迁移**；玩家可见新增文案只有扫描提示与扫描日志两句。**【已推送 origin/main】**
- 2026-09-16：**报障「从谜质中获得了图纸货柜」⇒ 取证为误认（玩家澄清看错）· 按船长指示本条口径不固化（船长报障 · 一号 · main）**——**①船长原话（照抄）**：「**有玩家反应，从谜质中获得了图纸货柜**」＋追答「**好像是谜质的虫洞地点**」⇒ 结案「**玩家澄清，是看错了**」⇒ **船长追加指示（2026-09-16）：「这条口径不用固化」**（故不落常驻用例，只留本条目当报障记录）。**②结论（先行动作）**：**现行代码里谜质格发不出任何 `box-*`**——谜质格的动作只有"取回谜质"一条（`wormholeMatterDeviceAt` 从 23 台 `mat-*` 抽取池按 (种子, 层, 格) 定死一台）；`box-bp-*`（图纸货柜）的正规来源只有两条、都在别的地点：**遗迹首次打捞**（层 2 起 70%，命中后按全货柜池 10 种抽：贵重品柜 50% + 其余 9 种各 ≈5.6%）与**墓场残骸堆**（每收走一堆 0.75%、单次上限 1 个、四类货柜类等权）。**③取证（走真 API + 入库快照差分 · 一次性探针跑完即删）**：40 种子 × 层 1~5 扫 **420 个谜质格**——激活 **420/420** 成功且发出条目**全是 `mat-*`**（23 台装置全覆盖），同格上打捞/采集/逐堆拾取 **420/420 全被拒**、不扣回合、不动仓库；**阳性对照**：同一套差分打在 **423 个遗迹格**上抓到 **10 种 `box-*`**（贵重品柜 134 · 安全柜五族 28/24/24/19/13 · 三档图纸柜 17/13/12 · 军用柜 15）＋ AI 核心 52 枚 ⇒ 证明"差分抓得住货柜"，故"谜质格没抓到"是结论、不是工具失灵。**④落码：无**（玩家可见文案 / 数值 / 存档零改动；临时探针按工具纪律删除，工作文档按归档三步并入本条后删除）。**⑤闸门**：typecheck 四包 0 错 · core **158 文件 / 1,724 例** · `content:check` ✅（仅既有预警）。**⑥没堵死的一条口子（登记备查·正常流程到不了）**：存档清洗（`save.ts` 的 `cleanWormholeGrid`）逐格搬运 `piles` 时**不校验它与该格 `place` 是否相称** ⇒ 手改档 / 极老档里"谜质格上躺着一件货柜"理论上是可加载的。**⑦留给船长的可点单（未做）**：结算单的「货柜 N 件」不标来源（来源只在日志「遗迹深处发现…」/「残骸堆里翻出…」里）——若这类误认还要减少，可给那一格加来源提示。**【已推送 origin/main】**
- 2026-09-16：**虫洞「路径拦截」：直线路径上有敌人 ⇒ 移动被拦下并开战（船长 · 一号 · main）**——**①船长原话（照抄）**：「**虫洞内，当玩家点击某个位置准备移动过去时，假设直线路径上有敌人，能否新增一个敌人拦截机制（提升玩家有路径上有敌人阻拦，直接前往会触发战斗）**」（原文"提升玩家"按上下文读作"**提示**玩家"）。**②现状（动手前查的）**：移动是**点到点**（`wormholeGridTravel` 直接把 `pos` 设成目标格、**不限距离、恒定 1 回合**，没有逐格行走）⇒「直线路径」= 起终点连线经过的中间格；且**只有"终点恰好是舰船信号"才开打**（`arrived.autoBattle`），**从敌人旁边穿过去完全无事**。**③六问六答**：①**未清掉的舰船信号格都拦（含未扫描格）** · ②**就地停下并开打、移动的 1 回合照扣、打赢后自己再点一次继续走** · ③**拦截战不额外扣回合** · ④**地图画路径 ＋ 被拦格描红 ＋ 弹确认框** · ⑤**只拦最近的一处** · ⑥**中间格不揭开**。**④§5.2 冲突上报与裁定**：新做法（未扫描格也描红）与 2026-09-13「**玩家只有到达目标地点后才能知道目标地点的确切信息**」那条、以及同批**已修掉的一处泄漏**（未扫描格不上信号色）冲突 ⇒ 船长裁定「**甲**」= **已知格描红指名、未扫描格只把路径线置警示色、不指名**（确认栏写「路径上**可能**有敌人阻拦」）⇒ 2026-09-13 那条修漏口径**继续有效**。**⑤落码**：`core/wormholeGrid` 新增 **`hexLine`**（六边形直线：含两端 · **双向同集**＝先按 `(q,r)` 字典序规范化方向再画 · 立方取整 ⇒ 平局确定、不消耗 rng）与 **`wormholePathInterceptAt`**（最近一处未清舰船信号格 · **起点终点不参与** · 出口格不拦）；`core/wormhole` 的 `wormholeGridTravel` 增 `confirmIntercept` 闸（未确认 ⇒ `code='path-blocked'`**且不扣回合**）＋`arrived.intercepted{target,known}`（`known` = **拦之前**是否已知，供甲案分流）＋**移动截断到拦截点**＋日志「🕳 途中被拦下（Q,R）…」；⚠ **三道闸顺序 = 未知地点 → 回合不足 → 拦截确认**（0 回合时先说"只能撤离"，不弹走不成的确认框）；`core/wormholeBattle` 的 `wormholeTravelTo` 透传并在拦截格**就地开战**（开战失败沿用**整趟回滚**：回合/位置/visited/scanned/activated/exitKnown 全还原）；界面新增 SVG 路径折线（`app-wh-path`，未扫描拦路者走 `is-blocked` 警示色）＋已知拦截格 `is-intercept` 描红＋两种确认文案；手册「回合与全损」补「途中被拦」。**⑥读数**：**几何咬合面**（`_` 探针跑完即删 · 200 盘 × 层 1/3/5/7 · 34,800 个"前往"组合）= **30.1% 会被拦**（层 1 **14.8%** → 层 5 33.2% → 层 7 **35.1%**；按距离：距 1 **0%**（无中间格）· 距 2 18.0% · 距 4 29.6% · **距 8 53.6%**）；**整趟 20 趟**（`wormhole:econ`）拦截 **0.25 次/趟**，⚠ **工具口径严重低估**——已查明该政策**68% 的走法是距离 1**（候选必须"已扫描"、扫描半径小 ⇒ 没有中间格可拦），故 `--charge-through` 开关与默认读数逐格相同，两点都已登记进工具头注；**看这机制的分量请认几何读数**（移动与距离无关 ⇒ 玩家实际会跨盘跳格）。**⑦闸门**：typecheck 四包 0 错 · core **154 文件 / 1,664 例**（新增 `wormhole-intercept.test.ts` **16 条**：连线逐对穷举/双向同集 · 只拦最近 · 未扫描也拦 · 已清不拦 · 起点终点与出口不拦 · 三道闸顺序 · 截断落点与回合 · `known` 语义 · 开战失败整趟回滚）· `content:check` ✅ · `ui:rot-check` ✅ · 桌面与网页 build ✅ · **Pages 逐字节核验**（`index-BAO8MI7u.js`）。**⑧交付物**：验收档 **`wh-intercept`**（层 3 · 三条线摆成三种形态：拦路者已知/未扫描/路径干净 · 带**三元约束搜索**避免现场塌成一种；生成后用 `_` 探针逐条自核后删除）＋ `docs/test-saves/README.md` 试法五步。**【已推送 origin/main】**
- 2026-09-16：**虫洞「踩中埋伏」：进未扫描地点踩到敌人 ⇒ 事件提醒 → 确认开战（船长 · 一号 · main）**——**①船长原话（照抄）**：「**添加一个新的提示，如果移动途中被敌方拦截或者进入未扫描地点踩到怪了，都要弹窗提示，玩家确认后进入战斗。**」＋当轮澄清「**这并不是新机制吧，触发形式应该是踩中雷后，调用事件提醒，之后触发战斗，并不是在战斗前提示。**」＋四答：**确认前只能选「开战」** · **路径拦截那条不改** · **已知敌格不弹** · 提醒**只写来由与坐标**。**②落法（不新造机制）**：原样复用仓库既有的「遗迹惊扰守备」那套（`run.pendingRuinsBattle` → 页内警告条 → 点确认才开战、标记没清时别的层内动作全被拦）——船长说的"调用事件提醒，之后触发战斗"正是这个形状。**③机制**：点了一个**出发前没扫描过**的地点、走到才发现里面是敌人 ⇒ **到达那一刻先发事件提醒、不直接开打**：`arrived.ambush` ＋ `run.pendingNodeBattle` 挂起 ＋ 日志「🕳 踩中埋伏（Q,R）…准备交火」＋ 警告条「⚠ **踩中埋伏**：这个地点（Q,R）出发前没有扫描过，里面是敌人——对方已经发现我们，打完才能继续探索」（**只写来由与坐标**）；玩家点「**开战**」⇒ `wormholeStartBattle('node')` ⇒ 真进战斗（成功即清标记）。**边界四条**：①**已知**的敌格是自己点上去的（明知故往）⇒ 照旧**到达即开打**；②**路径拦截**那条也照旧直接开打（移动前已确认过，不搞两次确认）；③`deferAmbush` **只有界面传** ⇒ 读数工具/老调用方/用例**零行为变化**；④确认期间**层内动作全被拦，但撤离照走**（逃生门与遗迹那条同口径；⚠ 这一条按"同级相似项"照抄，若船长要求连撤离也堵，一句话即改）。**④落码**：`core/wormhole.ts`（可选字段 `pendingNodeBattle` · `gridActionBlocked` 增一条同句法理由 · `arrived.ambush` 判据 = `autoBattle && !scanned && !intercept` · 踩雷日志）· `core/wormholeBattle.ts`（`opts.deferAmbush` ＋ 回执 `ambush`/`pendingBattle:'node'` · 开战成功清标记）· `core/engine.ts`＋`apps engine`（`CommandResult.ambush`、`pendingBattle` 联合类型加 `'node'`；界面是唯一传 `deferAmbush` 的调用方）· `Wormhole.tsx`（警告条复用 `.app-wh-extract-ask` 同族 ＋ 地点说明「舰船信号」改准）· `Handbook.tsx`（新增「踩中埋伏」）· 词典新术语登记「**踩中埋伏**」（并登记「路径拦截」）。**⑤闸门**：typecheck 四包 0 错（首跑抓到一处 `run` 可能为 null 的类型错）· core **156 文件 / 1,682 例**（新增 `wormhole-ambush.test.ts` **7 条**：踩雷先提醒不开打 · 点开战真开打且清标记 · 标记没清时层内动作全被拦而撤离照走 · 已知敌格直接开打 · 拦截直接开打 · 不传 `deferAmbush` ⇒ 原行为 · 老档缺字段零迁移）· `content:check` ✅ · `ui:rot-check` ✅ · 双构建 ✅ · Pages 逐字节核验（`index-mBYl3kvx.js` 含「踩中埋伏」等新文案）。**【已推送 origin/main】**
- 2026-09-16：**奢侈品扩到十款（船长 · 一号 · main）**——**①船长原话（照抄）**：「**添加更多奢侈品，让奢侈品有10个类型，分布在目前的3个奢侈品价格附近，并且市场内奢侈品的价格波动允许达到30%。**」＋三答：**波动那条「保持不变」** · 价格表取我给的表 · **十件等权**。**②现状核实（动手前）**：扩表前 3 款（星港陈酿 10 万 / 贵族香料 40 万 / 失落艺术品 200 万 · 各 2 m³ · `kind: 'luxury'` · 市场 common 常驻可买可卖 · 来源 = 贵重品货柜拆解 5~30 件）。**波动实测**（`_` 探针跑完即删 · 30 天游戏时间 · 每 10 分钟采一次 · 4,320 次/商品）：奢侈品与对照品**都能摆到 ±40%**（撞满 `pool.noise` 的 ±0.4 钳制）——星港陈酿 −40.0%~+39.5% · 贵族香料 −40.0%~+39.4% · 失落艺术品 −40.0%~+39.2% · 重型炮台 MK2 −40.0%~+40.0% ⇒ **船长要的"能达到 30%"本来就成立**，故**行情参数一字未动**（`noiseStep` / `noiseHalfLifeMs` / 价格上下限全未碰）。⚠ **探针自纠（如实登记）**：首版探针**忘推进 `state.gameMs`**（`advanceMarket` 只补到当前时刻）⇒ 读数全是 0.0%，据此得出的"奢侈品价格是死的"**结论作废**；修正后才有上表。**③十款（3 旧 + 7 新，围绕 10/40/200 万三簇）**：低带 **陈年雪茄 6 万** / 星港陈酿 10 万(旧) / **异域织物 14 万**；中带 **香木雕刻 25 万** / 贵族香料 40 万(旧) / **宫廷乐谱 60 万** / **古法香膏 80 万**；高带 **星图真迹 120 万** / 失落艺术品 200 万(旧) / **王冠遗钻 320 万**。**十款等权** ⇒ 均价 **87.5 万**（原 83.33 万）。**④连带重算（契约自动跟走）**：一箱内容期望 = 17.5 × 87.5 万 = **1,531.25 万** ⇒ **贵重品货柜箱价 365 → 382.8 万**（= 期望 ×0.25；物品卡与市场行两处同值）。**⑤落码**：`data/items.ts`（十款 ＋ 箱价；头注重写）· `data/marketCatalog.ts`（补 7 行 common）· `data/rarityTier.ts`（补 7 条档位 1，与"common ⇒ 必须 1"契约一致）· `core/wormholeSalvage.ts`（`WORMHOLE_LUXURY_ITEM_IDS` 3→10，**顺带改正两处过期注释**：旧的 2.4/4.8/9.6 万与 24,000/48,000/96,000 字样）· `tools/content-check.ts`（**物品总数哨 79 → 86**，附 +7 名单）· `tests/wormhole-unbox.test.ts`（「三档都抽得到」改为**按常量长度断言** ⇒ 以后再扩表自动跟着走）。**⑥闸门**：`content:check` ✅（读数列：**均价 875,000 ⇒ 一箱期望 15,312,500 · 箱价 3,828,000（= ×0.25）**；首跑被"物品总数应为 79，实际 86"拦下 ⇒ 按哨的要求显式登记后通过，正是该哨的用途）· typecheck 四包 0 错 · core **156 文件 / 1,683 例** · 双构建 ✅ · Pages 核验（`index-BT4bnGVZ.js` 含七件新品名）。**【已推送 origin/main】**
- 2026-09-16：**市场分类三条：货物改名 · 奢侈品子分类 · 货柜独立并挂四档子类（船长 · 一号 · main）**——**①船长原话（照抄）**：「**目前奢侈品有哪些？给市场的添加奢侈品分类，放入物品下，物品改名叫货物。**」＋「**将货柜添加到市场的分类里，和货物同级**」＋追答「**货柜要二级子分类**」·「**谜质先不管**」·「**3，先不改**」（导航页/手册命名先不改）。**②现状（动手前查的）**：一级类型改前 = 全部类型 / **物品** / 消耗品 / 残骸 / 高·中·低槽装备 / 舰船 / 蓝图 / 核心；「物品」下实际住了**7 个大类 37 条**（货柜 10 · 原矿 8 · 原材料 8 · 气体 4 · 奢侈品 3 · 冰矿 3 · 虫洞谜质 1），其中**只有原矿/原材料/气体/冰矿有子分类**，**货柜 10 条与虫洞谜质 1 条任何子分类都筛不到**。同类先例两处：「残骸」2026-09-08 独立 · 「消耗品」2026-09-11 独立 ⇒ 本批照此办理（**数据层零改动**，只改筛选口径）。**③落码（三次同类拆分）**：**A · 改名**：「物品」→「**货物**」（`MarketPage.KIND_TEXT.item`；**只是市场下拉的显示名**，导航页与手册图鉴走各自单点、**未随改**）＋「货物」子分类补一档「**奢侈品**」（`ITEM_SUBS`，与物品种类 `luxury` 同名）。**B · 货柜独立**：新增 **`CONTAINER_KIND_KEYS = ['container']`**（键集合单点）· `subPasses` 的 `item` 分支同时剔除消耗品与货柜 · `KIND_TEXT.container = '货柜'` · `KIND_OPTIONS` 在 `'item'` 之后插入 `'container'` · `kindPasses` 新增 container 分支。**C · 货柜子分类**（追答）：`CONTAINER_SUBS` 四档（**遗迹安全货柜 / 图纸货柜 / 贵重品货柜 / 军用备货柜**）＋ **`containerSubKeyOf(refId)` 按 id 规则派生、不另存清单**（五族 = `box-relic-<族字母>`，与 core `wormholeRelicBoxPoolOf` 同一把尺；三档图纸柜读 core `WORMHOLE_BP_BOX_IDS`；贵重品/军用读 core 两个货柜常量）⇒ 以后加一族/一档自动跟上。**④常驻护栏**：`content:check` 新增「**市场类型契约**」（扫 UI 源码非注释行）六条——①必须有 `container: '货柜'` ②下拉里「货柜」**紧跟「货物」** ③一级类型名不得变回「物品」 ④`CONTAINER_KIND_KEYS` 必须在 ⑤「奢侈品」子分类必须在 ⑥**货柜四档子类必须在**（四个中文名逐个核）；**负向验证两次各自精准**（改名一处即红、复原即绿）。**⑤实测读数**（`_` 探针直接打共用的 `subPasses`，跑完即删）：一级类型 全部 **460** / **货物 34**（原矿 8 · 原材料 8 · 气体 4 · 冰矿 3 · **奢侈品 10** · 虫洞谜质 1）/ **货柜 10**（子类 **5 / 3 / 1 / 1** ⇒ **10/10 全覆盖**）/ 消耗品 15 / 残骸 23；**同时落进「货物」与「货柜」的行 = 0**（互斥 ✓）。⚠ **探针自纠**：首版探针照抄 `kindPasses` 时漏了 `good.kind !== kind` 那道门 ⇒ 把 375 条非物品商品也算进「货物」（读数 412）——探针错、不是代码错。**⑥闸门**：typecheck 四包 0 错 · `content:check` ✅ · `ui:rot-check` ✅ · 双构建 ✅；推送后按**强标记**核线上产物（`key:"safe",label:"遗迹安全货柜"` 这类字面量结构，不用物品名里本来就有的词）。**⑦船长明确不做（登记备查）**：**虫洞谜质**（1 条）**先不管** ⇒ 仍是「货物」下唯一无子分类的一档；**导航页「物品」与手册「物品图鉴」命名先不改**（日后若统一，`App.tsx` 导航标签 + `Handbook.tsx` 分区名 + 教程靶点文本 `TutorialGuide.tsx` 必须**同批动**，否则教程那一步指不到东西）。**【已推送 origin/main】**
- 2026-09-16：**装备/物品文案审查落码（船长点名 → 甲案全改 · 三号 · verify）**——**①船长原话（照抄）**：「**审查装备和其他物品的文案，将比如（市场稀有）；（护盾与装甲都被打穿之后，最后那段血比别人厚出六成；这是全游戏唯一能加厚结构层的模块）；炮台装填间隔 ÷1.18（射速计算机 MK3 是 ÷1.12）；这类明显不适合让玩家看到或者重复数据的部分修改并列给我看**」→ 出清单后裁决「**行，按你推荐来**」。**②四类判据与落码（43 对替换，逐对断言"恰好命中一次"才写盘）**：**A 渠道/档位标签 32 处**（`（市场稀有）`×19 · `（市场稀有，无蓝图）`×2 · `（市场稀有现货，无蓝图）`×2 · `（市场专供）`×2 · `（市场专供，无蓝图）`×2 · `（市场奇货）`×1 · `（奇货现货，无蓝图）`×1 · `仅限奇货市场（需高声望）`×3 ⇒ 全删；**"能不能造"改中性句**——确实无蓝图的 4 件写「（**不可制造**）」）· **B 跨件对比 19 件**（改写为"只讲本件"，不再复述他件的数）· **C 绝对化 5 处**（巨构骨架"全游戏唯一…"／掠袭机库"本套最省"／巨构护盾矩阵"本套最贵"／护盾充能"唯一能把盾点起来"收敛语气）· **D 开发词 1 处**（贵重品货柜「拍卖行**口径**」→「拍卖行里的」）· **补批 6 处**（第二轮扫描补出的同类漏网：陵寝棱镜炮 · 巨构残骸炮 · 生体甲壳板 · 陵墓护盾阵列 ＋ **自查补漏两处**：隐秘行动装置 MK3「最重」· 巨构近防阵列「本套最快」）。**③顺带修 8 处事实错误**：6 处说明称"无蓝图"而数据**有蓝图**（重型激光炮 MK2 / 攻坚激光炮 MK3 / 重型导弹架 MK2 / 巡航导弹架 MK3 / 无人机甲板扩展 MK3 / 战术导控阵列 MK3）＋ 近防炮 MK1「唯一能打敌方机群」（实际带防空属性的有 4 件）＋ 虚空母矿「虫洞深处唯一能带回来的东西」。**④体检当场拦下一处**：流亡中继桅「省下 12 点 CPU」的 `12` 靠**跨件差值**被「产物说明契约」解释，删跨件句后立刻报"无法解释" ⇒ 改本件绝对值「CPU 只吃 34」⇒ 复跑 **无法解释 0 条**（说明引他件数字既是阅读问题、也是维护问题）。**⑤保留**：蜂鸟/猎鹰/构件哨戒的**四型定位**（`droneRoles.ts` 契约的玩家可见表达）· 「贴身缠斗最凶」等风味语 · 扫描器误报（本件自身值）。**⑥口径归档**：并入约定 **§十三**「道具说明四条禁写」（渠道标签 / 跨件对比 / 绝对化 / 开发词）＋ **审查口径**（含两处实测判据缺口：**部件名简写**漏检 · `最X` 词表不全）＋ **护栏缺口登记**（「文案纯净契约」不扫 `MODULES`/`ITEMS` 的 `description`，是否补扫描另议）。**⑦验证**：typecheck 四包 0 错 · core **153 文件 / 1646 用例** · `content:check` ✅（产物说明契约 273 条声明 / **无法解释 0 条**）· `ui:rot-check` ✅ · 双 build ✅；**产物核对 6 项**（新句「带防空属性的武器才筛得到敌方机群」「不可制造」在包内；旧句「（市场稀有」「全游戏唯一能加厚结构层」「射速计算机 MK3 是 ÷1.12」「拍卖行口径」全消失）。**【已验收 · 已合入 main · 未推送】**
- 2026-09-16：**电子舰 CPU +100（165 → 265 · 两艘 · 只动一个字段）**（船长：「**提高电子舰的CPU，提高100**」· 三号 · verify）——**①范围**：「电子舰」= 子分类 `subClass: '电子舰'`（全仓恰好 2 艘）——`sh-wh-a-frigate` 掠袭电子舰 · `sh-wh-d-frigate` 哨戒电子舰，`cpu` **165 → 265**；槽位 / 三层血 / 命中 / 回避 / 分辨率 / 锁定 / 速度 / 机动 / 货舱 / 价格与图纸**一律不动**（`--numstat` = 8 插入 / 2 删除）。**②实际含义（装配面）**：从"火控件与武器争 CPU、装不满"变成"武器 + 整套设备一起带满"——3× 攻坚炮台 MK3（52×3）＋ 隐秘行动装置 MK3（80）= **236** ⇒ 改前 165 装不下、现 265 余 29；哨戒 2 炮（104）＋ 索敌（40）＋ 陀螺（40）= 184 ⇒ 改前只能"两炮 + 一件支援"。**③用例 +3**（`wh-ship-baseline.test.ts` 新增「电子舰 CPU +100」：两艘 = 265 且全仓电子舰恰好 2 艘 · **只动 CPU** 的护栏（槽位/三层血逐项原值，防"顺手补偿"）· 装配面 236 ≤ 265 且 > 165）；**负向验证**：改回 165 ⇒ **2 例红**。**④近失如实登记**：复原负向验证时用 PowerShell 正则批量改回，**误伤虎鲨级**（同为 165、下一行同为 `droneBayM3`）——在 `git diff` 逐行核对时抓到（8/2 → 9/3），已逐行改回并三重复验（全仓 `cpu: 265` 只剩两艘电子舰 · 虎鲨 = 165）⇒ 教训：批量/正则替换必须改完立刻复核命中集合。**⑤无契约冲突**：舰船 CPU 在体检里只断言"正整数"；舰价/图纸价由料价与档位系数决定（与 CPU 无关）；`tools/wh-ship-tune.ts` 自带 `subClass` 自锁。**⑥验证**：typecheck 四包 0 错 · core **153 文件 / 1649 用例** · `content:check` ✅ · `docs:index --check` ✅ · 双 build ✅ · 主树产物已重建（真 context 复核：掠袭 **265** / 哨戒 **265** / 虎鲨 **165**）。**【已验收 · 已合入 main · 未推送】**
- 2026-09-16：**后勤舰特性：我方修最缺血的队友 · 敌方后勤舰 50% DPS 转修理（＋新建一艘 G 族 T3 敌舰）（船长逐条裁定 · 二号 · d2）**——**①船长原话（照抄）**：「**后勤舰添加特性，维修装置可以修理血量最少的队友。**」·「**敌人后勤舰则是将50%的自身DPS转换为修理值。**」·「**敌方后勤舰新增一艘舰船**」·「**T3巡洋**」·「**先不进卡**」·（补充裁定）「**敌方的修理无法以其他敌方后勤舰为目标（包括自己）。**」＋六问六答全取推荐（判据 = `subClass` · 目标 = 三层剩余比例最低含自己 · **修复量不变** · 敌方加 `repairPct` 字段 · 开火减半＋按秒修 · **所有战斗生效**）。**②我方落码**：`UnitSpec.logistics`（`createPlayerSpec` 按 `ShipDef.subClass === '后勤舰'` 写入）⇒ `pulseRepairsFor` 里该舰的维修脉冲改修**三层剩余比例最低的队友（含自己；并列取编队顺序靠前）**；**只换目标、不改量**（仍每跳耗 1 枚组件）· 上限取**被修那艘**的出场满值 · **排除"甲+结构 都满"的单位**（修不动就换下一个；全场都满 ⇒ 空转不耗料）；其余装了维修装置的船**只修自己**（旧口径零变化，用例有对照）。现只有「亡军后勤舰」`sh-wh-g-destroyer` 一艘 `subClass: 后勤舰`。**③敌方落码**：`FoeShipDef.repairPct?: number`（可选、零迁移）＋ `UnitSpec.repairPct` 下发；**开火单发 ×(1−repairPct)**（光束/近盲/掷命中三条伤害路径统一走 `foeRepairDiscountedShot`）· 被折掉的半**每 5 秒一跳**转成修理量（`pulseFoeRepair`：每跳 = `名义 DPS × repairPct × 5 秒`，名义 DPS 取**战斗中炮台面板** `Σ 单发 × 门数 × 1000 ÷ 装填`，不含命中/距离）· 目标 = **非后勤**敌舰里三层剩余比例最低者（**永不含任何后勤舰，含自己**——船长补充裁定）· 不超满血、**不耗组件** · **只在敌阵真有后勤舰时建账本**（`battle.foeRepair`，随档 ⇒ 战中重载不重置计时）。**④新舰（备用壳体）**：G 族 **T3 巡洋**「**残军补给舰**」`foe-g-remnant-tender`——hp **1080**（T3 基线 900 × 1.20）· 单发 **62**（124 × 0.50，"火力让位给修理"）· 命中 0.85 · 装填 4000ms · 射程 502~6604 · 动能8:高爆2 · orbit · 族定值 `speedRatio 1.05`（实速 271）· **`repairPct: 0.5`** ⇒ 名义 15.5/s、**修理 38.75/跳**（≈ 我方 MK2 维修装置的 36/跳，但只修一个目标）；**不挂机群**。**船长定「先不进卡」** ⇒ 照 2026-09-12 战列舰先例做**备用壳体**，实战零影响、上场时只需写进某张卡（编成改动须过船长）。**⑤契约与用例**：`content:check` 新增「**后勤契约**」（`repairPct ∈ (0,1]` · **带 repairPct 的舰必须在白名单里显式登记** · 玩家侧至少一艘 `subClass: 后勤舰` · **已备未上场 = 预警不阻断**，本批正落在这一行）；新增 `tests/logistics-repair.test.ts` **8 例**（我方：修最低比例队友 / 非后勤舰对照仍只修自己 / 全场满血空转不耗料；敌方：舰级登记与名义 DPS 口径 / 开火打折（62→31，无字段原值）/ 每跳修理量与"永不修自己" / 全满空转 / **无 repairPct 的敌阵不建账本**的回归钉子）；`foe-ship-path.test.ts` 速度矩阵补新舰（G 族 4→5 条）。**⑥五闸门**：typecheck 四包 **0 错** · core **154 文件 / 1,655 例** · `content:check` ✅ · `ui:rot-check` ✅ · 桌面构建 ✅。**⑦读数（如实登记）**：**本批不跑实战读数**——新舰不进卡 ⇒ 敌我双方读数**逐字不变**（这正是"先不进卡"的直接好处）；等船长说「上场」（点名进哪张卡）再跑 `battle:calibrate` + 洞内逐层配平。**⑧文档**：词典 §二 新增「**后勤舰（维修脉冲改修队友 · 2026-09-16 船长定）**」条。**【已推送 origin/main】**
- 2026-09-16：**限时活动「虫洞大量生成」（扫描加速 ＋ 一次性赠 5 处）＋ 活动徽标两处修正 ＋ 公告入库（船长 · 一号 · main）**——**①船长原话（照抄）**：「**将扫描虫洞所需时间\*0.25，持续到9月20号，并给予所有玩家5个虫洞（同样持续到20号为止，到20号之后提醒我清理这个过期的赠送）**」⇒ 四问四答：**①每人只发一次 5 个** · **②只给已解锁者**（协会声望 ≥ 40）· **③到期只停止赠送**（已发出的不回收）· **④提醒 = 挂账 ＋ 只读检查**；展示口径「**5和虫洞限时缩短写在一起，但是要润色成虫洞大量生成之类的**」；公告「**写好后给我看**」⇒ 验收后「**完成修改后，进行合并和检查，然后可以直接推送，包括公告**」。**②机制（一条促销 = 三项同源）**：`core/tuning.ts` 新增 **`PROMOS`（促销表）**，与 `TUNING_RULES` **共用同一套日期判据**（`dayWindowActiveAt` / `dayWindowEndMs`：本地时区、`from` 含当日 00:00、`until` 当天整天有效、**次日 00:00 失效**）。`PromoRule` = `id`（`state.promoClaimed` 的键）· `label`/`detail`（活动栏徽标文案）· `from`~`until` · **`scanMul` 0.25**（`wormholeScanWindowMs` 相乘 ⇒ 基准 12 小时 → **3 小时**，再吃技能口径）· **`giftWormholes` 5** · `claims: ['wormholeScanMs']`（被认领的倍率键**不再单列**徽标：同一件事不显示两遍）· **`open: 'wormhole-scan'`**（点击去向，单点数据驱动）。**赠送口径**（`wormholeScan.ts` 的 `reconcileWormholePromoGift`，照 `reconcileWormholeScanWelcome` 范式）：**逐 tick 幂等 + 只发一次**（靠 `state.promoClaimed[promoId]`；老档缺席 ⇒ 下一拍自动补发）· **只给已解锁者**（未达标不发、也**不记领取** ⇒ 达标后仍在活动期内自然领到）· **允许暂时超过库存上限**（手里 3 处 ⇒ 送 5 处得 **8 处**，船长确认；⚠ 放宽**只作用于赠送这一条路径**——扫描产出与"满则停机"仍严守上限：拆出 `stockPushUncapped`，`wormholeStockPush` 语义未动）· **到期只停止赠送**（不改任何已存状态 ⇒ 零迁移、零回收代码）。**存档**：新增**可选**字段 `state.promoClaimed?: Record<string, true>`（白名单读写、空表不落键）⇒ **不升版本、零迁移**。**③验收后两条 UI 修正**：船长「**虫洞大量生成6个字显示不完全。建议宽度要保证标题文字都能显示。**」⇒ 根因两条 = 剩余时间用**全量** `formatDurationMs`（4 天档 10 个汉字 ≈105px）＋ 徽标定宽 190px 且**允许被压缩**、名称带省略号；修法 = 新增紧凑时长 **`formatDurationShort`**（只留两级最大单位 `4天21小时`，悬停 tip 仍全量）＋ 时间列 `min-width: 7em` 右对齐（剩余时间变化不再推挤标题）＋ 徽标 `width: max-content`（保 190px 观感）`flex-shrink: 0`、名称去掉省略号 ＋ 活动窗头部 `flex-wrap: wrap`（窄窗整枚徽标换行，而不是把标题裁掉）。船长「**并且点击后，不会跳转扫描虫洞界面**」⇒ `PromoRule.open` = `'wormhole-scan'` ⇒ 点击进星图页「扫描虫洞」选项卡（`WormholeScanTab`），不写则与限时加成徽标同款走「星图」；`content:check` 促销契约新增第 ⑧ 条拦"未知去向"＋ 用例守卫"删掉 `open` 即红"。**④在办中抓到一处日历重叠（已修）**：既有用例 `tuning.test.ts` 的开关④把"现在"取成 **2026-09-20 12:00**，正落在本促销窗口内 ⇒ 两把乘子叠加（`×0.5` 被算成 `×0.25`）；修法 = 该用例墙钟挪到窗口外的 **9/25**，并把"该时刻无任何促销"**写成断言**（日后新促销再盖住它会立刻红）。**教训**：促销是**全时段环境乘子** ⇒ 凡给扫描窗口定读数的用例都必须避开活动窗口。**⑤落码与工具**：`tuning.ts`（`PromoRule`/`PROMOS`/`activePromos`/`promoActiveAt`/`promoScanMul`/`activePromoGifts`/`dayWindowEndMs`）· `wormholeScan.ts` · `state.ts` · `save.ts` · `engine.ts`（`advanceGame` 逐 tick 认领）· `index.ts` · `ActivityBar.tsx`（复用 `.app-activitybar-tuning*` 同族样式）· **新工具 `npm run tuning:expired`**（列出生效期已过/未开始的限时条目；**不进 `content:check`**——体检与标定读数不吃日历）· `content-check.ts` 新增「限时促销契约」七条。**⑥用例**：`tuning.test.ts` ＋5（窗口边界 / 读数 / 回落 / 引擎读取点 / **两表同判**）· `wormhole-promo-gift.test.ts` ＋8（含存档往返与"只发一次/只给已解锁/可超上限"）。**⑦负向验证（各自精准 · 复原即绿）**：`giftWormholes` 改 0 ⇒ 8 条如实红 ＋ 契约点名；`until` 挪到过去 ⇒ 11 条如实红 ＋ 提醒工具如实列出；摘掉 `open` ⇒ 去向守卫如实红。**⑧闸门与上线**：typecheck 四包 **0 错** · core **151 文件 / 1,614 例** · `content:check` ✅ · `ui:rot-check` ✅ · 桌面 ＋ 网页 build ✅ · 编码三查（全 CRLF / 无 BOM / 往返乱码 0）；推送 `73ca8efb..482d0301`，**Pages 核验**：线上已切 `assets/index-rkKiY5U0.js`，与本地 **SHA256 逐字节一致**（`8B0B66CB…D540710F` · 1,600,814 字节），产物内含公告文案「虫洞大量生成」✅。**⑨公告**：`announcements.ts` 最上方新增 `ann-wormhole-bloom-20260916`「虫洞大量生成」（tag 活动 · 3 条要点 · 第三人称、不写倍率数字）**已随同批上线**；待审稿留档 `docs/design/announcement-draft-20260916-wormhole-bloom.md`。**⑩到期清理（9/21 起）**：`npm run tuning:expired` ⇒ 删 `PROMOS` 那一行 ⇒ 重跑 `content:check`；**不需要任何回收动作**（`promoClaimed` 留着无害）。**【已推送 origin/main · 促销 9/20 到期】
- 2026-09-16：**报障「船体维修装置在虫洞里无效」→ 逐舰维修 ＋ 逐舰护盾充能 ＋ 我方近防炮逐舰（船长裁决「甲」· 一号 · main）**——**①船长原话（照抄）**：转述报障「**玩家反应，船体维修装置在虫洞里无效**」⇒ 裁决「**甲，并检查是否还有什么其他僚舰相关没做的**」⇒「**将缺少的一并实现。洞内维修量级保持不变**」。**②核查结论（先说清"是不是没接线"）**：**接线是通的**——洞内开战会预载装置、脉冲按 5 秒跳、每跳扣组件、装甲/结构真回血；玩家觉得"无效"对应**两条各自独立的原因**：**(a)** 旧 D 批边界「**维修装置只预载主控**（僚舰修理包不参战）」⇒ 装置装在**僚舰**上时洞内**完全不工作**（实测 `battle.repair === null`、0 跳 0 组件）；**(b)** **量级被洞内火力盖过**——民用 5 / MK1 10 / MK2 18 **每 5 秒**一跳（= 1~3.6 点/秒），而洞内节点敌人（层 1 就是威胁 45 档）≈ **163 DPS**、深层上千 DPS ⇒ 即便参战肉眼也看不出血条回升。**探针读数**（真引擎 · 同 seed 同敌卡 A/B）：洞内装置装主控 ⇒ 不装 `a 0 / h 0`（被打沉）vs 装 `a 5 / h 5`、**pulses 6 / kitsUsed 6**；装僚舰 ⇒ `battle.repair = null`；组件只放主控货舱 ⇒ 预载正常（50 枚全抽进本场账本）；洞外对照（既有用例）45 秒 **9 跳 = 精确 +90 点**。**③裁决与落码（逐舰维修 ＋ 逐舰护盾充能）**：`state.ts` 抽出 `BattleRepairLedger` / `BattleShieldChargeLedger`，新增**可选**逐舰账本 **`repairBy` / `shieldChargeBy`**（键 = 战斗 tag 含 `player`）——`repair` / `shieldCharge` 仍是**主控那一份** ⇒ **老读法零迁移**；`combat.ts`：预载组件来源改**本舰货舱**优先（新增 `removeCargoOfShip`；旧口径读**驾驶船**货舱 ⇒ 僚舰用量记到主控头上）、`pulseRepairsFor` / `pulseShieldChargeFor` 逐舰版（旧 `pulseShieldCharge` 保留为兼容入口）、唯一读取入口 `repairLedgersOf` / `shieldChargeLedgersOf`（无 `*By` ⇒ 退化成"只有主控那一份"= 旧行为）、**`refundRepairKitsAll` 逐舰退款**、`repairUsageText` 按类型**合计全队**消耗、`startFleetBattleFor` 逐舰预载；`expedition` / `encounters` / `ai` / `wormholeBattle` 四档战报的退款入口统一换成 `refundRepairKitsAll`（单船路径自动退化成旧行为）；`save.ts` 把两表登记为 **persist**（丢了 ⇒ 僚舰预载组件凭空消失）＋ `cleanLedgerMap` 清洗；`BattleScreen.tsx` 徽标改**全队合计**（任一舰在跑即"运转中"）、悬停列出**逐舰明细**；`index.ts` 导出三个逐舰入口。**④僚舰审计（船长第二问）**：机群放飞 / 逐舰战损 / 回收（2026-09-14）· 敌方点防逐舰选靶 · 弹药装载（`loadAmmoTier(shipId)`）· 护盾**被动**回充 · 推进器周期 · 承伤持久化（`persistFleetHullDamage`）· 沉船与全损**本已逐舰**；**本批补齐 = 修理组件预载 ＋ 护盾充能装置 ＋ 我方近防炮**（下条）；**仍未逐舰 = 敌方点防令牌**（**核定维持全队共用**，见下条）。**⑤用例**：新增 `tests/wormhole-fleet-repair.test.ts` **6 条**（僚舰装置真参战 · 组件逐舰各扣各的 · 退款逐舰＋幂等 · **老档在途战斗兼容**（只有 `repair` ⇒ 只有主控修）· 护盾充能逐舰 · 战报按类型合计）；既有 1,615 条**零改动全绿**。**⑥闸门**：typecheck 四包 **0 错** · core **151 文件 / 1,621 例** · `content:check` ✅（**顺带抓到并修掉**一处真问题：UI tooltip 里写了 Markdown `**强调**`，玩家会原样看到 ⇒ 去记号，正是「文案纯净契约」该拦的）· `ui:rot-check` ✅ · 桌面与网页 build ✅。**⑦量级项关闭**：船长定「**洞内维修量级保持不变**」⇒ 每跳 5/10/18 维持原值（深层依旧近乎不可见＝**已知取舍**，不做"洞内每跳 ×2"那案）。**⑧复查建议**：洞内续航被逐舰化抬高 ⇒ 下次洞内配平批用 `npm run wormhole:econ` 复核（本批未跑）。**【已推送 origin/main】**
- 2026-09-16：**我方近防炮逐舰化（令牌 ＋ 集火锁）＋ 敌方点防令牌核定「全队共用」（船长复核「点防没问题」· 一号 · main）**——**①原缺口（两处，均在 `combat.ts`）**：**① 反应式令牌 `battle.droneHitAt.me` 是全队一个**（敌机打中任何一舰都写同一个时刻，且一次反击就消费掉 ⇒ 4 舰编队整队每轮只换到一发反击，僚舰近防炮基本沉默）；**② 集火锁 `battle.mePdFocus` 只按"武器下标"存**（不含舰 tag ⇒ 多舰的 0 号武器互相顶锁）。**②落码**：新增 **`droneHitAtMeBy`**（键 = **被打的那艘我方舰 tag**：敌机打过**谁**，就只给**谁**开反击窗口；`pickFoeDroneTarget` 新增 `myTag` 入参，缺省 `player` ⇒ 单船路径逐字不变）＋ **`mePdFocusBy`**（键 = **`舰tag:武器下标`**，每舰每门炮各锁各的）；两表在 `createBattleState` **开场即建空表** ⇒ 新战斗一律走逐舰路径，旧字段只服务"本次改动之前开的在途战斗"（**运行态字段、不随档 ⇒ 零迁移**，`save.ts` 登记表已补）。**③用例**：`tests/pd-player-target.test.ts` 改写为逐舰口径 ＋ 新增 3 条（**令牌逐舰**：僚舰挨打不给主控令牌 · **集火锁逐舰**：`player:0` 与 `ally-1:0` 各锁各的 · **老形状兼容**：只有旧字段时照旧工作）。**④读数口径（重要，别把这条当成"伤害提升"）**：真引擎 A/B（2 舰各带近防炮 MK1 · 敌卡 4 架出击型机群 · 90 秒）⇒ **逐舰口径与旧口径完全相同**（击落 **4/4 vs 4/4** · 我开火 102 vs 102 · 近防炮标记 0 vs 0）——本批修的是**归属与语义**（被打的那艘还手、每门炮锁自己的靶），**不是**"更多反击、更多杀伤"；整场输出量由"每次敌机攻击换一次反击"这条既有规则决定，两口径**总令牌数一样多**。**⑤敌方侧核定（本条的收口）**：`resolvePointDefense` 的 `droneHitAt.foe` **仍是全队共用**（我方无人机打到**任意一艘**敌舰 ⇒ 当场**所有冷却已就绪**的敌点防舰**各还手一次**）——它自己的旧注释写「一次攻击换一次还手（**对每艘点防舰各一次**）」，**代码与注释不符**（旧工作文档里"只有第一艘轮得到"的说法也不准，已按代码核实更正）；船长 2026-09-16 复核定论「**点防没问题**」⇒ **按现状保留**，`combat.ts` 三处 ＋ `state.ts` 文档 ＋ `tools/pd-tune.ts` 机制说明**同批改写为实况并写明"勿照逐舰改敌方侧"**（纯注释、零行为变化；顺带改正 pd-tune 旧句「哨戒机不被打」→ 实为**要在 `PD_SENTRY_RANGE_M` 2500m 内才进候选池**）。**⑥闸门**：typecheck 四包 **0 错** · core **153 文件 / 1,646 例** · `content:check` ✅（3 条既有预警：奇货类物品市场卡、bp-shieldchg-2/3 书价，与本批无关）· 注释改动不涉 UI ⇒ `ui:rot-check` 不适用。**【已推送 origin/main】**
- 2026-09-16：**洞外「受损自动修补」改为需要船体维修装置（取件随装置 · 公告不发）（船长 · 一号 · main）**——**①船长原话（照抄）**：「**虫洞外，原本的损伤严重自动消耗维修组件功能，需要修改。改成需要玩家携带对应的船体维修装置。消耗的维修组件类型也跟着装置走。**」＋「**记得相关通讯也要一块修改。**」（**本条为行为改判**：旧口径"不看装置、民用优先"作废）。**②新旧口径对照**：**前置条件** 旧＝不看装置（装甲或结构 <50% 就地自动用组件修）→ 新＝**必须装着船体维修装置**（中槽），没装 ⇒ **一枚组件都不动、不写日志**；**取件序** 旧＝民用优先 → 军用 → 其它带回复值的组件 → 新＝**只吃装置指定的那一种**（民用装置 → `repairkit-civ`；MK1/MK2 → `repairkit-mil`；两者都装 ⇒ 两种都能吃，按装配顺序）；**来源**（重复清剿只读货舱；低安遇袭 = 货舱优先、仓库兜底）与**修复量**（组件基础值 × 层容量增幅 × 舰体快修学）**不变**；**判定返回值**新增 `hasDevice`，**没装置时 `outOfKits` 恒为 false** ⇒ 低安遇袭**不会**把"没装装置"误判成"断料返港"（只按结构线判）；**无组件自愈件**（`repairFree` 生体件）**不算**驱动自动修补的装置。**③两个调用点**（洞外，全部走单点 `shipyard.repairWithKitsFor`）：重复清剿自动再出发（`expedition.advanceAutoLoopBounty`，阈值 50% / 目标 60%）与低安遇袭收场（`encounters.settleEncounterTail`，目标 `encounter.repairTargetFrac`）；⚠ **手动「使用修理组件」按钮未动**（船长指示只针对"自动消耗"那条链），洞内维修装置脉冲也不受影响。**④落码**：`shipyard.ts` 新增 `repairKitIdsOf`（装置 → 组件映射，装配顺序去重）、`RepairWithKitsResult` 增 `hasDevice` / `kitIds`、`repairWithKitsFor` 加装置门槛 ＋ 只吃指定组件、`takeRepairKit` 改**白名单取件**（删掉"民用优先/兜底其它组件"）、日志点名实际消耗的组件种类、`repairWithKits` 改返回完整结果；`expedition.ts` 自动环**停环理由分两种说法**（**未装装置** / **对应组件耗尽**——玩家下一步动作不同）；`encounters.ts` 头注按新口径改写；`Handbook.tsx`「受损后先修再判」两条改写；`messages.ts` **两条通讯一并改**（`msg-lowsec-rules` 低安须知末条 ＋ `msg-ambush-retreat` 被袭自动撤离，均写明"装着维修装置才自动修、组件随装置"）。**⑤用例**：新增 `tests/auto-repair-device.test.ts` **7 条**（没装置不修 / 民用只吃民用 / MK1 只吃军用 / 装置在但没对应组件＝断料 / 两装置都能吃 / 生体件不算 / 修复量口径不变）＋ 改既有夹具（`b1.test.ts` 4 处装装置 ＋ 新增"没装置"用例 · `expedition.test.ts` 2 处装装置 ＋ 新增"没装置 ⇒ 停环且理由点名" · `t8.test.ts` / `hull-persist.test.ts` 装装置 ＋ `repairWithKits` 返回值改口）。**⑥负向验证（结构性）**：旧口径"没装置也能修"的路径已被用例钉死为正红（`hasDevice=false` ⇒ `used=0`）。**⑦玩家可见面**：装了装置且带对应组件的船**行为一字不变**；**没装装置的船不再自动修补**——低安遇袭只按"结构 <50%"决定返港，重复清剿会在出发前停环并提示"自动修补需要该船装着船体维修装置"；手册与两条通讯已同步。**⑧公告判定 = 不发**（船长 2026-09-16「**不写入公告**」；历史公告（2026-09-13 长途运输那条写有「随船或仓库里的修理组件」）**按公告留档纪律不回改**，待审稿留档 `docs/design/announcement-draft-20260916-repair-needs-device.md`）。**⑨闸门**：typecheck 四包 **0 错** · core **152 文件 / 1,636 例**（新增 7 ＋ 4 条口径用例）· `content:check` ✅ · `ui:rot-check` ✅ · 桌面与网页 build ✅。**⑩存档**：零迁移（不新增字段，只改判定）。**【已推送 origin/main】**
- 2026-09-16：**维修量「统一吃」层容量加成 ＋ 星图记录学改每级 +2（船长 · 一号 · main）**——**①船长原话（照抄）**：「**统一吃，并在相关说明中提及（提高维修量等）。**」（承接"维修量是否吃额外护甲/结构加成"）＋「**星图记录学，效果错误，应该为每级+2，满级+10。**」。**②维修量统一吃层容量增幅**：改前 ＝ 修理组件**吃**（`round(组件值 × 层容量增幅 × 恢复量技能)`）而**装置每跳不吃**（只吃恢复量技能）；改后 ＝ **两条路径同一把尺**（装置每跳 = `round(装置值 × 层容量增幅 × 恢复量技能)`）。**层容量增幅 = 该层满值 ÷ 舰船档案基础值**（`createPlayerSpec` ÷ 舰船定义），来源 = 装甲件 `armorHpBonus`（装甲那一路）· 任意槽结构件 `hullHpBonus`（结构那一路）· 技能「船体加固理论」· 重装族「重装舰操作」；**分层生效**（装甲那一路只吃装甲容量加成，与组件同口径）。**实测读数（真引擎 · 无甲件）**：民用装置每跳 无技能 5 / 满恢复量技能 8 / **＋船体加固理论满级（层增幅 ×1.2）9** / **重装族（×1.44）11**；MK1 **10 / 15 / 18 / 22**；MK2 **18 / 27 / 32 / 39**；一条民用件 **5 / 8 / 9 / 11**、一条军用件 **10 / 15 / 18 / 22** ⇒ **两档恰好对齐**：满技能下「民用装置每跳 = 一条民用组件的恢复量」「MK1 每跳 = 一条军用组件」（各 9 / 18），MK2 每跳 32 高于一条军用件 18（它本就是高配档）。**实现**：`combat.ts` 新增 **`layerAmpOf()`**（与 `shipyard.kitHealFor` 的 `capA/baseA · capH/baseH` **完全同源**，同一处 `createPlayerSpec` ÷ 档案值），`preloadRepairFor` 的**吃组件装置**与**无消耗自愈件**两条分支都乘它（开战时折进快照，与"装配 + 技能"同一份快照语义）。**文案同步（船长"在相关说明中提及"）**：`modules.ts` 三档维修装置 ＋ 两件生体自愈件（生体甲壳板 / 生体损管腔）说明补「每跳修复量随装甲/结构容量加成与舰体快修学放大」；`skills.ts` 船体加固理论 / 重装舰操作说明补「容量变厚的同时，修理组件与船体维修装置的修复量按同一比例提高」；`ui/shipInfo.tsx` 装配详情面板"自动维修/生体自愈"块新增 **「修复量」**一行；`Handbook.tsx`「战中续命」条补同款说明。**③星图记录学：阶跃 → 每级线性 +2**：旧口径（**作废**）＝ **满级（Lv5）一次性 +10**，Lv1~4 不加 ⇒ 库存上限只有 5 或 15；新口径 ＝ **每级 +2** ⇒ **7 / 9 / 11 / 13 / 15**（**满级总量不变**，中段开始就有用）。落码：`wormholeScan.ts` 新增 `WORMHOLE_STOCK_BONUS_PER_LEVEL = 2`、`wormholeStockMaxOf` 改 `5 + 2×等级`；`WORMHOLE_STOCK_BONUS = 10` 保留为**满级锚**（读档钳制 `WORMHOLE_STOCK_MAX_HARD = 15` 不变），并由用例守卫「**满级锚 == 每级值 × 5**」（防两处各自漂）；文案 `skills.ts` 改「每级 +⟦2⟧ 处（满级 +⟦10⟧ 处，由 5 处提到 15 处）」、`WormholeScan.tsx` 提示改「每级 +2，满级 +10」；`content:check` 技能说明契约登记表补 `chart-archive · per: 2`（保留 `per: 10` 的满级锚条目）。**④用例**：装置吃层容量增幅 2 组断言 ＋ 星图记录学每级 4 档 ＋ 满级锚守卫（core **153 文件 / 1,646 例全绿**）。**⑤闸门**：typecheck 四包 **0 错** · `content:check` ✅（技能说明契约 **51 条引擎现场复核**全过；首跑被"per:10 现场找不到"拦下——因满级锚一度改成算式 ⇒ 已改回字面量 `10` 并用例守卫，**正是该契约的用途**）· `ui:rot-check` ✅ · 桌面与网页 build ✅ · `battle:calibrate --std`：标准矩阵**逐格不变**（该矩阵固定配装**不含维修装置** ⇒ 本批读数零影响）。**⑥归档去向**：词典「修理组件回血口径」条补本批 ＋「虫洞」条改"每级 +2（7/9/11/13/15）"。**【已推送 origin/main】**
- 2026-09-16：**市场价格小史改回「30 分钟/点」（船长 · 二号 · d2）**——**①船长问**：「**玩家影响了市场价格后的价格，是否会在市场详细的折线图内显示**」⇒ 查实：**会**（折线记的是**均衡价 L**：基准 × 池库存压力 × 慢速噪声 × 冲击动量 ⇒ 砸盘/扫货的**持久影响一定进图**），**但当时是"每个 60 秒窗口记一点"** ⇒ **48 点只覆盖 48 分钟**，与注释/悬停标注的「样本间隔 30 分钟」「48 窗 ≈ 24 小时」**对不上**（顺带查出：`sampleAgoLabel` 按 `× 30` 分钟换算 ⇒ 悬停「约 N 分钟前」**错 30 倍**，最老那点会写"约 23 小时 30 分前"）。**②船长裁定（照抄）**：「**按照原来的30分钟来**」⇒ 改**采样**而不是改文案。**③落码**：新增 `PRICE_SAMPLE_MS = 30 * 60_000`；`processWindow` 里改为**按游戏时刻跨 30 分钟边界采样**（判据 = `lastTickGameMs → nextNow` 的桶号变化 ⇒ **不新增存档字段、零迁移**）；`PRICE_HISTORY_LIMIT = 48` 加注释「48 点 × 30 分钟 = 24 小时」；`marketHistory` / `marketTrend` 注释同步（趋势 = 现在 vs **24 点前 = 12 小时**）；**历史为空时补一个起点**（`processWindow` 与 `ensureMarket` 的 freshInit 分支各一处，守卫都是 `hist.length === 0` ⇒ 新档/老档开市即有基线，折线图不必空等 30 分钟）；界面 `MarketPage` 的悬停换算改用同一个常量 `PRICE_SAMPLE_MS`（不再写死 30），折线/回退/趋势三处注释改按"点"与小时表述。**④代价（已知会并接受）**：成交后**最迟 30 分钟**才落点（原为最迟 1 分钟）；**池库存压力**这种持久影响照旧进图，而"**冲击动量**"（半程分钟级）可能在被采样前已回落 ⇒ 短线砸盘的瞬态在图里可能看不全。**⑤用例**：`market.test.ts` 新增「价格小史采样节奏」1 条（前 29 分钟不加点 · 第 30 分钟 +1 · 大离线 48 小时封顶 48 点 + 开盘补起点）；既有 3 条"离线后历史非空"的断言在补起点后逐字通过（其中 5 分钟离线那条原先只靠 freshInit 补点，故把补点推广到"历史为空即补"）。**⑥五闸门**：typecheck 四包 **0 错** · core **153 文件 / 1,646 例** · `content:check` ✅ · `ui:rot-check` ✅ · 桌面构建 ✅。**⑦文档**：词典 §三 新增「**市场 · 价格小史（折线图采样）**」条（采样间隔/覆盖/记什么价/趋势口径/补起点）。**【已推送 origin/main】**
- 2026-09-16：**隐秘行动装置（高槽支援件 · 开火前隐身）＋ 对照读数工具（船长逐条裁定 · 三号 · verify）**——**①船长原话（照抄）**：「**添加隐秘行动装置，高槽，效果是自身武器开火前，隐身30秒（不被锁定，不被攻击）**」→ 六问六答：**Q1 甲**（开战隐身 · **本舰首次开火立即现形** · 最长 N 秒先到者为准）· **Q2 甲**（敌方**选不到** ⇒ 主炮与机群**停火待机**）· **Q3 甲**（**只护装了装置的那一艘**）· **Q4**（两档）· **Q5 丙**（先交对照读数再定代价 ＋ **追加禁令：有推进器类时直接解除隐身**）· **Q6 甲**（零新增界面 ＋ 预估计入）；定数：「**MK2吃55CPU，价格提高到300W，MK3吃80CPU，价格提高到1000W，MK2为稀有订单稀有度3，MK3为奇货，稀有度4**」；边界：「**不算推进类**」（给速度但非推进器族的两件不触发禁令）。**②落码**：数据 `mod-stealth-2/3`（`stealthMs` 20000/30000 · CPU **55/80** · 300 万 / 1000 万 · 稀有档 3 / **奇货档 4** · 蓝图书价 ×2.5/×4 = **750 万 / 4000 万** · 材料 ≈ 产物价 45% · 2400s/4800s · 奇货蓝图不挂声望门槛）；引擎 `createPlayerSpec`（取最长一件 ＋ **推进器族在装即判 0**）→ `createBattleState` 写逐舰 `stealthUntilMs`（**随档**）→ 敌方选靶单点 `pickMyUnitTarget` 用 `isMyUnitTargetable` 排除（**单船路径零随机数消费照旧**）→ 开火（`stats.meShots`）与到点（每拍收尾）两支清窗 ＋ 战斗内提示两句（照机制提示纪律**不写 addLog**）；`bountyDamageForecast` 把敌方有效输出时长扣掉窗口（玩家可见胜率走蒙特卡洛真引擎 ⇒ 自动计入）；`equipment.stackingOf` 新增 **`max` 组**（多件取最长，界面不谎报"全额叠加"）。**③契约**：「隐秘行动装置契约」把**船长给定值逐项钉死**（两档/高槽/时长/CPU/价格/渠道与档位/书价/可买可造）＋ 支援件第七类效果（隐身 = 高槽）。**④读数（新工具 `npm run battle:stealth-readings`，真引擎 · 大白鲨 + 全技能 L3 + 40 种子/格）**：纯机制净效果（同装配只差装置）灰霾清剿 **40% → 45%（MK2）/ 65%（MK3）**、敌开火 95.1 → 77.6、甲残 16% → 31%；**CPU 合法装** 4 炮 / 3 炮+MK2 / 2 炮+MK3 = **40% / 45% / 45%** ⇒ **MK2 只丢 1 门炮就净赚 5 个点**。**⑤用例 +7**（`tests/stealth-device.test.ts`：窗口内不被锁定不掉血 · 到点现形 · 开火即现形 · 两档与多件取最长 · **推进器禁令按家族动态枚举（含微型跃迁引擎）＋ 非推进器中槽件对照** · 编队只护自身 · 存档往返 · 预估计入）；**负向验证四处各自精准**（选靶排除 / 推进器禁令 / 开火现形 / 禁令缩水成"只认矢量推进器"⇒ 点名 `mod-mwd-1` 红）。**⑥闸门**：typecheck 四包 0 错 · core **153 文件 / 1645 用例** · `content:check` ✅（书价与档位系数 0 处不符、料/价出带 0 处）· `docs:index --check` ✅ · `ui:rot-check` ✅ · 双 build ✅；**主树产物已重建**（产物核对：装置名/短行/限制语/叠加方式四项在包内）。**⑦流程如实登记**：首版 CPU/价格等**我自作主张定过一版、未事前送审**，经船长指出后改为他给的数并把契约改为逐项钉死。**【已验收 · 已合入 main · 未推送】**
- 2026-09-16：**扫描页虫洞卡片加「敌情」（族名 + 主系一句 · 悬停列三档）（船长 · 二号 · d2）**——**①船长原话（照抄）**：「**扫描虫洞界面，给虫洞卡片添加更多信息（虫洞内是什么敌人，以什么类型伤害为主）**」＋三问三答全取推荐（「**甲：族名 + 主系一句**」·「**甲：卡片族级 + 悬停列三档**」·「**甲：卡片直显 + 悬停细节 + 准备页对齐**」）＋「**不给**」（= 不做"建议抗性"）。**②改了什么**：**core 单点**新增 `WORMHOLE_FAMILY_ETHNIC`（族称：**A 海盗 · C 异形 · D 守墓 · E 巨构 · G 亡军**）· `DAMAGE_TYPE_LABELS`（动能 / 高爆 / 能量，与战斗界面同一套叫法）· `WORMHOLE_TIER_LABELS`（浅/中/深）· **`wormholeFamilyIntel(family, ctx)`**（浅层卡名 + 主系一句话 + 三档「卡名 + 火力构成」，构成取 `foeDamageComposition` ⇒ 与战斗结算、胜率预估同源；主系规则：单系「纯高爆」· 头名 ≥75%「动能为主」· 否则「动能 / 高爆并重」）；**engine** 暴露 `wormholeFamilyIntel(family)`（走 `ctx.anomalies` **全表**——洞内卡都 `hidden`，用过滤目录会漏出内部 id，2026-09-14 踩过）；**渲染层**新增 `ui/wormholeIntel.ts`（**扫描页与准备页共用**），`WormholeScan` 卡片**新增第三行**「敌：劫掠支队（A 族 · 海盗）· 动能为主」并把悬停换成完整口径，`Wormhole` 准备页同批对齐（**仅在已知是哪一处时显示** ⇒ 从库存进来才有，直接开面板不猜不编）；**零样式改动**（`.app-inv-main` 本就是纵向 flex ⇒ 自然换行）。**③五族实拉文案**：A 海盗「动能为主」（三档一致 动能80/高爆20）· C 异形「能量为主」（能量80/高爆20）· D 守墓「能量为主」（浅/中 能量80/动能20 · **深 陵墓王庭 能量60/动能40**）· **E 巨构「高爆 / 动能并重」**（浅/深 50:50 · **中 导弹残响 纯高爆**——唯一单系档）· G 亡军「动能为主」；⚠ 这两处**档间差异**正是"卡片只报族级、差异写进悬停"这条口径存在的理由。**④契约与用例**：`content:check` 新增「**卡片敌情契约**」（五族 × 三档都必须算出**非空卡名且不露内部 id + 非空构成 + 份额和 = 1**），读数行 `A 海盗「动能为主」 · C 异形「能量为主」 · D 守墓「能量为主」 · E 巨构「高爆 / 动能并重」 · G 亡军「动能为主」`；`wormhole-scan.test.ts` 新增 **2 条**（五族族称与主系文案钉死，含 E 族"并重"不硬说主系 · 三档齐备 + 档间差异：D 族深层 6:4 与 E 族中层唯一单系）。**⑤五闸门**：typecheck 四包 **0 错** · core **152 文件 / 1,638 例** · `content:check` ✅ · `ui:rot-check` ✅ · 桌面构建 ✅。**⑥文档**：词典虫洞段「库存项」条改写并**新增「卡片敌情行」条**（族称/主系规则/三档差异/两处同源/护栏）。**⑦边界**：不动任何数值、概率与存档字段；不给"建议抗性"、不上色 chip（船长明确不要）；卡片原第二行「原型名，发现于 X月X日」一字未动（2026-09-14 那条口径不冲突）。**【已验收 · 已推送 origin/main】**
- 2026-09-16：**遗迹保底改为从 3 层开始 · 层 1 不出遗迹（船长 · 二号 · d2）**——**①船长原话（照抄）**：「**遗迹的保底，改为从3层开始保底。1层没有遗迹**」＋（两问两答全取推荐）「**甲：沿用原公式（层 3 起生效）**」·「**不例外**（「遗迹密集」原型在层 1/2 不给遗迹加成）」。**②落码**：新增常量 **`WORMHOLE_RUINS_FLOOR_MIN_DEPTH = 3`**；`wormholeRuinsFloorFor(d)` ⇒ 层 1/2 = **0**（旧表 层1/2 = 1 作废）、层 3 起 `1 + ⌊(d−1)÷2⌋` ⇒ **层3/4 = 2 · 层5/6 = 3 · 层7/8 = 4**（沿用原公式，只挪起效层）；生成器里 **层 1 的遗迹份额 = 0**（残骸信号全给舰船墓场）· **层 2 = 基础 30%**（**不给**「遗迹密集」的 50% 份额与 +1 保底 ⇒ 可能一个都没有）· 层 3 起按原型；下限兜底翻转同样只在层 3 起生效。**③实测（600 种子/层）**：遗迹格均值 层1 **0.00**（恒 0）· 层2 **0.79**（59.5% 的层有 ≥1 张）· 层3 **2.60** · 层4 2.67 · 层5 4.33 · 层6 4.31 · 层7 4.97 · 层8 4.99；连带**出货柜**（遗迹 70%、层 2 起）⇒ 该层期望柜数 层2 **0.55** · 层3 1.82 · 层5 3.03 · 层7 3.48 · **层 1 恒 0**（层 2 的"至少出 1 柜"概率从 76% 降到 **45%**）。**④契约与用例（改了 3 条旧期望 + 新增 2 条）**：`content-check` 的「层间盘面契约」新增**层 1 遗迹恒 0** 的硬断言、读数行改印「层1≥0 · 层2≥0 · 层3≥2…（**保底从层 3 起**；层 1 恒 0 张 · 层 2 无保底）」；`wormhole-nebula.test.ts` 两处旧期望改写（下限表 1/1/2/2/3/3 → **0/0/2/2/3/3/4/4**；「遗迹跟着层数涨」改成 **层1 = 0 · 层2 < 层3 < 层5**）；`wormhole-grid.test.ts` 的「遗迹 = 残骸信号的 30%」用例从层 1 挪到**层 3** 验比例、并新增**层 1 恒 0** 的钉子；新增 `describe('虫洞网格 · 遗迹保底')` **2 条**（层 1 恒 0 —— 200 种子；下限表 + 层 2 "允许 0、也常 >0" + 层 3~8 逐层守下限）。**⑤闸门**：typecheck 四包 **0 错** · core **151 文件 / 1,624 例** · `content:check` ✅ · `ui:rot-check` ✅ · 桌面构建 ✅。**⑥文档**：词典虫洞段新增「**遗迹格数量与保底**」条（新表 + 实测均值 + 出货柜连带）。**【已推送 origin/main】**
- 2026-09-15：**洞内稀有残骸漏摘施工期闸门（真 BUG，已修）＋ 虫洞谜质说明补「不建议出售」（船长报障/指示 · 三号 · verify）**——**①船长原话（照抄）**：「**虫洞谜质添加说明：不建议出售。精炼炉好像缺少虫洞的稀有残骸回收？稀有残骸是按种族划分吗？**」＋ 追问命名后「**残骸先保持不变，修完BUG就行**」。**②报障取证链（逐环节核过）**：洞内墓场/遗迹/舰船信号**真会出稀有残骸**（每 3 堆普通判一次 35% / 遗迹 2~3 堆 / 舰船信号固定 1 堆）→ 撤离收口把它们 `addWare` **带回仓库** → 但物品卡上**还留着施工期 `unreleased: true`**（`data/src/context.ts`，注释写"随虫洞一起上线"，**2026-09-14 虫洞上线时漏摘这一个字段**）→ 而精炼炉「残骸回收」列表走**玩家可见目录**（`visibleItemDefs` / `itemReleased`）⇒ **打捞带回的洞内稀有残骸不进回收列表、高级箱开不了**（手册物品图鉴同样看不到）；漏摘没被拦的原因：体检那条"虫洞专属内容必须已上线"契约当时**只枚举装备/舰船/图纸/无人机，物品不在判据里**。**③修法（= 设计稿写的上线动作）**：删掉该字段 ＋ 把**物品**纳入那条契约（`wreck-rare-wh-` 前缀 ⇒ 读数 **90 → 105 条**）＋ 逐张洞内敌卡断言"存在对应稀有残骸物品"（防"打捞回来的箱子开不了"）；顺带更正 `wormholeFoes.ts` 那句过时注释。**④踩坑如实登记**：新哨首版读**静态 `ITEMS`** ⇒ 空集、**误报 15 处**（稀有残骸是 `context.ts` **运行时注册**的，不在静态数组里）；改读 `buildSimContext().items` 后通过（已在代码注释里登记）。**⑤命名口径（回答第三问）**：**名字按卡、开箱池按族**——普通残骸 = `${卡名}残骸`、稀有残骸 = `稀有残骸（卡名）`（如「稀有残骸（灰霾伏击团清剿令）」），而高级箱专属装备池按**敌族**取（`FOE_LAIR_GEAR[族]`）；共 **20 张窝点卡 + 15 张洞内卡 = 35 种**稀有残骸（另有 B 族 2 件退役留档）。**⑥谜质说明**：`mat-wh-essence` 说明补「**不建议出售**」（张力已登记：该物品眼下**只收不卖**、唯一去向就是卖给 NPC ⇒ 这句按**前瞻提示**读；船长 2026-09-16 未要求改写）。**⑦验证**：typecheck 四包 0 错 · core **150 文件 / 1615 用例** · `content:check` ✅ · `docs:index --check` ✅ · `ui:rot-check` ✅ · 双 build ✅；**负向验证两处各自精准**：把 `unreleased: true` 加回去 ⇒ 用例恰好 1 例红 ＋ 体检精确点名 15 件「仍标着 unreleased」。**【已验收 · 已合入 main · 未推送】**
- 2026-09-15：**贵重品货柜内容物提价（船长：「奢侈品货柜的价值（开出的东西）太低了」 · 二号 · d2）**——**①船长原话（照抄）**：「**奢侈品货柜的价值（开出的东西）太低了**」⇒（三选一 + 占比追问后，船长自定口径）「**单价差距提高（10/40/200万），数量上下限拉大到5~30，箱价 = 内容期望 ×0.25**」＋「**占比保持 50%**」。**②改动（四个数 + 一条折扣）**：奢侈品三档 `lux-1/2/3` **2.4/4.8/9.6 万 → 10/40/200 万**（拉开 4 倍与 5 倍，均价 5.6 万 → **83.33 万/件**）；件数 `WORMHOLE_VALUABLES_UNITS_MIN/MAX` **10~20 → 5~30**（均值 15 → **17.5 件**）；贵重品货柜箱价 `box-valuables` **50 万 → 365 万**＝内容期望 **1,458.33 万 ×0.25**（**本箱专属折扣**，其余三类柜仍是 ×0.6 —— 所以它现在是最划算去拆的箱子）；物品卡与市场行两处同步（`items.ts` + `marketCatalog.ts`）。遗迹掉落占比 **保持 50%** 不动；军用柜、安全货柜、谜质、残骸堆一律未动。**③量级对照**：一箱内容期望 **84 万 → 1,458 万（×17.4）**，箱价 **50 万 → 365 万**；按层 4 遗迹格均值 3.17 格算 ⇒ 约 1.1 个贵重品柜/层，该档货值从 ~0.92M/层 提到 ~**1,604 万/层**（= 1.1 × 1,458 万）。**④契约加三条哨（本次顺手补的空白）**：`content:check` 的「战利品扩充契约」把件数哨改成 5/30，并新增 **⑧ 贵重品箱价 = 内容期望 ×0.25（±1%）** 与 **⑨ 两个新货柜"物品卡价 = 市场行价"＋军用柜"箱价 = MK3 期望 ×0.6（±1%）"**——后两条正是补批 A 那次"卡改了价、市场行没跟"的教训；读数行新增「贵重品货柜读数」「军用备货柜读数」。**⑤负向验证（两处 · 各自精准 · 复原即绿）**：a) 市场行价改回 50 万 ⇒ ⑧ 报「箱价 500,000 ≠ 内容期望 14,583,333 ×0.25 = 3,645,833」**并同时**报「卡价 ≠ 行价」；b) 物品卡价改回 50 万 ⇒ ⑨ 报「box-valuables：物品卡价 500000 ≠ 市场行价 3650000（同物两价）」。**⑥同步**：词典八之二「战利品四件」的奢侈品/贵重品柜两条改写；公告待审稿（经办一号）的备注与第 2 条注记按新数值更新（**公告文案只需说"一批奢侈品"，不必写具体数**）；用例只改了三处文案（断言走常量，自动跟随）。**⑦五闸门**：typecheck 四包 **0 错** · core **150 文件 / 1,611 例** · `content:check` ✅ · `ui:rot-check` ✅ · 桌面构建 ✅。**【已推送 origin/main】**

## 封存卷索引（更早批次已移入 `docs/archive/`，点开即读）

| 卷 | 收录日期 | 条目 | 体量 |
|---|---|---|---|
| `docs/archive/roadmap-2026-09-04.md` | 2026-09-04 | 28 条 | 13 KB |
| `docs/archive/roadmap-2026-09-05.md` | 2026-09-05 | 12 条 | 12 KB |
| `docs/archive/roadmap-2026-09-06.md` | 2026-09-06 | 1 条 | 2 KB |
| `docs/archive/roadmap-2026-09-08.md` | 2026-09-08 | 18 条 | 15 KB |
| `docs/archive/roadmap-2026-09-09.md` | 2026-09-09 | 45 条 | 46 KB |
| `docs/archive/roadmap-2026-09-10.md` | 2026-09-10 | 71 条 | 169 KB |
| `docs/archive/roadmap-2026-09-11.md` | 2026-09-11 | 81 条 | 237 KB |
| `docs/archive/roadmap-2026-09-12.md` | 2026-09-12 | 76 条 | 173 KB |
| `docs/archive/roadmap-2026-09-13.md` | 2026-09-13 | 95 条 | 265 KB |
| `docs/archive/roadmap-2026-09-14.md` | 2026-09-14 | 124 条 | 397 KB |
| `docs/archive/roadmap-2026-09-15.md` | 2026-09-15 | 33 条 | 115 KB |

> 上面每卷都是**原文冻结件**：不重写、不摘要、不追加。`docs/archive/README.md` 另有"已办结待办单"卷。
