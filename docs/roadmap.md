# 后续工作流备忘（Roadmap / Backlog）

> **2026-09-15 改版**：本文件只留三块 —— **① 待办活面**（置顶单 / 待办段 / 交接开放项）· **② 最近 20 条批次条目（滚动窗口）** · **③ 封存卷索引**。
> 更早的批次条目已**原文封存**进 `docs/archive/roadmap-YYYY-MM-DD.md`（一字未改，含船长原话照抄）——查老决策先看索引表、再 grep 对应卷。
> 封存动作 = `npm run docs:seal`（滚动窗口之外自动进卷，幂等；`--dry-run` 先看计划）· 封存区规则见 `docs/archive/README.md` · 全仓文档总索引 = `docs/INDEX.md`。

---

## 置顶：船长测试反馈改动单（2026-09-04 加入，按列序逐条推进）
**执行规则**：批次 A = 无歧义可直接实现；批次 B = 需先给设计总结等确认（大系统/结构级/规则数值）。
- **T1~T11 全部 [✓] 办结**（2026-09-15 逐条核对后原文移出）⇒ 全文见 `docs/archive/roadmap-todo-done-20260915.md`；本节留段头，新的船长反馈单另起一段。

## A. 体验走查 + 打磨（建议优先，收口"内容感"）
- [ ] A1 船长通读悬赏文案与界面观感（2026-09-05 注：需船长走查后按条关闭；2026-09-04 首轮已走查并清理过一批）
- [ ] A2 信息密度检查：星图（安全色 + ⚔悬赏徽标 + ′标称 + 名称是否拥挤）、悬赏列表排序/卡片密度、矿带卡布局细节
- [ ] A3 定版存档策略（2026-09-05 注：需船长确认正式档基线后关闭）

## B. 玩法闭环延伸（需确认流程的大方向）
- **本段 4 项全部 [✓] 办结**（2026-09-15 核对后原文移出）⇒ 见 `docs/archive/roadmap-todo-done-20260915.md`。
## C. 战斗与舰船系统余项
- [ ] **C3 V18 立项：EVE 式高/中/低槽重组**（2026-09 船长拍板"V17 收尾、高低中槽立项 V18"；
      **2026-09-04 交接：实施由大鲸鱼二号负责**，一号不再触碰；**2026-09-04 设计总结已确认**
      （docs/design/v18-slots.md：复数安装/数量制/槽位映射/Q4 布局草案 19 船表 + 高槽无人机
      家族 6 件——甲板扩展/战术导控，装备 48→54）——**V18A 实施已完成（2026-09-04，core 301 tests）**；
      **C4 校准轮（多炮/多矿枪/无人机件复核）与本里程碑联动，另行收尾**）：
      船体槽位布局按 role 族差异化（武装多高槽/装甲多低槽等）+ 模块归属高/中/低 +
      槽位数量/布局制（**炮尺寸制已随 V18 口径取消移除，无尺寸位**——变更记录见下）；
      fitted 结构迁移一次做干净（存档 17→18；旧六槽原位映射 high=[炮,矿] mid=[盾,推] low=[甲,货]）；
      V17 参数（乘入抗性/CPU 档位/推进代价）与槽制正交、届时零返工
      （见 docs/design/v17-modules.md §预留；v10b-combat-data.md 原"留战斗阶段"的旧账在本里程碑清偿；
      2026-09 设计问答已收：Q1 复数安装 / Q2-final 数量制不加尺寸位 / Q3 槽位映射 / Q4 布局草案）
- [ ] **C6 无人机 vs 炮位 DPS 比例评估**（2026-09-04 船长确认 V18 时追加：**延后**——武器体系
      全面后（V18B 炮族差异化 + 导弹架之后）再评估无人机流与炮击流的高槽伤害比例；
      无人机件数值（+15/35/70 m³、+12/25/40%）为暂定初值，届时可调）
      **2026-09-10 评估完成（二号；`docs/design/c6-drone-vs-gun-20260910.md` + 正式工具
      `tools/drone-vs-gun.ts`，`npm run battle:drone-vs-gun`）**：王鲭级满配同船对比——
      名义 DPS 无人机流 = 炮流 **81%**（每高槽名义反而 121%），但有效 DPS（命中修正）**48%**、
      每高槽有效 **73%**；成因 = ①命中链缺口（无人机固定命中 0.6，火控/索敌对无人机无效；
      炮台 ≈0.98）②支援链缺口（稳定器/射速/索敌只作用炮台，炮流有效 +33%）③CPU 双计
      （装置 + 每架，D3 占 310/320）④高槽不产生本体火力。实战：威胁 96 穹顶守卫
      纯炮 100%/76s/58% vs 无人机 D3 0%/74s（被击毁）；加目标锁定 MK3 后 D3 升到 60%。
      情景推演：单靠"导控 +60% + 无人机 CPU×0.65"有效仅到 68%、硬卡 40%——
      **须动命中链/支援链才能拉平**。
      **2026-09-10 第一轮调整已实施（船长拍板）**：无人机命中/衰减下放机型本体
      （`ItemDef.hitRate/falloff`）——**蜂鸟/赤鸢/猎鹰：命中 75% 且不随距离衰减**（falloff 1）；
      **雷鸥哨戒：命中 110%（用意 = 抵掉部分衰减）+ 保留正常衰减**（falloff 0.35）；
      combat 读本体（旧档兜底 0.6/0.35）、命中公式零改动；UI 无人机详情/手册新增「放飞命中」行；
      新增 `tests/drone-hit.test.ts`（6 项）；glossary 增词条「无人机命中与衰减」。
      **改后实测（王鲭级）**：有效 DPS@2km 48% → **70%**（D3 184 vs 炮流 263）、
      每高槽有效 −27% → **+5%**；实战 D3 反超炮流（穹顶 **100%/65s** vs 76s、坟场 27s vs 45s、
      噬口 41s vs 50s），但无人流行残血偏低（散火击杀节奏慢；装目标锁定 MK3 后 35%→57%）。
      **待船长判断是否回调**（旋钮：导控 +40%→+25% / 三型命中 0.75→0.70 / 无人机每架 CPU 上调）。
      **2026-09-10 船长裁定：先保持现状**（回调未实施，数值维持本行口径），由船长实机实测后再定。
      **2026-09-10 船长追加：无人机专用舰高槽 −2**（梭鱼级 5→**3**、王鲭级 6→**4**；其余带机巢
      舰船不动）——`ships.ts` 两行 + 旧档由 repair 链尾件退库（不丢装备、清单保留）+ 新增
      `tests/drone-ship-slots.test.ts` 钉布局与旧档兼容；校准行 D1/D2 改三槽、新增 D2b
      （槽位吃紧后单件导控升 MK3）、D3 四槽恰好占满；工具与测试存档同步。
      **复跑结论（重要）**：削槽对**相对关系是净利好无人机流**——王鲭炮流 6 门→4 门
      名义 272→**183**（−33%），而无人机流 D3 本体只占 4 槽、名义 220 **不变**；于是
      无人机流/炮流名义比 81%→**120%**、有效 DPS@2km 比 70%→**105%**；
      实战王鲭 4 槽对比：无人机 D3 穹顶 **65s** vs 纯炮 104s、坟场 27s vs 68s、噬口 41s vs 76s。
      梭鱼 3 槽：无人机 D2b 有效 107 vs 纯炮 3×MK3 120，实战仍更快（穹顶 85s vs 157s，但 0% 胜）。
      原因 = 无人机流火力来自机群（受舱容/CPU 约束，不吃槽位），炮流火力随槽位线性下降。
      **若目标是削弱无人机流，杠杆是 CPU / 舱容 / 战术导控，而非高槽数量**（详见设计稿 §九）。
- [x] **无人机四型定位 + 「无人机可被击落」**（2026-09-10 船长拍板，**本批已完成**）：
      **① 四型定位契约已确立（已实施）**——侦察机闪避最高装甲最薄 / 战斗机全属性平均 /
      攻坚机血量最厚·单发高·闪避最低 / 哨戒机射程最远·单发最高（狙击）·血量与侦察机相仿；
      现有四型按此调整（**蜂鸟闪避不变 0.45**；赤鸢 0.40→**0.25**、血 34→40；猎鹰 0.32→**0.10**、
      血 66→**97**、抗性装甲向；雷鸥 0.25→**0.18**、血 105→**23**、抗性削到单条）。
      契约**可执行化**：新增 `data/droneRoles.ts`（`DRONE_ROLE_SPECS` 四档区间 + 逐机/跨类阶梯校验），
      `content:check` 与 `tests/drone-roles.test.ts`（7 项）双重守卫——**新无人机越档即被拦下**；
      glossary 增词条「无人机四型定位」；设计稿 `docs/design/drone-roles-20260910.md`。
      注：`DroneDefense` 目前**不进战斗**（只有详情/图鉴/手册显示），故本批**不改胜率、校准矩阵不变**。
      **②「无人机可被击落」已实施（船长选乙：永久损失/耗材流）**：敌方**近防炮**——**威胁 ≥ 60**
      的敌舰才装，每舰**每 0.5 秒判定一次**，随机挑一架**正在攻击的放飞无人机**（**哨戒机不被打**），
      按 `pdAcc − 机型闪避` 掷命中、命中按 `pdDmg` 走机型三层抗性，打空即击落并停火；
      **不看距离**（"毕竟你要飞过去"）、**不参与**敌舰对玩家的常规攻击、单场最多击落 50%；
      **战后从无人机舱清单永久扣除**（主控远征胜/败/撤退 + 遭遇战 + AI 副船全接线），
      事件日志 + 在线 toast 提示，重复清剿在本场战损过半时自动停环提示补货；
      手册/物品说明/装配页删除"不消耗、不被击落"并改为近防炮口径；测试 `tests/drone-loss.test.ts`（5 项）；
      新增调参工具 `npm run battle:pd-tune`（支持 `--acc/--dmg/--cap/--period` 传参试档）、
      `drone-vs-gun` 表 3 增战损列。
      **实测（满技能 5 种子，王鲭 D3 满编；合法且符合流派的配装）**：软卡交火短（27~41s）战损近 0；
      硬卡穹顶（96）约 **3.2~3.6 架且全是攻坚机**（哨戒机免疫）；硬卡仍 **100% 胜**——
      因编队火力核心（6 架雷鸥）不受近防炮威胁，压力在"打得更久 + 要补货"。
      战损估值占该卡奖励约 1.1~1.2%。
      **数值已确认（2026-09-10 船长）**：「如果没有特意说明，默认按 0.5/5 来算」——
      未特意说明的敌舰一律 **0.5 秒/舰判定 · 命中 0.5 · 每发伤害 5**（逐舰独立、上限 50% 保留）。
      **默认档实测（王鲭 D3 满编）**：噬口(80)/坟场(88) **0 战损**；穹顶(96) **2.2 架（全为攻坚机）**、
      战损估值 1.1 万 ISK ≈ 该卡奖励 **0.7%**；三卡仍 100% 胜——因哨戒机免疫且编队火力核心是雷鸥，
      压力落在"打得更久 + 攻坚机要补货"。调参参照档（0.45/7 → 3.6 架；0.30/12 → 3.2 架；
      0.55/3 → 0 架）见设计稿 §七。
      **2026-09-10 船长追加**：**非哨戒无人机全部被摧毁后，近防炮转而攻击哨戒无人机**——
      哨戒机的"豁免"变成有条件的（靠同队其它机型当肉盾）；已实施 + 测试覆盖
      （`tests/drone-loss.test.ts` 新增"非哨戒机全灭后转火"用例），重档实测可见效果
      （acc 0.5 / dmg 10：噬口与穹顶在攻坚机全灭后哨戒机开始被击落）。
      **2026-09-10 船长追加（无人机舰专属加成）**：无人机舰的 `powerBonus` 只喂炮台、对机群无效
      （王鲭 0.6 与长尾鲨同级却吃不到机群）→ **削火力 + 加专属**：新增船体字段
      `ShipDef.droneDmgBonus`（只乘入放飞无人机单发，与战术导控/作战学乘算）；**王鲭 powerBonus
      0.6→0.3 且 +12%**、**梭鱼 0.45→0.25 且 +8%**。实测：王鲭 D3 名义 220→**245**、
      同船炮流 183→**149**（−19%，实战 104s→120s）；梭鱼 D2b 132→142、炮流 126→109；
      近防炮战损 2.2→**1.4 架**（机群更强→交火更短）。测试 `tests/drone-ship-bonus.test.ts`。
      **2026-09-10 船长追加（机舱联动）**：**卸下「无人机甲板扩展」后，超出机舱的无人机也要卸下**——
      新增 `trimDroneLoadToBay()`（按清单逆序卸下、退回仓库、写日志 + 一次性提示），
      接在 `unfitAt`（UI 传 ctx）与 `repairDeprecatedModules`（旧档槽位对齐裁掉甲板件）之后；
      测试 `tests/drone-bay-trim.test.ts`（5 项）；手册「装配」条目补口径。
      **2026-09-10 船长追加（损坏与回收 + 无人机线技能）**：①**取消单场 50% 击落上限**——
      战斗内机群可 **100% 损坏**；②**战后回收**：损坏机体按 `10% + 8%/级（回收学）`、满级 **50%**
      **四舍五入**找回并**回无人机舱清单**，未回收部分才永久损失（日志/toast 写明 损坏/回收/净损）；
      ③新增**五条无人机线技能**（战斗组）：无人机打击学 rank4 单发 +4%/级、无人机耐久学 rank2
      三层血 +4%/级（满级 **+20%**）、**无人机强化学 rank4 三层血再 +6%/级（满级再 +30%，与耐久学乘算，双满 ×1.56）**、
      无人机回收学 rank3 回收率 +8%/级、无人机规避学 rank4 闪避 +2%/级（满级 +10%）；
      数值常量集中在 `combat.ts` 的 `DRONE_SKILL`。测试 `tests/drone-skills.test.ts`（6 项）+
      `drone-loss.test.ts` 改口径；词典增词条「无人机线技能」。**实测**：默认近防炮档不变
      （软卡 0、穹顶 1.4 架）；重档（0.5/10、0.5/20）可见**整队 10/10 损坏**且胜率掉到 0~40%——
      取消上限后的真实风险与"耐久/强化/回收"技能的价值都在这里体现。
      **2026-09-10 船长二次修订**：耐久学**拆成两条**（rank2 基础档 + rank4 强化学进阶档，
      **乘算**叠加）；**其中一条满级改为 20%**——现行 = **rank2 耐久学 +20%（+4%/级）/
      rank4 强化学 +30%（+6%/级），双满 ×1.56**（越难练收益越高）；**规避学满级由 +20% 降为 +10%**（+2%/级）。
      **2026-09-10 玩家反馈修复（悬赏奖金口径）**：反馈"卡片赏金与实际到账不一样"——根因
      ① 结算带 ±15% 浮动（`rewardJitter`）而卡片展示名义值；② **赏金任务（窝点）卡漏乘
      "赏金猎手学"系数**。船长按甲案拍板：**取消奖金浮动**（rewardJitter 0.15→0）——展示=到账，
      唯一随机奖励 = 情报彩蛋 +10%（触发即写日志）；并修掉赏金任务卡展示漏项。
      测试 `tests/bounty-reward-display.test.ts`（4 项）；词典增词条「悬赏奖金口径（展示=到账）」。
      **口径修正（自检）**：早期工具给王鲭配了 3 中+3 低支援件（超出 2/2 布局）导致结论失真，
      已修正并统一为合法配装。
      表现层（击落演出/战报行）待三号对接，引擎侧字段契约见设计稿 §八。
- [ ] **C5 炮族差异化 + 导弹架**（2026-09 船长审核批注：同 MK 三系炮"只差弹药、无性格差异"——暂定稿可玩，**与导弹版统一做**；
      **2026-09-04 船长重定向：按伤害类型设计真武器形态（质量炮=动能 / 导弹=爆炸 / 激光=能量），
      旧三系同参炮 = 临时填充数据，逐批替换——V18B-1 导弹架 ✅ 已完成**（docs/design/v18b-missiles.md：
      追踪命中不随距离衰减的远程爆破 + 近盲安全射距；原高爆炮 1:1 迁移；爆破导弹弹药归位）；
      **V18B-2 激光炮 ✅ 已完成**（docs/design/v18b2-lasers.md：必中光束 + 距离衰减作用于威力
      （幅度减半 50%）+ 消耗能量弹药；能量炮/异星原型 1:1 迁移激光；per-gun 多键预载完成
      ——混装异型不再饿死；core 345 tests）：
      **V18B-3 ✅（2026-09-05 船长确认完成）** = 动能炮 → 质量炮形态（届时确认是否改名），并联动 V18 槽制
      （炮尺寸制已随口径取消移除，见变更记录）；
      v17b-weapons.md §已知限制已记录

## D. 经济/数值精调
- [ ] D1 balance（2026-09-05 甲案已做：体检工具扩"总资产口径"——钱包+舰队/已装模块+仓库/书架按市场常驻 base 折算，重跑 24h 三策略：钱包/总资产 A 934k/2067k、B 410k/1589k、C 312k/1499k；B/C 钱包低估约 1.7~2.2×，总资产差收窄至 A 的 73~77%，缺口主要来自技能训练时间与蓝图书沉没而非倒挂。是否需数值拉平待船长定目标）
  **2026-09-08 大改批后重跑（平衡-check，24h 30s 步长，同口径）**：钱包/总资产 A 原矿流 1,127k/**2,278k**（鲸吞级）、B 精炼流 577k/**801k**（掘洞级）、C 装备流 607k/**844k**（掘洞级；造 MK1/MK2）；B/C 总资产仅 A 的 **35~37%**（旧 73~77% 反转收窄）；双修贸易变体 24h 内技能未练成、与基础线同值。**状态（2026-09-08 船长定）：后续定位/拉平挂起——船长将调整技能体系（技能直接影响收益），待技能改完重跑再定目标；本行 09-08 数字保留为改前基线**。
  **2026-09-09 技能/工业大改后复跑（O3；工具修正后）**：总资产 A 原矿流 **2,293k**、B 精炼流 **2,338k**、C 装备流 **2,663k**（三线均鲸吞级；钱包 1,128/1,150/1,433k）——B/C 从 A 的 35~37% → **102%/116%**（技能投入有回报、装备流最优，A 原矿流与基线持平 +0.7% 未受损）；双修贸易变体仍 24h 内练不成、与基础线同值。**工具修正（balance-check.ts）**：船分级后沙猫/掘洞同 T1，原"目标 tier > 当前 tier"升级门槛卡死 12 万换掘洞（A 流伪影性卡在 89 万买不到鲸吞、总资产仅 91 万）——改按升级梯子内位置比较后恢复真实路径（三线均按 沙猫→掘洞→鲸吞 正常滚雪球）；里程碑/每小时曲线干净。**待船长定目标带**（B/C 已反超 A 是否到位，或需微调）。

## E. 基础设施
- [ ] E2 云存档 / 在线化：架构预留（决策 1/2/5），未启动；Phase 2/3 见 architecture.md

## 2026-09-08 一号交接开放项（来自临时交接卡 docs/design/handoff-2026-09-08-current.md，已按约定迁移后删除）
- [ ] **O1 组装机卡「手动 vs AI」两档净/h**：现为单一手动口径（二号 A 项）；手动/AI 基础核心档分列估算待船长拍板。
- [ ] **O2 奇货卖出侧溢价巡游**：挂价高于收购价线的奇货卖单仍可能被巡游采购按挂价成交（买入侧已 20L 收口，卖出侧未动）；处理前先问口径。
- [ ] **O3 balance-check 24h 策略复跑**：工业扩容（工位数大增）后未复跑；范围先问船长。
- [ ] **O4 顶部 AI 徽标计数与「执行中 N」偏差**：早期玩家反馈，根因未实证。
- [ ] **O6 二号船分级批次验收（已在 main）**：舰船尺寸分级 + 掠食者巡洋线 A1–A9；公告待审稿在 6b79a61（id 2026-09-09-ship-size-tier）未经船长批准不得写入 announcements.ts；巡洋 T3 数值/渠道声望待船长终审（涉二号分工，一号只读 ship-size-tier-rework.md）。
- 说明：O5 数值文档分层 = 现数值以 docs/design/manufacture-refine-worker-econ.md 与 docs/glossary.md 为准（refine-balance-50.md 为历史锚）；O7 真档验收状态 = 2026-09-08 引擎级实测探针 20/20 通过（临时探针已删），UI 目测项待船长开游戏确认（市场蓝图 chip/奇货低挂提示/AI 指挥中心 +N 工业/交付航线按钮与弹窗/工业页数值行/公告三条）。

## 2026-09-09 一号收尾开放项（收尾盘点后登记，待办照旧先读本段）
- 备忘（2026-09-09 自交接卡迁移）：**精炼净收益差异问题 = 船长延后**——差异来源 = 吞吐桶 ×8.7 × 矿价 ×37.7 ≈ 卖原矿差 328×，精炼净再 ×净率差；重开先回看 refine-cycles.md 头注与 content-csv 工作簿 items sheet。
- [ ] **P0-2「开战距离 / 先手」提案 + 预演（船长 2026-09-11 选定本批 =「先出提案 + 预演，审核后再落码」；二号）**——**本批零引擎、零数值改动，产出 = 提案稿 `docs/design/p0-2-engagement-20260911.md`（含全部实测读数）**。**①三条决定性事实**：(a)「够不着」= **距离拔河的胜负**——同一张蜃影、同一把 4×动能MK2，**带推进器 A0 = 100%/65s 闭合到 3.3km**，**仅去掉推进器 = 0%/140s、全程停在 9.99km、我开火 0.0**；(b) **教学卡「敌人一炮未放」与开战距离无关**——开局从 6314m 缩到敌人带内（1980/1500m）敌人**仍只放 0.2 炮、战斗缩到 1s**，敌人血抬到 **×12（264）** 也**依然 0 炮**（根因 = 交战距离停在玩家期望位 3.2km 而敌人带只有 2.2km ＋ 玩家先手一轮清场）；(c) **全局改法「开战距离 = 双方射程重叠区上沿」不成立**——全卡清点 **78 格中 77 格**被改动（含旧路径 20 张）⇒ 打穿「旧路径逐字不变」判据。**②病灶 A 实测（kite 卡，5 播种 1/7/13/29/51）**：蜃影 T48 的 A1/⑤/③ 三行现状 **0%｜140s｜我 0.0｜敌 136**；候选甲（可及性钳制：敌期望交距 ≤ 我射程×0.95）⇒ **20%/100%/80%**、我开火 28~40 次，但**连带把 A0 行从残 32% 拉到 58%**（= 船长刚裁定「保留」的那一行）；灰霾 T28 三行 **0%/600s ⚠触顶 5/5｜敌 584** ⇒ 甲后 **100%/36~59s**；赤潮 T34 三行 **0%/373s｜敌 364** ⇒ 甲后 **100%/41~64s**；**旧路径对照卡「幽灵舰信号」四行全 100%**（从未够不着 ⇒ 差异只来自舰级路径按"自身带×0.85"算站位）。**③候选四选一（甲乙丙丁）**：甲 全局可及性钳制 / **乙 逐卡覆写（复用既有 `rangeMinM/rangeMaxM` 旋钮，不新增字段；建议先做灰霾+赤潮、蜃影单独议）** / 丙 只做可读性（提前脱战 + 战报写明"够不着" + 卡面预告交距）/ 丁 不动；戊（全局改开战距离）已实测淘汰。**④顺带发现（建议先修）**：`battle-calibrate` 的**敌开火(头目/杂鱼)**列用 `b.fx.slice(seen)` 计数，而 `fx` 是 **48 条环**（`pushBattleFx` 超长丢最旧）⇒ **环满即停、长战全漏**：同格真值 **584.0**（头 145/杂 438）↔ 工具法 **48.0**（漏 92%）、蜃影 A1 136 ↔ 48、蜃影 A0 54.0 ↔ 43.2（= 蜃影稿 §2.1 记的 10.8/32.4 正是此口径）；**我开火列不受影响**（工具用 `stats.meShots` 权威累计）；修法 = 按 `BattleFx.seq` 续读（一行级）。**⑤四问待裁**：病灶 A 走哪条路 / 若走乙点名哪几张卡 / 要不要一并修工具漏计 / 教学卡「一炮未放」是否单列。**⑥验证**：本批**不改代码**，故只跑读数工具（`npx tsx _probe-p02*.ts`，两个探针**用完即删**，`_` 0 个）；提案落码时再走四连 + 重跑 A0~A3 与六组。**改动全在 d2 工作树，未合入 main、未推送。****【提案 · 未落码 · 待船长裁决】**
- [ ] **★★ 挂账①「低威胁卡敌人一炮未放」+ ②「名义火力 / 实际火力脱节」（2026-09-11 船长要求处理；二号）**——**①实测形态**：边境/碎晶（威胁 12/20）在 A0/A3 参考行**敌人全程 0 开火**（我开火 11~12 次、18~25 秒清场）——根因 = 敌军自带带太短（快艇 1~1883、护卫舰 326~4275），而玩家参考行站 3.2 km；**候选**：甲 逐卡给敌军**加宽射程带**（让其进入自己的交战带）· 乙 逐卡**开战距离覆写**（把开局放进敌军带内，实测 1500~1980m 时敌人会开火 1.0~4.2 次）· 丙 给低威胁卡**先手**（开场即开火）。**②实测形态**：纸面单发与实际输出脱节（例：赤潮头目 beam 必中单发 21 ↔ 灰霾头目 fixed 单点 59，两者实收相近）；**候选**：甲 单发按「名义 DPS × 装填 × 实收命中」**反推**（纸面 = 实收，须重标全部 26 卡）· 乙 只做**登记口径**（在词典写明"纸面 ≠ 实收"，不重标）。**两份提案待船长选型后再落码。****【提案 · 未落码】**
## 最近批次（滚动窗口 · 最新 20 条）

> 越窗即封存（`npm run docs:seal`）；窗口大小 = 20 条，需要临时调大用 `--window=N`。

- 2026-09-15：**虫洞基础回合数 60 → 100（船长一条 · 一号 · main）**——**①船长原话（照抄）**：「**虫洞基础回合数提高到100**」。**②四问四答（全取推荐）**：**(a) 系数** = **0.53 不动**（只改基础 ⇒ 轻编队 +37 / 最重可行编成 +19 回合，轻重差幅 1.93 倍 → **1.94 倍**，「越重时间越短」强度不变）；**(b) 谜质「时序核心」** = **仍 +10 回合/台**（相对价值由 4×T3 的 31.3% 降到 18.9%，登记不动）；**(c) 老趟** = **只对新跃入的趟生效**（`turnsTotal`/`turnsBase` 已锁定在 `run` 里 ⇒ 存档零迁移、零额外代码）；**(d) 目的** = **整体放宽**（威胁曲线/盘面/谜质一概不碰，深层强度另案）。**③改动**：`wormhole.ts` 常量 **60 → 100** ＋ §二段标题/公式注/`wormholeTurnBudget` 注的沿革与读数（**55 → 60（09-14）→ 100（09-15）**）· 手册《编队与质量》硬编码数字「4 艘护卫舰 **56**／4 艘巡洋舰 **32**」→「**93 / 53**」· 把旧值钉住的 **4 个用例文件**（`wormhole-admission` 编成表 8 行 ＋ 边界「空载 100 / 满载 floor(100×0.47)=47」＋ 用例标题 · `wormhole-run` 3 处 4×T1 = 56→93 · `wormhole-pickup` 2×T3 = 46→**76** ＋ 注释 · `wormhole-battle` 注释 46→76）· `tools/make-test-save.ts` 注释（4×T3 = 32→**53**）。**④逐格新表（引擎复算，现行列与引擎现值逐格吻合）**：4×T1 2,000 **56→93** · 3×T1+1×T2 3,000 54→**90** · 4×T2 6,000 48→**80** · 2×T3+2×T2 10,000 40→**66** · 3×T3+1×T1 11,000 38→**63** · 4×T3 14,000 32→**53** · 2×T4 14,000 32→**53** · 2×T4+1×T2 15,500 **29→48** · 空载 60→**100** · 满载 28→**47**。**⑤A/B 读数（`npm run wormhole:econ --runs=20` · 同 20 趟种子 · 同政策 · 同编队 4×T3 长尾鲨满配 · 只差这一个常量）**：**基础 60（32 回合/趟）** ⇒ 稀有残骸 **0.95 件/趟** · 撤离 **19/20** · 平均第 **2.3** 层 · 存活 **3.60/4** · **终局余 0.3 回合** · 平均动作 **32.5**（扫 9.3/走 13.7/打捞 4.8/采集 2.4/交战 2.4）· 收集 1,137,442 ⇒ 到手 1,110,729 ISK（到手率 98%）；**基础 100（53 回合/趟）** ⇒ 稀有残骸 0.65 件/趟 · 撤离 **13/20**（＋2 全损 ＋**5 趟"未结束"**）· 平均第 **2.8** 层 · 存活 **3.05/4** · **终局余 13.0 回合** · 平均动作 **41.5**（11.3/17.3/6.2/3.1/**3.3**）· 收集 1,558,052 ⇒ 到手 1,130,400 ISK（到手率 73%）。**⑥本轮最值钱的发现（修正一条旧结论）**：**「回合不是瓶颈」是线性节点时代的读数**（2026-09-14 那次终局余 5~7 回合）；**F3a 网格化后每个动作（扫/走/打捞/采集）都吃 1 回合** ⇒ 现行 32 回合下**终局平均只剩 0.3 回合**、政策被回合逼停 —— 所以这次加到 100 才**真的有内容可动**：多出的回合被用掉约 **9 个动作/趟**（32.5 → 41.5）、走到层 3 的样本 **5/20 → 15/20**。**但"加回合 ≠ 变简单"**：走得更深 ⇒ 交战 2.4 → **3.3** 场、存活 3.60 → **3.05/4**、撤离率下降；稀有残骸/趟 0.95 → 0.65 里**大半是读数假象**（5 趟被工具政策卡住、收集物不计入到手；**收集量其实持平，20 件/20 趟**）。**⑦负向验证（精准 · 复原即绿）**：把常量改回 60 ⇒ **恰好 5 例红**（`wormhole-admission` 编成表「4×T1 回合预算 expected 56 to be 93」＋ 边界「expected 60 to be 100」· `wormhole-pickup`「expected 46 to be 76」· `wormhole-run` 2 处「expected 56 to be 93」）。**⑧五道闸门**：typecheck 四包 **0 错** · core **145 文件 / 1560 用例全绿** · `content:check` ✅ · `ui:rot-check` ✅ · 桌面 ＋ 网页 build ✅。**⑨文档**：设计稿 `wormhole-extraction-endgame-20260912.md`（§一 12 项表 · §三「最轻 ~93 / 8,000 档 ~73」· **§4.3 公式与编成表 8 行 ＋ 校验差幅 ＋ 标定留档** · §4.4 编成表回合列（53/51）· §11.3 那句"当前实测 26~51"标成**当时口径**并指向新读数 · **新增 §4.6「基础 60 → 100 的整趟 A/B 读数」** · §Q4 决策表）· `wormhole-fleet-optimum-20260913.md`（公式那行原写着**过期的 55** ⇒ 一并归位到 100；编成表回合列 51/44/29/32/26 → **93/80/53/60/48**；§4.1 逐条读数标注**当时口径 = 预算 29**）· 词典八之一「回合预算」条（新表 ＋ 基础沿革 ＋ 时序核心相对价值）· 本条目。**⑩登记的两条既有问题（本批均未动，等船长裁）**：**(a) 工具缺口**：`wormhole:econ` 整趟政策**不处理"临时空间"** ⇒ 深层多拿谜质/物品时 5 趟卡在「临时空间里有 N 件没处理」（读作"未结束"、收集不计到手），会系统性低估基础 100 的产出 —— 修法 = 给政策加一步"清空临时空间"（属工具改动，另批）；**(b) 行尾存档异常**：全仓只有 `packages/core/src/wormhole.ts` 与 `types.ts` 被 git 按**非文本（`-text`）**存档（blob 里存的是 CRLF，其余源文件都是 LF）—— 历史遗留、**diff 仍逐行干净**，但将来若有 `git add --renormalize` 会整文件重写；本批**不动**（会造出整文件 diff，且他人在并行改这两个文件）。**⑪不变量**：威胁曲线 · 盘面格数与内容 · 谜质效果 · 回合消耗口径（每节点 1 / 每多一波 +1 / 每捡一堆 +1；扫描/移动/激活各 1）**一字未动**；**不发公告**。**【待验收 · 本地提交未推送】**
- 2026-09-15：**重装线（甲壳族 3 艘）货舱 −20%（船长 · 三号 · verify）**——**①船长原话（照抄）**：「**下调重装船的货仓20%。且发现有些非重装船被分到了重装类别里。**」＋（追问「哪些算重装」后）「**只保留基础的3艘重装船，货舱修改的也是它们**」⇒ 本线的"重装船" = **基础 3 艘**（陆龟级 / 玳瑁级 / 玄武级，即 `ships.ts` 里的甲壳重装线），货舱改动**只落在它们身上**。**②改动（只动一个字段）**：`packages/data/src/ships.ts` 三艘 `cargoM3` ×0.8 —— 陆龟 **7000 → 5600** · 玳瑁 **12000 → 9600** · 玄武 **19000 → 15200**；槽位 / 三层血 / 循环与每循环产量 / CPU / 行价 / 质量 / 甲壳族抗性（甲爆炸 0.5）**一律不动**（用例逐项钉住，防后续"顺手补偿"把这笔平衡改动抵消）。**③玩家可见文案同源**：`packages/data/src/shipBlueprints.ts` 蓝图说明里的「货舱 N m³」跟着改（**玳瑁 2 处** = 常规蓝图 `sbp-hawksbill` ＋ 一次性图纸 `sbp-once-hawksbill`、**玄武 2 处** = `sbp-xuanwu` / `sbp-once-xuanwu`；陆龟级蓝图说明本就是定性描述、无数字 ⇒ 一处不动）——`content:check`「蓝图说明契约」会把说明与 `cargoM3` 对不上判红，故必须同步。**④用例**：新增 `packages/core/tests/armored-line.test.ts` **4 条**（① 三艘货舱 = 原值 ×0.8 · ② 三艘仍挂 `role: 'armored'` · ③ 其余字段逐项原值 · ④ 蓝图文案数字已同步且不留旧数字、陆龟那条不许被塞数字）。**⑤验证**：typecheck 四包 **0 错** · core **146 文件 / 1559 用例全绿**（本批 +1 文件 / +4 例）· `content:check` **✅**（蓝图说明契约、舰船价格口径、舰种契约均不受影响）；本批**纯数据 + 文案**（无 UI / 样式改动 ⇒ 第五道闸门 `ui:rot-check` 不适用），合并后重建主树产物。**⑥§5.2 冲突上报（未动，等船长裁定）**：船长同一条里的后半句「非重装船被分到了重装类别」指向**虫洞专属 6 艘**（C 族 幼虫截击舰 / 甲壳截击舰 / 巢群重型突击巡洋舰 · D 族 哨戒电子舰 / 陵卫指挥舰 / 陵寝巡洋舰，现挂 `role: 'armored'`）——把它们改成 `armed` 会让 **4 处已确认的契约/用例失效**：`content:check`「武装舰高槽 ≥ 低槽 + 1」（6 艘**全不满足**：2/3/3 · 2/4/4 · 4/3/5 · 2/3/3 · 2/4/4 · 4/4/4）、「武装舰必带 `powerBonus`」（6 艘**全无**）、「武装舰护盾 > 装甲（族定位）」（**C 族 3 艘甲厚盾薄**，全不满足），以及 core `tests/wh-ship-baseline.test.ts`「武装舰仍满足高槽契约」；连带影响 = 这 6 艘的技能从「重装舰操作（+4%/级 甲/壳容量）」换成「武装舰操作（+3%/级 武器单发）」、战斗/地图里的舰形回退剪影从"厚壳重甲舰"换成"尖翼突击舰"（这 15 艘没有专属美术资产）、`tools/wh-ships-review` 的"同档同 role 参照组"整表读数漂移。**三个可选做法**（甲 = 改 role ＋ 把三条"武装舰族定位"契约按登记表豁免这 6 艘 · 乙 = 改 role ＋ 给这 6 艘补齐武装舰三件套 = 重排槽位与三层血、会推翻 C 族"甲壳"族性 · 丙 = 新增第 5 个类别 id）已连同推荐一并报给船长，**裁定后再动**。**【待验收 · 本地提交未推送】**
- 2026-09-15：**洞内敌卡扩充 · 「甲 vs 现状」几何对照（`_` 临时探针，跑完即删）——两条结论修正了原判断（二号 · d2）**——**①做了什么**：为了给船长在 甲/乙/丙 之间拍板提供数字，写了一个 `_` 前缀临时探针（`tools/_probe-wh-reach.ts`，**跑完已删**）：**不做仿真**，只把**层 6 派生后**的 15 张卡逐张算"舰级绝对射程 · 敌期望交距"与"我方期望交距"摆在一起（参考装配 = 导弹架 MK3，射程带 1,400~14,880 ⇒ **0.8 档 = 12,184m · 0.5 档 = 8,140m**，与设计稿/roadmap 已登记的读数一致）。**②读数（判定分三类：白刷 = 我够它·它够不到我 / 对射 / 贴脸 = 它主动压上来）**：**0.8 档（现状）：白刷 9/15 · 对射 1（E 中 · 导弹残响 15,000m）· 贴脸 5（A 中 · A 深 · C 浅 · C 中 · C 深）**；**0.5 档（甲）：白刷 6/15 · 对射 4（多出 D 中 12,000 / D 深 9,600 / E 深 8,500）· 贴脸仍是那 5 张**。**③两条结论（都与我原先的猜测不同，如实修正）**：**(a) 「甲」只能缓解、不能根治白刷** —— 0.5 档把射程 7,000~12,000 的三张（D 中 / D 深 / E 深）从"白刷"翻成"对射"，但仍有 **6 张短臂卡**（A 浅 4,275 · D 浅 7,000 · E 浅 7,000 · G 浅 5,745 · G 中 6,103 · G 深 7,200）在**任何**期望档下都够不到长射程装配 ⇒ 这是**射程不对称的固有形态**（洞外悬赏线一模一样：长射程装配无伤刷短射程卡），洞内因为"**收益按层给**"才被放大成"无风险刷层"；**(b) 「墙」与期望档无关** —— 那 5 张贴脸卡在 0.8 与 0.5 下都是贴脸 ⇒ 层 7~8 的墙来自"**深层 ×1.2 血 + 全 brawl 编成把长射程装配压到脸上**"，甲/乙/丙**都治不了它**，只能靠**逐卡 `dmgMul` 配平**（动难度，须船长点头）。**④修正后的建议**：**甲（把白刷 9 → 6）+ 对两张深层贴脸卡（A 深「海盗战团」· C 深「孢群巢穴」，含 ×1.2 血）做读数驱动的 `dmgMul` 配平（治墙）**；剩下 6 张短臂白刷卡按"洞外同款"口径**不动**（不是这批新内容引入的）。**⑤边界（如实登记）**：探针是**几何口径**（不算命中/减员/自愈/时长），故只能回答"谁够得到谁"，不能替 20 播种的胜率读数；且只算了一个层（层 6）与一种参考装配（长射程导弹）——换近距装配结论会反向。**⑥文档**：设计稿新增 **§八之三「甲 vs 现状的几何对照」**（读数表 + 两条修正 + 修正后的建议）。**【待验收 · 本地提交未推送 · 仍等船长在 甲/乙/丙（是否附加配平）之间点一个】**
- 2026-09-15：**洞内敌卡扩充 · 风险①收口：新机型（孢群机 / G 三系蜂群机）的近防炮击落读数（二号 · d2）**——**①为什么量**：设计稿 §八 风险① 登记过"孢群机三层血 **88** 是敌机里最厚的一型，而**敌机血量不吃按卡归一缩放** ⇒ 近防炮可能打不动"（判据来自 2026-09-12 那次"警戒机削血 40%"的教训：当年一架都打不下来）。**②工具扩容（常驻）**：`tools/pd-vs-foe-drone.ts` 的卡表由**四张**扩到**六张** —— 加入 **`wh-alien-hive`**（C 族孢群巢穴 · 孢群机 ×3）与 **`wh-exile-line`**（G 族残军战列线 · 蜂群机 动能/等离子/爆炸 各 1），装配/技能/播种与老四张同口径 ⇒ 以后每加一个带机群的洞内卡，这条读数都能覆盖。**③读数（5 播种 · 9 行配装 · 日志 `tools/_ui-artifacts/pd-vs-drone-wh.log`）**：**孢群机**（池 264 = 3×88）—— **P1 5×近防炮 MK1：击落 3.0/3.0、池 264 → 0（19 发 / 1,909 伤）**；P3 2×PD MK1 + 3×动能：3.0/3.0 → 0；P6 **单门** PD MK1 + 4×动能：3.0/3.0 → 125；P8 单门 MK3 + 4×动能：1.8 → 106；P9 4×PD MK1 + 1×动能：3.0 → 0；**P4 对照（0 门防空）：0 击落、池 264 → 264 ✓**（没防空就打不到机群，口径正确）。**G 战列舰三系蜂群机**（池 78 = 3×26）：各配装均 **3.0/3.0、池 78 → 0**。**④结论（风险解除）**：**不打不动** ⇒ **不调整孢群机血量**（原预案"打不动就下调到 60~70"**未触发**）。**为什么 88 血反而好打**：击落看的是**机群池的总血 ÷ 每发伤害**，不是单架厚度 —— C 族是**攻坚机档、只 3 架**（池 264），E 族是 5~10 架（池 275 / 550 / 1,100）⇒ 单架最厚 ≠ 最难清。**⑤验证**：`npm run battle:pd-vs-drone` 全绿跑完（工具输出含公式面/装备面/机型面对照）；typecheck 四包 0 错 · core 全量用例全绿 · `content:check` ✅ · `ui:rot-check` ✅ · 桌面构建 ✅（本批仅工具卡表 + 文档，引擎与数据零改动）。**⑥文档**：设计稿 §八 风险① 就地标注"已读数、风险解除"并附读数。**【待验收 · 本地提交未推送】**
- 2026-09-15：**洞内敌卡扩充 · 20 播种读数与一处待裁决的口径交叉（二号 · d2）**——**①船长确认**：「**是 星髓幼虫（T1）**」（回答我 C 族浅层「星髓虫」的歧义）⇒ 落码即终稿，**零改动**，仅在设计稿 §九 把"待确认"改为"已确认"。**②20 播种读数**（`npm run wormhole:econ --seeds=20`，日志 `tools/_ui-artifacts/econ-seeds20.log`，参考编队 4×长尾鲨导弹巡满配 + 战斗系 Lv3）：节点胜率 **层1~6 全 100%**（残血 84/79/70/85/**100**/**100**%）、**层7 10%**、**层8 0%**；守卫 层1~7 100%、层8 95%；撤离 95~100%。工具判级：层 5~6 **太软**（且 **敌开火 0**）、层 7~8 **墙**；收益曲线（解析口径）逐层严格上升 ✓；每回合 ISK 51,842 → 156,273。**③根因（核过机制，不是"某张卡数值没配平"）**：洞内**默认期望交距档**在 2026-09-15 由船长口误回滚**从 0.5（中段）改成 0.8**（与星图同档，见 `docs/design/desire-band-20260915.md`），而本稿洞内敌卡是按"进入后被拉到中段（≈6,300m）"的口径配的 ⇒ 回滚后行为分成两极：**(a) 对静止/短臂敌人**——玩家停在 0.8 档（导弹 MK3 ⇒ **12,184m**），而 E 族静物残骸只有 7,000 / 8,500m、D 战列舰 9,600m ⇒ **玩家站在敌射程外把它们磨死**（层 5~6："太软 + 残血 100% + **敌开火 0**"就是这个形态）；**(b) 对冲脸敌人**——A 深卡（头目 + 快艇 ×3 全 brawl）与 C 深卡（孢群异虫 + 幼虫 ×2 全 brawl）主动压脸 ⇒ 长射程装配在贴脸距离被打穿（层 7~8 的墙）。**⇒ `dmgMul` 治不了"白刷"那一端**（它源于射程差 + 静物不能追，不是血/火力比），故本批**不擅自配平**，把三档做法摆给船长（见设计稿 §八之二 表）：**甲**（推荐）洞内恢复独立默认期望档 0.5——即把那次回滚**只限洞内**翻回来（他那句"口误"指的正是"洞内维持中段"，而它恰是 2026-09-13「进去就得挨打」的旋钮）· **乙** 不动期望档、把 E 静物残骸射程抬到 12,500~13,000（**要连带改 E 族射程带契约**「最高 7000~10000」，船长 2026-09-11 定）· **丙** 接受两极（代价：层 5~6 存在无风险刷层）。**④读数工具两处盲区（登记）**：参考编队只有长射程导弹一种装配 ⇒ "两极"是这一种装配下的形状（换近距装配会反向）；每层那一场由确定性抽卡决定 ⇒ 20 播种仍可能只覆盖部分卡，逐卡配平要用 `--card=N --depth=N` 单看。**⑤文档**：设计稿新增 **§八之二「首轮读数」**（含读数表 / 根因 / 三选项对照 / 工具盲区）。**【待验收 · 本地提交未推送 · 等船长在甲/乙/丙之间点一个再动配平】**
- 2026-09-15：**洞内敌卡扩充 · 合入 main ＋ 首轮难度读数（二号 · d2→main）**——**①合入过程**：主树此前被一号在途改动占着（`balance/combat/expedition/types` + 用例 + 两份文档），按 §4「主树不干净不合入」等了它；主树一干净即 `git merge main`（**3 处冲突**：`docs/roadmap.md` 与 `docs/development-conventions-changelog.md` **两侧都保留**、`foe-ship-path.test.ts` 的 import 块合并两侧新增）⇒ 五闸门全绿后 ff-only 进 main（`5bbf8dd6`，main 树干净）。编码三查：BOM 无 / 裸 LF 0 / 乱码 0；父提交比对：`roadmap` 对 main 侧 0 行缺失、对我侧缺 1 行 = **一号已被自己后续条目取代的那条旧「期望交距」条目**（main 侧有意改写，非遗漏）、`changelog` 两侧 0 缺失。**②合并撞出的一处语义交叉（已按族级口径收口）**：一号同日刚落地的船长裁定「**给 C 族添加全血条 25% 爆炸抗性**」（`C_FAMILY_RESISTS`，四条 C 族舰级展开引用）与我批 2 新增的第五条 C 族舰级「**孢群异虫**」交叉 —— 它的用例①断言 C 族 = 4 条、用例②对**全部 C 族洞内卡**逐单位断言三层 25% 爆炸抗 ⇒ 两条同时红。**处置 = 按族级口径补齐**（不是改断言绕开）：给 `FOE_ALIEN_SPORE_HIVE` 加 `...C_FAMILY_RESISTS`（同族同口径），并把用例①的"四条"改成"**五条**（含孢群异虫）"、注释同步；用例②零改动即通过（它本来就在守这条口径）。**③首轮读数（`npm run wormhole:econ --seeds=3`，参考编队 4×巡洋 MK2 满配 + 战斗系 Lv3）**：节点胜率 层1 **100%**(残血 86%) · 层2 **100%**(78%) · 层3 **100%**(60%) · 层4 **0%** · 层5 100%(**残血 100% · 敌开火 0**) · 层6 100%(83%) · 层7 **0%** · 层8 **0%**；守卫 层1/2 100%、**层3 0%**、层4 100%、层5 100%(92%)、层6 **0%**、层7/8 0%；撤离战胜率全 100%。收益曲线（解析口径）逐层严格上升 ✓；每回合 ISK：40,077 → 42,090 → 78,385 → 57,446 → 98,491 → 130,845 → 78,690 → 168,872。**④读数解读（如实登记，不粉饰）**：**(a)** 3 播种太少 + **新池制下"每层那一场"由确定性抽卡决定** ⇒ 只有一两个种子命中某张卡就会得到 0%/100% 的极端格（层 4 墙、层 5 太软、层 6 守卫 0% 都是这个形态），**不能据此判定某族整体失衡**；**(b)** 方向性结论只有一条站得住：**单舰深层卡（陵墓王庭 / 残军战列线 / 压境）的方差明显大于旧的多舰卡** —— 归一后全部血与火力压在一艘上，命中"墙"与"太软"的两端都被放大（设计稿 §八 风险③ 已登记此形态）；**(c)** 层 5「残血 100% 且敌开火 0」= 抽到了**静物/长臂卡**（导弹残段 15 km 或巨构系）后我方在某装配下形成"够不着/被够不着"的僵局形态，须专门核（属"读数工具对单舰长臂卡的显示盲区"）。**⑤下一步（未做 · 待办）**：**(a)** 跑 **`--seeds=20`** 取稳定读数（工具支持），逐层逐档定位是哪些卡在制造两端；**(b)** 定位后用**卡上 `dmgMul`** 配平（**不碰船长定的分层血量修正**），把逐格读数与取舍写回设计稿 §八；**(c)** 单舰长臂卡（导弹残段 15 km）与单舰深层卡的"僵局/秒杀"两端各留一条读数证据。**:arrow_right: 本条按"待验收"挂账（读数与配平未完成，故未推送 origin）**。**【待验收 · 本地提交未推送】**
- 2026-09-15：**洞内敌卡扩充 · 批 5（G 族：启用亡军战列舰 + 挂蜂群机 + 三卡）——15 张卡齐备 · 收口（船长逐轮裁定 · 二号 · d2）**——**①船长原话（照抄）**：「**G族添加战列舰动能伤害为主。**」＋「**G族战列可以添加机群。**」→（问"挂几架"）「**挂**」；编成：「**G浅层为围攻残兵舰*3，中层为残响残舰*2高层为亡军战列舰*1。**」。**②亡军战列舰启用**（foe-g-exile-battleship，**数值一字未动**：1,760 血 / 216 单发 / 实速 215 / 射程 600~7,200 / 动能 8:2（壳体原值本就"动能为主"，正是船长要的）/ orbit）＋**挂蜂群机 ×3（动能 ×1 + 等离子 ×1 + 爆炸 ×1，无后备）** ⇒ 三系齐备；**P-20a「等离子必须在场」仍满足**；**体检长期那条「已备未挂机型：蜂群机（foe-drone-g-bee-exp）」警告随之消失**（现读数：**机型表全部在役**）。沿革：船长 2026-09-12「战列舰暂时空置…先建壳体」⇒ 2026-09-15 本批启用（契约口径不改：**G 族 T4 只能是这条壳体**，只是从"无卡引用"变为"有卡引用"）。**③三张卡**：浅 **wh-exile-blockade**「亡军封锁」编成 **×2 → ×3**（船长「G浅层为围攻残兵舰*3」）/ 中 **wh-exile-swarm**「残响蜂群」（**残响残舰 ×2**）/ 深 **wh-exile-line**「残军战列线」（**亡军战列舰 ×1**）；三张一律 random（G 族族格）、隐藏卡、零赏金零掉落。**④撞出并解决的一处口径冲突（如实登记 · 本轮最值得记的一条）**：启用战列舰后**敌速口径契约首次校验它** —— G 族 speedRatio 定值 **1.05 × T4 基准 205 = 215 m/s** ⇒ 比率 **0.73×**，被 **orbit 战术带（0.90~1.25）误拦**（此前它无卡引用 ⇒ 这条口径从未被实战卡触发过）。按仓库既有先例（**B / D / E 三次同款教训：族级口径必须用族级带表达**）新增 **G 族族级速带 `[0.70, 1.25]`** 与 G 族支路（支路内同时断言 `speedRatio === 1.05`），并在汇总行与读数行单列；**连带修一处作用域坑**：两个契约各在自己的 `{}` 块里、块内 `const` 互不可见 —— 首次把 G 族常量写在其中一个块里 ⇒ 另一个块直接 `ReferenceError`（实测），现两个 G 族常量提到**模块作用域**。G 族实测读数：围攻残兵舰 357(1.22) · 残响残舰 310(1.06) · 天底封锁舰 271(0.93) · **亡军战列舰 215(0.73)**。**⑤收口状态**：**15 张洞内敌卡齐备**（五族 × 浅/中/深各一）· **4 艘新舰全部落码**（C 孢群异虫 / D 守墓王座舰 / E 导弹残段 新建 + G 亡军战列舰 启用）· 1 个新机型（C 孢群机 + 虫族 SVG）· 出场池（层1浅 / 层2~3 中2:浅1 / 层4+ 深2:中1:浅1）与分层血量修正（浅×1 / 中×1.1 / 深×1.2）自批 1 起在线；`content:check` 洞内敌卡读数 = **15 张全部 hidden 且无赏金**。**⑥五道闸门**：typecheck 四包 **0 错** · core **145 文件 / 1,556 用例全绿**（'缺档兜底'用例改写为「**十五张齐备：五族三档俱全 + 守卫取档（层1浅/层2~3中/层4+深）**」，并注明缺档兜底分支转为**防御性代码**（数据上不可达但保留，若有人把某档写回 null 会有 not-null 断言报红）；两张计数守卫 40 → **42**（= 15 张洞内卡齐备）· **content:check ✅**（**机型表全部在役**；G 族速带读数入汇总）· ui:rot-check **✅** · 桌面构建 **✅**。**⑦文档**：设计稿 wormhole-foe-variety-20260915.md §九 补批 5 段与"收口状态"。**【待验收 · 本地提交未推送】**
- 2026-09-15：**洞内敌卡扩充 · 批 4（E 族：长臂导弹平台 + 三卡 + 纯爆炸白名单）（船长逐轮裁定 · 二号 · d2）**——**①船长原话（照抄）**：「**E族添加巡洋级导弹残骸段，使用导弹。**」＋「**导弹残段射程提高到 1W5**」＋「**④按0.85给**」（命中）＋「**伤害为100%纯爆炸**」＋「**导弹沿用现在的风格**」＋「**E族浅层为奥罗残骸段，深层为巨构残骸段。**」。**②新舰「导弹残段」**（foe-missile-hulk · E 族 **T3 巡洋**）：血 **990**（T3 档基线 900 × 角色 1.10）/ 单发 **161**（124 × 1.30，"单发重、射速慢"）/ 装填 **5,000ms** / **静物 0 m/s**（族格"巨构不讲机动"）/ 装甲型 0.2/0.55/0.25 / **纯爆炸**（{ explosive: 10 }）/ 命中 **0.85** / **射程 100~15,000m** · orbit / **无机群**。**③"导弹"怎么落（零引擎改动）**：本作"导弹"= 武器族别，其特性 = **命中不随距离衰减 + 无视近盲带** ⇒ 用现有字段表达（`falloff: 1` + `blindDmgMul: 1`）；敌方没有"武器族别"字段，故**没有引入任何新机制**；船长选「沿用现在的风格」⇒ **表现层零改动**（不做专门弹道演出）。**④三张卡**：浅 **wh-titan-echo**「巨构残响」（奥罗残骸段 ×1 · 机群 5，沿用）/ 中 **wh-titan-missile**「导弹残响」（**导弹残段 ×1** · 纯爆炸）/ 深 **wh-titan-hulk**「巨构压境」（**巨构残段 ×1** · 机群 7 · 受击增程）；三张一律 random（E 族族格）、隐藏卡、零赏金零掉落；**核心舱段（T5 旗舰）按船长编成退出洞内池**（仍只服务悬赏卡）。**⑤白名单第二例 + 一条新守卫**：纯爆炸卡走「舰级口径优先」（`FOE_SHIP_MIX_AUTHORITY_IDS` 增列 foe-missile-hulk）；体检该分支**新增"纯系卡收束旋钮"断言** —— 单系卡的主体必须**掷命中 + 命中 < 1**（否则"必中 + 满效"没有任何约束）⇒ 本卡 energyForm 非 beam、命中 0.85 ✓ 通过；混伤汇总行读数：**舰级口径优先 2 张**（陵墓王庭 6:4 + 导弹残响纯爆炸）。**⑥契约核对（核过才敢走）**：E 族射程带契约**只约束带机群的 E 族舰级**（实现门控 = hasDrones）⇒ 导弹残段不挂机群、**15,000m 不触发该契约**（设计稿 §六 已登记为船长定的族内特例）；敌速契约：speedRatio 0 ≤ 1、比率 0 落在 E 族族格速带内 ✓。**⑦五道闸门**：typecheck 四包 **0 错** · core **145 文件 / 1,556 用例全绿**（速度矩阵补 E 族导弹残段 0 m/s；两张计数守卫 38 → **40**；'缺档兜底'用例改断"已齐三档的 A/C/D/E 深层池"、只剩 G 族待批 5）· **content:check ✅**（E 族特例 5 张 5:5 + 舰级口径优先 2 张）· ui:rot-check **✅** · 桌面构建 **✅**。**⑧文档**：设计稿 wormhole-foe-variety-20260915.md §七 / §九 同步（批 4 段）。**【待验收 · 本地提交未推送】**
- 2026-09-15：**洞内敌卡扩充 · 批 3（D 族：战列舰 + 三卡 + 「舰级口径优先」白名单）（船长逐轮裁定 · 二号 · d2）**——**①船长原话（照抄）**：「**D族添加一艘战列舰。用的是50%动能+50%能量的混合炮台。**」→ 六问六答后改判「**按乙调整为 6:4**」；同日另一处纠正：「**D组不应该是以护盾为主吗**」（我原按守墓长舰写成装甲型，采纳纠正）；编成：「**D族浅层改为幽灵*2，中层是长舰+静滞卫舰，深层是守墓王座舰×1。**」；契约：「**撞到的契约开白名单**」。**②新舰「守墓王座舰」**（foe-d-throne · D 族 **T4 战列舰**）：血 **1,840**（T4 档基线 1,600 × 角色 1.15）/ 单发 **252**（240 × 1.05）/ **实速 123 m/s**（倍率 0.60）/ **血型护盾型 0.5/0.25/0.25**（船长「D组不应该是以护盾为主吗」）/ **等离子 6 : 动能 4** · energyForm beam · **必中**（主系仍等离子 ⇒ 不像平手口径会落到动能、变成掷命中实弹）/ 命中 1 / 射程 **1,400~9,600m** · orbit / 无机群 / 不写 gunRangeMulOnHit（体检只允许静滞卫舰）。**③两条设计约束（都是体检/读数逼出来的，如实登记）**：**(a) 速度不能取 0.40** —— 205 × 0.40 = 82 m/s ⇒ 比率 0.28 **掉出 D 族速带 0.40~1.15**，取 **0.60**（123 m/s，仍慢于静滞卫舰 129 = 全族最慢，族格"越往里越慢"成立）；**(b) 旧口径兑现** —— 船长 2026-09-11 亲定的「D 族档位 = **1 驱逐 2 巡洋**（**更高级的船还没出**）」由本批兑现为 **1 驱逐 2 巡洋 1 战列**，用例 foe-ship-path.test.ts 的 id/档位/速度/构成四处断言与注释同步改写（新增"战列舰 6:4 是族内唯一例外"的分支断言）。**④三张卡**：浅 **wh-grave-watch**「守墓巡哨」编成 **长舰+幽灵 → 幽灵舰 ×2**（船长「浅层改为幽灵*2」）/ 中 **wh-grave-sentry**「静滞哨链」（守墓长舰 ×1 + 静滞卫舰 ×1）/ 深 **wh-grave-throne**「陵墓王庭」（**守墓王座舰 ×1** 单舰 ⇒ **无需条目覆写**，卡面 6:4 与舰级一致）；三张一律 top-output + 0.4（族定选靶）、隐藏卡、零赏金零掉落；光束族的 dmgMul 0.5 配平系数沿用（待逐卡读数复核）。**⑤白名单机制落地**（船长「撞到的契约开白名单」）：新增 **FOE_SHIP_MIX_AUTHORITY_IDS**（data 侧登记 + data index 导出）——**舰级口径优先**：主体舰级登记了自有构成口径的卡，卡面按"**与主体一致**"校验，**不套**通用"主 8 副 2"、也不套族级 5:5；content:check 的敌方混伤契约新增 **④b 分支**（含"卡面与主体逐键一致"的兜底断言）并在汇总行单列读数：**本批 1 张**（陵墓王庭）；批 4 的 E 族导弹残段（纯爆炸）走同一条通道。**⑥五道闸门**：typecheck 四包 **0 错** · core **145 文件 / 1,556 用例全绿**（D 族舰级用例 3 条 → 4 条：档位 [2,3,3,4]、速度 [325,232,129,123]、战法 [orbit,orbit,kite,orbit]、构成"仅战列舰 6:4"分支；两张计数守卫 36 → **38**；'缺档兜底'用例改用 **G 族**并改断"已齐三档的 A/C/D 深层池 = 浅/中/深"）· **content:check ✅**（混伤汇总行新增「**舰级口径优先** 1 张」）· ui:rot-check **✅** · 桌面构建 **✅**。**⑦文档**：设计稿 wormhole-foe-variety-20260915.md 的 §七 新船表与 §九 落码记录同步（批 3 段）。**【待验收 · 本地提交未推送】**
- 2026-09-15：**洞内敌卡扩充 · 批 2（C 族：无人机舰 + 孢群机 + 三卡）（船长逐轮裁定 · 二号 · d2）**——**①船长原话（照抄）**：「**C组添加一艘巡洋舰，为无人机舰，释放蜂群机。炮台同样是近战。**」＋「**孢群机主要是装甲和结构，攻坚机模板，射程6000，需要专门的无人机SVG(属于虫族)。**」＋「**C族浅层为星髓虫×4。**」＋（选靶/出场）「**选靶按照族限定。层 2~3出场抽取按照2:1抽。层4+出场抽取按照2:1：1抽。**」。**②新舰「孢群异虫」**（foe-alien-spore-hive · C 族 T3 巡洋）：血 **1,125**（档基线 900 × 角色 1.25）/ 单发 **87**（124 × 0.70，"炮台让位给机群"）/ **实速 400 m/s**（倍率 1.55 ⇒ 既在 C 族带 1.30~2.10 内，又**高于 A 族同档最快 374**，契约硬要求）/ 血型均衡 / 等离子 8:2 / energyForm 'spit'（C 族四档一律掷命中）/ 近战 **1~2,700m · brawl**（船长「炮台同样是近战」）/ **孢群机 ×3、无后备** / droneFireShare 0.6（A5 守恒：机群吃六成火力）。**③新机型「孢群机」**（foe-drone-c-spore · C 族攻坚机档）：三层血 **15/28/45 = 88**（护盾 17% / 装甲 32% / 结构 51% = 船长「主要是装甲和结构」；与 C 族专属玩家机「巢卫」同语言）/ 甲·壳三系抗 **0.20** / 闪避 **0.08**（最笨）/ 单发 **9** 等离子 / 命中 **0.78** / falloff 1（攻坚机档：命中不随距离衰减）/ **射程 6,000m**（船长指定）/ 装填 4,400ms。**④虫族线稿（新 SVG）**：ui/droneArt.tsx 新增 SPORE_DRONE_ART —— 族外形语言 = **孢子荚 + 甲壳节**（两道节缝、尾端孢子囊、前缘短触须、孢光点），族色**磷光绿 #9fe6a4**，与 G 族"蜂窝六边舱段"、E 族"斜装甲楔形"同一套线稿纪律（细描边 / currentColor / 禁 CSS 拼形状）；**观感待船长实机复看**（按约定 §九，我不以截图作观感结论）。**⑤三张卡**：浅 wh-alien-swarm「巢群游猎」编成 **畸变幼虫 ×3 → 星髓幼虫 ×4**（船长「C族浅层为星髓虫×4」）/ 中 wh-alien-brood「巢群兵潮」（星髓成虫 ×1 + 畸变幼虫 ×2）/ 深 wh-alien-hive「孢群巢穴」（**孢群异虫 ×1 + 畸变幼虫 ×2**）；三张一律 smallest + 0.4（族定选靶）、隐藏卡、零赏金零掉落。**⑥一处按读法落地（待船长一句话确认）**：船长写的「**星髓虫**」没指明幼虫/成虫 —— 我按 **星髓幼虫（T1）** 落（浅层该是最轻的那档），若你要的是**星髓成虫（T2）**，改一行即可。**⑦用例与契约**：core 新增 1 条（**C 族深层卡的机群口径**：孢群机 ×3 · artId = foe-drone-c-spore · 机群占总单发 0.55~0.65 ⇒ 钉住"机型同族"与 droneFireShare 0.6）；「缺档兜底」用例改用 **D 族**（C 已三档齐）并加一条"C 族深层池 = 浅/中/深三档"；foe-ship-path.test.ts 的 C 族舰级数 **4 → 5**、速度矩阵补 **孢群异虫 400**、两张卡与一处交距用例的计数守卫 **34 → 36**。**⑧五道闸门**：typecheck 四包 **0 错** · core **145 文件 / 1,556 用例全绿** · content:check **✅**（洞内敌卡 9 张；敌机登记 6 处；机型表 5 条中仍只 1 条已备未挂 = G 爆炸系蜂群机，批 5 会挂上）· ui:rot-check **✅** · 桌面构建 **✅**。**⑨读数项（如实登记）**：孢群机三层血 88 是敌机里最厚的一型，而**敌机血量不吃按卡归一的缩放** ⇒ 近防炮击落效率待 wormhole:econ 全表齐备后一并重标定；打不动则按设计稿 §八 风险①下调到 60~70 并报船长。**⑩文档**：设计稿 docs/design/wormhole-foe-variety-20260915.md 新增 §九 落码记录（批 1 / 批 2）。**【待验收 · 本地提交未推送】**
- 2026-09-15：**洞内敌卡扩充 · 批 1（结构 + A 族三卡）（船长逐轮裁定 · 二号 · d2）**——**①船长原话（照抄）**：「**增加敌人的配置种类和敌族新舰船。**」→（新船商量）「**C组添加一艘巡洋舰，为无人机舰，释放蜂群机。炮台同样是近战。D族添加一艘战列舰。用的是50%动能+50%能量的混合炮台。E族添加巡洋级导弹残骸段，使用导弹。G族添加战列舰动能伤害为主。**」→（编成与出场）「**C族浅层为星髓虫×4。D族浅层改为幽灵*2，中层是长舰+静滞卫舰，深层是守墓王座舰×1。E族浅层为奥罗残骸段，深层为巨构残骸段。G浅层为围攻残兵舰*3，中层为残响残舰*2高层为亡军战列舰*1。**」＋「**将浅层中层深层分别定为1/2/4层开始出现。且配置还要有单独修正。中层配置血量*1.1.深层配置血量*1.2**」＋「**选靶按照族限定。层 2~3出场抽取按照2:1抽。层4+出场抽取按照2:1：1抽。确认开工。撞到的契约开白名单。**」。**②本批落码（结构 + A 族）**：**(a) 一族三档**——core 新增 `WormholeCardTier`（浅/中/深）＋ `WORMHOLE_FAMILY_CARDS`（族 → 三档卡，缺档 = `null` 允许分批补）＋ `WORMHOLE_TIER_UNLOCK_DEPTH`（**1/2/4 层起**）＋ `WORMHOLE_TIER_HP_MUL`（**1 / 1.1 / 1.2**）＋ `WORMHOLE_FAMILY_TARGETING`（**族定选靶**：A 非战斗船 · C 打最小 · D 打输出最高 · E/G 随机）＋ `WORMHOLE_FAMILY_TARGETING_CHANCE = 0.4`；**(b) 出场池**——新增**唯一取值点** `wormholeCardIdForRun({族, 种子, 层, 用途, 序号})`：层末守卫取**该层最深已解锁档**，其余按权重轮盘 **层 1 = 浅 1 · 层 2~3 = 中 2 : 浅 1 · 层 4+ = 深 2 : 中 1 : 浅 1** 确定性抽取（`(种子, 层, 节点序号)`，网格层序号 = `gridContentIndex` ⇒ 同格恒同卡）；**缺档自动剔除、其余权重不变** ⇒ 分批上线期间照样能开战；**删掉旧的全表轮换 `wormholeCardIdFor`**（工具与用例同步改走新单点）；**(c) 分层血量修正**——`wormholeAnomalyOf` 把"按卡归一"的缩放**拆成 `scaleHp` / `scaleDmg` 两个系数**（血吃档位系数、火力不吃）⇒ 表现为「更耐打、不更疼」；档位由**卡 id 反查**、**不新增存档字段**；**(d) A 族两张新卡**——`wh-pirate-hunt`「劫掠围猎」（护卫舰 ×2 + 快艇 ×1 · 动能 8:2 · 非战斗船 0.4）与 `wh-pirate-warband`「海盗战团」（头目舰 ×1 + 快艇 ×3 · 动能 8:2）；浅层五张 **id 与卡名一字不动**（只改编成，存档里进行中的战斗零漂移）。**③体检新契约**：`content:check` 新增段「一族三档 / 族定选靶 / 出场层 / 出场权重 / 分层血量修正」五组哨（档位表与清单同集合、每卡族字母、卡面模式与概率 = 族定值、1/2/4 层、1/1.1/1.2、三层权重逐格）；三处清单同步 5 → **7 张**。**④一处体检拦下的设计修正（如实登记）**：A 族中卡原设计「护卫舰 ×2 + 劫掠狙击舰 ×1」被**敌速口径契约**拦下——狙击舰是 `kite`（实速 325 ⇒ 比率 1.11×），而**隐藏卡**走的是**战术带**（kite 0.60~0.85）而不是 A 族全族提速带；要留它只能压 `speedMul`，与船长「A 族速度都快（方便突袭）」的族格冲突 ⇒ 改用同族**快艇**（brawl 1.34×）；狙击舰仍服务悬赏线。**⑤五道闸门**：typecheck 四包 **0 错** · core **145 文件 / 1,555 用例全绿**（新增 5 条：出场池与确定性/守卫取档、缺档兜底、族表与清单与目录三处一致、分层血量修正只加血、引擎按卡 id 反查档位；改写 3 条旧的"32 张卡"计数守卫 → 34）· `content:check` **✅** · `ui:rot-check` **✅** · 桌面构建 **✅**。**⑥文档**：新设计稿 `docs/design/wormhole-foe-variety-20260915.md`（状态：已确认 · 分 5 批；含全部裁定照抄、15 张卡表、出场池、分层修正、族定选靶、白名单口径、新船数值与读数计划）。**⑦本批不做**：C/D/E/G 四族的中/深卡与 4 艘新舰（批 2~5）；读数配平待全表齐备后统一重标定（`wormhole:econ`）。**【待验收 · 本地提交未推送】**
- 2026-09-14：**修虫洞面板两条（战报后回不到面板 / 换层遗留上一层按钮）（船长报障 · 二号 · d2）**——**①船长原话（照抄）**：「**虫洞的战斗胜利后，点击战报有时会退出虫洞界面。**」＋「**前往下一层后，上一层遗留的按钮未消失。**」**②第一条（回不到面板）**：根因 = 遗迹守备确认条那句 `onClose()`（原文注释"关掉面板，全屏战场接管"）与**同文件既有的设计条款直接冲突**——`Wormhole.tsx` 末尾写着「**战斗中不渲染本面板**（`.app-battle-screen` z-100 低于弹层遮罩 z-120 ⇒ 面板开着必然压住战斗）…**`whOpen` 保持为真 ⇒ 战斗结束后面板自动回来，玩家不用再点一次**」。面板一关就再也回不来 ⇒ 战报一关人落在星图上（= 退出虫洞界面）。**"有时"得到解释**：只有这一条路关面板，另两条开战路径（`doActivate` 的舰船信号 / 层末守卫）不关 ⇒ 那两条打完都能回到面板。**修法** = 去掉那句 `onClose()`（面板保持打开；战斗中本组件 `return null`，收口后自动回来），与另两条路径一致。**③第二条（换层遗留按钮）**：根因 = `pendingCell`（"点了未扫描的格 ⇒ 待确认前往"那个**双按钮条**：确认前往 / 取消）是**记着上一层 Q/R 坐标**的普通 state，**不随换层重置** ⇒ 深入后仍挂在新层头顶，且同坐标在新层另有格子（点下去会跑错格）。**修法** = 换层 effect（`layerKeyForFx` 变化那处）里与 `fx` 一起 `setPendingCell(null)`。**④验证**：typecheck 四包 0 错 · core **145 文件 / 1,551 例**（本批纯渲染层，引擎未改 ⇒ 读数不变）· `content:check` ✅ · `ui:rot-check` ✅ · 桌面构建 ✅。**⑤纯渲染层两处小改**（不动引擎/数值/存档）；**观感与手感请船长实机复看**（按约定 §九）。**【待验收 · 本地提交未推送】**
- 2026-09-15：**非战斗舰（采矿艇 / 货舰）血量提档：三层按比例放大到「同档官方战斗舰总血中位 × 0.8」（船长 · 一号 · main）**——**①船长原话（照抄）**：「**提高所有非战斗舰船的血量，使其约等于同级官方战斗舰船血量的 0.8**」；**四问四答（全取推荐）**：**(a) 基准** = 同档**官方战斗舰总血中位 ×0.8**；**(b) 放大方式** = **三层（护盾 / 装甲 / 结构）各按原比例同乘**，取整余数记入结构层 ⇒ **总血恰好命中目标**；**(c) 范围 = 含沙猫**（教学船一起提）；**(d) 价格 / 货仓 / 槽位 / CPU / 产出三件**一律不动。**②「官方战斗舰」定义（写进 `ships.ts` 头注，免得日后各读各的）**：同档 `role` ∈ {`armed`, `armored`} 的**玩家舰**（含侦察 / 电子 / 重装等子分类），**排除虫洞专属 `sh-wh-*`**（洞内产出、不参与"同级官方"基准）。**③参考中位与目标值（档 : 官方战斗舰艘数 → 总血中位 → ×0.8 = 目标）**：T1 3 艘 228 → **182** · T2 4 艘 384 → **307** · T3 7 艘 675 → **540** · T4 2 艘 1273 → **1018** · T5 1 艘 2355 → **1884**（五档取整后与"中位 ×0.8"逐档吻合）。**④11 艘落值（护盾 / 装甲 / 结构；括号 = 改前总血 → 改后总血）**：沙猫级 `sandcat` 47/40/95（69 → **182**）· 掘洞级 `burrower` 48/39/95（126 → **182**）· 鲸吞级 `whale` 78/69/160（201 → **307**）· 开拓级 `pioneer` 79/70/158（210 → **307**）· 飞鱼级快运舰 `sh-flyingfish` 52/66/189（195 → **307**）· 鲸王级 `whale-king` 141/118/281（288 → **540**）· 座头鲸级矿舰 `sh-humpback` 68/193/279（406 → **540**）· 旗鱼级高速货舰 `sh-sailfish` 88/119/333（312 → **540**）· 蝠鲼级重载货舰 `sh-bowhead` 111/343/564（412 → **1018**）· 剑鱼级大型货舰 `sh-swordfish` 170/226/622（324 → **1018**）· 皇带鱼级旗舰货舰 `sh-colossal` 231/514/1139（586 → **1884**）；`git diff` 恰好 **33 行数值**（11 艘 × 三层），**既有注释一行未动**。**⑤零存档迁移（已核）**：存档只存**耐久比例** `durability` 与**装甲比例** `armorPct`，层满值由 `hullLayerCaps` 从定义现推 ⇒ **老档的船自动按新上限算**（等价于绝对血量上涨），**不动版本号、不写迁移**。**⑥常驻护栏**：`content:check` 新增「**非战斗舰血量契约**」——`role` 为 `industrial` / `hauler` 的船**三层总和必须等于本档目标**（表 `CIVILIAN_HP_TARGET` 与 `ships.ts` 头注同源，改一处必改另一处）。**负向验证**：沙猫 `hullHp` 95 → 96 ⇒ 契约**恰好点名 2 艘**（沙猫 ＋ 掘洞级，T1 总血 183 ≠ 182——`hullHp: 95,` 一行同值匹配两船、一起被加），复原即绿。**⑦新工具（常驻）**：`tools/ship-hp-table.ts` ＋ **`npm run ship:hp`**——全船按档 × 角色列三层血 · 各档官方战斗舰的中位 / 均值 / 极值 ＋ 目标值核对 · 11 艘现状与"按比例放大建议" · 一口遇袭占比；`ships.ts` 头注直接引用该命令。**⑧A/B 读数（演习场驱逐令 `ano-training` × 沙猫裸船 · 5 播种 1/7/42/99/2026）**：改前（总血 69）胜率 **80%** · 均时长 **63 秒** · 均残血 **28%** · 解析预估 **88%**；改后（182）胜率 **100%** · 均时长 **63 秒（不变）** · 均残血 **77%** · 解析预估 **98%** ⇒ **变的是"能挨多少"，不是"打多久"**。**⑨低安遇袭的连带（一口 = 威胁 42 × `foeDpsPerThreat` 0.8 × `hitFirepowerSec` 5 秒 = 168 HP，占各档总血）**：T1 **92.3%**（仍会咬到结构）· T2 **54.7%**（会咬到结构）· **T3 31.1% / T4 16.5% / T5 8.9% 一律"不破甲"** ⇒ 2026-09-14 那次「一口咬穿 T3 以上装甲」的观感**随血量变厚而消失**（承伤口径本身一字未改）。**⑩五道闸门**：typecheck 四包 **0 错** · core **1555 用例全绿**（无测试断言这些数值）· `content:check` **✅** · `ui:rot-check` **✅** · 桌面 ＋ 网页 build **✅**。**⑪文档**：`ships.ts` 头注（口径 ＋ 目标表 ＋ 参考中位 ＋ 零迁移说明）· 词典新增「**非战斗舰血量基准**」条 · 本条目。**⑫已知取舍**：只抬生存、不抬任何产出 / 载货 / 造价 ⇒ 采矿与运输的经济读数**不变**；教学与演习场的容错变大（80% → 100%）；低安遇袭的"皮薄"威慑下降（T3 以上不再被一口破甲）。**⑬不发公告**（船长「不用发公告」）。**【待验收 · 本地提交未推送】**
- 2026-09-15：**期望交距口径收尾：取消「洞内维持中段」分档（船长：那是我口误）＋ 洞内近战怪开局保留中段（船长选「乙」）**——**①船长原话（照抄）**：先问「**假设玩家设置过一次距离后，就不按照默认来了对吗**」（我答：星图**按星系记**、洞内只记本趟、AI 副船不吃），随后「**这个是我口误。可以回滚「洞内默认期望维持中段」这句**」⇒ 我把两处口径的牵连摆清后问「甲/乙」，船长回「**乙**」。**②乙口径（现行）**：`balance.battle.desireBandStarMap/desireBandWormhole` 两个旋钮**合并为一个** **`desireBandMid = 0.8`**（`desiredRangeFor` 缺省档、星图与洞内同值），另立 **`wormholeBrawlOpenBand = 0.5`** 专管**洞内"近战怪开局距离"**（2026-09-13「进去就得挨打」不动）；`balance.ts` / `types.ts` / `combat.ts`（默认参数、`startFleetBattleFor` 去掉 `bandMid` 分支、`steadyDistance` 去掉 `isWormholeCard` 分辨并删掉该助手）/ `expedition.ts`（`battleTacticDesire` 去掉 inWormhole 分支，改回单档）全链同步。**③读数**：导弹架 1,400~14,880 ⇒ 默认期望 **12,184 m**（洞内同样）；洞内遇近战怪开局仍 **8,140 m**（中段）。**④用例（3 处改 + 1 处补）**：`main-weapon-range.test.ts` 的默认档锁定改成「`desireBandMid = 0.8` / `wormholeBrawlOpenBand = 0.5` / 默认 9,588 / 近战开局档 6,330」，并把 5 处历史"中距 = 带中点"断言改成 0.8 档（激光 2,300→**3,680** · 近防炮 1,251→**2,000** · 并列 3,000→**4,200** · 导弹 6,330→**9,588**）＋真数据一致性那条改成 0.8 公式；`wormhole-battle.test.ts` 的护栏改名「**洞内默认期望跟星图同档 0.8；近战怪开局仍守中段 0.5**」并断言 `brawlPos < midPos`，「开战距离特殊规则」的近战那一支**显式传 `wormholeBrawlOpenBand`**（不许写成缺省档）；`desire-per-galaxy.test.ts` 三处改 `desireBandMid`。**⑤负向验证（两处各自精准 · 复原即绿）**：a) `desireBandMid` 改回 0.5 ⇒ `main-weapon-range` **6 例** ＋ 洞内锁定 **1 例**红；b) `wormholeBrawlOpenBand` 改成 0.8 ⇒ 恰好 **2 例**红（洞内锁定 ＋ 默认档锁定）。**⑥五闸门**：typecheck 四包 **0 错** · core **全量 1555 例全绿** · `content:check` ✅ · `ui:rot-check` ✅ · 桌面＋网页 build ✅。**⑦文档**：设计稿 `docs/design/desire-band-20260915.md` 标题/§二/§四/§五 按「乙」改写（含"口误更正"沿革）· 词典两条（「目标距离（按星系独立保存）· 胜率预估口径」与「主武器（战术距离口径）」）同步 · **上一条 2026-09-15「默认期望交距：中段 → 射程带高位」里的"洞内维持中段 0.5"一句就地标作废**。**【待验收 · 本地提交未推送】**
- 2026-09-15：**C 族（异形生物）加「全血条 25% 爆炸抗性」（船长一条）＋ 由此带出的引擎缺口：玩家打敌的伤害结算此前一律传空抗性（敌抗性长期不生效）**——**①船长原话（照抄）**：「**我现暂时只打给 C 族添加全血条 25% 爆炸抗性**」。**②口径落点（族级一份常量，四条舰级共用）**：`packages/data/src/foe-ships.ts` 新增 `C_FAMILY_RESISTS = { shieldResist/armorResist/hullResist: { explosive: 0.25 } }`（**三层各 25% 爆炸抗**），四条 C 族舰级（畸变幼虫 · 星髓幼虫 · 噬口巨兽 · 星髓成虫）展开引用 ⇒ 改数只改一处；`FoeShipDef`（core/types.ts）新增三个**可选**层抗字段（缺省不写 = 无抗 ⇒ 既有全部舰级**零行为变化**）。**③⚠ 本轮最值钱的发现（真缺口 · 长期潜伏）**：先按要求取改前读数（`battle:calibrate --std`），加完族抗后**前后对照 128 格一格未变** ⇒ 查下去发现**玩家打敌舰的五处伤害结算全部传空抗性**（`applyDamage(rt.hp, {}, …)` / `rawDamageToKill(prevHp, {}, …)`：主段 · 附加伤害段 · 全体攻击两段 · 齐射溢出结转）⇒ **敌舰的抗性从来没被消费过**（此前敌舰 `resists` 恒为 `{}`，所以这个缺口一直没被发现，**四道闸门全绿也照样漏**）。**修法**：五处一律改传**目标自己的** `resists`（`foeTarget.resists` / `other.resists` / `next.resists` ＋ 溢出前序目标的 resists）。**④读数（修好后 · `battle:calibrate --std` 前后对照 · 日志 `tools/_ui-artifacts/calib-{before,after2}-cresist.log`）**：**128 格中只有 2 格变化**，且**正是「C 族 × 爆炸装配（A2 = 4×重型导弹架 MK2）」**——**A2 深渊之门卫队 时长 24s → 50s**（胜率/残血不变）、**A2 裂谷畸变体猎杀令 32s → 68s**（残血 100% → **98.9%**）；**其余 126 格（含全部非 C 族卡）一格未动** ⇒ "只影响 C 族"得到读数印证，也说明 A0/A1/A3 三行（动能装配）不受影响（C 族只抗爆炸）。⚠ **时长翻倍不止 1.33×** 的原因：C 族生体件有**无消耗自愈**（`repairFree`）⇒ 敌人活得久 → 自愈回合更多，时间效应是超线性的。**⑤用例（+4 条，`foe-ship-path.test.ts` 的「C 族族抗性」describe）**：① 数据面（四条 C 族舰级三层逐字一致）· ② 建档面（真 C 卡开战 ⇒ `createFoeSpecs` 的敌单位 resists 三层都带 0.25）· ③ 边界面（**非 C 族一艘都不许有抗** ＋ 抗性值落在 `applyDamage` 有效域 `[0,0.9]`）· ④ **真打一场：敌舰层抗必须被消费**（同种子同装配，只差敌舰有无 25% 爆炸抗 ⇒ 实收伤害必须恰好 ×0.75；守的就是③那个缺口）。**⑥负向验证（各自点名后还原）**：a) 引擎不读舰级抗（退回 `resists: {}`）⇒ 用例②红（`巢群游猎 · 畸变幼虫 · shield: expected undefined to be 0.25`）；b) 给 A 族四条舰级加抗 ⇒ 用例③红（`海盗快艇（A 族）本轮不该有族抗`）；c) **玩家打敌主段改回空抗性 ⇒ 用例④红（实收比例 1 ≠ 0.75）**。**⑦验证**：typecheck 四包 **0 错** · core **145 文件 / 1555 用例全绿** · `content:check` ✅（舰级契约不校验字段白名单，新增字段不影响）· `ui:rot-check` ✅ · 桌面 ＋ 网页 build ✅。**⑧文档与口径**：本条目；词典新增「**敌舰层抗（FoeShipDef.shieldResist/armorResist/hullResist）**」条（含"三层 · 只减不减 · 缺省无抗 · 由玩家打敌五处结算消费"＋"此前长期不生效"的欠账记录）。**⑨待你定（两条，我没动）**：**(a) 要不要把族抗写进玩家可见说明**——2026-09-10 混伤裁定的先例是「**并需要在任务中告知玩家**」，而族抗同样是"改伤害数学"的敌人特性；现在玩家只能靠手感发现"爆炸打 C 族变肉"（建议在悬赏卡/图鉴/手册各补一句，等你点头）。**(b) 难度补偿**——C 族的爆炸装配时长翻倍（上表），你当时选的是「只加抗、血量不动」⇒ 本轮未做补偿；要按读数回调同族血量/威胁我随时可以跑对照。**【待验收 · 本地提交未推送】**
- 2026-09-15：**悬停提示统一：延迟 200→500ms ＋ 三条路径同源 ＋「内层优先」＋ 原生提示整条祖先链压住（船长三条报障 · 三号 · verify）**——**①船长原话（照抄）**：「**鼠标悬浮按钮的提示会和上一级的悬浮提示相互冲突。且悬浮的反应时间太快。建议所有 UI 统一下**」。**②查实（三处不统一 + 两种冲突成因）**：**不统一**——`ui/Tooltip.tsx` 有**三条**提示路径但只有一条带延迟：`title`/`data-tip` 全局接管层（200ms）· **富内容悬停**（`ShipHover`/`InfoHover` 的 `onMouseEnter → showTip`，**即时弹**）· `HoverTip` 组件（**即时弹**）。**冲突成因一**：外层卡片/行与内层按钮**各有提示**时，React 的 `mouseenter` 会沿祖先链派发 ⇒ 外层富内容面板先弹、内层再顶掉（或反之）。**冲突成因二（真 BUG）**：接管层旧实现是 `removeAttribute('title')`——摘掉之后指针下这个元素就"没有 title"了，浏览器**顺着祖先链找到上一级卡片那条 title、弹系统默认提示** ⇒ 站内自绘提示与上一级原生提示**同时在在场**。**③改法（已在约定第九章与 `AGENTS.md` §6 落定）**：**(a)** 延迟收敛成**唯一常量 `TIP_DELAY_MS = 500`**（2026-09-13 的 200ms 作废），**三条路径共用**；**(b)** 新增 **`hoverTipProps(content)`**：富内容悬停的唯一接线方式（与接管层同延迟、同单例层），并在 `mouseenter` 时做**"内层优先"**判定（指针目标到自身之间若有更深的提示归属 ⇒ 不接）；接管层对带 `data-tip-hover` 的元素**不再重复接管**；**(c)** 接管时把 `title` 写成**空串**并**把祖先链上所有 `title` 一并置空**（记原值、离开逐个还原），`srcOf` 把 `title=""` 视作"没有 title"继续从暂存取真文本 ⇒ 原生提示在接管期间不可能出现；**(d)** `HoverTip` 组件改为走 `hoverTipProps`（不再即时弹）。**④新守卫**：`content:check`「悬停提示契约」**+1 条**——同一 JSX 元素不许同时带 `title` 与 `{...hoverTipProps(…)}`（两个提示归属抢同一个单例层）；**负向验证实测会红**并点名 `apps/desktop/src/renderer/src/ui/shipInfo.tsx:980（<Tag>）`，还原即绿。**⑤验证**：typecheck 四包 **0 错** · core **145 文件 / 1549 用例全绿**（本次是纯渲染层改动，引擎未动）· `content:check` **✅** · `ui:rot-check` **✅** · 桌面 ＋ 网页 build **✅**。**⑥已知边界（如实登记）**：浏览器**原生提示不可脚本化观测**（DOM 无接口）⇒ "祖先链压住"这条只能靠机制 ＋ 船长人工观感确认，无头探针里断言不了；其余三条（同延迟 / 内层优先 / 双归属守卫）都静态可核。**⑦改动文件**：`apps/desktop/src/renderer/src/ui/Tooltip.tsx`（主体）· `ui/shipInfo.tsx`（两处富内容悬停改走 `hoverTipProps`）· `tools/content-check.ts`（新守卫）· `docs/development-conventions.md` §九 · `AGENTS.md` §6 · `development-conventions-changelog.md`。**【待验收 · 本地提交未推送】**
- 2026-09-15：**内容工作台：修「两表导不回去」的既有缺陷（anomalies 多源文件 · ships 常量/引号键白报改动）（船长「一起修」· 一号 · main）**——**①船长原话（照抄）**：「**一起修**」（承接上一条我登记的两处往返缺陷）。**②anomalies 表：schema 支持多源文件**——`TableSpec.file` → **`files: string[]`**；anomalies 声明 `['data/anomalies.ts', 'data/wormholeFoes.ts']`（`ANOMALIES` 摊进了 5 张 `wh-*` 洞内敌卡，卡实体住在 `wormholeFoes.ts`）。导入端**逐文件收集对象块并合并**（同一 id 出现在两处 ⇒ 直接拦下），`ObjInfo` 带上 `srcPath`，`planRow`/`applyChanges` 按**对象自己的文件**取原文与 AST，回写**按文件分组**（`✅ 已回写 <文件>（N 处字段变更）`），自动校验失败时的还原提示改 `git restore <文件…>`（不再教 `git checkout --`）。**③ships 白报改动：两条根因一起修**——**(a) 键名带引号**：`objPairsOf` 原先 `p.name.getText()` 对 `{ "kinetic": 0.5 }`（历史导入留下的 JSON 风格键）会连引号返回 ⇒ 与表头派生的 `kinetic` 对不上 ⇒ 判成"键不存在"、**每轮导入都重写**（实测 15 条 `sh-wh-*` 白报 **54 处**）；现字符串键取 `name.text`（去引号）。**(b) 常量引用被当字面量**：卡面 `threat: ANCHOR_THREAT`（同文件 `const ANCHOR_THREAT = 45`）原先 `Number('ANCHOR_THREAT')` = NaN ⇒ 5 张 `wh-*` 卡每轮都"要改"、真写下去会把**全族共用常量钉死在单卡上**；现新增 `collectNumConsts` / `numOfExpr`：同文件常量**按值比对**（同值即不动），**是常量引用就只读跳过并点名**（`ℹ️ wh-titan-echo·威胁…（源为常量 ANCHOR_THREAT=45）`），跨文件常量/计算式同样只读跳过——**宁可不动，也不擅自把表达式换成字面量**。**④实测（真写盘 + 还原）**：把 `wh-titan-echo` 奖励格改 12345 跑**真实导入** ⇒ `✅ 已回写 packages/data/src/wormholeFoes.ts（1 处）`、`git diff` 精确一行 `rewardIsk: 0 → 12_345`（**证明多源写入落对文件**）；再把威胁格改 99 ⇒ 如实打印上述只读提示且 `✅ 无差异`（**证明常量列改不动**）；随后**从备份还原** `wormholeFoes.ts`（未用 `git checkout`）并重导工作台、复核 `git status` 干净。**⑤7 表往返终检（重导后逐表 --dry-run）**：**skills · items · modules · ships · anomalies · belts 全部 ✅ 无差异**，market ✅（附既有预期的「跳过 23 行派生只读卡」）。**⑥验证**：typecheck 四包 **0 错** · `content:check` ✅ · 编码三查通过。**⑦文档**：本条目。**【待验收 · 本地提交未推送】**
- 2026-09-15：**修货柜拆解两处报障：卡面/日志文案按产线分岔 ＋ 拆完最后一件当场收工（船长 · 三号 · verify）**——**①船长原话（照抄）**：「**精炼炉拆解货柜的文字显示不对。且拆解完毕后，货柜为0时还是会进行一次拆解。**」**②报障一：文字显示不对（根因 = 货柜卡整张落在"精炼"那一支）**：`IndustryPage.tsx` 的 `FurnaceCard` 只有 `isWreck / else` 两支，`isBox`（`kind === 'container'`）虽已声明却只用于启动命令 ⇒ 货柜卡显示的是精炼口径：数据行「可用 ×2（**4,000 m³**）· 每批 **10 单位 / 6 秒** · 约 1 批炼完」（`unitM3 = 2000` 被当体积算，周期/批量读的都是精炼默认值）、名册行「**已炼** N 批」、按钮「手动运转 / AI 运转」、手动 tooltip「循环**精炼**」；core 停机日志也是「**精炼炉停**：遗迹安全货柜 原料耗尽（共 N 批）」。**改法（三套量词互不借用）**：货柜 = **件 · 拆 · 货柜拆解停**（数据行「可用 ×N 件 · 每件 90 秒 · 拆完自动停（一箱开一件）」· 名册「已拆 N 件」· 进度条悬停「每件 90 秒」· 按钮「手动拆解 / AI 拆解」· 开工提示「货柜拆解开工」（图纸货柜也走这条路，不再写死"安全货柜"）· 秒数一律读 `UNBOX_CYCLE_MS` 不写死）；精炼 = 单位 · 炼；残骸回收 = m³ · 拆解。停机日志分岔：货柜 = 「**货柜拆解停：X 货柜已拆完（共 N 件）**」。**③报障二：拆完还会空转一个批周期（看着像"又拆了一次"）**：`advanceRefining` 批末那句"当场收工"**原先只覆盖私有料账**（2026-09-11 玩家反馈「稀有残骸空了精炼炉还在运转」时加的 `claimedUnits <= 0` 一档）⇒ 公共库存吃光后仍会把到点时间推到下一批，要到**再下一个到点**才由循环头部判"料尽"停炉（货柜 = 白转 90 秒、进度条再走一格）。**改法**：把该判据推广到**全部产线**——批末算一次"下一批还够不够"（私有料账吃完 / 公共库存 0 / 不足一批都算不够）⇒ 不够就把 `finishAtGameMs` 拨到"现在"再 `continue`，下一轮立刻进**既有**停机分支（**日志与停机口径一字未改，只是提前一拍**）。口径依据 = 2026-09-06 船长拍板「余量不足即停工、余料保留，凑够一批再开」。**④用例**：`wormhole-unbox.test.ts` 那条改钉新时序（拆完最后一件**同一拍**收工 ＋ 停机文案含「货柜拆解停」「货柜已拆完（共 2 件）」 ＋ 两只箱子只开两次、再推两拍不复活）；`recycle.test.ts` 那条「跑完一批后下一批到点才见底」按新口径改写（`toHaveLength(1)` → `0`）——两条都是"口径该变"，不是真坏。**⑤负向验证（各自点名后还原）**：a) 批末判据退回"只覆盖私有料账" ⇒ `wormhole-unbox` ＋ `recycle` **两条同时红**（`expected [ { active: true, … } ] to have a length of +0 but got 1`）；b) 停机文案退回精炼口径 ⇒ `wormhole-unbox` 红（`expected false to be true`）。**⑥验证**：typecheck 四包 **0 错** · core **145 文件 / 1549 用例全绿** · `content:check` ✅ · `ui:rot-check` ✅ · 桌面 ＋ 网页 build ✅。**⑦文档**：词典八之三补两条（文案/停机口径按产线分岔 · 批末"当场收工"覆盖全部产线）；本条目。**【待验收 · 本地提交未推送】**
- 2026-09-14：**修洞内战斗视图：敌卡口径漏接（敌族图形/动画 · 舰种体积 · 开局机位）（船长报障 · 二号 · d2）**——**①船长原话（照抄）**：「**虫洞内的战斗，敌方舰船动画不对。**」＋「**战斗开始位置似乎不对。**」＋（追问后）「**敌方的战斗动画图形和敌族对不上**」。**②根因 = 一类"洞内战读了远征口径"的漏接**：洞内战斗宿主是 `state.wormhole.run.battle`（**不占** `expedition.battle`）、敌卡是按层**派生**的卡（不落在 `expedition.anomalyId`）⇒ 凡"只认远征"的读法在洞里**静默取到 null/undefined 而照样渲染**。本轮一次抓到 **6 处**：**① `BattleScreen.foeAnomaly`（主犯）**：`foeKey = foeFamilyOf(undefined)` ⇒ 兜底族 **A 海盗** ⇒ **洞里 C/D/E/G 族全画成海盗舰体与动画**（= 船长那句"图形和敌族对不上"）；`foeShipTierOf` 拿不到 ⇒ 体积回落 170/90 ⇒ **舰种体积阶梯在洞内失效**，而体积又喂 `layout()`（锚点跟实际舰宽、机位/米制跨度 `usable`）⇒ **开局机位整体偏**（= "开始位置不对"）；② 死敌预登记恒不跑 ⇒ 洞内退出再进战场**血量 0 的敌人复活**（2026-09-10 修过的同款 BUG 当年只在洞外修好）；③ 星场基准船速恒早退；④ `core.battleTacticDesire` 默认卡只取远征 ⇒ 洞内返回 **0** ⇒ 点「突击/中距/风筝」= **整队贴脸**；⑤ `sceneOfShipwin` 洞里不切交火场景；⑥ 公告自动弹没挡洞内战。**③修法**：新增 core 取值单点 **`wormholeBattle.battleFoeAnomaly`**（洞内 = 按层派生卡、洞外 = 远征卡**逐字照旧**），`BattleScreen` 改读它（**渲染层不再直读 `expedition.anomalyId`**）；死敌预登记/星场船速改用**本场已解析的 `battle`**（锚船与 33ms 循环同口径）；`battleTacticDesire` 与 `setBattleDesire` 同口径解析（洞内 = 本趟敌卡 + 编队首舰）；状态窗与公告各补洞内分支。**④常驻护栏**：`content:check` 新增「**战斗宿主双口径契约**」（源码级扫描）：`BattleScreen` 非注释行 `expedition.anomalyId` **必须 0 处**、`expedition.battle` **恰好 1 处**且带 `whView`、两处视图必须保留洞内分支。**⑤验证**：core 用例 +2（**敌卡取值单点**：洞内派生卡 id/族同源 + `foeShipTierOf`/`foeUnitNameOf` 可解析 + 摘掉洞内战 ⇒ 拿不到卡 + 洞外照给；**洞内战术期望距离**三档非零且走洞内中段档、小于星图档）；**负向验证两条各自精准**（退回旧口径 ⇒ 战术用例红 `expected 0 to be greater than 0`；塞一处直读 ⇒ 契约点名 1 处）；五道闸门全绿（typecheck 四包 0 错 · core **145 文件 / 1,551 例** · `content:check` ✅ · `ui:rot-check` ✅ · 桌面构建 ✅）。**⑥边界**：只动"取哪张卡/哪个宿主"的解析，**不动**数值/布局几何/动画时长/CSS；洞外口径逐字不变；洞内开局距离仍按 2026-09-13 洞内规则。**⑦文档**：新设计稿 `docs/design/wormhole-battle-view-20260914.md`（6 处清单 + 修法 + 契约 + 负向验证）。**【待验收 · 本地提交未推送 · 图形与观感请船长实机复看】**
- 2026-09-15：**内容工作台：表头按新名词改名 ＋ 旧表头别名 ＋ 修 items 表枚举漏项（船长「按照新名词改名」· 一号 · main）**——**①船长原话（照抄）**：「**按照新名词改名**」（承接上一条「依旧还是有三钛合金一类旧名称存在」我提的待定项）。**②表头改名（跟随现行术语）**：`kind(ore**原矿**/mineral**原材料**/gas气体/ice冰矿/ammo弹药/drone无人机)` · `空间站收购价**信用点**` · `空间站售价**信用点**(0=自带/仅制造)` · `奖励**信用点**`（`ISK` 退场、`矿石/矿物` 退场）。**③顺带挖出并修掉一个硬阻断（原表根本导不回去）**：items 表 `kind` 列**手写了 7 类枚举**，而引擎 `ITEM_KIND_LABELS` 有 **12 类**（多 `wreck/container/matter/fragment/aicore`）⇒ 导出件里那几类被判"非法枚举"，`content:import items` **整表校验不过、一个字节都不写**（实测 dry-run：5 张货柜卡 + 蓝图货柜 + 谜质 + AI 核心全红）。改法 = **枚举与表头一律从引擎单点派生**（`ITEM_KINDS = Object.keys(ITEM_KIND_LABELS)`；表头 `ITEM_KIND_HEAD` 由它拼出）⇒ 以后加物品类别自动跟随，回到 schema 自己的「枚举值域纪律」。**④旧表头别名（保升级不丢列）**：表头是**两端契约**（导入按表头匹配列），改名会让"改名前的文件"那几列变未知表头而**静默不回写** ⇒ `content-schema.ts` 新增 **`HEAD_ALIASES`（旧表头 → 现行表头）**＋`normalizeHead()`，`content-import.ts` 先归一化再匹配、用到即打印「ℹ️ 文件里有 N 个旧表头列，已按现行术语映射（建议重新 export）」。**⑤实测（7 张表全量往返）**：重导后 `content:import <表> <csv> --dry-run` 逐表跑——**skills ✅ · items ✅（修好）· modules ✅ · belts ✅ · market ✅（跳过 23 行派生只读卡，属既有预期）**；**两处既有缺陷如实登记、未擅动**：**(a) anomalies 表导不回去**——导出含 5 张 `wh-*` 洞内敌卡，但它们住在 `data/wormholeFoes.ts`，而 schema 的 `file` 只指 `anomalies.ts` ⇒ 导入端"找不到对象块"（需要把 `file` 扩成多文件）；**(b) ships 表往返不幂等**——不改任何数据也会报「计划改动 54 处（15 条 `sh-wh-*`）」，且源文件里已留有 JSON 风格残迹 `shieldResist: {"kinetic":0.5}`（像历史导入写过），疑为 obj 列"值相同也照写"；两者都不影响本次改名，按"发现→报告→等裁定"处理。**⑥验证**：typecheck 四包 **0 错** · `content:check` ✅（「内容工作台契约」照旧全绿）· `ui:rot-check` ✅ · 重导 7 表 854 条；旧表头文件的 dry-run 导入**实测走别名通道**（提示 2 列已映射、不再报未知表头）。**⑦文档**：`docs/content-workbench.md` 补「表头已按现行术语统一 ＋ 旧表头可导入 ＋ 改名后必重导」三条；上一条 roadmap 的「待你定」同步标注为已裁定。**【待验收 · 本地提交未推送】**

## 封存卷索引（更早批次已移入 `docs/archive/`，点开即读）

| 卷 | 收录日期 | 条目 | 体量 |
|---|---|---|---|
| （暂无——批次条目都还在窗口内） | — | — | — |

> 上面每卷都是**原文冻结件**：不重写、不摘要、不追加。`docs/archive/README.md` 另有"已办结待办单"卷。
