# 开发约定（Development Conventions）

> 本文件是仓库级"协作铁律"，与代码同等重要。**每一轮会话开工前，先读本文件。**
> 所有与用户（船长）的沟通、思考过程呈现、总结、文档、代码注释、日志一律使用中文。

---

## 一、确认流程（最高优先级，用户明确要求）

用户原话（语义记录）：

> "当你有疑问提问完后进行总结时，应该等我确认你的总结文档没有问题后再开始进行工作流程。"

### 适用场景（满足其一就必须走流程）
- 新的大功能 / 系统级改动（如：市场、战斗、舰队重构这类）；
- 涉及设计取舍、数值体系、存档结构（version 迁移）的改动；
- 用户需求模糊、有多个可选方案、或我判断需要拍板的地方；
- 用户没有明确说"开始做"的实现请求（先确认范围再动手）。

### 标准步骤（四步闸门）
1. **集中提问**：有疑问先一次性问完（不要边做边问）；
2. **中文设计总结**：给出完整总结（方案 + 关键取舍 + 涉及模块/数值 + 边界情况），
   并在结尾明确写"**以上总结请你确认，确认后我再开始实现**"；
3. **等待确认**：停手。**"用户没有反对" ≠ 确认**，必须得到明确的"可以 / 没问题 / 确认"；
4. **确认后实现**：开工，并把已确认的决策落进对应设计文档
   （`docs/design/…`，顶部标注"状态：已确认"）。

### 例外与回补
- **完全基于已确认设计的"执行落地"**（如 V9 市场系统：此前多轮确认过 V1→V5 总结，
  含"砸盘叠加上限不做限制"）可以继续实现；但每完成一个段落要主动向用户说明
  "这轮做了 X，基于你此前确认的 Y"，并给出复核入口（总结/文档）。
- 若实现中发现需要**推翻已确认设计**的重大问题：停下来重新提问 + 总结 + 等确认，
  不允许擅自替换方案。

### 红线
- 未确认不动工（设计未定的部分）；
- 不把"随便 / 你看着办 / 都行"当正式确认（需再追问一句明确范围）；
- 不在总结里藏私货：修改了什么、删了什么、新增了什么，必须列清楚，等用户看见。

---

## 二、思考与工作过程约定

### 动脑前先动手读
- 接手任何任务先读：**仓库根 `AGENTS.md`**（二号 2026-09-06 发布：可执行精简版约定，随会话自动注入上下文；**压缩/续接后若上下文中未见其内容，先重读它**）→ 本文件 → `docs/architecture.md` → 相关 `docs/design/` 文档 → 代码现状；
- 不凭记忆/猜测写代码：改结构前先查全部引用（界面、测试、工具脚本）再动手；
- 项目内已有"验证闭环"和"文档纪律"，新代码必须沿用，不要另起炉灶。

### 实施节奏
1. 大任务先列任务清单（分步、可见进度）；
2. 一次只推进一组相关改动，改完立即自检，避免长距离无验证的"连写"；
3. 每步自检闭环（见下），出现失败先查明原因再继续，不绕路；
4. 有疑问但用户不在场：记录下来进总结，不猜着做。

### 验证闭环（项目固定动作）
- `npm run typecheck`（core / data / ui / desktop 四包全绿）；
- `npm run test -w @whale/core`（vitest；改行为必须同步改/加测试，断言确定性优先）；
- `npm run build`（electron-vite）；结构改动后跑 Electron 冒烟 + 真实存档迁移检查
  （`%APPDATA%\whale-idle\save.json`，动它之前先备份）；
- 数值/经济改动跑 `npm run balance`（tools/balance-check.ts，三策略 24h 对比）；
- 临时调试文件（`tools/_*.ts`、`tests/_dbg.test.ts` 之类）**不一律"用后即删"**：按工具纪律
  （第十章）收尾二选一——有复用价值转正式入库，确属一次性才删除。

### 汇报与文档纪律
- 每完成一段功能：中文汇报——**改了哪些文件 + 行为变化 + 验证结果 + 已知取舍**；
- 功能/方案设计 → `docs/design/`（标注确认状态）；结构/版本 → `docs/architecture.md`；
- 新约定、教训、决策 → 追加/更新本文件；
- 会话结束时若 Hindsight 记忆可用，把约定与已确认决策同步进记忆库；不可用则本文件即记忆。

---

## 三、多 agent 并行协作（大鲸鱼二号 / 其它会话）

> 背景：同一工作区可能有多条工作线并行（如"大鲸鱼二号"负责战斗系统与物品导入）。
> **本会话与二号没有直接通信通道**——用户是唯一协调桥梁；本会话通过共享文件系统观察对方。

### 疑似检测信号（命中任意即进入"疑似"状态）
- 一批文件在短时间内被批量改写（几分钟内十几个文件、mtime 密集变化）；
- 结构版本号/类型定义跳变（如 CURRENT_STATE_VERSION 突然 +1、新增 XXX.ts 基础设施文件）；
- 出现他人风格的大范围重构痕迹（新常量文件、类型字段扩充：ModuleSlot 增加槽位、ShipDef 增加字段等）；
- typecheck/测试突然大面积红且报错指向"我最近没碰过的文件"。

### 发现疑似时的行为（先停手，再报告）
1. **立即停止写入**疑似重叠区域：不覆盖、不回滚、不替对方"修复"中间态；
2. **核对自己的改动是否存活**（grep 关键函数/常量），并确认对方改动是否基于我的代码之上；
3. **第一时间向用户报告**，格式固定：
   - 证据（文件清单 + 时间窗 + 内容推断）；
   - 影响面（红测试/编译阻塞清单；波及哪些我的成果）；
   - 我的判断（中间态 or 已完成；与分工的重叠点）；
   - 交接建议与"完成信号"（如 typecheck 全绿 = 可以接手的明确信号）；
4. 用户协调明确前，对相关区域保持**只读观察**；收到指示后再继续（等对方合入 / 我接管 / 分区并行）。

### 协作红线
- 同一文件双方都需要改 → 默认让用户协调错峰，不抢先覆盖；
- 不做"预防性重构"去适配对方未完成的设计；
- 对方完成后要求我适配（如 UI 跟随新类型）时，先确认其"完成信号"再动手。

### 工作区分工与合入纪律（船长 2026-09-05 定）
> 背景：曾发生两人在同一棵工作树改文件导致改动互相污染、回滚误伤（冷却改造与 v21 收尾混在同一 diff）。
> 船长拍板：既然用了 git，两个 agent 的工作区相互独立。

- **物理隔离**：主工作树 `H:\大鲸鱼\Deepseek-EVE`（分支 main，一号/船长独占）；二号工作树
  `H:\大鲸鱼\Deepseek-EVE-d2`（同仓 `git worktree`，分支 `d2/*`，2026-09-05 建立）。
  二号**永不直接写主树文件**，一切改动在 d2 完成并提交。
- **基线同步**：二号开工前 `git fetch . main`（同仓）确认基线；主树有一号未提交 WIP 时视基线
  未定型，等稳定 commit 再动手/合入。
- **完成即合入（船长原话："每完成一次安排的工作后就进行合并"）**：二号完成一件安排的工作、
  自测全绿（core 测试 / typecheck / 相关工具验证）后即合并回 main——先在 d2 侧
  `git fetch . main` + rebase 解冲突，确认主树干净（无未提交改动）后在主树执行 merge；
  合完二号侧追平 main 并清理临时分支。
- **推送闸门（船长 2026-09-08 定，与公告发布同批）**：**开发/推进过程中只做本地 commit，不推送
  `origin`**；只有在该工作**确实完成**（自测全绿 + 船长验收口径满足，含大更时公告稿已通过
  船长审核并写入 `announcements.ts`）后，才把**代码与公告一起推送**一次；
  未完成批次即使已本地提交也留在本地，标注"待验收/待发布"；
  需要远程同步的临时理由（如二号合并需要主树基线推送）先问船长。
- **推送网络策略（船长 2026-09-08 定：直连失败则走代理）**：本机系统代理 = `127.0.0.1:7897`
  （git 默认未配置代理、直连 GitHub 常被重置）。推送/拉取先试直连，失败即用
  仅命令级代理重试：`$env:HTTP_PROXY='http://127.0.0.1:7897'; $env:HTTPS_PROXY='http://127.0.0.1:7897'; git push origin main`
  （不写持久 git 配置；若日后代理端口变化，以系统代理设置/监听端口为准）。
- **文档同步（船长 2026-09-05 定）**：docs 目录留在主树仓库内，是**唯一权威**；二号 d2 工作树
  不维护 docs 副本，需要读规则/设计文档时直接来主树 `H:\大鲸鱼\Deepseek-EVE\docs` 读取
  （天然无法跨工作树共享，避免重复维护两份）。
- **真档与实测**：涉及 %APPDATA% 真档的操作只读或走副本备份，绝不覆写一号正在玩的档。

---

## 五、检索纪律（用户 2026-09-04 定）

> 查找某个物品/条目/字段并定位其文本时，**尽量不靠上下文，而是依靠 id/唯一键快速检索**。

- 本仓库全部内容与状态对象都以 **id / key 为主键**（item/anomaly/belt/galaxy/skill/module/ship 的 `id`、
  市场 `good.key`、类型字段名、存档字段名），可做到快速精确检索：
  1. **首选 grep 唯一锚点**：如 `id: 'ore-glowstone'`、`good.key: 'gas-neon'`、字段名 `standingReq`；
  2. **改条目文本/数值**：先以该条 id 定位所属块，再在该块内作小范围修改，**禁止用整段长文本
     近似匹配去做改写**（易与另一条相似文本误匹配、也易受文案改版影响）；
  3. **文案重写**：按 id 索引定位 `description:` 所在行，整行替换（不必先猜旧文案全文）；
  4. 需跨表核对时可用一次性 node/tsx 脚本读取 data 目录做结构化查询（如安全等级/距离矩阵），
      用完按工具纪律（第十章）处置——有复用价值转正式入库，一次性即删（`tools/_*.cjs|ts`）；
  5. 无 id 的纯文案场合（日志/提示语）才回退到"少量上下文 + 唯一化判断"。
- 好处：检索与替换稳定、不误伤、对内容演进的改动（改名/改文案）免疫。

---

## 六、会话续接与压缩纪律（用户 2026-09-04 定）

> 目标：把"上下文压缩导致细节丢失"的影响降到最低，让长会话/跨会话工作流稳定续接。

事实与机制（对船长透明）：
- 模型**无法读取剩余上下文配额，也无法主动触发压缩**——压缩由运行环境在轮次边界自动进行
  （本仓库会话已验证：自动压缩后同一工作线无缝续接，磁盘上的代码/存档/文档不受影响）；
- 压缩的真正风险只在"尚未落进文件的口头约束 / 进行中任务的精确下一步"这类对话内细节。

纪律（我方承诺执行）：
1. **里程碑即固化**：每个已确认功能完成 → 落 `docs/design/`、`architecture.md`、本文件（文档即记忆）；
2. **新系统级大工程开工前，主动提醒船长"此处是良好压缩点"**，并先产出「续接卡」：
   下一步、涉及文件、验证状态、待确认项——再开始动手；
3. 每轮汇报末尾保留"下一步"要点（便于 checkpoint 提取与船长跟进）；
3b. **批量需求纪律（船长整批下发 roadmap 置顶单时）**：默认逐条走四步闸门；归类"可直接执行"须先说明归类理由；**任何含糊表述一律先问清，禁止以合理猜测替代**——对确认流程红线的强化；
4. 船长可随时要求"整理续接卡"或"先固化状态"——我方立即把当前会话状态写入文档/列表再继续；
5. 开启全新会话接力时：先读 本文件 → 相关 design 文档 → 续接卡/变更记录，再动代码。

---

## 七、沟通与语言
- 一律中文：面向用户的叙述、推理过程呈现、总结、文档、代码注释、游戏内文案、日志；
- 汇报不刷屏：结论先行（做了什么 / 能否用 / 有什么坑），细节附录或展开待问；
- 用户给出新约束时：先复述确认（一句话），再落进本文件/设计文档，形成闭环；
- 船长询问"下一步 / 新工作流 / 继续做什么"时，**先读 `docs/roadmap.md`（后续工作流备忘）再讨论**；roadmap 状态随采纳/完成情况同步更新。

---

## 八、测试门槛存档约定（船长 2026-09-04 定）

> 目的：大功能交付后船长能立刻实测，不需要手工改存档凑测试条件。

- **适用判定**：批次 B（大系统/结构级/规则数值）与新玩法数值的首次交付；批次 A 小修不配。
- **交付物（双份）**：
  1. 仓库内可复现脚本 `tools/make-test-save.ts`（按功能注入门槛）；
  2. 交付时运行脚本生成的存档文件 + 说明，落 `docs/test-saves/`（README 记录加载方法）。
- **注入基础**：**基于船长当前真档复制注入**（保留其现有进度；运行前自动把真档备份到输出目录旁，绝不写坏原档）。
- **加载方法**（交付说明内固定写明）：游戏内先「存档管理 → 备份」→ 退出游戏 → 用生成的存档文件替换
  `%APPDATA%\whale-idle\save.json` → 启动即测；测完可用游戏内恢复备份回原档。
- **注入原则**：只补"难达成的门槛"（资金/声望/星系点亮/舰船/核心/装配弹药等），
  可达成的操作（跑一段航程、点一个按钮）不代做；注入前后各写一条 system 日志说明；
  走 loadSaveFile/serializeSaveFile 保证字段合法与版本兼容。
- **功能门槛注入以 case 注册制扩展**：每个功能一个 case（b1 = 低安遭遇…），列表维护在脚本头注释。
- **命名规则（2026-09-08 船长定）**：测试存档命名必须符合用途——文件名 `<feature>` 段 =
  注册 case 名（该档服务的唯一用途），固定 `test-save-<feature>-<stamp>.json`，禁止随意命名；
  新 case 先注册（脚本头注释 + INJECTORS + docs/test-saves/README.md 档案清单）再生成。

---

## 九、UI 风格一致性（船长 2026-09-04 定）

> **底层规则（船长 2026-09-06 升格）**：任何位置**新增内容**——新卡片/新行/新弹层/新标签页/
> 新页面/新图标/新增文案位——样式一律**优先参考"同级下的其它相似项"**：同列表或卡片组内的
> 相似项 > 同页面其它区块 > 同功能模块其它页面 > 全仓 `app-*` 家族（styles.css）。先复刻既有
> 结构与类名、再按需做最小差异，禁止另起炉灶自造"看起来差不多"的新样式——避免风格割裂。
> （做任何界面调整同理：先对照同级是否已有规范风格。）

- **先复刻后差异**：新内容落地前先找同级相似实现（同款卡片/行/按钮/弹层），复制其
  结构 + 类名再改差异部分；不确定"同级相似项"是谁时，对照最接近的既有界面或先问船长；
- 页面级「功能标签页」规范 = 星图页的 `.app-subtabs / .app-subtab`（胶囊按钮 + 图标 +
  label，激活青色高亮，`role="tablist"/"tab"` + `aria-selected`）——示例：物品页
  「仓库/货仓」子标签已按此统一；
- 行级列表/卡片规范 = `.app-inv-list/.app-inv-row`、`.app-shipchip`、`.app-btn`
  家族、`.app-subtab` 等既有样式；新增页面先 grep 同级页面与 styles.css 是否已有
  同类实现再动手；
- 命名沿用 `app-` 前缀与状态后缀（`is-active`/`is-warn`…）；CSS 尽量只补缺口，
  不重定义既有组件样式。
- **尺寸稳定性规范**（船长 2026-09-05 定）：窗口/容器/卡片/格子尽量用固定尺寸
  （固定 height / min-height / grid 等宽列），**不允许随内容物变大变小**——装备入槽、
  文案换行等状态变化不得引起界面跳动；设计时按最大情况预留空间（长名
  `text-overflow: ellipsis` 单行截断、图标行固定行高、状态后缀元素计入高度）；
  空态与满态共用同一盒模型（含 `box-sizing: border-box`），只换内容不换尺寸。
  实证：装配页槽位格在装入装备后高度变化，已修（`.app-fit-slot-icon` 固定 76px）。
- **一级页不滚 / 滚动只进二级窗口**（船长 2026-09-08 定）：
  ①一级页 = 导航栏直系页（星图/舰船/装配/物品/市场/工业/技能）：**整页无页面滚动条**，
  页面高度 = 主窗口内容区高度（导航/活动栏固定），溢出滚动一律放进**二级窗口**
  （页面内标签页/功能窗的列表、卡网格、表格等内部滚动条）；
  ②**前提红线：一级不滚仅在其内容能被窗口完整装下时成立**——不得为了去掉滚动条
  而硬裁/隐藏任何应有内容；内容确实放不下、又无法拆分进二级窗口的页面，
  **单独提出与船长讨论修改方案**，不擅自强行落地；
  ③二级窗口滚动统一走既有内滚样式族（`.app-*` 列表/网格 + `overflow-y:auto;
  min-height:0`），滚动条不新造样式。

---

## 十、工具纪律（船长 2026-09-05 定）

> 背景：小工具/脚本"用完即删"或"不入库搁置"都浪费——正式工具入库沉淀（有版本、有文档、
> 可复跑），临时探针无版本地原地演进后往往需要从头重建（观察实证：C4 期间 `_synth-check.ts`
> → `_dbg-synth.ts` 活体迭代，git 历史中正式工具零删除）。

- **正式工具**（体检/校准/批量生成等可复跑）：`tools/xxx.ts`（不带 `_` 前缀）**入库** +
  头部注释（用途/用法/输入输出）；常用者挂 `package.json` 的 npm script
  （现例：`content:check` / `balance` / `battle:calibrate`）；改动随所属功能提交维护。
- **临时探针**（单次诊断，`tools/_*.ts`、`tests/_dbg.test.ts` 等）：允许以 `_` 前缀暂存不入库，
  但**任务收尾时二选一**，判定标准一句话——"下次平衡/体检/内容变更还会复跑它吗？"
  1. 会 → **转正式**：去 `_` 前缀、补头部注释、入库（必要时挂 npm script）；
  2. 不会 → **删除**，不留工作区垃圾。
- 探针命名一律 `_` 前缀：一眼区分 + 收尾清点入口 = `git status` 里的 `?? tools/_*.ts` 待处置清单。
- 多 agent 并行时探针归作者自处置：**不替对方删除或收编其探针**——疑似对方在用则保持只读
  并向船长报告（同第三章协作红线）。

---

## 十一、术语词典纪律（船长 2026-09-05 定）

> 背景：游戏自创术语多、跨里程碑复用，会话压缩后名词含义可能漂移（实证：B3 讨论中
> "低安"等既有术语被遗忘/重新发明）。为跨会话沟通一致，术语以词典为权威。

- **权威词典 = `docs/glossary.md`**（仓库级，根目录）：通用游戏术语 + 各里程碑专用术语
  一律先登记再使用；写作文档/代码注释/与船长沟通前，名词存疑先查词典，不凭印象猜；
- **新术语登记制**：新系统设计定稿词（船长拍板或总结确认后）由经办人**当天登记进词典**，
  带定义/公式/出处与状态（已定稿/待定）；
- **压缩遗忘处理**：上下文压缩导致遗忘属正常——查词典即可恢复，不必让船长重新解释
  （确有歧义或词典缺失才提问，提问时注明"词典未收录"）；
- 各 `docs/design/` 文档可保留局部细化，但与词典冲突时以词典为准并回头修订词典。

---

## 十二、游戏内更新公告（船长 2026-09-05 定；2026-09-08 增"发布审核"流程）

> 每个"大的系统添加或更新"完成并入主树时，**经办人必须准备一条玩家可见的更新公告**
> ——规则同时适用于一号与二号（谁完成大更谁备公告，不需要等船长/对方）。
> 公告数据 = `packages/data/src/announcements.ts`（每批在**文件最上方**新增一条）。

- **判定"大"**：新系统/新玩法上线、存档结构或大数值体系变更、界面信息架构调整、重大修复批次
  （如序章·苏醒整批）；纯文案/小修不强制，可顺手并入同批公告；
- **写法**：`id`（日期+短名）· `title`（一句话）· `date` · `tag`（系统/内容/数值/修复）·
  `bullets` 3~5 条玩家向说明（改了什么、如何体验），中文；
- **发布审核（2026-09-08 船长定）**：开发交流/设计讨论内容**不得直接写进公告数据**；
  批次发布前，经办人把公告文案（id/title/tag/bullets）**整理成待审稿递送船长审核**，
  得到明确批准后才写入 `announcements.ts` 合入——被驳回则按船长意见修改后再审；
- **人称与话术（2026-09-08 船长补定）**：①公告文案**禁用第一/第二人称代词**
  （我/我们/你/你的/我方/玩家你……），一律以**「玩家」**作第三人称表述
  （例："玩家接近敌方近身盲区时……"，不写"你可以……"）；②**开发/验收话语不进公告**——
  复测/校准/对齐口径/目标带/矩阵/参数名等"我们调过并验证"类表述一律不写，
  公告只说"世界发生了什么"（改了什么、玩家会体验到什么），不说"开发者做了什么/我们如何保证"；
- **生效方式**：公告数据随代码合入即发布——玩家下次启动若存在未读（顶栏「公告」红点）会
  自动弹一次，之后随时可从「公告」按钮回看历史；已读状态存 localStorage
  （`whale-idle:announce-seen`），不清档不重弹；
- **UI 归属**：顶栏「公告」入口在 `apps/desktop/src/renderer/src/panels/Announcements.tsx`
  （App 顶栏按钮 + 弹层），新增公告无需改 UI。

---

## 十三、游戏内文案纯净与设定（船长 2026-09-06 定）

> **底层规则**：玩家可见的一切文本（UI 文案、按钮/悬停提示、日志、公告、手册、教程、
> 任务/事件/卡面描述等）**必须符合游戏内世界观设定**；**开发/协作/商讨阶段的内容不得
> 进入玩家可见范围**——玩家只应看到"这个世界里的话"，不应看到"开发者的话"。

- **禁入清单（开发痕迹）**：版本号/日期/作者/经办人、「复核/待定/暂定/TODO/占位/口径/
  兼容/旧档」等工程措辞、内部代号/文件名/字段名、讨论与审批语境的表述、调试/探针信息
  ——一律不得出现在玩家可见文本；确需表达"尚未确定/后续会变"时，用符合设定的叙事措辞
  或与船长约定玩家向表述；
- **新文案默认先过"设定关"**：动笔时默认自己是游戏世界里的角色，科技/组织/地名/物品的
  表述以 `docs/glossary.md` 与既有内容表风格为准；与现实世界、开发语境、工程术语混同的
  一律改写；
- **交付前自查**：经办人对自己新增/修改的可见字符串做清单扫描（UI 卡面/日志模板/公告/
  教程/任务与事件文本/悬停提示），确认无版本日期、复核词、占位符、口径词、内部标识后
  交付；发现历史残留（旧版本遗留的开发备注、日志冒出开发文案等）按第五章检索纪律定位
  清理，并在汇报中报告；
- 「公告」「教程」等带 meta 性质的界面同样适用：一律玩家向语言，不出现开发语境。

---

## 十四、变更记录
- 2026-09-08：**AI 核心统一启用上限（大鲸鱼二号；玩家反馈"AI 核心可无限制启动精炼炉"，船长定底层口径 = 「人工智能专家」限制同时启用的 AI 核心总数、与用途无关）**——①新语义：同时启用上限（`aiCoreCap`）= AI 核心上限技能贡献之和（现唯一 = ai-expert 等级，后续上限技能在此叠加），Lv0 = 0 不可启用；②占用池 = AI 副船任务 + AI 精炼炉/回收炉/制造线**共用**（副船 checkAssignable 与三处炉/线启动走同一 `aiCoreCapBlock` 守卫；旧作业豁免的制造线不计入）；③存量超限（读档/技能变化）不中断运行、但计入占用，新启用必须先降到上限内；④`maxAiSlots/aiSlotsUsed` 更名 `aiCoreCap/aiCoreUsed`，副船名额并入统一池；⑤文案/口径同步：skills.ts「人工智能专家」描述改"每级 +⟦1 枚⟧ AI 核心上限"、ShipPage AI 指挥中心（"AI 核心启用 X/N"、不点名单技能）、手册「AI 副船/精炼/制造」条目、ai.ts/industry.ts/manufacturing.ts/balance/types 头注、tools（salvage-econ 炉位规则、playthrough-sim、make-test-save）；⑥测试：core +6（统一池真值表：Lv0 拒/炉满额再开拒/副船×炉互斥/线同池第三处拒/存量超限不中断照常结算/旧作业豁免不计）+ 既有 AI 炉/线用例补资格 → **515 绿** + typecheck ×4 绿；⑦公告口径（2026-09-08-market-manufacture 劳动者制第 2 条"AI 核心驱动不受此限/核心库存即并行上限"）需修订 = 待审稿递船长，未写入【待验收/待发布：推送闸门内】。
- 2026-09-08：**连续出击离线续跑修复（大鲸鱼一号；玩家反馈）**——simulateOffline 在连击开启且非战斗冻结时按 30s 分片推进、片边界同在线条件自动再出发（离线持续讨伐有战果）；最后一片结束不触发；非连击离线仍单次大推进；core 490 + typecheck ×4 + desktop build 绿【待验收/待发布】。
- 2026-09-08：**文案归位第一批执行（大鲸鱼一号；船长指示续做存档批次）**——星图·远征页「停靠母港/停靠副站」空态行改情境向（去说明书式机制段）；机制细节移入手册速览：「远征」条目更新为当前口径 + 新增「副空间站」条目；copy-cleanup-pending.md 转执行记录（速览整体改版与后续巡检批次待办）；typecheck + desktop build 绿【待验收/待发布】。
- 2026-09-08：第三章补"推送网络策略"（船长 2026-09-08 定：直连失败则走代理）——本机系统代理 = `127.0.0.1:7897`，git 未配置代理时直连 GitHub 常被重置；推送/拉取先试直连，失败即用仅命令级代理重试（`HTTP_PROXY/HTTPS_PROXY=http://127.0.0.1:7897`），不写持久 git 配置。
- 2026-09-08：第三章补"推送闸门"（船长 2026-09-08 定，与公告发布同批）——开发/推进中只本地 commit 不推送 origin；工作确实完成（自测全绿 + 船长验收口径满足，大更含公告稿已过审）后代码与公告一起推送一次；未完成本地提交标注"待验收/待发布"；AGENTS.md §4 已同步。
- 2026-09-08：第九章新增"一级页不滚 / 滚动只进二级窗口"规则（船长 2026-09-08 定）——一级页（导航直系页）整页无滚动、高度 = 主窗口内容区，滚动只进二级窗口（标签/功能窗列表与卡网格内滚）；**前提红线：内容必须能完整装下，不得硬裁去滚；放不下又无法拆分的页面单独提出与船长讨论方案**；AGENTS.md §6 同步。
- 2026-09-03：确立确认流程（四步闸门）、思考工作过程约定、中文语言约定、验证与文档纪律。
- 2026-09-04：新增"多 agent 并行协作"约定——疑似对方工作区改动时先停手、报告证据/影响面/判断/交接信号，用户协调前保持只读；确认无直接通信通道，用户是唯一协调桥梁。
- 2026-09-04：新增"检索纪律"——查找/改写内容一律以 id/唯一键为锚点快速检索，禁止长文本近似匹配改写；并确认快速检索完全可行（id 主键体系 + grep 锚点 + 一次性结构化查询脚本）。
- 2026-09-04：新增"会话续接与压缩纪律"——模型无法主动触发压缩（环境在轮间自动执行），但承诺：里程碑即固化文档；新系统级大工程开工前主动提示压缩点并先产续接卡；船长可随时要求整理续接卡/固化状态。
- 2026-09-04：新增"测试门槛存档约定"（本章八）——B 批次与新玩法数值交付时配可测存档（tools/make-test-save.ts + docs/test-saves/），基于当前真档注入。
- 2026-09-04：新增"UI 风格一致性"约定（本章九）——界面调整前先对照同级相似界面的既有规范并复用类名；物品页「仓库/货仓」标签与星图页 `.app-subtab` 规范统一；顺带清理变更记录历史重复条目。
- 2026-09-05：新增"工具纪律"约定（本章十，船长拍板"采纳并写入约定"）——正式工具（可复跑）一律入库 + 注释 + 常用挂 npm script；临时探针（`_` 前缀）任务收尾二选一：有复用价值 → 转正式入库，一次性 → 删除；多 agent 不替对方处置其探针；同步修订第二章验证闭环与第五章检索纪律里"用后即删"旧表述。
- 2026-09-05：新增"术语词典纪律"约定（本章十一，船长要求"名词解释做交流文档 + 查询文档写入约定"）——建立 `docs/glossary.md` 仓库级术语权威，新术语登记制，压缩遗忘以词典为准。
- 2026-09-05：第九章补"尺寸稳定性规范"（船长反馈"槽位高度建议固定，不要随内容物变大变小，设计需考虑最大情况"）——容器/格子固定尺寸、空满两态共用盒模型；实证修复装配页 `.app-fit-slot-icon` 固定高 76px。
- 2026-09-05：精炼炉改**多工位并行**（船长拍板：主控亲自运转限 1 台 + 每枚闲置 AI 核心驱动一台；核心库存即并行上限）——存档结构 v19（refineRun 单例 → refineRuns 工位表，含 v18→v19 迁移）；停炉按资源 id 定位；顶部活动栏精炼逐炉一条；工业页精炼炉按矿带卡网格常驻，运转中卡不改样式只把按钮变停炉。⚠️ **需二号复核（B3 经济）**：残骸回收炉现可多台并行 → `tools/salvage-econ` 的"单炉炉速 1440 m³/h 上限"假设不再成立，回收吞吐上限 = 闲置核心数 × 炉速，请复核经济锚与打捞-回收链平衡。**✅ 2026-09-06 二号复核收口（船长确认）**：`tools/salvage-econ` 已按多炉并行重跑，结论 Y 与单炉经济锚均不变，上限口径更新为"闲置核心数 × 单炉炉速"，挂账关闭。
- 2026-09-05：第三章补"工作区分工与合入纪律"（船长拍板）——主树（main，一号独占）与二号 worktree（`H:\大鲸鱼\Deepseek-EVE-d2`，`d2/*`）物理隔离；二号每完成一件安排的工作、自测全绿后即合入 main（先 rebase 解冲突、主树干净才 merge），真档只读/走备份。
- 2026-09-05：第三章补"文档同步"（船长定：docs 留主树为唯一权威，二号 d2 不维护副本、需要时来主树读取）。
- 2026-09-05：⚠️ **能量伤害克制数值变更（需 C4 复核，二号）**：`combat.typeLayerMult` 能量（plasma）对护盾 0.75 → **1.25**（船长拍板；激光/能量弹系对盾显著增强、无克制弱点）——影响胜率预估与 PvE 时长结构，请二号复核平衡与模拟基线；同时弹药 chip 悬停与手册「航行须知·伤害克制速查」补矩阵展示（数值与 typeLayerMult 同源）。**✅ 2026-09-06 二号复核收口（船长确认）**：战斗复核结论 = 维持 1.25（激光系早期/中期强势、高威胁墙仍在；能量 1.25 不回调），挂账关闭。
- 2026-09-05：新增"游戏内更新公告"约定（本章十二，船长要求"每次大的系统添加或更新就发布公告告知玩家，写入规则以方便告知一号"）——大更完成由经办人补 `packages/data/src/announcements.ts` 新条目；顶栏「公告」入口（未读红点 + 启动自动弹一次 + 历史回看）已上线，首个公告 = 序章·苏醒整批。
- 2026-09-06：建立根目录 `AGENTS.md`（船长定：现有约定执行不佳，改为 AGENTS.md 随会话自动注入以强制约束）——内容为本文件"可执行精简版"（开工顺序/四步闸门/验证闭环/工作区合入/检索术语/UI 工具/游戏公告/会话续接/测试档），细则冲突一律以本文档为准；**AGENTS.md 内容变更必须同步本文档变更记录**。
- 2026-09-06：第二章「动脑前先动手读」补 **AGENTS.md 为首读项 + 压缩/续接重读纪律**（一号；船长转达二号提示"仓库根有 AGENTS.md，开工先看"）——开工顺序 = AGENTS.md → 本文件 → architecture → 相关 design → 代码现状；自动注入非 100% 保证，压缩后上下文未见其内容即重读。
- 2026-09-06：**平衡复核收口 + AI 副船返航同口径（二号；船长 2026-09-06 复核会"我没什么异议"确认默认项）**：①能量（plasma）1.25 战斗复核——维持现状收口、挂账关闭；②B3 打捞-回收经济复核——`tools/salvage-econ` 多炉并行重跑已并入，结论 Y 与单炉锚不变、挂账关闭；⑤**AI 副船采矿/打捞返航腿扩展同口径**——`trips.scaledReturnMs`（满仓基准 × 货仓占比，钳 0~1、下限 1ms）先行缩放再按核心效率拉长（采矿 ai.ts oneLegMs 腿、打捞 ai.ts legBaseOf 腿；满舱占比 1 = 原时长不变，部分货载/富矿脉余量返航变快）；core 测试 +2（采矿/打捞各一条部分货载 25% → 实返航 75s 回归，旧口径需 300s）。
- 2026-09-06：**新增两条底层规则（船长 2026-09-06 定）**：①第九章「UI 风格一致性」升格总则——任何位置**新增内容**（卡片/行/弹层/标签页/页面/图标/文案位）样式一律**优先参考同级下的其它相似项 UI**（同组相似项 → 同页其它区块 → 同功能模块其它页面 → 全仓 `app-*` 家族），先复刻后最小差异，禁止自造相似新样式防风格割裂；②新增第十三章「游戏内文案纯净与设定」——玩家可见文本（UI/日志/公告/任务/教程/手册/悬停提示）必须符合游戏设定，开发/商讨痕迹（版本/日期/复核/待定/占位/口径/内部代号等）禁入玩家可见范围，交付前自查 + 历史残留清理上报；AGENTS.md §5/§6 已同步（维护：AGENTS.md 变更需同步本记录）。
- 2026-09-06：**P2 第二轮「稀有配额制」（二号；已合入主树 9565c44，后被第三轮取代）**——全市场 rare 供给改为全局配额（每窗口 4~9 张浮动 + 整批解锁 +1/批，「现货抢购学」按旧语义放大）；闸内（bmStanding 未达标）商品以 0.04 低权重参与同一配额（取代旧 ×0.01 独立静默），命中即 ×4 价暗市单（标 bm 可绕过拦截，玩家向隐身不变）；rare NPC 订单寿命 ×4（9→36 分钟，买卖两侧）；解锁整批后权重/价格恢复、配额 +1、不转常驻；旧 P2「窗口命中 ×0.01」机制废弃（历史见 `docs/design/power-ladder-rework.md` 实施记录，参数表待船长复核）。
- 2026-09-06：**P2 第三轮「抽取节拍制」定稿并实施（二号，船长逐项确认后 d2 实施）**——取代第二轮配额制：①rare/奇货出单抽取节拍拉长为 **10 分钟**（`RARE_DRAW_PERIOD_MS`；60s tick/common/收购侧不变）；②rare 每窗抽 N = max(1, 8%~15% × 已解锁件数 × 抢购学) 张、**加权有放回（同窗可重复抽中同一类型）**，基数=仅已解锁件数、删除整批 +1/批与每件每窗 1 张限制；③现货抢购学削弱为每级 **10%**（原 25%）；④**奇货订单寿命 4 分钟 → 6 小时**（`orderLifeMs.exotic`，两侧），每抽取窗每件独立 0.8% 掷骰、全窗命中 >2 张随机抽选保留（`EXOTIC_CAP_PER_DRAW=2`）；⑤存档加可选节拍字段（ensureMarket 自动补齐，零迁移）；⑥玩家可见文案同步（市场页限定奇货 4 分钟闪现→6 小时/每 10 分钟一轮）。
- 2026-09-06：**建站交付紧急修复（二号，玩家反馈"空间站建设任务资源无法提交"）**——根因：悬赏/扫描"任务完成即返航"改动后，在建（未建成）副站不再有任何停靠入口，而交付引擎与任务卡仍要求"停靠该站"（首档『奠基』本就应先到工地交付才能解锁泊位，形成死锁）。修复 = 交付前置改为**工地现场**（`playerAtSite`：停靠该站 或 野外停留于站点星系——掩护巡逻/作业到场即可，母港特例保留）；任务卡同步改为现场判定与提示、到位日志改为"现场交付、无需停靠"；core t9 +3 回归例（首档野外交付/解锁后停靠继续/异地拒绝）；core 463 绿 + typecheck ×4 绿。
- 2026-09-08：**公告发布审核流程（船长 2026-09-08 定）**——开发交流/设计讨论内容**不得直接写入公告数据**；批次发布前把公告文案（id/title/tag/bullets）整理成待审稿**递送船长审核**，明确批准后才合入 `announcements.ts`；被驳回按船长意见修改后再审（本章十二已更新；AGENTS.md §7 同步）。
- 2026-09-08：**任务中心·时效任务板收尾固化（二号；船长确认；doc = docs/design/task-board-v24.md）**——机制收口登记（20 分钟整板同源刷新/候选池门槛/税前锚定防套利/快递真实航程与在途互斥/离线末窗结算；sideTasks 自 fb8e6db 起无实质改动、无挂账）；词典登记 时效任务板/资源任务/快递任务 三词；**快递机制复核 = 保持现状（船长拍板）**：免费的是"去程就位"，返程类移动仍计时（自动返航 2×单程等），快递为唯一定向跨站直达航程，等待互斥与"船在跑"一致，×1.30 运费补贴保留，不改代码。
- 2026-09-08：**本地悬赏返航段 + 演习场奖励重锚（二号；船长 2026-09-08 两问拍板）**——母港目标（大鲸鱼Ⅳ）悬赏胜利后不再 0 航程回港：固定返港段 **120s**（= 矿工本地满载返航单程 localLegMs，防零航程白刷，期间不可召回同胜利返航）；演习场讨伐令奖励按新港每分钟费率对齐：1,000 → **3,600 ISK**（3,600÷(交火 2min+返港 2min)≈900/min ≈ 新港 6,400÷7min≈914，取整百略留教学利差）；悬赏卡 meta（本港返港 2 分钟 / 估算奖励·小时含返港段）同步；expedition.test 相应时序回归更新（结算在 chunk 末尾 → 返港段需后续推进）；core 466 绿 + typecheck ×4 绿。
- 2026-09-08：**已建成副站 = 母港镜像 + 所有自动返航选最近已建成站（二号；船长拍板，镜像与返航同批收尾）**——①引擎 `isAtHomeLike`（母港或已建成副站 = 协会基地网络）替换精炼/残骸回收/逆向研究/组装机的 isAtHome 门，CargoPage 出售放行建成副站（共用全局市场），工业/星图/远征/手册文案口径统一；②自动返航统一口径：返航基准 = 目标星系最近已建成站（`returnBackMs` / `builtSiteAtGalaxy`；无建成副站 = 母港），悬赏胜利/失利/撤退与扫描完成到港**停靠建成站**（dockedSite 落点 + 日志动态化）；本地悬赏（落点 = 目标星系）套 120s 返港段；采矿/打捞/AI 本就是最近站口径；**手动召回仍回母港（例外，船长如需再改另议）**；③UI 悬赏卡按最近站动态展示返航分钟与估算/小时；④测试：t9 +3（镜像门真值表/越过门/工地拦截）、expedition +1（目标星系建成站胜利落点 = 该站）与既有时序回归；core 467 绿 + typecheck ×4 + content:check 绿。
- 2026-09-08：**战斗界面敌方射程显示修复（二号；玩家反馈"最小射程显示不正确"）**——battleArcsFor 敌方聚合以 0 作 min 初值再取最小 → 敌方近盲带起点恒显示 0；改为 +∞ 初值取极值（kite 等带近盲的卡现在正确显示 min，如赤潮 ≈1.4km 起）；回归测试 +1（kite/僚机 foe.minM>0）；**敌能量武器命中模型原按方案 B 保留，随后船长改拍光束必中（见下条）**；core 468 绿 + typecheck ×4 绿。
- 2026-09-08：**敌武器命中体系改造（二号；船长 2026-09-08 逐点拍板）**——①**能量（plasma）敌武器 = 光束必中**（开火即中、无视回避、无 miss 弹道），**近盲带保留**（带内威力 ×blindDmgMul），带内至远端威力按 beamPowerFactor 距离衰减（与玩家激光同源语义，实时开火与胜率预估同改）；②**动能/爆炸普遍高命中**：`balance.foeHitRate 0.55 → 0.85`（单发由公式自动降，期望 DPS 恒定）；新增 `AnomalyDef.foeHitRate` 逐卡覆写做**低命中特例**（2026-09-08 首设两张贴设定：占港武装通缉=拾荒者乱射 0.55、泰坦残骸勘探=老化自动炮 0.55）；③新增 `AnomalyDef.foeDmgMul` 等效回退口，四张能量主伤卡（赤潮/深渊门/星髓/蜃影）先挂 **0.35 初值待实测复核**；④测试：expedition +3（specs：beam 化/近盲保留/命中缺省与特例、foeDmgMul 缩放 shotDmg）；core 470 绿 + typecheck ×4 + content:check 绿（红环试验档可复测手感）。
- 2026-09-08：**命中体系平衡补偿 + 锚装矩阵复测（二号；船长选方案 A）**——高命中+低单发在守方回避>0 时期望承伤上升（减法削弱的回避收益），新增 `balance.foeHitCompMul=0.62`（动能/爆炸单发再乘，锚点 ≈ 典型回避 0.22 × 中距衰减 0.7 折算旧 0.55 模型；能量光束不消费）；`battle:calibrate` 全矩阵复测：**主验收（S2/S4 中位）全 watch 卡 100%**（赤潮 34s/深渊门 45s/蜃影 47s/裂谷 63s/泰坦 62s/星髓 38s/噬口 62s/坟场 75s/穹顶 S2 75s·S4 52s），段内时长回带；低命中特例与能量 0.35 保持；core 470 绿 + typecheck ×4。状态：**✅ 2026-09-08 船长实测验收（赤潮 MK1 满配险胜≈合理、MK2 锚装轻松击杀）+ 公告 `2026-09-08-foe-weapons` 过审写入；随批推送 origin**。
- 2026-09-08：**公告人称与话术规则（船长 2026-09-08 补定）**——①公告文案**禁用第一/第二人称代词**（我/我们/你/你的等），一律以**「玩家」**第三人称表述；②**开发/验收话语不进公告**（复测/校准/对齐口径/目标带/矩阵等只说"世界发生了什么"，不说"开发者做了什么"）；本章十二更新、AGENTS.md §7 同步。
- 2026-09-08：**命中体系验收归档（船长）**——赤潮实测通过（MK1 满配险胜≈合理 / MK2 锚装轻松击杀）；**占港武装通缉 / 泰坦残骸勘探的低命中特例（0.55）船长决定跳过逐卡实测**，作为已交付已知项归档（数值面已进矩阵：低命中→单发自动加重保持期望）；后续如需实感复核，单卡 `foeHitRate` 一处即可微调。
- 2026-09-08：**活动栏 AI 生产占用修复（二号；玩家反馈）**——AI 核心驱动的制造线/精炼炉不再占用「玩家活动」行（并入 ⚙ AI 徽标计数：副船+AI 生产，副船为 0 时点击跳工业页；AI 生产取消仍在工业页卡片）；t1 用例同步；core 471 绿。**✅ 船长验收通过（2026-09-08）**。
- 2026-09-08：**事件日志不落盘（会话级；大鲸鱼一号；船长两轮定：先定"不做本地保存/每次开启空白"，后定"不做启动强制清空"）**——存档写盘前剥离 logs；启动/恢复备份**不做强制清空**：离线补时日志照常显示，另补单条「离线结算报告」汇总（钱包/收获/装备/新船/技能明细）；首次载入旧格式档会显示一次旧日志，此后快速重启自然空白；弹窗「详见右侧事件日志」指引保留；通讯提示改「记入右侧事件日志」；无存档版本号变化，loadSaveFile 兼容旧档；core 472 + typecheck ×4 + desktop build 绿【待验收/待发布】。
- 2026-09-08：**精炼配平（两轮定稿）+ 工业页净收益估算（大鲸鱼一号；船长确认；docs/design/refine-balance-50.md）**——玩家反馈"精炼炉收益没扣材料成本"（查证为估算口径问题，引擎每批本就实扣料）：**一轮** 14 条配方按「无技能 ≈ 持平」上调 + 工业页精炼卡 ≈ISK/h 改净口径（扣耗料、负值警示、tooltip 列毛产值；回收卡不变——残骸不可卖无耗料）+ content-check 配平护栏；**二轮（船长复核：工业技能收益过肥）** balance.refining baseRate 0.5→1.0、maxRate 0.95→1.6，配方 perOre ÷2 等价迁移（无技能产出与一轮逐单位相同），满技能净 +82~110% → **+60%~+79%**（技能每级收益约减半）；玩家口径「收率」全量改**「产出倍率」**（skills/工业页/手册/README/词典注销旧词/content-check 与 core 测试同步为 100% 基准）；回收线技能不动（回收 vs 采矿满配差距扩为另行议题）；balance-check 顺带适配组装机劳动者制签名；core 472 + typecheck ×4 + content:check + build 绿【待验收/待发布：推送闸门内，公告稿待船长审核】；组装机收益调整 = 二号分工不涉及。
- 2026-09-08：**取消每次制造费（二号；船长拍板"费用优先"，范围 = 仅每次开工的制造费；蓝图学习价与材料链另行议）**——引擎 `startManufacturing` 不再校验/扣除 `buildCostIsk`（BuildSpec/蓝图数据字段保留为历史遗留并注释）；取消制造/完成日志与提示去"制造费不退"；工业页蓝图卡（制造费行 →"制造免费：只耗材料与时间"）与面板说明、手册蓝图书条目与帮助词条、市场蓝图书悬停、活动栏取消按钮/toast、renderer engine.ts 注释同步；core 测试改口径（开工分文不扣、钱包不足不再拒开工、材料不足拒绝不变）+0 条 472 绿 + typecheck ×4；C2 护栏改"现货 ≥ 材料成本"并按材料基价重估：自制 ÷ 现货 = 0.17~0.61×、21 条全无倒挂（原含费 105% 巨灵鲸级 → 0.61×，试算见 c2-craft-cost.md §六；glossary/roadmap 同步）。状态：**待船长验收（2026-09-08）**。
- 2026-09-08：**市场站内让利吸收（二号；船长确认四问全推荐项 + rare/奇货 F 按字面 0.3/0.1 口径）**——卖侧吸收量与价格挂钩：每窗总吸收保底 = 基础吸收额 F×折价倍率 E，**先吃簿、差额站内补、不叠加**（吃簿 ≥ 配额时不站内收）；E = min(5, 1 + 0.4×折价百分点)（折 1% ×1.4 / 5% ×3 / 10% ×5 封顶，更深让利不再提速）；F：池商品 = supplyFlow、common 单件 = 1、rare = 0.3、奇货 = 0.1 件/窗（per-good 可覆写 absorbQtyPerWindow）；只对挂价 ≤ 现行买盘价（buyPrice 价线）的卖单生效；结余 absorbCredit 小数跨窗结转、撤单即弃（PlayerOrder 可选字段零迁移，save 白名单补读）；税/声望加成/池入账同簿成交，大额让利抛售计入 netVol → 冲击压价自稳；市价大单吃穿簿后的余单自动按边际价享受提速；MarketPage 卖出单价行加动态提示（保底/当前让利 % 与吸收 ×E）；market.test +6（平价保底不叠加 10 窗精确清完、折价 12.5% 封顶、折价 5% ×3、rare/奇货小数结转、p>b 不接 + 撤单退回）；core 478 绿 + typecheck ×4；定稿存档 = docs/design/market-sell-absorb.md（glossary/roadmap 同步）。状态：**待船长验收（2026-09-08）**；挂账：买侧价格档 0.6/0.65/1.0、单件收购件数、抢单系统、公告发布。
- 2026-09-08：**市场收购侧改版：档位 + 簿面件数 + 两侧抢单（二号；船长确认四问全推荐项）**——①收购档位（demandMultiplier 语义改档位，marketCatalog 127 行脚本化迁移）：原料池 1.0L 不变、池耗材（弹药/修理组件/无人机 9 条显式）0.6L、common 单件 0.6L、rare 0.65L、奇货 1.0L 全价回收；单件缺省按 rarity 兜底（BUY_TIER，导出 acquisitionFactorOf 供 UI 同源）；防套利不变式 = 收购 ≤1.0L < 供应、倒卖亏税；②簿面件数放大：common 单件收购单 ×1→×3/张、rare ×1→×2/张（奇货/池不变）——平价流速 ≈2.6/窗 仍低于折价上限，让利提速不失效；③两侧抢单：卖单挂价 > 收购价线 → 每 60s 窗 30%·e^(−6r)；买单挂价 < 供应价线 → 20%·e^(−14s)（r/s = 相对价线偏移），命中成交 1 件 @ 挂单价（日志"巡游采购/巡游供货"；钱包不足跳过；balance 4 参数 snatchSellChance/Decay·Buy*）；窗口顺序 簿撮合→抢单→让利吸收，越线与吸收/簿互斥；④测试：档位真值表（含 fallback）、开盘簿 qty3 与 rare spawn qty2@0.65L、卖侧 +10% 溢价 200 窗统计 ≈33 件、买侧 −5% 统计 ≈20 件、既有断言更新（helpers/events/v10 档位同步、蓝图书回卖 600、卖船 72k、AI 核心簿 qty3、高挂改赌巡游）；core 482 绿 + typecheck ×4 + content:check + build 绿；定稿存档 = docs/design/market-acquisition-rework.md（glossary/roadmap/market-sell-absorb 挂账段同步）。状态：**待船长验收（2026-09-08）**；挂账：公告稿（数值向更新，验收后另出待审稿）→ **✅ 2026-09-08 公告 `2026-09-08-market-manufacture` 船长批准写入（v2：收购价/让利不写具体数值）；随批推送 origin**。
- 2026-09-08：**市场批审核修复（二号自审）**——①UI 与引擎通道基准统一：导出 `buyLineOf/askLineOf`（buyPrice/sellPrice 价线同源；吸收/抢单/簿 jitter 三基准合一），MarketPage 默认价与四态提示全部改用价线（原来用簿最高价 quote.buy 兜底，含 ±2% jitter 会在临界价让"平价档"实际落入抢单/吸收错位）；②数据/工具过时注释清理（marketCatalog 回卖 0.5×L/半价/亏 6% → 档位语义；content-schema 列说明）；③档位用例补 buyLineOf/askLineOf 同源断言；经济平衡复核（探针）：自制平价卖站净收益 civ/MK1 ≈ +10~21%、MK2 +48~82%（70~100 分钟/件 ≈ 0.7~1.3k/min）、MK3 +52~75%（5~7 小时/件）、弹药 动能 +19%/能量 +10%/爆破 −5%（10 秒/批，旧 1.0L 时动能 ≈1.8k/min → 新 ≈0.4k/min，压 4 倍达成"造弹卖站无利"方向）；远低于采矿/悬赏费率，无印钱；core 482 绿 + typecheck ×4 + desktop build 绿 + playthrough-sim 冒烟 0 异常。随批并入本批次，状态同为**待船长验收（2026-09-08）**；✅ 随 `2026-09-08-market-manufacture` 公告同批推送 origin。
- 2026-09-08：**离线报告：AI 核心作业清单 + 预估收入（大鲸鱼一号；船长定）**——离线结算内存统计（settleStats.ts，不落盘零迁移）：按核心类型统计 AI 采矿返港卸货趟数/打捞完成次数/AI 精炼与回收炉批数/AI 制造完成件数；预估收入按站内收价与市场基准价粗估（残骸与碎片不计）；advanceGame opts.settleStats 透传三处完成点（正常推进零开销）；离线简报「AI 核心作业」小节 + 日志单条携带；测试 +3；core 485 + typecheck ×4 + desktop build 绿【待验收/待发布】。
- 2026-09-08：**连续作战保险：带伤预警 + 战内自动撤退（二号；船长拍板方向 + 四问确认）**——玩家反馈连续战斗（巡回讨伐）偶发带伤下场失利→弃船损失（既有保护只覆盖开战前，战内无止损点）：①**带伤预警**（只改展示）：悬赏胜率 = 原显示（满耐久基准）− 预计装甲损耗×10pp − 预计结构损耗×25pp（新参 winPenaltyArmorPerFull/winPenaltyHullPerFull；steadyPreview 抽出与 battleWinPreview 同源；导出 bountyDamageForecast/bountyWinPercentGuarded；结算与 AI/模拟口径 battleWinPreview 一律不变）；②**战内自动撤退**（仅巡回场次）：battle.hullEscapeFrac=0.5 由 beginBattleAt 按 autoLoop 挂载，advanceBattleFor 每步检查结构剩余 <50% → autoEscaped 中止 → advanceExpedition 走撤退结算（retreatBattle 重构为共用 settleBattleRetreat(mode)：承伤写回/半损骰（最低1%）/下限 5% 绝不弃船/维修费/**终止连续出击**/转返航）；玩家实时战斗与低安遭遇不挂载（不抢操作）；③**巡回自动再出发门槛提前到装甲**：装甲或结构 <50% → 修理组件自动修补目标 0.5→**0.6**，不足停环（防开局秒退缓冲）；④**动力随损伤 = 维持现状**（agility 不参与承伤/脱离判定，无 >50% 结构损失路径——船长条件判定）；AI 副船远征停用/隐藏 = 挂起另任务（本批不动 AI）；UI 悬赏卡胜率 + 悬停（预计装甲/结构损耗说明）；expedition.test +4（预警自洽/装甲门槛停环/修补 60% 再出发且 battle 挂 0.5/巡回场损失过半自动撤退保船停环返航）；core 486 绿 + typecheck ×4 + desktop build 绿；定稿存档 = docs/design/bounty-insurance.md（glossary/roadmap 同步）。状态：**待船长验收（2026-09-08）**。
- 2026-09-08：**顶栏「QQ 讨论群 621172698」入口（二号；船长 2026-09-08 指示）**——顶栏右侧新增「💬 QQ群 621172698」按钮（复用 app-btn 同级样式）：点击复制群号并在按钮上短暂显示「✓ 群号已复制」；title 提示"到 QQ 搜索群号即可加入"。
- 2026-09-08：**AI 副船远征：UI 彻底隐藏收尾（二号；船长 2026-09-08 定：推进挂起任务）**——引擎侧维持 2026-09-05 软下线（assignAiExpedition 一律拒绝、历史任务取消归还核心，代码/注释保留待恢复）；本次隐藏全部玩家可见入口与过时文案：①ShipPage AI 指挥中心移除「远征任务」指派选项与整套目标/成功率表单（mode 收窄 采矿/打捞；执行中列表远征行改为防御分支注释）；导航 title 去"暂停受理"提示；②手册「AI 副船」词条、赏金猎手学技能描述（去"含 AI 副船远征"）、隼枭舰船描述（"护送 AI 副船行动"）清理；③备注补全：ai.ts 头注 / renderer assignAiExpeditionAt 注释（含恢复步骤）/ ai-design.md 状态注 / glossary favor 口径行更新；恢复 = 删引擎前置返回 + 加回 ShipPage 表单即可。desktop typecheck + build 绿。
- 2026-09-08：**技能 rank 分散化：相似效果错开级别（二号；船长观察"多个相同 rank 技能效果相同" + 三轮澄清确认）**——①范围：同效成对（拆半双修线）与跨域同型技能；保留双技能结构只改 rank（数值/接线/文案零改动；rank 仅驱动训练时长 = 档底 × rank × 2^(级−1)）；②表 1（同值成对）**上调**错开 → r3：贸易谈判学（对会计学 r2）/残骸富集识别学（对富矿勘探 r2）/打捞装置整备学（对采矿护卫舰 r2）/装甲调谐学（对护盾调谐 r2）/信号过滤学（对信号分析 r2）/火控阵列学（对索敌统合 r2）；③表 2（同乘区异值补强对；船长：放弃上调、改为下调）**下调**错开：批量生产学/组件标准化/货舰操作 r3→r2、货舱管理学/空间站协议学 r2→r1、重装舰操作 r3→r2（主轴 工业理论/材料学/深空物流/维修工程学/船体加固理论 保持原档）；④无市场/战斗域整体偏好（草案撤销）；无 r4 新增；⑤明确不动：主轴技能、炮族与三族专精（族限互斥可选）、产量互补系；例外登记：循环 −3% 三兄弟中副船整备学与打捞整备学同 r3（AI 域组合罕见，全异秩仅余 r4=67h 不建议）；⑥验证：core 489 绿 + typecheck ×4；D1 体检重跑与改前一致（改动技能不在 A/B/C 训练链内，纯生产策略基线不受影响，拉平目标仍挂起）；定稿存档 = docs/design/skill-rank-dispersion.md（roadmap 同步）。状态：**待船长验收（2026-09-08）**。
- 2026-09-08：**新手教程新增「出售物品」步骤（二号；船长反馈：交付后的矿石不自动卖、玩家不知如何出售）**——插入独立教程步骤 3「出售：把矿石换成 ISK」（编号顺延：1 采集 → 2 交付 → **3 出售** → 4 修复 → 5 试炼 → 6 技能归档 → 7 分身 → 8 收尾 → 99）：交付只扣 20 单位、剩余矿石留在仓库不自动售出——教程先教学去「物品页·仓库」点富凡晶石「市价卖出」（可只卖一部分留作材料）；①core onboarding：新 ONB_SELL=3（常量全量顺延），交付完成记录 oreSellBaseline（交付后余量，可选字段零迁移，save 白名单透传），自动推进 SELL 段 = 仓库富凡晶石 < 基线（卖出 ≥1）→ ONB_REPAIR；②UI：App TUT_LOCK/锁界/技能归档聚焦全改常量键并加步骤 3（只开 items 页——矿只减不增判定稳定），TutorialGuide 新增步骤 3 卡片与光圈目标（物品/仓库/市价卖出），ImportantTasks 显示窗口常量化；交付时仍切回隼枭（维修对象语义保留）；③测试：t24 更新（交付 → SELL 未卖不推进 → 卖 1 推进修复 → 基线清空；技能归档断言语义化）core 489 绿 + typecheck ×4 + desktop build 绿。状态：**待船长验收（2026-09-08）**。
- 2026-09-08：**技能限制句清理：上限/下限护栏拆除（大鲸鱼一号；船长 2026-09-08 定：③ 文案全改、除材料学外机制同拆、抗性 90% 上限保留）**——①文案：skills.ts 21 处去「至多缩短/至少保留/封顶/合计最多/总下限」工程句（规避机动学与护盾/装甲调谐学改装备同款「乘入制、上限 90%、多来源收益递减」句式；精炼/高级回收改「双修满级合计 160%」；采矿护卫舰/工业理论/武器装填/无人机整备/高效学习法/残骸回收学/打捞装置整备学/信号过滤学/维修工程学×空间站协议学/会计学×贸易谈判学/弹药集约学等按行删句或改「与 XX 乘算叠加」）；②引擎护栏拆除：mining 循环 minTimeRatio、manufacturing 制造 minTimeRatio、combat 装填/无人机装填 0.6、training 0.6、explore 总下限 0.4、shipyard 维修合计下限 0.4（双满级实付 0.375）、industry 工业自动化/回收批 0.6 与 refineRate maxRate、market 税防负 min(1,·)（balance/types 相应字段同步删除，docs 三处同步）；③保留：材料学全部机制（0.7 乘区下限 + 单种至少 1 单位）+ 护盾/装甲调谐 90% 上限 clamp（与装备同体系）；④安全论证：每技能封顶 5 级、每级削减 ≤10%，多源乘算最坏乘积 0.375>0，无归零/负值；⑤测试：skills-effects 维修双技断言随新口径更新（×0.375），core 496 绿 + typecheck ×4 绿【待验收/待发布：推送闸门内】；工业自动化（3-4 行）引擎下限已拆、文案待 ⑤ 定稿后随批落地。
- 2026-09-08：**卷A2 ④⑤⑦ 落地 + ⑧⑬ 挂起（大鲸鱼一号；船长拍板：④ 方向 A / ⑤ 方案① / ⑦ +20%/级；⑧⑬ 挂起待重置）**——④ 采矿技术文案改「矿藏采集的核心技术：矿石、气体与冰矿的采集循环产量每级 +6%（与星质地质学、深空采集学乘算叠加）」（机制本就全矿生效，仅纠文案）；⑤ 工业自动化扩手动：精炼炉（炉心熔炼学之后再乘一区）与组装机（工业理论×批量生产学之后再乘一区）手动与 AI 核心驱动同享 −5%/级，AI 仍先 ÷核心效率（industry.ts startRefineRun / manufacturing.ts startManufacturing 分支重构；skills 工业自动化文案定稿「自动化作业调度：精炼炉与组装机…手动与 AI 核心驱动同享」）；⑦ 离线作业管理学 +8%→**+20%/级**（simulation.ts capEff = capMs×(1+0.2×级)，满级 16 小时；skills 文案同步）；⑧ 建筑工程学效果应用面太窄 → **挂起等后续重置**（含 −40%→−29% 文案纠错一并缓办）；⑬ 星际奇遇学整行（含漂流物打捞学去「远征」词文案）**挂起**，随卷 B 重置批次（事件/奇遇议题）处理。测试：skills-effects 自动化用例改手动同享（4500ms）、离线场景 20h 离档 → 满级 16h；manufacturing +1（手动线 450 秒）；core 497 绿 + typecheck ×4 + content:check 绿【待验收/待发布：推送闸门内】。
- 2026-09-08：**卷B1 ⑧⑬⑫ 技能重置收口（大鲸鱼一号；船长 2026-09-08 逐条定稿）**——⑧ 建筑工程学从“交付计件放大”改为**建站建材需求直减每级 −8%**（满级 −40%、每档 ≥1；station.ts `tierNeedOf` 统一需求口径，旧档“计件放大”虚高已缴自动结算兼容，建站卡 UI 同源）；⑬ 星际奇遇学＝**在线随机事件间隔每级 −8%**（满级 ×0.6，事件密度约 +67%；rollGapMs 播种/续掷统一缩放），低安遇袭单次率 ×同因子 → **每小时遇袭期望不变**（船长定），出击出发遭遇 ×1.15/级 保留、文案改“在线事件间隔 −8% + 出发事件 ×1.15”口径；⑫ 事件分红学机制不动、文案加“娱乐向小彩头”声明；组装机产物属性悬浮＝船长误派（二号工作）已撤销回滚（856d349 不保留）；测试 +4（t9 满级 60/90 实耗建成与旧档虚高自动结算、events 满级因子 0.6 与同种子间隔 ×0.6）；core 501 绿 + typecheck ×4 + content:check 绿【待验收/待发布：推送闸门内】。
- 2026-09-08：**卷B2 ⑥ 富矿脉重做 + AI 副船返航速度修复（大鲸鱼一号；船长已逐条定稿口径，直接实现）**——①富矿勘探学改「概率触发 + 红利窗口」：每分钟基础触发率 **3%**（满级 6%，系数沿用 richVeinFactor=1+0.2×级），掷点按循环占用分钟数缩放 p=min(1,分钟×0.03×系数)（balance.richVeinChance 语义改 0.03/分钟、字段名与 0=禁用保留）；命中 = **连续 2 循环 ×3（含触发当轮）**（触发当轮置 rvLeft=1，次轮递减至 0）；窗口内不掷点、结束不写日志（只在触发写一条）；窗口与矿带绑定（换带/停止/作业结束清零，自动循环返航卸货回原带保留）；主控与 AI 每船独立（state.mining.rvLeft / task.rvLeft，旧档缺失按 0 零迁移不升版本）；长时间期望 ≈ +7.2%×系数/小时（旧 ≈ +1%）；②**AI 副船采矿/打捞返航腿不再 ÷核心效率**（与玩家一致：效率只加速出航/空船腿与循环周期，返航仍按货仓占比 scaledReturnMs；aiTaskView 视图同源）；③文案/工具同步：skills.ts 富矿勘探学描述、README 彩蛋行、tools/bounty-econ.ts 富矿期望（×1.01 → ≈×1.07）；④测试：mining +6 / ai +2 与返航时序 3 处改新口径、save 往返期望补 rvLeft；core **509 绿** + typecheck ×4 + content:check 绿【待验收/待发布：推送闸门内】。
- 2026-09-08：**卷B3 ⑨⑩⑪ 技能收口（大鲸鱼一号；船长逐条拍板：⑨-A / ⑩-A 效率 +2%每级 / ⑪ 淘货折扣 E1）**——⑨ 残骸富集识别学改「完好舰体当场直发」：命中率按打捞分钟语义（balance.intactHullRatePerMin 0.08%/分钟 ×1.2/级；与富矿脉⑥同哲学），命中不再翻体积 → 当场直发该敌群回收彩头（rollIntactHullLoot：基础件必中 1 件·主题优先 → 装备库；低安按 intactMk2Chance=0.04 掷 MK2 档；威胁 ≥17/≥41 掷蓝图碎片），主控/AI 同源、产物不占货仓、判定恒掷保时序（0=禁用）；⑩ 新增首个 **rank5「AI 核心调度学」**（ai-core-dispatch）：核心驱动的全部作业效率在核心档位之上**每级 +2 个百分点累加**（基础 40% → 满级 50%、伽马 60%/贝塔 70%/阿尔法 85%，封顶 100%；balance aiCore dispatchPerLevel；aiEfficiency 单点接线 → 副船与站内炉/线共源，返航腿不 ÷eff 天然不受影响）——叠加于二号「AI 核心统一启用上限」（a0fbb3e）之上，正交无冲突；⑪ 二手市场学改「淘货折扣」：**稀有供给单价格每级 −2%**（满级 −10%，含闸内 ×4 暗市同乘；**奇货不参与**——收购 1.0L 全价回收档，折价会击穿防套利不变式，文案写「稀有商品」与市场标签一致）；顺手修正现货抢购学描述残留旧口径（引擎 0.1/级 → 文案 ×1.1/满级 ×1.5）；测试 +7（⑨×4：必中入装备库/低安 MK2 与高安对照/禁用/ AI 同享 + 概率语义改写；⑩×2：效率累加 0.4→0.5 精确/满级循环 12000ms；⑪×1：双跑对照 0.9 与奇货 toEqual）；core **522 绿** + typecheck ×4 + content:check 绿【待验收/待发布：推送闸门内，公告稿待船长审】。
- 2026-09-08：**组装机同蓝图多线读档丢队修复 + AI 启用计数口径同源（大鲸鱼一号；玩家反馈两连）**——①根因：save.ts 读档归一化按 blueprintId 去重制造线（v21 同蓝图可多条线后已错误）+ 未保留 worker 字段 → 刷新后同蓝图第二条线丢失、AI 线变"旧作业"豁免（出库核心既不计数也不归还 = 永久占用）；修复 = 去 blueprintId 去重（多线保留、id 重新唯一分配）、worker 随档保留（缺省 = 劳动者制前旧作业，零迁移）、结构性坏条丢弃时归还其出库核心、防御重复 pilot 线；测试 +2 回归（同蓝图 AI 双线往返保留 + 坏条退核心；旧档无 worker 豁免保留）；②玩家反馈"顶部 ⚙ 徽标 22 与实际对不上"：徽标 = AI 副船 + AI 生产（炉/线，tooltip 已分行），AI 指挥中心「执行中(N)」的 N 含生产但列表只列副船 → 标题按行同源拆分「副船 N · AI 生产 +M（工业页取消）」、空态文案同步；根因未拿到玩家档无法完全实证，留待下版实测复核；core 524 绿 + typecheck ×4 绿【待验收/待发布：推送暂缓（船长 2026-09-08 指示）】。
- 2026-09-08：**残骸可到市场出售 + 独立「残骸」分类（大鲸鱼一号；船长定：档位一 50~60% + 推荐默认）**——①22 张悬赏残骸收购卡（marketCatalog WRECK_BUY_GOODS，按星系安全等级推基础密度定回收档）：收价 = 常 30 / 险 **40** / 危 **50 ISK/m³**（**2026-09-08 当日修正**：三档无技能拆解保底均 ≈57/m³，原报 45/60 实为 79%/105% 会击穿最低锚；修正后严格 <57 → 任何档直接卖都不如拆解，对"该档典型特色回收"约 50~56%，拆解 + 彩头 + 碎片 + 技能 ×1.75 仍更赚，卖站 = 折价清仓/应急通道）；池模型（poolTarget 30k / supplyFlow 500，窗口吞吐保障清几百 m³）；②**只收不卖**：引擎给 MarketGoodDef 的 `playerBuyable=false` 补全语义（此前仅买入侧巡游抢单消费）——NPC 常驻刷单不出售、稀有/奇货抽取不刷、市价买入返回 0、挂买单拒绝，防"低价买残骸→拆解套利"；③市场左栏类型过滤新增「**残骸**」单独分类（物品类含残骸者拆出，行徽标/悬停同显"残骸"，供应位显示"只收不卖"、买入页签禁用+回收点提示）；④文案：残骸物品介绍改"可市场按废料价出售应急或拆解（更值）"，工业页回收卡/离线统计注释/测试注释同步；content-check：item 卡解析改用动态物品表（残骸/碎片不入静态 31 件表）+ **残骸卡价格锚护栏**（档位表一致、收价 < 无技能拆解保底 57、只收不卖）→ 市场卡 149 → **171 张**全绿；测试 +1（只收不卖：120 窗供应簿恒空、收购簿常驻、buyAtMarket=0、挂买单拒绝）；core **526 绿** + typecheck ×4 + content:check + build 绿【待验收/待发布】。
- 2026-09-08：**巡游采购单次件数随贴线度 + 池量放大（大鲸鱼一号；玩家反馈"比买盘高 1 块却每窗只卖 1 件"；船长定方案 B + 数量与该商品池订单生成量挂钩）**——机制澄清：越线（挂价 > 收购价线）本就只剩巡游车道，概率衰减 e^(−6r) 早已"贴线惩罚小"，但单次成交固定 1 件对池大宗杯水车薪；补上数量维度并**按池量挂钩**：新增导出 `snatchSellFill(def, r)`——池商品单次 = round(supplyFlow × 档位比例)（贴线 ≤1% → 0.5、≤3% → 0.25、≤8% → 0.1、更远 1 件；supplyFlow 缺省 = poolTarget/120；例：氖云气 45/窗 → 贴线 ~23 件，矿石大池按比例自动放大）；**单件商品** 5/3/2/1；买侧供货维持 1 件/窗；任何档位仍远慢于平价簿 + 让利吸收（期望 ≈ 命中率 ~28% × 单次量 vs 平价簿 ≈2.55×supplyFlow/窗）→ 价线纪律与"让利换清仓"保留；市场详情卖出指引动态显示"命中一次约收 N 件（越贴近价线越多）"；测试 +2（按池量真值表单元 + 池商品 +1% 挂卖 200 窗销量 >700 显著超旧口径 ≈56）；core **528 绿** + typecheck ×4 + build 绿【待验收/待发布】。
- 2026-09-08：**无人机舱大改（大鲸鱼二号；船长 2026-09-08 Q1-Q7 与平衡参数逐项拍板；doc = docs/design/drone-bay-rework.md）**——废弃"战斗自动贪心装载"，改为玩家主动管理的「无人机舱」清单制：①引擎/存档：FleetShipState 新增 `droneLoad?: Record<string, number>`（数量制，save normalize 清洗：正整数/删 0/缺省无，旧档零迁移无版本号）；combat 单位构建**只放飞该船 droneLoad 清单**（不再读仓库/货仓贪心；伤害 = dmg×(1+Σ导控)×(1+无人机作战学 5%/级)、装填 2200ms×整备技能不变；CPU/舱防御截断保留，超量逆序剔除并 warn——正常由 UI 预占互斥避免）；equipment 新增导出 `droneCpuUsed/droneLoadM3/adjustDroneLoad`（清单 CPU/体积汇总 + 仓库⇄清单命令：逐型增减、舱容与 CPU 双校验、正数钳制），装配 fit/unfit 与 CPU 预算条把该船清单占用并入（装机/装无人机共用预算互斥）；renderer engine 接 `adjustDroneLoadAt`；②UI（FitPage）：低槽组下方新增「无人机舱」区——容量条（m³ 已用/上限（船体+甲板扩展）·清单 CPU，复刻 CPU 条视觉）+ 一型一卡 ×N（+/- 逐架微调、悬浮说明）+「全部卸下」清空 +「＋装入」占位卡 → 选型数量弹层（仓库余量 × 舱余量 × CPU 余量钳制）；无舱船不显示；装配信息行「无人机舱（含甲板扩展）上限 m³ · 战斗只放飞清单」；③全船机舱平衡（ships.ts 11 艘）：隼枭 30→**0**、伯劳/虎鲨 45/55→**10**、灰鲭鲨 85→**30**、大白鲨 130→**20**、陆龟 20→**40**、玳瑁 40→**50**、玄武 60 不变、飞鱼/旗鱼/剑鱼 15/20/30→**40/50/60**（工业/矿舰线维持 0）；④新船 ×2（无人机平台延伸，掠食者同系盾抗动能 50%）：**蜂群级**（sh-swarm armed t3 高5中3低2：180/90/108·cpu235·舱160·2600m³·620k 市场 rare 行）与 **哨兵级**（sh-sentinel armed t4·奇货 声望 6：高6中2低2：210/105/126·cpu320·舱320·3600m³·2.6M exotic 行）；content-check 船数护栏 19→**21**；⑤工具/文案同步：battle-calibrate 无人机行 D1-D3 改用新船（蜂群轻/中装、哨兵重装）+ droneLoad 清单注入（各行 = 装配后余 CPU×舱容合法满载组合）；make-test-save drone 档改 蜂群级·中装（驾驶，清单注入）/ 哨兵级·重装 / 灰鲭鲨·S2 炮流对照，无人机库存只放仓库；手册「装配」条目、物品页分类说明（kindExtraNote：无人机需从装配页无人机舱装入，仓库余量不自动出战）、FitPage 信息行等旧"自动放飞/无需装船"文案全量清理；⑥回港补足钩子 = 结构预留（停靠点 stub noop + 注释激活条件：未来无人机击毁/消耗机制需配套价格重锚与自制蓝图再激活）；⑦验证：core 测试改清单驱动（v18 pilot helper / combat 无人机作战学·整备学 / v181 往返共 7 处）→ **528 绿** + typecheck ×4 + content:check + desktop build 绿【待验收/待发布：推送闸门内；无人机数值方向（此前校准显示满技能超炮流）待船长用新船实测后定】。
- 2026-09-08：**市场成交静默 + 建站交付航线 + 市场蓝图标识 + 未建成副站彻底收口（大鲸鱼一号；船长 2026-09-08 定：A~C 按稿确认，D 改"移除所有未建成空间站功能、未完成前不视作任何站点"）**——①**成交静默化**：站内吸收（absorbViaStation）与两侧越线抢单（snatch）的成交日志全部与普通簿面成交同模板（不再出现"让利售出/站内收购/巡游采购/巡游供货"字样），MarketPage 价格指引删除吸收额/巡游概率等通道披露文案、只留普通撮合语义（挂价与收购/供应价线关系）——玩家视角看不出这些通道存在（保留让利/吸收机制与价格纪律本身）；market.test 断言全部改"挂单成交/挂单买入成交"前缀 + 新增静默专项；②**建站交付航线（真实航程）**：新指令「前往工地交付」（星图该星系行动区 + 建站任务卡工地卡，双入口同引擎指令）——前置：主船空闲且停靠在空间站（母港或已建成副站 = 协会基地网络）、工地未建成且所在星系已探明、仓库+货仓有可交付建材；出发 = 按真实航程（travelLegMs 换算、吃航行技能与 debugQuick 1s）驶往工地星系（transit.delivery.phase=to-site），到点野外停留 → **自动交付**（接受名单循环扣缴、逐档推进、日志同现场交付）→ **自动返航最近空间站**（to-station；若本次恰好建成 → 就地停靠新落成副站）；**随时可取消**（无惩罚，取消即立即返航停靠最近已建成站；顶栏活动显示"建站交付·前往工地/返航中"并带「取消交付」）；ShipTransitState 增可选 delivery{siteId,phase}（save 白名单透传、旧档回退 null）；出航期间 awayGalaxy=出发星系 → isAtHomeLike/站内门天然关闭；③**市场蓝图标识**：蓝图行与详情卡 chip 标注 已学习（learnedRecipes）/已获得 ×N（blueprintStock），均未持有显示"尚未获得"（新增 is-learned/is-stock 同族 chip 色）；④**未建成副站彻底收口（D 改版）**：stations.ts 档位解锁描述改"施工推进（功能统一在建成后开放）/建成一次性并入基地网络开放泊位与全部服务"；停靠只认已建成站（onArriveAtGalaxy 未建成一律现场停留、noteStationSiteAt 文案去"首档解锁泊位"）；引擎 advanceGame 逐 tick reconcileDockSanity 将旧档残留的未建成/未知停靠纠正为工地现场野外停留（幂等、只记一条日志）；shipyard 维修/换驾驶对未建成停靠一律拒办（含状态残留防御，建成后放行）；测试：t9 +8（前置校验/端到端自动交付并返航/建材足量一次建成就地停靠/途中取消与取消前置/空跑照常返航/纠正幂等/维修与换驾驶门禁）+ t8 断言补 delivery 字段；core **537 绿** + typecheck ×4 + content:check + build 绿【待验收/待发布：推送闸门内，公告稿待船长审】。
- 2026-09-09：**悬赏胜率预估改蒙特卡洛推演（大鲸鱼二号；玩家反馈"预估胜率和实际战斗不一致"；船长拍板：N=21 + 带伤修正内部吸收 + 不加临界文案；doc = docs/design/win-estimate-mc.md）**——旧玩家可见预估（bountyWinPercentGuarded）为稳态解析 + 外挂"带伤扣分"，探针实测（5 装配 × 7 档悬赏 × 9 种子）双向大偏差：多处显示 2%（guarded 下限）实际 9/9 全胜（S4 对穹顶、S1 对坟场），无人机装对赤潮/深渊门旧稳态 32~84% 而实测 0——失真根因 = 不结算护盾战中被动回充（2%/s）、敌方火力不随减员下降、无视接近期与动态距离、带伤开局按残余缩血但预估按满血；离线/连击胜负本就与在线同引擎真实模拟（非按预估掷骰）。①core 新增 `winEstimate.ts`：评估快照（buildEvalState：新档 + 驾驶船同装配/无人机清单/耐久 + 技能 + 教程进度复制；弹药足量）上按独立种子跑 **N=21 局真实模拟**（startBattleFor+advanceBattleFor：残余开局/距离机动/近盲/盾回充/减员/超时血比全内建）取平均 = 胜率与平均损耗——**带伤影响内建，旧 winPenalty 扣分公式退役不叠加**；同输入可复现、不污染真实存档 rng/弹药；②renderer engine 胜率缓存：战力指纹（驾驶船+装配+无人机清单+技能+耐久）变化 → 全板分帧预热（每批 ~60ms、节流 ≥400ms、交火期跳过；换船/换装/技能升级/维修/损伤 ≤1s 自动触发；平时心跳零成本）；`winEstimateOf` 供悬赏卡读取，未就绪短暂回退旧口径后随批变准；③UI：悬赏卡胜率/预计损耗改读缓存，tooltip 去失效"带伤预警口径"字样改"按当前船况实测推演…单局仍有随机波动"；`bountyWinPercentGuarded/bountyDamageForecast` 保留为回退/活动视图/测试兼容并标注退役；`battleWinPreview`（稳态）维持供 AI 门槛与离线模拟工具——两套口径注释互指；④验证：探针 MC(21局) vs 实测(9 种子) 70 格绝大多数差 0、最大 ±14pp（独立样本抽样误差内）——"2% 假必败"与高估格全修正；core 新增 winEstimate 用例 +6（必胜=1/必败=0/可复现/runs/不污染存档/快照战力复制）→ **543 绿** + typecheck ×4 + desktop build 绿【待验收/待发布】。
- 2026-09-09：**舰船归类修正：鲸盟大型货舰分出 + 徽标「工业」→「采矿」+ 技能名同步（大鲸鱼二号；船长反馈"弓头鲸和巨灵鲸是货船却和矿船同类，鲸名易误导"后三轮拍板：归航运族 / 改名蝠鲼级·皇带鱼级 / 保留鲸盟制造设定 / 顺带徽标改采矿）**——①`ships.ts`：`sh-bowhead`（弓头鲸级货舰 → **蝠鲼级重载货舰**）与 `sh-colossal`（巨灵鲸级旗舰货舰 → **皇带鱼级旗舰货舰**）role `industrial → hauler`（挖矿不再吃「工业舰操作」、改吃「货舰操作」+5%/级货舱加成；鲸盟护盾抗动能 25% 族性保留，desc 去"工业级大型货舰"字样保留"鲸盟总装"），鲸盟线剩 6 艘纯矿舰（沙猫/掘洞/鲸吞/开拓/鲸王/座头鲸）；②`labels.ts` SHIP_ROLE_LABELS `industrial: '工业'→'采矿'`（role id 不变、存档零迁移，徽标/图鉴/详情/手册全部自动跟随）；③技能显示名对齐：`skills.ts` 工业舰操作 → **采矿舰操作**、描述"工业舰族/工业族"→"采矿舰族/采矿族"（id industrial-ops 不变 = 训练存档键不可动），`mining.ts` 注释同步；④`shipBlueprints.ts`：蓝图名随船改「皇带鱼级舰船蓝图」+ 顺手校正过时描述数值（座头鲸蓝图旧稿"货舱 9000/10s/40u" → 实际 19000/30s/140；皇带鱼蓝图旧稿"26000 m³" → 实际 36000，均玩家可见文案）；⑤文档：ships.ts 头注与鲸盟段注、README 四族线提法、v18-slots 草案表两行归属同步；⑥验证：content:check（role 分布 industrial=6/armed=7/armored=3/hauler=5、tier 血均值护栏不破）+ typecheck ×4 + core **543 绿**【待验收/待发布】。
- 2026-09-08：**建站交付航线 v2 修订（大鲸鱼一号；船长追问"到点扣缴遇仓库变动会白跑"后定稿：物理载货 + 自动循环）**——①**出发装载**：一键启动即把货仓装满建材（仓库按名单顺序装载，上限 = min(空闲 m³, 全站剩余需求)，保留船上原有货物；仓库无料 / 货仓满载 → 拒绝出发并提示原因）；②**到点只清空本趟装载**：deliverStationResources 增 `opts.cargoOnly`（只扣驾驶船货仓、不补扣仓库——"航行中仓库被 AI 精炼/资源订单/自用消耗"导致的白跑结构性消除；旧版在途档无 loaded 时按货仓现存接受建材兜底，不碰仓库）；ShipTransitState.delivery 增可选 loaded 明细（save 白名单透传）；③**自动多趟循环**：返港后工地仍有缺口且仓库能装料 → 同一引擎推进内立即再出发（逐趟真实航程），直到①全部档位建成（就地停靠新落成副站）或②仓库建材耗尽（终止：事件日志 warn + 一次性 `state.deliveryNotice`，渲染层引擎心跳/离线结算后读取弹窗并清除，零迁移不落档）；④**取消 = 停止整个循环**（无惩罚、立即返航停靠最近已建成站、本趟装载随进港卸回仓库，之后不再自动续趟）；UI 同步 v2 文案（星图行动区与建站任务卡：仓库建材量 + 货仓空闲 m³ + 循环语义；活动栏取消文案；手册「副空间站」条）；测试：t9 交付航线组改写（出发即装货/途中货物随船不受仓库变动影响/只清本趟装载/小货仓 60 自动 5 趟建成/仓库耗尽终止提示/取消停止循环不续趟/满载与空仓拒绝）+ t8 兼容断言；core **537 绿** + typecheck ×4 + content:check + build 绿【待验收/待发布：推送闸门内，公告稿待船长审】。
- 2026-09-08：**奇货买侧"巡游供货"专项（大鲸鱼一号；船长观察"玩家 43W 挂单买到鲸王级奇货蓝图"后定·试跑口径待实测复核）**——买侧越线抢单对奇货此前与普通商品同规则（基准 = 供应价线 ≈L，挂价低于 L 即按 20%·e^(−14s)/窗凭空供货）：玩家可 0.7~0.9L 低挂持续截胡奇货（43W 挂单 ≈1 张/4.4h，约为自然抽取节拍的 3~5 倍；贴线 5~10% 挂单可达自然出率的数十倍），并与奇货"收购 1.0L 全价回收"构成折价买→全价回售套利面（击穿当初二手市场学折扣排除奇货的防套利设计）；修复 = 奇货挂单买入的**越线基准单独改为 20L**（matchPlayerOrders 买侧分支：rarity=exotic 时 ask = L×20，稀有/普通仍按供应价线；卖出侧不变）：正常挂单相对 20L 偏离 s≈95%+ → 每窗命中 ≈0.2·e^(−13)~3e−7 量级（约数年一见），奇货供给实际回归 10 分钟抽取节拍（每件每窗 0.8%×现货抢购学），同时保留天文级小概率而非硬禁；静默日志口径不变；market.test +1（奇货 0.9L 低挂 1000 窗零成交 vs common 对照组照常供货）；core **544 绿**（52 文件，含二号 09-09 蒙特卡洛批次）+ typecheck ×4 + content:check + build 绿【待验收/待发布：推送闸门内，公告稿待船长审】。
- 2026-09-08：**奇货低挂买单的玩家提示（大鲸鱼一号；船长定：20L 修正不进公告；挂奇货买单低于 L 时给提示与合适价位，不披露 20L/巡游机制）**——MarketPage 详情「买入」侧价格指引：奇货挂价 ≥ 参考价（供应价线 ≈L，文案"奇货参考价"）→ 提示"协会奇货到货（行情价上下浮动）时直接按单成交"；挂价 < 参考价 → 提示"奇货只按稀见到货，挂太低很可能长期无人接单——建议挂到参考价附近（X ISK 上下），到货即按单成交"（无 20L/巡游字样，纯行情向设定文案）；「挂买单」成功 toast 同步附加同口径提醒（仅奇货且低于参考价时）；稀有/普通商品指引不变；typecheck ×4 绿【待验收/待发布：推送闸门内】。
- 2026-09-08：**工业产能扩容：AI 核心阶梯化 + 工业专用工位（大鲸鱼一号；船长观察"站内生产因 AI 核心上限而整体削减"后定稿：现有技能不动、ai-expert 降 rank2 更名、原「工业自动化」改名让位、新 rank4「工业自动化」每级 +2 工业专用工位）**——①技能表：ai-expert「人工智能专家」→「AI 核心操作学」（rank4→**2**，60 秒档入门，机制不变每级 +1 共用上限；id 不变零迁移）；industrial-automation「工业自动化」→「产线节拍学」（rank3 不变，名字贴合 −5%/级 周期效果）；新增 rank4「工业自动化」（id industrial-ai-cap，工业组）：AI 核心驱动的站内精炼炉/回收炉/制造线在共用上限之外**每级 +2 枚工业专用工位**（满级 +10；仅产业、不占副船名额、每工位仍占一枚实体核心）；②引擎：ai.ts 新增 industryAiBonus（balance.aiCore.industrySkillIds×industrySlotsPerLevel——未来新增工业 AI 扩容技能即往表里追加）+ aiCoreCapBlock(state,ctx,scope=ship|industry) 分路：副船仍按共用上限，站内三处启动（industry 精炼炉/回收炉、manufacturing 组装机）按 共用上限+扩容；③UI/文案：ShipPage AI 指挥中心右上角 启用 X/Y(+N 工业)、上限说明与空态引导补扩容提示；手册「AI 副船」、教程、核心日志（onboarding 特典）、注释、tools 注释全量更名（旧公告历史保留原名不动）；④测试：ai +1（扩容真值表 + scope 分路：共用满而扩容余 → 副船拒、产业放行）；⑤定稿存档 docs/design/industry-ai-slot-expansion.md（glossary/changelog 同步）。状态：**待船长验收（2026-09-08）**；公告稿待船长审核，推送闸门内。
- 2026-09-08：**工业工位收益体检与展示收尾（大鲸鱼一号承接二号；船长逐轮拍板：只升不砍 / 净率+周转双口径 / 精炼无技净率≈20% 且满技在原数值上下调约20% / 组装机收益率下限≈20%）**——①**精炼炉**：基础产出倍率 100%→**120%**（无技能净率 ≈0 → ≈+20%），技能每级加半（精炼学 8%→4%、高级回收 4%→2%），满级倍率 160%→**150%**（满技净率 ≈+55%，原 60~79% 上下调约两成）；技能描述/工业页产出倍率行/手册「精炼」条/items-industry 头注同步；content-check 护栏改 1.2×[1.15,1.35]（低批量 floor 步长允高，玄晶等）；industry/v10 数值断言随 1.2 倍更新；②**组装机低周转行只升不砍**：净率<20% 的 7 行现货 basePrice 上调至 ≥20%（mod-miner-1 30k→31.2k、mod-cargo-1 28k→29.2k、动能/爆破/能量弹 6/7/8→7/8/9、修理组件 civ/mil 3k/21k→3.3k/23.1k）——高收益行（舰船/中高档装备）不动、无倒挂（自制÷现货<1）；③工具/展示收尾：manufacture-econ 头注同步（产线节拍学、单位工位口径、扩容不计入单线）；组装机卡净 ≈ISK/h 随价自动更新；④定稿存档 docs/design/manufacture-refine-worker-econ.md（glossary/changelog 同步：产出倍率再定 + 工业工位口径行）。状态：**待船长验收（2026-09-08）**；公告稿待船长审核，推送闸门内。
