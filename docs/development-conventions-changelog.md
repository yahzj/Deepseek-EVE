# 开发约定 · 变更记录（development-conventions.md 的历次变更）

> 本文件是 `docs/development-conventions.md`（仓库唯一权威）的**变更记录分册**：每次约定条目或根目录
> `AGENTS.md` 的内容变更，都在本文件**最上方追加一条**（最新在前）。
>
> 读取纪律：日常干活**不必通读本文件**；该读哪些章节/文件见 **`docs/catalog.md`（文档目录）**——
> 按任务类型指认，避免每次通读全部约定。

## 变更记录
- 2026-09-24：**玩家可见文案「开发字眼 · 族」清理批（船长报障 · 三号 · verify-activity-win）**——**①船长原话（照抄）**：「**文案存在不少类似'武器族''XX族'这样的开发字眼，会给玩家造成误导。**」**②根因（诊断）**：**一个「族」字被当成三个互不相干的意思用** —— **A 武器**（`weaponFamilyBonus` 的键 = **伤害类型**：动能/爆破/能量）· **B 舰船类别**（`ShipRole`：industrial/armed/armored/hauler）· **C 敌方势力**（海盗/武装拾荒者/异形生物/守墓古舰/泰坦巨构/鱿烬亡军）。**A、B 两类是内部键名直接印给玩家**，而"族"在玩家侧已经被 C 类占用 ⇒ 误导。**修法 = 去掉 A/B 的"族"字，保留 C**（C 是游戏设定词，不是开发字眼）。**③本批落地（zh + en 双语同步，改动处均留 `⟪文案调整 2026-09-24⟫`）**：a) **武器类型**——装配页属性行「武器族加成」⇒「**武器类型加成**」（`ui.shipInfo.008`）、内联句「本族武器单发 +N%（装别族武器无加成）」⇒ 新词条 `ui.shipInfo.184`「同类型武器单发 +N%（装其它伤害类型的武器无加成）」（`shipInfo.tsx` 改走 `tr()`，不再内联中文）、弹药档三条「同族取能装得最多的一档」⇒「**同类型**」（`ui.FitPage.094/154/155`）、六条技能说明里的「武器族专精/武器族技能/族专精技能」⇒「**武器类型加成**」。b) **舰船类别**——「采矿舰**族**专精驾驶 / 航运**族** / **矿族**的产量顶点 / **武装舰族**的量产主力 / **机库族** / **巨构族**」⇒ 按界面既有类别名正字（**采矿舰 / 航运舰 / 武装舰 / 无人机套系 / 泰坦巨构**）。c) **裸字母代号**——虫洞情报句原先印「**A 族** · 海盗」，属数据侧 family 键 ⇒ 改为**只印势力名**（`core.wormholeFoes.016/017` ＋ `wormholeIntel.ts` 两处调用点去掉 `{p3}`）。d) **英文侧**同步把 family 正字为 type / faction（`ui.Expedition.193/251` · `ui.weekend.005` 等）。**④保留（有意）**：C 类「敌族 / 族徽 / 五族 / 这一族」一律不动（玩家已熟悉，公告与洞内文案同口径）——已登记为待船长点名项（工作件 §三）。**⑤边界**：只动**玩家可见文案与显示层取词**，**零机制/零数值/零存档改动**；字段名与代码注释里的"族"一律保留。**⑥验证**：typecheck 四包 0 错 · core **2270/2270（201 文件）** · `content:check` ✅ · `l10n:check` ✅（**id 形态契约当场抓出我第一版写的 `ui.shipInfo.008b` 不合规** ⇒ 改 `ui.shipInfo.184`）· `ui:rot-check` ✅ · 静态复扫：渲染层含「族」的未接线字面量 **0 条**。工作件 = `docs/design/copy-jargon-20260924.md`（含第二批待点名项）。**【待验收 · 本地提交未推送】**
- 2026-09-24：**交接+消档：本批三条尾巴一并结清（船长三条复令 · 三号 · verify-activity-win）**——**①船长原话（照抄）**：「**不推送，不补。不保留日志。消档后检查之前三号还未完成的工作**」。**②三条对应关系（逐条）**：a) 「**不推送**」= 原交接卡 §三 第 1 条（`main` 本地领先 `origin`，未推送）⇒ 代码与公告**都不推**，本批**不补发公告**（该批全是修复与工具：一次战斗黑屏事故 ＋ 三处报障修复 ＋ 8 个探针入库，按 §十二 判定不构成"大更"）；b) 「**不补**」= 原 §三 第 2 条（那位玩家"动能武器打能量弹药"要不要补弹药）⇒ **不补发任何弹药**；一次性舰船两艘已于 2026-09-24 依前令补入导入档，那一笔不在本次消档范围；c) 「**不保留日志**」= 原 §三 第 3 条（三号提议把「最近 N 条日志」随档只读展示）⇒ `save.ts` 的 `logs: []` **照旧**，不加存档字段，该提议**作废不再入账**。**③依据**：三条全部按约定 §2 那条纪律「**销档（消档）条目不重新入账**」（2026-09-12 船长终裁「①消档」）就地标注——不算待办、不进汇报的"仍挂账/待您点"清单、不写成"需另裁"的开放选项；将来若要重启属**船长新裁定**。**④落点**：交接卡 `docs/design/handoff-verify-20260924.md` 新增 **§七 已消档**（表格逐条对账）＋ 原 §三 三条就地加 ⛔ 标；`docs/roadmap.md` 的 L 段三条同步为**复核后的现状**（L2 已清场 · L3 余量更新 · L1 未做）。**⑤边界**：纯文档台账，**零代码/零数值/零玩家可见变化**；未推送状态不变。**⑥验证**：`npm run docs:index -- --check` ✅（306 份文档，最新）· 编码自检（无 BOM · 无 U+FFFD · CRLF 与改前一致）。
- 2026-09-22：**验证闭环新增一道"存档往返体检"闸门 ＋ 立下"随档字段两处落笔"规则（船长报障 · 二号 · d2 · 已推送 origin/main）**——**①起因（船长原话照抄）**：「**玩家刷新可以重复领取补发的打捞器**」。**②根因**：`core/save.ts` 的 `normalizeState` 是**手工白名单重建**（读档时逐字段重建对象），打捞器补发的去重键 `importantTasks.salvagerGift` 只写了引擎侧、**没进白名单** ⇒ 读档即丢 ⇒ 下一拍 `backfillSalvagerIfMissing` 又发一台（刷新一次 +1 台）。**同款前车之鉴**：`wormhole.run` 漏过 `turnsBase`/`turnsTechBonus` —— 这类 bug **不报错、四闸门全绿**，只在"刷新/重进"时表现为**重复发奖 / 状态回退**。**③新增闸门**：`npm run save:roundtrip-audit`（`tools/save-roundtrip-audit.ts`）——把 `docs/test-saves/*.json` 逐份跑"读档 → 落盘 → 再读档"，报**第二轮丢掉的键 / 类型变了的键**（首跑读数：75 份真档 · 丢键 0 · 类型变化 0）。**④配套护栏（两条，落在 `packages/core/tests/save.test.ts`）**：① `Record<keyof ImportantTaskState, true>` 的**类型穷尽**核对——给类型加字段而没在这里补行 ⇒ `typecheck` 直接红；补了行但没进白名单 ⇒ 用例红；② **引擎跑过的档往返不许丢键**的自动护栏（口径：空值 `false/0/''/空表` 不算内容；内存里 `active === false` 的残留作业条按设计丢弃）。**⑤新规则（写进 `docs/development-conventions.md` §二验证闭环）**：**新加"随档字段"必须同时在「写入点」与 `normalizeState` 白名单两处落笔**，并跑 `save:roundtrip-audit`。**⑥验证**：typecheck 四包 0 错 · core 2098 用例全绿 · `content:check` ✅ · `l10n:check` ✅ · `ui:rot-check` ✅ · `skilltree:layout-check` ✅ · `save:migrate` 76/76 ✅ · `save:roundtrip-audit` 丢键 0 ✅ · `build` ✅。**【已推送 origin/main】**
- 2026-09-20：**洞内战斗「可最小化」——作废 2026-09-13 的「虫洞中的战斗画面不可以退出」（船长改判 · 二号 · d2）**——**①船长原话（照抄）**：「**虫洞内的战斗，允许玩家将战斗窗口最小化。**」**②被作废的旧裁定（原文）**：2026-09-13 船长七条之 e)「**虫洞中的战斗画面不可以退出**」（落码 = `BattleScreen` 的「← 退出战场」在 `inWormhole && stage==='live'` 时 `disabled` ＋ 悬停写明原因，配词条 `ui.BattleScreen.036`）。**③新口径（按 §5.2 先摆冲突后获裁，船长选「只放开观战窗口」）**：洞内洞外**同一行为**——点「← 退出战场」= **只关观看界面**（`App.tsx` 的 `onClose` 仅 `setBattleOpen(false)`，**零副作用**：2026-09-10 那个"退出误停连击"的回调 `onBattleViewClosed` 已彻底移除）；**人还在洞里**（面板开着）⇒ `run.attending` 仍为 true ⇒ **战斗照常推进**，随时从右上角/活动栏「⚔ 战斗中」点回来。**④没动的两条（有意）**：① 虫洞面板「**✕ 关闭**」在 `run.battle` 期间**仍然禁用**（那才是"离开虫洞"⇒ `wormholeLeave`：活动停止、主控释放、洞内一切冻结，回来时战斗时钟整体前移不补算）；② 洞内**不给撤退**（船长 2026-09-12 第 8 条：一开必须打完）——**最小化 ≠ 撤退**。**⑤顺带修一条失实文案**：`ui.BattleScreen.039` 原写「洞内：本场必须打完（**战场也不能退出**）」⇒ 去掉括号里那半句（改判后不再成立）；旧键 `ui.BattleScreen.036` **删除**（其唯一用途就是那条禁用态悬停，留则成死键）。**⑥验证**：typecheck 四包 0 错 · core **2002** 用例全绿 · `content:check` ✅ · `ui:rot-check` ✅ · `ui:tip-check` ✅ · `l10n:check` ✅ · 桌面 build ✅。**【待验收 · 本地提交未推送】**
- 2026-09-20：**渠道带放宽：`rare` 由 2~4 拓到 2~5（船长令「一次性蓝图在稀有」· 二号 · d2）**——**①船长原话（照抄）**：「**新增高槽装备，护盾充能力场装置 MK2……都只有一次性蓝图。一次性蓝图在稀有。装备成品在奇货。MK2 和 MK3 的稀有度分别是 4 和 5。平均价格分别是 400W 和 2000W**」；追问「书的档位取多少」时答：**「和装备一致稀有度」**。**②冲突（按 §5.2 先摆出）**：`content:check` 的渠道一致性契约原为 **`rare` ⇒ 2~4**（`exotic` ⇒ 3~5 · `common` 不约束）；而船长要求 MK3 的**书**（一次性蓝图）落 **rare 渠道**、档位**与装备一致**（= **5**）⇒ **档 5 出现在 rare 渠道**，旧带判红。**③处置（按船长裁定）**：把该契约的带放宽为 **`rare` ⇒ 2~5**（`exotic` 3~5 与 `common` 不约束一律不动），并在 `tools/content-check.ts` 就地写明这次的由来与日期。**④影响面**：只影响"档 5 能否走 rare"这一条判据——现有存量里 rare 渠道最高只到档 4，故**除新增的两张书外零实际变化**；`rareTierWeight` 的档位权重表（档 3 ×0.15 · 档 4 ×0.05）**不动** ⇒ 档 5 在 rare 渠道按缺省权重 ×1（与档 2 同权），后续若要给档 5 单独的稀缺系数，另立条目。**⑤边界**：纯契约与文档，**不动任何数值/存档/玩家可见文案**。**⑥验证**：`content:check` ✅（放宽后无残留判红）· typecheck 四包 0 错 · core **2000** 用例全绿。**【待验收 · 本地提交未推送】**
- 2026-09-20：**成就系统第二批（里程碑 18 枚）完成并合入 main —— §十一之二「未完成」规则的第二次实操（船长令「开始第二批」· 二号 · d2）**——**①船长原话（照抄）**：「**开始第二批**」；展示改版三条：「**成就徽章页面内不要分系列，所有成就卡片排列在一起**」「**比如宇宙探索家 · 1 级这种非常难看，可以采用见习探索家这种**」「**悬浮窗内，名字要单独起一行。时间单独起一行。不要挤一块**」。**②交付**：里程碑 **18 枚**（六个家族，判据 = **终身计数**达阈值：深渊层深 2/3/4/5 · 层末守卫 2/4 · 稀有残骸额外战利品 1/5/20/60/150 · AI 核心 1/2/3/4 类 · 副空间站 1/2 座 · 谜质科技 23 项全满级）⇒ 徽章总数 **63 → 81**；新增 `AchievementSource` 的 `milestone` 分支 · 4 个计数键进可选字段 `state.firstStats`（**不升存档版本**）· 新增**峰值型**写入 `peakFirst`（只升不降、幂等）· 6 枚新图案 `ach-mile-*` · 成就用例 **22 → 29 条**。**③本条的规则实操价值**：收尾时 `grep -rn '⟪未完成'` 复查 —— 上一批留下的是**真记号**（成就第二批未开工），本批完成后**逐处删净**（`data/achievements.ts` 头注 / `data/index.ts` / `core/achievements.ts` / 用例头注 / 设计稿 / roadmap 清单行），并按 §十一之二从 roadmap「未完成功能清单」**删条**。删后复查：全仓残留的 12 处 `⟪未完成` **全部是规则条文与说明的引用**（不是真记号）——这正是该记号的设计意图：**该留的（规则说明）留、该删的（实际豁免面）删**。**④顺手修一处真缺陷**：`state.rareBoxesOpened`（该型稀有残骸"累计已开箱数"）此前**全仓没有任何业务写入点**（只有读档清洗、初始化与两条测试在写它）⇒ 那本账一直空转；已在**全仓唯一出箱点**（`industry.ts` 的 `rarePayout` ＝"烧满 30 m³ 必给一次"）补上写入，并顺带记全局累计供里程碑读取。**⑤一次被护栏当场抓住的错误（留档）**：我第一版把里程碑文案写成"累计开启 N 个**高级箱**"——那是**施工期工作名**，`content:check` 的「陈旧术语契约」判红拦下 ⇒ 按现行口径改为「累计取得 N 件**稀有残骸额外战利品**」。**⑥边界**：纯展示（不发任何 ISK/物品/数值，与第一批同口径）· **只计手动虫洞**（自动探索不经过 `wormholeDescend`）· 老档**零迁移**（新键缺省 0，载入后第一拍现算补发）。**⑦验证**：typecheck 四包 0 错 · core **1975** 用例全绿 · `content:check` ✅ · `ui:rot-check` ✅ · `ui:tip-check` ✅ · `l10n:check` ✅ · `save:migrate` **73/73**（老档零迁移核对）· 桌面 build ✅ · **真渲染冒烟**（react-dom/server：81 枚卡 · 单网格 · 81 个图案无兜底退化 · 18 个新名字全上屏）。**【待验收 · 本地提交未推送】**
- 2026-09-20：**约定新增 §十一之三「本地化＝id 映射制」＋ `AGENTS.md` §1 同步（船长令 · 一号 · main）**——**①船长原话（照抄）**：「**将本地化采用id映射写入规则，以后新内容直接采用这个为其他语言本地化直接提供替换id。**」（同句附「你可以理解润色下重发我看看」⇒ 先出草稿递船长过目 → 船长「可以」⇒ 本条生效）。**②规则要点（正文见约定 §十一之三）**：id 是**唯一键**、语言只是**列**（`packages/data/src/l10n/table.ts` 的 `id → { zh, en, … }`；内容数据名称/说明走按 def id 的英文覆盖表 `packages/data/src/l10n.ts`，`ship/mod/item/…` 域已在工具预留）；**新写的玩家可见文案必须先取 id**——① 造 id（形态 `<域>.<短名>.<三位序号>`）② 调用点写 `t('id')`（core 文案用 `textId`/`errorId`）③ 表里补 `zh`+`en`，**不得内联中文字面量**；id 一经使用**不复用不改名**，只递增序号。**③为什么这么定**：加一门语言 = 给同一批 id **补一列译文**、实现零改动；并避开旧方案「以中文源串当 key」的坑（改一次中文 ⇒ key 失效、既有译文全丢）。**④例外与门槛**：确需保持原样的串就地标 `l10n-keep`（声明过不算未译，新功能收口时复核）；合入前必跑 `npm run l10n:check`（id 形态 · 调用点必须传 id · 表与源码一一对应 · 英文值禁残留中日韩 · 占位符对齐）＋ `npm run l10n:list`。**⑤与既有条文的关系**：`AGENTS.md` §1 的双语是**目标**、本条是**实现方式**；§十一之二（未完成功能不计入排队）是**排队口径**——功能完成合入 main 时按本条补齐 id 与译文；术语专名仍以 `docs/glossary-en.md` 为权威。**⑥落点**：`docs/development-conventions.md` 新增 §十一之三 · `AGENTS.md` §1 加一行（本文件按页首"仅船长明确指示时修改"——本条即该指示）· `docs/catalog.md` 补指路。**⑦边界**：纯规则与文档，**零行为变化**（不改任何表/工具/代码）。**⑧验证**：`docs:index` 重跑 · 编码自检（无 BOM、无 U+FFFD）。**【待验收 · 本地提交未推送】**
- 2026-09-20：**归档：谜质科技批 ＋ 成就/稀有度批（船长令「归档」· 二号 · d2）**——**①船长原话（照抄）**：「**归档**」（紧接其前为「检查你目前还有什么工作没做完」⇒ 本条按 §8／§十五 归档三步办）。**②归档三步（逐条对账）**：① **关键内容并入老文档** —— `docs/glossary.md` 新增 **§十一 谜质科技树**（含 `mt:harvest` 读数、"技能 id 假名"那次结论反转、以及**尚未修的** `tools/wormhole-econ.ts`／`counter-audit.ts` 假技能 id 缺陷）· **§十二 成就徽章** · **§十三 稀有度**，并把旧词条「三联读数」改名为「**编队读数**」；`docs/roadmap.md` 新增两条精简条目（谜质科技 ＋ 每趟真实收获读数 · 成就徽章 ＋ 稀有度收口）。② **删掉工作文档**（细节以 git 历史兜底）——`git rm docs/design/matter-tech-20260919.md` ＋ `docs/design/handoff-20260919-matter-tech-d2.md`。③ **重跑 `npm run docs:index`** ⇒ `287 份文档 · 549 行`，`--check` 报最新。**③顺手做的收口清理（三处）**：① 死链清理——`docs/test-saves/README.md` 等处对已删工作文档的指向改为现行落点；**`docs/design/archive/*` 封存卷与船长原话历史一字未动**（封存件不追改）。② 一号续接卡 `docs/design/handoff-20260920-to-new-pilot1.md` **外科式改 5 处**（原指向我已删的卡片/工作文档），每处标「**二号补注，一号正文未改**」——不重写别人的文档。③ **changelog 第 17 条补注**：「成就第一批交付」里那句"界面 = 二级窗口 · 入口在任务中心"是**改判前**的口径，与同日上条"成就入口改判"冲突 ⇒ 加 🔧 归档期补注指向上条，**历史条目不改写**（台账只追加）。**④本次复核查出的口径错误（已修 · 值得记）**：`packages/data/src/achievements.ts` 头注原文写「**48 枚链徽章**：12 条一般链 × 1/4/7/10 = 48」——**算式对、称谓错**：链徽章实为 **13 条链共 50 枚**（12 条一般链 ×4 = 48 **＋ 探索家 2 枚**），而同一文件下方 `chainAchievements()` 的注释与 `achievements.test.ts` 断言写的都是 **50** ⇒ 同文件两处自相矛盾，且 13+48=61≠63 与总数打架。**实测复核**：总数 **63** = `first-task` 13 ＋ `chain` 50，链组 **13** 条（`explorer` 档位 1/5，其余 12 条 1/4/7/10），**无重复 id、无命名异常、13 条链全覆盖**——即数据与测试一直是对的，**错的只是注释措辞**。已改（`data/achievements.ts` 头注 ＋ `glossary.md` §十二 徽章词条补「链徽章 50 枚（共 13 条链）」把两个数**并列写清**）。⚠ **未改**：设计稿 `docs/design/achievements-20260920.md` §3.1 的拆分表（12 条一般链 48 ＋ 探索家 2 ＋ 任务 13 = 63）**本来就是对的**，故一字未动。**⑤验证**：typecheck 四包 0 错 · core **176 文件 / 1956 用例全绿** · `content:check` ✅ · `docs:index --check` ✅ · 8 个改动文件编码自检（无 BOM · 无 U+FFFD · `.ts` 裸 LF／`.md` CRLF 与改前一致）。**⑥边界**：纯文档收口 ＋ 一处注释更正，**零行为变化**；`docs:design/achievements-20260920.md` **有意保留**——成就**第二批（里程碑成就内容）未开工**，仍挂 `⟪未完成 2026-09-20⟫`，待第二批完成再归档。**【待验收 · 本地提交未推送】**
- 2026-09-20：**星图「敌对派系活跃」标记的图标与文字行互换（船长令 · 二号 · d2）**——**①船长原话（照抄）**：「**星图的敌对派系活跃里的图标和文字行互换一下。**」**②改法**：星图节点上那组标记原为「✦ 徽标在下（中心−20）· 『敌对派系活跃』全称在上（中心−34）」⇒ **对调两行的 `y` 值** = **✦ 在上（中心−34）· 全称在下（中心−20）**。**③为什么只对调两个数就够（值得记）**：互换**不改变标记组的极值**——「组顶边」仍由 ✦（11px）的顶边决定（≈ 中心−42）、「组底边」仍由全称（8.5px）的降部决定（≈ 中心−16）⇒ 上面两个**依赖组顶/底边的常量与函数一字不用改**：`FACTION_MARK_TOP = 41`（画布裁切判据）与 `factionDy` 的避让带（中心−40 ~ −16）**行为与改前完全一致**，不会把"与星系按钮对齐 + 冲突时整组上抬"那套避让算坏（该逻辑是 2026-09-10 船长专门定过的）。仅把常量的**注释基准**更新为"顶端现由 ✦ 决定"。**④边界**：纯星图 SVG 标记内的两行位置；标记组的横向居中、避让、方框选中、圆点脉动、星系名加粗一律未动；不涉数值/机制/存档/文案内容（`✦` 与「敌对派系活跃」文字本身未改）。**⑤验证**：typecheck 四包 0 错 · core **176 文件 / 1946 用例全绿** · `ui:rot-check` ✅ · `ui:tip-check` ✅ · 桌面 build ✅。**观感审查权在船长**（本条属观感改动，未自行截图当结论）。**【待验收 · 本地提交未推送】**
- 2026-09-20：**两张稀有度表合并为一张（船长令 · 二号 · d2）**——**①船长原话（照抄）**：「**渠道归属不动，两个稀有度表没有区别就合并，并删除多余的表。**」**②合并动作**：把 `OFF_MARKET_RARITY_TIER`（2026-09-20 早些时候为"图标标签"单开的那张 · 24 条残骸/碎片）**并入 `RARITY_TIER`** 并**删除该表**（含导出）；`rarityTierOf` / `itemRarityTierOf` 都只读这一张。**合并的前提经实测确认**：两表键空间**无冲突**（24 vs 493，零重叠），且市场外物品在两张表里**键本来就同形**（残骸 `wreck-*`、碎片 `frag-*`）⇒ 直接并入不产生歧义。**③表的新定位（头注已改写）**：本表 = **"物品自身稀有度"的唯一来源**（同时装市场商品 refId 与市场外物品 id 两类键）；**渠道归属仍由 `marketCatalog.ts` 的 `rarity` 字符串决定、本表不参与**（船长「渠道归属不动」）——即"两个概念各归一处，但稀有度只有一份"。**④契约随之改写**（`content:check`）：删掉「键数 = 市场卡数」与「多余键」两条（其前提＝"表=市场卡专属"已不成立），改为——每个市场卡必须有项 · 每个键**要么是市场 refId、要么是真实物品 id**（物品 id 有三族来源：静态表 ＋ `FRAGMENT_RECIPES` 派生的碎片 ＋ `WRECK_GROUPS` 派生的残骸）· 值域 1~5 · **残骸必须有档**（漏配＝静默不显示）· 渠道带（common 不约束 / rare 2~4 / exotic 3~5）。**⑤边界**：纯表结构与文档口径整理，**零行为变化**（合并前后同一物品查到的档完全相同——残骸/碎片原先经 `OFF_MARKET` 辅助表查得同值，现直接命中主表；市场商品的档一字未动）。**⑥验证**：typecheck 四包 0 错 · core **176 文件 / 1946 用例全绿**（用例同步改写：合并核对 ＋ 值域单一来源）· `content:check` ✅ · `ui:rot-check` ✅ · 桌面 build ✅。**【待验收 · 本地提交未推送】**
- 2026-09-20：**弹药 MK2 移入稀有订单 ＋ 废除「common 渠道档必须=1」契约（船长令 · 二号 · d2）**——**①船长原话（照抄）**：「**弹药 MK2移动到稀有订单。「common 渠道档必须=1」的契约废除，改为'common 渠道稀有度不影响交易'。**」。**②落码三条**：① `marketCatalog.ts` 三系弹药 MK2（动能/爆炸/等离子，45/60/80 ISK）由 `common` 改 **`rare`**；② `RARITY_TIER` 对应三行 **R1 → R2**（与 `rare` 的允许带 2~4 相符）；③ `content:check` 的渠道契约改为**三带**：**`common` ⇒ 不约束档位**（只要求 1~5 合法）· `rare` ⇒ 2~4 · `exotic` ⇒ 3~5。**③契约表述的实证（不是只改注释）**：常驻渠道**确实不参与档位运算**——`market.ts` 的 `rareTierWeight`（档 2 ×1 · 档 3 ×0.15 · 档 4 ×0.05）**只在 rare/exotic 分支被调用**，`common` 那条分支（`market.ts:788` 起）从不用它 ⇒ 拿档位卡常驻商品属**把不参与运算的字段当约束**（过度约束），废除有据。**④连带说明（已写进代码注释）**：弹药 MK2 原先走 `common` 的"池商品阶梯"（`poolTarget` 518,400 / `supplyFlow` 4,320 那套盘口），改档后**不再吃那条路** ⇒ 池参数**保留不删**（改回 common 时需要，也是产能标定的原始读数）；本次改档的**实质影响 = 供货渠道由"常驻盘口"变为"稀有订单"**，不是"加权"。**⑤验证**：typecheck 四包 0 错 · core **176 文件 / 1945 用例全绿** · `content:check` ✅（含改后的渠道契约）。**【待验收 · 本地提交未推送】**
- 2026-09-20：**图标模式新增「稀有度小标签」（船长令 · 二号 · d2）**——**①船长原话（照抄）**：「**希望给每个物品的图标模式右上角添加物品稀有度展示的小标签（采用R1表示1级稀有度，并以此类推）**」。**②三问三答（全按推荐）**：① 档位**复用既有 `RARITY_TIER`**（1~5，不新造体系）② 缺档的先**补齐**再上 ③ **所有图标网格**都上。**③落码**：`ui/itemView.tsx` 的共用网格 `ItemGlyphGrid` 加 `rarity` 字段 ＋ 右上角小标签（`app-hand-cell-rarity`）· 物品页/货仓页/手册图鉴三处传档 · 配色**同源市场页既有稀有度色**（1 灰 · 2 淡青 · 3 琥珀 · **4 = `.app-chip.is-rare` 金 `#f4c95d`** · **5 = `.app-chip.is-exotic` 品红 `#ff8ad8`**），不新造视觉语言 · 绝对定位 ⇒ **不占布局**（格子几何与滚动一字未动）。**④撞契约 ⇒ 按 §5.2 摆冲突后获裁**：`RARITY_TIER` 被 `content:check` 锁死为「键集 = 市场卡全集」，而残骸(18)与碎片(6)**没有市场行、也不该有** ⇒ 船长选「另设一张**市场外档档表**」：新增 `OFF_MARKET_RARITY_TIER`（**同为 1~5 档、同一含义**，只是"不参与市场渠道"的那一段；查询一律走单点，调用方不知道有两张表）。**⑤新契约三条 ＋ 一条反向**（`content:check`）：市场外档表的值域 1~5 · 键必须真实存在 · **与主表键集不得重叠**（重叠＝同一物品两个档、查询静默偏向主表）＋**残骸必须都有档**（漏配就永远没标签＝静默半成品）。**⑥首次跑新契约就抓到 3 个坑（护栏有效）**：① 只查静态物品表 ⇒ **6 条碎片全判死键**（碎片 id 是 `FRAGMENT_RECIPES` 派生的）② 同理 **18 条残骸**（`WRECK_GROUPS` 组键派生）⇒ 契约改为**三族来源**（静态表 ＋ 碎片派生 ＋ 残骸派生）；③ 我自己写探针时用 `T[k] ?? '缺省'` 判"有没有" ⇒ 值为 `0/false` 会被误判，改用 `hasOwnProperty`（这条已写进用例注释防后人重踩）。**⑦过程中的一次实质纠错（留档）**：我曾以为 AI 核心与舰船需要"物品 id → 市场 refId"的映射，**实测后作废**——`ai-core-*` 与 36 艘舰船**本来就在主表**（同形键）；**唯一的键形态适配只有鲸王**（物品 id `sh-whale-king` · 主表键 `whale-king` · 市场 refId `ship-whale-king`，三个名字各不相同）。**特别记一条**：市场里另有一条**可交易**的 `core-gamma` 行（refId `gamma` · 档 4），与洞内实物物品 `ai-core-gamma`（档 1）**是两回事** ⇒ 查档函数**只做物品 id 的键适配、绝不把市场短名套上去**（否则洞内核心会被错标 R4）。**⑧覆盖率（实测）**：物品 132/132 · 装备 142/142 · 舰船 41/43 · 蓝图 198/206 ⇒ **"无标签"的只有船长明令不上市场的那几件**（沙猫级 · `sh-dunkleosteus` · 8 张零件蓝图）；**查不到就不显示标签**，不硬塞 R1。**⑨验证**：typecheck 四包 0 错 · core **176 文件 / 1943 用例全绿**（新增 7 条）· `content:check` ✅ · `ui:rot-check` ✅ · `ui:tip-check` ✅ · `l10n:check` ✅ · 桌面 build ✅。**【待验收 · 本地提交未推送】**
- 2026-09-20：**查「多个一次性图纸挨个撤退」查出并修掉一个静默缺口（船长点名 · 二号 · d2）**——**①船长原话（照抄）**：「**检查多个一次性蓝图工作挨个撤退是否有问题**」。**②查出的缺口（真 bug，非理论）**：`spentOneTimeRecipes` 是**数组**（只表达"有没有"，**不记几条**），而一次性图纸存量可以 > 1（同名线能同时在跑）⇒ 上一条"取消退书"若**无条件摘标记**，会出现「**书架上有书、却被判名额已用尽**」的死结。触发序列：存量 2 → 开 A、B（标记置位）→ 取消 A（退 1 本、摘标记）→ 拿这本再开 C → **B 完工**（标记重新置位）→ 取消 C：书退回来了，但 `filter` 摘不掉 B 留下的标记 ⇒ **有书也永远开不了工**（玩家侧看到的是"这张图纸废了"）。**③修法**：`refundOneTimeBook` 里的"摘标记"改为**只在没有别的同名线还在跑时**才做 —— **标记的所有权归最晚完工的那条线**（它完工时会置位，等于替后续取消接管了这个标记）；等它也被取消，才轮到它摘掉。**④验证方式（含反向验证）**：新增 3 条用例（多张不同图纸同时开、挨个撤退；同名两条线跑出"完成 1 条 + 在跑 1 条"再撤；同名三条线挨个撤退守恒）。**反向验证**：临时把修复条件去掉 ⇒ 新用例**红 2 条**，装回即 16/16 全绿 ⇒ 用例确实钉得住缺口，不是摆设。**⑤过程中的两处自我纠错（留档，避免下一个人重踩）**：① 制造线**不记开工时刻**，构造"B 完工而 C 仍在跑"只能靠推进 `gameMs` 错开开工；② **错开 1ms 不够**——`advanceManufacturing` 的判据是 `gameMs >= finishAtGameMs`，推进到 B 的完工时刻会把 C 一并结算（实测 B=1500000 / C=1500001）⇒ 用例里错开 500 秒。**⑥验证**：typecheck 四包 0 错 · core **174 文件 / 1925 用例全绿**。**⑦边界**：只动"退书时的标记归属"，不涉数值/经济/存档结构（`bookSpent` 仍为可选字段）。**【待验收 · 本地提交未推送】**
- 2026-09-20：**一次性图纸「取消制造退书」改判（船长令 · 二号 · d2）**——**①船长原话（照抄）**：「**一次性蓝图的制造取消后返还玩家蓝图**」。**②冲突的旧裁定（已作废）**：2026-09-12 一次性图纸批的 **3甲** 原文「书在**开工那一刻**扣掉（与材料同源）；**取消/失败不退还**（书代表"一次制造资格"）」——本条按新令改判，旧条在 `tests/one-time-blueprint.test.ts` 头注与 `manufacturing.ts` 处标明作废。**③新口径三条（船长三问三答，全按推荐）**：① 取消时**书与"名额"一起退还**（只退书是"假退"——`recipeCapability` 的判据是 `spentOneTimeRecipes` 那个标记，不退标记书回来了仍判 `exhausted`）；② **未完工就能取消、不分进度**（不卡时点）；③ **完工不退**（跑完的线已不在在跑列表，书已兑现成产物——那才是"只能制造一次"的落点）。**④落码**：`manufacturing.ts` 新增 `refundOneTimeBook`（与 `spendOneTimeBook` **严格互逆**：书回书架 ＋ 从 `spentOneTimeRecipes` 摘掉；**幂等**：不在标记表里就什么都不做，重复取消不会凭空造书）；`ManufacturingRunState.bookSpent`（**显式记账**"本线确实扣了一本书"，不靠取消时事后推断——推断在"同名书存量 > 1 且其中一次已完工"时会**多退**）；`cancelManufacturing` 据此退书并改写日志文案。**⑤顺带查了"能不能靠反复开-取消刷东西"（结论：不能）**：材料的扣与退走**同一个 `matNeedCount` 公式**（退的正好等于扣的），产物只在完工时给 ⇒ **无净收益**；另有用例钉住"取消普通图纸时书架与名额表一字不动"。**⑥界面文案同步**：组装机的取消按钮悬停、取消成功提示、蓝图书架那枚提示图标（原写"开工时消耗"，现补"取消制造会连同名额一起退回书架"）。**⑦验证**：typecheck 四包 0 错 · core **174 文件 / 1922 用例全绿**（一次性图纸组 13 条，含新增"退书+名额恢复 / 只退这一本 / 重复取消拒绝 / 普通图纸零变化"）· `content:check` ✅ · `save:migrate` **71/71** · `ui:tip-check` ✅ · `ui:rot-check` ✅ · 桌面 build ✅。**⑧边界**：不动经济数值与其它机制；`bookSpent` 为**可选字段**（缺省 = 没吃书 ⇒ 旧档遗留线取消时不动书架，零行为变化）。**【待验收 · 本地提交未推送】**
- 2026-09-20：**成就入口改判：左侧导航一级页 ＋ 页内二级子窗口容器（船长澄清 · 二号 · d2）**——**①船长原话（照抄）**：「**成就页面入口不是说放到左侧导航页内吗**」→ 追问后答：「**一级页，但是内部再加一个二级子窗口容器。**」**②我错在哪（如实记录）**：闸门第一轮船长选的**就是「独立一级页」**，是我提出它与「一级页不滚」红线冲突后，船长改口「改放二级窗口」；**我把那句理解成"取消一级页导航、改做浮层弹窗"**（做成了 `app-modal-mask` 弹窗 ＋ 任务中心一个按钮当入口）⇒ 这一步是我自己推的，船长并未说要撤掉导航位。**③改判后的口径**：**左侧导航新增「成就」一级页**（`App.tsx` 的 `NAV_ITEMS` ＋ `PageKey` 加 `'achieve'` ＋ 登记进 `PAGE_NO_SCROLL`）；页内是**固定头 ＋ 内容内滚的子窗口容器**（`Panel className="is-fill"` ＋ `.app-win-body`，与任务中心 `TaskPanel` 同一套结构）⇒ **页面本体不滚、红线照守**，滚动只发生在容器里；任务中心那个入口按钮**已撤**。**④教训（与 §5.2 同族）**：船长为绕开红线改口时，**要么按原方案＋例外开口子、要么明确"去掉哪一部分"**——「改放二级窗口」这种相对旧规则的措辞**不足以推断"导航位也撤"**，下次遇到同类改口应先确认"原来那个位置还留不留"，别自行归并。**⑤验证**：typecheck 四包 0 错 · core **174 文件 / 1916 用例全绿** · `ui:rot-check` ✅ · `ui:tip-check` ✅ · `l10n:check` ✅ · 桌面 build ✅。**⑥边界**：纯界面位置与结构，不涉数值/机制/存档/文案内容。**【待验收 · 本地提交未推送】**
- 2026-09-20：**成就系统第一批（徽章框架）完成并合入 main —— 第 11 条那条「未完成」规则的首次实操（船长令 · 二号 · d2）**——**①船长原话（照抄）**：「**继续之前的成就系统，并标注未完成。新增规则，未完成的功能不计入本地化工作排队。**」＋链徽章两条裁定「**链徽章在1级，4级，7级，10级时候各给一枚，图案相同，用颜色区分。**」「**宇宙探索家，只做2枚，分别是1级和5级。**」。**②交付**：徽章 **63 枚**（`data/achievements.ts`，由 `FIRST_TASKS` 与链配置**派生** ⇒ 任务表增删自动跟随）· 核心 `core/achievements.ts`（`advanceAchievements` 每拍**现算补发**、幂等）· 存档 **v30** ＋ `MIGRATIONS[29]` ＋ 载入清洗 · 挂点 = `engine.ts` 的 `advanceFirstChains` 之后（任务与链同拍可见）· 界面 = **二级窗口** `panels/Achievements.tsx`（入口在任务中心，不占一级导航 ⇒ 不碰「一级页不滚」红线）〔🔧 **归档期补注（2026-09-20）：界面口径已于同日改判为「左侧导航一级页 ＋ 页内子窗口容器」，见上条"成就入口改判"；本条交付描述中的"入口在任务中心"已作废，页面本体不滚的红线照守**〕· 新增 `ach-*` 盾徽图案族（14 个 SVG 线稿）· 界面词条 7 条 · **17 条**新用例。**③本条的规则实操价值（重点）**：施工收尾时用 `grep -rn '⟪未完成'` 复查，**抓出 3 处"把已完成的第一批也标了未完成"**（`core/index.ts` / `dict.en.ts` / 测试头注）——
  这正是该记号最危险的失效模式：**残留 ⇒ 本地化永远跳过它**（条文里已写明"完成即删"）。已按条文改正：
  记号只留在**第二批（里程碑成就内容）**的落点（`data/achievements.ts` 的 `milestone` 分类注释、
  `core/achievements.ts` 头注释、`roadmap` 清单行）＋ 规则本身所在的两份文档。
  **④两条设计偏差（施工中发现，已回写设计稿）**：① 徽章总数 **58 → 63**（设计稿初版把 `FIRST_TASKS`
  数成 12 条，实际 **13 条任务 / 13 条链**）② **老档补发不写在迁移里**：`MIGRATIONS` 只吃 raw state、
  拿不到内容表 ⇒ 改由载入后第一拍现算补发（判定只有一份、幂等）。**⑤验证**：typecheck 四包 0 错 ·
  core **174 文件 / 1913 用例全绿**（新增 17）· `content:check` ✅ · `ui:rot-check` ✅ · `ui:tip-check` ✅ ·
  `l10n:check` ✅ · `save:migrate` **71/71**（v25/v28/v29 真档 → v30，资产不变）· 桌面 build ✅。
  **⑥边界**：徽章**纯展示**（船长裁定：不发 ISK/物品/数值）⇒ 不动经济、不影响任何既有行为；
  老档迁移零行为变化。**【待验收 · 本地提交未推送】**
- 2026-09-20：**约定新增 §十一之二「本地化排队口径：未完成的功能不计入」＋ roadmap 新增「未完成功能清单」（船长令 · 二号 · d2）**——**①船长原话（照抄）**：「**新增规则，未完成的功能不计入本地化工作排队。**」（同句前半为「继续之前的成就系统，并标注未完成」⇒ 本条为其中"标注未完成"与"本地化排队"两件的规则化）。**②新条文（§十一之二）**：**未完成**（在建／挂起／只有预留接口）的功能，其文案**不进本地化工作排队**；三号看到记号**一律跳过**，等该功能**完成并合入 main** 后再排。**③识别记号（统一 · 必写）**：改动处留一行 **`⟪未完成 YYYY-MM-DD⟫`** ＋一句状态/恢复入口（照 §十三 `⟪文案调整⟫` 同款；`⟪⟫` 为全仓保留记号）⇒ `grep -rn '⟪未完成'` 一次列全豁免面；**功能完成并合入 main 时须删掉该处记号**（残留 = 本地化永远跳过它，属交付缺陷）。**④为什么**：未完成的文案会随实现继续变（改名/改口径/整段重写都常见）⇒ 先翻等于白翻，还会撑大 `l10n:check`「未译读数」的分母、让进度读数失真。**⑤配套载体**：`docs/roadmap.md` 新增「**未完成功能清单**」条目（功能名 · 状态 · 挂起原因 · 恢复入口），与本节记号**两条一起用**——记号管"本地化豁免"，清单管"工作挂账"。**⑥现状登记（本批同时办理）**：成就系统分两批——**第一批徽章框架：设计稿待船长确认（未开工）**；**第二批里程碑成就内容：未开工**；两处均标 `⟪未完成 2026-09-20⟫`。**⑦边界**：纯工作流程与文档纪律，不涉数值/机制/存档/玩家可见内容。**【待验收 · 本地提交未推送】**
- 2026-09-19：**约定 §十三 新增「改过的玩家可见文案要留痕」＋ 谜质科技悬停文案改三行（船长两条 · 二号 · d2）**——**①船长原话（照抄）**：「**有过调整的文案都留个备注，方便之后快速查找。以及我建议名称+等级为一行，效果单独起一行，最后的可研究下一级（5 谜质 + 6,000,000 信用点）这种再另起一行**」。**②新条文（§十三）**：凡**按船长报障/裁定调整过**的玩家可见文案（界面文字 · 按钮与**悬停提示** · 日志模板 · 卡面说明…），① 在**改动处**留一行可检索备注，记号**固定**为 **`⟪文案调整 YYYY-MM-DD⟫`** ＋缘由（船长原话摘要，照抄优先）；② 在**当批工作文档**的「**文案调整台账**」登记一行（日期 · 位置 · 原 → 新 · 缘由），归档时随工作文档并入老文档（§十五）；③ **只标调整过的**，新写的不标（`git blame` 已足够）。**为什么**：`git log -S` 只查得出"某句变过"，查不出"改过哪几句、各自为什么改"⇒ 记号让 `grep -rn '⟪文案调整'` 一次列全（`⟪⟫` 实测全仓未占用，专供此事）。**③同批落码**：`panels/MatterTechTab.tsx` 科技节点悬停改**三行**（①`{名称} · {x}/{y} 级` ②`{效果}`（数据表 `note`，每级值在内）③`{已满级 / 可研究下一级（5 谜质 + 6,000,000 信用点） / 暂不可研究（原因见详情）}`）——上一条（同日本条之前）已先把"前置条件"换成"科技效果"；机制上 `\n` 能生效是因为 `.app-tip` 是 **`white-space: pre-line`**（二号上一轮在代码注释里误记"换行不生效"，同批改正）。**④验证**：typecheck 四包 0 错 · `ui:rot-check` ✅ · `ui:tip-check` ✅ · 桌面构建 ✅ · `l10n:check` ✅（三行各为一条干净 key，交三号逐行翻）。**⑤边界**：纯渲染层文案与流程纪律，不涉数值/机制/存档。**【待验收 · 本地提交未推送】**
- 2026-09-19：**新增正式工具 `tools/commit-msg.ts`（`npm run commit:msg`）＋ 约定 §三 那条纪律改指它（船长令 · 二号 · d2）**——**①船长原话（照抄）**：「**那能帮我更新吗**」（紧接上一条：船长问「我看你的双引号被 PowerShell 弄坏了好多次了是否能提前规避」，二号答"可再落一个 `npm run commit:msg` 从 stdin 读消息、绕开命令行拼装"，本条即那位"零思考保险"）。**②工具做什么**：消息**从 stdin 读字节**（也支持 `--file`）⇒ 不经命令行参数，彻底绕开 5.1 的拼装陷阱；内置 **①编码自检**（UTF-8 优先，出替换符则退 **GBK/936**——管道编码随 `[Console]::OutputEncoding`，本机实测 = utf-8）· **②体检**（空消息/空标题拒绝 · 半角双引号计数提醒〔引号可能已在命令行那步被吞，真吞了工具看不出来〕· GBK 误读产物特征字告警 · 行尾归一 LF · 写出**无 BOM UTF-8** · **逐行预览**）· **③转交**（写 `$TEMP/whale-commit-msg-*.txt` ⇒ `git commit -F` ⇒ **删临时文件**；`git` 自己的参数原样透传，另有 `--add`〔先 `git add -A`〕与 `--repo`〔指定工作树〕）。**③配套**：`package.json` 新增脚本 `commit:msg`；约定 §三 该条 ① 由"手工 `-F`"改为"**首选 `npm run commit:msg`**，手工 `-F` 同理"；头注带**版本自检**四条（游戏 v0.1.0 · 存档结构 **v29** · 最后核对/跑过 2026-09-19）⇒ `tools:audit` 可识别。**④验证**：**用本工具提交它自己**（消息含多行 + 中文引号 + 反引号；`git log -1 --format=%B` 逐字核对与预览一致、无吞字）· 实测两条边界并把写法固化：**①无参数** `$msg | npm run commit:msg` **可用**；**②给 git 传参数**要写 **`npm run commit:msg '--' --amend`**（裸 `--` 会被 PowerShell 当"参数结束"标记吃掉 ⇒ npm 收不到转发分隔符、参数被当 npm 配置丢掉，实测报 `npm warn Unknown cli config`），或直连 `npx tsx tools/commit-msg.ts --amend`。**新增 `--dry-run`**（只打印将要执行的 git 命令，专为排查参数透传）⇒ 上述结论即由它实测得出。typecheck 四包 0 错 · `tools:audit` 收录。**⑤边界**：纯开发流程工具，不碰数值/机制/存档/玩家可见内容。**【待验收 · 本地提交未推送】**
- 2026-09-19：**约定 §三「编码纪律」新增一条：递给原生命令的参数里禁带双引号（船长令 · 二号 · d2）**——**①船长原话（照抄）**：「**我看你的双引号被 PowerShell 弄坏了好多次了是否能提前规避**」（起因：二号用 `git commit -m $msg` 传多行中文提交消息，两次被 PowerShell 5.1 弄坏——消息里的 `"` 被吞掉、整条消息被按空格/换行切成一堆 pathspec ⇒ git 报 `did not match any file(s) known to git`，改走 `-F <临时文件>` 才提交成功）。**②根因（实测证据）**：本机 `pwsh` = **Windows PowerShell 5.1**（非 PS 7），它把参数递给原生进程（git/node/tsx…）时**自己拼命令行**，参数内的 `"` 既不被转义也不被保留——`$s = 'a "b" c'` 经 `node -e 'console.log(JSON.stringify(process.argv.slice(1)))' $s` 到达时打印 **`["a b c"]`**（引号凭空消失）；长文本另会被按空格/换行**切碎成多个参数**。**③条文四条**：①多行或含引号的文本**一律走"写文件 + `-F`"**（`git commit -F <临时文件>`；消息文件写 `$env:TEMP`、用完即删；写盘用 .NET 显式 UTF-8，与本节既有条一致）；②短消息**避开 `"`**，中文引号用「」；③单引号 here-string `@'…'@` **只保证 PowerShell 自己不解析内容**、**不保证原生侧收到原样** ⇒ **禁止**把 `-m $msg` 当安全路径；④登记同族 5.1 坑：**无 `??`**（语法错）· **双引号串里反引号是转义符**（`` `r`n `` 换行、单引号串里是普通字符）· 单引号串里字面单引号要写 `''`。**④验证**：按新写法提交成功（`f2cf81a8`）；编码三查（BOM 与改前一致 / 行尾 CRLF 一致 / 乱码 0 命中）。**⑤说明**：本条为**开发流程纪律**，与玩家可见内容无关，不涉数值/机制/存档。**【待验收 · 本地提交未推送】**
- 2026-09-19：**语言口径改判：玩家可见文案中英双语（船长令 · 三号 · verify40）**——**①船长原话（照抄）**：「**希望对游戏进行英语本地化处理。**」（四步闸门集中提问 → 七问七答，船长全按推荐口径裁定，末句「**可以**」为方案确认）。**②改判内容**：`AGENTS.md` §1 那条「一律中文：…游戏文案、日志」拆为两条——**开发侧（沟通/总结/文档/注释/日志）仍全文中文**，**玩家可见文案（界面 / 内容数据 / 说明）做中英双语**（本条即船长特批的授权记录）；同日旧裁定 `docs/design/steam-store-draft-20260911.md:159`「英文版文案：本次按裁决不做」**待归档时标注作废**（工作期不改旧文档，见 §8）。**③已定口径七条**：范围 = **可玩骨架**（UI + 名称/短说明；手册词条/通讯/公告等长文列第二批）· 切换 = **设置里可切 + 默认跟随系统**（语言存 `localStorage`，**不进存档**）· 老档日志**保持中文**（`state.logs[].text` 存的是渲染好的文本；「新日志跟语言」在引擎侧文案批兑现）· 翻译来源 = **三号出术语表 + 全量英文初稿，船长终审** · 专名 = **直译**（长尾鲨级 = Thresher-class · 钛钢合金 = Tritanium Alloy · 鲣鱼级 = Skipjack-class）· 排版 = **先出逐页溢出读数、船长逐页定**（「一级页不滚」红线默认维持）· 铁律 = **只许玩家可见文案双语**。**④批量实测（侦察口径：含 CJK 的字符串字面量）**：`packages/data` ≈2,495 · `packages/core` ≈4,809 · 渲染层 ≈3,531 ⇒ 合计 ≈10,800 条；长串（≥60 字符）≈725 条；`addLog` 调用 315 处；手册词条 105 条；硬编码 `toLocaleString('zh-CN')` 353 处；**仓库无任何 i18n 骨架**。**⑤落码分期**：P0 术语表 + 专名译名表（`docs/glossary-en.md`，船长审后**冻结**）→ P1 i18n 骨架（locale 偏好 + `t()` 源串词典 + 数字/单位格式化 + Latin 字体栈 + 新工具 `npm run l10n:check`）→ P2 内容英文（≈600 条名称/短说明）→ P3 界面 chrome 英文（≈600~900 条）→ P4 逐页溢出读数 → P5 长文与引擎文案（手册 / 通讯 / 公告 / core ≈1,000 处）。**⑥架构取舍（两条，为免上千处改动）**：内容层走**按 id 索引的英文覆盖表 + `localizeCtx(ctx, locale)`**（`locale==='zh'` **原样返回** ⇒ 工具/测试/模拟逐字不变）；界面层走**以中文源串为 key** 的词典（**缺 key 回退中文** ⇒ 可分批翻、永不白屏、可出未译清单）。工作文档 `docs/design/l10n-en-20260919.md`。**【待验收 · 本地提交未推送】**
- 2026-09-18：**约定 §二 新增「报障处理：玩家反馈的问题不在本地存档」＋ 组装机循环制造改按「批」计数（船长两句 · 二号 · d2）**——**①船长原话（照抄）**：「**并不是本地存档。但是同意将UI改成按批制作。建议加入提示，所有玩家反馈的问题，并不在本地存档。**」（起因：玩家报障「军用修理组件设目标 1000，自动生产 151 件后自己停止」）。**②新条款（§二）**：玩家报障说的是**他们自己那份线上档**上的现象 ⇒ **本地（船长）存档与此无关，不许拿它复现/验证/覆写**（真档只读）；处理报障的**唯一正道 = 从代码与真引擎复现**（一次性探针或确定性用例），结论要写清「复现路径 / 根因代码位置 / 玩家可见现象为何长这样 / 改法与影响面」，若结论是"设计如此"还要把"提示够不够醒目"交给船长裁。**③本批取证（真引擎复现）**：`bp-repairkit-mil` = **3 个/批**，而「循环制造」的 `produced`/`goal` 一直按**批**计数、界面却写"件"⇒ 玩家那份「151 件」实为 **151 批 = 453 件**；实测（材料只够 200 批）引擎跑满 200 批后**材料耗尽停线**，日志写「本卡合计 200 件」而**实际到手 600 件** ⇒ 停线本身是设计内行为，坏的是**单位口径**。**④落码（船长裁定：UI 改按"批"，引擎口径不动 ⇒ 零迁移）**：`manufacturing.ts`（日志「本卡合计 N 批」/ 停因「已达成目标 N 批」/ 头注补"单位口径"段）、`Industry.tsx`（目标输入 tooltip「目标**批数**…一批的产出件数见蓝图说明」）、`ShipPage.tsx`（线行进度「本卡合计 X/Y 批」）；新增用例钉住多产出蓝图（目标 2 批 ⇒ 到手 6 件 · 日志写"2 批"）。**⑤验证**：typecheck 四包 0 错 · core 166 文件 / 1817 例全绿 · `content:check` ✅ · `ui:rot-check` ✅ · 桌面构建 ✅。**【待验收 · 本地提交未推送】**
- 2026-09-18：**新增正式工具 `tools/chain-balance.ts`（`npm run balance:chains`）＋ 约定 §二 验证闭环登记一行（船长令 · 一号）**——**①船长原话（照抄）**：「**市场订档留，临时探针合并为工具。**」（前句 = 市场链顶档 1,000 亿信用点保留；同批上游指令见 `docs/design/chain-retune-20260918.md`）。**②改了什么**：约定「验证闭环」清单在 `npm run balance` 那行后**新增一行**——任务/次数链/市场收入/技能训练 的"要多久"读数跑 `npm run balance:chains`（`tools/chain-balance.ts`，四段：`tasks` 13 条「第一次」任务各自耗时（能跑引擎的实跑）· `chain` 13 条次数链顶档＋逐级累计（读 `CHAIN_TIERS`，改表重跑即得新读数）· `market` 交易收入链标定（各矿种满产卖出 ISK/小时 ＋ 市场吸纳上限对照）· `skills` 训练时长与满级总级数；`-- chain` 可只跑一段）。**③合并来源**（四个 `_` 临时探针**已删除**）：`_task-times.ts`（逐条任务耗时）· `_chain-times.ts`（链逐级/顶档）· `_market-income4.ts`（交易收入标定）· `_skills-time.ts`（技能训练）；工具头注释带「版本自检」三条（存档结构 **v27** · 2026-09-18），`tools:audit` 列为【在版本内】。**④口径**：新档（无技能；6 条线 = 驾驶 1 ＋ AI 核心上限 5；采矿 50 单位/分·船）、游戏内时间与真实时间 1:1；**每段自己 `buildSimContext()` ＋ 新档，互不干扰**，跑引擎的段只在内存里推进、不写任何存档。**⑤验证**：四段全跑 exit 0（149 行读数）· `tools:audit` ✅（4 个工具在版本内）· `git status` 里 `?? tools/_*.ts` 待处置清单**已清空**。**【待验收 · 本地提交未推送】**
- 2026-09-18：**「说明文案 ≤30 字」废止并清档（船长令 · 一号）**——**①船长原话（照抄）**：「**「说明文案 ≤30 字」的计划已经废止，清档所有相关内容。**」（同一句指令前半是「继续完成未完成内容」）。**②改了什么**：约定 §十三 第 6 条（一次说明 ≤30 字）**划掉并标废止**，其计数口径（只数汉字/字母/数字）随之作废；护栏由「两条契约」缩为**一条**——`content:check` 的「**说明文案原因解释契约**」（`parenSpans` / `REASON_HINTS`），且**取消基线宽限**（命中即红）；**删除件**：`tools/copy-len-baseline.json`（268 条存量基线）· `tools/copy-len-baseline.ts`（基线生成器）· `tools/copy-len.ts` 里的 `COPY_LEN_MAX` / `copyLen()` · content-check 的「说明文案长度契约」与「存量待改写」清单行 · `AGENTS.md` §5 那一行里的②与"存量 268 条" · `packages/data/src/modules.ts` 里引用 `copyLen` 的注释一句 · 本仓用例里 `brief ≤ 30` 的断言一条。**③保留**：第 5 条「**不写原因解释**（括号只许放规格）」及其护栏**照旧生效**（此条船长未废止）。**④验证**：`content:check` ✅（报告行现在是「说明文案原因解释契约：核对 533 条说明 · 括号原因解释报红 0 处（**长度上限已于 2026-09-18 废止，相关基线/存量清单已清档**）」）· typecheck 0 错 · core 全绿 · 双 build ✅。**⑤背景**：此规矩原为 2026-09-17 船长立（见下方同日条目），本次按船长令整体裁撤，**不再有"存量待改写"这项工作**。**【待验收 · 本地提交未推送】**
- 2026-09-17（**交接**）：**说明文案存量改写工作转交三号；二号消档**——船长指示「**修改文案的工作已经交给了三号，你这边消档**」⇒ 二号删除工作文档 `docs/design/copy-rules-20260917.md` 与改写清单 `docs/design/copy-rewrite-list-20260917.md`（**268 条拟稿留在 git 历史 `a2582893`**，需要可随时取回）；**规则与护栏一件不撤**（约定 §十三 第 5/6 条 · `AGENTS.md` §5 一行 · `content:check`「说明文案长度契约 / 说明文案原因解释契约」· `tools/copy-len.ts` 口径单点 · `tools/copy-len-baseline.ts` 与 268 条宽限基线）⇒ **三号按同一套护栏推进存量改写**，写完删基线文件即转全量强制。**二审要点（二号留下的经验，供三号参考）**：① 旧文案里的数必须与 `content:check`「产物说明契约」自洽（改完必跑）；② `box-relic-*` 的「占货仓 2×2 格」是 2026-09-15 改规格前的**陈旧数**（现行 3×2）；③ 残骸/碎片/货柜类旧文高度同构，可整组共用一句新文案；④ **基线要跟着别人的文案批次重生成**——首次合并时一号的「去槽位标签」批（`2090a825`）改了 6 条但仍在 30 字以上，护栏按"改动过 ⇒ 报红"点了名；按船长「存量只列清单不阻断」的裁定，**重跑 `npx tsx tools/copy-len-baseline.ts`** 把它们放回「存量待改写」（267 条），闸门复绿。⇒ 口径 = **基线 = "当前尚未改写的存量快照"**，任何人的文案批次落地后都重生成一次。（⚠ **本条所述护栏已于 2026-09-18 随废止删除**，存档备查。）
- 2026-09-17：**约定 §十三 新增「道具说明两条硬规矩：不写原因解释 ＋ ≤30 字」＋ 两条自动护栏（船长三条指令 · 二号 · d2）**——**①船长原话（照抄）**：「**无人机算开火，说明文不用更新**『隐身期间自带的固定舰炮不会自动开火（否则开战第一拍就会被它暴露）』**。过于多余。还有，新增文案相关规则，不要在任何说明文案内写入原因解释（特别是使用括号进行解释的这种）。文案要保证在30个字以内。**」；**三问三答（船长裁定）**：**存量一起改**（乙）· **计数 = 汉字/字母/数字各 1 字、标点不计**（甲）· **落点 = 本约定 ＋ `AGENTS.md` §5 ＋ `content:check` 护栏（只对新增/改动亮红）**（甲）。**②条文（§十三 第 5/6 条）**：(5) **不写原因解释**——说明只回答"**这件是什么 / 有什么用**"，不解释"为什么这样设计"，**括号只许装规格**（「（20 秒）」可以；「（船一动就藏不住）」「（否则开战第一拍就会被它暴露）」不行）；(6) **一次说明 ≤30 字**（数值优先级：本件独有、界面别处看不到的**必留**；CPU/价格/槽位/单发等面板可见的**可省**）。**③护栏（落码）**：`content:check` 新增「**说明文案长度契约**」＋「**说明文案原因解释契约**」；口径单点 = 新文件 **`tools/copy-len.ts`**（`COPY_LEN_MAX` / `copyLen` / `parenSpans` / `REASON_HINTS` / `copyEntriesOf`，**基线生成器与体检共用，不许各写一份**——首版两边各写一份 kind 键，基线当场失效 0 宽限，已收口）；覆盖**装备/物品/舰船/蓝图/敌卡 533 条**；**宽限机制** = `tools/copy-len-baseline.json`（`npx tsx tools/copy-len-baseline.ts` 生成/收缩）：**基线内且文本一字未动 ⇒ 只列清单、不阻断**；**新增的或改过文本的**超线/括号原因 ⇒ **立即报红**；存量改写推进时基线自然收缩，**全部改完 ⇒ 删基线文件转全量强制**。**④实测（口径生效时）**：存量超线 **268 条**（装备 74 · 物品 137 · 舰船 26 · 蓝图 8 · 敌卡 23；中位 33/56/33/22/34 字，最长 `mod-lair-turret-e` 104 字）、括号原因解释存量 **2 处**（`mod-stealth-2/3` 的「船一动就藏不住」）；**负向验证**：把沙猫说明改成超线＋括号原因 ⇒ 两条契约各自点名「舰船 `sandcat`（32 字）」与「括号内『否则会被打爆…』」，撤销后回绿。**⑤同批撤回**：本轮先前给 `mod-stealth-2/3` 说明书加的那句「（否则开战第一拍就会被它暴露）」与开战提示条新增的半句**已撤回**（`packages/data/src/modules.ts` 与改动前**逐字一致**）——船长判「过于多余」；**无人机算开火**（机群开火照旧现形）亦为同日裁定，只闭基础舰炮那一门。**⑥待办**：存量 **268 条**分批改写（装备 → 物品 → 舰船/蓝图 → 敌卡），每批出「旧 → 新」对照表交船长验收；改写原则与样例见工作文档 `docs/design/copy-rules-20260917.md`（**原则待船长点头**）。**【待验收 · 本地提交未推送】**
- 2026-09-16：**约定 §十三 新增「道具说明四条禁写」＋ 审查口径与护栏缺口登记（船长点名审查装备/物品文案后立）**——**①起因（船长原话照抄）**：「**审查装备和其他物品的文案，将比如（市场稀有）；（护盾与装甲都被打穿之后，最后那段血比别人厚出六成；这是全游戏唯一能加厚结构层的模块）；炮台装填间隔 ÷1.18（射速计算机 MK3 是 ÷1.12）；这类明显不适合让玩家看到或者重复数据的部分修改并列给我看**」→ 出清单后裁决「**行，按你推荐来**」⇒ **43 对替换落码**（渠道标签 32 处 · 跨件对比 19 件 · 绝对化 5 处 · 开发词 1 处 ＋ 第二轮扫描补出的同类漏网 6 处），并**顺带修 8 处事实错误**（6 处"无蓝图"与数据不符 · 近防炮「唯一能打敌方机群」· 虚空母矿「唯一能带回来的东西」）。**②本条新增（并入 §十三，四条禁写）**：**(a) 不写渠道/档位标签**（市场稀有/奇货现货/专供/需高声望 ⇒ 一律删；"能不能造"用中性句「**不可制造**」，有蓝图的件不提）；**(b) 不复述另一件装备的名字与数字**（跨件对比——两条理由：说明回答"这件是什么" ＋ 引他件数字会在他件调值时静默过期；实测删掉跨件句后靠"跨件差值"解释的数字当场被「产物说明契约」判"无法解释"）；**(c) 不用绝对化/推销语**（全游戏唯一 / 本套最省/最贵/最慢；**例外** = 登记在案的定位口径如无人机四型定位、以及非数据断言的风味语）；**(d) 开发词照旧禁入**（「拍卖行**口径**」→「拍卖行里的」）。**③同时登记两件事**：**审查口径**（四类机械扫描的判据，含两处实测踩到的判据缺口——**部件名简写**会漏检、`最X` 形容词表不全）与**护栏缺口**（「文案纯净契约」不扫 `MODULES`/`ITEMS`/蓝图的 `description` ⇒ 第 (d) 类在道具说明里无护栏，补法 = 给「产物说明契约」加同一份 `BANNED` 扫描，**是否立项听船长**）。**④验证**：typecheck 四包 0 错 · core 153 文件 / 1646 例 · `content:check` ✅（产物说明契约 273 条数值声明 / **无法解释 0 条**）· `ui:rot-check` ✅ · 双 build ✅（**纯文案**：零数值/机制/存档改动）。**【已验收 · 已合入 main · 未推送】**
- 2026-09-16：**电子舰 CPU +100（165 → 265 · 两艘 · 只动一个字段）**（船长：「**提高电子舰的CPU，提高100**」）——两艘电子舰（`sh-wh-a-frigate` 掠袭 / `sh-wh-d-frigate` 哨戒，子分类 `subClass: '电子舰'` 全仓恰好 2 艘）`cpu` **165 → 265**；槽位/三层血/命中/回避/分辨率/锁定/速度/机动/货舱/价格一律不动（`--numstat` = 8 插入 / 2 删除）。实际含义 = 从"火控件与武器争 CPU、装不满"到"武器 + 整套设备一起带满"（3× 攻坚炮台 MK3 156 ＋ 隐秘行动装置 MK3 80 = 236：改前 165 装不下、现 265 余 29）。**用例 +3**（`wh-ship-baseline.test.ts` 的「电子舰 CPU +100」：两艘 = 265 且电子舰恰好 2 艘 · 只动 CPU 的护栏 · 装配面 236 ≤ 265 且 > 165）；**负向验证**：改回 165 ⇒ 2 例红。此条**不改约定正文**，仅登记（数值以 `packages/data/src/ships.ts` 为准）。**【已验收 · 已合入 main · 未推送】**
- 2026-09-15：**AGENTS.md 全面压缩（19 KB → **12 KB** / 199 行 → **89 行**）＋ 新规矩「AGENTS.md 仅船长明确指示时修改」（船长定）**——**①船长原话（照抄）**：「那决定压缩AGENTS.md，并且之后除非我特意说明修改AGENTS.md，否则不要修改」。**②压缩方式（要点化，非删规则）**：长细则压成短句 + 「细则见约定 §X」指针——§1 汇报两条长款压短 · §2 四步闸门合段不丢四步三红线 · §3 rot-check 括号细则改指约定 · §4 编码纪律压成一句 + §三指针 · §5 新增「**文档类查询先查 catalog §三 + INDEX**」 · §5.2 实证压一行（详情已封存 roadmap 卷） · §6 悬停/浏览器/观感/巡检四条各压 30~50%（🔴 红线句原样保留）· 页首加 🔴 新规矩。**③配套**：约定 §十五 例外条款同步改（AGENTS 从"随时可改"改"仅特批"）；本条目。**④验证**：typecheck · content:check ✅ · docs:index --check ✅ · 编码三查（LF-only 0 / 乱码 0 / 无 BOM）。**【待验收 · 本地提交未推送】**
- 2026-09-15：**「目录索引」移出 `AGENTS.md` → 独立目录文档 `docs/catalog.md`（船长：「用目录文档替换 AGENTS.md 内的『目录索引』，AGENTS.md 指引你们去读目录文档」）**——**① AGENTS.md**：§0.1 由 13 行指路表 ＋ 8 行文档清单（2.6 KB）缩成 2 行指针，§0 第 2 步改为「**读 `docs/catalog.md`（文档目录）**」；AGENTS 19 KB → 17 KB。**② 新 `docs/catalog.md`**（手写指路目录，属"例外清单"可随时改）：权威文档清单 · 按任务类型指路表 · **检索与查证顺序**（先查词典 → 先查 `docs/INDEX.md` → 查历史先看封存卷索引 → 续接顺序）· 文档工作流速览；与机器生成的 `docs/INDEX.md` 的分工写进两份文件的头部。**③ 全仓 5 处「目录索引」引用改指 catalog.md**（`architecture.md` · `development-conventions.md` 三处 · changelog 头注）；历史条目（2026-09-10 可读性改造那条）按"历史条目不回改"未动。**④ `tools/docs-index.ts`**：`docs/catalog.md` 收进「一、权威文档」组，索引头部「怎么用」改指 catalog。**【待验收 · 本地提交未推送】**
- 2026-09-15：**新增第十五章「文档工作流：工作文档 → 归档」（船长定）＋ `AGENTS.md` §8 整段改写 ＋ 文档精简封存批**——**船长原话（照抄）**：「**工作流没有进行收尾归档事，不允许直接修改旧文档。应该新建一个该工作的临时文档记录工作相关。等到归档时，再合入旧文档。**」＋同日补充裁定「**归档后当前工作文档直接删除，只将关键内容归档。**」。**条文七点**：① 开工先建工作文档 `docs/design/<主题>-<YYYYMMDD>.md`（头部四行：`状态：进行中`（经办人·分支·日期）· 船长原话照抄 · 范围/不做 · 待裁决点）；② **工作期间只改工作文档**（＋代码/数据/测试）——不追加 `roadmap` 批次条目、不改 `glossary` 现行词条、不改 `architecture` 与既有设计稿；③ **归档触发** = 船长验收 ＋ 合入 main 之后**当批**做（一件工作一归档，不攒批）；④ **归档三步** = 关键内容**并入**该去的老文档（roadmap 一条精简条目 · 该更新的词典词条 · 必要时 architecture/本约定）→ **删除工作文档**（`git rm`、不留存档副本；过程细节以 `git log -S` 兜底 ⇒ 结论/数值必须已并入）→ 重跑 `npm run docs:index`；⑤ **例外清单**（允许随时改）：根 `AGENTS.md` · 本约定 · 本 changelog（规则类当场生效）· `docs/glossary.md` 的**新术语登记** · 生成件 `docs/INDEX.md` 与 `docs/archive/` 封存卷；⑥ 违规判据 = `git diff` 里"旧文档正文新增"却没有对应工作文档；⑦ **与既有条款的关系 = 只改"固化时点"**：`AGENTS.md` §8 旧话「里程碑**即**固化到文档」的"即"改指"归档时"，`docs/roadmap.md` 定位同时改为「**待办活面 ＋ 最近批次滚动窗口 ＋ 封存卷索引**」（封存机制 `npm run docs:seal`，规则见 `docs/archive/README.md`）。**背景与同批动作**：roadmap 曾涨到 **1523 KB / 824 行**（单条平均 1.9 KB）、词典 294 KB ⇒ 同日把 2026-09-04~09-15 的 **556 条**批次条目按日原文封存成 10 卷、41 个已办结待办项移出，roadmap **90 KB / 191 行（−94%）**；新增 `docs/archive/` 封存区（14 卷 + README 四条纪律）与 `docs/INDEX.md` 全仓文档索引（`npm run docs:index`，248 份）。**AGENTS.md 同步三处**：§0.1 目录索引新增一行「记一件工作 / 写文档」· 文档清单更新（roadmap 新定位 / INDEX / archive）· §8 由「会话续接与文档固化」改写为「文档工作流：工作文档 → 归档」。**验证**：零损失校验（旧 roadmap 822 行非空内容逐行复查 0 缺失、条目数守恒 556+20=576）· 编码三查（LF-only 0 / 乱码 0 / 无 BOM）· typecheck 四包 0 错 · core 146 文件 / 1564 例 · content:check ✅ · docs:index --check ✅。**【待验收 · 本地提交未推送】**
- 2026-09-15：**悬停提示统一（延迟 200→500ms · 三条路径同源 · "内层优先" · 原生提示整条祖先链压住）（船长三条报障 · 三号 · verify）**——**①船长原话（照抄）**：「**鼠标悬浮按钮的提示会和上一级的悬浮提示相互冲突。且悬浮的反应时间太快。建议所有 UI 统一下**」。**②改了什么（约定第九章「悬停提示」条 + `AGENTS.md` §6 同步改写）**：**(a) 延迟统一**——唯一常量 `TIP_DELAY_MS = 500`（`ui/Tooltip.tsx` 导出；**2026-09-13 定的 200ms 作废**），**三条路径全部共用**：`title`/`data-tip` 全局接管层 · **`hoverTipProps(content)`**（`ShipHover` / `InfoHover` 等富内容悬停，此前是 `onMouseEnter` **即时弹**）· `HoverTip` 组件（同样此前即时弹）⇒ 现在任何悬停说明都是"停够 500ms 才弹、移开即收"。**(b) 冲突根治一（内层优先）**：`hoverTipProps` 在 `mouseenter` 时从指针目标往上走到自身，若中途出现**更深的提示归属**（`title` / `data-tip` / 另一个 `hoverTipProps`）⇒ 本次不接、让给内层；接管层对带 `data-tip-hover` 的元素**不再重复接管**。**(c) 冲突根治二（原生提示整条祖先链压住）**：接管时把被接管元素的 `title` 写成**空串**（**不再 `removeAttribute`**——摘掉后浏览器会顺着祖先链找到上一级卡片那条 title 弹**系统默认**提示，这正是"相互冲突"的第二种成因），并**把祖先链上所有 `title` 一并置空**（记原值、离开逐个还原；React 若已写新值以新值为准）；`srcOf` 把 `title=""` 视为"没有 title"，继续从暂存里取真文本。**(d) 新守卫**：`content:check`「悬停提示契约」加一条——**同一 JSX 元素不许同时带 `title` 与 `{...hoverTipProps(…)}`**（两个提示归属抢同一个单例层；负向验证实测会红并点名文件/行/标签）。**③口径与取舍**：`showTip/moveTip/hideTip` 低层 API **保留导出**（特殊场景仍可用），但**组件层一律走 `hoverTipProps` / `HoverTip`**；500ms 是**一个数**，船长若要更快/更慢改这一处即可。**④已知边界（如实登记）**：浏览器原生提示**不可脚本化观测**（DOM 无接口），"祖先链压住"这条只能靠机制与人工观感确认，无法在无头探针里断言；其余三条（同延迟、内层优先、双归属守卫）都是静态可核的。
- 2026-09-14：**修虫洞面板两条（船长报障「战斗胜利后点击战报有时会退出虫洞界面」「前往下一层后上一层遗留的按钮未消失」· 二号 · d2）**。**①** 遗迹守备确认条那句 `onClose()` 与同文件既有设计条款「战斗中不渲染面板、`whOpen` 保持为真 ⇒ 打完自动回来」冲突 ⇒ 只有这一条开战路径打完回不到面板（另两条不关面板，故"有时"）⇒ 去掉该 `onClose()`。**②** `pendingCell`（未扫描格的"确认前往/取消"双按钮条）记着上一层 Q/R 且不随换层重置 ⇒ 深入后挂在新层头顶（同坐标另有格子）⇒ 换层 effect 里一并 `setPendingCell(null)`。**验证**：typecheck 四包 0 错 · core 145 文件 / 1,551 例 · content:check ✅ · ui:rot-check ✅ · 桌面构建 ✅（纯渲染层，引擎未改）。**【待验收 · 观感请船长实机复看】**
- 2026-09-14：**修洞内战斗视图：敌卡口径漏接（船长报障「敌方舰船动画不对 / 敌方动画图形和敌族对不上 / 战斗开始位置似乎不对」· 二号 · d2）**——根因 = **一类"洞内战读了远征口径"的漏接**（洞内宿主 `run.battle`、敌卡按层派生，都不在 `expedition` 上）⇒ 只认远征的读法在洞里静默取 `undefined` 而照样渲染。6 处：**`BattleScreen.foeAnomaly`（主犯：族形落兜底 A 海盗 ⇒ 图形与敌族对不上；舰种体积回落 170/90 且喂 `layout()` ⇒ 开局机位偏）**、死敌预登记（洞内退出再进敌人复活）、星场基准船速、`battleTacticDesire`（洞内返回 0 ⇒ 点战术=整队贴脸）、状态窗场景、公告自动弹。**修法**：新增 core 取值单点 `battleFoeAnomaly`（洞内派生卡 / 洞外远征卡逐字照旧）+ 其余五处改双口径；**护栏** = `content:check` 新增「战斗宿主双口径契约」（BattleScreen 的 `expedition.anomalyId` 必须 0 处等三条）。**验证**：core 用例 +2（含两条负向验证）· 五道闸门全绿（core 145 文件 / 1,551 例）。**文档**：`docs/design/wormhole-battle-view-20260914.md`。**【待验收 · 图形与观感请船长实机复看】**
- 2026-09-14：**洞内产出链「只收不卖」＋ 虚空晶产出半量（船长 · 二号 · d2）**——船长原话：「**虚空晶和虚空母矿也添加只收不卖。**」「**市场不会出现虚空晶和母矿的卖单。**」「**然后精炼炉，虚空晶的产出量下调到一半**」「**价格不动。**」。**落法**：两条市场行 `playerBuyable: false`；虚空母矿精炼的虚空晶 `perOre` **0.5 → 0.25**（副产物与价格不动）。**同批修一处引擎真缺陷**：`market.seedCommonBook` 原无条件铺 NPC 卖单 ⇒ 只收不卖商品也挂着买不了的卖单（`box-relic-a` 实测 2 件）⇒ 按 `playerBuyable` 收口（收购侧不受影响）⇒ NPC 侧再无任何渠道给出这两样的卖单。**读数**：每单位母矿卖原矿 780 vs 炼成三件套 1,016 → **566** ⇒ 由"炼更赚"翻成**炼不如卖**（"更稀缺"的代价，已在设计稿登记）。**护栏**：`content:check` 洞内非商品契约 +④；新用例 `tests/void-chain.test.ts` 4 例（含簿面/端到端）；负向验证两条各自精准。**验证**：core 145 文件 / 1,548 例 · 五道闸门全绿。**文档**：`docs/design/void-chain-20260914.md` · 词典产出条目改写。**玩家侧挂卖 = 船长裁定「保持现状」**（NPC 侧零卖单；玩家挂卖照旧，不动既有「所有物品允许玩家挂卖」定案）。**【待验收 · 本地提交未推送】**
- 2026-09-14：**手册 · 图鉴详情加「↖ 查看市场」（船长 · 二号 · d2）**——船长原话：「**玩家查看图鉴内的道具时，添加一个跳转市场的按钮。**」＋「按你推荐来」＋「可以」。**行为**：点它 = 关手册 + 切市场页 + 一次性聚焦该商品（行情详情自动展开），**只跳转、不下单**；入口复用 App 的 `onGotoMarket`（全仓一处口径）。**范围**：物品 / 装备 / 舰船 / 蓝图四类图鉴详情、浮层头部右侧、「↖ 查看市场」；**无市场行不渲染按钮**；蓝图跳蓝图自己。**单一实现**：映射单点 `ui/marketJump.ts` 的 `handMarketKeyOf`（单独成文件 = 体检要跨层调它，而 `Handbook.tsx` 带 `@whale/ui` 的 CSS、node 侧加载不了）。**护栏**：`content:check` 新增「图鉴市场跳转契约」（实测 505 条 ⇒ 可跳 439 · unreleased 闸 27 · 残骸碎片 37 · 例外 2）+ 反向判据；**负向验证**：把 items 映射写错 ⇒ 点名 46 条。**验证**：core 144 文件 / 1,537 例 · 五道闸门全绿。**文档**：`docs/design/handbook-market-jump-20260914.md` · 词典新条。**【待验收 · 本地提交未推送】**
- 2026-09-14：**新装备「跃迁计算机」MK2/MK3（低槽支援件 · 跃迁速度 +20%/+35% · 多装递减）（船长 · 二号 · d2）**——船长原话：「**添加低槽装备，跃迁稳定器（只有MK2和MK3），效果是提高玩家舰船的跃迁速度，分别提高20%和35%，多件叠加惩罚。基础价格可以按照同级装备价格的四倍，有蓝图。**」＋「**④去掉上限。其他按你推荐来。名字可以改为跃迁计算机。**」。**落法**：新字段 `ModuleDef.warpSpeedBonusPct`（支援件第六类效果）→ 生效处 `travel.warpSpeedAus`（**星系际航行唯一入口**，逐船）⇒ 有效跃迁速度 = 船表值 × 装备倍率，与航行技能乘算；**不碰战斗机动**。**多装递减 = EVE 曲线**：MK2 ×1.20/×1.41/×1.57/×1.66、MK3 ×1.35/×1.76/×2.11/×2.32。**价 = 低槽支援件同档 ×4**（192.4 万 / 944 万）＋ 两张蓝图（481 万 / 2,832 万）＋ 市场 4 行 ＋ `rarityTier` 3。**同批删上限**：`warpSpeedAus` 的 12 AU/s 上限删除（下限 0.5 保留）——快船装 2 件 MK3 即撞顶。**界面**：装配页显示有效跃迁速度 + 新增「跃迁速度（含装备）」行；图鉴仍是档案值。**验证**：新用例 `tests/warp-computer.test.ts` 9 例（含零变化守卫与删上限）+ 负向验证（关掉加成 ⇒ 正好 6 条红）· core 144 文件 / 1,533 例 · 五道闸门全绿。**文档**：`docs/design/warp-computer-20260914.md` · 词典新条 + 两条补注。**【待验收 · 本地提交未推送】**
- 2026-09-14：**回滚 C 族敌人射程 +1000（船长「可以回滚C族敌人射程增加的修改」· 二号 · d2）**——船长原话：「**可以回滚C族敌人射程增加的修改。**」。**范围 = 当日那批全撤**：四舰级带回 2552/2655/2655/2713 · 卡覆写回 2600/2713 · **四张卡的 `desireRangeM` 钉子（511/521/532/543）全部撤回**。**为什么撤钉不动交距**：钉子值本就是回滚后带的推导值（`1 + 0.20×(带max−1)`）⇒ 插值重新推出同样的数，期望交距零变化。**校准 A/B**：128 格中 16 格变化（全在 C 族四卡 + 虫洞「巢群游猎」）——远中距装配变轻松（星髓 A0 残血 77.3%→91.2%、巢群游猎 A0 62.1%→94.1%、星髓 A1 胜率 80%→100%），但**近距 A3 反而更疼**（开局随"最远武器"缩短约 1,000 m：裂谷 100%→80%/23→58s）⇒ 回滚不是单向变简单；噬口四行仍全 0%。**顺带补账**：词典「敌冲锋」与「敌编队接近速度」两条还停在冲锋改判前的旧口径（×2 / `max` 补丁 / 冷却 20 秒 / 编队级），已按现行口径（逐单位 · 巨兽 3 / 小虫 1.5 · 自身命中解除 + 到达兜底 · 冷却 10 秒）重写。**验证**：core 143 文件 / 1,522 例 · 五道闸门全绿。**文档**：`foe-c-range-20260914.md` 头部标 ⛔ 已回滚 + §八 回滚记录。**【待验收 · 本地提交未推送】**

- 2026-09-14：**敌冲锋改判：逐单位 · 自身炮台命中解除 · 倍率 3 / 1.5 · 冷却 10 秒（船长报障 + 四条改判 · 二号 · d2）**——船长原话：「**发现BUG了，冲锋到达目标距离后并不会解除。建议将大虫子的冲锋倍率改为3，给小虫子添加冲锋，倍率为1.5.冲锋解除机制为自身攻击命中后解除冲锋状态，并进入10秒冷却**」＋「各自触发冲锋的提速」＋（问「命中解除冲锋现在是按各自命中吗」→ 答"现状是编队级一个标志"）＋「**确认**」。**BUG 根因**：解除条件当时只有「压到期望交距」，而"我方更快且期望交距更远"时距离停在拔河平衡点（`myDesire`）⇒ `arrived` 永不可达 ⇒ 冲锋永不解除。**改法**：解除加「**自身炮台判定命中我方**」（机群/近防炮不算）＋保留到达兜底 · 冷却 20→**10 秒** · 全局倍率 4.0→**3.0** 并新增**舰级覆写**（噬口 3 / 三种小虫 1.5 且新开资格）· 状态**编队级→逐单位**（`BattleState.foeCharges[tag]`）· 接近速度改**逐单位乘各自倍率取平均**（删 2026-09-11 的 `max` 补丁）· 抽出单点 `unitSpeedMulOf` 合并推进器/冲锋的乘子层（状态机与时钟相位各自留在原处）。**校准 A/B**：128 格中 20 格变化（全在 C 族四卡 + 虫洞「巢群游猎」）——小虫会冲后交距压近 1,000~1,500 m、承伤上升（星髓虫群 A3 残血 66%→37.1%），噬口猎杀令四行仍全 0%。**验证**：`thruster-charge.test.ts` 19 例（含 BUG 修复 / 逐单位 / 倍率平均三条新证据与反证）· core 143 文件 1,516 例 · 五道闸门全绿。**文档**：`docs/design/foe-charge-20260914.md`（`foe-c-range-20260914.md` §七 的 ×4 已标取代）。**【待验收 · 本地提交未推送】**

- 2026-09-14：**冲锋倍率 ×2 → ×4（船长试验 · 二号 · d2）**——船长原话：「**还有，我感觉大虫子的冲锋似乎没有效果，将效果改为4倍我测试下**」。改动 = `balance.battle.foeChargeMul` 2.0 → **4.0**（触发条件/冷却/门槛不动）；`combat.ts` 与 `thruster-charge.test.ts` 的注释与参数口径同步。⚠ **"没效果"的直接原因已查明（×4 不改这一点）**：触发条件是「距离 > 冲锋者自己射程」，而虫子本批刚 +1000 射程 ⇒ 噬口 **3,713 m**，平时 543~1,400 m 交距下**永远不触发**。**同日船长选「乙」**：`balance.battle.foeChargeTriggerMarginM = 1,000`（距离 > 期望交距 + 1000 就冲 ⇒ 噬口 **1,543 m** 起冲，与"够不着"取或）；丙备选未用。用例含反证（同距离把余量调到极大 ⇒ 不冲）。读数：巨兽冲锋期 297 → **1,188 m/s**。验证：`thruster-charge.test.ts` 10 例过 · 五道闸门全绿。文档：`docs/design/foe-c-range-20260914.md` §七。**【待验收 · 本地提交未推送】**

- 2026-09-14：**C 族（虫子）全族射程 +1000 · 目标距离不变（船长 · 二号 · d2）**——船长原话：「**给所有虫子添加1000的射程，但是目标距离不变**」；两问两答：只敌方 C 族 4 舰级 + 卡覆写同幅 +1000 · 只钉期望交距（开战距离允许变）。**落法**：四舰级带 → 3552/3655/3655/3713；卡覆写 → 3600/3713；**每张 C 族卡的每个条目写 `desireRangeM`（511/521/532/543）**——用的是既有的"期望距离覆写优先"机制（先例 = 2026-09-11 E 族「战术调整、期望距离不改」）。**效果**：远距离命中/威力折减同步提高（1380 m 命中 58.8%→65.4%；2713 m 35.5%→48.3%、DPS +72%），"拉远就免疫"的安全区外推 1000 m；**冲锋门槛随之推远**（已报备）。**校准 A/B**：A2 最远档零变化，近中档普遍多挨打，噬口猎杀令变化最大（A0 80%→0%）。**验证**：`foe-ship-path.test.ts` 同步 7 处 + 新增 1 条（52 例过）· 五道闸门全绿。**文档**：`docs/design/foe-c-range-20260914.md`。**【待验收 · 本地提交未推送】**

- 2026-09-14：**修「装配页卡片没有说明」＋ 新增体检契约「装备卡片说明契约」（船长报障 · 二号 · d2）**——船长原话：「**护盾充能装置，在装配时候的卡片上没有说明**」。根因：卡片正文由渲染层 `moduleShortEffect` **按 slot 分支**拼串，`shield` 分支只认盾容/盾抗，而**护盾充能装置只有 `shieldPulsePct`** ⇒ 空串；同批查出**打捞器 ×3 也没有分支**。改法：`shield` 补「每 30 秒回盾 X%（按满盾）」（秒数取 `SHIELD_PULSE_MS` 单点）· 新增 `salvager` 分支「每 N 秒 1 具残骸」· 打捞周期抽单点（详情面板与短行同源）。**护栏**：`content:check` 新增「装备卡片说明契约」（遍历 MODULES 断言短效说明非空）——⚠ 该契约跨层 import 渲染层函数（desktop 包无测试运行器）；负向验证：撤掉修复 ⇒ 契约点名正好 6 件。**验证**：五道闸门全绿。【待验收 · 本地提交未推送】

- 2026-09-14：**官方 T3 槽位对齐两艘（船长「2.鹦鹉螺+1槽位，牛鲨-1槽位。」· 二号 · d2）**——落格（二问二答）：**鹦鹉螺级测绘巡洋舰 中槽 +1 → 4/4/3（11 槽）**（与同子分类「侦察舰」的幽影侦察舰「中槽最多」同形）· **牛鲨级突击巡洋舰 中槽 −1 → 5/2/4（11 槽）**（保住「厚甲重炮」那一排低槽）；两形都过武装舰契约「高槽 ≥ 低槽+1」。**护栏**：`content:check` 新增 **`OFFICIAL_SLOT_ALIGNED` 名单契约**（已点名对齐的官方船必须**恰好等于**该档默认；名单外不校验——还有 14 艘偏离，船长逐艘点名）。**用例**：`wh-ship-baseline.test.ts` +1 · `scout-cruiser.test.ts` 的舰体定案 10 槽(4/3/3) → **11 槽(4/4/3)**（船长原话照抄不动）。**⚠ 同批更正一处我自己的记数错误（如实登记）**：上一批写「官方 23 艘里 19 艘不在线上」，**实际 16 艘**（在线上 7 艘）⇒ 设计稿 §4.9 / roadmap / 本条均就地改正。**验证**：五道闸门全绿（含第五道 `ui:rot-check`）。**【待验收 · 本地提交未推送】**

- 2026-09-14：**装配方案（预设）：保存当前装配 / 套用预设（船长 · 二号 · d2）**——**①船长原话（照抄）**：「**装配界面，添加一个保存当前装配和使用预设装配的功能。**」；集中提问四问四答（全取推荐档）：**按船型归口** · **每型 3 套** · **存槽位装备 + 无人机舱装载** · **尽力装 + 逐条提示** · **先卸光再装**；入口追加裁定：「**在标题栏下面的'装配目标'栏的右侧**」。**②落码（core 新模块 + 渲染层 3 文件）**：新增 `packages/core/src/fitPresets.ts`（`saveFitPreset` / `renameFitPreset` / `deleteFitPreset` / `applyFitPreset` / `unfitAllModules` / `clearDroneLoad` / `fitPresetsOf` / `fitPresetBrief`，口径写在头注释）· `state.ts` 加 `ShipFitPreset` 与 `GameState.fitPresets?` · `save.ts` 加清洗（每型 ≤3 · 名称去空白限长 12 · 位数组裁尾 ≤7 · 无人机正整数 · 全空丢弃 · 删空清字段）· `index.ts` 再导出 · `engine.ts` 五个包装（`saveFitPresetFor` / `applyFitPresetAt` / `renameFitPresetAt` / `deleteFitPresetAt` / `unfitAllFor`）· `FitPage.tsx`（「装配目标」栏右侧两按钮 + 方案浮层 + 一键卸下）· `styles.css`（`app-fit-preset-*`，复刻同级语言）。**③零迁移（重要口径）**：**不升版本（保持 v25）、不写迁移函数**——与同日 `shipStore` / `wormholeScan` 同款"可选兼容字段"落法；归一化**只在真有方案时落键**（无条件写空表会让老档往返多一个键 ⇒ 快照用例红，`bountySeenWindow` 的教训）。**④装配顺序**：**扩容件 → 高 → 中 → 低 → 无人机**（协处理器给 CPU 预算，先装才不会"假超载"）；逐件走 `fitModule` 四道校验 ⇒ 中间态永不超载。**⑤锁判定位置**：放在 `unfitAllModules` **函数内**（装配页「一键卸下全部装备」直接调它，不能只靠套用入口判定）——进洞/自动探索中的船三连拒。**⑥用例**：`tests/fit-presets.test.ts` **11 例**（保存/裁尾/摘要 · 3 套上限 + 同名覆盖 + 改名 + 删除 · 整船换装 · 缺件部分应用 · CPU 超载清单 · 方案比船长 · 无人机不足 · 进洞锁定 · 一键卸下 · 存档往返/缺省不落键/清洗层 · 清洗规则）。**⑦负向验证两处（各自精准）**：a) 归一化改无条件写键 ⇒ 只有「存档」用例红（`expected {} to be undefined`）；b) 拿掉一键卸下的锁判定 ⇒ 只有「锁定」用例红（`expected true to be false`）。⚠ **过程如实登记**：第一版「不落键」断言**咬不住清洗层**（`serializeSaveFile` 走内存态、不经过 `normalizeState`），靠负向验证发现后补了直压清洗层的断言。**⑧验证**：typecheck 四包 0 错 · core 全量用例全绿 · `content:check` ✅ · 桌面构建 ✅。**⑨文档**：新设计稿 `docs/design/fit-presets-20260914.md` · `v18-slots.md` 与 `v18.1-support.md` 的「不做 preset」标注改为**已开工**（从"后续候选"移入本期）· 词典新增「装配方案（预设）」词条 · roadmap 一条。**⑩待船长过目**：入口在既有那一行内（该行本就 `flex-wrap`，窄窗可能多折一行）、浮层为 overlay 不占页高——**观感/几何请你判**（我没截图）。【待验收 · 本地提交未推送】
- 2026-09-14：**虫洞专属舰船：鱼雷舰强化 ＋ 槽位基准线核账（船长拍板「可以」· 二号 · d2）**——**①船长原话（照抄）**：「**鱼雷舰添加额外20火力加成，高槽数比较多。因此亡军鱼雷舰需要挪一个中槽到高槽**」＋（追加代价）「**亡军命中率-0.1和回避率-0.1**」＋（一问一答）「**构件+25.亡军+20**」；槽位基准线原话：「**默认的舰船，按级别分别是 7/9/11/14/18 个槽位。种族专属的会在这个基础上 +1 槽位**」＋「**是基准线，也检查下官方船**」。**②鱼雷舰强化**：构件 `powerBonus` 0.25→**0.50**（+25 点，槽位不动）；亡军 0.62→**0.82**（+20 点）＋ `hitBonus` 0.21→**0.11**、`evasion` 0.105→**0.005**（各 −0.10）＋ 槽位 5/5/3→**6/4/2**（中槽→高槽、总数守 12）。**③槽位基准线核账（专属 = 默认 +1 ⇒ 8/10/12）**：15 艘里 5 艘不在线上，全部修正——掠袭电子舰 3/3/1→**3/4/1**（补 1）· 机库无人机作战舰 4/4/3→**4/3/3** · 亡军后勤舰 4/4/3→**4/4/2** · 巨构无人机作战舰 5/5/3→**5/4/3** · 亡军鱼雷舰→**6/4/2**；根因 = 09-13 那批「每艘 +1」相对**各舰创建值**算，而创建值未对齐默认线。**④官方船核对（已交船长、待另裁）**：官方 23 艘里 **16 艘不在线上**（原写 19 是我记数错，2026-09-14 已更正）（工业/货运系统性偏低：飞鱼 5 / 旗鱼 7 / 剑鱼 7 / 蝠鲼 8 / 皇带鱼 8 / 座头鲸 8）。**⑤护栏**：`content:check` 新增契约「专属舰槽位总数 = 该档默认 +1」（`TIER_SLOT_BASE` 表 + 逐舰断言）。**⑥验证**：typecheck 四包 0 错 · core 全量用例全绿（新增 `wh-ship-baseline.test.ts` 7 例）· `content:check` ✅ · 桌面构建 ✅；负向验证精准（亡军临时改回 5/5/3 ⇒ 用例 3 条红 + 契约点名红）。**⑦文档**：设计稿新增 **§4.9**（含改判表、实测读数、取舍与官方船待裁）。【待验收 · 本地提交未推送】
- 2026-09-14：**新增第五道闸门 `npm run ui:rot-check`（旋转口径体检）＋「旋转口径」立为硬口径（船长定 · 一号 · main）**——起因：船长连报两起手机竖屏缺陷（① 返回虫洞黑屏 ② 讯息界面只能查看最新的讯息 / 侧边栏选项超出屏幕），根因同一个：手机竖屏自动横屏（`.app-root.is-mobile-rot`）后界面活在**逻辑空间**（宽 ≈1200 · 高 = `--mob-h` ≈ 554），而**媒体查询与 `vw`/`vh` 量的都是物理竖屏**（~390×844）⇒ 覆盖层被挤窄/撑高、`@media (max-width: 1180/1023px)` 那两套窄窗口排版在旋转下照样命中（通讯页曾被改成单列横排，而收件箱是倒序 ⇒ 只看得到最新几封）。**①约定 §验证闭环**新增一条（`content:check` 与 **`ui:rot-check`**，后者标注「2026-09-14 起为第五道闸门」）；**§九**「表现的观感审查权在船长」那句里的「四闸门」改成「**五闸门（typecheck / 测试 / content:check / 旋转口径 / 构建）**」。**②`AGENTS.md` §3** 同步同一行（口径：UI/样式改动必跑 `ui:rot-check`——覆盖层尺寸必须按逻辑空间**同轴**覆盖、窄窗媒体查询在旋转下不许误触发、侧栏让位只许收内边距；只读 `styles.css`、不用浏览器）。**③体检本体** = `tools/ui-rot-check.ts`（已挂 npm script）四条契约：**物理单位契约** / **轴不许搞反** / **窄窗白名单** / **侧栏让位顺序**；首跑读数：旋转块规则 63 条 · 覆盖类 56 个 · 受约束声明 34 处 · 窄窗白名单 4/4 · 导航项受控规则 1 条；**负向验证两轮**（改坏→点名报错→还原→全绿，共 5 处；含"类名加后缀不算改"那条无效用例的如实记录）。**④同批落码（`styles.css`）**：通讯页旋转时恢复桌面双列 · 8 处覆盖层高度按 `--mob-h` 封顶 · 手册弹窗与虫洞弹层同口径 · 离线简报卡与市场左列目录按逻辑空间取值 · 侧栏让位顺序（出港唯一可压 + 导航项只收上下内边距）· 虫洞弹层内部按桌面款还原 · **`@media (max-height: 730px)` 桌面矮窗口同口径**（船长当日追加裁定「桌面矮窗口也要，与手机同口径」）。**⑤词典**新增「**旋转口径（手机竖屏自动横屏）**」条。五闸门全绿（typecheck 四包 0 错 · core 141 文件 / 1479 用例 · `content:check` ✅ · `ui:rot-check` ✅ · 桌面＋网页 build ✅）。

- 2026-09-14（同日修正）：**「Markdown 记号不得进玩家文案」不写进约定与 AGENTS.md —— 只保留体检契约（船长裁定 · 三号 · verify）**——经过：当天先按「船长问『为什么有些说明文字有 ** 文字**』」立了规（约定 §十三 补一条 ＋ `AGENTS.md` §5 同步 ＋ 体检新增「文案纯净契约」）。随后船长复核时明确：**「B 可以不用写入规则」** ⇒ ①约定 §十三 里那 6 行**已删**；②`AGENTS.md` §5 里那 1 行**已删**；③**体检的「文案纯净契约」保留**（它只是防线，不派活、不加条款），其报错文案里的「详见约定 §十三」也一并改成自解释（写明"游戏里是纯文本渲染、记号会原样显示给玩家，请去掉"）。行为面零变化：当天清掉的 77 处（＋合并时拦下的一号新代码 1 处）照旧保留。**同时保留**同日加的「模板串契约」（它自始就没写进任何条款文档）。
- 2026-09-14：**§十三 补「Markdown 记号不得进玩家文案」＋ 新增体检「文案纯净契约」（船长问「为什么有些说明文字有 ** 文字**」· 三号 · verify）**——起因：船长在界面上看到原样的强调记号。查实：游戏文案全是**纯文本渲染**（无 Markdown 解析器），而开发文档习惯用强调记号写重点，一旦写进字符串就照原样显示。全仓 AST 扫出 **77 处**（`packages/data/src/modules.ts` 35 处模块说明 · 虫洞界面/洞内日志 20 余处 · `ships.ts` 4 · `skills.ts` 3 · 手册 3 · 通讯正文 1 等），用一次性 codemod **只改文本节点**（字符串/模板串文本块/JSX 文本）清掉，**注释里的强调记号一处未动**（本仓大量注释就这么写）。同时立规＋加硬契约：`content:check` 的**「文案纯净契约」**与「模板串契约」共用一遍 AST 遍历，命中即红并给出 `文件:行`；**负向验证**：重新塞一个记号 ⇒ 体检立刻报红（`WormholeScan.tsx:78`），还原即绿。四闸门全绿。
- 2026-09-14：**§九「悬停提示」补 SVG 口径：HTML 用 `title` · SVG 用 `data-tip`；禁止 SVG `<title>` 子元素（船长报障「部分情况仍会出现系统默认的鼠标悬浮 title 窗口」· 三号 · verify）**——起因：船长报"部分情况下还会冒出系统默认的提示框"。实测定位**两类漏网**：①**SVG 的 `<title>` 子元素**不是属性、接管层 `closest('[title]')` 看不见 ⇒ 浏览器按 SVG 标准照弹原生提示（全仓 2 处：星图航线时长 `panels/Expedition.tsx`、虫洞格子 `panels/Wormhole.tsx`）；②**悬停期间才写入 `title`**（进入时还没有、React 随后写进来）原先没人管（观察器只在"已摘过 title"之后才挂）。另实测**排除**常见嫌疑"禁用控件不派发事件"：当前 Chromium 对 `disabled` 按钮的 mouse/pointer 事件都照打、接管正常。改动：①`ui/Tooltip.tsx` 接管层——新增 **`data-tip`**（SVG 作者属性；React 的 SVG 类型**不接受** `title`，这正是历史上写成 `<title>` 子元素的原因）、纳入 SVG `<title>` 子元素兜底（命中就**清空其文本**、离开放回；只清文本不摘元素，避免 React 卸载时 `removeChild` 抛错）、观察器**提前到"进入即挂"**（覆盖后到的 title）、并修掉一个真回归：浏览器按"外层→内层"顺序派发 `mouseover`，记录"指针所在元素"时不能因"新目标是旧目标的子元素"就早退（否则 `hovered` 卡在最外层容器上、内层永不接管）；②作者层两处 `<title>` 子元素改 `data-tip`；③体检新增**「悬停提示契约」**静态守卫（渲染层出现 `<title` 即红）；④§九 该条与 `AGENTS.md` §6 同步。验证（**真产物** `web/dist` ＋ 无头 Chromium 真鼠标事件 · 读数）：HTML 按钮 ⇒ `.app-tip` 弹出、`title` 摘走、离开原样放回；SVG `data-tip` 夹具 ⇒ 自绘提示、作者属性保留；SVG `<title>` 子元素夹具 ⇒ 自绘提示且 `<title>` 文本被清空、离开恢复。四闸门全绿。
- 2026-09-13：**§一「中文优先」修订：货币单位从"单位例外"里移出，玩家可见文案一律写「信用点」（船长定 · 二号 · d2）**——
  起因是船长的两条指令：「**更换金钱单位为信用点**」＋「**用 ISK 容易出事**」；集中提问后船长选定
  「**①全改**（玩家可见文案统统改）②**乙：只改玩家可见文案 + 注释，字段名不动**」。
  **落码**：① **显示单点** = `packages/core/src/money.ts` 的 `MONEY_UNIT` / `moneyText` / `moneyExactText` /
  `moneyDelta`（放 core 而非渲染层：**desktop 包没有测试运行器**，放渲染层等于没有回归门禁）；
  ② **全仓 128 处 ISK 单位用法 → 信用点**（25 个文件；判据 = 两侧都不是字母/数字/下划线的 ISK，
  实测**零误伤**：`priceIsk` 384 处 · `wallet.isk` 264 处 · `escrowIsk` 38 处 · 触发器枚举
  `insufficient-isk` 7 处 · `kind` 取 `isk` 的触发器 10 处全部原样；`isc` / `risk` 等小写词天然不受影响）；
  ③ **位数分级**（船长「希望考虑到钱位数过多时的处理」）：`1 万`以下千分位全写 · 到万写「万」（1 位小数）·
  到亿写「亿」（2 位小数，整数部分照样千分位）· **精确值一律挂 `title`**（悬停见全精度）·
  负数取绝对值分级 · `NaN` / `Infinity` 兜底显示 0；
  ④ **本条的"单位例外"改写**：原文允许"时间/货币等常见单位（ISK、秒/分/时、m³）直接用英文"
  ⇒ 现把**货币**移出（时间与 m³ 照旧），并注明引擎内部字段名仍叫 `isk`。守卫 = `tests/money.test.ts`（8 例）。
  词典同步 = `docs/glossary.md` §三 新增「**信用点**」条。

- 2026-09-13：**新增 §九 一条：整页内容有最大宽度 `--app-max-w`（船长定 · 一号 · main）**——
  起因是船长转述玩家反馈「**有玩家反应，当屏幕偏长时，游戏界面过宽。给游戏界面的内容加个最大宽度。
  当窗口超出这个宽度时，在左右填充空白（大概是这样）**」。做法：顶栏 `.app-header` 与工作区
  `.app-workspace` 各自 `width:100% + box-sizing:border-box + max-width:var(--app-max-w)` + 左右 `auto`
  ⇒ 超宽时整块居中、两侧露出宇宙底图；不超宽时逐像素不变。**两个坑一并写进条款**：
  ① 只写 `max-width` + `margin:auto` 会被纵向 flex 的 auto 外边距取消 stretch（顶栏缩成内容宽）；
  ② `width:100%` 不带 `box-sizing:border-box` 会变成"100% + padding"⇒ 挤出横向滚动条。
  实测：1600 `x=0·w=1600` · 2560 `x=400·w=1760` · 3440 `x=840·w=1760`，三档无滚动条。
  同时固化**网页版启动入口同调 `applySpaceBg()`**（此前只有桌面入口调，网页版没有全站星云底图）。

- 2026-09-13：**新增 §九 一条：悬停提示一律走自绘接管层（一号固化 · main）**——
  起因是船长报障「**当玩家鼠标悬停在活动界面的工业AI标签上过久时，还是会显示旧的title**」：
  那枚徽标的 `title` 是**实时读数**（活动条数 ＋ 每条剩多久/百分比），引擎每拍重算 ⇒ React 把
  **新的 `title` 写回元素**；而接管层只在悬停 200ms 时摘一次 `title`，摘完就不再管 ⇒
  写回来的 `title` 让浏览器**原生提示**与自绘提示**同时显示**（扫过不现、停久必现）。
  **修法**（`ui/Tooltip.tsx`）：展示期间挂只盯 `title` 属性的 `MutationObserver` → 再摘一次，
  并把提示内容刷成新值。
  **条款**（写入 `development-conventions.md` §九）：① 玩家可见的悬停说明**只写 `title`**，
  统一由 `ui/Tooltip.tsx` 接管，**不许**自造悬停/浮层机制；② **实时读数的 `title` 照常只写 `title`**
  （该层已自动重摘，新增无需特殊处理），**不要**在展示期间直接操作提示层 DOM。

- 2026-09-13：**🔴 新增底层规则：表现的观感审查权在船长（船长定 · 一号 · main）**——
  **船长原话**：「**还有为什么你会试图截图来自行判断？不应该实现后由我审查吗**」。
  **条款**（写入 `development-conventions.md` §九 与 `AGENTS.md` §6）：UI / 观感类改动**实现 + 四闸门跑完就直接交船长审查**，
  **不许自己起无头浏览器截图、再把截图当"已经改好了"的依据**——好不好看/像不像/够不够醒目只有船长能判，
  截图只能判"有没有渲出来"；而且起无头浏览器要占机器、抢端口。
  **例外只有两类且能少则少**：① **读数型问题**（溢出不溢出 / 有没有滚动条 / 跨窗口尺寸几何 / 性能帧数这类
  "光看代码判不了"的事实）；② **船长明确要求**。要跑探针就先看代码能不能判、能判就不跑；
  要跑就挂后台任务，并在汇报里写明"这是读数、不是观感结论"。

- 2026-09-13：**🔴 新增底层规则：一律不许关闭浏览器进程（船长定 · 一号 · main）**——
  **船长原话**：「**给我将不允许直接关闭浏览器进程写入底层规则，我的浏览器又被你们关掉了**」。
  **条款**（写入 `development-conventions.md` §十 与 `AGENTS.md` §6，**取代**此前那条"无头浏览器只能按命令行特征点名关"）：
  ① **禁止**以任何形式结束浏览器进程——`Stop-Process`/`taskkill` 打在 chrome/msedge/firefox/brave **任何**浏览器进程上都不行，
  **"按进程名批量杀"与"按命令行特征点名"同样禁止**（`Get-CimInstance … Where CommandLine -match '--remote-debugging-port' … | Stop-Process`
  这类写法同样禁止）；② 也不许对"不确定是不是自己起的"实例发 CDP `Browser.close`；
  ③ **正确做法**：优先把实例**挂后台任务**（`run_in_background`）并用 `job_kill` 收尾（进程树归 harness 管）；
  自己 `Start-Process` 就**留住 PID**、只对这一个 PID 收尾（先核对启动时间）；
  拿不准/PID 丢了 ⇒ **就让它跑着**并在汇报里写明"还留着无头实例（端口 N）"，由船长处置；
  ④ 探针端口一律用**非默认端口**并先探占用（占用就换）——否则自己的实例静默失败、探针会连到**别人的**浏览器上。
  **判据一句话**：**宁可留个进程，也不许误关船长的浏览器。**

- 2026-09-13：**新增一条"经办人作业纪律"：后台任务巡检（船长口头定 · 一号 · main）**——
  **后台任务巡检**：任何后台任务（无头探针 / 长跑工具 / 构建 / 预览服务）**每 2 分钟没有新输出就主动查一次**
  （读日志尾、看进程在不在、看是否卡在等待），不许"挂上去就不管、等超时才回头"；探针日志一律落
  `tools/_ui-artifacts/*.log` 便于随时看尾。写进 `AGENTS.md` §6（UI 与工具纪律）。
  （⚠ 同日稍早那条"无头浏览器只能按命令行特征点名关"已由本节最上方的新规则**取代并作废**：
  现在**一律不许关闭浏览器进程**。）

- 2026-09-13：**三条"待落机制"全部落码 + 孢子巢补偿 + 旧重装舰价格跟涨（船长拍板 · 二号）**——**船长令**：「1，可以补到0.6。2.10架。3.先不动。4.跟着涨。做完后将还没落地的机制挨个落地」。**落码**：① **C 孢子导弹巢「对所有敌方同时攻击」**：新字段 ModuleDef.hitsAllFoes → WeaponSpec.allFoes；开火路径**逐目标独立掷命中 + 各吃各自的层克制与抗性**（副目标**不吃锁定加深**，基数用 dmg）；**胜率预估器同步**（steadyPreview 里 ×在场敌舰数）；演出层逐目标推弹道。**实测**：同一波 3 艘敌舰同时掉血（0/156/72 · 0/136/72 · 30/158/72）⇒ 非"打死一艘其余满血"。② **G 残兵结构层 → 鱿蜂结构层 · 无人机结构 +80%**：新字段 ModuleDef.droneHullHpBonusPct: 0.8 → 聚合到 UnitSpec.droneHullBonusPct → **两处建池**（单船/编队）把该舰机群生存池的**结构层** ×1.8。③ **「所有流亡蜂换成鱿蜂」**：全仓 **25 个文件**改名（**id 一律不动**）；⚠ **nnouncements.ts 已发布公告那 1 处按船长「先不动」保留**。④ **孢子导弹巢命中 0.5 → 0.6**（机制落地前的补偿，说明文案同步）。⑤ **旧重装舰价格跟涨**：陆龟 330k → **450k**、玳瑁 760k → **1.1M**，市场行同步、船图纸按档位系数（825k→**1.125M**、1.9M→**3.3M**）+ 材料同比放大。**验证**：typecheck ×4 · core **1232 用例** · content:check · desktop build 全绿。【待验收 · 待发布】
- 2026-09-13：**删除 V18「装甲舰低槽多」弱断言 + 两艘虫洞巡洋舰槽位点名（船长拍板 · 二号）**——**船长原话**：「陵寝巡洋舰中槽和低槽各-1。巢群重型突击巡洋舰高中低槽各-1。CPU回滚」＋（问出处后）「删掉以后按乙」。**合同删除**：	ools/content-check.ts 的 
ole==='armored' ⇒ low >= high + 1 **整条删除**（保留 rmed ⇒ high >= low + 1 半条，现行 42 艘全通过）。**溯源（回答船长「这是什么规则、什么时候的」）**：该断言 **2026-09-04 随 V18A 槽位落库**（提交 68bb806f「合入 V18A 槽位（大鲸鱼二号）」），依据是 docs/design/v18-slots.md **§四布局草案表**里三艘重装舰的低槽偏多形态（陆龟 2/2/3＝7「甲厚（低槽多）」、玳瑁 2/3/3＝8、玄武 2/3/4＝9）——**属"草案精神"的提炼，不是独立裁定**；且草案数值早被后续平衡批突破（玄武现为 4/4/6＝14）。**落码**：陵寝巡洋舰 槽 **4/5/5 → 4/4/4**（12 槽）· CPU **保持 300**；巢群重型突击巡洋舰 槽 **5/4/6 → 4/3/5**（12 槽）· CPU **保持 280**（船长答「CPU 回滚」在本轮澄清为"保持现值、不按提议涨到 380/340"）。**同批**：新增武器输出对比工具 	ools/wh-weapon-dps.ts（引擎口径；7 组配对读数见设计稿 §4.8）。**验证**：typecheck ×4 · core **1223 用例** · content:check · desktop build 全绿。【待验收 · 待发布】

- 2026-09-13：**虫洞族专属舰船二轮调整 + 三条船体新机制（船长拍板 · 二号）**——**船长原话**：「重突可以，不过要移除额外火力加成」「指的是 D 组全体」「D 组特色换成高护盾比」「（侦察舰/电子舰）编入队伍就有效。且可以叠加。指挥舰不叠加。炮艇只加动能」。**改动**：① **重型突击巡洋舰（A/C）**：三层血 **×1.1**（A 775→**855**、C 960→**1055**）、**0 抗性一律提到 0.25**（已有的 0.5/0.25 保留）、**移除族武 +0.15**；② **D 组全体（陵墓）特色改为"高护盾比"**（不再做最厚）：哨戒电子舰 260（盾 65%）· 陵卫指挥舰 385（盾 60%）· 陵寝巡洋舰 1000→**920**（盾 55%），三层盾抗 0.25 + 甲爆炸 0.5 ⇒ **"最厚"头衔移交 C 重突 1055**；③ **三条船体固有新机制**（新字段 + 接入点 + 实测）：侦察舰/电子舰 `wormholeScanRadiusBonus: 1`（**编队求和、可叠加**——`wormholeScanBonusOf` + `wormholeMakeGrid(seed, depth, Σ)`；实测 3 艘 ⇒ scanRadius **4**、单艘 ⇒ **2**）· 指挥舰 `fleetDamageBonusPct: 0.15`（`startFleetBattleFor` 对全队武器 ×1.15，**取最高不叠加**；实测规格值 13→**14.95**、129→**148.35**、僚舰 9→**10.35**）· 炮艇 `weaponRangeBonusPct: { kinetic: 0.3 }`（与模块 `rangeTypeBonusPct` **同链**；实测 5,740→**7,462 m = ×1.30**）；④ 界面加「船体特性」行 + `content:check` 三条边界契约。**⏳ 待接线（一号）**：`wormholeDescend` 新增可选入参 `scanBonus`（默认 0 = 旧行为），面板深入下一层时需传 `wormholeScanBonusOf(ctx, run.fleet)`。**同日修正（船长：「D 组现在不是重装种族了，改成高盾占比的，是否移除这个种族的削弱部分」）**：**D 组的种族级削弱（速度/机动）已移除**——陵卫指挥舰 速 154 → **220 m/s**、机动 0.383 → **0.45**（原先按"D = 陵墓重装、坐镇后方"给的 −30%/−15%，身份改为高护盾比后前提消失）；**保留子分类级/身份级代价**：货舱 −25%（与 A 族电子舰同口径）＋**甲/壳血占比低（甲 19~20% / 壳 21~22%）= 高护盾比的天然短板**。**教训（可复用）**：**战斗里编队接近速度 = 全队存活舰 `combatSpeed` 的平均**（`combat.ts:3871`）⇒ 单舰的速度惩罚会让**全队替它买单**（2 舰编队 −13%），足以吃掉"全舰 +15% 伤害"的光环；给"光环型"舰船配代价时**不要放在影响编队机动的项上**。**验证**：typecheck ×4 · core **1223 用例** · `content:check` · desktop build 全绿；三条新机制均实测生效。【待验收 · 待发布】

- 2026-09-13：**修正：舰船"锁定 / 分辨率 / 信号"不是战斗属性——族专属舰船的强增改挂真吃战斗公式的字段（船长指出 · 二号）**——**船长原话**：「锁定/分辨率不是间接属性吗，并不直接影响战斗」。**代码核实**：① `lockRangeM`（锁定范围）**全引擎零消费**（仅 `types.ts:254` 声明 + `shipInfo.tsx:388` 展示）⇒ 纯展示；② `signatureM`（信号半径）同样**零消费**（`combat.ts:268` 注释明写"公式不消费"）；③ `scanResMm`（扫描分辨率）**不是战斗属性**，只进 `expedition.ts:981` 的**悬赏重复冷却**（`10s × √(500/分辨率)`）⇒ 经济/节奏向；④ 经核实**真吃战斗**的是：速度/机动（`combat.ts:395 combatSpeed`）、命中 `hitBonus`、回避 `evasion`、机巢、CPU、族武 `weaponFamilyBonus`、无人机伤害 `droneDmgBonus`、三层血与抗性。**处置**：把挂在「电子舰 / 指挥舰 / 侦察舰」上的锁定·分辨率·信号强增**换成真属性**——掠袭电子舰 命中 0.15→**0.25**、回避 0.18→**0.216**；哨戒电子舰 命中 0.10→**0.22**、回避 0.07→**0.109**；陵卫指挥舰 命中 0.06→**0.11**、新增**无人机伤害 +0.08**；幽影侦察舰 命中 0.14→**0.20**；分辨率加成**保留但改记为经济向**（悬赏冷却 ≈ −18%），锁定/信号保留为**叙事/展示**项、不再计入"强增"。**教训（可复用）**：给舰船做"强增"前必须先确认字段**在引擎里有消费点**——`types.ts` 里声明、装配界面里显示，都不代表它参与战斗。**⏳ 待船长定**：是否把锁定距离/信号半径**接进战斗**（新机制：超出锁定距离不能开火 / 锁定耗时 = f(锁定, 分辨率, 信号)）。【待验收 · 待发布】

- 2026-09-13：**虫洞族专属舰船：舰种子分类 + 数值重排 + 改名（船长拍板 · 二号）**——**原话**：「希望根据种族有几个大方向的强力增幅，其他方面甚至允许少量削弱。但是平均强度要高上10%，并且多出一个槽位（上中下选择其中一个）」＋「削弱幅度提高到30%」＋「并将现有的舰船名字进行调整。（比如炮艇）」＋「子分类进界面」；另裁：**D 族巡洋舰无子分类**、**G 驱逐舰改名**（侦查驱逐舰 → **亡军后勤舰**）。**落码**：① `ShipDef.subClass` 新字段（`ShipSubClass` 九值联合，**只有这 15 艘写**）＋ `content:check` 白名单契约（取值合法 + 非 `sh-wh-*` 不得写）；② 15 艘**按子分类重排**：Σ血 = 官方同档中位 × 族签名比（**平均 1.149 = +14.9%**）、CPU = max(中位×1.10, 现 CPU+40)、**每艘 +1 槽位**（按子分类定槽）、抗性按角色补（武装 盾动能 0.5 / 重装 甲爆炸 0.5，C·D·E 加第二层）、族武 +0.15（炮艇/重突/鱼雷）· 无人机伤害 +0.10/+0.14（无人机作战舰）· 机巢 +50%（指挥舰/无人机作战舰/后勤舰）；**削弱 ≤ −30%** 落在货舱/机动/回避/信号/分辨率/三层血占比上；③ **15 艘全部改名**（掠袭电子舰/掠袭炮艇/掠袭重型突击巡洋舰/幼虫截击舰/甲壳截击舰/巢群重型突击巡洋舰/哨戒电子舰/陵卫指挥舰/陵寝巡洋舰（无子分类）/构件鱼雷舰/机库无人机作战舰/巨构无人机作战舰/幽影侦察舰/亡军后勤舰/亡军鱼雷舰）；④ **进界面**：`ui/shipInfo.tsx`「定位 / 档次」行与 `pages/ShipPage.tsx` 两处船卡的**子分类徽标**（复用 `app-chip app-role-chip`）；⑤ **15 张舰船图纸同步改名 + 重写说明**（蓝图说明契约核对通过）；⑥ 新工具 `tools/wh-ship-tune.ts`（⚠ 只对未调过的基线运行，检测到 `subClass` 即拒绝，防二次叠加）。**契约让步 1 处**：`role: 'armored'` 的「低槽 ≥ 高槽 + 1」⇒ **C 族巡洋 +1 槽位由高槽改低槽**（3/4/5）。**验证**：typecheck ×4 · core **1218 用例** · `content:check`（含新契约与蓝图说明核对）· desktop build 全绿。【待验收 · 待发布】

- 2026-09-13：**契约定向豁免：专属无人机不受「射程分类」档位硬钉（船长拍板「专属无人机开豁免」· 二号）**——**背景**：本批新增两型**族专属无人机**（C 巢卫攻坚 `drone-wh-c-heavy` / E 构件哨戒 `drone-wh-e-sentry`），原设计射程 3,600 m / 6,000 m 撞上船长 2026-09-10「**射程分类 1/4~4/4**」那条硬钉（`tools/content-check.ts` 的 `DRONE_CLASS_RANGE = { scout 2500, combat 3000, assault 3500, sentry 5000 }` 对**全机型**断言等值，专属机型亦无豁免）；经办人按 §5.2 先摆冲突请裁，**船长裁定「专属无人机开豁免」**。**落地**：断言改为 `exclusive === true` 时**跳过并登记打印**（`· 无人机射程豁免（船长 2026-09-13「专属无人机开豁免」）：…`），其余两道约束照旧——`DRONE_ROLE_SPECS[cls].rangeM` 区间 + `droneRoles.ts` 硬边界 500~20000 m；**制式四型仍按 2500/3000/3500/5000 硬钉**。**读数**：豁免清单 3 条（`drone-exile-bee` 2500 属档内、`drone-wh-c-heavy` 3,600、`drone-wh-e-sentry` 6,000）。同批另记：船长「C 组无人机护甲和结构抗性提高 10%」（甲/壳抗 0.25→0.35）与「E 组哨戒不做更改」。【待验收 · 待发布】

- 2026-09-13：**层位克制表改判：逆克制劣化 0.5 → 0.75（船长拍板 · 二号）**——**原话**：「将克制系统进行修改，爆炸对护盾改为\*0.75，动能对装甲改为\*0.75」。**改动**：`packages/core/src/combat.ts` 的 `typeLayerMult`（动能对甲、爆炸对盾 `0.5 → 0.75`；克制/劣化比 3:1 → **2:1**），同源自动跟随 UI 速查 `layerMultText`、`content:check` 产物说明对账、`packages/data/src/items.ts` **四条玩家可见弹药说明**、`packages/core/tests/combat.test.ts` 两处钉值；契约文档 `docs/design/v10b-combat-data.md` 表下**就地**补「2026-09-13 修订」块（含 2026-09-05「能量对盾 0.75 → 1.25」旧改判，避免两套口径并存）。**读数**（新入库 `tools/counter-audit.ts`）：对 balanced 血型有效倍率 动能 **+8.2%** / 爆炸 **+8.5%** / 能量 ±0；对 armor 血型动能 **+16.7%**；对 shield 血型爆炸 **+14.3%**；齐射数新旧对拍 盾型 179→157 · 甲型 205→170 · 均衡型 193→164 轮。**同日实测回答船长追问「有无测试过针对有不同抗性的敌人伤害」**：**敌人侧没有抗性这条链路**——31 张敌卡 / 76 条敌舰单位 `createFoeSpecs` 出来 `resists` 全空（敌舰卡类型无抗性字段、建档处写死 `{}`）· `BattleUnitRt` 无抗性字段 · 我方打敌舰的两处 `applyDamage(rt.hp, {}, …)` **硬编码空抗性**；抗性只在「我方挨打」与「机群池」两条链生效 ⇒ 能测的只有**血型（盾/甲/均衡）+ 层克制**。将来若要给敌舰加抗性，接通点 = `createFoeSpecsFromShips` 建档 + 上述两处调用改传目标抗性（**属新机制**）——**船长 2026-09-13 当日裁定「① 先不动，维持'敌人只有血型'」**。**⚠ 同日处置（船长裁定「① 最小调整夹具」· 已执行）**：同一次改动曾让两处既有用例翻盘，**都是"我方在该场被打沉"**——① `target-lock.test.ts`「集火弹道」：诊断读数 = 我方 **0 次开火**、敌方 **4 发**把我方打到 0/0/0，夹具是合成小船（每层 30 血 / 单发 30，破层 **4 发 → 3 发 = −25%**）⇒ 夹具船每层血 **30 → 90**；② `b1.test.ts`「低安遭遇应战存活」：结算后 `durability = 0`（船被打没），夹具用**开局沙猫级**（护盾 **18** 点）⇒ 夹具船抬到 **400/700/700**（实测后以 **5% 结构**活下来）。**两处都只动测试夹具、内容数值一字未动**，被测行为（集火弹道 / 应战结算且船还在）不变，复测 **30/30 全过**。**另测折中值**：逆克制改 0.6 ⇒ b1 过、target-lock **仍挂**（说明它不是幅度问题而是尺寸问题）⇒ 折中不是解法。**边界说明**：极端颗粒度夹具（"一炮≈一层血"）对层倍率天然敏感，真实量级下同一改动为 +8.2%（balanced）。【待验收 · 待发布】

- 2026-09-12：**新增汇报纪律：修正链之后的数一律标「实收」（船长纠正 · 三号）**——**实证**：三号汇报近防炮对机群命中时，把 G 族蜂群机卡上**已计入机型闪避 0.45 之后**的实收命中 **77%** 当裸读数报出，船长据"设计应是 90"判断为未修复 ⇒ 白改一轮（改判「无视距离的 90」后又按船长「**哦 滚回到上一个闪避生效的版本**」回滚）。**新条款**（挂 §1「验证证据的标注粒度」之后）：凡报**经过修正链之后的数**（守方回避 / 距离衰减 / 技能 / 配件 / 舰种档 / 层抗等），必须**写明是"实收"值并列出吃过的修正**，且与**基础值**分开写（例：「基础 0.9 ⇒ 扣闪避 0.18 ⇒ **实收 0.72**」）；只报一个数而不说口径，等于把结论的边界藏起来——船长会按基础值理解并据此判断功能是否生效。**同时**：口径被改判时，**旧读数不删**，按"基础 / 实收"两列并列呈现，避免同一指标在不同轮次里被误比。【已入册】

- 2026-09-12：**新增纪律：销档（消档）条目不重新入账（船长终裁「①消档」纠正 · 三号）**——**实证**：船长当日已裁定「**近防炮开火节奏**」与二号公告**双双消档**（续接卡 `docs/design/archive/handoff-20260912-to-new-pilot1.md` §7「挂账（当前 **0 条**）」明确写着「**不要拿这两条去问船长**；将来若要重启，属**新裁定**」），但三号在近防炮平衡批里**把它当成"仍挂账"重新提出并请船长裁决**，被船长一句「①消档」纠正。**新条款**（挂 §2 四步闸门之后）：① **汇报与请示前先查"挂账现况"**——以续接卡 §挂账（及 `docs/design/foe-pending-index-*.md` 的 ✅/销档标记）为准，**销档条目 = 不在账**；② **销档条目不得以任何形式重新入账**（不算待办、不进汇报的"仍挂账/待您点"清单、不写成"需另裁"的开放选项）；③ 文档里旧的开放措辞（如"若要进一步解决需另裁…"）在销档后应**就地标注销档**，避免后来人照旧文重开；④ 将来若确需重启，**由船长新裁定**，不是经办人自行恢复。**⚠ 同类（2026-09-12 二次实证，三号）**：**船长裁定"不发"的公告待审稿同样视同销档**——`docs/design/announcement-draft-20260912-foe-overhaul.md` 顶部早已写明「**已作废 · 不发布**」（船长原话「**B1，B2，清理剩余待审稿，不发。**」），三号却在两条推送回执里写「**若需随本次更新发公告，请示下即可落库并再推一次**」，被船长再答「**不写公告。**」⇒ 该两处措辞已就地封死；**汇报里不得再出现"要不要发公告 / 可随时落库"这类开放措辞**。【已入册】

- 2026-09-12：**`AGENTS.md` §1 补一条：船长的验收视图默认 = 本地（船长原话「没有特意说明的情况下，都是本地查看的效果」· 三号）**——**背景（实证）**：本轮排查"战斗里敌人仍是一字横排"时，经办人按"船长看的是线上网页版（推送后 Pages 部署）"一路排查（核对 Pages 部署记录、拉线上产物比对标记），实际船长看的是**本地构建**；真相 = 旧包/旧标签页残留，代码无问题。**新增条款**（挂在他 2026-09-11「验证证据的标注粒度」那条之后）：船长报障/报读数时**默认看本地**，排查先按"本地那份产物是不是最新"查、改完代码要提醒他**重建/重启**本地那份；**不要**一上来推断"你看的是线上旧产物"或让他刷网页（线上只是另一条通道）；**需要区分时先问一句**"你用的是本地还是线上"，不替他假定。【已入册】
- 2026-09-12：**新增纪律：合入与批量文档写入的编码纪律（船长「允许新增纪律」；二号 · d2）**——**事故**：2026-09-12 二号在 d2 解两次合并冲突（`7ccf95f` / `eaa00ce`）时，`docs/roadmap.md` 被写成「UTF-8 文本按 GBK 解码后又存回 UTF-8」的**转码产物**（**449 / 548 行乱码、10,460 字**），并**丢掉 19 行正文**（`## C./D./E.` 三个分节标题 + A1 待办行 + 置顶引言 + 15 条段落续行）；**typecheck ×4 / core 972 用例 / `content:check` / desktop build 四连全绿也发现不了**（纯文档无门禁）。**范围核查**：全树 485 个文本文件扫描 ⇒ 只有该文件中招；两次合并涉及的全部 27 个文本文件按「两侧父提交都有、合并结果却没了」逐行审计 ⇒ 仅该文件（pending-index 的 P-21 行属撞号改号 P-43 的正常改写）。**还原**：两次合并的两侧父提交均干净（main `9a58edd` / d2 `9f6b94f`）⇒ 按「干净候选 → 模拟损坏」比对**449 行唯一命中**、另 19 行按「不在两侧父提交中」定位后**19/19 精确配对**；自检 = 548 行 / 乱码 0 / 两侧内容零缺失 / 无多余行。**新纪律**（写入 `development-conventions.md` §三，`AGENTS.md` §4 同步一句）：① 取历史版本首选 **`git checkout <rev> -- <path>`**（不经变量中转）；② 必须经变量时**编码显式**——本机 `pwsh` 工具实为 **Windows PowerShell 5.1**，收外部输出用 .NET `StandardOutputEncoding=UTF8`、写回用 `WriteAllText` + `UTF8Encoding`，禁 `Set-Content`/`Out-File` 裸用，含中文的 `.ps1` 探针须带 BOM 再跑；③ **合入/批量写文档后三条自检**（BOM 与行尾一致 · 全仓**往返解码**乱码 0 · 与两侧父提交比「父提交有的行还在」）；④ 乱码与丢行按**事故**处理，只许用干净父提交逐行还原、禁止凭记忆重写，并把现象/溯源/还原法/读数写进 `docs/roadmap.md` 当日条目。【待验收 · 待发布】

- 2026-09-11：**汇报口径微调：验证证据的「标注粒度」（船长裁定「乙」）**——船长问「为什么要强调：无头 Chrome + 网页版」，核清事实后（该流程源自**一号交接件** `docs/design/handoff-20260910-session-turnover.md:125` 与 `handoff-20260911-to-new-pilot1.md:211`，**无任何文档把"标注验证目标"写成必须**；三号近期文档提交为舰种体积/阵形/Steam 文案，与本口径无关），船长裁决 **「乙」**：**只在与结论容易被误读的场合标注验证目标与工具**——① 桌面外壳（窗口/菜单/IPC 存档读写/打包产物）② 真档只读取证 ③ 性能与耗时 ④ 跨窗口尺寸几何；**纯文案/纯数据/纯逻辑类默认不标注**（不必每次写"无头 Chrome + 网页版"），用「实测」「已核」「全绿」这类结论性表述即可。分寸：**标注是为了讲清证据边界，不是为了显得严谨**。已写入 `AGENTS.md` §1（末条）。【待验收 · 待发布】

- 2026-09-11：**游戏改名：`大鲸鱼 · 深空工业` → `大鲸鱼-深空放置`（一号；船长「将游戏的名称改为大鲸鱼-深空放置」）**——**性质**：这是**游戏名（产品名）**变更，不是仓库/标识符变更；`AGENTS.md` 标题随改（本文件顶部按纪律记这一条）。**改到的展示位**：`apps/desktop/src/main/index.ts`（BrowserWindow `title`）· `apps/desktop/src/renderer/index.html` 与 `web/index.html`（`<title>`，网页版保留「（网页版）」后缀）· `App.tsx` 顶栏 `app-logo` · `apps/desktop/electron-builder.yml`（`productName` / `shortcutName` / `copyright`）· `apps/desktop/package.json` `author` · 根 `package.json` `description` · `README.md` 标题 · `AGENTS.md` 标题。**明确未动（重要）**：① **`深空工业协会`**——它是**游戏内势力**（章鱼人官方，玩家雇主），出现在 `commsFactions.ts` / 通讯发件人 / 手册 / 星图声望提示等约 20 处，**不随游戏名改**；② **`大鲸鱼Ⅳ`**——母港**星系名**（`universe.ts` / 公告 / 文档）；③ **`大鲸鱼一号/二号/三号`**——并行 agent 代号（工作约定域）；④ **一切标识符与路径**：`appId com.whale.deepspace`、包名 `@whale/*`、仓库名 `whale-eve-idle`、**存档键与目录 `whale:idle:save` / `%APPDATA%\whale-idle`**（主进程启动即 `app.setName('whale-idle')`，故 `productName` 改名**不会**移动存档）、存档 `format: whale-idle-save`、渲染层 `whale-idle:*` 偏好键、测试档与历史文档里的旧名（按「历史条目不回改」保留）。**游戏内开局欢迎行**：`packages/core/src/state.ts` 的 `欢迎加入「大鲸鱼深空工业」。` 是**唯一在玩家可见文本里出现的旧产品名**（其余全是势力名）；船长裁定「**甲**」⇒ 改为 **`欢迎加入「深空工业协会」。`**（指向游戏内真实势力＝教程简报的发件方，设定自洽；该行无测试断言）。**产物核对**：网页版与桌面版 `index.html` 标题、两份打包 JS 内的品牌串均为新名，旧名在产物中 **0 处**；`深空工业协会` 在产物里仍有 **17 处**（势力文案完好）。【待验收 · 待发布】

- 2026-09-11：**舰船价格口径对齐 + 蓝图价同步 + 价格预警（一号；船长「检查市场所有舰船的价格，我感觉部分船价格不合理」→ 裁决「按你推荐的来，3 改为预警，将舰船蓝图价格也同步」；随后追加裁决「甲」）**——**（一）病根（并行提交事故，同类第二次）**：2026-09-09「巡洋价位定档 9/11/13/15M」的重放提交 `6148f0d`（提交语自称"此前被并行提交交叉吞失"）与三号的 `0bd0809` 都按**旧数值**匹配，把 9/11/13/15M 写到了 `330_000/760_000/210_000/480_000` 四行上——那其实是**陆龟/玳瑁/飞鱼/旗鱼**的价，而真正的 4 艘掠食者巡洋（3.4/3.6/3.8/4.4M）**一次都没改**：4 艘小船的 `priceIsk` 被放大 14~62 倍、4 艘巡洋停留在旧价。**（二）口径写死**：① `marketCatalog` 市场行 `basePrice` = **玩家真正付的价**（`buyShip` 走市场报价）＝锚；② `ships.ts priceIsk` 必须等于它（**定制船 = 0**：无市场行、或市场行 `playerBuyable:false`）；③ `shipBlueprints priceIsk` = 市场价 × 档位系数（≤30 万 ×2 / 30~100 万 ×2.5 / 100~400 万 ×3 / >400 万 ×4，±1 万取整余量）。违反的代价是**维修费档位读 `priceIsk`**（`repairTierWeight`：<20 万 ×0.4 / <80 万 ×0.7 / <150 万 ×1.0 / 其余 ×1.4）⇒ 船价与维修费脱节，且平衡工具（`balance-check`/`playthrough-sim`）按它决定买船与进度。**（三）修复**：9 处对齐市场行（陆龟 9M→33 万、玳瑁 11M→76 万、飞鱼 13M→21 万、旗鱼 15M→48 万；长尾鲨→9M、电鳐→15M、锤头→11M、牛鲨→13M；座头鲸 235 万→135 万）；蓝图价同步 2 张（鲣鱼 8→8.4 万、陆龟 83→82.5 万）；**新增 `content-check`「舰船价格口径（预警）」——船长明确"只预警不阻断"**（`warn` 通道，扫 priceIsk 与蓝图价两路）。**（四）预警当天即抓出同族遗漏并由船长裁决「甲」**：皇带鱼级旗舰货舰 `priceIsk=0`（图鉴写"仅制造"）却挂着 550 万可买现货——2026-09-09「蓝图船成品现货下架」那次清扫的漏网（当时只点了开拓/鲸王）⇒ 补 `playerBuyable: false`（只收不卖；二手可卖），并让 `buyShip` 对只收不卖船直接给出准确文案（原先会落到"挂收购单失败（余额不足…）"的误导提示）。**教训（与 §5.2 同源）**：跨文件同步数值时**必须按稳定键（id）定位**，禁止按"旧值"批量替换——这正是本次事故的手法是值匹配。

- 2026-09-11：**新增底层规则「新提议与旧规则冲突时，先提醒船长」（船长定：「新增约定，出现我的提议和旧规则冲突时，先提醒我」）**——`AGENTS.md` 新增 **§5.2**：船长的新提议与既有已确认规则（本文件 / `development-conventions.md` / `docs/design/*` 里标注「船长定 / 已确认 / 已定案」的条款、既有契约与测试）冲突时，**先停下把冲突摆出来请他裁决**——不许默默按新的做，也不许拿旧规则当挡箭牌拒绝执行；提醒格式五步（① 新提议原话 ② 冲突条款原文＋出处行号＋日期 ③ 二者不可同时成立的后果 ④ 可选做法与推荐 ⑤ 问「按哪个来」）；判据 = 只要"按新提议做"会让任何已确认规则、契约或测试失效即算冲突；裁决后把新裁定写回文档并作废旧条款（不让两套口径并存），等不到答复就先做无关部分并把冲突点标注待定。**实证**：稀有残骸「每次 30 m³ 必给彩头」与 `docs/design/bounty-tasks-20260910.md:142`「每炉锁死 1 件 = 一炉一箱（船长 2026-09-10 定）」冲突时未先提醒，白改两轮、6 个用例失败。**本规则为船长直接指令，待船长决定是否同步进 `development-conventions.md` 权威正文。**
- 2026-09-11：**新增底层规则「禁止启动子代理」（船长定：「新增底层规则，不要启动子代理。写入AGENTS.md中」）**——`AGENTS.md` 新增 **§5.1 禁止启动子代理**：任何会话、任何任务一律不得启动子代理（含子代理、会话分叉子代理、worktree 隔离会话、多代理编排 workflow、Ralph 式循环执行等**所有形式**）；活由本会话自己干（读文档/搜代码/写实现/跑验证/做审查都不许分包），理由 = 子代理拿不到本会话上下文与船长口径、易跑偏且违反四步闸门，多 agent 只指 §4 里船长安排的**一号/二号/三号工作区**（不是本会话可自行派生的）；做不完就**分阶段自己做完并如实汇报**，或请船长排期，不得靠派生代理"并行提速"。**本规则为船长直接指令，待船长决定是否同步进 `development-conventions.md` 权威正文。**
- 2026-09-11：**文档体检：补记第三工作区，清掉计数与口径里的过时点（一号；船长：「顺便检查下现有文档，将明显过时的东西进行清理，精简文档」）**——① `AGENTS.md` §4 标题「**两 agent 并行**」→「**多 agent 并行**」，物理隔离条补记**三号工作树 `H:\大鲸鱼\Deepseek-EVE-verify`（同仓 worktree，分支 `verify`，**不叫 d3**，核验与收尾）**，并把"二号永不直接写主树文件"扩为"二号/三号"；§0 首行会话清单补"三号"（原"一号/二号/新助手"）。② `AGENTS.md` §0.1 文档清单的 `docs/design/*` 计数由**"89 份"改为"现约 100 份，以目录为准"**，并补列交接件 `handoff-*.md`/`handover-*.md`（实测 `docs/design/` 下 **116 份 md = 设计稿 100 + 公告待审稿 7 + 交接件 9**；写死计数会反复过期，故改为带"以目录为准"的约数）。③ `development-conventions.md` §三「工作区分工与合入纪律」的**物理隔离**条补第三工作树、把"二号"泛化为"二号/三号"，并写明**三号的合入舞步**（verify 提交 → `merge main` 解冲突 → 主树 ff）；基线同步与完成即合入两条同步去个性化表述。**本次只补记既有事实与修过时叙述，未改任何规则。**
- 2026-09-10：**新增第十四章「表现层实现与性能纪律」（船长定：新开一章，不塞进 §九）**——把无人机战斗动画批次踩过的 7 条实现层规则写进权威正文：①渲染体提前 `return` 之后禁止出现 hook（hooks 数量不一致会让 React 卸载整棵树＝黑屏无响应）；②逐帧移动的物件位置不得依赖 React 重渲染节奏，用 rAF 直写 `transform` + "值未变不写"；③组件局部的"生死/演出"登记必须能在入场时按引擎真值重建（否则重挂载后已摧毁单位复活）；④位移动画勿交 CSS 关键帧 `forwards`（终止值冻结致物件停在旧坐标）；⑤大 SVG 子树禁用 `filter` 做状态表现、大幅 `box-shadow` 缩放走合成层（实测绘制尖峰 50–60ms→0）；⑥锚点几何依赖的测量值须随布局变化重测（否则弹道落点整体偏移）；⑦引擎事件的展示字段（`src`/`artId`）是表现层差异化的地基，先做地基；附注"改表现层前先用 perfHub 取数，别凭猜"。同批：原「变更记录」指针章顺移为**第十五章**；`AGENTS.md` §0.1 索引的"改 UI / 改战斗表现层"两行补入 §十四，文档清单同步为"一~十四章"。

- 2026-09-10：**约定文档可读性改造（船长定：拆分 + 目录索引，让执行不同工作时不必通读全部）**——① 第十四章「变更记录」**独立成册**为本文件（`docs/development-conventions-changelog.md`）：主文件从 394 行降到 326 行，且不再随记录累积变厚；约定正文与 `AGENTS.md` 的每次变更都追加到本册**最上方**；② 根目录 `AGENTS.md` 新增 **§0.1 目录索引**：按任务类型（改 UI／改战斗表现层／改数值经济／改存档迁移／改玩家文案／备公告／建工具／多 agent 并行合入／续接压缩／查历史口径／交付测试档）指认"该读哪几份文档"，并附文档清单用途一览；开工顺序相应改为"读 AGENTS.md → 按索引只读本次需要的几份"（仅首次接手/大工程/压缩后续接才通读约定全文）；③ 主文件第十四章改为指针段并写明读取纪律；AGENTS.md 维护说明同步指向本册。

- 2026-09-10：第九章新增"视觉物件一律 SVG 线稿"规则（船长 2026-09-10 定；AGENTS.md §6 同步）——图形物件（舰船/敌舰资产与小视觉件：状态飘浮物、开火闪光、弹道点、场景点缀等）一律 SVG 绘制、与舰船资产同语言（细描边/currentColor/viewBox）；禁止 CSS 拼形状（clip-path/伪元素/渐变裁形/圆角拼图）；背景氛围光效（渐变/光带/光晕）可用 CSS；实证：导航舰船状态窗飘浮物与炮口火光由 CSS 形状重绘为 SVG 线稿。
- 2026-09-09：第七章补"中文优先、少用非单位英文缩写"规则（船长 2026-09-09 定；AGENTS.md §1 同步）——单位类（ISK、秒/分/时、m³ 等）与词典专名可直接用，其余英文缩写与代码标识符（civ/mil/蓝图/制造耗时等）在沟通/总结/文档正文先给中文全称或纯中文改写；代码/提交/表头标识符不受限。
- 2026-09-08：**AI 核心统一启用上限（大鲸鱼二号；玩家反馈"AI 核心可无限制启动精炼炉"，船长定底层口径 = 「人工智能专家」限制同时启用的 AI 核心总数、与用途无关）**——①新语义：同时启用上限（`aiCoreCap`）= AI 核心上限技能贡献之和（现唯一 = ai-expert 等级，后续上限技能在此叠加），Lv0 = 0 不可启用；②占用池 = AI 副船任务 + AI 精炼炉/回收炉/制造线**共用**（副船 checkAssignable 与三处炉/线启动走同一 `aiCoreCapBlock` 守卫；旧作业豁免的制造线不计入）；③存量超限（读档/技能变化）不中断运行、但计入占用，新启用必须先降到上限内；④`maxAiSlots/aiSlotsUsed` 更名 `aiCoreCap/aiCoreUsed`，副船名额并入统一池；⑤文案/口径同步：skills.ts「人工智能专家」描述改"每级 +⟦1 枚⟧ AI 核心上限"、ShipPage AI 指挥中心（"AI 核心启用 X/N"、不点名单技能）、手册「AI 副船/精炼/制造」条目、ai.ts/industry.ts/manufacturing.ts/balance/types 头注、tools（salvage-econ 炉位规则、playthrough-sim、make-test-save）；⑥测试：core +6（统一池真值表：Lv0 拒/炉满额再开拒/副船×炉互斥/线同池第三处拒/存量超限不中断照常结算/旧作业豁免不计）+ 既有 AI 炉/线用例补资格 → **515 绿** + typecheck ×4 绿；⑦公告口径（2026-09-08-market-manufacture 劳动者制第 2 条"AI 核心驱动不受此限/核心库存即并行上限"）需修订 = 待审稿递船长，未写入【待验收/待发布：推送闸门内】。
- 2026-09-08：**连续出击离线续跑修复（大鲸鱼一号；玩家反馈）**——simulateOffline 在连击开启且非战斗冻结时按 30s 分片推进、片边界同在线条件自动再出发（离线持续讨伐有战果）；最后一片结束不触发；非连击离线仍单次大推进；core 490 + typecheck ×4 + desktop build 绿【待验收/待发布】。
- 2026-09-08：**文案归位第一批执行（大鲸鱼一号；船长指示续做存档批次）**——星图·远征页「停靠母港/停靠副站」空态行改情境向（去说明书式机制段）；机制细节移入手册速览：「远征」条目更新为当前口径 + 新增「副空间站」条目；copy-cleanup-pending.md 转执行记录（速览整体改版与后续巡检批次待办）；typecheck + desktop build 绿【待验收/待发布】。
- 2026-09-08：第三章补"推送网络策略"（船长 2026-09-08 定：直连失败则走代理）——本机系统代理 = `127.0.0.1:7897`，git 未配置代理时直连 GitHub 常被重置；推送/拉取先试直连，失败即用仅命令级代理重试（`HTTP_PROXY/HTTPS_PROXY=http://127.0.0.1:7897`），不写持久 git 配置。
- 2026-09-08：第三章补"推送闸门"（船长 2026-09-08 定，与公告发布同批）——开发/推进中只本地 commit 不推送 origin；工作确实完成（自测全绿 + 船长验收口径满足，大更含公告稿已过审）后代码与公告一起推送一次；未完成本地提交标注"待验收/待发布"；AGENTS.md §4 已同步。
- 2026-09-08：第九章新增"一级页不滚 / 滚动只进二级窗口"规则（船长 2026-09-08 定）——一级页（导航直系页）整页无滚动、高度 = 主窗口内容区，滚动只进二级窗口（标签/功能窗列表与卡网格内滚）；**前提红线：内容必须能完整装下，不得硬裁去滚；放不下又无法拆分的页面单独提出与船长讨论方案**；AGENTS.md §6 同步。
- 2026-09-03：确立确认流程（四步闸门）、思考工作过程约定、中文语言约定、验证与文档纪律。
- 2026-09-04：新增"多 agent 并行协作"约定——疑似对方工作区改动时先停手、报告证据/影响面/判断/交接信号，用户协调前保持只读；确认无直接通信通道，用户是唯一协调桥梁。
- 2026-09-04：新增"检索纪律"——查找/改写内容一律以 id/唯一键为锚点快速检索，禁止长文本近似匹配改写；并确认快速检索完全可行（id 主键体系 + grep 锚点 + 一次性结构化查询脚本）。
- 2026-09-04：新增"会话续接与压缩纪律"——模型无法主动触发压缩（环境在轮间自动执行），但承诺：里程碑即固化文档；新系统级大工程开工前主动提示压缩点并先产续接卡；船长可随时要求整理续接卡/固化状态。
- 2026-09-04：新增"测试门槛存档约定"（本章八）——B 批次与新玩法数值交付时配可测存档（tools/make-test-save.ts + docs/test-saves/），基于当前真档注入。
- 2026-09-04：新增"UI 风格一致性"约定（本章九）——界面调整前先对照同级相似界面的既有规范并复用类名；物品页「仓库/货仓」标签与星图页 `.app-subtab` 规范统一；顺带清理变更记录历史重复条目。
- 2026-09-05：新增"工具纪律"约定（本章十，船长拍板"采纳并写入约定"）——正式工具（可复跑）一律入库 + 注释 + 常用挂 npm script；临时探针（`_` 前缀）任务收尾二选一：有复用价值 → 转正式入库，一次性 → 删除；多 agent 不替对方处置其探针；同步修订第二章验证闭环与第五章检索纪律里"用后即删"旧表述。
- 2026-09-05：新增"术语词典纪律"约定（本章十一，船长要求"名词解释做交流文档 + 查询文档写入约定"）——建立 `docs/glossary.md` 仓库级术语权威，新术语登记制，压缩遗忘以词典为准。
- 2026-09-05：第九章补"尺寸稳定性规范"（船长反馈"槽位高度建议固定，不要随内容物变大变小，设计需考虑最大情况"）——容器/格子固定尺寸、空满两态共用盒模型；实证修复装配页 `.app-fit-slot-icon` 固定高 76px。
- 2026-09-05：精炼炉改**多工位并行**（船长拍板：主控亲自运转限 1 台 + 每枚闲置 AI 核心驱动一台；核心库存即并行上限）——存档结构 v19（refineRun 单例 → refineRuns 工位表，含 v18→v19 迁移）；停炉按资源 id 定位；顶部活动栏精炼逐炉一条；工业页精炼炉按矿带卡网格常驻，运转中卡不改样式只把按钮变停炉。⚠️ **需二号复核（B3 经济）**：残骸回收炉现可多台并行 → `tools/salvage-econ` 的"单炉炉速 1440 m³/h 上限"假设不再成立，回收吞吐上限 = 闲置核心数 × 炉速，请复核经济锚与打捞-回收链平衡。**✅ 2026-09-06 二号复核收口（船长确认）**：`tools/salvage-econ` 已按多炉并行重跑，结论 Y 与单炉经济锚均不变，上限口径更新为"闲置核心数 × 单炉炉速"，挂账关闭。
- 2026-09-05：第三章补"工作区分工与合入纪律"（船长拍板）——主树（main，一号独占）与二号 worktree（`H:\大鲸鱼\Deepseek-EVE-d2`，`d2/*`）物理隔离；二号每完成一件安排的工作、自测全绿后即合入 main（先 rebase 解冲突、主树干净才 merge），真档只读/走备份。
- 2026-09-05：第三章补"文档同步"（船长定：docs 留主树为唯一权威，二号 d2 不维护副本、需要时来主树读取）。
- 2026-09-05：⚠️ **能量伤害克制数值变更（需 C4 复核，二号）**：`combat.typeLayerMult` 能量（plasma）对护盾 0.75 → **1.25**（船长拍板；激光/能量弹系对盾显著增强、无克制弱点）——影响胜率预估与 PvE 时长结构，请二号复核平衡与模拟基线；同时弹药 chip 悬停与手册「航行须知·伤害克制速查」补矩阵展示（数值与 typeLayerMult 同源）。**✅ 2026-09-06 二号复核收口（船长确认）**：战斗复核结论 = 维持 1.25（激光系早期/中期强势、高威胁墙仍在；能量 1.25 不回调），挂账关闭。
- 2026-09-05：新增"游戏内更新公告"约定（本章十二，船长要求"每次大的系统添加或更新就发布公告告知玩家，写入规则以方便告知一号"）——大更完成由经办人补 `packages/data/src/announcements.ts` 新条目；顶栏「公告」入口（未读红点 + 启动自动弹一次 + 历史回看）已上线，首个公告 = 序章·苏醒整批。
- 2026-09-06：建立根目录 `AGENTS.md`（船长定：现有约定执行不佳，改为 AGENTS.md 随会话自动注入以强制约束）——内容为本文件"可执行精简版"（开工顺序/四步闸门/验证闭环/工作区合入/检索术语/UI 工具/游戏公告/会话续接/测试档），细则冲突一律以本文档为准；**AGENTS.md 内容变更必须同步本文档变更记录**。
- 2026-09-06：第二章「动脑前先动手读」补 **AGENTS.md 为首读项 + 压缩/续接重读纪律**（一号；船长转达二号提示"仓库根有 AGENTS.md，开工先看"）——开工顺序 = AGENTS.md → 本文件 → architecture → 相关 design → 代码现状；自动注入非 100% 保证，压缩后上下文未见其内容即重读。
- 2026-09-06：**平衡复核收口 + AI 副船返航同口径（二号；船长 2026-09-06 复核会"我没什么异议"确认默认项）**：①能量（plasma）1.25 战斗复核——维持现状收口、挂账关闭；②B3 打捞-回收经济复核——`tools/salvage-econ` 多炉并行重跑已并入，结论 Y 与单炉锚不变、挂账关闭；⑤**AI 副船采矿/打捞返航腿扩展同口径**——`trips.scaledReturnMs`（满仓基准 × 货仓占比，钳 0~1、下限 1ms）先行缩放再按核心效率拉长（采矿 ai.ts oneLegMs 腿、打捞 ai.ts legBaseOf 腿；满舱占比 1 = 原时长不变，部分货载/富矿脉余量返航变快）；core 测试 +2（采矿/打捞各一条部分货载 25% → 实返航 75s 回归，旧口径需 300s）。
- 2026-09-06：**新增两条底层规则（船长 2026-09-06 定）**：①第九章「UI 风格一致性」升格总则——任何位置**新增内容**（卡片/行/弹层/标签页/页面/图标/文案位）样式一律**优先参考同级下的其它相似项 UI**（同组相似项 → 同页其它区块 → 同功能模块其它页面 → 全仓 `app-*` 家族），先复刻后最小差异，禁止自造相似新样式防风格割裂；②新增第十三章「游戏内文案纯净与设定」——玩家可见文本（UI/日志/公告/任务/教程/手册/悬停提示）必须符合游戏设定，开发/商讨痕迹（版本/日期/复核/待定/占位/口径/内部代号等）禁入玩家可见范围，交付前自查 + 历史残留清理上报；AGENTS.md §5/§6 已同步（维护：AGENTS.md 变更需同步本记录）。
- 2026-09-06：**P2 第二轮「稀有配额制」（二号；已合入主树 9565c44，后被第三轮取代）**——全市场 rare 供给改为全局配额（每窗口 4~9 张浮动 + 整批解锁 +1/批，「现货抢购学」按旧语义放大）；闸内（bmStanding 未达标）商品以 0.04 低权重参与同一配额（取代旧 ×0.01 独立静默），命中即 ×4 价暗市单（标 bm 可绕过拦截，玩家向隐身不变）；rare NPC 订单寿命 ×4（9→36 分钟，买卖两侧）；解锁整批后权重/价格恢复、配额 +1、不转常驻；旧 P2「窗口命中 ×0.01」机制废弃（历史见 `docs/design/power-ladder-rework.md` 实施记录，参数表待船长复核）。
- 2026-09-06：**P2 第三轮「抽取节拍制」定稿并实施（二号，船长逐项确认后 d2 实施）**——取代第二轮配额制：①rare/奇货出单抽取节拍拉长为 **10 分钟**（`RARE_DRAW_PERIOD_MS`；60s tick/common/收购侧不变）；②rare 每窗抽 N = max(1, 8%~15% × 已解锁件数 × 抢购学) 张、**加权有放回（同窗可重复抽中同一类型）**，基数=仅已解锁件数、删除整批 +1/批与每件每窗 1 张限制；③现货抢购学削弱为每级 **10%**（原 25%）；④**奇货订单寿命 4 分钟 → 6 小时**（`orderLifeMs.exotic`，两侧），每抽取窗每件独立 0.8% 掷骰、全窗命中 >2 张随机抽选保留（`EXOTIC_CAP_PER_DRAW=2`）；⑤存档加可选节拍字段（ensureMarket 自动补齐，零迁移）；⑥玩家可见文案同步（市场页限定奇货 4 分钟闪现→6 小时/每 10 分钟一轮）。
- 2026-09-06：**建站交付紧急修复（二号，玩家反馈"空间站建设任务资源无法提交"）**——根因：悬赏/扫描"任务完成即返航"改动后，在建（未建成）副站不再有任何停靠入口，而交付引擎与任务卡仍要求"停靠该站"（首档『奠基』本就应先到工地交付才能解锁泊位，形成死锁）。修复 = 交付前置改为**工地现场**（`playerAtSite`：停靠该站 或 野外停留于站点星系——掩护巡逻/作业到场即可，母港特例保留）；任务卡同步改为现场判定与提示、到位日志改为"现场交付、无需停靠"；core t9 +3 回归例（首档野外交付/解锁后停靠继续/异地拒绝）；core 463 绿 + typecheck ×4 绿。
- 2026-09-08：**公告发布审核流程（船长 2026-09-08 定）**——开发交流/设计讨论内容**不得直接写入公告数据**；批次发布前把公告文案（id/title/tag/bullets）整理成待审稿**递送船长审核**，明确批准后才合入 `announcements.ts`；被驳回按船长意见修改后再审（本章十二已更新；AGENTS.md §7 同步）。
- 2026-09-08：**任务中心·时效任务板收尾固化（二号；船长确认；doc = docs/design/task-board-v24.md）**——机制收口登记（20 分钟整板同源刷新/候选池门槛/税前锚定防套利/快递真实航程与在途互斥/离线末窗结算；sideTasks 自 fb8e6db 起无实质改动、无挂账）；词典登记 时效任务板/资源任务/快递任务 三词；**快递机制复核 = 保持现状（船长拍板）**：免费的是"去程就位"，返程类移动仍计时（自动返航 2×单程等），快递为唯一定向跨站直达航程，等待互斥与"船在跑"一致，×1.30 运费补贴保留，不改代码。
- 2026-09-08：**本地悬赏返航段 + 演习场奖励重锚（二号；船长 2026-09-08 两问拍板）**——母港目标（大鲸鱼Ⅳ）悬赏胜利后不再 0 航程回港：固定返港段 **120s**（= 矿工本地满载返航单程 localLegMs，防零航程白刷，期间不可召回同胜利返航）；演习场讨伐令奖励按新港每分钟费率对齐：1,000 → **3,600 ISK**（3,600÷(交火 2min+返港 2min)≈900/min ≈ 新港 6,400÷7min≈914，取整百略留教学利差）；悬赏卡 meta（本港返港 2 分钟 / 估算奖励·小时含返港段）同步；expedition.test 相应时序回归更新（结算在 chunk 末尾 → 返港段需后续推进）；core 466 绿 + typecheck ×4 绿。
- 2026-09-08：**已建成副站 = 母港镜像 + 所有自动返航选最近已建成站（二号；船长拍板，镜像与返航同批收尾）**——①引擎 `isAtHomeLike`（母港或已建成副站 = 协会基地网络）替换精炼/残骸回收/逆向研究/组装机的 isAtHome 门，CargoPage 出售放行建成副站（共用全局市场），工业/星图/远征/手册文案口径统一；②自动返航统一口径：返航基准 = 目标星系最近已建成站（`returnBackMs` / `builtSiteAtGalaxy`；无建成副站 = 母港），悬赏胜利/失利/撤退与扫描完成到港**停靠建成站**（dockedSite 落点 + 日志动态化）；本地悬赏（落点 = 目标星系）套 120s 返港段；采矿/打捞/AI 本就是最近站口径；**手动召回仍回母港（例外，船长如需再改另议）**；③UI 悬赏卡按最近站动态展示返航分钟与估算/小时；④测试：t9 +3（镜像门真值表/越过门/工地拦截）、expedition +1（目标星系建成站胜利落点 = 该站）与既有时序回归；core 467 绿 + typecheck ×4 + content:check 绿。
- 2026-09-08：**战斗界面敌方射程显示修复（二号；玩家反馈"最小射程显示不正确"）**——battleArcsFor 敌方聚合以 0 作 min 初值再取最小 → 敌方近盲带起点恒显示 0；改为 +∞ 初值取极值（kite 等带近盲的卡现在正确显示 min，如赤潮 ≈1.4km 起）；回归测试 +1（kite/僚机 foe.minM>0）；**敌能量武器命中模型原按方案 B 保留，随后船长改拍光束必中（见下条）**；core 468 绿 + typecheck ×4 绿。
- 2026-09-08：**敌武器命中体系改造（二号；船长 2026-09-08 逐点拍板）**——①**能量（plasma）敌武器 = 光束必中**（开火即中、无视回避、无 miss 弹道），**近盲带保留**（带内威力 ×blindDmgMul），带内至远端威力按 beamPowerFactor 距离衰减（与玩家激光同源语义，实时开火与胜率预估同改）；②**动能/爆炸普遍高命中**：`balance.foeHitRate 0.55 → 0.85`（单发由公式自动降，期望 DPS 恒定）；新增 `AnomalyDef.foeHitRate` 逐卡覆写做**低命中特例**（2026-09-08 首设两张贴设定：占港武装通缉=拾荒者乱射 0.55、泰坦残骸勘探=老化自动炮 0.55）；③新增 `AnomalyDef.foeDmgMul` 等效回退口，四张能量主伤卡（赤潮/深渊门/星髓/蜃影）先挂 **0.35 初值待实测复核**；④测试：expedition +3（specs：beam 化/近盲保留/命中缺省与特例、foeDmgMul 缩放 shotDmg）；core 470 绿 + typecheck ×4 + content:check 绿（红环试验档可复测手感）。
- 2026-09-08：**命中体系平衡补偿 + 锚装矩阵复测（二号；船长选方案 A）**——高命中+低单发在守方回避>0 时期望承伤上升（减法削弱的回避收益），新增 `balance.foeHitCompMul=0.62`（动能/爆炸单发再乘，锚点 ≈ 典型回避 0.22 × 中距衰减 0.7 折算旧 0.55 模型；能量光束不消费）；`battle:calibrate` 全矩阵复测：**主验收（S2/S4 中位）全 watch 卡 100%**（赤潮 34s/深渊门 45s/蜃影 47s/裂谷 63s/泰坦 62s/星髓 38s/噬口 62s/坟场 75s/穹顶 S2 75s·S4 52s），段内时长回带；低命中特例与能量 0.35 保持；core 470 绿 + typecheck ×4。状态：**✅ 2026-09-08 船长实测验收（赤潮 MK1 满配险胜≈合理、MK2 锚装轻松击杀）+ 公告 `2026-09-08-foe-weapons` 过审写入；随批推送 origin**。
- 2026-09-08：**公告人称与话术规则（船长 2026-09-08 补定）**——①公告文案**禁用第一/第二人称代词**（我/我们/你/你的等），一律以**「玩家」**第三人称表述；②**开发/验收话语不进公告**（复测/校准/对齐口径/目标带/矩阵等只说"世界发生了什么"，不说"开发者做了什么"）；本章十二更新、AGENTS.md §7 同步。
- 2026-09-08：**命中体系验收归档（船长）**——赤潮实测通过（MK1 满配险胜≈合理 / MK2 锚装轻松击杀）；**占港武装通缉 / 泰坦残骸勘探的低命中特例（0.55）船长决定跳过逐卡实测**，作为已交付已知项归档（数值面已进矩阵：低命中→单发自动加重保持期望）；后续如需实感复核，单卡 `foeHitRate` 一处即可微调。
- 2026-09-08：**活动栏 AI 生产占用修复（二号；玩家反馈）**——AI 核心驱动的制造线/精炼炉不再占用「玩家活动」行（并入 ⚙ AI 徽标计数：副船+AI 生产，副船为 0 时点击跳工业页；AI 生产取消仍在工业页卡片）；t1 用例同步；core 471 绿。**✅ 船长验收通过（2026-09-08）**。
- 2026-09-08：**事件日志不落盘（会话级；大鲸鱼一号；船长两轮定：先定"不做本地保存/每次开启空白"，后定"不做启动强制清空"）**——存档写盘前剥离 logs；启动/恢复备份**不做强制清空**：离线补时日志照常显示，另补单条「离线结算报告」汇总（钱包/收获/装备/新船/技能明细）；首次载入旧格式档会显示一次旧日志，此后快速重启自然空白；弹窗「详见右侧事件日志」指引保留；通讯提示改「记入右侧事件日志」；无存档版本号变化，loadSaveFile 兼容旧档；core 472 + typecheck ×4 + desktop build 绿【待验收/待发布】。
- 2026-09-08：**精炼配平（两轮定稿）+ 工业页净收益估算（大鲸鱼一号；船长确认；docs/design/refine-balance-50.md）**——玩家反馈"精炼炉收益没扣材料成本"（查证为估算口径问题，引擎每批本就实扣料）：**一轮** 14 条配方按「无技能 ≈ 持平」上调 + 工业页精炼卡 ≈ISK/h 改净口径（扣耗料、负值警示、tooltip 列毛产值；回收卡不变——残骸不可卖无耗料）+ content-check 配平护栏；**二轮（船长复核：工业技能收益过肥）** balance.refining baseRate 0.5→1.0、maxRate 0.95→1.6，配方 perOre ÷2 等价迁移（无技能产出与一轮逐单位相同），满技能净 +82~110% → **+60%~+79%**（技能每级收益约减半）；玩家口径「收率」全量改**「产出倍率」**（skills/工业页/手册/README/词典注销旧词/content-check 与 core 测试同步为 100% 基准）；回收线技能不动（回收 vs 采矿满配差距扩为另行议题）；balance-check 顺带适配组装机劳动者制签名；core 472 + typecheck ×4 + content:check + build 绿【待验收/待发布：推送闸门内，公告稿待船长审核】；组装机收益调整 = 二号分工不涉及。
- 2026-09-08：**取消每次制造费（二号；船长拍板"费用优先"，范围 = 仅每次开工的制造费；蓝图学习价与材料链另行议）**——引擎 `startManufacturing` 不再校验/扣除 `buildCostIsk`（BuildSpec/蓝图数据字段保留为历史遗留并注释）；取消制造/完成日志与提示去"制造费不退"；工业页蓝图卡（制造费行 →"制造免费：只耗材料与时间"）与面板说明、手册蓝图书条目与帮助词条、市场蓝图书悬停、活动栏取消按钮/toast、renderer engine.ts 注释同步；core 测试改口径（开工分文不扣、钱包不足不再拒开工、材料不足拒绝不变）+0 条 472 绿 + typecheck ×4；C2 护栏改"现货 ≥ 材料成本"并按材料基价重估：自制 ÷ 现货 = 0.17~0.61×、21 条全无倒挂（原含费 105% 巨灵鲸级 → 0.61×，试算见 c2-craft-cost.md §六；glossary/roadmap 同步）。状态：**待船长验收（2026-09-08）**。
- 2026-09-08：**市场站内让利吸收（二号；船长确认四问全推荐项 + rare/奇货 F 按字面 0.3/0.1 口径）**——卖侧吸收量与价格挂钩：每窗总吸收保底 = 基础吸收额 F×折价倍率 E，**先吃簿、差额站内补、不叠加**（吃簿 ≥ 配额时不站内收）；E = min(5, 1 + 0.4×折价百分点)（折 1% ×1.4 / 5% ×3 / 10% ×5 封顶，更深让利不再提速）；F：池商品 = supplyFlow、common 单件 = 1、rare = 0.3、奇货 = 0.1 件/窗（per-good 可覆写 absorbQtyPerWindow）；只对挂价 ≤ 现行买盘价（buyPrice 价线）的卖单生效；结余 absorbCredit 小数跨窗结转、撤单即弃（PlayerOrder 可选字段零迁移，save 白名单补读）；税/声望加成/池入账同簿成交，大额让利抛售计入 netVol → 冲击压价自稳；市价大单吃穿簿后的余单自动按边际价享受提速；MarketPage 卖出单价行加动态提示（保底/当前让利 % 与吸收 ×E）；market.test +6（平价保底不叠加 10 窗精确清完、折价 12.5% 封顶、折价 5% ×3、rare/奇货小数结转、p>b 不接 + 撤单退回）；core 478 绿 + typecheck ×4；定稿存档 = docs/design/market-sell-absorb.md（glossary/roadmap 同步）。状态：**待船长验收（2026-09-08）**；挂账：买侧价格档 0.6/0.65/1.0、单件收购件数、抢单系统、公告发布。
- 2026-09-08：**市场收购侧改版：档位 + 簿面件数 + 两侧抢单（二号；船长确认四问全推荐项）**——①收购档位（demandMultiplier 语义改档位，marketCatalog 127 行脚本化迁移）：原料池 1.0L 不变、池耗材（弹药/修理组件/无人机 9 条显式）0.6L、common 单件 0.6L、rare 0.65L、奇货 1.0L 全价回收；单件缺省按 rarity 兜底（BUY_TIER，导出 acquisitionFactorOf 供 UI 同源）；防套利不变式 = 收购 ≤1.0L < 供应、倒卖亏税；②簿面件数放大：common 单件收购单 ×1→×3/张、rare ×1→×2/张（奇货/池不变）——平价流速 ≈2.6/窗 仍低于折价上限，让利提速不失效；③两侧抢单：卖单挂价 > 收购价线 → 每 60s 窗 30%·e^(−6r)；买单挂价 < 供应价线 → 20%·e^(−14s)（r/s = 相对价线偏移），命中成交 1 件 @ 挂单价（日志"巡游采购/巡游供货"；钱包不足跳过；balance 4 参数 snatchSellChance/Decay·Buy*）；窗口顺序 簿撮合→抢单→让利吸收，越线与吸收/簿互斥；④测试：档位真值表（含 fallback）、开盘簿 qty3 与 rare spawn qty2@0.65L、卖侧 +10% 溢价 200 窗统计 ≈33 件、买侧 −5% 统计 ≈20 件、既有断言更新（helpers/events/v10 档位同步、蓝图书回卖 600、卖船 72k、AI 核心簿 qty3、高挂改赌巡游）；core 482 绿 + typecheck ×4 + content:check + build 绿；定稿存档 = docs/design/market-acquisition-rework.md（glossary/roadmap/market-sell-absorb 挂账段同步）。状态：**待船长验收（2026-09-08）**；挂账：公告稿（数值向更新，验收后另出待审稿）→ **✅ 2026-09-08 公告 `2026-09-08-market-manufacture` 船长批准写入（v2：收购价/让利不写具体数值）；随批推送 origin**。
- 2026-09-08：**市场批审核修复（二号自审）**——①UI 与引擎通道基准统一：导出 `buyLineOf/askLineOf`（buyPrice/sellPrice 价线同源；吸收/抢单/簿 jitter 三基准合一），MarketPage 默认价与四态提示全部改用价线（原来用簿最高价 quote.buy 兜底，含 ±2% jitter 会在临界价让"平价档"实际落入抢单/吸收错位）；②数据/工具过时注释清理（marketCatalog 回卖 0.5×L/半价/亏 6% → 档位语义；content-schema 列说明）；③档位用例补 buyLineOf/askLineOf 同源断言；经济平衡复核（探针）：自制平价卖站净收益 civ/MK1 ≈ +10~21%、MK2 +48~82%（70~100 分钟/件 ≈ 0.7~1.3k/min）、MK3 +52~75%（5~7 小时/件）、弹药 动能 +19%/能量 +10%/爆破 −5%（10 秒/批，旧 1.0L 时动能 ≈1.8k/min → 新 ≈0.4k/min，压 4 倍达成"造弹卖站无利"方向）；远低于采矿/悬赏费率，无印钱；core 482 绿 + typecheck ×4 + desktop build 绿 + playthrough-sim 冒烟 0 异常。随批并入本批次，状态同为**待船长验收（2026-09-08）**；✅ 随 `2026-09-08-market-manufacture` 公告同批推送 origin。
- 2026-09-08：**离线报告：AI 核心作业清单 + 预估收入（大鲸鱼一号；船长定）**——离线结算内存统计（settleStats.ts，不落盘零迁移）：按核心类型统计 AI 采矿返港卸货趟数/打捞完成次数/AI 精炼与回收炉批数/AI 制造完成件数；预估收入按站内收价与市场基准价粗估（残骸与碎片不计）；advanceGame opts.settleStats 透传三处完成点（正常推进零开销）；离线简报「AI 核心作业」小节 + 日志单条携带；测试 +3；core 485 + typecheck ×4 + desktop build 绿【待验收/待发布】。
- 2026-09-08：**连续作战保险：带伤预警 + 战内自动撤退（二号；船长拍板方向 + 四问确认）**——玩家反馈连续战斗（巡回讨伐）偶发带伤下场失利→弃船损失（既有保护只覆盖开战前，战内无止损点）：①**带伤预警**（只改展示）：悬赏胜率 = 原显示（满耐久基准）− 预计装甲损耗×10pp − 预计结构损耗×25pp（新参 winPenaltyArmorPerFull/winPenaltyHullPerFull；steadyPreview 抽出与 battleWinPreview 同源；导出 bountyDamageForecast/bountyWinPercentGuarded；结算与 AI/模拟口径 battleWinPreview 一律不变）；②**战内自动撤退**（仅巡回场次）：battle.hullEscapeFrac=0.5 由 beginBattleAt 按 autoLoop 挂载，advanceBattleFor 每步检查结构剩余 <50% → autoEscaped 中止 → advanceExpedition 走撤退结算（retreatBattle 重构为共用 settleBattleRetreat(mode)：承伤写回/半损骰（最低1%）/下限 5% 绝不弃船/维修费/**终止连续出击**/转返航）；玩家实时战斗与低安遭遇不挂载（不抢操作）；③**巡回自动再出发门槛提前到装甲**：装甲或结构 <50% → 修理组件自动修补目标 0.5→**0.6**，不足停环（防开局秒退缓冲）；④**动力随损伤 = 维持现状**（agility 不参与承伤/脱离判定，无 >50% 结构损失路径——船长条件判定）；AI 副船远征停用/隐藏 = 挂起另任务（本批不动 AI）；UI 悬赏卡胜率 + 悬停（预计装甲/结构损耗说明）；expedition.test +4（预警自洽/装甲门槛停环/修补 60% 再出发且 battle 挂 0.5/巡回场损失过半自动撤退保船停环返航）；core 486 绿 + typecheck ×4 + desktop build 绿；定稿存档 = docs/design/bounty-insurance.md（glossary/roadmap 同步）。状态：**待船长验收（2026-09-08）**。
- 2026-09-08：**顶栏「QQ 讨论群 621172698」入口（二号；船长 2026-09-08 指示）**——顶栏右侧新增「💬 QQ群 621172698」按钮（复用 app-btn 同级样式）：点击复制群号并在按钮上短暂显示「✓ 群号已复制」；title 提示"到 QQ 搜索群号即可加入"。
- 2026-09-08：**AI 副船远征：UI 彻底隐藏收尾（二号；船长 2026-09-08 定：推进挂起任务）**——引擎侧维持 2026-09-05 软下线（assignAiExpedition 一律拒绝、历史任务取消归还核心，代码/注释保留待恢复）；本次隐藏全部玩家可见入口与过时文案：①ShipPage AI 指挥中心移除「远征任务」指派选项与整套目标/成功率表单（mode 收窄 采矿/打捞；执行中列表远征行改为防御分支注释）；导航 title 去"暂停受理"提示；②手册「AI 副船」词条、赏金猎手学技能描述（去"含 AI 副船远征"）、隼枭舰船描述（"护送 AI 副船行动"）清理；③备注补全：ai.ts 头注 / renderer assignAiExpeditionAt 注释（含恢复步骤）/ ai-design.md 状态注 / glossary favor 口径行更新；恢复 = 删引擎前置返回 + 加回 ShipPage 表单即可。desktop typecheck + build 绿。
- 2026-09-08：**技能 rank 分散化：相似效果错开级别（二号；船长观察"多个相同 rank 技能效果相同" + 三轮澄清确认）**——①范围：同效成对（拆半双修线）与跨域同型技能；保留双技能结构只改 rank（数值/接线/文案零改动；rank 仅驱动训练时长 = 档底 × rank × 2^(级−1)）；②表 1（同值成对）**上调**错开 → r3：贸易谈判学（对会计学 r2）/残骸富集识别学（对富矿勘探 r2）/打捞装置整备学（对采矿护卫舰 r2）/装甲调谐学（对护盾调谐 r2）/信号过滤学（对信号分析 r2）/火控阵列学（对索敌统合 r2）；③表 2（同乘区异值补强对；船长：放弃上调、改为下调）**下调**错开：批量生产学/组件标准化/货舰操作 r3→r2、货舱管理学/空间站协议学 r2→r1、重装舰操作 r3→r2（主轴 工业理论/材料学/深空物流/维修工程学/船体加固理论 保持原档）；④无市场/战斗域整体偏好（草案撤销）；无 r4 新增；⑤明确不动：主轴技能、炮族与三族专精（族限互斥可选）、产量互补系；例外登记：循环 −3% 三兄弟中副船整备学与打捞整备学同 r3（AI 域组合罕见，全异秩仅余 r4=67h 不建议）；⑥验证：core 489 绿 + typecheck ×4；D1 体检重跑与改前一致（改动技能不在 A/B/C 训练链内，纯生产策略基线不受影响，拉平目标仍挂起）；定稿存档 = docs/design/skill-rank-dispersion.md（roadmap 同步）。状态：**待船长验收（2026-09-08）**。
- 2026-09-08：**新手教程新增「出售物品」步骤（二号；船长反馈：交付后的矿石不自动卖、玩家不知如何出售）**——插入独立教程步骤 3「出售：把矿石换成 ISK」（编号顺延：1 采集 → 2 交付 → **3 出售** → 4 修复 → 5 试炼 → 6 技能归档 → 7 分身 → 8 收尾 → 99）：交付只扣 20 单位、剩余矿石留在仓库不自动售出——教程先教学去「物品页·仓库」点富凡晶石「市价卖出」（可只卖一部分留作材料）；①core onboarding：新 ONB_SELL=3（常量全量顺延），交付完成记录 oreSellBaseline（交付后余量，可选字段零迁移，save 白名单透传），自动推进 SELL 段 = 仓库富凡晶石 < 基线（卖出 ≥1）→ ONB_REPAIR；②UI：App TUT_LOCK/锁界/技能归档聚焦全改常量键并加步骤 3（只开 items 页——矿只减不增判定稳定），TutorialGuide 新增步骤 3 卡片与光圈目标（物品/仓库/市价卖出），ImportantTasks 显示窗口常量化；交付时仍切回隼枭（维修对象语义保留）；③测试：t24 更新（交付 → SELL 未卖不推进 → 卖 1 推进修复 → 基线清空；技能归档断言语义化）core 489 绿 + typecheck ×4 + desktop build 绿。状态：**待船长验收（2026-09-08）**。
- 2026-09-08：**技能限制句清理：上限/下限护栏拆除（大鲸鱼一号；船长 2026-09-08 定：③ 文案全改、除材料学外机制同拆、抗性 90% 上限保留）**——①文案：skills.ts 21 处去「至多缩短/至少保留/封顶/合计最多/总下限」工程句（规避机动学与护盾/装甲调谐学改装备同款「乘入制、上限 90%、多来源收益递减」句式；精炼/高级回收改「双修满级合计 160%」；采矿护卫舰/工业理论/武器装填/无人机整备/高效学习法/残骸回收学/打捞装置整备学/信号过滤学/维修工程学×空间站协议学/会计学×贸易谈判学/弹药集约学等按行删句或改「与 XX 乘算叠加」）；②引擎护栏拆除：mining 循环 minTimeRatio、manufacturing 制造 minTimeRatio、combat 装填/无人机装填 0.6、training 0.6、explore 总下限 0.4、shipyard 维修合计下限 0.4（双满级实付 0.375）、industry 工业自动化/回收批 0.6 与 refineRate maxRate、market 税防负 min(1,·)（balance/types 相应字段同步删除，docs 三处同步）；③保留：材料学全部机制（0.7 乘区下限 + 单种至少 1 单位）+ 护盾/装甲调谐 90% 上限 clamp（与装备同体系）；④安全论证：每技能封顶 5 级、每级削减 ≤10%，多源乘算最坏乘积 0.375>0，无归零/负值；⑤测试：skills-effects 维修双技断言随新口径更新（×0.375），core 496 绿 + typecheck ×4 绿【待验收/待发布：推送闸门内】；工业自动化（3-4 行）引擎下限已拆、文案待 ⑤ 定稿后随批落地。
- 2026-09-08：**卷A2 ④⑤⑦ 落地 + ⑧⑬ 挂起（大鲸鱼一号；船长拍板：④ 方向 A / ⑤ 方案① / ⑦ +20%/级；⑧⑬ 挂起待重置）**——④ 采矿技术文案改「矿藏采集的核心技术：矿石、气体与冰矿的采集循环产量每级 +6%（与星质地质学、深空采集学乘算叠加）」（机制本就全矿生效，仅纠文案）；⑤ 工业自动化扩手动：精炼炉（炉心熔炼学之后再乘一区）与组装机（工业理论×批量生产学之后再乘一区）手动与 AI 核心驱动同享 −5%/级，AI 仍先 ÷核心效率（industry.ts startRefineRun / manufacturing.ts startManufacturing 分支重构；skills 工业自动化文案定稿「自动化作业调度：精炼炉与组装机…手动与 AI 核心驱动同享」）；⑦ 离线作业管理学 +8%→**+20%/级**（simulation.ts capEff = capMs×(1+0.2×级)，满级 16 小时；skills 文案同步）；⑧ 建筑工程学效果应用面太窄 → **挂起等后续重置**（含 −40%→−29% 文案纠错一并缓办）；⑬ 星际奇遇学整行（含漂流物打捞学去「远征」词文案）**挂起**，随卷 B 重置批次（事件/奇遇议题）处理。测试：skills-effects 自动化用例改手动同享（4500ms）、离线场景 20h 离档 → 满级 16h；manufacturing +1（手动线 450 秒）；core 497 绿 + typecheck ×4 + content:check 绿【待验收/待发布：推送闸门内】。
- 2026-09-08：**卷B1 ⑧⑬⑫ 技能重置收口（大鲸鱼一号；船长 2026-09-08 逐条定稿）**——⑧ 建筑工程学从“交付计件放大”改为**建站建材需求直减每级 −8%**（满级 −40%、每档 ≥1；station.ts `tierNeedOf` 统一需求口径，旧档“计件放大”虚高已缴自动结算兼容，建站卡 UI 同源）；⑬ 星际奇遇学＝**在线随机事件间隔每级 −8%**（满级 ×0.6，事件密度约 +67%；rollGapMs 播种/续掷统一缩放），低安遇袭单次率 ×同因子 → **每小时遇袭期望不变**（船长定），出击出发遭遇 ×1.15/级 保留、文案改“在线事件间隔 −8% + 出发事件 ×1.15”口径；⑫ 事件分红学机制不动、文案加“娱乐向小彩头”声明；组装机产物属性悬浮＝船长误派（二号工作）已撤销回滚（856d349 不保留）；测试 +4（t9 满级 60/90 实耗建成与旧档虚高自动结算、events 满级因子 0.6 与同种子间隔 ×0.6）；core 501 绿 + typecheck ×4 + content:check 绿【待验收/待发布：推送闸门内】。
- 2026-09-08：**卷B2 ⑥ 富矿脉重做 + AI 副船返航速度修复（大鲸鱼一号；船长已逐条定稿口径，直接实现）**——①富矿勘探学改「概率触发 + 红利窗口」：每分钟基础触发率 **3%**（满级 6%，系数沿用 richVeinFactor=1+0.2×级），掷点按循环占用分钟数缩放 p=min(1,分钟×0.03×系数)（balance.richVeinChance 语义改 0.03/分钟、字段名与 0=禁用保留）；命中 = **连续 2 循环 ×3（含触发当轮）**（触发当轮置 rvLeft=1，次轮递减至 0）；窗口内不掷点、结束不写日志（只在触发写一条）；窗口与矿带绑定（换带/停止/作业结束清零，自动循环返航卸货回原带保留）；主控与 AI 每船独立（state.mining.rvLeft / task.rvLeft，旧档缺失按 0 零迁移不升版本）；长时间期望 ≈ +7.2%×系数/小时（旧 ≈ +1%）；②**AI 副船采矿/打捞返航腿不再 ÷核心效率**（与玩家一致：效率只加速出航/空船腿与循环周期，返航仍按货仓占比 scaledReturnMs；aiTaskView 视图同源）；③文案/工具同步：skills.ts 富矿勘探学描述、README 彩蛋行、tools/bounty-econ.ts 富矿期望（×1.01 → ≈×1.07）；④测试：mining +6 / ai +2 与返航时序 3 处改新口径、save 往返期望补 rvLeft；core **509 绿** + typecheck ×4 + content:check 绿【待验收/待发布：推送闸门内】。
- 2026-09-08：**卷B3 ⑨⑩⑪ 技能收口（大鲸鱼一号；船长逐条拍板：⑨-A / ⑩-A 效率 +2%每级 / ⑪ 淘货折扣 E1）**——⑨ 残骸富集识别学改「完好舰体当场直发」：命中率按打捞分钟语义（balance.intactHullRatePerMin 0.08%/分钟 ×1.2/级；与富矿脉⑥同哲学），命中不再翻体积 → 当场直发该敌群回收彩头（rollIntactHullLoot：基础件必中 1 件·主题优先 → 装备库；低安按 intactMk2Chance=0.04 掷 MK2 档；威胁 ≥17/≥41 掷蓝图碎片），主控/AI 同源、产物不占货仓、判定恒掷保时序（0=禁用）；⑩ 新增首个 **rank5「AI 核心调度学」**（ai-core-dispatch）：核心驱动的全部作业效率在核心档位之上**每级 +2 个百分点累加**（基础 40% → 满级 50%、伽马 60%/贝塔 70%/阿尔法 85%，封顶 100%；balance aiCore dispatchPerLevel；aiEfficiency 单点接线 → 副船与站内炉/线共源，返航腿不 ÷eff 天然不受影响）——叠加于二号「AI 核心统一启用上限」（a0fbb3e）之上，正交无冲突；⑪ 二手市场学改「淘货折扣」：**稀有供给单价格每级 −2%**（满级 −10%，含闸内 ×4 暗市同乘；**奇货不参与**——收购 1.0L 全价回收档，折价会击穿防套利不变式，文案写「稀有商品」与市场标签一致）；顺手修正现货抢购学描述残留旧口径（引擎 0.1/级 → 文案 ×1.1/满级 ×1.5）；测试 +7（⑨×4：必中入装备库/低安 MK2 与高安对照/禁用/ AI 同享 + 概率语义改写；⑩×2：效率累加 0.4→0.5 精确/满级循环 12000ms；⑪×1：双跑对照 0.9 与奇货 toEqual）；core **522 绿** + typecheck ×4 + content:check 绿【待验收/待发布：推送闸门内，公告稿待船长审】。
- 2026-09-08：**组装机同蓝图多线读档丢队修复 + AI 启用计数口径同源（大鲸鱼一号；玩家反馈两连）**——①根因：save.ts 读档归一化按 blueprintId 去重制造线（v21 同蓝图可多条线后已错误）+ 未保留 worker 字段 → 刷新后同蓝图第二条线丢失、AI 线变"旧作业"豁免（出库核心既不计数也不归还 = 永久占用）；修复 = 去 blueprintId 去重（多线保留、id 重新唯一分配）、worker 随档保留（缺省 = 劳动者制前旧作业，零迁移）、结构性坏条丢弃时归还其出库核心、防御重复 pilot 线；测试 +2 回归（同蓝图 AI 双线往返保留 + 坏条退核心；旧档无 worker 豁免保留）；②玩家反馈"顶部 ⚙ 徽标 22 与实际对不上"：徽标 = AI 副船 + AI 生产（炉/线，tooltip 已分行），AI 指挥中心「执行中(N)」的 N 含生产但列表只列副船 → 标题按行同源拆分「副船 N · AI 生产 +M（工业页取消）」、空态文案同步；根因未拿到玩家档无法完全实证，留待下版实测复核；core 524 绿 + typecheck ×4 绿【待验收/待发布：推送暂缓（船长 2026-09-08 指示）】。
- 2026-09-08：**残骸可到市场出售 + 独立「残骸」分类（大鲸鱼一号；船长定：档位一 50~60% + 推荐默认）**——①22 张悬赏残骸收购卡（marketCatalog WRECK_BUY_GOODS，按星系安全等级推基础密度定回收档）：收价 = 常 30 / 险 **40** / 危 **50 ISK/m³**（**2026-09-08 当日修正**：三档无技能拆解保底均 ≈57/m³，原报 45/60 实为 79%/105% 会击穿最低锚；修正后严格 <57 → 任何档直接卖都不如拆解，对"该档典型特色回收"约 50~56%，拆解 + 彩头 + 碎片 + 技能 ×1.75 仍更赚，卖站 = 折价清仓/应急通道）；池模型（poolTarget 30k / supplyFlow 500，窗口吞吐保障清几百 m³）；②**只收不卖**：引擎给 MarketGoodDef 的 `playerBuyable=false` 补全语义（此前仅买入侧巡游抢单消费）——NPC 常驻刷单不出售、稀有/奇货抽取不刷、市价买入返回 0、挂买单拒绝，防"低价买残骸→拆解套利"；③市场左栏类型过滤新增「**残骸**」单独分类（物品类含残骸者拆出，行徽标/悬停同显"残骸"，供应位显示"只收不卖"、买入页签禁用+回收点提示）；④文案：残骸物品介绍改"可市场按废料价出售应急或拆解（更值）"，工业页回收卡/离线统计注释/测试注释同步；content-check：item 卡解析改用动态物品表（残骸/碎片不入静态 31 件表）+ **残骸卡价格锚护栏**（档位表一致、收价 < 无技能拆解保底 57、只收不卖）→ 市场卡 149 → **171 张**全绿；测试 +1（只收不卖：120 窗供应簿恒空、收购簿常驻、buyAtMarket=0、挂买单拒绝）；core **526 绿** + typecheck ×4 + content:check + build 绿【待验收/待发布】。
- 2026-09-08：**巡游采购单次件数随贴线度 + 池量放大（大鲸鱼一号；玩家反馈"比买盘高 1 块却每窗只卖 1 件"；船长定方案 B + 数量与该商品池订单生成量挂钩）**——机制澄清：越线（挂价 > 收购价线）本就只剩巡游车道，概率衰减 e^(−6r) 早已"贴线惩罚小"，但单次成交固定 1 件对池大宗杯水车薪；补上数量维度并**按池量挂钩**：新增导出 `snatchSellFill(def, r)`——池商品单次 = round(supplyFlow × 档位比例)（贴线 ≤1% → 0.5、≤3% → 0.25、≤8% → 0.1、更远 1 件；supplyFlow 缺省 = poolTarget/120；例：氖云气 45/窗 → 贴线 ~23 件，矿石大池按比例自动放大）；**单件商品** 5/3/2/1；买侧供货维持 1 件/窗；任何档位仍远慢于平价簿 + 让利吸收（期望 ≈ 命中率 ~28% × 单次量 vs 平价簿 ≈2.55×supplyFlow/窗）→ 价线纪律与"让利换清仓"保留；市场详情卖出指引动态显示"命中一次约收 N 件（越贴近价线越多）"；测试 +2（按池量真值表单元 + 池商品 +1% 挂卖 200 窗销量 >700 显著超旧口径 ≈56）；core **528 绿** + typecheck ×4 + build 绿【待验收/待发布】。
- 2026-09-08：**无人机舱大改（大鲸鱼二号；船长 2026-09-08 Q1-Q7 与平衡参数逐项拍板；doc = docs/design/drone-bay-rework.md）**——废弃"战斗自动贪心装载"，改为玩家主动管理的「无人机舱」清单制：①引擎/存档：FleetShipState 新增 `droneLoad?: Record<string, number>`（数量制，save normalize 清洗：正整数/删 0/缺省无，旧档零迁移无版本号）；combat 单位构建**只放飞该船 droneLoad 清单**（不再读仓库/货仓贪心；伤害 = dmg×(1+Σ导控)×(1+无人机作战学 5%/级)、装填 2200ms×整备技能不变；CPU/舱防御截断保留，超量逆序剔除并 warn——正常由 UI 预占互斥避免）；equipment 新增导出 `droneCpuUsed/droneLoadM3/adjustDroneLoad`（清单 CPU/体积汇总 + 仓库⇄清单命令：逐型增减、舱容与 CPU 双校验、正数钳制），装配 fit/unfit 与 CPU 预算条把该船清单占用并入（装机/装无人机共用预算互斥）；renderer engine 接 `adjustDroneLoadAt`；②UI（FitPage）：低槽组下方新增「无人机舱」区——容量条（m³ 已用/上限（船体+甲板扩展）·清单 CPU，复刻 CPU 条视觉）+ 一型一卡 ×N（+/- 逐架微调、悬浮说明）+「全部卸下」清空 +「＋装入」占位卡 → 选型数量弹层（仓库余量 × 舱余量 × CPU 余量钳制）；无舱船不显示；装配信息行「无人机舱（含甲板扩展）上限 m³ · 战斗只放飞清单」；③全船机舱平衡（ships.ts 11 艘）：隼枭 30→**0**、伯劳/虎鲨 45/55→**10**、灰鲭鲨 85→**30**、大白鲨 130→**20**、陆龟 20→**40**、玳瑁 40→**50**、玄武 60 不变、飞鱼/旗鱼/剑鱼 15/20/30→**40/50/60**（工业/矿舰线维持 0）；④新船 ×2（无人机平台延伸，掠食者同系盾抗动能 50%）：**蜂群级**（sh-swarm armed t3 高5中3低2：180/90/108·cpu235·舱160·2600m³·620k 市场 rare 行）与 **哨兵级**（sh-sentinel armed t4·奇货 声望 6：高6中2低2：210/105/126·cpu320·舱320·3600m³·2.6M exotic 行）；content-check 船数护栏 19→**21**；⑤工具/文案同步：battle-calibrate 无人机行 D1-D3 改用新船（蜂群轻/中装、哨兵重装）+ droneLoad 清单注入（各行 = 装配后余 CPU×舱容合法满载组合）；make-test-save drone 档改 蜂群级·中装（驾驶，清单注入）/ 哨兵级·重装 / 灰鲭鲨·S2 炮流对照，无人机库存只放仓库；手册「装配」条目、物品页分类说明（kindExtraNote：无人机需从装配页无人机舱装入，仓库余量不自动出战）、FitPage 信息行等旧"自动放飞/无需装船"文案全量清理；⑥回港补足钩子 = 结构预留（停靠点 stub noop + 注释激活条件：未来无人机击毁/消耗机制需配套价格重锚与自制蓝图再激活）；⑦验证：core 测试改清单驱动（v18 pilot helper / combat 无人机作战学·整备学 / v181 往返共 7 处）→ **528 绿** + typecheck ×4 + content:check + desktop build 绿【待验收/待发布：推送闸门内；无人机数值方向（此前校准显示满技能超炮流）待船长用新船实测后定】。
- 2026-09-08：**市场成交静默 + 建站交付航线 + 市场蓝图标识 + 未建成副站彻底收口（大鲸鱼一号；船长 2026-09-08 定：A~C 按稿确认，D 改"移除所有未建成空间站功能、未完成前不视作任何站点"）**——①**成交静默化**：站内吸收（absorbViaStation）与两侧越线抢单（snatch）的成交日志全部与普通簿面成交同模板（不再出现"让利售出/站内收购/巡游采购/巡游供货"字样），MarketPage 价格指引删除吸收额/巡游概率等通道披露文案、只留普通撮合语义（挂价与收购/供应价线关系）——玩家视角看不出这些通道存在（保留让利/吸收机制与价格纪律本身）；market.test 断言全部改"挂单成交/挂单买入成交"前缀 + 新增静默专项；②**建站交付航线（真实航程）**：新指令「前往工地交付」（星图该星系行动区 + 建站任务卡工地卡，双入口同引擎指令）——前置：主船空闲且停靠在空间站（母港或已建成副站 = 协会基地网络）、工地未建成且所在星系已探明、仓库+货仓有可交付建材；出发 = 按真实航程（travelLegMs 换算、吃航行技能与 debugQuick 1s）驶往工地星系（transit.delivery.phase=to-site），到点野外停留 → **自动交付**（接受名单循环扣缴、逐档推进、日志同现场交付）→ **自动返航最近空间站**（to-station；若本次恰好建成 → 就地停靠新落成副站）；**随时可取消**（无惩罚，取消即立即返航停靠最近已建成站；顶栏活动显示"建站交付·前往工地/返航中"并带「取消交付」）；ShipTransitState 增可选 delivery{siteId,phase}（save 白名单透传、旧档回退 null）；出航期间 awayGalaxy=出发星系 → isAtHomeLike/站内门天然关闭；③**市场蓝图标识**：蓝图行与详情卡 chip 标注 已学习（learnedRecipes）/已获得 ×N（blueprintStock），均未持有显示"尚未获得"（新增 is-learned/is-stock 同族 chip 色）；④**未建成副站彻底收口（D 改版）**：stations.ts 档位解锁描述改"施工推进（功能统一在建成后开放）/建成一次性并入基地网络开放泊位与全部服务"；停靠只认已建成站（onArriveAtGalaxy 未建成一律现场停留、noteStationSiteAt 文案去"首档解锁泊位"）；引擎 advanceGame 逐 tick reconcileDockSanity 将旧档残留的未建成/未知停靠纠正为工地现场野外停留（幂等、只记一条日志）；shipyard 维修/换驾驶对未建成停靠一律拒办（含状态残留防御，建成后放行）；测试：t9 +8（前置校验/端到端自动交付并返航/建材足量一次建成就地停靠/途中取消与取消前置/空跑照常返航/纠正幂等/维修与换驾驶门禁）+ t8 断言补 delivery 字段；core **537 绿** + typecheck ×4 + content:check + build 绿【待验收/待发布：推送闸门内，公告稿待船长审】。
- 2026-09-09：**悬赏胜率预估改蒙特卡洛推演（大鲸鱼二号；玩家反馈"预估胜率和实际战斗不一致"；船长拍板：N=21 + 带伤修正内部吸收 + 不加临界文案；doc = docs/design/win-estimate-mc.md）**——旧玩家可见预估（bountyWinPercentGuarded）为稳态解析 + 外挂"带伤扣分"，探针实测（5 装配 × 7 档悬赏 × 9 种子）双向大偏差：多处显示 2%（guarded 下限）实际 9/9 全胜（S4 对穹顶、S1 对坟场），无人机装对赤潮/深渊门旧稳态 32~84% 而实测 0——失真根因 = 不结算护盾战中被动回充（2%/s）、敌方火力不随减员下降、无视接近期与动态距离、带伤开局按残余缩血但预估按满血；离线/连击胜负本就与在线同引擎真实模拟（非按预估掷骰）。①core 新增 `winEstimate.ts`：评估快照（buildEvalState：新档 + 驾驶船同装配/无人机清单/耐久 + 技能 + 教程进度复制；弹药足量）上按独立种子跑 **N=21 局真实模拟**（startBattleFor+advanceBattleFor：残余开局/距离机动/近盲/盾回充/减员/超时血比全内建）取平均 = 胜率与平均损耗——**带伤影响内建，旧 winPenalty 扣分公式退役不叠加**；同输入可复现、不污染真实存档 rng/弹药；②renderer engine 胜率缓存：战力指纹（驾驶船+装配+无人机清单+技能+耐久）变化 → 全板分帧预热（每批 ~60ms、节流 ≥400ms、交火期跳过；换船/换装/技能升级/维修/损伤 ≤1s 自动触发；平时心跳零成本）；`winEstimateOf` 供悬赏卡读取，未就绪短暂回退旧口径后随批变准；③UI：悬赏卡胜率/预计损耗改读缓存，tooltip 去失效"带伤预警口径"字样改"按当前船况实测推演…单局仍有随机波动"；`bountyWinPercentGuarded/bountyDamageForecast` 保留为回退/活动视图/测试兼容并标注退役；`battleWinPreview`（稳态）维持供 AI 门槛与离线模拟工具——两套口径注释互指；④验证：探针 MC(21局) vs 实测(9 种子) 70 格绝大多数差 0、最大 ±14pp（独立样本抽样误差内）——"2% 假必败"与高估格全修正；core 新增 winEstimate 用例 +6（必胜=1/必败=0/可复现/runs/不污染存档/快照战力复制）→ **543 绿** + typecheck ×4 + desktop build 绿【待验收/待发布】。
- 2026-09-09：**舰船归类修正：鲸盟大型货舰分出 + 徽标「工业」→「采矿」+ 技能名同步（大鲸鱼二号；船长反馈"弓头鲸和巨灵鲸是货船却和矿船同类，鲸名易误导"后三轮拍板：归航运族 / 改名蝠鲼级·皇带鱼级 / 保留鲸盟制造设定 / 顺带徽标改采矿）**——①`ships.ts`：`sh-bowhead`（弓头鲸级货舰 → **蝠鲼级重载货舰**）与 `sh-colossal`（巨灵鲸级旗舰货舰 → **皇带鱼级旗舰货舰**）role `industrial → hauler`（挖矿不再吃「工业舰操作」、改吃「货舰操作」+5%/级货舱加成；鲸盟护盾抗动能 25% 族性保留，desc 去"工业级大型货舰"字样保留"鲸盟总装"），鲸盟线剩 6 艘纯矿舰（沙猫/掘洞/鲸吞/开拓/鲸王/座头鲸）；②`labels.ts` SHIP_ROLE_LABELS `industrial: '工业'→'采矿'`（role id 不变、存档零迁移，徽标/图鉴/详情/手册全部自动跟随）；③技能显示名对齐：`skills.ts` 工业舰操作 → **采矿舰操作**、描述"工业舰族/工业族"→"采矿舰族/采矿族"（id industrial-ops 不变 = 训练存档键不可动），`mining.ts` 注释同步；④`shipBlueprints.ts`：蓝图名随船改「皇带鱼级舰船蓝图」+ 顺手校正过时描述数值（座头鲸蓝图旧稿"货舱 9000/10s/40u" → 实际 19000/30s/140；皇带鱼蓝图旧稿"26000 m³" → 实际 36000，均玩家可见文案）；⑤文档：ships.ts 头注与鲸盟段注、README 四族线提法、v18-slots 草案表两行归属同步；⑥验证：content:check（role 分布 industrial=6/armed=7/armored=3/hauler=5、tier 血均值护栏不破）+ typecheck ×4 + core **543 绿**【待验收/待发布】。
- 2026-09-08：**建站交付航线 v2 修订（大鲸鱼一号；船长追问"到点扣缴遇仓库变动会白跑"后定稿：物理载货 + 自动循环）**——①**出发装载**：一键启动即把货仓装满建材（仓库按名单顺序装载，上限 = min(空闲 m³, 全站剩余需求)，保留船上原有货物；仓库无料 / 货仓满载 → 拒绝出发并提示原因）；②**到点只清空本趟装载**：deliverStationResources 增 `opts.cargoOnly`（只扣驾驶船货仓、不补扣仓库——"航行中仓库被 AI 精炼/资源订单/自用消耗"导致的白跑结构性消除；旧版在途档无 loaded 时按货仓现存接受建材兜底，不碰仓库）；ShipTransitState.delivery 增可选 loaded 明细（save 白名单透传）；③**自动多趟循环**：返港后工地仍有缺口且仓库能装料 → 同一引擎推进内立即再出发（逐趟真实航程），直到①全部档位建成（就地停靠新落成副站）或②仓库建材耗尽（终止：事件日志 warn + 一次性 `state.deliveryNotice`，渲染层引擎心跳/离线结算后读取弹窗并清除，零迁移不落档）；④**取消 = 停止整个循环**（无惩罚、立即返航停靠最近已建成站、本趟装载随进港卸回仓库，之后不再自动续趟）；UI 同步 v2 文案（星图行动区与建站任务卡：仓库建材量 + 货仓空闲 m³ + 循环语义；活动栏取消文案；手册「副空间站」条）；测试：t9 交付航线组改写（出发即装货/途中货物随船不受仓库变动影响/只清本趟装载/小货仓 60 自动 5 趟建成/仓库耗尽终止提示/取消停止循环不续趟/满载与空仓拒绝）+ t8 兼容断言；core **537 绿** + typecheck ×4 + content:check + build 绿【待验收/待发布：推送闸门内，公告稿待船长审】。
- 2026-09-08：**奇货买侧"巡游供货"专项（大鲸鱼一号；船长观察"玩家 43W 挂单买到鲸王级奇货蓝图"后定·试跑口径待实测复核）**——买侧越线抢单对奇货此前与普通商品同规则（基准 = 供应价线 ≈L，挂价低于 L 即按 20%·e^(−14s)/窗凭空供货）：玩家可 0.7~0.9L 低挂持续截胡奇货（43W 挂单 ≈1 张/4.4h，约为自然抽取节拍的 3~5 倍；贴线 5~10% 挂单可达自然出率的数十倍），并与奇货"收购 1.0L 全价回收"构成折价买→全价回售套利面（击穿当初二手市场学折扣排除奇货的防套利设计）；修复 = 奇货挂单买入的**越线基准单独改为 20L**（matchPlayerOrders 买侧分支：rarity=exotic 时 ask = L×20，稀有/普通仍按供应价线；卖出侧不变）：正常挂单相对 20L 偏离 s≈95%+ → 每窗命中 ≈0.2·e^(−13)~3e−7 量级（约数年一见），奇货供给实际回归 10 分钟抽取节拍（每件每窗 0.8%×现货抢购学），同时保留天文级小概率而非硬禁；静默日志口径不变；market.test +1（奇货 0.9L 低挂 1000 窗零成交 vs common 对照组照常供货）；core **544 绿**（52 文件，含二号 09-09 蒙特卡洛批次）+ typecheck ×4 + content:check + build 绿【待验收/待发布：推送闸门内，公告稿待船长审】。
- 2026-09-08：**奇货低挂买单的玩家提示（大鲸鱼一号；船长定：20L 修正不进公告；挂奇货买单低于 L 时给提示与合适价位，不披露 20L/巡游机制）**——MarketPage 详情「买入」侧价格指引：奇货挂价 ≥ 参考价（供应价线 ≈L，文案"奇货参考价"）→ 提示"协会奇货到货（行情价上下浮动）时直接按单成交"；挂价 < 参考价 → 提示"奇货只按稀见到货，挂太低很可能长期无人接单——建议挂到参考价附近（X ISK 上下），到货即按单成交"（无 20L/巡游字样，纯行情向设定文案）；「挂买单」成功 toast 同步附加同口径提醒（仅奇货且低于参考价时）；稀有/普通商品指引不变；typecheck ×4 绿【待验收/待发布：推送闸门内】。
- 2026-09-08：**工业产能扩容：AI 核心阶梯化 + 工业专用工位（大鲸鱼一号；船长观察"站内生产因 AI 核心上限而整体削减"后定稿：现有技能不动、ai-expert 降 rank2 更名、原「工业自动化」改名让位、新 rank4「工业自动化」每级 +2 工业专用工位）**——①技能表：ai-expert「人工智能专家」→「AI 核心操作学」（rank4→**2**，60 秒档入门，机制不变每级 +1 共用上限；id 不变零迁移）；industrial-automation「工业自动化」→「产线节拍学」（rank3 不变，名字贴合 −5%/级 周期效果）；新增 rank4「工业自动化」（id industrial-ai-cap，工业组）：AI 核心驱动的站内精炼炉/回收炉/制造线在共用上限之外**每级 +2 枚工业专用工位**（满级 +10；仅产业、不占副船名额、每工位仍占一枚实体核心）；②引擎：ai.ts 新增 industryAiBonus（balance.aiCore.industrySkillIds×industrySlotsPerLevel——未来新增工业 AI 扩容技能即往表里追加）+ aiCoreCapBlock(state,ctx,scope=ship|industry) 分路：副船仍按共用上限，站内三处启动（industry 精炼炉/回收炉、manufacturing 组装机）按 共用上限+扩容；③UI/文案：ShipPage AI 指挥中心右上角 启用 X/Y(+N 工业)、上限说明与空态引导补扩容提示；手册「AI 副船」、教程、核心日志（onboarding 特典）、注释、tools 注释全量更名（旧公告历史保留原名不动）；④测试：ai +1（扩容真值表 + scope 分路：共用满而扩容余 → 副船拒、产业放行）；⑤定稿存档 docs/design/industry-ai-slot-expansion.md（glossary/changelog 同步）。状态：**待船长验收（2026-09-08）**；公告稿待船长审核，推送闸门内。
- 2026-09-08：**工业工位收益体检与展示收尾（大鲸鱼一号承接二号；船长逐轮拍板：只升不砍 / 净率+周转双口径 / 精炼无技净率≈20% 且满技在原数值上下调约20% / 组装机收益率下限≈20%）**——①**精炼炉**：基础产出倍率 100%→**120%**（无技能净率 ≈0 → ≈+20%），技能每级加半（精炼学 8%→4%、高级回收 4%→2%），满级倍率 160%→**150%**（满技净率 ≈+55%，原 60~79% 上下调约两成）；技能描述/工业页产出倍率行/手册「精炼」条/items-industry 头注同步；content-check 护栏改 1.2×[1.15,1.35]（低批量 floor 步长允高，玄晶等）；industry/v10 数值断言随 1.2 倍更新；②**组装机低周转行只升不砍**：净率<20% 的 7 行现货 basePrice 上调至 ≥20%（mod-miner-1 30k→31.2k、mod-cargo-1 28k→29.2k、动能/爆破/能量弹 6/7/8→7/8/9、修理组件 civ/mil 3k/21k→3.3k/23.1k）——高收益行（舰船/中高档装备）不动、无倒挂（自制÷现货<1）；③工具/展示收尾：manufacture-econ 头注同步（产线节拍学、单位工位口径、扩容不计入单线）；组装机卡净 ≈ISK/h 随价自动更新；④定稿存档 docs/design/manufacture-refine-worker-econ.md（glossary/changelog 同步：产出倍率再定 + 工业工位口径行）。状态：**待船长验收（2026-09-08）**；公告稿待船长审核，推送闸门内。
- 2026-09-08：**市场/活动栏/换船交互实测修复五连（大鲸鱼一号；船长实测反馈）**——①市场详情不再单开「已学习/已获得」横幅（列表行 chip 保留，移除 app-mkt-detail-badges）；②详情切换买入/卖出**保留已填单价与数量**（仅打开时按价线预填一次，盘口行点击预填保留）；③声望不足等门槛原因挂买单时明示原因（不再笼统报"价格或数量无效"，lock/bmGate 先检）；④活动栏：重复清剿进行中不另开独立活动行——返航中停止入口并入本次讨伐行（按钮"停止清剿"，不影响本趟返回）；玩家向术语「连续出击/连击」改拍定稿为**「重复清剿」**（09-06 稿"巡回讨伐"旧称废止；核心/UI/测试/glossary §九 全量同步）；⑤**返航（expedition.phase=back）中允许切换驾驶**：本次返航转旧船"远征善后"账本（shipReturns 新增可选 reason=expedition，save 白名单透传零迁移；按剩余航程到港自动卸货入仓库），并立即停止重复清剿；出击/交火中仍拒绝切换（文案补"召回"）。验证：core 545 绿 + typecheck ×4 + content:check + desktop build 绿【待船长验收/待发布】。
- 2026-09-08：**工业收益批次数值修正（大鲸鱼一号；船长纠正口径："是技能本身的加成效果下调 20%，不是最终倍率"，组装机同口径）**——上一条"每级减半 +4/+2、满级 150"作废，改为技能本身每级加成下调约 20%（就近取整）：精炼学 +8→**+6%**、高级回收 +4→**+3%**（基础 120% 不变，满级倍率 165%）；组装机时间技能 工业理论 −5→**−4%**、批量生产学 −4→**−3%**；材料技能 材料学 −2→**−1.5%**、组件标准化 −1→**−0.8%**（产线节拍学不变）；技能描述/工业页/手册/头注/工具常量（T_FULL 0.68、M_FULL 0.888）同步；skills-effects 两处断言按 0.925/0.96/0.85 更新；docs 定稿修正（refine-balance-50 附注、manufacture-refine-worker-econ 二节、glossary 两行）。core 545 绿 + typecheck ×4 + content:check + build 绿【待验收/待发布：推送闸门内，公告稿待船长审核】。
- 2026-09-09：**舰船尺寸分级重构 + 掠食者巡洋舰线（大鲸鱼二号；船长多轮拍板：全船统一 5 档 / 质量分级 / armored×0.65 修正 / 稀有度同型内上调不改档 / 海鲜命名统一 / 巡洋级入奇货 / 主力以上本批不入场 / 战斗内容完成前不合并；doc = docs/design/ship-size-tier-rework.md）**——①**尺寸大分类**：护卫 T1 / 驱逐 T2 / 巡洋 T3 / 主力 T4 / 旗舰 T5(等效质量 = massKg×(armored?0.65:1);区间 0.4~2.4 / 2.4~5.8 / 5.8~13 / 13~29 / ≥29M),21 艘全量重排:护卫 T1×5(鲣鱼/沙猫/马鲛/虎鲨/掘洞)、驱逐 T2×6(陆龟修正后 3.9M/梭鱼/飞鱼/大白鲨/鲸吞/开拓)、巡洋 T3×5(王鲭/旗鱼/座头鲸/玳瑁/鲸王)、主力 T4×3(剑鱼/蝠鲼/玄武)、旗舰 T5×1(皇带鱼);旧规格词(炮舰/巡舰/母舰/艇…) = 子分类称号保留在船名;②**海鲜命名统一**(鸟名淘汰):隼枭级武装艇→**鲣鱼级护卫舰**、伯劳→**马鲛级护卫舰**、蜂群→**梭鱼级无人机护卫**、哨兵→**王鲭级无人机母舰**(id 不变零迁移;onboarding 日志/教程/公告/校准工具/测试档全链改名);③**新增掠食者巡洋舰线 4 艘**(全 T3·奇货):长尾鲨级导弹巡洋(sh-thresher 3.4M@8)、电鳐级激光巡洋(sh-electricray 3.6M@8)、锤头鲨级炮击巡洋(sh-hammerhead 3.8M@9)、牛鲨级突击巡洋(sh-bullshark 4.4M@10)——槽 5/4/2~5/3/4、盾 220~270、cpu 345~390;content-check 护栏 21→25;④**UI/文案**:labels 新增 SHIP_SIZE_CLASS/shipSizeLabel,定位行 =「武装 · 巡洋舰 T3」(图鉴/详情/手册同源);词典登记尺寸词 6 条;desc 部门名统一「掠食者武装部门」;ships 头注补协会部门世界观;手册速览新增「势力与舰船」条目;⑤**校准**:battle-calibrate 增 T3 巡洋行(MK3 满配全技能对 96 顶:电鳐 26s/牛鲨 32s/锤头 39s/长尾鲨 53s,对照 S4 38s——激光强/导弹慢,**数值方向待船长实测复核**);make-test-save 注册 cruiser 巡洋实测档(四艘满配+声望 13+全星系)并生成;⑥**渠道口径**(记录待船长终审):武装线巡洋 T3+ = 奇货(已就位);矿/货/重装工船例外不随尺寸入奇货;大白鲨 = T2 驱逐·炮舰型·奇货精装保留;⑦公告待审稿入档(2026-09-09-ship-size-tier,未批准不写入);⑧验证:typecheck ×4 + core **545 绿**(合并一号工业批后)+ content:check + desktop build;已一次性 ff 合入 main【待验收/待发布:推送闸门内;公告稿与巡洋数值方向待船长】。
- 2026-09-09：**建站收购网络扩容（大鲸鱼二号；玩家反馈"后期市场流动量太少、库存移除严重"，船长拍板：建成副站后玩家卖出侧提升——乘法叠加每站 ×1.5 无封顶，仅单件商品、池品不动；doc = docs/design/market-station-sell-expansion.md）**——①`balance.market.builtSellMulPerSite = 1.5`；helper `builtStationCount`/`builtSellBoost`(= 1.5^N，N = stage≥tiers 的建成副站数)；②应用点：`refreshGoodOrders` 单件商品 NPC 收购簿（common 单件 3 件/张、rare 3%×2 件、exotic 1%×1 件 → 出现率与件数各 ×boost，概率防御上限 0.9）与 `absorbViaStation` 站内吸收补差配额 F（common 单件 1 / rare 0.3 / exotic 0.1）→×boost；**池商品分支跳过**（矿石/矿物/弹药等大宗不乘，防冲击挖矿经济）；absorbViaStation export 供测试白盒；③测试 +3：boost 真值（0/未建成/2 站 = 2.25）、common 单件吸收一窗 0 站 1 件 vs 2 站 2 件、池品吸收 0/2 站同 10 件不乘；core **548 绿** + typecheck ×4 + content:check【待验收/待发布：推送闸门内】。
- 2026-09-09：**巡洋价位定档 + 特殊舰船声望门槛调高（大鲸鱼二号；玩家反馈"新增舰船过便宜、立刻全有"——对照 EVE 收入/船价比率（EVE 巡洋 ≈老手十几分钟产出；本游戏按后期可持续 ~2M/h 折算 9~15M ≈一晚一艘）后船长拍板）**——①掠食者巡洋 4 艘价格按战力序重锚：长尾鲨 3.4M→**9M**、锤头鲨 3.8M→**11M**、牛鲨 4.4M→**13M**、电鳐 3.6M→**15M**（ships.ts priceIsk 与 marketCatalog basePrice 同步；声望 8~10 维持）；②特殊舰船声望调高：**王鲭级无人机母舰 6→10**、**鲸王级采矿艇（船行与蓝图书行）补 standingReq 11**（制造门槛对齐）；③设计稿/变更记录同步【待验收/待发布：推送闸门内】。
