# 封存卷 · roadmap 批次条目 2026-09-20

> **冻结件**（2026-09-20 当日批次条目）：从 `docs/roadmap.md` **原文搬运、一字未改**（含船长原话照抄、逐问逐答、负向验证与读数）。
> 封存日期 **2026-09-15**（三号 · verify40）· 搬运时原行号区间 2026-09-20 当日条目。
> **检索**：本目录内 grep，或 `git log -S "<关键词>" -- docs/roadmap.md`（历史版本随时可翻）。
> **不要在本卷里追加新内容**——新批次写进 `docs/roadmap.md` 的滚动窗口，越窗后由 `npm run docs:seal` 封存进新卷。

---
- 2026-09-20：**方案：链上耗时比 4:2:1（原矿→原材料→零件→高级零件）· 仅提案 · 未实行（一号 · main）**——
**①船长原话（照抄）**：「**能否让原矿-》原材料-》零件-》高级零件，满产能所需的时间比为4:2:1？**」＋追问后「**并不需要现在实行，给我提出一个方案**」「**我只是在估算收益**」⇒ **一行数据都没改**。
**②口径三条（缺一条结论不成立）**：满产能按**掘洞级 T1 ＋ 满采矿技能 ＋ 2× 强化采集器 MK1 = 6.52 单位/秒**（换成鲸王级 T3 = 29.0 单位/秒 ⇒ 比例变 `1:2.5:2.4:1`，精炼/零件反成瓶颈）· 「4:2:1」按**每级减半**读作 `采矿4 : 精炼2 : 基础件1 : 高级件0.5` · 不计航程/返港、AI 线与手动线差、小批量取整。
**③现状（真数据 · 满技能 · 每批 10 件 · 七张高级件等权平均）**：采矿 48.7s（增值 9,177 ISK）· 精炼 29.9s（3,813）· 基础件 29.2s（2,073）· 高级件 12.0s（7,807）⇒ **链合计 119.8s / 22,870 ISK（增值之和 = 成品值，闭环自检过）**，比例 **4.05 : 2.48 : 2.43 : 1.00**（已是金字塔形，两级零件偏慢）。
**④达标所需（锚采矿不动）**：精炼 **×0.82**（`refineCycleMs` 10/12/…/60 → 8/10/…/49 秒）· 基础件 **×0.42**（15/16/16/17/21/26/45 → 6/7/7/7/9/11/19）· 高级件 **×0.51**（14/14/15/22/23/43/103 → 7/7/8/11/12/22/53）⇒ 链总时长 119.8 → **91.4 秒（−24%）**，链 **ISK/h ×1.31**（≈68.7 万 → ≈90 万），其中基础件 ISK/h **×2.4**、高级件 ×2.0、精炼 ×1.22。另给两种锚点：**锚总时长**（采矿 ×1.31 · 精炼 ×1.08 · 基础件 ×0.55 · 高级件 ×0.67 ⇒ ISK/h ×1.00）· **锚高级件**（采矿 ×2.02 · 精炼 ×1.66 · 基础件 ×0.84 ⇒ 链 160 秒、ISK/h ×0.75）。
**⑤红线**：精炼提速会同步抬精炼 ISK/h（现值增幅 +21~37%/批），要"提产能不提收益"只能压精炼产值 ⇒ 破「零技能不吃亏」配平护栏（不建议）；基础件"约 15 秒一批"的节奏会作废；档内收益差（同级 ≤1.5 倍）仍守得住（等比缩放）。
**⑥追记（同日两批船长令已部分落地）**：高级件已 ×0.25、基础件已 ×0.5（29.2 → 14.6 秒）⇒ 基础件环节**基本到位**（实测 1.21 : 1 vs 目标 1 : 1），**剩余缺口 = 精炼 ×0.82 · 高级件 ×0.51**。备选口径：乙案（基础与高级同档 1 ⇒ 高级件要 ×0.11）· 丙案（只算三段增量耗时，未算）。
**【已合入 main · 已推送】**

- 2026-09-20：**外部审查报告逐条核对与处置（船长朋友发来的报告 · 一号 · main · 已推送）**——
**①四条代码缺陷（全部属实/部分属实 ⇒ 已修）**：① `tools/flow-newgame.ts` 把 `good.id` 传给 `placeSellOrder`（`MarketGoodDef` 只有 `key`/`refId`）⇒ 挂出 `"undefined"` 幽灵单（修后读数 `ore-veldspar ×1`）· ② `placeSellOrder` **缺主键校验** ⇒ 补 `ctx.marketGoods.has(goodKey)`（"不扣库存"是设计契约、不是漏洞）· ③ 交火分支本拍 `dt` 被丢弃 ⇒ 改为"先把 dt 并进余额、再统一按 100ms 切片"（读数：带 500ms 进战斗 落后 **1000ms → 0**；窗口被节流 20 秒 落后 **20.9s → 0**；顺带修掉旧写法"余额为 0 时把巨大 dt 整段推进"的反向毛病）· ④ `wormhole-econ` / `counter-audit` 用了 **10 处假技能 id** ⇒ 换成真实 id（修后 `wormhole:econ`：撤离 **12/20** · 稀有残骸 0.60 件/趟 · 到手率 **49%**）。
**②顺带产出**：新增常驻体检 **`npm run skill:audit`**（`tools/skill-id-audit.ts`：扫 tools 三种技能写法逐个与 `data/src/skills.ts` 对账，读数 **62 文件 / 154 处候选 / 82 真实技能 ⇒ 全绿**）· `wormhole-econ` 补 `clearTempSpace`（未结束 **2/20 → 0/20**，属工具政策缺一步、非游戏 bug）。
**③`tools/` 不在 typecheck 覆盖面内**（实证代价 = 上面那条 `good.id`）⇒ 只读清单已出：**134 行报错 / 19 文件**（~40 条真错 · ~12 半真 · ~46 strict 噪音）；清单已抓出并修好 3 处"活着的静默故障"：`content-check` 的 `WORMHOLE_MATTER_FLOOR` 没从 core index 导出（契约一直静默通过 ⇒ 补导出并把契约加严成"先判是不是数字"）· `chain-balance` 用了不存在的 `ctx.wrecks`（改走 `wreckDensityOf`）· `content-validate.ts` import 了不存在的 `ITEM_KIND_ORDER` 且**全仓无人引用**（建议删除，见"遗留待定项"）。
**④船长已裁定不改**：虚空晶流动性 · MK2 与谜质"非常驻" · 4 项蓝图书价 + 7 项零件料价出带（报告那三条只作记录）。**20 个工具"版本自检落后 ≥2 个大版本"**属实但属自检元数据 ⇒ 按最后改动日期排期核对。
**⑤验证**：typecheck 四包 0 错 · core 全绿 · `content:check` / `l10n:check` / `ui:rot-check` / `build` / `flow:newgame` / `skill:audit` / `tools:audit` 全绿；临时探针用完即删（仓库 `_` 探针 0 个）。**【已合入 main · 已推送】**

- 2026-09-20：**MK2 弹药"供货与收购"修好（乙案 ＋ 收购额度 ×5 · 船长裁定后落地 · 一号 · main · 已推送）**——
**①船长原话（照抄）**：「**3 种 MK2 弹药供应量和收购太少了，起码要足够玩家消耗和交易出售**」＋裁决「**弹药走乙，但是收购额度增加约 5 倍。其他按你推荐来。**」
**②病根（实测）**：三件早先被改判进「稀有订单」渠道，而那条渠道是为"一件一件的稀罕货"设计的（每 10 分钟抽 8%~15% × 167 件 rare ⇒ 每张单 1~3 件）⇒ **24 游戏小时只能买到 61~81 发**，而 5 门动能炮的巡洋舰**单场预载 ≈755 发、4 舰编队 ≈3,000 发/场** ⇒ **差 50~100 倍**；且卡片上 `poolTarget 518_400` / `supplyFlow 4_320` 仍在、吸收还按池口径读它 ⇒ **"买不到、却卖得掉"两头不对称**。
**③落地**：渠道**留在稀有订单**（`rarity: 'rare'` 不动）＋ 两个新字段 `rareQtyMul: 200`（单张件数 1~3 → **200~600 发**，供货与簿面收购两侧同乘）· `rareWeightMul: 3`（抽取/收购概率 ×3 ⇒ 每系每天约 **3 万发**供货，对 167 件池的挤占 +6/167 可忽略）＋ `absorbQtyPerWindow: 21_600`（**收购额度 = 池口径 4,320 × 5**，写在卡上）。接线三处（`market.ts` 稀有分支的抽取权重 / 单张件数 / 收购单概率与件数）＋ 4 条用例。
**④新契约**：`content:check`「稀有批量档契约」——写了 `rareQtyMul`/`rareWeightMul` 的卡必须是 rare 档、**必须同时写明 `absorbQtyPerWindow`**（否则退回"买得到、卖不掉"）、乘子有下限（≥10 / ≥2）；并把"非常驻卡玩家产出无法稳定卖出"那条告警收窄（现存告警只剩 `mat-wh-essence`，船长已定不改）。**【已合入 main · 已推送】**

- 2026-09-20：**新手链路大修（一）：任务中心改「顺序解锁」＋ 里程碑单开子页（船长转玩家反馈 · 一号 · main · 已推送）**——
**①船长原话（照抄）**：「**玩家反应，新手引导的重要任务一次性太多了，建议按顺序排列解锁。并且已经完成'第一次'任务后的里程碑任务链，建议单开一个任务中心的子页面'里程碑任务'来装。**」＋同日三选：**丙案**（只显示当前这一条）· 页签**恒显＋未解锁提示** · 链领奖**搬到新页**。
**②落码**：`visibleFirstTasks` 只给数组序里第一条没完成的；`FirstTaskDef.prereq` 字段与 10 处数据**删除**（旧口径「13 条自由选择完成、prereq 只控可见」作废）；新增 `firstTaskProgress`（页头 N/13）与 `milestoneBoard`（13 条链各一行 · `unlocked` = 对应「第一次」已完成）；`panels/MilestoneTasks.tsx`（页头「◆ 里程碑 · 已解锁 N/13」· 空态提示 · 每链一卡 · **领奖按钮只在这里**）；任务中心加第 5 页签。
**③第二道令**（同日）「**第一次任务内，不要显示里程碑相关内容。建议隐藏**」⇒ 撤掉卡上三处里程碑残留（只读行 · 「可领奖」小标 · 链条奖金行），**顺带修好一处死分支**：旧写法 `def.chain ? 奖金行 : 奖励行` 而 13 条全带链 ⇒ **一次性奖励行（采集器/沙猫蓝图/AI 核心…）从来没画在卡上过**，现在画的正是它。
**④验证**：typecheck 四包 0 错 · core 180 文件 / 2,014 例全绿（`first-tasks.test.ts` 重写 2 例）· content/l10n/rot/build/flow 全绿。⚠ 观感（第 5 个页签排版）船长过目。**【已合入 main · 已推送】**

- 2026-09-20：**新手链路大修（二）：判定收口「未显示的第一次任务不许提前完成」（船长报障 · 一号 · main · 已推送）**——
**①船长原话（照抄）**：「**未显示的第一次任务可以提前完成。**」
**②根因**：顺序解锁当初只落在**显示**上（`visibleFirstTasks` 只给一条），而 `advanceFirstTasks` **每拍把 13 条全判一遍** ⇒ 不显示也照样：提前发一次性奖励与情报信、提前点成就徽章、提前解锁里程碑页，还会提前满足「寻找人类」的发布闸门。
**③修法**（`core/firstTasks.ts`）：`advanceFirstTasks` **只判当前那一条**（数组序里第一条 `done !== true`）· **一拍最多判过一条** · 判据仍读 `state` 现状 ⇒ **轮到那一拍按档内现状补齐（自愈、进度不丢）** · 老档 v25 迁移一次性写 `done`、不经过本函数 ⇒ 不受影响。`visibleFirstTasks` 与模块头注释写明"判定的当前条 = 显示的当前条"。
**④用例/工具**：`first-tasks.test.ts` 新增 `reachQueue(state, taskId)` 夹具（把排在它前面的标 `done`，只写标记不发奖励），6 条"越序完成"的既有用例接上它；新增 1 例钉本次报障（越序动作不判过/不发奖励 ＋ 轮到即补齐）；`flow-newgame` 按新的队列顺序**整篇重排**（动作顺序必须 = 队列顺序），补齐⑥打捞与⑪造船两步。
**⑤验证**：typecheck 四包 0 错 · core **181 文件 / 2,046 例**全绿 · flow ✅ · content/l10n/rot/build/skill/tools 全绿。**【已合入 main · 已推送】**

- 2026-09-20：**新手链路大修（三）：悬赏↔打捞对调 · 末段并列批 · 三条奖励填充（船长第三道令 · 一号 · main · 已推送）**——
**①船长原话（照抄）**：「**将第一次完成悬赏和第一次打捞残骸交换位置。完成11 · 第一次指派 AI 副船后，就可以将寻找人类和第一次长途运输以及 第一次虫洞同时显示给玩家。寻找人类位于顶部。没有给予奖励的任务，安排1万信用点的奖励填充。**」
**②顺序（数组序 = 解锁序）**：扫描 → 采集原矿 → 精炼炉 → **完成悬赏** → 维修舰船 → **打捞残骸** → 生产 → 挂单 → 造船 → 技能 → AI 副船 → 长途运输 → 虫洞。换过来后新手先后顺了：**先打一场拿钱**（实测进账 3,600，正好付港内维修 1,584）并在该星系留下残骸 ⇒ 回港修船 ⇒ 再带打捞器收残骸（母港残骸密度实测 ≈61）。
**③末段并列批**：前 **11 条**仍顺序解锁、一次一条、只判当前那一条；走完 ⇒「第一次长途运输」「第一次虫洞」**一起显示**（`visibleFirstTasks` 返回两条）＋ 贯穿任务「寻找人类」**同一拍发布**（发布闸门由"13 条全完成"提前到"前 11 条"，**旧口径作废**）并由 `Expedition.tsx` 把它画在「第一次」列表**之上**（位于顶部）；并列期两条**判定互不阻塞**（`advanceFirstTasks` 对末段批一起判、**同拍可判过多条**）。导航徽标改用**显示组签名**记账（顺序段 = 单条 id，与旧档逐字一致 ⇒ **零迁移**；末段 = `first-haul|first-wormhole`）；页头副标题按显示条数切换（新增 `ui.FirstTasks.036`「剩下的可以按任意顺序做」，中英齐）。
**④奖励填充**：三条原先只有情报信的条目各补 **10,000 信用点**（打捞 / 挂单 / 指派 AI 副船，走既有 `reward.isk` 通道）＋ 三封通讯各加一句附件说明。
**⑤验证**：typecheck 四包 0 错 · core **181 文件 / 2,054 例**全绿（+8 例：顺序组 · 末段并列批组 · 发布闸门重写）· flow ✅（新顺序走完 11 条、末段两条同显＋同拍发布）· content/l10n/rot/build/skill/tools 全绿。**【已合入 main · 已推送】**

- 2026-09-20：**首次任务奖励前移：采集器 ⇒ 扫描 · 打捞器 ⇒ 挖矿 ＋ 100 橄榄岩（船长两条令 · 一号 · main · 已推送）**——
**①船长原话（照抄）**：「**第一次任务中，采集器和打捞器给的任务应该往前调，采集器是扫描星系给，而打捞器应该是挖矿任务给。**」＋「**任务完成后额外给玩家100橄榄岩用于下一阶段任务。**」
**②落地**：`first-scan` 新增 `mod-miner-1` · `first-mine` 的奖励由采集器改 `mod-salvager-1` ＋ `ware: ore-veldspar ×100`（精炼每批要 100 单位、沙猫一趟约 70 ⇒ 正好凑够第一炉）· `first-salvage` 删除奖励行（**后于第三道令补回 10,000 信用点**）· 三封情报信附件行跟着挪。
**③老档不追溯**：奖励只在"真正第一次完成"当下发放 ⇒ 已发的照旧；**已完成扫描、还没挖矿**的档拿不到采集器（市场 31,200 ISK 有售 / `bp-miner-1` 可造）——要不要补发见"遗留待定项"。
**④顺带**：`exploration.test.ts` 两条"读日志最后一条"的事件池用例改成读"最后一条 ✦ 事件行"（奖励行现在落在事件行之后）；`simulation.test.ts` 离线事件计数用例先隔离出奖励行。**【已合入 main · 已推送】**

- 2026-09-20：**打捞提示给「去装配」入口 ＋ 工业页解锁改挂「第一次操作精炼炉」（船长令 · 一号 · main · 已推送）**——
**①船长原话（照抄）**：「**打捞需要打捞器的提示，添加让玩家去装配的提示。并且解锁工业界面要和第一次精炼的任务挂钩一起解锁。**」
**②打捞页**：`MapPage` 新增 `onGotoFit`，驾驶船一台打捞器都没装时（判据 = core 的 `salvagerCyclesOf(...).length === 0`，与出发门槛同一把尺）页首插一行提示 ＋「装配」按钮（落点与舰船页那枚同款）；`ui.MapPage.118` 新增、`ui.MapPage.049` 由"舰船页"改指**「装配」页**；`core.salvaging.003` 文案改成"先到「装配」页给驾驶船的高槽装一台"（中英同步）。
**③工业页**：新增 `UNLOCK_AT_TASK = { industry: 'first-refine' }`（**轮到即开**）＋ `unlocked()`/`unlockNeedTitle()` 两种口径（先 `UNLOCK_AT_TASK`、再 `FIRST_UNLOCKS`）；`FIRST_UNLOCKS.industry` 删除。**为什么不能按"该任务完成"开**：精炼炉就在工业页里 ⇒ 按完成后解锁会**死锁**。**三选②** ⇒「第一次操作精炼炉」前移到采矿之后（第 3 条），工业页与那张卡**同时**亮起，顺带让打捞那条到步时回收炉已可用。**【已合入 main · 已推送】**

- 2026-09-20：**导航「任务中心」加「第一次」推进提醒（船长令 · 一号 · main · 已推送）**——
**①船长原话（照抄）**：「**每推进一阶段第一次任务时，在导航栏的任务中心选项处进行提醒。**」
**②口径**（与「赏金新板提示」同款，不新造机制）：判定 = 当前显示组 ≠ 看过的那一组（`core.firstTaskNotice`）· 形式 = 左侧导航数字徽标（与赏金那条**合流**：`赏金新板数 + 1`）· 悬停先说「第一次」（`ui.App.120`，两条都有时各占一行）· **进「任务中心」页即记账**（挂在 `page === 'task'` 的 effect 上 ⇒ 导航/通讯「前往」/教程跳转三条入口都覆盖）· 状态 = `state.firstTaskSeenId`（可选兼容字段、**零迁移**、没记过账不写键）。
**③起点**：`finishPrologue` 替新档记第一笔 ⇒ **新档开局不亮**、完成第一条后才开始亮；老档不经过序章收尾 ⇒ 首帧亮一次；13 条全做完 ⇒ 不亮。**第四道令后**该键存"显示组签名"（末段并列批 = `first-haul|first-wormhole`）。
**④船长裁定（同日）**：徽标数字保持 **+1**（"有 1 条新的"，不显示剩余条数）· 里程碑与成就**不加**导航提醒 · 两条合流的悬停**保持多行**。**【已合入 main · 已推送】**

- 2026-09-20：**长途运输中断警告（四点两段确认）＋ 采集任务换船引导 ＋ 顺带修真 BUG（船长三条令 · 一号 · main · 已推送）**——
**①船长原话（照抄）**：「**第一次采集原矿任务文本添加建议玩家去舰船切换采矿船。**」「**任务完成后额外给玩家100橄榄岩用于下一阶段任务。**」「**当玩家在跑长途运输，要切换其他主控活动打断长途运输时，弹出一个警告，告诉玩家中断当前长途运输将无法获得本趟的报酬之类的。**」
**②口径先说清**：`advanceHauling` **每段到站才结酬** ⇒ 途中中断 = 本段报酬拿不到；`haulingHalt` 让舰船**即时返港停靠本段出发站**、虚拟货无残留、无其它惩罚。
**③四个中断点改"两段确认"**（第一下只警告、第二下才执行）：活动栏「停止运输」· 长途运输页「停止运输」· 舰船页「换驾驶」（会终止运输）· 虫洞「进洞」（会自动停运）——新文案 `ui.Hauling.033/034/035`（中英齐）。另把 8 条「长途运输进行中…到站即止」的 busy 文案改成「**中断本趟就拿不到本趟报酬**（报酬到站才结）」（涉及 9 处，表格中英同步）——顺带修掉旧口径残留「到站即止」（实际是即时返港）。
**④顺带修一条真 BUG**：`shipyard.changeShip` 里"终止长途运输"原先写在**函数最前面** ⇒ **换驾驶被别的原因拒掉时本趟运输已经被杀掉了**（玩家只看到"没换成功"，实际白丢一趟报酬）。现在按"能拒绝的校验一律排在终止之前"重排（远征/掩护巡逻先查 ⇒ 再终止运输 ⇒ 野外/未建成站照旧在后）＋ 新增用例「掩护巡逻中换驾驶被拒 ⇒ `hauling.active` 仍为 true」。**【已合入 main · 已推送】**

- 2026-09-20：**快递任务出发地钉成母港（不在母港则自动返航 · 不加守卫）（船长两句令 · 一号 · main · 已推送）**——
**①船长原话（照抄）**：「**所有快递任务，起点都是从母港触发，以避免玩家到达某个空间站后原地送快递**」＋「**出发不用加守卫，点击出发后自动返回母港。**」
**②病根**：报酬与限时按「母港→目标」标定，而**真实航程**按「玩家当前所在星系→目标」；到站结算又把玩家留在目标副站 ⇒ 接着接单可从副站起算、甚至短到 0（`arriveAt <= departAt` 的"立即结算"分支）⇒ **白拿母港级运费**。
**③改法**：**不加守卫**——不在母港时点「出发投送」**先把舰船自动送回母港**（返航段 **0 时间**，与 `location.startTransitHome` 的"换港返航即时到站"同尺）再按母港航线投送；**位置复位放在全部前置校验之后**（忙碌互斥/目标可用/货舱/跃迁门槛/航路可达都过了才动人）⇒ 不会"先送回母港、再告诉他这单发不了"；老档兜底解析目标站由"距当前位置最近"改成"距**母港**最近"；新增日志 id `core.sideTasks.020`。中途那版"硬守卫 ＋ `core.sideTasks.019`"按第 ② 句**已撤**（该条文案一并删）。**【已合入 main · 已推送】**

- 2026-09-20：**A 族全族贴脸：除劫掠狙击舰外最小射程改 1（船长令「全动」· 一号 · main · 已推送）**——
**①船长原话（照抄）**：「**将除劫掠狙击艇外的A族船，最小射程改为1.**」＋集中提问后「**全动。**」（= 四张旧隐藏遭遇模板的射程覆写一起改 ＋ 附改前/改后读数）。
**②改了什么**：`foe-pirate-corvette` 326 → **1** · `foe-pirate-raider` 326 → **1**（另两条本来就是 1）· 卡面覆写 `enc-pirate-1` 350 → 1、`enc-pirate-2` 383 → 1（主/僚两条）；**劫掠狙击舰 1255~9619 及以它为主体/杂鱼的三张卡全部豁免**；上限/单发/血量/速度/战术/命中一律不动。
**③口径改写**：2026-09-11 的「A 族全族射程 −15%」原先连近界一起缩 ⇒ 现改为**近界 = 船长特批 1 · 上限仍按 −15%**；`enc-pirate-*` 的"迁移守恒"冻结值在**近界**这一列被本批打破（其余列仍逐字守恒，四张卡都不在悬赏目录）。
**④读数**：解析面近界 326 → 1、**期望交距 2498 → 2352 m**（orbit 前压 146 m）；**对局面 90 场/版本逐格一致**（3 档玩家 × 2 战术 × 3 张卡 × 5 种子）⇒ **不改胜负与耗时**；顺带实测"当前距离动力学下玩家不容易把交距压进 200~326 m"（死区更像口径隐患而非可稳定利用的打法）。新增回归 1 例（舰级 5 条 ＋ 卡面覆写面）。**【已合入 main · 已推送】**

- 2026-09-20：**虫洞盘面阶梯二次改判（每 2 层 +1 环、不封顶）＋ 地图可拖动（船长两句令 · 一号 · main · 已推送）**——
**①船长原话（照抄）**：「**那还是改回「每 2 层 +1」的机制，不封顶**」＋「**以及允许玩家拖动虫洞探索地图**」。
**②阶梯**：半径 `min(4, 2+⌊(层−1)/2⌋)` →（同日一批 `2+(层−1)`）→ **`2 + ⌊(层−1)/2⌋`、不封顶**；即层 1~2 = R2 · 3~4 = R3 · 5~6 = R4 · 7~8 = R5 · 9~10 = R6 …（实测 1~12 层：**19/19/37/37/61/61/91/91/127/127/169/169 格**，空占比 50%→39%，信标 1/1/2/2/3/3~4/5/5/7/7/10/10，星云层 4 起 4→37 格）。波数、遗迹/谜质保底、空占比 −1%/层、星云 +2%/层封顶 80%、回合预算一律未动。
**③地图可拖动**（放大后按住即拖）：位移阈值 **4px**（以下当点击格子）· 夹取 `wormholeMapPanClamp` · 拖过之后**吃掉紧随的 click** · 平移与缩放同一 transform、拖动中关掉过渡 · 换层/进出洞/「适应」都复位 · `.app-wh-mapbox{touch-action:none}` ⇒ 手指按图上就是拖图；图例说明（改 `ui.Wormhole.145` 的值）补一句。
**④追加报障修正**：船长报「**站在地图最下方，放大后拖不到上方的位置**」——根因是首版夹取写成**对称的 `±(z−1)·半幅`**，而缩放层以**玩家格为锚点** ⇒ 可拖范围本就不对称（上/左 ≤ k·锚点、下/右 ≤ k·(幅−锚点)）。改为按锚点分轴算：`x ∈ [−k·(w−anchor.x), +k·anchor.x]`、`y ∈ [−k·(h−anchor.y), +k·anchor.y]` ⇒ ① 任何一格都拖得到 ② 怎么拖都不露框外空白（读数：R4 站盘底 z=1.5 旧只给 108/需 198 ⇒ 新 198，盘顶屏幕 y 由 −90 ❌ → **0** ✅）。顺带把几何口径（缩放档/`viewBox`/锚点/夹取/自动缩放）抽到新模块 **`panels/wormholeMapGeom.ts`**（纯函数，探针能真调同一份实现）。**【已合入 main · 已推送】**

- 2026-09-20：**深层虫洞盘读档被丢 —— 结案「深入下一层后显示本层没有网格」（玩家报障 · 一号 · main · 已推送）**——
**①玩家报障（照抄）**：「**玩家虫洞深入下一层后，显示本层没有网格。**」（性质 = 当日盘面阶梯批的连带回归，出在我这边）
**②根因**：`save.ts` 的读档清洗 `cleanWormholeGrid` 写死 `radius <= 8`（当年"每 2 层 +1、封顶 R4"时代的余量）⇒ 阶梯改成"每 1 层 +1、不封顶"后，**层 8 起（R≥9）的盘被判坏结构、整块丢掉**，该层退回旧式线性地图 ⇒ 界面显示"本层没有网格"。触发时机 = 任何存档→读档往返（重启/恢复备份/导入/网页刷新），层 1~7 恰好通过。
**③修法**：`wormholeGrid.ts` 新增 **`WORMHOLE_GRID_SAVE_MAX_R = 64`**（**只作坏档护栏、与阶梯彻底解耦**，注释写明本事故）· `save.ts` 改用它。**回归守卫**：`save.test.ts` 新增 1 例——**逐层走真实存档往返（层 1~40）**，断言 `grid` 与 `radius/cells.length/pos` 全保留（以后再耦合会直接红）。**④影响面（如实）**：已经中招的那一层**不会自动回来**（盘在那次读档后就不在档里了）；可行的"按 `(seed, depth)` 重建同盘"路径代价是已探索进度重置 ⇒ 见"遗留待定项"。**【已合入 main · 已推送】**

- 2026-09-20：**虫洞信标：出现率 50% ＋ 每层保底 1 个（船长令 · 一号 · main · 已推送）**——
**①船长原话（照抄）**：「**虫洞中，信标的出现率降低到50%，但是有每层1个的保底数量。**」
**②落地**：信标数由**逐层定额**改为**逐个名额掷 50%**（`WORMHOLE_BEACON_RATE = 0.5`）＋ **≥1 保底**；**独立随机流**（`seed*7919 + depth*104729 + 29`）⇒ 空地点洗牌/分配洗牌/逐格 `pickPlace` 的骰子全不受影响、同 `(seed, depth)` 仍必得同盘；被砍名额**按权重让给其余四类信号**（池子仍填满 ⇒ **空信息占比阶梯一字未动**）；权重表与"信标不落入口格"均不动。
**③读数（400 seed/层）**：层 5 满额 3 → **均值 1.64** · 层 7 5 → **2.50** · 层 9 7 → **3.54**（≈满额一半），每层恒 ≥1（层 1/2 = 恒 1 个）。
**④两条连带**：旧口径「信标数逐层定额、不随 seed 漂」**作废**（要的就是"率"）；「第 2 个信标揭示谜质」这条新规**会变少见**——约一半的盘只有 1 个信标（层 5 = 48% · 层 9 = 8%）⇒ 那些盘只够标出入口、不再揭示谜质（**要不要把保底改成 2 个**见"遗留待定项"）。**【已合入 main · 已推送】**

- 2026-09-20：**谜质科技 · 洞外工业新增 T4「工业多核调度」（工业 AI 工位 +1/级 · 5 级）（船长令 · 一号 · main · 已推送）**——
**①船长原话（照抄）**：「**谜质研究的洞外工业，添加T4科技，增加工业AI上限，每级+1，最高5级**」
**②新节点**：`mt-industry-ai` · 效果 `industryAiSlots`（每级 +1 站内工业 AI 工位，满级 +5）· 费用 **30/60/90/120/150 谜质**（1.2/2.4/3.6/4.8/6 亿信用点）· 前置 `mt-industry-void`（同支 T3）。
**③读数**：满级站内 AI 工位 共用 5 ＋ 技能 15 = **20 → 25**；谜质科技树 **23 → 24 节点（探索 6 / 战斗 14 / 工业 4）· 76 → 81 级 · 满树 1,196 → 1,646 谜质 ＋ 17.22 → 35.22 亿信用点**（新节点自己 450 谜质 ＋ 18 亿，**本树最贵的一件**）。
**④接线三处**（缺一处就"点了没反应"）：`types.ts` 的 `MatterTechEffect` 关键字登记 · `ai.ts` 的 `industryAiBonus` = 谜质科技 ＋ 技能表两支**相加** · `content:check` 新增反向护栏「AI 扩容契约」（说明里承诺"专用工位"的节点必须挂 `industryAiSlots`）。另新增 2 用例（节点表 ＋ 点满科技 ⇒ 工位 25、只点科技不练技能也能开站内产业）。⚠ 既有 l10n 缺口：谜质科技各节点 **`note` 全树只有中文**（归三号本地化范围）。**【已合入 main · 已推送】**

- 2026-09-20：**零件工时两批：高级件 ×0.25 · 基础件 ×0.5（船长两条令 · 一号 · main · 已推送）**——
**①船长原话（照抄）**：「**将高级零件的制造时间缩短至25%。**」「**将基础零件的生产所需时间缩短至50%**」（口径 = 原值 ×0.25 / ×0.5，四舍五入；高级件 ×0.25 后 .5 秒进位）。
**②新表**（秒）：高级 **14 / 14 / 15 / 22 / 23 / 43 / 103**（量子协处理器芯 / 无人机神经原件 / 护盾发生装置 / 能量射流阵列 / 军规火控计算机 / 舰用龙骨组件 / 引力子补偿器）· 基础 **8 / 8 / 8 / 9 / 11 / 13 / 23**（电路基板 / 冷却导管 / 结构框架 / 超导电缆 / 光学透镜组 / 装甲板 / 陀螺稳定器座）；定价、料价、产出件数（10）、蓝图书价一律不动。
**③读数**：高级件毛利/秒 41.8~61.2 → **164.3~243.5**（净/h 15~22 万 → **59~88 万**）· 基础件 18.0~26.9 → **36.0~53.8**（净/h 6.5~9.7 万 → **12.2~18.8 万**）；**档内收益差**（首批裁定：同级 ≤1.5 倍）两批后仍是 **1.50 / 1.48 倍** ✓，但**跨档倍数**由 1.55~3.4 倍 →（高级件批后）6.1~13.5 →（基础件批后）**4.5~6.8 倍**；**链上比例** 采矿:精炼:基础:高级 = 4.05 : 2.48 : **1.21** : 1.00（对照 4:2:1 方案，基础件环节基本到位）。
**④验证**：`parts.test.ts` 三处同步（改钉 14 秒/8 秒并把 spec 改成读真数据 ＋ 新增工时钉死数组 高级 `[14,14,15,22,23,43,103]` / 基础 `[8,8,8,9,11,13,23]`）。**【已合入 main · 已推送】**

- 2026-09-20：**零件卡产物行补「每批 10 件」＋ 精炼增幅基线读数（船长令 · 一号 · main · 已推送）**——
**①船长原话（照抄）**：「**组装机零件的生产卡片内，产物那边并没有表明是每次生产10个对应零件。顺便查询下，各个原矿精炼成原材料，收益增幅约为多少**」
**②卡面**：零件档 14 张卡产物行按弹药档同款补 `×10 件`（`bp.outputUnits ?? 1`；**新增 `ui.Industry.152/153` 中英**）；装备/舰船档（缺省 1）不写尾巴。
**③精炼增幅读数（站内收价口径 · 每批 100 单位）**：**现值（无技能 1.20）≈ +21%~+37%（均值 ≈ +29%）** · **满技能（1.65）≈ +66%~+88%（均值 ≈ +77%）** · 配平基准（1.00）0.3%~14.2%（均值 7.4%）；**没有负收益配方**（最低 蓝霜冰 +20.5% @1.20）。⇒ 顺带发现 `docs/glossary.md`「精炼配平护栏」旧数字（零技能净 +0.4%~+4.6%）与实测不符（多半是 2026-09-08 材料定价体检后没重算）⇒ **词典已按实测改写**（不留两套数字）。
**④验证**：l10n/rot/build 全绿；⚠ `ui:overflow` 读数未取到（该工具要求网页版构建在 4173 ＋ 无头 Chrome 在 9222，本机都没跑）⇒ 产物行只多 5 个字符，版面请船长过目。**【已合入 main · 已推送】**

- 2026-09-20：**推进器多件：由「EVE 曲线乘算」改为「折权加算」（船长令 · 一号 · main · 已推送）**——
**①船长原话（照抄）**：「**基础改为加算，但是依旧有多件衰减**」（背景：EVE 曲线 `Π(1+pᵢ·wᵢ)` 对**大额件反而放大**——2× 微型跃迁引擎 MK3 合计 ×11.10，比全额加算的 ×6.00 还高）。
**②合成式**：`Π(1 + pᵢ × wᵢ)`（`curveMult`）→ **`1 + Σ pᵢ × wᵢ`（`weightedSum`）**；权重（100%/87%/57%/28%/11%…）**不变** ⇒ 多件衰减照旧；收敛分组由 `curve` 改 **`weighted`**；收敛池与生效窗口（推进器爆发期，默认 60/60、MWD 10/60）不变。
**③读数**：MWD MK3 四件 **×46.01 → ×7.81**（三件 ×26.95 → ×7.10）· 矢量 MK3 四件 ×7.53 → ×3.72 · 混装 MWD3＋矢量3 ×6.54 → **×4.37** · 全族五件 ×12.36 → **×5.12** ⇒ 大额件不再被乘积形放大，但**每多一件边际收益仍小于上一件**。
**④范围**：**只动速度这一支**（推进器族 `speedBonusPct`）；命中 / 跃迁速度 / 目标锁定**仍是 EVE 曲线**（要一起改说一声）。**【已合入 main · 已推送】**

- 2026-09-20：**玩家三报障修复：遗迹收尾战 · 虫洞地图点不动 · 已建成的空间站拿不到成就（船长转述 · 一号 · main · 已推送）**——
**①玩家原话（照抄）**：「**虫洞打捞有 BUG，我带一个打捞器摸 2、3 残骸的遗迹。惊动敌人后有时候需要继续打捞，把残骸清空才能对敌。**」「**以及进层扫描以后会有卡住的 bug，要到主界面再返回虫洞才好。要缩放到最大才能点击触发。**」「**已经建好了的空间站无法完成成就。**」
**②遗迹收尾战**：收尾战触发时机 2026-09-16 已改到"遗迹第一次打捞"，而 `wormholeStartBattle('ruins')` 的守卫还写着旧口径「先捞空再打」＋ `wormholeActivateAt` 没查"欠着一场战斗" ⇒ 玩家只能一直捞到清空。修法：守卫改 **`run.pendingRuinsBattle`**（堆没清也能打；凭空开战仍被拒）· 新增 **`wormholePendingBattleReason`** 单点拒因、`gridActionBlocked` 与 `wormholeActivateAt` 都读它 · 渲染层新增 `wormholeLayerBlocked()`（作业按钮置灰＋理由行；**撤离仍只读动作闸**）。
**③地图点不动**：根因 = 地图框 `pointerdown` **无条件 `setPointerCapture`** ⇒ `pointerup` 被改派到地图框、`click` 的公共祖先变成地图框 ⇒ **格子的 onClick 永不触发**（`z = 1` 时提前 return 不捕获 ⇒ **只有缩放过的地图点不动**；换层自动缩放（半径 > 8）⇒ 与报障"进层以后/跟缩放有关"完全对上）。修法：**越过拖动阈值、确认是拖动那一刻才捕获** ＋ 新增 `onLostPointerCapture` 收尾。
**④已建站成就**：四个里程碑计数原先**只在事件发生那一刻写账**（`sitesBuilt`/`aiCoreKinds`/`matterTechMaxed`/`whMaxDepth`）⇒ 事件发生在成就系统之前/之外的档账上永远是 0。修法：`engine.reconcileMilestoneStats` **每拍现算**（改用 `peakFirst`，只升不降 ⇒ 幂等、零迁移），并按船长令「**里程碑都加入追溯检查**」把追溯面**扩到全部六个里程碑计数键（18 枚徽章全覆盖）**，函数头列出每个键的**推导来源与精度**（`rareBoxes`/`sitesBuilt`/`matterTechMaxed` **精确**；`aiCoreKinds`/`whMaxDepth`/`whBossClears` **下界**——下界只让"该拿的"拿到、不会凭空发）。另修成就「谜质通晓」阈值 23 → **24**（随 T4 节点表）。**仍不可追溯的两处**（要精确需新加存档字段）见"遗留待定项"。
**⑤验证**：core 180 文件 / 2,025 例全绿（新增 6 例）· content/l10n/rot/build/flow 全绿。⚠ 报障②属界面指针行为（仓库无 DOM 测试运行器）⇒ 请船长本地进层 → 扫描 → 直接点格子复测。**【已合入 main · 已推送】**

- 2026-09-20：**成就徽章（63 枚）＋ 稀有度收口（二号 · d2 · 已合入 main）**——
**①船长原话（要点）**：「继续之前的成就系统，并标注未完成。**未完成的功能不计入本地化工作排队**」·「链徽章在1级，4级，7级，10级时候各给一枚，**图案相同，用颜色区分**」·「宇宙探索家，**只做2枚**，分别是1级和5级」·「给每个物品的图标模式右上角添加物品稀有度展示的小标签（**采用R1表示1级稀有度**）」·「**渠道归属不动，两个稀有度表没有区别就合并，并删除多余的表**」·「**弹药 MK2移动到稀有订单**。「common 渠道档必须=1」的契约废除，改为『**common 渠道稀有度不影响交易**』」。
**②结果**：徽章 **63 枚**（13 任务 ＋ 12 链×1/4/7/10 ＋ 探索家 1/5 级 2 枚）· **纯展示**（不发任何奖励）· 每拍**现算补发**（老档自愈，不另写迁移补发）· 记**两个完成时刻**（游戏内 ＋ 现实墙钟；core 不调 `Date.now()`）· 界面 = **左侧导航一级页 ＋ 页内子窗口容器**；存档 **v30**。稀有度：**合并为唯一一张表**（市场外物品的档并入 `RARITY_TIER`、删掉第二张表）· 图标右上角 `R{档}` 小标签（查不到**不显示**，不硬塞 R1）· **档位只影响稀有订单刷新权重**，常驻/奇货不受影响。
**③新增规则**：约定 **§十一之二**（未完成功能不排本地化队列 · 记号 `⟪未完成 YYYY-MM-DD⟫` · 完成即删记号）；roadmap 本节上方新增「**未完成功能清单**」。
**④验证**：typecheck 四包 0 错 · core 1946 用例全绿 · `content:check`（新契约：稀有两表合并后的键集与值域 · 渠道带）· `ui:rot-check` · `ui:tip-check` · `l10n:check` · `save:migrate` 71/71 · 桌面 build。**词典已登记**（glossary §十二 成就 / §十三 稀有度）。

- 2026-09-20：**成就系统第二批（里程碑 18 枚）＋ 展示改版（单网格 · 卡名称号 · 悬停三行）（二号 · d2 · 已合入 main）**——
**①船长原话（要点）**：「**开始第二批**」；展示改版三条：「**成就徽章页面内不要分系列，所有成就卡片排列在一起**」「**比如宇宙探索家 · 1 级这种非常难看，可以采用见习探索家这种**」「**悬浮窗内，名字要单独起一行。时间单独起一行。不要挤一块**」。
**②四问四答**：档位用词 = **见习/资深/王牌/传奇** · **链名不动**（只给徽章另取「行当」）· 13 枚任务徽章**不改名**（与任务标题同名）· **不配英文**（交三号排队）。
**③里程碑 18 枚 = 六个家族、判据只读终身计数**：深渊层深 2/3/4/5 层 · 层末守卫 2/4 个 · 稀有残骸额外战利品 1/5/20/60/150 件 · AI 核心 1/2/3/4 类 · 副空间站 1/2 座 · 谜质科技 23 项全满级。**阈值依据实测**（4 船真档编队 12 趟平均到达 **2.90 层**）。**只计手动虫洞**（自动探索不经过 `wormholeDescend`）。
**④零迁移**：4 个新计数键进可选字段 `state.firstStats` ⇒ **不升存档版本**；其中两个走新增的**峰值型**写入 `peakFirst`（只升不降、幂等——"到达过的最大层深"是状态不是事件）。**顺手修一处真缺陷**：`state.rareBoxesOpened`（该型残骸已开箱数）此前**全仓无业务写入点**、那本账一直空转 ⇒ 在全仓唯一出箱点补齐。
**⑤展示改版**：去掉「任务 / 次数链」两个分区标题与按链分组 ⇒ **63+18 枚排在同一张网格**；链徽章卡名改 **档位词 ＋ 行当**（`见习探索家`/`传奇赏金猎人`，**不带级别数字**），为 13 条链配行当（`学而不厌→学者`/`舰队调度→调度官`/`星际货运→货运长`/`深渊探索者→深渊行者`，避免叠字）；悬停富卡改**三行**（名字/说明/时间）。**清掉失去消费者的休眠代码**：`chainAchievementGroups`、`.app-ach-sec/.app-ach-chain*`、3 条死词条。
**⑥验证**：typecheck 四包 0 错 · core **1975** 用例全绿 · `content:check`（陈旧术语契约当场抓出"高级箱"这个施工期旧称，已改「稀有残骸额外战利品」）· `ui:rot-check` · `ui:tip-check` · `l10n:check` · `save:migrate` **73/73** · 桌面 build · **真渲染冒烟**（81 枚卡 · 单网格 · 81 个图案无兜底 · 18 个新名字全上屏）。**词典已更新**（glossary §十二）。
