# 封存卷 · roadmap 批次条目 2026-09-10

> **冻结件**（2026-09-10 当日批次条目）：从 `docs/roadmap.md` **原文搬运、一字未改**（含船长原话照抄、逐问逐答、负向验证与读数）。
> 封存日期 **2026-09-15**（三号 · verify40）· 搬运时原行号区间 2026-09-10 当日条目。
> **检索**：本目录内 grep，或 `git log -S "<关键词>" -- docs/roadmap.md`（历史版本随时可翻）。
> **不要在本卷里追加新内容**——新批次写进 `docs/roadmap.md` 的滚动窗口，越窗后由 `npm run docs:seal` 封存进新卷。

---
- 2026-09-10：**族系战术改判 + 能量卡火力重标（船长「推进下一批」＋逐条裁定；二号）**——①**族谱取证更正**：4 张 `enc-pirate-*` 按 `shipArt` 的 `FOE_FAMILY` 是**缺省 F 制式巡逻**（不是 A 海盗）；权威族谱 = **A 海盗 6 / B 靶机拾荒 3 / C 异形 4 / D 守墓 4 / E 巨构 2 / F 巡逻 4 / G 流亡 3**。②**战术改判 ×2**（船长 ①②「可以」）：**坟场守墓人 88 brawl→orbit**（D 族设定「靠必中远程炮与三系全能盾立身」，却在 2.8 km 贴脸）、**噬口猎杀令 80 orbit→brawl**（C 族＝有机曲线+螯颚+酸液喷吐器，「噬口」应贴脸吞噬）；连带三件套——射程带 2.8km↔7.1km 互换、期望交距 555↔4151 m、速度 433↔345 / 338↔426；**难度守恒**：噬口由墙变可过（中位 S2 **0%→100%/78s**）、坟场由可过变墙（**100%→0%**），E 段中位通过卡数不变。③**血量**（船长「除了战术变动的其他保持不变」）：**只动噬口 4000→1844**（时长口径，D(T)=67s），坟场 3600 保持，其余 24 张一行未动。④**能量卡火力重标**（船长「两处 foeDmgMul 都移除」）：**星髓虫群 72** 移除 0.35（基础单发 104.7 全额；对主力行是死旋钮、只影响被贴脸的薄皮行）；**深渊之门卫队 45** 改为**纯能量**（`{plasma 10}`，光束必中）＋ 移除 0.35 ＋ **`foeFalloff 0.1`**（远端威力 ×0.28）＋「直接调基础伤害」→ 新增字段 **`foeShotDmg` 直写 45**（推得基础单发本是 90）。⑤**⚠ 流程缺口补齐（船长追问「有无考虑堆抗性的情况」）**：此前所有扫描只跑「只堆主系」配装——补扫两端后发现 **深渊之门是"不堆能量抗 0% / 换上能量抗件 100%/残血 27%"**，即全游戏**第一张真正奖励抗性取向**的卡；**此后伤害类标定必须同时跑"只堆主系 / 堆对应抗"两端**（已写进词典）。⑥**坟场 3600 正常吗**（船长追问）→ **不正常**：它是 brawl 口径的遗产（那时够不着、血量只承担时长不承担承伤）。实测血量曲线 6~120→80%/20s、**310→100%/30s/残血 23%**、≥1200→0%；**降命中（0.85/0.80）与去第二波都救不回**（仍 0~20%）。**本批保持 3600**：接受它是 D 族第三张 E 段墙（虚海 4300 / 穹顶 5600 同口径也是 0%），砍到 ~310 会让威胁 88 的卡比威胁 20 的碎晶带（285 血）还薄；**E 段可打属整体议题，三张一起动**。⑦**引擎新增两个逐卡字段**：`foeFalloff`（远端衰减覆写）与 `foeShotDmg`（基础单发直写，短路推导链）。⑧**契约**：`content:check` 混伤契约新增「纯能量卡」特例——单系 plasma 合法，但**强制必须显式给 `foeFalloff`**。⑨**交付实机档**（船长「给你个存档测试下」）：`docs/test-saves/test-save-abyssgate-20260911-084959.json` —— 驾驶 灰鲭鲨 4×动能MK2+支援 + 中位技能 Lv3、声望 13、星图全亮，**装备库备盾/甲 × 动能/能量 四系抗性件各 3 件（换件即换抗）**，四张验收卡（深渊之门/星髓/噬口/坟场）全可接。⑩**工具**：`battle-calibrate` 新增 `--tactic` / `--solve-hp` / `--solve-dur` / `--hp-sweep` / `--dmg-sweep`（两端配装）/ `--resist-sweep` / `--grave-sweep`；`make-test-save` 新增 case `abyssgate`。**⑪验证**：typecheck ×4 + core **798/798** + content:check（含新特例）+ desktop build 全绿 + 矩阵证据刷新。【待验收/待发布】

- 2026-09-10：**敌速重标 · 落码（船长：「1，可以落码，2和3一起另外开一批」；二号）**——**口径**：固定锚定 + 基准船 = **速度中位线船只（船池 25 艘取中位：长尾鲨级导弹巡洋舰 272 / 敏捷 0.54 → 基准战斗机动 165.2 m/s）**；比率 = 敌战斗机动 ÷ 165.2。**①落点（本批实改）**：`data/anomalies.ts` **26 张卡全部逐卡显式 `foeSpeedMps`**（逐张注释写明比率与旧值）——brawl 11 张 **低段缓坡 1.10/1.15/1.20（威胁 6/10/12）→ 1.29~1.48×**（322/337/351/377/394/408/410/412/418/420/433）、orbit 10 张 **0.96~1.20×**（281/286/291/291/307/316/327/338/345/351）、kite 5 张**维持**（201/204/234/234/235）；`balance.ts` `foeSpeedCapMul` **1.2→1.55**，并把段参考船旧公式**降级为"未逐卡标定的新卡"兜底默认**（注释写明口径）。**②关键发现（决定了重标形状）**：按中位船口径，原**低段 4 张比玩家还慢 18~38 m/s**（演习场 0.77×、新港 0.77×、边境 0.88×、碎晶带 0.89×）——**这才是"贴脸怪不贴脸"的真正根因，不是射程**；高段反而早已超标（星髓 1.40×、坟场 1.44×）。⇒ 重标为**低段大提速、高段按船长「不压反提」提到 1.43/1.48×**，11 张净贴近速度统一到 **+17~+79 m/s**。**③工具**：`battle-calibrate --proposal` 的表已与落码值同步，**72 行矩阵与出厂基线逐格 0 差异（一致性自检 ✅）**；并保留"近战卡虚拟装配逐卡对照"段供审核。**④护栏（新增）**：`content:check` 「**敌速口径契约**」——① 每张敌军卡必须显式声明 `foeSpeedMps`（防新增卡悄悄回落到旧公式口径）；② 比率必须落在战术带内（brawl 1.05~1.55 / orbit 0.90~1.25 / kite 0.60~0.85）；**负向验证**：把演习场临时改成 900 → 契约如实报错「比率 3.07× 越界」并失败，还原后全绿。**⑤落码实测**：全矩阵重跑（证据 `calibrate-matrix-mechanics-20260910.txt`）——近战卡真挨打了（中位 S2 坟场 112%→**103%**、中位 S1 坟场 112%→**106%**、无技能 S1 泰坦 100%→**76%**、奥罗 100%→**72%**），E 段基本守住（中位 S4 虚海 20%→**60%**、T3锤头鲨 虚海 70%→73%、穹顶 60%→60%）；**由胜转负 10 格 / 由负转胜 5 格**，其中 4 格是"无技能薄皮行打高段近战"（100%→0%）——**这是本批"血量未重标"（船长：血量暂时不重标）的已知代价**，日后动血量时即为入口清单。**⑥未动**：射程带（brawl 2200/orbit 4600/kite 9200）、两段式 DPS 换算与 `D(T)`、逐卡 `foeHpOverride`、三处个性口（泰坦 65% 命中 / 星髓 `foeDmgMul 0.35` / 坟场两波）。**⑦下一批已立项（船长「2 和 3 一起」）**：**战术改判 + 族系冲突**，必须成套（改战术 = 射程带 + 逐卡速度 + 血量三件一起动）——高优先 = **坟场守墓人 88（D 族设定「靠必中远程炮立身」却在 2.8 km 贴脸）**、**噬口猎杀令 80（C 族异形"噬口"却在 7.1 km 环绕）**；中优先 = 深渊之门卫队 45、伏击劫掠队 22、边境海盗前哨 12 / 狂徒巡逻编队 40；详见 `docs/design/enemy-speed-retune-20260910.md` §6。**⑧挂起**：装配面板 `FitPage.tsx:511`「机动速度（含加力）」口径问题（显示 `spec.speedMps`，未乘 0.6 与敏捷修正 → 面板值约为实战机动的 1.65 倍；且标签「含加力」自推进器周期化后已过期）——船长指示先挂起。**⑨验证**：typecheck ×4 + core **798/798** + content:check（含新契约）+ desktop build 全绿；`battle:calibrate` 落码前/后对照已留档。【待验收/待发布】

- 2026-09-10：**敌速重标 · 第二轮：按船长裁决改为「固定锚定 + 中位船基准」并出近战卡逐张对照（船长：「采取固定锚定，参考按照速度中位线的船只进行参考。近战卡给出对比和虚拟装配由我一一审核」；二号）**——**仍零游戏改动**。**①基准船**：船池 25 艘按 `maxSpeedMps` 排序取中位 = **长尾鲨级导弹巡洋舰（船速 272 / 敏捷 0.54）→ 战斗机动 165.2 m/s**（`--proposal` 里运行时自算，不写死）。**②重标形状变了**：按中位船口径实测出**低段近战卡全都比玩家慢 18~38 m/s**（演习场 225→0.77×、新港 225→0.77×、边境 257→0.88×、碎晶带 261→0.89×）——**这才是"贴脸怪不贴脸"的真正根因，不是射程**；而高段（星髓 **1.40×**、坟场 **1.44×**）早已超过目标带顶。⇒ 提案 = **低段大提速（+43~64%）、中段 +15~26%、高段 +5%、星髓/坟场反而压回 1.36/1.39×**，全 11 张统一落在 1.25~1.39×、净贴近速度 41~64 m/s。**③预演（1872 格 / 832 格变化）质量明显好于第一版**：胜率翻转仅 **由胜转负 6 格 / 由负转胜 7 格**（第一版曾出现"中位虎鲨2kin2 打坟场 100%→0%"式翻车）；近战卡确实开始掉血（中位虎鲨2kin2 星髓 112%→76%、中位 S2 坟场 112%→106%）。**④新增工具段**：`--proposal` 现在同时打印**近战卡虚拟装配逐卡对照**（射程带/期望交距/编队/总血/三层/总DPS/单发/装填/命中/伤害构成/`foeDmgMul`/速度现状→提案/相对基准比率/净贴近速度），供船长逐张审核；并修正两处工具读数（「近战贴脸系数」单位混算 → 改为"敌战斗机动 ÷ 中位船战斗机动"；逐卡对照里现状速度误取提案值 → 改用出厂 bal）。**⑤⚠ 新增待裁决（附带影响）**：orbit 高段按新口径**降速**（穹顶 356→307、虚海 329→305、噬口 325→302），**E 段顶格被放松**——满技能 S4 打穹顶残血 **53%→76%**、T3锤头鲨 93%→103%、T3牛鲨 98%→112%、中位 T3锤头鲨穹顶胜率 60%→80%、无技能 T3长尾鲨穹顶 40%→**100%** ⇒ 请裁 E 段三张要不要**豁免本次重标、维持现状速度**以保住终局墙。**⑥剩余待裁 6 条**（写进设计稿 §7）：低段要不要缓坡（T6 1.10×/T10 1.15×/T12 1.20×）、星髓/坟场压不压、E 段豁不豁免、近战卡血量目标残血带、既有个性口（泰坦 65% 命中 / 星髓 `foeDmgMul 0.35` / 坟场两波）确认不动、3 张可选改判候选。**⑦验证**：typecheck ×4 + core **798/798** + content:check + desktop build 全绿。【待船长逐张审核·未落码】

- 2026-09-10：**敌速重标提案 + 预演（船长：「之前设定是敌人速度略快过玩家是吧？决定对速度进行重新调整。包括敌人的战术倾向和射程以及DPS换算等」→ 六项口径答复 → 按「先出提案+预演，审核后再落码」执行；二号）**——**本轮零游戏改动**（`packages/**` 与 `data/anomalies.ts` 一行未动），产出 = 提案 + 工具预演。**①先回答船长**：原设定确实是「无推进玩家多数持平/略快；brawl 再 ×1.28 贴脸」，现状实测比率（敌战斗机动 ÷ 段参考船战斗机动）= brawl **0.95~1.32×** / orbit 0.75~1.05× / kite 0.76~0.79×。**②提案口径**：brawl **1.25→1.40×**、orbit **0.95→1.05×**（选项1 的 0.85~0.95 上移 0.1）、kite **维持现状**；全 26 卡显式给速（223~421 → **201~443**），配套 `foeSpeedCapMul` 1.2→1.55。**③工具**：`battle-calibrate` 新增 **`--proposal`** 开关（只改本工具上下文，按提案口径替换全 26 卡敌速）与 `--dps=` / `--hpmul=` / `--brawlmul=` 灵敏度旋钮；**并更正「近战贴脸系数」的单位混算读数错误**（拿敌速（船速池单位）除以玩家**战斗**速度，虚高约 1.67 倍——星髓显示 2.51×、真实 1.08×），改为「敌战斗机动 ÷ 段参考船战斗机动」并新增「速度口径校验」段。**已验证默认路径不受污染：重跑基线矩阵 72 行逐格 0 处差异。** **④预演（1872 格中 674 格变化）**：近战卡终于打得到人（中位虎鲨2kin2 星髓 112%→**76%**、中位 S1 坟场 112%→**99%**、中位 S2 坟场 112%→106%），但**长战近战卡薄皮行翻车**（中位虎鲨2kin2/三族混装 坟场 100%→**0%/40%**、满技能鲸王 裂谷/泰坦/奥罗 60~80%→**40/40/20%**），**满技能档依旧 120% 免疫**。**⑤决定性发现（改变了提案的框架）**：**固定锚定达不到 1.25~1.40×**——同一份敌速在玩家侧的实际比率横跨 **0.64×~1.55×**（玩家战斗机动跨度 161~294 m/s，2.4 倍；裸船鲣鱼满技能 294 m/s 对边境 12 只有 **0.64×**）；且**比率只决定谁赢距离拔河，"多久贴上来"取决于差值**（中位 S2 对星髓 1.14× → 净接近仅 **31 m/s**，3220→2655 m 要 18 秒，50 秒短战里于是几乎没挨打）。⇒ **需船长在甲（固定锚定，保留"换轻船/装推进器就能甩开"的反制）/ 乙（动态锚定＝开战时按本场玩家实际机动 ×比率，任何装配下都真的更快）/ 丙（动态+上下限钳制）之间选**——只有**乙/丙**能兑现「逼玩家换轻船或装推进器」这句话。**⑥血量重标曲线**（`--brawlmul` 扫描 ×0.85/0.7/0.55）：近战卡总血 **×0.7 是"能过但紧"一档**（中位 S2 坟场 134s→102s、薄皮行从 0% 回到 20%）、**×0.55 又变回白刷**（中位 S1 坟场回到 112% 满血）、**满技能档对血量完全不敏感**（全 120%）⇒ 需逐卡定值，不能用一个全局倍率。**⑦两条附带发现**：**orbit 上调 0.1 反而让 E 段略轻**（满技能 S4 大白鲨对穹顶 53%→**65%**、虚海 90%→94%）——orbit 敌变快后更快收敛到自己的期望距离，接近期少挨一段打；**kite 维持不变**则 5 张卡逐字未变。**⑧射程/战术**：射程带一律不动（brawl 2200 / orbit 4600 / kite 9200，按船长「保留近战身份、靠速度压进来」）；战术归属**逐卡复核后建议维持 26/26**，另列 3 张**可选改判候选**（泰坦残骸/奥罗残骸 → orbit、噬口 → brawl）请船长裁。**⑨验证**：typecheck ×4 + core **797/797** + content:check + desktop build 全绿。【待船长审核·未落码】

- 2026-09-10：**舰船蓝图同批补锚：23 张料耗上调到同族锚线**（船长对上一条"舰船蓝图料/价 22~35%、毛利 354% 要不要一并处理"的回话 = 「**调整**」；一号执行）——①**改动**：`packages/data/src/shipBlueprints.ts` 中**低于锚的 23 张**（除皇带鱼级外全部）按**保持配方内各矿物比例**放大用量 ⇒ 料/价 **22.0% → 44.3~45.2%**、毛利 **354~474% → 121~122%**（diff 78 行纯数值）。②**只补不足、不削已有**：`sbp-colossal` 皇带鱼级旗舰货舰料/价 **61%（高于锚 45%）保持不动**——其毛利 64% 本就在合理带内、不是失衡来源，且不做无端 buff（若要"全族一律 45%"需另说一句，那会把它的料耗下调 26%）。③**效果**：`sbp-thresher` 长尾鲨级变现 **175.5 万 → 123.7 万 ISK/h**、`sbp-whale-king` 鲸王级 **132.1 万 → 88.1 万/h**、`sbp-humpback` 座头鲸级 **38.3 万 → 18.0 万/h**（它现货价 135 万而料耗 60.7 万 ⇒ 变现档位 0.65 吃掉了大半毛利）；全表 ⚠（料/价 <40%）**只剩 3 张武器件**（bp-turret-2 32.5% / bp-turret-3 33.8% / bp-missile-3 40.0%，属武器族一致带，按前批口径不动）。④**工具/文档**：`manufacture:econ` 的「料/价 %」列直接可见落点；设计稿 `manufacture-refine-worker-econ.md` §六 与词典「工业工位」同步；临时 codemod `_codemod-ship-recipe.ts`（数值自推）用完即删（仓库 `_` 文件 0 个）。⑤**验证**：typecheck ×4 + core **798/798** + content:check（蓝图 装备 89 + 舰船 24）+ desktop build 全绿；`manufacture:econ` 复跑核对落点。【待验收/待发布】

- 2026-09-10：**组装机（制造）配方补锚：46 张非武器蓝图料耗上调**（船长：「接下来对组装机生产的物品贩卖进行平衡」→ 量化回报后定「**上调所需材料**」→ 口径追问定「**配方锚保持不变**」＝锚就用既有 45%、照表落码；一号执行）——①**问题**：同日"MK2/MK3 装备价对齐同级武器价"上调了 46 件成品价、**材料配方没跟着走** ⇒ 这些件的「料 ÷ 现货价」从 ~42% 掉到 **3.0~18.3%**，制造毛利率从 ~120% 飙到 **446~2625%**（`bp-armor-plate-3` 一个工位变现 **222 万 ISK/h**、`bp-hullrep-1` 442 万/h），远超采矿（掘洞满配 26~626 万/h）与赏金（深层卡 126.9 万/h）；而"高收益行不设上限"的三个理由（稀缺工位/长周期/吸收流自约束）在这些行上**都不成立**（周期 900~2900s、吸收额不卡）。②**锚 = 仓库既有同族线**：MK1 全线 / 民用 / 弹药 / 修理组件实测 **44.8~51%**（2026-09-08 按"卖站净率 ≥20%"定的），**不是新设锚**。③**改动**：`packages/data/src/blueprints.ts` **46 张非武器蓝图**（护盾四件 / 装甲四件 / 矢量推进器 / 无人机三件 / 采集·货舱·打捞·陀螺·索敌·射速·锁定·维修·稳定器 的 MK2·MK3）按**保持配方内各矿物比例**放大用量（取整：用量 ≥200 → 50 的倍数，否则 10 的倍数）⇒ 料/价回 **44.8~45.4%**（diff 182 行、纯数值）。④**未动**：武器族（32.5~42%，属其族内一致带）、MK1/民用/弹药/修理组件（已在锚线）、所有 `basePrice`、`buildSeconds`、技能；**舰船蓝图（料/价 22~35%、毛利 354%）本次不在范围内**——工具已用 ⚠ 标出，留待船长定夺。⑤**效果**：制造毛利率区间 **78%~2625%（34 倍）→ 78%~≈123%**；示例（变现口径无技）`bp-armor-plate-3` **222 万 → 73.5 万 ISK/h**（毛利 2098%→122%）、`bp-hullrep-1` **442 万 → 188 万/h**（毛利 463%→123%；周期仅 180s，单位时间仍偏高属"短周期件"特征，毛利率已与同族一致）。⑥**连带**：这些线矿物需求涨 3~15 倍 ⇒ **精炼炉成为新瓶颈**（一条 MK3 装甲线 ≈ 2.3 台满技能炉供料），"挖矿 → 精炼 → 制造"闭环才真正立起来；矿物池（同日按产能扩容后）供给充足（三钛池每小时可买 ≈109 万件 ≫ 单线需求 ≈20.7 万件）。⑦**工具**：`manufacture:econ` 每行新增「**料/价 %**」列（<40% 打 ⚠，日后改价/改配方复跑即见）；临时探针 `_probe-recipe-anchor.ts` 与 codemod `_codemod-recipe-anchor.ts`（数值由数据自推、非手抄）用完即删，仓库 `_` 文件 **0 个**。⑧**文档**：设计稿 `manufacture-refine-worker-econ.md` 新增 §六；词典「工业工位（收益口径）」补 2026-09-10 补锚条。⑨**验证**：typecheck ×4 + core **798/798** + content:check + desktop build 全绿；`manufacture:econ` 复跑（46 张落回锚线、⚠ 只剩 3 张武器件 + 舰船）；`liquidity:audit` 复核矿物池覆盖达标。【待验收/待发布】

- 2026-09-10：**推进器爆发幅度回档 30/60/100%（船长：「将推进器效果恢复为30/60/100%（之前是这个效果吧）」；二号执行）**——**本条**取代下方同日「推进器周期爆发」条目里的 **+40/80/130%** 口径（历史条目按纪律**不回改**，以本条为准）。**改了什么**：`packages/data/src/modules.ts` 三档 `speedBonusPct` **0.4/0.8/1.3 → 0.3/0.6/1.0**（＝船长同日先前提档前的原值，git 可核），描述文案同步为「点火期间战斗速度 +30%/+60%/+100%」；`tools/battle-calibrate.ts`、`tools/mixed-damage-review.ts` 的档位标签与注释同步为 +30/+60/+100；词典「推进器周期点火」、设计稿 `thruster-charge-20260910.md` §2.1 表与裁决摘录同步。**未动**：周期口径（点火 60s / 冷却 60s / 开场即点火）、`hitPenalty` 0.05/0.12/0.20、敌突进全部参数与开关、任何其它数值。**净效果（要说清）**：因为现在是**周期点火**而非常驻，回到原幅度后**平均机动低于改动前的常驻版本**（以 S2 灰鲭鲨装 MK2 为例，基础战斗机动 214 m/s：提档期 +80% → 前半 60 秒 385、后半 60 秒 214，120 秒均值 ≈300；回档 +60% → 前半 343、后半 214，均值 ≈278；**而改动前的常驻版是全程 342**），但**失稳代价也只在点火期生效**（冷却期命中不受罚）→ 总体是"峰值略低、代价窗口更窄"。**③复跑结果（证据已刷新）**：`battle:calibrate` 全矩阵与 `battle:mixed-review` A~G 段全部重跑——**F 段落回提档前的读数**（深渊之门 45 对 MK1/MK2/MK3 = 50s/21 次/残血 49%、43s/18 次/58%、36s/15 次/64%），E 段敌伤旋钮行与 §9.1 表也回到提档前口径；**顺带发现并更正复核报告两处与证据不符的旧数**（§9.3 的"噬口 78%"实为 **0%**、§4 的 S2 满技能噬口 9:1 由 78% 改为 **100%**），并把 §4/§10.1/§10.2 的三处时长按新证据对齐（噬口 MK3 84→85s、虚海 MK3 77→78s、穹顶 MK3 61→**39s**）。**验证**：typecheck ×4 + core 全绿 + content:check + desktop build + `battle:calibrate` + `battle:mixed-review` 复跑。【待验收/待发布】

- 2026-09-10：**市场物品池按玩家产能标定（流通性体检）**（船长：「市场流通性还是较差，建议根据玩家对物品的生产能力调整市场的物品池」→ 基准追问后定「**按 MK1 照掘洞级估算**」→ 分层原则「**理论上越低级的矿需求量越大，但差异太大玩家挖高级矿会卖不出去**」→ 确认「**照表落码，矿物侧一并审**」；一号执行）——①**量化**（新增正式工具 `npm run liquidity:audit`，原临时探针 `_probe-liquidity.ts` 按纪律转正后删除，仓库 `_` 探针 0 个）：覆盖比 = 池日吸收 ÷ 基准日产；改前**采矿侧 17 个矿带里 15 个覆盖比 < 1**（气冰类仅 **0.02~0.05×**、高阶矿 0.08~0.45×），**矿物侧**冥铁 0.16× / 星髓 0.24× / 同位聚晶 0.38× / 超噬 0.82×；根因 = 池量早期按"感觉"定（越稀有越小）、与产能脱节且梯度失控到 **833×**（flow 6~5,000/窗）。②**基准与规则（船长定）**：采矿侧基准 = **掘洞级 + 满采矿技能 + 2×强化采集器 MK1**（引擎 `getMiningParams` 实测 15.4k~20.0k 件/h ⇒ 日产 37~48 万件），矿物侧基准 = **主控精炼炉满技能**（批 100、高级回收满级批量 ×1.3、精炼产出倍率 1.65，每种矿物取产率最高那支矿）；覆盖比**按单价分层**：≤20 ISK → ×15 ／ ≤200 → ×6 ／ ≤400 → ×3 ／ >400 → ×2；换算 `supplyFlow = round(日产 × 覆盖比 ÷ 1440)`、`poolTarget = supplyFlow × 120`。③**落地**：`marketCatalog.ts` **21 个池**改写——矿石/气冰 14 个（富凡 5,000→**4,991** ×1.0、灼烧 2,000→4,991、希莫非特 150→1,997、氖云气 45→1,899、曦棱晶 330→1,539、蓝霜冰 35→1,899、辉云岩 300→1,539、离子风暴云 12→950、寒髓冰 16→950、极光云 6→950、磷光霾 10→950、玄晶 60→770、暗星冰 6→950、星幽矿 20→**513**）+ 矿物 7 个（三钛 10,000→22,684、类银 6,000→15,847、类晶体 3,000→5,631、同位聚晶 500→7,841、超噬 400→2,924、星髓 120→1,487、冥铁 20→258）；虚空晶无精炼来源（回收彩头）池不动。④**效果**：flow 区间 **513 ~ 4,991/窗 = 9.7× 差距**（原 833×）——梯度保留（越低阶需求越大）而最低档仍有基准产能 **2 倍**吸收 ⇒ **挖高阶矿也卖得掉**；**新手期几乎零变化**（富凡 flow 不变、灼烧仅 ×2.5）。⑤**未动**：所有 `basePrice`、产率/船型/技能、弹药/修理件/无人机等其它池、稀有渠道与蓝图权重、奇货渠道。⑥**收入含义（已报船长）**：池不再是收入天花板 ⇒ 采矿/精炼收入 = 产率 × 单价（掘洞满配富凡带 ≈26 万 ISK/h、极光云带 ≈626 万 ISK/h）；日后要压收入量级，旋钮是**原料单价**而不是池。⑦**验证**：`liquidity:audit` 复跑**幂等**（21 个池覆盖比正好落在各自层级目标、建议值 = 现值 ×1.0）；typecheck ×4 + core **795/795** + content:check + desktop build 全绿。⑧**文档**：新增设计稿 `docs/design/market-pool-liquidity-20260910.md`（状态：已确认并实现）；词典「吸收额 F」补标定口径、「巡游抢单」的氖云气示例同步（45 → 1,899/窗）。【待验收/待发布】

- 2026-09-10：**收尾归档：短射程近战敌"够不着"等裁决的挂账盘点（船长：「刚刚的短射程近战敌『够不着』等裁决是否未收尾？如未收尾先进行收尾归档」；二号执行）**——**结论：裁决本身已落地，但有两处"状态没跟上现实"的挂账，本轮全部理清并归档**。**①已收尾 9 件**（混伤复核选甲·维持现状 / 超时判负 / P1 口径复跑 / 派系掉落 5% / ①超时文案保留 / ②无技能底线降级为预警 / ③S2 可磨行不处理 / ④E 段实测档 etier / ⑤P1 旧账 7 处终审 / ⑥回收率 20% / ⑦两处 `foeDmgMul 0.35` 复核归位 / ⑧五族装备验收档 lairgear / ⑨公告数字同步（一号执行，`announcements.ts` 已由「基础一成」改为「基础两成」并补核验注）/ ⑩短射程"够不着"裁决）；**②仍挂账 4 件**（写入 `docs/review/pending-decisions-20260910.md` **§十一 收尾归档**，逐项写"等谁/等什么/怎么关"）：**(a) 敌突进未实装 → 短战"够不着"仍敞着**（长战已被推进器周期化治好：坟场 25 次→4~6 次开火；短战 星髓/泰坦 仍 0~1 次）——**等船长一句话**：启用（改一个开关 + 复跑）或按 D 口径承认"近战短战卡=高血沙包（威胁按时长与消耗算）"；**(b) E 段顶格实测回传**（`etier` 档已就位；已知换双抗救不回：虚海 20%→40%、穹顶仍 0%）→ 回传后定"单卡降伤"或"接受"；**(c) 两处 `foeDmgMul 0.35` 的"待实测复核"标注**（复核结论=深渊之门 0.35 精确必要、星髓病灶在射程不在火力）→ 建议维持并把标注改「船长 2026-09-10 复核：维持」，**需一句话**；**(d) P1 预警线的提示形式**（口径已降级为预警指标，但复跑工具还没打印提示行）→ 下次跑矩阵顺手补一行，无需给数。**③文档同步**：复核报告补 **§9.6 裁决与收尾**（A 已排除、走机制路线、长战已治/短战未治的实测对照、与 §10 装配适配互证"换装救不回来"）；清单 §0 总览改写为"已结清 9 / 挂账 4"、§一~§十 标注为**当时审核原文留档**、新增 §十一 收尾归档与本轮产出物索引。**验证**：typecheck ×4 + core **795/795** + content:check + desktop build 全绿。【待验收/待发布】

- 2026-09-10：**重大机制变更后全量重跑悬赏战斗 + 「预想装配」适配更新（船长：「因为有重大机制变更，重跑所有悬赏的战斗，预想装配最好也适配更新」；二号执行）**——**①全量重跑**：26 张悬赏 × **24 装配行**（原 21 + 新适配 3）× 3 技能档全矩阵（`battle:calibrate`），证据入库 `docs/design/battle-data/calibrate-matrix-mechanics-20260910.txt`。**②装配适配（新增 3 行，依据公开可核）**：**(a) 混伤适配 ×2** ——原锚行（S1/S2/S4/T3/弹药变体）一律「盾抗动能 + 甲抗动能」＝**只堆主系**，但克制表里**动能对甲层本就 ×0.5**，在甲层再堆动能抗属低效，而混伤后副系（能量）绕开主抗 → 适配版 = **盾抗主系、甲抗副系**（覆盖两系）：`S2 双抗(盾动能·甲能量)`、`S4 双抗(盾动能·甲能量)`；**(b) 推进器适配 ×1** ——原表**没有任何 MK3 推进器行**（S1 用 MK1、S2 用 MK2）→ 补 `T3锤头鲨+推进器MK3(周期点火)`（拿闪避陀螺换顶档，与同船原行对照）。**③三条新结论（决定后续方向用）**：**(a) 换双抗救不回 E 段**——中位档 S4 虚海 **20%→40%**、穹顶仍 **0%**；S2 两格仍 0%；满技能档只在残血上 +1~5 点（虚海 77→78%、穹顶 53→58%）⇒ **E 段缺口不是"玩家配装没配对"，指望换装解决不了**（支持前几轮的结论：要动数值或机制）。**(b) 周期点火后，推进器在"够不着"的卡上是负收益**——`T3锤头鲨+MK3` 对同船原行：噬口残血 **99%→51%**、虚海 **100%→20%**、穹顶 **60%→0%**（中位；满技能也全面更慢更低）。原因三条：点火期失稳代价（命中 ×0.80）压 DPS → 战斗拉长（噬口 80→84s、坟场 64→88s）；点火只有 60 秒而 E 段战斗 60~95 秒 → **后半段没有加速**；**在"敌人本来就够不着"的卡上机动不值钱**。**但对长射程 kite 敌是明显正收益**（§F 段实测：深渊之门卫队 45——档位越高越安全，敌开火 29→13 次、残血 30%→69%）⇒ **推进器已成"定向件"**（对风筝型敌人有用、对够不着的近战敌是纯代价），**不直观，建议写进手册/公告口径**。**(c) 无人机流"贴脸挨打"的根因查实**——D1/D2/D2b 对坟场守墓人 88 中位档 **26~30 秒暴毙（0%）**，而炮流锚行同卡是 **100%/135~245s/残血 112%（一点没挨打）**：因为**期望距离由自己武器射程决定**，无人机行没有炮台、只有兜底「基础舰炮 0~2500 m」→ 中距期望 **1250 m** → 必然站在近战敌（2.8 km）射程之内 → 吃满伤害；炮流站 3220 m 之外够不着。⇒ **不是 bug，是无人机流的固有代价**（机群本就要抵近），**校准时不该拿炮流的"零承伤"当无人机流的期望值**。**④文档**：复核报告新增 **§10 装配适配实测**，并把章节号理顺（原 §10→**§9**、§12→**§10**、§13→**§11**），**全仓 6 处引用同步**（`balance.ts` 注释 / 词典 / 交接清单 / 设计稿 / roadmap）。**验证**：typecheck ×4 + core **795/795** + content:check + desktop build 全绿。【待验收/待发布】

- 2026-09-10：**两条复核裁决（船长，随上一条市场批的连带数）**——①**高级箱兜底：维持不收口，接受 482 万 ISK/天**。背景 = 上一条"MK2/MK3 装备价对齐武器价"把兜底件（主题增幅件，按市价折算）抬高：**每箱均值 18.6 万 → 45.3 万、日产出 240 万 → 482 万/天（×2.0）**（灰霾伏击团 ×12.4 / 虚海守望者 ×3.8 / 碎晶带 ×2.5 / 信标猎手 ×2.4；穹顶守卫不变——其兜底是未调价的 MK3 三武），已给船长三个选项（维持 / 兜底不再给可售成品 / 另拟旧价折算），**船长取"维持"**：仍视为低安深层的合理红利。②**`rareTier3Weight` 保持 0.15**：tier3 **每窗总量占比由 10% 升到 13%**（行数 57 → 69 把权重降幅摊薄）已知情，船长口径取**「逐件更难碰」**——原 T3 行更难（tier3 蓝图 40.9h → 51.3h）、新升档的 12 件由 0.9h → 5.6h（难 6 倍），故不再下调（若要压总量，0.10 ≈ 7.7% / 0.08 ≈ 6% 已算好备查）。③文档同步：设计稿 `bounty-tasks-20260910.md`（复核段与裁决）与 `market-rarity-tier-20260909.md`（船长复核行）。**本批零数值改动**（纯留档），故未重跑四连。【待验收/待发布】

- 2026-09-10：**市场高阶稀有订单降权 + MK2/MK3 装备价对齐同级武器价（船长：「将市场稀有度的权重比进行调整，使高稀有度的稀有订单出现概率更低」＋「同时上调所有 MK2 和 MK3 级别的装备价格，对齐该级别的武器价格（参照列出供我审查）」；审查表过目后定「丙＝线性映射进攻器带」「按档位归」「RARITY_TIER 保持不动」，再追加「乙＝12 件升 T3」；一号执行）**——①**权重**：`balance.market.rareTier3Weight` **0.25 → 0.15**（同源收口：`market.ts` 兜底 `?? 0.15`、`types.ts` 注释、设计稿与词典同步）。②**价格**：`marketCatalog.ts` **46 条** `basePrice` 按「线性映射进攻器带」改写——第 2 档 23 件 2.8~21 万 → **38.5~48.1 万**（合计 268.7 万 → 993.3 万，×3.70）、第 3 档 23 件 17~112.2 万 → **192~240 万**（合计 1,322.9 万 → 4,876 万，×3.69）；三武价、第 1 档与民用价、奇货价、蓝图与舰船价**一律未动**；**属性/数值零改动**。③**档位**：`rarityTier.ts` **12 件 MK3 装备由 2 升 3**（护盾增强器三系 / 护盾扩展器 / 装甲镀层三系 / 装甲增厚板 / 矢量推进器 / 无人机甲板扩展 / 中继天线 / 导控阵列）——它们随调价跨过 50 万线，与其余 MK3 装备同级；**按档位归**（船体维修装置 civ/MK1/MK2 = 第 1/2/3 档，其 MK2 跟 MK3 走 → 92 万 → **229 万**）；**档位自此与价格解耦**，日后调价不再自动改档。④**工具**：新增正式工具 `npm run price:audit`（`tools/price-audit.ts`；原临时探针 `_probe-mk-price-audit.ts` 按纪律转正后删除，仓库 `_` 探针 **0 个**）——列「同级别装备现价 vs 同级武器价」对照 + 两套对齐规则提议价，供日后价格校准复跑。⑤**实测（`market-rarity-sim`，2880 窗 × 5 种子）**：rare 供给行 126 → tier2 57 / **tier3 69**（改前 57）；tier3 每窗 1.84 张（**占比 13%**，改前 1.50 张 / 10%）、单行间隔 **5.6 小时**（改前 5.7h——权重 −40% 与行数 +21% 相抵）、**tier3 蓝图行 40.9h → 51.3h**、tier2 单行 0.9h；`price:audit` 复跑幂等（两档「丙案＝现价」×1.00，46 件全部落进各自武器带）。⑥**⚠ 连带影响（重要）**：**高级箱日产出 240 万 → 482 万 ISK/天（×2.0）**——兜底件正是本批调价的装备（主题件按市场价折算），**与同日「兜底不收口」的裁决直接相关**，已提请船长复核；打捞**彩头 EV 4,979 → 5,764 ISK/h（+15.8%）**（仍在「≤保底 10%」护栏内，占比 4.0%）；保底 EV/h 不变（矿物未调价）。⑦**验证**：typecheck ×4 + core **794/794** + content:check + desktop build + `balance` + `price:audit`/`bounty:econ`/`salvage:econ`/`market-rarity-sim` 全绿。【待验收/待发布】

- 2026-09-10：**推进器周期爆发（已实装）+ 高威胁近战敌突进（已实现·未实装）**（船长两轮裁决：「给予近距离敌人突进加速的能力，当进入射程2秒后，突击能力消失，之后需要10秒冷却才能再次启动突进。同时修改推进器效果，推进器效果为每隔一段时间赋予玩家速度增加，持续一段时间后进入冷却。同时战斗场景中，屏幕下方会显示推进器冷却时间。」→「敌方突进的冷却时间延长至20秒。且只出现在高威胁的敌人身上。我方推进器持续时间改为60秒，冷却也为60秒。开场就启动。」→「按建议」（门槛 60 / 爆发 +40/80/130% / 突进 ×2.0 / 限定近战型）→**「敌人突进暂时先取消实装，仅实现功能。」**；二号执行）——设计稿 = `docs/design/thruster-charge-20260910.md`（状态：已确认并实现）。**①推进器（已实装）**：不再常驻提速 → **点火 60 秒 / 冷却 60 秒 / 开场即点火**；点火期战斗机动 ×(1+爆发倍率)（**MK1 +40% / MK2 +80% / MK3 +130%**，多件按 EVE 曲线收敛），冷却期**回基础速度**；`createPlayerSpec` 的 `speedMps` 改为**不含推进器的基础值**、推进器进新字段 `thrusterBoost`；新增导出 **`thrusterPhase(battle, bal)`**（**由战斗时钟推导 → 不占任何存档字段**，引擎与界面**同源**）。**②敌突进（已实现·默认关闭）**：仅「威胁 ≥60 且战术=brawl」，够不着时**机动 ×2** 逼近、**进入自己武器射程后再维持 2 秒**结束、随后 **20 秒冷却**（无总时长上限）；状态机 `updateFoeCharge` + `BattleState` 三个可选字段（零迁移、不进 `cleanBattle` 白名单，同 `hullEscapeFrac` 待遇）+ `UnitSpec.foeCanCharge`；**由 `balance.foeChargeEnabled` 总开关控制，默认 `false` → 任何战斗都不触发**（船长：仅实现功能），启用只改这一个开关。**③界面**：战斗界面底部「装填冷却」行**新增一格「推进器」**（复刻同级 `.app-bts-reload*` 结构：色点+名称+冷却条+倒计时/就绪，点火=绿「推进 Ns」、冷却=灰倒计时）；敌方突进时在武器 chip 旁出**「敌突进中」**（复用同级运行态 chip `.app-bts-repair is-down`，**不自造新类**）；星空视差速率改为跟随**当前实际速度**（点火期星空更快，同一信号的直观反馈）。**④玩家可见文案全链同步**：三档推进器 `speedBonusPct` 0.3/0.6/1.0 → **0.4/0.8/1.3** + 描述改写为周期点火口径、`ui/shipInfo.tsx` 推进器行补「点火 60 秒 → 冷却 60 秒，开场即点火」、手册「装配」条目补推进器周期说明、技能「矢量机动操作」措辞改为「与推进器**点火**加成乘算」。**⑤回归**：新增 `tests/thruster-charge.test.ts`（7 例：周期相位边界/无推进器不变/爆发进 thrusterBoost 且多件曲线递减/**默认关闭时任何敌人都不具资格**/门槛与战术过滤/够不着→突进→进射程结束→记录冷却/参数口径）；**两处旧用例按新语义同步**（`v17.test.ts`、`v181.test.ts` 由"速度 ×(1+speedBonusPct)"改为"基础速度不变 + 加成进 thrusterBoost"）；core **794/794**。**⑥实测对照（工具新增 F 段，9 种子·中位技能·同船换中槽）**：**长战有效**——坟场守墓人 88（111~148s，跨过点火期）敌开火 无推进器 **25 次** → MK1/MK2 **6 次**、MK3 11 次；**短战未变**——星髓虫群 72 / 泰坦残骸 60（40~51s，整场落在点火期内）仍 0~1 次开火（**这正是敌突进要治的**，而它现已实现但未启用）；对照狙击卡深渊之门 45：档位越高越安全（开火 29→13、残血 30%→69%）。**⑦复跑**：`battle:calibrate` 全矩阵 + P1 复跑表（`power-ladder-rework.md` **§7.4**）——**无推进器的装配行逐字未变**（T3/S4，证明作用面干净），**带推进器的 S1/S2 两行只有小幅时长漂移（±10~15s）、胜负判定一格未变**（S2 虚海/穹顶仍 0%、S1 赤潮仍 100%）。**⑧测试档**：`etier` **无需重生**（存档只存状态、推进器是代码）。**⑨已知取舍（写进设计稿）**：冷却期零加成 → 长战尾巴更吃紧；**失稳代价已按船长追加的「行」改为随点火窗口生效**（同日收口：冷却期不失效稳——新增同源 helper `effectiveHitMul(spec, boosting)` + `stepBattle` 的 `meAtk`；模块描述/装配界面「点火代价」行/手册/词典全链同步；复跑 F 段：坟场 MK1/MK2/MK3 由 129/135/148s 降到 **124/130/134s**、敌开火 6/6/11 → **4/6/6**，"长战有效"结论不变）。**验证**：typecheck ×4 + core **795/795** + content:check + desktop build 全绿。【待验收/待发布】

- 2026-09-10：**高级箱「主题件兜底」裁决＝不收口 + 该口径算全并入正式工具（船长追问「高级箱『主题件兜底』为什么要收口」→ 一号按"先去真实引擎量化再回答"回报 → 船长选「丁、不收口，维持现状」）**——①**算全的方法**：`tools/bounty-econ.ts` 的「稀有残骸·高级箱对照」小节由"每族抽样 1 张卡 × 2 万次"扩为「**全 19 张候选卡逐卡 8 千次** + 每箱可兑现分布 + **期望日产出**（真实引擎赞助 120 天日板，逐席按 `LAIR_RARE_WRECK_GAIN` 展开件数 × 该卡每箱可兑现）」——原临时探针 `_probe-rarebox-daily.ts` 因有复用价值**转正并入正式工具**后删除（仓库 `_` 探针 **0 个**）。②**实测**：每箱可兑现 最低 ≈3,082（边境海盗前哨，外围箱且无主题池 → 只剩矿物）｜ 中位 ≈10.2 万 ｜ 均值 ≈18.6 万 ｜ **最高 ≈196.2 万（穹顶守卫）**；期望日产出 **≈240 万 ISK/天**（日板 227.1 万 + 派系活跃 13.0 万；分档 外围 44.9 / 核心 86.0 / 深层 96.2 万）。③**根因**：兜底件走 B3.1 主题池，而**穹顶守卫**白名单含 **MK3 三武**（`mod-turret-kin-3` 市场基准价 191.6 万）→ 它的"未命中"= 每箱发一件成品 MK3，单卡 ≈146 万/天、占日产出**约六成**（高级箱因而是 MK3 的批量水龙头）。④**裁决 = 维持现状**：船长认定为**低安深层的合理红利**；命中率 5/8/10%、矿物单位、专属装备节奏**一律不动 → 零游戏数值改动**。⑤**留档旋钮**：高级箱兜底池剔掉 MK3 成品——全仓**只有穹顶守卫一张卡**的白名单含 MK3 成品（`content:check` 的 B3.1 契约原文「武器白名单仅穹顶守卫 × MK3 三武」），故"全卡兜底降档"与"只拧穹顶守卫"实为同一处改动；改后该卡每箱 195.5 万 → 约 3 万、日产出 240 万 → 约 87 万，复跑 `npm run bounty:econ` 即可核。⑥**两条边界（回答时已讲清）**：日产出是**日板全清**口径（实际取决于打得过几张——`bounty:econ` 表里多张深层卡在参考装配下胜率 0%）；账面按市场基准价计，变现要过市场深度与税。⑦**文档同步**：设计稿 `bounty-tasks-20260910.md` 经济对照表（日供给 9.8 → 9.1、补"分布"与"日产出"两行、裁决注）；**交接文档 §5 待决策清单第 1/2/3 条一并标结案**（公告 3 张已写入、派系掉落定 5%、兜底不收口），供后任一号一眼看清"§5 只剩第 5 条验收"。⑧**验证**：`npm run bounty:econ` 复跑通过（tools/ 不在 typecheck 覆盖内，按纪律跑一次为准）；**本批无游戏数值改动**，故未重跑四连。【待验收/待发布】

- 2026-09-10：**styles.css 乱码注释复原（船长：「进行复原」）**——承接上一条：三号核验时扫出 `apps/desktop/src/renderer/src/styles.css` 有 **40 行 mojibake 注释**（分区标题与规则说明，如第 3、29、47、305、721、3576、5346… 行）——系历史事故留下的乱码，且**随该文件首次提交 `ad5ddb7 1.0` 就存在**（git 历史里没有干净原文可回取）。**复原方法（不靠猜）**：先证成因 = 「原文 UTF-8 字节被按 GBK 读」，再用**求解器**反推——对每个乱码字符列出全部候选 GBK 字节对、以 **UTF-8 结构合法性**剪枝做 DFS，迭代求解到"再解就出替换符"为止。**结果**：**20 行求出唯一字节解**（其中 18 行另经**正向模拟**逐字符命中验证：`启动画面`/`整体骨架`/`主体三栏`/`技能行`/`训练队列`/`状态小标签`/`浮动提示`/`采矿台：矿带选择`/`采矿台：控制与状态`/`物品栏`/`舰船码头`/`训练面板（紧凑整合版）`/`细滚动条（EVE 终端感）`/`右侧固定事件日志`/`耐久条`/`采矿设置（星图页）`/`AI 指挥中心`/`蓝图制造台`）；**另 22 行各丢 1 个字节**（老工具对非法双字节吞 2 字节，数学上不可逆），按"反推残文 + 紧随其后的 CSS 规则"逐行补全（如 `.app-isk` 上方 = `顶部 ISK 余额`、`.app-col-right` 上方 = `右列布局：训练 / 物品 / 日志`、`.app-fit-slots` 上方 = `装配台`；`═` 分隔线按文件既有体例统一为 15 个 `═`）。**改动范围 = 纯注释**：diff 共 **40 行**（40 增 40 删），逐行核对**非注释行为 0**、**无任何选择器/属性/数值改动**；全文件复扫乱码特征 **0 命中**。**验证**：typecheck ×4 + core **787/787** + content:check + desktop build 全绿；产物核对 = 打包 CSS 内含 `启动画面`/`整体骨架`/`AI 指挥中心`/`蓝图制造台`/`细滚动条`/`耐久条`/`装配台`/`收尾演出`/`V9 市场页：搜索` 等复原文案。**工具**：求解器与施放脚本均为 `%TEMP%` 一次性脚本（未入库、用后即弃），仓库内 `_` 探针 0 个【待验收/待发布】。

- 2026-09-10：**公告文实修正 + 已发布公告开发词清理（船长：「照建议改：基础一成 → 基础两成」「清：改成玩家说法」；一号执行）**——①**文实同步**：二号在 `eda4d9f` 报「已上线公告卡 `2026-09-10-drone-rework` 第 2 条写『基础一成』，而船长 ⑥ 批已把无人机战后基础回收率 10% → 20%」→ 按船长批准改字：`packages/data/src/announcements.ts` 与同文待审稿 `docs/design/announcement-draft-20260910-drone-losses.md` 两处「基础一成」→「基础**两成**」（其余文字一字未动；满级五成不变）；`docs/review/announcement-factcheck-20260910.md` 补**变更注**（基础 10% → 20%、每级 +8% → +6%，满级仍 50%）；`docs/review/pending-decisions-20260910.md` 第 9 项标 **✅ 已结案**（汇总行 + §九 结案段 + §4 待办表三处状态同步，便于船长一眼看清"只剩第 10 项"）。②**开发词清理（三处已发布公告）**：`2026-09-08-vein-ai-batch` 第 4 条「AI 副船的满载返航与玩家**同口径**」→「AI 副船的满载返航不再被核心效率拖慢：卸货速度与主控亲自出港时一样快」；`2026-09-08-manufacturing-labor` 第 4 条「AI 线耗时与 AI 炉**同一套参数**：按核心效率拉长（基础核心 0.4×→2.5 倍时长）……主控线维持原**耗时公式**」→「AI 制造线的耗时与 AI 精炼炉同一个规矩——核心越好越快（用基础核心时约 2.5 倍耗时），再由「工业自动化」技能缩短（满级 −25%）；主控亲自开的线耗时不变」（事实按 `manufacturing.ts:188` 复核：AI 线 = 基础耗时 **÷ 核心效率**，"核心越好越快"成立）；`2026-09-06-refine-digest` 标题「产出**口径**与残骸回收对齐」→「产出与残骸回收看齐」。依据 = 约定第一章禁入玩家可见范围的开发/商讨用词（口径 / 参数 / 公式）。**「主控」一词按词典保留**（`glossary.md` 正式术语「主控 / 驾驶船」，非开发词）。③**验证**：typecheck ×4 + core 787/787 + content:check + desktop build 全绿；产物核对 = 包内三处新句命中、公告数据里旧串「基础一成」「同口径」「产出口径」「同一套参数」「原耗时公式」**0 处**。【待验收/待发布】

- 2026-09-10：**回收卡「特色掉落」提亮配色 + 稀有残骸只介绍族专属装备（船长：「将特色掉落的内容也换个更显眼的颜色。同时稀有残骸的特色掉落只介绍族专属装备」）**——①**配色**：具名特色件改用**最强调色** `.app-belt-feat`（`#ffe08a`，与公告标题 / 战报标题 / 战斗飘字同族的最强档金），泛化兜底（系列装备与蓝图碎片）**内嵌 `.app-dim` 保持次级灰**——一行之内形成"具名亮 / 兜底灰"的层级；样式挂在既有 `.app-belt-*` 家族内（紧跟 `.app-belt-out`），只新增一个类 + 一个 `.is-strong` 修饰（加粗），未动其它样式。②**稀有残骸只介绍族专属装备**（船长同日追加）：`recycleFeatureOf` 在命中专属装备池时**直接返回**——不再附主题追加件、不再写「另有民用与 MK1 系列装备 / MK2 系列装备」、不再写蓝图碎片句，整行 `tone='strong'`（强调色 + 加粗）；普通残骸照旧（具名主题件 + 泛化兜底）。③`FlavorTip` 由"单一 `parts` 数组"改为 **`named` / `generic` 两组 + `tone`**，工业页与星图打捞页两处调用同步改（星图侧仍按"汇总去重 + 最多 4 组 + 等 N 组"限长）。④**验证**：typecheck ×4 + core **787/787** + content:check + desktop build 全绿；**产物核对** = CSS 含 `.app-belt-feat` ×2（基类 + `.is-strong`）与 `#ffe08a`，JS 含 `app-belt-feat` / `is-strong` / 「特色掉落」；**实机文案核对**（临时探针打印后已删）：普通残骸 = 具名亮金 + 泛化灰，稀有残骸 = 整行仅「…（该敌群专属装备，每炉必给一件）」（G 族含「鱿蜂无人机 ×10 架」）。⑤文档：词典「特色掉落」条目补配色与稀有残骸口径。**顺带发现（非本批引入，待船长定夺）**：`styles.css` 有 **15 行 mojibake 注释**（分区标题，如第 3、29、47、641… 行），系历史上用 PowerShell 改中文留下的乱码（该坑已记在交接文档），**仅注释、不影响构建与玩家可见文案**；要修我可按上下文逐行复原中文后单独提交【待验收/待发布】。

- 2026-09-10：**装甲件 CPU 统降 20% + 船体维修装置 CPU 统升 20%（船长：「下调所有装甲容量和抗性相关的装备20%CPU占用，上调船体维修装置20%的CPU占用」；集中提问三问后定「只算装甲槽」「跨界件都不动」「四舍五入到整数」；一号执行）**——①**降 CPU（13 件，全在装甲槽）**：装甲镀层 MK1·三系 **5 → 4**、MK2·三系 **15 → 12**、MK3·三系 **40 → 32**；装甲增厚板 MK1 5 → 4、MK2 15 → 12、MK3 40 → 32；**陵寝装甲层（D 专属）52 → 42**（41.6 进位）。**效果数值一律未动**（抗 +25/40/55%、容量 +20/45/80%、+110% 全部原样）。②**升 CPU（3 件，中槽）**：民用船体维修装置 **6 → 7**、MK1 **14 → 17**、MK2 **26 → 31**；修复量 5/10/18、每跳组件消耗、脉冲间隔、市场价（3 万/18.5 万/92 万）一律未动。③**边界（船长裁）**：`生体甲壳板`（C·装甲槽，30）与 `生体损管腔`（C·中槽，34）**都不动**——两件是"既带抗性又带自愈"的跨界件，船长明确不归类；`巨构骨架`（E，主职为结构件，50）与 `赃物强化舱`（A，主职为货舱件，36）**不降**；护盾全家族（含陵墓护盾阵列 42）、高槽武器/无人机/推进/锁定/工业件全部不动。④**取整**：`content-check` 契约强制 `cpuUse` 为正整数（`tools/content-check.ts:408`），故按船长选的**四舍五入**落地，实际幅度落在 **−19.2%**（陵寝装甲层）~ **+21.4%**（维修 MK1）之间，并非精确 20%。⑤**连带同步**：`modules.ts` 两处族注释补决策说明、陵寝装甲层**玩家可见描述**「52 点 CPU」→ 42（其余 15 件描述本就没写 CPU）；`lair-gear-d.test.ts` 的「CPU 42 + 62 + 52 = 156」用例改为 **146**；设计稿 `bounty-tasks-20260910.md` 两处（表格 + 基准段）、`hull-repair-module-20260909.md` 档位表（6/14/26 → 7/17/31）。**上一批「D 族定削」条里的「陵寝装甲层 CPU 52 / 三件合计 156」由本条取代**（历史行不改写，以本条为准）。⑥**验证**：typecheck ×4 + core 全量 + content:check + desktop build 全绿 + 产物核对。⑦**已知取舍**：装甲件变便宜 → 堆甲/堆抗更易上舰，**部分回补**二号混伤复核里"堆单系抗收益腰斩"的痛点（8:2 下硬化主系 TTK 提升曾从 +110~117% 降到 +58~61%）；D 三件合计 156 → 146，守墓舰低槽预算变宽（效果数值未变、可装配置变多 = 强度间接回升）；维修涨价则压缩"战中续命"流。【待验收/待发布】

- 2026-09-10：**船长逐条批复 8 项待裁决 + 两项测试档交付（船长：「①保留。②允许突破底线。底线留作预警用。③不做处理。④给我相关存档做实机测试。⑤进行终审。⑥基础数值提高到20%，满级回收技能回收率仍为50%。⑦详细说一下。⑧参考④，需要。」；二号执行）**——汇总件 = `docs/review/pending-decisions-20260910.md`（含裁决结果表）。**①超时文案**：保留「耐久 -X%，维修花去 Y ISK」尾句 → 无改动。**②「无技能宽容线」降级为预警指标**：P1 r4 立过的「零技能档 E 段 100%」**不再作为验收线**，改为**预警指标**（跌破只提示、不阻断、不要求数值补偿）——**零数值改动**；口径落 `docs/design/power-ladder-rework.md` **§7.3**（含"日后要回头救"的入口 = 给 E 段 4 卡挂 `foeDmgMul`，参照墙点现用 0.27~0.30）。**③S2「可磨」行不处理**：虚海/穹顶 0%（纯系亦 0%，与混伤基本无关）留档不动。**④E 段顶格实机复核档（新增 case `etier`）**：`docs/test-saves/test-save-etier-*.json` —— 三船即 P1 复跑表那三行（**大白鲨 S4 = 驾驶** 5×动能MK3 + 支援、锤头鲨炮巡、灰鲭鲨 MK2）+ **战斗系 20 项 = Lv3（中位档）** + 声望 13 + 全星系点亮 + 弹药三型 ×5000 + 备件 ×3；注入清单里直接写明复跑表参考值（大白鲨 = 噬口 100%/坟场 100%/**虚海 11%**/**穹顶 0%**…），请船长重点体感"虚海/穹顶是还有血却被磨死、还是根本打不动"→ 实测回传后再定要不要单卡降伤；**已读档自检**（驾驶船高槽 = 5×`mod-turret-kin-3`、中/低槽与 P1 行逐项一致、技能 Lv3 落盘）。**⑤P1 旧账终审结清**：`anomalies.ts` **7 处** `foeHpOverride`（边境海盗前哨 150 / 占港武装通缉 292 / 碎晶带劫匪通缉 285 / 信标猎手悬赏 365 / 灰霾伏击团清剿令 430 / 天底静区封锁 2040 / 星髓虫群 1530）注释由「2026-09-06，待终审」改为「**2026-09-06 定值，船长 2026-09-10 终审**」，残留 0 处。**⑥无人机战后基础回收率 10%→20%**（满级仍 50%）：`combat.ts` `DRONE_SKILL.recoveryBase 0.1→0.2`、`recoveryPerLevel 0.08→0.06`（**每级由 +8% 改 +6%**，否则 5 级会撞上限浪费一级；进度 20/26/32/38/44/50）——**全链同步**：`data/skills.ts` 技能文案 ⟦…⟧（玩家可见）、`panels/Handbook.tsx` 手册「装配」条目（玩家可见）、`tools/pd-tune.ts` 打印行、`tests/drone-skills.test.ts`（0.1→0.2、0.34→0.38）+ `tests/drone-loss.test.ts`（0.1→0.2）、`docs/glossary.md` 词典条目、`docs/design/drone-losses-20260910.md` 数值表。**⑦深渊之门卫队/星髓虫群 `foeDmgMul 0.35`**：本轮给出详细说明（详见汇报），**等船长裁示维持或调整**。**⑧五族专属装备验收档（新增 case `lairgear`）**：`docs/test-saves/test-save-lairgear-*.json` —— 在 rarebox 全门槛之上**装备库预置五族专属模块 14 件 ×2**（A/C/D/E 各 3 + G 2）+ **仓库预置专属无人机「鱿蜂无人机」×30 架**（正常一箱 10 架）+ 四型制式无人机各 +40 对照 + **新增王鲭级无人机重装 ×1**（否则专属机无处放飞）；**已读档自检**（14/14 模块落库、专属机 30 架、稀有残骸 18 件、王鲭高槽 rack3×2+tac3×2 配置正确）。**⚠ 连带发现（已受控，等船长一句话）**：一号当日写入的**已上线公告卡 `2026-09-10-drone-rework`** 第 2 条写的是「基础**一成**」、练满五成——⑥ 改成 20% 后该句与实现不符（同文的待审稿 `announcement-draft-20260910-drone-losses.md` 与三号核验件 `announcement-factcheck-20260910.md` 同样记 10%）。**公告改动需船长批准**，故本轮**未擅自改公告**，已列入待裁决清单第 9 项（一字同步：一成→两成）。**工具与文档**：`tools/make-test-save.ts` 注册两个新 case（头注释 + INJECTORS）+ `docs/test-saves/README.md` 档案清单补两段；临时探针 `tools/_check-save.ts`（读档自检）用完即删。**⑨追加复核（船长追问「深渊之门卫队和星髓虫群威胁不够？」）**：结论 = **两张卡情况相反**——**深渊之门卫队 45 不是威胁不够，恰恰相反**（kite 射程 1737~13314 m，深在玩家偏好射程 3220 m 内，29 发/29 中、0.35 下已扣 304/410 血；抬到 0.5 → S2 中位只剩 67%、回到 1.0 → **25 秒被打死**）→ **0.35 精确必要，不宜动**；**星髓虫群 72 确实威胁不够，但病根不在火力**（brawl 射程仅 **1~2655 m**，交火停在 **2561 m**＝敌射程边缘，46 秒**只开火 3 次**；`foeDmgMul` 0.35→1.0 抬到 **2.86 倍**，9 种子四档结果**完全一样**）→ 炮口抬不起来，火力再高也没用。**系统性观察**：S2 灰鲭鲨中位对**全部短射程（brawl ≈2.2~2.8 km）**敌卡残血**一律 112%（满血）**（威胁 12/20/40/58/60/62/70/72/88 九张），而长射程卡都在真打人（噬口 orbit 7.1km 78%、虚海 7.4km 0%、穹顶 7.7km 0%、幽灵舰 kite 13.4km 84%、蜃影 13.7km 56%）⇒ **当前威胁来源＝"射程能否覆盖玩家偏好射程（3220 m）"**，与火力无关。**处置方向待船长裁示**：A 加射程 / **B 先查接近逻辑为何在敌速 2.51× 玩家中位时仍收敛在敌射程边缘** / C 改玩家射程或后退逻辑 / D 接受"近战敌=高血沙包"并把 brawl 卡标定改为按血量与时长。**建议 B 取证**（独立于混伤，建议单独立项）。工具侧新增 **E 段敌伤旋钮对照**（`npx tsx tools/mixed-damage-review.ts E`，支持只跑单段）；临时探针 `tools/_probe-starcore.ts`（敌方开火/距离取证）用完即删。详见 `docs/design/mixed-damage-review-20260910.md` §9 与 `docs/review/pending-decisions-20260910.md` §十。**⑩B 取证（船长选「B」，并指出主因「推进器效果加强」——已坐实）**：逐秒追踪（灰鲭鲨 4×MK2 中位 vs 星髓虫群 72）——**有推进器 MK2**：我方机动 **343 m/s** vs 敌 **231 m/s**（0.67×），距离稳在 **3197 m**、**敌人全程够不着、开火 0 次、我方满血 410**；**无推进器**：214 vs 231（1.08×），距离被压到 2561 m、敌 **t=45s 才进射程**、只开火 3 次。机制 = ①战斗机动 = 船速×0.6×敏捷修正，而**推进器 +30/60/100% 直接乘船速**、敌方机动是逐卡锚定的固定值；②距离是"拔河"（`rate = steerStep(我方)+steerStep(敌方)`），谁快谁把**自己期望距离**开出来——我方期望 **3220 m**（主武器射程中点）> brawl 敌武器上限 **2655 m** → 差 542 m **永久够不着**。**一般规律**：敌武器射程带 ∋ 3220 m → 打得到；否则永远打不到（brawl 全灭 / orbit 与 kite 都打得到）。**连带推论（同一根因另一面）**：期望距离由自己武器射程决定 → **射程越长站位越远越免疫**（长尾鲨导弹巡中距 **8140 m**，连 orbit 7.1~7.7km 都够不着它——与矩阵"全程满血"一致）→ 属**结构性问题**。修法选项：**A 定向抬 brawl 组敌速**（要压过 343 → 敌船速需 >553 ≈ **×1.35**，只动 9 张短射程卡、可逆）/ B 改"中距"定义（射程带 0.85 处，全局影响）/ C 机制级"被压制时不许自由后退" / D 接受并把 brawl 卡标定改为按血量与时长；**修法选项（决定性数字：玩家机动随推进器跨度 2 倍——机动=船速×0.62、敌恒定 231：无推进器 214 / MK1 278 / MK2 342 / MK3 428 ⇒ 装 MK1 起就比 brawl 敌快）**：**A 抬 brawl 敌速 ❌ 已排除**（压过 MK3 需敌船速 ×1.86，而不装推进器的玩家面对 2.0× 敌人会被瞬间贴脸打崩；只盖 MK1 则 MK2/MK3 照旧免伤——**无法同时兼容两端**）/ **E 近战突进（新提·推荐）**= 近战敌**在自身射程之外时**突进加速，只动敌人、只作用于"够不着"窗口、对各档一视同仁 / C 机制级"被压制时不许自由后退"（改我方机动语义）/ B 改"中距"定义（全局站位前移、削弱风筝流）/ D 接受并改标定口径；**建议 E**。**本轮未改任何数值，等船长给数。** 验证：typecheck ×4 + core **787/787** + content:check + desktop build 全绿。【待验收/待发布】

- 2026-09-10：**残骸回收卡说明精简：列保底矿物 + 只讲特色掉落 + 徽标改「稀有」（船长：「对精炼炉的残骸回收相关卡片进行说明精简。仿照精炼矿物的格式列出该残骸能回收的保底矿物。对于低出率掉落，仅介绍特色掉落。假如会出 MK2 装备，就提一嘴 MK2 系列装备之类的」；追问确认五项口径 + 追加「将稀有残骸回收的标题中的高级箱标签文字换成稀有」）**——**①保底矿物块（新）**：回收卡 econ 区在 `◆ 保底 ≈N ISK/h` 之上插入 `♨ 保底矿物：` + 每矿物缩进一行「矿物名 ×约 N（仓库 M）」（完全仿精炼卡的两段式与 `.app-belt-out`/`.app-dim`/`countWare` 口径，**零新增样式**）；每批量 = 批体积 10 m³ × 档位单方产量 ×(1+8%×残骸提纯学)×**池权重占比**（＜10 保留一位小数）；矿池取 **core 新增单点 `recycleMineralPoolOf(profile)`**（敌群特色池优先、缺省回落档位基础池），引擎 `rollRecycleGuarantee` 同时改为读该单点——界面与引擎同源，不会再出现"卡面列的矿与实际拆出的不一致"；原「产出倾向」一句话（`recycleNote`）**降为该块悬停说明**（星图打捞页仍作独立行）。**②`低出率掉落` → `特色掉落`**：卡面 `产出倾向：…` 行**删除**；**具名**只留真特色（该敌群主题件；稀有残骸 = 该敌群**专属装备具名**并注明「每炉必给一件」，G 族无人机写「鱿蜂无人机 ×10 架」；关底三把 MK3 武器自然在具名列）；**其余一律泛化不具名**——低安另有「MK2 系列装备」、直出基础池另有「民用与 MK1 系列装备」、蓝图碎片「高威胁另有蓝图碎片（MK2 集 25 片 / MK3 集 250 片，威胁 ≥17/≥41 与引擎同源）」；无具名件时整行标签退为**「其他掉落」**。**③卡头徽标「高级箱」→「稀有」**（船长追加）——**机制名「高级箱」在悬停、开箱日志、手册、公告里照旧不变**；徽标悬停同时压成一句（专属装备未出给主题件 + 高阶矿物，每件只结算一次）；卡面描述行由"来源与低出率物见下两行"简化为「每批拆解 = 保底矿物 + 概率彩头」。**④连带（同批，避免文案自相矛盾）**：手册「回收残骸」条目改写新口径；词典新增「保底矿物（残骸回收）」条目、「产出倾向」标注改展示位、「低出率掉落」更名「特色掉落」并重写；**已发布 B3.1 公告第 2 条按船长批准改写**（原文写「标出『产出倾向』与『低出率掉落』两行」→ 新口径）。**⑤改动文件**：`packages/core/src/salvage.ts`（新增单点导出，**数值/掉落逻辑零改动**）· `index.ts`（导出 `recycleMineralPoolOf`/`RARE_BOX_DRONE_UNITS`）· `ui/wreckFlavor.tsx`（重写为 `recycleFeatureOf` + `mineralRowsOf` + `FlavorTip` 可自定义标签）· `pages/IndustryPage.tsx`（矿物块、特色行、徽标与悬停、描述行）· `pages/MapPage.tsx`（星图同类行对齐；**不加矿物块**、保留倾向行——船长同意）· `panels/Handbook.tsx` · `data/announcements.ts` · `glossary.md`。**⑥验证**：typecheck ×4 + core **786/786**（新增 1 条 `recycleMineralPoolOf` 用例：特色池优先 + 缺省回落档位池 + 与引擎抽取同源）+ content:check（主题追加件 18 张断言照旧）+ desktop build 全绿；**负向验证**：把该单点临时改成 `profile.pool ?? []` → **6 项如实失败**（含引擎侧 5 项），还原后 786 全绿；**产物核对**：包内「♨ 保底矿物：」×1、「特色掉落」×6、「其他掉落」×2、「MK2 系列装备」×1、「民用与 MK1 系列装备」×1、「高威胁另有蓝图碎片」×2、旧串「低出率掉落」**0 处**、卡头徽标源码为「稀有」；**文案实机核对**：临时探针逐卡打印 6 张普通残骸卡 + 4 张稀有残骸卡 + G 族专属无人机卡的最终显示原文（探针用完即删，仓库 `_` 探针 0 个）【待验收/待发布】。

- 2026-09-10：**D 族「守墓古舰」专属装备定削 + 公告三张写入（船长：「对守墓古舰的装备进行削弱，回归到MK3附近的强度」→ 追问「是不是有一个敌族特色装备按照异星武器强度对标」→ 定「守墓者长炮DPS再下调10%，陵墓护盾阵列全抗性下调5%」→「修改口径。后公告写入。」；一号执行）**——①**数值**：`mod-lair-turret-d` 的 `dmgMult` **11.5 → 10.35**（装填 7350ms / 命中 1.0 / 射程 1300~12000 / 远端衰减 0.45 / CPU 62 / 耗弹 12 **全不动**）→ 名义 DPS **1.565 → 1.408/秒 = 精确 −10.0%**（占同族攻坚炮台 MK3 由 89.7% 降到 **80.7%**、单发由 2.24 倍降到 **2.02 倍**；**命中加权后仍为 MK3 的 103.5%**）；`mod-lair-shield-d` 三系 `shieldResistAdd` **0.35 → 0.30**（CPU 42 不动）→ 穿透 0.65 → 0.70（**承伤 +7.7%**），灰鲭鲨实例 动能 0.675 → **0.65**、爆炸/等离子 0.35 → **0.30**。陵寝装甲层（+110% / 速度 −25% / CPU 52）**未动**；三件合计 CPU 仍 156 ＜ 灰鲭鲨 195。**零存档迁移**（纯数值，玩家已持有的件自动生效）。②**口径作废与替换**：船长原口径「D 族是最强敌族，装备可对标异星档、可略强」（同日逐件过审时定）**作废**——触发是船长自问"是不是有个敌族装备按异星武器强度对标"，按"先去真实引擎量化再回答"核过后回报：**异星档全仓只有一件战斗件可比**（异星原型激光炮 vs 攻坚激光炮 MK3 = 名义 DPS ×1.056、射程 ×1.238、远端衰减**反而更重** 0.30 vs 0.35、CPU ×1.346），而 D 与之**不同构**（名义早就压到 MK3 的九成，强在必中与 63% 射程）。新口径 = **「最强敌族，强度略高于 MK3、靠必中与射程立身」**，同步改 `core/lairs.ts` D 族池注释（注明原口径作废）、`tests/lair-gear-d.test.ts` 头注释、设计稿 `docs/design/bounty-tasks-20260910.md`（三行表格 + D 族基准段 + 与 E 族分工句）。③**回归**：`lair-gear-d.test.ts` 两例改写并**新增两处定稿值钉死**（`shieldResistAdd` 三系 0.30、`dmgMult` 10.35）；`lair-gear-e.test.ts` **无需改**（E1 名义 1.91 仍 ＞ D2 新值 1.408，"E 名义更高"的分工断言不破）；`content-check` 护盾抗性值域 (0, 0.9] 内合法。④**公告（船长逐条批复后写入）**：14 张候选压到 **3 张**，写入 `packages/data/src/announcements.ts` **顶部**（22 → **25 条**）——`2026-09-10-rare-wreck-exclusive-gear`（稀有残骸解禁 + 五族专属装备，合并原第 11、12 卡；按船长口径**不提开箱/高级箱、不提概率与每炉件数**，解禁一句带过、专属装备单独介绍一遍）、`2026-09-10-drone-rework`（无人机流派重做，合并原第 14、1 卡）、`2026-09-10-blueprint-fragments`（**只留碎片门槛 100/1000 → 25/250**，市场段与量化句全删）。**取消不发**（船长判）：敌方混伤、组装机循环制造、界面与说明整理（含手册 / 图标动画 / 市场分档 / 宇宙底 / AI 徽标 / 状态窗动画 / 星图赏金徽标 / 调度学档位 / 悬赏附赠移除）。三份待审稿文件顶部均加「已结案」处置表。**写入时一处一字校正**：解禁句「即可获得」→「**可获得**」（专属装备非必得，避免承诺必出）。三号核验件 `docs/review/announcement-factcheck-20260910.md` 的三处待定夺随之结案：第 8 卡配色不符与主题相邻**随该卡取消而消解**、第 10 卡「口径」开发词**随市场段删除而消失**（新卡已复扫无开发词）。⑤**验证**：typecheck ×4 + core **786/786** + content:check + desktop build 全绿；**产物核对** = 包内 `dmgMult: 10.35` 与三张新公告标题齐备。⑥**并行会话留档（本轮期间 main 被推进）**：`341f087` 派系掉落定数 5%、`be5ec36` 战斗超时判负 + P1 口径复跑、`9bcf9e2` 待裁决清单；经核**无重复劳动**——`docs/review/pending-decisions-20260910.md` 第 170 行明写「公告（14 张待审稿）已按您指示移交一号」，`docs/design/power-ladder-rework.md` 未触及 D 族专属装备数值。**§5 待决策清单随之更新**：第 2 条（派系活跃掉落概率）**已由三号按"选定 5%"落地**；第 3 条（高级箱主题件兜底是否收口）仍待船长定数。【待验收/待发布】

- 2026-09-10：**战斗超时判负（视同被迫撤退）+ P1 口径复跑（船长三连裁决：「1,重跑P1口径。2不调整。3,超时直接判负,认定玩家撤退处理。」→ 追加「1.判负。2.停止循环清剿。3.精简说明：战斗超时，舰船被迫撤退正在返航。」；二号执行）**——设计稿 = `docs/design/timeout-defeat-20260910.md`（状态：已确认并实现）。**①判胜口径**：`combat.ts` 结束判定的超时分支**不再按剩余血量比判胜**（旧口径 `meRatio >= bestFoe` 即判我方胜 = **平局算赢**，作废；`totalHpRatio` 随之成死代码已删），改为 `ended='foe'`（判负）+ `autoEscaped=true` + 新增 `escapeReason='timeout'`（`BattleState` 可选字段，缺省 `'hull'`，**旧档零迁移**）；`balance.ts` 仅注释改口径（`maxBattleMs` 仍 10 分钟，**平衡数值零改动**）。**动机 = 关闭白刷口子**：旧口径下「无技能 鲣鱼+导弹MK3」（CPU 110 装 52、导弹射程 14.88km > 敌军 2.6~7.7km）可在威胁 52/58/66/80/88/88/96 **七格打满 10 分钟、满血、未击杀却记 100% 胜**，`expedition.ts:430/467` 据此照发全额赏金。**②结算（按撤退处理）**：主控远征两条自动撤离来源（结构损失过半 / 战斗超时）共用 `settleBattleRetreat`，新增第三档模式 `'timeout'` —— 耐久 = 失利扣损骰 ×0.5（7.5%~15%）、下限 5% 保护绝不弃船、维修费 = 期望奖励 ×25%、承伤持久化、弹药/修理件退回、无人机战损照扣、**停重复清剿**、`returnReason='retreat'` 转返航可召回；**遭遇战与 AI 副船走各自既有失利路径**（它们没有撤退概念，不新造轻损特例——船长裁决第 1 条）。**③文案（船长要精简）**：`⏱ 战斗超时（目标）：舰船被迫撤退，正在返航——耐久 -X%，维修花去 Y ISK。`（与船长样板「战斗超时，舰船被迫撤退正在返航」同形；**保留损失数值**是因为另两条同级撤退日志都带，不写会让超时成唯一静默扣费场景——**若船长要彻底去掉，删尾句即可**）；停环时另加 `重复清剿已停止（战斗超时）`。**④回归**：`tests/retreat.test.ts` +1（双方血量拉 1e9 必走超时判定，不依赖取样运气）断言「判负 / `escapeReason='timeout'` / **钱包不增反减** / 三条日志 / 耐久轻损且 ≥5% / 船还在」，core **786/786**。**⑤P1 口径复跑（船长裁决第 1 条，已含新判胜口径）**：结果落 `docs/design/power-ladder-rework.md` **§七**（9 种子 + **纯主系对照列**做归因）——**归因两笔账**：**(甲) 时长漂移不是失守**：§五 的 40~90s 带是**单波**口径，09-09 **多波次**批次已把**四张 E 段卡全部拆波**（噬口/坟场 2 波 0.5~0.55+0.45~0.5；虚海/穹顶 3 波 2+2+1），目标改为「90~150s 无喘息」，今日锤头鲨中位 E 段 76/67/95/72s 落在新口径内；**未拆波**的赤潮/蜃影两格（38s/54s）与定稿（37s/51s）**几乎逐秒吻合**＝反证漂移来自拆波而非全局数值松了。**(乙) 真正失守三处**：a.「无技能宽容线」4 格 **100%→0%**（纯系也仅 0~56% → **主因多波次，混伤补一刀**）；b. S2「可磨」行 虚海/穹顶 100%→0%（纯系亦 0% → 与混伤基本无关）；c. **锤头鲨中位穹顶 100%→33%**、**S4 虚海 100%→11%、穹顶 100%→0%**（纯系仍 100%/100%/89% → **主因混伤**）。**仍达标**：两墙点（赤潮对 S1 中位、蜃影对 S2 中位）**均 100%** ✓、锤头鲨中位噬口/坟场 100%（残血 89%/112%）✓。**结论**：P1 定稿表已不能直接当今日验收线（时长带被多波次取代属设计演进，但「无技能宽容线」与「S2 可磨」两条底线**在混伤之外就已失守**）——**数值处置（重标敌血/敌伤 or 重立验收带）需船长给数，本轮只重跑不改数**。**⑥工具**：`tools/mixed-damage-review.ts` 新增 **D 段 P1 口径复跑**（复刻 §五 表行列 + 纯系对照 + 定稿基线，`npm run battle:mixed-review` 一键复现）。**⑦待裁决清单**：本节结出的 8 项（超时文案尾句 / 无技能宽容线 / S2 可磨行 / E 段顶格 / P1 旧账 7 处待终审 / 无人机回收率 / 两处 foeDmgMul 0.35 / 验收存档）+ 已结清 6 项，已汇总为 `docs/review/pending-decisions-20260910.md`（船长：「将所有详细列出让我审核下」）。**验证**：typecheck ×4 + core **786/786** + content:check + desktop build 全绿。【待验收/待发布】

- 2026-09-10：**派系活跃稀有残骸掉落概率定数 10% → 5%（船长：「派系掉落概率要调」→ 选定 5%）**——**改动**：`packages/core/src/lairs.ts` 的 `FACTION_RARE_DROP_CHANCE` **0.1 → 0.05**（注释改写为"船长 2026-09-10 定：5%"，并写明期望产出 ≈0.7 件/天与"不与当日赏金席位争主供给"的理由）。**依据 = 正式工具 `npm run faction:audit` 的期望产出对照**（均值 3.5 趟/时 → 4 小时 ≈14 趟）：5% → **0.7 件/天**（4h 刷）· 10%（旧）→ 1.4 · 15% → 2.1 · 20% → 2.8；对照当日 5 席赏金全清 8~11 件/天。**连带收口（防口径漂移）**：①`tools/content-check.ts` 的体检行由「待船长核定」改为「**船长 2026-09-10 核定**」；②`tools/faction-audit.ts` 头注与打印行同步（表内 `← 当前` 自动落在 5% 行）；③**`tools/bounty-econ.ts` 的「日供给」原先把派系活跃写死 1.4 件/天，现改为按 `FACTION_RARE_DROP_CHANCE × 审数基线 14 趟/天` 推导**（复跑：日供给 **9.8 → 9.1 件/天** = 全清日板 8.4 + 派系活跃 0.7）；④**测试去运气化**：`packages/core/tests/bounty-tasks.test.ts` 的掉落频率用例在概率减半后，"40 局至少命中一次"会变成 **13% 概率的运气失败**（0.95⁴⁰≈0.129）→ 局数提到 **200**（0.95²⁰⁰≈3.5e-5）并把窗口收到 ±0.1（6.5sd）；⑤文档同步：词典「敌对派系活跃掉落概率」条目改写为已核定 5%（并清掉残留的旧"占位 20%"口径）、设计稿 `docs/design/bounty-tasks-20260910.md` §7 由"唯一待审数值"改为"已定 5%"+定数理由表。**验证**：typecheck ×4 + core **785/785** + content:check（打印「掉落概率 5% ×1 件，船长 2026-09-10 核定」）+ desktop build 全绿；产物核对 = 包内 `const FACTION_RARE_DROP_CHANCE = 0.05;`，卡面「N% 概率 ×1」由常数推导（自动显示 5%，无需改文案）；经济工具二次复跑 `faction:audit` / `bounty:econ` 通过【待验收/待发布】。

- 2026-09-10：**敌方混伤副作用复核 / 战斗复核（船长：「公告由一号负责，你进行战斗复核」；二号执行）**——复核报告 = `docs/design/mixed-damage-review-20260910.md`（状态：复核完成，**数值待船长裁决**），证据原件 = `docs/design/battle-data/mixed-damage-review-20260910.txt`，工具 = `tools/mixed-damage-review.ts`（**正式入库**，挂 `npm run battle:mixed-review`，一次性探针 `_` 前缀已按工具纪律转正）。**方法（零取样噪声优先）**：各臂**同种子**对照且**敌总伤/武器形态/命中/近盲/衰减完全一致**，唯一变量是伤害构成——纯主系臂 = 混伤上线前行为；A 段解析式「击穿玩家所需敌总输出」（TTK 代理，逐发真实结算含盾→甲→结构层迁移）全 25 张混伤卡全遍历；B 段真实模拟 26 档 × 3 装配 × 2 技能档 × 9 种子；**口径自检**=纯系口径与旧路径 `applyDamage` 逐字等价 ✅；**交叉验证**=8:2 臂与当日实机矩阵对得上（S4 中位 虚海 11% vs 矩阵 20%、穹顶 0% vs 0%）。**四条结论**：①**承伤倍数**（纯主系÷该口径，满技能档）：裸船 1.09 / S1 1.28 / **S2·S4·T3 均 1.51（最大 2.18~2.29）** / D3 无人机 1.11——**副作用随抗性堆叠程度放大**，越认真配装被削越多；旋钮标价：常驻 8:2→9:1 把平均倍数压回 **1.27（−16%）**、窝点 6:4→7:3 从 **1.89 压回 1.68~1.73（−11%）**。②**堆单系抗收益腰斩但激励方向未反转**：硬化主系 vs 无抗件的 TTK 提升 纯系 +110~117% → 8:2 **+58~61%** → 窝点 6:4 **+33~37%**；8:2 下硬化副系仅 +17~19%，堆主系抗仍是最优解（窝点 6:4 下两者才接近打平，是唯一"取向怎么选都差不多"的场景）。③**实战差异定向集中在 E 段**：26 档里 **19 档零差异**、3 档反而更轻（蜃影 +10 / 深渊之门 +10 / 赤潮 +3，副系按族签名取、非按玩家弱点取）、4 档更疼（穹顶 −32 / 虚海 −24 / 幽灵舰 −2 / 噬口 −1 点残血）；**破门槛格**：S4 中位 虚海 100%→**11%**、穹顶 89%→**0%**、S2 满 噬口 100%→**33%**、T3 中位 穹顶 100%→**33%**（满技能档仍全过、残血变薄）→ 口径是「**不是打不过，而是中位技能档被打穿、需满技能或更好装配**」。**⚠ 挂账给船长**：2026-09-09「敌人强度复调轮 P1」的验收口径按**纯系**标定，混伤后那些数字已不描述线上实况（尤其 E 段中位档）——重跑 P1 口径、还是把"E 段中位需满技能"当既定结果接受，请船长定。**最疼的卡根因**：泰坦残骸勘探/奥罗武装残骸群/坟场守墓人/虚海守望者/穹顶守卫 五张都是「主系动能 + 副系能量」（D/E 族签名），而玩家必堆动能（盾抗 70.8%）、能量只 10% 抗 → 20% 副系绕开主抗，8:2 承伤 2.00~2.18 倍、窝点 6:4 到 2.80~3.21 倍。**附带观察（与混伤无关，建议单独立项）**：`combat.ts:2111-2116` 打到 `maxBattleMs` 上限时按剩余血量比判定且 **`meRatio >= bestFoe` 即判我方胜（平局算赢）**，`expedition.ts:430/467` 据此**照发全额奖金 + 声望 + 战利品**；当日矩阵「无技能 鲣鱼+导弹MK3」在威胁 52/58/66/80/88/88/96 **七格全是 `100/600s/100%`**（打满 10 分钟、满血、未击杀却记 100% 胜），该行装配合法（鲣鱼 CPU 110、导弹架 MK3 占 52、射程 14.88km > 敌军 2.6~7.7km）——是"风筝成功"还是"接近期僵局"本探针未定根因，**待裁决是否改判胜口径**。**本次复核边界（未覆盖）**：窝点真实战斗（威胁倍率+僚机+波次，6:4/7:3 只做份额灵敏度）、无人机流 D1/D2/D2b、无技能档的实战臂。**未改任何数值**：甲乙丙丁四选项见报告 §7，等船长给数。**踩坑固化**：自检里参考循环漏写 `.hp`（`applyDamage` 返回 `{hp,dealt}`）导致 18 处假阳性——**工具自检失败先怀疑自检本身**，另 `applyDamage` 未从 `@whale/core` 导出、须从 `packages/core/src/combat` 相对导入。【待验收/待发布】

- 2026-09-10：**玩家可见文案清理：「占位 1 m³/件」→「按 1 m³/件 计入货舱」（船长：「改：统一为『按 1 m³/件 计入货舱』」）**——触发 = 三号核验公告待审稿时顺带扫出的**开发词残留**：「占位」属约定第一章明列的开发/商讨阶段用词，却出现在玩家可见界面（装备装船体积说明）。**改动（零机制、零数值，纯文案）**：`pages/CargoPage.tsx` 5 处（船载装备面板右上「N 种 · 按 1 m³/件 计入货舱」×2（列表模式与图标卡模式）、行内「×N（计入货舱 X m³）」、图标卡副行「×N · 计入货舱 X m³」、点选弹层「×N（计入货舱 X m³）」）+ `pages/ItemsPage.tsx` 4 处（装船失败提示、装船成功提示、两处「装到船上」按钮悬停说明），另同步 4 条开发注释的措辞（注释本可留，为免日后检索误判一并改）。**机制不变**：装备装船仍按每件 1 m³ 计入货舱（2026-09-09 船长口径 A），扣库与卸回路径一字未动。**验证**：typecheck ×4 + core **785/785** + content:check + desktop build 全绿；产物核对 = 新串「按 1 m³/件 计入货舱」**6 处**、「计入货舱」**9 处**、旧串「占位 1 m³/件」**0 处**；源码复扫确认「占位」仅剩注释内出现（非玩家可见）【待验收/待发布】。

- 2026-09-10：**三号核验：公告待审稿 14 张逐卡对数（船长：「继续之前的工作」）**——新任三号开工先追平基线（verify → `cb4d29e` = main，两树干净）并跑四连验证（typecheck ×4 + core **785/785** + content:check + desktop build 全绿），随后接上交接卡第一顺位：**在船长批复前把三稿共 14 张公告待审稿与代码事实逐条对数**，产出核验件 **`docs/review/announcement-factcheck-20260910.md`**（含逐卡对照表 + 可复现核验法 + 待船长定夺项）。**核验结论**：①**数值类断言 14 张全部与代码一致、无一处需要改数**（梭鱼 3/王鲭 4 高槽、调度学 rank5 = **89.66h** 与 +2 个百分点、碎片门槛 **25/250**、蓝图书权重 **0.05** 与寿命 **6 小时**、高级箱 **5/8/10%** 与一炉一件 30 m³、五族 **15 件**专属、混伤常驻 **8:2** 与窝点派生 **6:4**（`lairAnomalyOf` 实测 60:40）、教学卡纯系、点防 **500ms** 判定与回收 **10%→50%**）；②**闸门守住**：15 个待审 id 在 `announcements.ts` 里出现 **0 次**（该文件仍 22 条）；③**人称与话术**：无第一/第二人称，开发话语残留 1 处（第 10 卡「口径」）。**三处待船长定夺**：**(a) 第 8 卡配色描述与实际不符**——稿写「重装橙 / 航运蓝」，实测舰族色为重装 **`#cdd6e0` 灰白**、航运 **`#ffd166` 金黄**，且「与舰队卡一致」不成立（**舰队卡与装配页舰影用统一默认灰蓝 `#8aa0b8`**，只有战场主舰与状态窗传舰族色）→ 建议改文案；**(b) 第 8 卡与已发布 `2026-09-10-ship-visuals` 主题相邻**（该卡已含状态窗与「采掘与打捞时浮动并点尾焰」）→ 建议只留新内容或并入大更卡；**(c) 第 10 卡「口径」属开发词** → 建议改「同一比例」。**另有本次顺带发现的非本批残留**：玩家可见界面出现开发词「**占位 1 m³/件**」（`CargoPage.tsx` 5 处 + `ItemsPage.tsx` 4 处文案位，打包产物命中 6 处）——机制（装备装船按 1 m³/件计）无异议，建议文案改「按 1 m³/件 计入货舱」，待船长点头再作为独立小批落地。核验用临时探针 `tools/_verify-announce.ts` 用完即删（仓库内 `_` 探针 0 个）。**未推送**：本地领先 origin **132 个提交**（推送闸门照旧：待验收 + 公告批准后代码与公告一起推）【待验收/待发布】。

- 2026-09-10：**二号交接：给下一位二号的开工卡（船长：「书写交接文档，你的会话已经过于长了，准备交接新的二号继续工作」）**——新增 `docs/design/handover-d2-next-20260910.md`，定位是**接任者的开工卡**（不是又一份流水账），十节：**①身份与工作区**（谁是二号、d2 worktree 与主树边界）②**开工三步**（核基线命令：两侧 head / 两侧 `status` / 起手跑验证闭环）③**七条铁律**（四步闸门、完成即合入、推送闸门、玩家可见文本无开发词、**文件只用编辑器工具改**、**中文检索别用 PS 5.1 的 `Select-String`**、动数据先跑 content:check）④**合入 SOP**（含 `ff-only` 被拒的正确处置：停手报告等对方提交，绝不 force；`docs/roadmap.md` 冲突"两条并存"且 `checkout --theirs` 后重插条目**必须再 `git add`**）⑤**当前基线**（写卡时 d2 = main，core **785/785**，两侧工作区干净、无 `_` 探针；未推送计数以当日值为准）⑥**接手内容的地图**（本会话引入的 11 项引擎能力/契约 + 内容现状：五族 15 件、高级箱已解禁 5/8/10% 且每炉锁 1 件、敌方混伤 8:2/6:4）⑦**可调旋钮四个入口**⑧**待办与待船长决策（带优先级与"谁推"）**：公告批复（最紧，两条待审稿路径）、混伤副作用复核、验收存档、更早遗留旋钮、后续新活⑨**与一号/三号的协作界面**（冲突热点文件清单 + "对方在途压你文件就停手报告"口径；同日一号与三号也各写了给新一号/新三号的接手包，三份**互为对方视角**、不重复）⑩**命令速查 + 七条经验教训**（BOM/中文假阴性/别与对方并行改同一文件/测试别靠取样运气/UI 改动靠产物核对/数值先要船长的数）。同时把上一份 `docs/design/handover-d2-20260910.md` 的定位改成"开工卡的明细附录"并在其顶部加了指引。**交接状态**：d2 = main，两侧工作区干净，全部验证绿；本次落地途中连撞两次同日提交（一号 `4cad042`、三号 `d44d7c8`），`docs/roadmap.md` 每轮都同位插条 → 每轮按"两条并存"解冲突。typecheck ×4 + core **785/785** + content:check + desktop build 全绿【待验收/待发布】。

- 2026-09-10：**三号交接文档（船长：「书写交接文档，你的会话已经过于长了，准备交接新的三号继续工作」）**——新增 **`docs/design/handover-d3-20260910.md`**：「给继任的三号」的工位级交接 brief（本会话过长、压缩点已到）。八节：①**工位与铁律**（工位 = `Deepseek-EVE-verify` 分支 `verify`，**不叫 d3**；基线 main = verify = `9e7d6bf`；合入舞步「verify 提交 → merge main 解冲突 → 主树 ff」；合入前看主树脏否、绝不 force；推送闸门 126 个提交未推）②本会话 21 批交付一览（细节指向本归档件）③**★ 你要接手的事**（公告 14 张三稿待批 + 我最后三批未起草公告 + 第 8 卡与已发布 `2026-09-10-ship-visuals` 主题相邻 + 待核定数值 + 星图两处既有贴线 + 布局本地覆盖提醒）④我改过的地方（星图段/组装机卡/引擎与存档/坐标/数据/共用件/样式/契约，逐条标明入口文件名）⑤**船长已定口径七条**（循环制造卡片级、附赠移除但机制保留、派系标记居中+上抬+闪缩、两处坐标改动依据、市场三类型、透明度三档、舰影列按实宽）⑥我怎么验证（四连 + 产物核对 + 负向验证 + 星图几何探针复刻口径 + 探针用完即删）⑦已知坑（PowerShell 改文本弄花中文、PS 5.1 中文检索假阴性、roadmap 冲突两条并存、宽窄要量实宽、改数据先想契约、合并顺序）⑧接手检查单。项目级入口仍是一号的 `handoff-20260910-session-turnover.md`，本页是其**三号工位版补充**（不重复通用内容）。【待验收/待发布】

- 2026-09-10：**会话交接文档（船长：「书写交接文档,你的会话已经过于长了,准备交接新的一号继续工作」）**——新增 `docs/design/handoff-20260910-session-turnover.md`：给**零上下文的新一号**的接手包，八节——§0 一分钟开工顺序、§1 工作区与铁律（主树/d2/verify worktree、真档只读、**推送闸门：本地领先 origin 126 提交全未推送**、一级页不滚/视觉物件 SVG/公告需批准）、§2 船长的四步闸门与交互习惯（回答极简、给的数字即结论、"是否可行/是否被砍"要先去真实引擎量化）、§3 系统地图与**本批关键常量所在文件**、§4 本会话 8 批交付对照提交号、§5 **待船长决策清单**（14 张公告待批分三稿、派系活跃掉落概率仍标待核定、高级箱主题件兜底是否收口、常驻蓝图书渠道已定"不管"）、§6 验证闭环与常用命令（含无头 Chrome 探针流程）、§7 **已知坑与教训 10 条**（PowerShell 改中文/引号炸/PS5.1 检索假阴性/git 提交信息含直引号被截断/构建清 dist/tools 不在 typecheck/`need===100` 反推档位的陷阱…）、§8 接手检查单。文档内 17 条引用路径与 5 处关键常量值均已逐条核对【待验收/待发布】。

- 2026-09-10：**三号工作区（verify）收尾归档与续接卡（船长：「机制保留，进行收尾归档」）**——归档件 = **`docs/design/handover-verify-20260910.md`**（续接卡：本会话在 verify 交付的**全部批次与 commit 对照**、验证快照、本会话引入的 state/视图字段与共用件与护栏、可调旋钮索引、待船长决策项、移交事项、踩坑记录）。**船长"机制保留"的落实**：组装机「循环制造」的**卡片级机制**（`state.manufacturingLoops` + 结算链路 + 卡片控制条）与悬赏 **`loot` 字段/结算链路**一律**保留**，本会话只清数据（26 张悬赏卡 `loot` 全空）与改文案，不删字段、不删分支；重复清剿的「货仓放不下缴获」判定因此永不触发（手册原文按船长指示保留）。**归档时状态**：verify = main = `1dd743d`，两树干净；typecheck ×4 + core **785/785** + content:check + desktop build 全绿；**本地未推送 123 个提交**（推送闸门照旧）；仓库内**无 `_` 探针残留**。**归档中列出、需接手人留意的四件**：①公告待审稿 11 张（我拟 9 + 一号补 2），本会话最新三批（循环制造上卡片级 / 星图派系标记对齐与闪缩 / 移除悬赏附赠）**未起草公告**，需要发就补卡；②`FACTION_RARE_DROP_CHANCE`（10% ×1 件）与点防 PD 六参数仍待船长核定；③星图两处**既有**贴线（烬火星区→红环航道 距暗星坟场 11.5、柯尔边境→星髓迷宫 距灯塔长廊 3.1）待定夺；④星图布局 localStorage 覆盖会让内置坐标改动**看不见**（需清覆盖或拖一下）。【待验收/待发布】

- 2026-09-10：**归档（船长：「进行归档,并给我一个实机测试存档」）**——①**归档续接卡** `docs/design/handoff-20260910-rarebox-batch.md`：本批 6 个提交逐条对照（蓝图速率 / 碎片集齐前不重复 / 窝点地图级别 / 工业页产出显示 / 稀有残骸解禁 / 高级箱数值与每炉锁件）、关键数值与复跑命令、代码入口与契约护栏、存档兼容说明、待批尾巴（11 张公告稿、常驻蓝图书渠道"不管"、主题件兜底是否收口）。②**实机测试存档**：`tools/make-test-save.ts` 新增 `rarebox` case → `docs/test-saves/test-save-rarebox-20260910-224146.json`（＋同批 `user-backup-20260910-224146.json` 双保险）；注入五族稀有残骸共 11 件（330 m³）、普通残骸 100+100 m³ 做对照、基础 AI 核心 ×2（可并行 3 台）、碎片 20/250 对照、全舰耐久回满；`docs/test-saves/README.md`「档案清单」补该 case 的注入门槛与 6 步测试路径；生成后用引擎读档自检（11 件/330 m³、碎片 20 与 250、AI 核心 2、驾驶船白鲨级、版本 24、无残留在炉）【待验收/待发布】。

- 2026-09-10：**移除全部常驻悬赏的附赠矿石（船长：「将所有常驻悬赏附赠的矿石移除了」；追问后确认"全部 20 张一律清空"、"手册保留原文"）**——**原状**：26 张悬赏卡里 **20 张带 `loot` 附赠**（19 张是**矿物**：类银超金属/三钛合金/超噬矿/星髓晶/同位聚晶/类晶体胶体/虚空晶/冥铁合金，单张 300~45,000 ISK；**1 张是原矿**：占港武装通缉 → 富凡晶石×150 ≈1,950 ISK），全部附赠合计 ≈ **192,775 ISK**（每张卡各一次，占各卡奖金 0.3%~9.8%）；卡面显示为「奖金 X ISK **+ 富凡晶石×150** · 声望 +N」。**改法（数据层，零代码改动）**：`packages/data/src/anomalies.ts` 里 26 个 `loot` 字段**一律清空为 `[]`**（其中 20 个由非空清空），文件头补一段 2026-09-10 决策说明；两处提到附赠的卡面文案同步改写——占港武装通缉删掉「战斗后可从平台残骸顺手回收一批矿石。」、泰坦残骸勘探把「残骸里的矿物归你，赏金照发」改为「击退守墓编队，赏金照发」。**保留项（船长定）**：`loot` 字段与整条结算链路**原样保留**（远征/AI 副船发放、卡面「+ 附赠」条件显示、重复清剿的「货仓放不下缴获」判定），方便日后给新卡挂附赠；**手册里「货仓装不下缴获会暂停」一句按船长指示保留原文**（机制仍在，只是悬赏卡不再产生附赠）。**连带行为**：悬赏胜利不再往货仓塞东西 → 重复清剿的货仓判定自然不再触发（其余停机条件不变）；AI 副船战报的「战利品 …已入仓库」段随之为空而不再出现。**验证**：typecheck ×4 + core **774/774** + content:check + desktop build 全绿；产物核对（`anomalies.ts` 内 `loot: []` ×26、无非空残留）；测试不受影响（`dock-unload`/`t8`/`helpers` 等用例自带测试卡数据，不读真实悬赏表）；临时探针 `tools/_probe-loot.ts`（用来列出带附赠的卡与价值）已删；`docs/review/A1-bounty-review.md`（22 张走查表）按仓库惯例加了一条【2026-09-10 作废注】而不是改写历史行。【待验收/待发布】

- 2026-09-10：**二号工作区收尾归档 + 未完成工作盘点（船长：「进行收尾归档，检查未完成工作」）**——归档件 = `docs/design/handover-d2-20260910.md`（续接卡：本会话交付批次与 commit 对照、验证快照、**本轮引入的全部新字段/新契约/护栏**、可调旋钮索引、待船长决策项、移交事项、踩坑记录）。**盘点结论**：①**已全部落地并合入 main**：A/C/D/E/G 五族专属装备 15 件（`a891232`/`c9399c9`/`089d817`/`fa96850`/`7d7eefb`/`f52f01f`）、G 族第一件改造为专属无人机（`f76bb56`）、界面显示缺口修复（`d5d3c58`）、敌方混伤（`956db2f`）；两工作区均无我方未提交改动，无 `_` 临时探针残留。②**补上唯一测试缺口**：高级箱解禁后开箱链路正式上线，此前只测到 `rollRareBoxExtra` 返回值、没测"入仓"那一段——`lair-gear-g.test.ts` 新增一例（真实起炉 → **10 架进物品仓库 + 回收明细记「无人机 10 架」+ 日志写明 ×10 架**）；**该例当日被一号的命中率定稿 5/8/10% 变成脆用例**，故改为**把命中率临时拉满、只验命中之后的入仓链路**（不依赖取样运气，跑 6ms），core **785/785**。③**未完成项（已移交/待船长决策）**：**(a) 公告**——本会话三批尚无公告卡，已按约定拟 `docs/design/announcement-draft-20260910-d2-batches.md`（2 张卡：五族专属装备 / 敌方混伤，玩家第三人称、无开发话语）等待批复；⚠ 高级箱已解禁 = 内容**已对玩家生效**，建议优先批；无人机损失稿 `announcement-draft-20260910-drone-losses.md` 亦仍待审。**(b) 混伤副作用复核**：「堆单系抗」变弱，若实测承伤涨幅过大，最省旋钮 = 副系份额（8:2→9:1、6:4→7:3）。**(c) 验收存档**：是否要一份"五族装备 + 鱿蜂无人机 + 各档敌军"实测档（`tools/make-test-save.ts`）。**(d) 更早遗留旋钮**：无人机战后基础回收率 10%→20%、点防参数、`FACTION_RARE_DROP_CHANCE`（content:check 仍打印"待船长核定"）。**(e) 一号侧在途**（非我方）：其"高级箱数值定稿"批已提交（`42cc3a2`），我方本次合并时 `docs/roadmap.md` 再次同位插条 → 按"两条并存"解冲突；其临时探针已按约定并入 `tools/bounty-econ.ts` 后删除。**(f) 推送闸门**：本地领先 origin **119 个提交**，待验收 + 公告批准后代码与公告一起推。④**踩坑固化**（写进归档供后续接手）：`docs/roadmap.md` 是最常见冲突点（两位 agent 同往顶部插条）→ 解冲突口径"两条并存"，且 `git checkout --theirs` 后**重新编辑必须再 `git add` 一次**（否则合并提交漏内容，本会话踩过两次）；合入前先看主树脏否，被拒即停手报告、绝不 force；**对方在途改动压在我方文件上时（本次是 `lair-gear-g.test.ts`）先等对方提交再合，别抢先改同一文件**。typecheck ×4 + core **785/785** + content:check + desktop build 全绿【待验收/待发布】。

- 2026-09-10：**高级箱数值定稿 + 稀有残骸「每炉锁死 1 件」（船长：「概率降低至 5/8/10%，稀有残骸精炼时每炉数量锁死为 30 立方米是否可行?」→ 追问「非命中的收益是否被大砍?」→ 确认「行」）**——①**命中率**：`RARE_BOX_GEAR_CHANCE` 常/险/危 **25/40/55% → 5/8/10%**（原值一周就能集齐五族十五件，太容易）。②**每炉锁 1 件**：炉次新增 `lockUnits?`（= 本轮剩余可投料量），稀有残骸起炉写入 `RARE_WRECK_VOLUME_M3`（30 m³ = 一件），每批扣减、用尽走"原料耗尽"同一条路径自动停炉并写专门日志（「本炉定额完成（每炉 30 m³ = 1 件，共 3 批）…余料仍在货仓/仓库——想多开就再起一炉」）——**一炉恒等于一件**，与"一炉一箱"语义合一，玩家也不会再把多件丢进一炉而少拿箱子；普通残骸与精炼炉**不写该字段 = 不限制**（仍"整批直到料尽"），老档天然同义 → **零迁移**（`save.ts` 只在为正数时回填）。③**界面**：稀有残骸回收卡数据行改口径——「可用 N m³（M 件）· 每炉锁 30 m³ = 1 件 · 每批 10 m³ / 25 秒 · 一次起炉 = 开一箱（想多开就再起一炉，同型可多台并行）」；普通残骸卡文案不变。④**"非命中收益是否被大砍"的实测答复（船长问）**：没砍，反而升——命中给的是**不可出售**的专属装备，未命中给的是**可出售**的主题增幅件，命中率降下来只是让更多箱子落到能卖钱那一路：深层卡每箱可兑现 ≈2.1 万 → **≈3.5 万 ISK（+66%）**、外围有主题池卡（烬火）≈9.6 万 → **≈11.9 万（+26%）**；真正被砍的只有**专属装备到手速度**（深层档 ≈5.4 件/天 → **1.0 件/天**）。⑤**集齐节奏**：一炉一件 → 集齐一族三件 ≈**60 / 37.5 / 30 箱**（外围/核心/深层）→ 按全清日板 ≈9.8 件/天 折算 **≈3.1~6.1 天/族**（五族 ≈15~30 天）。⑥回归：`bounty-tasks.test.ts` 新增 2 条（**每炉锁死 1 件**：6 件起 1 炉只吃 1 件、3 批后定额停、料还剩 5 件、台账 1 箱、日志含「本炉定额完成」；**同型可再起一炉**：两炉各吃 1 件各开 1 箱、普通残骸 `lockUnits` undefined）；**连带修**：二号 `lair-gear-g.test.ts` 的无人机掉落用例在命中率下调后，靠"反复开箱到出专属"取样会先抽中同池两个模块 → 显式收窄池子为 `[BEE]`（只验"命中之后给什么"）。⑦工具：原临时探针 `_rarebox-econ.ts` 的对照**并入正式工具** `tools/bounty-econ.ts` 尾部新节「稀有残骸·高级箱对照」（每卡掷 2 万次：专属命中 / 主题件兜底 / 矿物 / 每箱可兑现 / 集齐一族天数），探针已删。⑧文档：设计稿 §4 数值表与经济对照全量更新、词典「稀有残骸」条目补"一炉一箱 + 每炉锁 1 件 + 新命中率"、公告待审稿第 11 卡同步定稿数值。typecheck ×4 + core **785/785** + content:check + desktop build 绿【待验收/待发布】。

- 2026-09-10：**稀有残骸解禁（船长：「二号已经将敌族专属装备完成，可以解禁稀有残骸」）**——**口径**：船长逐项定了三件——**一炉一箱**（按炉结算、不按件累积，保持现实现）、**卡面不写概率**（保持现状）、**箱内数值要调（方向待定，本批先用现值上线）**。**改动**：①`industry.ts` 删掉 `startRecycleRun` 的稀有残骸拒绝分支（"协会回收炉暂不受理…"），稀有残骸此后与普通残骸同一条链路，区别只在 `profile.rare === true` 时的"首批触发一次高级箱"；`isRareWreck` 导入随之下线。②`IndustryPage.tsx` 的 `wreckDefs` 去掉 `!isRareWreck(def.id)` 过滤（卡片不再缺席，自带「高级箱」徽标）。③**玩家可见文案全部改口**：战报 `expedition.ts`「✦ 敌对派系活跃战果…（回站用回收炉解体开高级箱）」、任务结算 `sideTasks.ts`「战场留下稀有残骸 ×N——可前往该星系打捞（回站用回收炉解体开高级箱）」、星图打捞卡悬停 `MapPage.tsx`（写明"常规保底之外必定额外掉落一件 + 一批高阶矿物"）、稀有残骸物品描述 `salvage.ts`（改为"回站解体可开高级箱"）。④回归：`bounty-tasks.test.ts` 两条旧"暂不开放"用例重写为**解禁用例**（"可起炉、普通残骸仍报'没有'而非'不受理'"）+ 新增**一炉一箱用例**（2 件同炉 = 6 批，第 1 批结算一次高级箱、后续 3 批不再开第二箱、产出并入 `recAcc` 台账）；该文件 35→36 项。⑤**经济对照**（临时探针 `tools/_rarebox-econ.ts`，真实引擎掷 2 万次/卡）：日供给（全清日板）**8.4 件/天**（外围 2.36×1＋核心 1.93×2＋深层 0.72×3）＋派系活跃 ≈1.4 件/天 = **≈9.8 件/天**；每箱专属命中 25/40/55%（未命中必给主题件）、矿物 300/120/40 单位（外围 ≈3~4.5k ISK、深层 ≈6~7k ISK）；**集齐一族三件 ≈12 / 7.5 / 5.5 箱 → 按 9.8 件/天 折算 0.6~1.2 天/族**——专属装备到手偏快，**箱内数值方向待船长定**（探针待定稿后并入正式工具再删）。⑥文档：设计稿 §4 补"已解禁 + 一炉一箱 + 经济对照表"、词典「稀有残骸」条目改口径、公告待审稿新增第 11 卡（稀有残骸解禁）。typecheck ×4 + core **783/783** + content:check + desktop build 绿【待验收/待发布】。

- 2026-09-10：**敌方混伤（船长：「给所有赏金任务的敌人添加额外攻击的副伤害类型，让其攻击造成混伤（主类型占大部分），并需要在任务中告知玩家」→ 同日追定「常驻悬赏也改，比例约 8:2」）**——设计稿 `docs/design/mixed-damage-20260910.md`（状态：已确认）。**实质 = 让一个早就存在、却只被用来"挑主系"的字段真正生效**：`AnomalyDef.dmgMix` 此前只经 `pickTopType` 取最高权重那系当敌人唯一伤害，玩家实际只挨一种伤、卡面只显示「敌主伤 ⬤」。**①配比与范围（船长裁决）**：常驻悬赏 / 4 张低安遇袭隐藏模板 = **主 8 : 副 2（80/20）**；窝点派生卡 = **主 6 : 副 4（60/40）**（窝点更"混"，本就是更硬的特色敌人，`lairAnomalyOf` 写 6:4 且**主系不许变**）；**教学卡（演习场讨伐令）不写 dmgMix = 保持纯系**（不给新手第一场上混伤）。**②语义变更**：`dmgMix` 的**正权重键 = 参战系**（≥2 系即混伤、份额按权重归一化）——旧口径"没写的系缺省权重 1"**作废**（那条会让只写一系的卡悄悄变成三系混伤）。**③副系 = 敌族签名序**（`FOE_SUB_DMG`，撞主系顺延）：A 海盗 动能→爆炸→等离子 / C 异形 爆炸→等离子→动能 / D 守墓 等离子→动能→爆炸 / E 巨构 等离子→爆炸→动能 / G 流亡 爆炸→动能→等离子（无族卡按 A 序兜底）；25 张卡据此逐卡定主副。**④引擎三处同源**（缺一处就会出现"卡面写混伤、预估按纯系算"）：`createFoeSpecs` 出多系单发（复用我方炮台 `shotsByType`，**Σ 恒等于总单发 = 敌总伤不变**）、实时结算新增 `applyFoeShot` **逐系** `applyDamage`（各系各吃自己的层位克制与层抗）、**胜率预估**改按份额加权。**形态/命中/近盲/衰减一律仍按主系**（主系是能量仍**光束必中**）。**⑤告知玩家**：三处卡片显示位（常驻悬赏卡 / 派系活跃卡 / **赏金任务卡**）由「敌主伤 ⬤」改为「**敌火力 ⚫动能 60% · ⚫爆炸 40%**」（新共用组件 `ui/shipInfo.FoeDamageMix`，数据源 = core `foeDamageComposition`，与战斗同源），悬停补"只堆主系抗会被副系穿透"。**⑥契约**：`content:check` 新增「敌方混伤契约」——非教学卡必须两系且主 8 : 副 2、副系须等于族签名值、窝点派生须同主系 + 6:4（现打印「25 张敌军卡主 8 : 副 2（窝点派生 6:4、主系不变）；教学卡保持纯系」）。**已知副作用（写进设计稿）**：「堆单系抗」变弱（副系绕开主抗，极端例：对动能抗 90% 的目标混伤能打进去明显更多）；单发极小时副系取整成 0 会并入主系一条（不产生 0 伤害条目）。回归 +8（`tests/mixed-damage.test.ts`：80/20 与 60/40 构成、教学卡纯系、19+ 张卡副系≠主系、拆分总量守恒、`createFoeSpecs` 两键且主系占大部分且形态仍按主系、混伤绕过单系抗、零份额键被忽略）；typecheck ×4 + core **782/782** + content:check + desktop build 绿【待验收/待发布】。

- 2026-09-10：**蜃影星系微微下移（船长：「蜃影星系微微下移。」）**——承接上一条：烬火星区右移到 x=600 后，「碎晶带→烬火星区」这条航线**正好从蜃影星系的圆点里穿过**（圆心垂距仅 **1.6**，而圆点半径 8）。`galaxy-mirage` **y 115 → 127（下移 12）**：垂距 **1.6 → 13.0**（圆点 r=8 + 约 5 的净间隙，视觉上让开星点）。**逐档试算**（临时探针 `tools/_probe-mirage2.ts`，已删；量的是"航线到圆心的最短垂距"）：下移 4/6/8 → 仍穿过圆点（5.4/7.3/9.2）；下移 **10 是最低限度**（11.1，勉强让开）；下移 12 → 13.0 干净让开。**副作用核查**：①全图"航线贴线（<14）"清单**前后一致**（修掉这一条后只剩两处既有情况：烬火星区→红环航道 距暗星坟场 11.5、柯尔边境→星髓迷宫 距灯塔长廊 3.1，均非本次引入）；②最近邻「暗星坟场」77 → **73**（仍很宽松）；③派系活跃标记落位（复刻界面 factionDy 判定）**前后完全相同**（需上抬/兜底清单逐项一致）→ 没有因为下移引出新的标记冲突。**验证**：typecheck ×4 + core **774/774** + content:check + desktop build 全绿；产物核对 `id: "galaxy-mirage" → x: 417, y: 127`【待验收/待发布】。

- 2026-09-10：**星图「敌对派系活跃」标记对齐 + 闪缩 + 烬火星区右移（船长：「关于星图的敌对派系活跃，希望能够对齐星系按钮，并且会闪缩」；追问后追加「把烬火星区往右挪一些距离」）**——①**对齐**（船长选"始终居中"）：✦ 与「敌对派系活跃」全称标签**横向永远居中在星系圆点正上方**——原实现沿用徽标栈的 `stackDx` 会为躲邻居节点左右平移 15/26/36，整组偏到星系旁边、看着不像"选中这个星系"；现改为冲突时**整组上抬**（候选 0 / −12 / −24，上抬不破坏对齐感；抬出画布顶部留白就不采用，兜底回到居中）→ 新增 `bandClear` 公共判定（`stackClear` 改为调它；⚑ 赏金任务徽标的横向避让**不动**）+ `FACTION_HALF_W/FACTION_MARK_TOP/FACTION_DY_TRIES/factionDy`。②**闪缩**（船长选"缩放脉动 + 呼吸"）：`.app-map-faction-box`（红方框）与 `.app-map-dot.is-faction`（红圆点）的脉动由"纯透明度呼吸 2.4s"改为 **`app-map-faction-pulse`：1.6s、1 ↔ 0.85 缩放 + 亮度 1 ↔ 0.55、两者同相位一缩一放**；SVG 元素必须显式 `transform-box: fill-box; transform-origin: center` 才绕**自身中心**缩放（否则绕用户坐标原点飞出去）；只动 transform/opacity（约定第十四章，不碰 filter/box-shadow）；旧 `app-map-faction-breathe` 关键帧删除。③**数据**：`galaxy-cinder`（烬火星区）x **560 → 600**（右移 40，`packages/data/src/universe.ts` 就地注明原因）。**几何自检**（临时探针 `tools/_probe-mapfaction.ts`，已删；复刻界面同一套包围带判定）：**右移前**该星系在居中位置的标记带正被「星噬之口」（546,137）的名称带压住 → **这正是玩家侧看到"标被挪到一边"的原因**；**右移后居中即无冲突 ✓**（与「虚海边缘」仍留 60 单位间距）。全量扫描"任一星系当天当派系目标"：仅「星髓迷宫」需上抬 24，「暗星坟场 / 深渊之门 / 碎晶带」三种抬法都会压住邻居 → 按船长"对齐优先"的取舍**兜底保持居中**（属布局本身拥挤的既有情况）。航线贴线量化：碎晶带→烬火星区 距「蜃影星系」3.9 → **1.6**（该航线**本来就穿过那个星系的圆点**，圆点不透明且画在航线上层，视觉上只是"航线从星点后方穿过"；同类既有情况如柯尔边境→星髓迷宫 距「灯塔长廊」3.1）。**验证**：typecheck ×4 + core **731/731** + content:check + desktop build 全绿；产物核对（CSS 含 `app-map-faction-pulse` ×3、`transform-box: fill-box`、旧关键帧为 0；JS 含 `id:"galaxy-cinder" … x: 600`）【待验收/待发布】。

- 2026-09-10：**精炼炉产出/组装机产物行尾显示「自己拥有多少个成品」（船长：「精炼炉和组装机，在每种产出的产品后面显示自己拥有多少个成品（每个产品单独占一行）」）**——船长三项口径均取推荐：**只做精炼炉产出 + 组装机产物（回收炉不动：它的产出按矿物池掷、没有固定清单）**、**计数走站内仓库/装备库/机库（与市场页「持有」同源）**、**为 0 也照样显示**。①**精炼炉**（`pages/IndustryPage.tsx`）：原来把该资源的多条产出挤成一行（`♨ 每批产出：三钛合金×120、类晶体胶矿×60`，用「、」连接），现改为**标题「♨ 产出：」独占一行 + 每种产物缩进一行**，逐行括注 `（仓库 N）`（`countWare`，与同卡材料行 `（仓库 N）` 及市场页「持有 N」同口径）——**同日按船长追加口径「改成类似上文那样」从"每行各带 ♨"再收敛成"标题 + 缩进行"**，为此新增最小类 `.app-belt-out { padding-left: 14px }`（挂在 `app-belt-*` 家族内，其余样式零新增）；14 种可精炼资源 = 9 种产 2 个矿物 + 5 种产 3 个 → 卡片增高 2~3 行（精炼/回收网格在功能窗内滚动，`.app-win-body { overflow-y: auto }`，不触碰「一级页不滚」）。②**组装机**（`panels/Industry.tsx` BlueprintCard）：产物行本就是一行一个产物，行尾补 `（装备库 N）`（装备走 `countModule`）/`（仓库 N）`（弹药·物品走 `countWare`）/`（机库 N）`（舰船按机库同型艘数数，与市场页同法——core 自然库存对舰船恒 0）；**新增 props `ownedCount` / `ownedWhere`**，三类产物在 ManufacturingPanel 构建条目时各自算好；悬停说明写明"自己已有的成品数量：<来处>；已挂单托管的量不计在内（与市场页「持有」同源）"。③**样式**：括注沿用既有 `app-dim`（复刻同级相似项），仅产出缩进新增上述一个最小类。验证：typecheck ×4 + core 774/774 + content:check + desktop build 绿 + 产物核对（`♨ 产出：` / `app-belt-out` / `（仓库 ` / 自己已有的成品数量 / ownedWhere 均在包内）；临时探针 `_refine-rows.ts` 用完即删【待验收/待发布】。

- 2026-09-10：**G 族第一件改造：机库模块「鱿蜂群巢」→ 专属无人机「鱿蜂无人机」（船长：「G 的鱿蜂群巢改为侦查无人机：鱿蜂无人机，我们进行相应的设计」）**——设计稿 `docs/design/g-exile-bee-drone-20260910.md`（状态：已确认）。这一改动的实质是**物品（无人机）钻进"专属装备池（模块池）"**，牵动四层：**①数据**：新增物品 `drone-exile-bee`（`kind: 'drone'` / `droneClass: 'scout'` / **`exclusive: true`** 新字段）：单发 **6**（= 制式侦察机 2×、与战斗机赤鸢持平）、闪避 **0.55**（全机型最高）、三层血 15（最薄）、射程 2500、命中 0.75、体积 5 m³、放飞 CPU **5**；撤下 `mod-lair-drone-rack-g`（老档走悬空件清理）。**②掉落链路**：`FOE_LAIR_GEAR` 池从"只装模块 id"扩成**可混装模块与物品**（`ctx.modules` / `ctx.items` 判别）——模块进装备库、无人机进**物品仓库**，一次 `RARE_BOX_DRONE_UNITS` = **10 架**；持有判定对无人机 = **物品仓库 + 各船机舱架数**（新增 `equipment.ownedItemCount`）→ 手上还有就不重复掉、**打光之后重新进池**（等于"再刷一次就能补回来"）；回收明细账新增"无人机 N 架"（`recAcc.drone`，含存档读写）。**③契约扩展**：四型定位契约显式化**锚点机型** `DRONE_ROLE_ANCHORS`（蜂鸟/赤鸢/猎鹰/雷鸥）——单发基准与跨类阶梯**只比锚点**，且**专属强化型豁免区间校验**（仍守硬边界），故"专属侦察机单发 6 > 制式战斗机同级"由船长裁决合法，四型定位本身不变；`content:check` 的**来源唯一契约**扩到专属**物品**（无蓝图 / 无市场卡 / 无碎片配方 / 不进常规残骸主题池，且必须标 `exclusive`）、死物品护栏对专属型号改判"不得有市场卡"。**船长三处裁决**：①补给 = **一次 ×10 架、仍不可造**；②数值 = A 档基础上**单发提到 6**；③冲突落法 = **CPU 提到 5 + 契约登记豁免**。**已知代价（写进设计稿供日后校准）**：它 = 赤鸢的火力 + 蜂鸟更飘的闪避 + 更省 CPU，但三层血只有 15（最薄）且不可造；实战单发 6 进战斗再 ×2 → 12 点/发（乘导控与船体加成后约 19.5/发）。回归：`tests/lair-gear-g.test.ts` 全量重写 8 项（池构成与撤件、机型数值、豁免与硬边界负向验证、锚点阶梯、掉落 ×10 与文案、持有判定与打光回池、未持有模块优先、进战斗整队 10 架放飞与射程、机舱容量照常裁切）+ `bounty-tasks.test.ts` 族池断言同步；typecheck ×4 + core **770/770** + content:check（来源唯一 15 件 = 装备 14 + 专属物品 1、无人机 5 种）+ desktop build 绿【待验收/待发布】。

- 2026-09-10：**碎片池「集齐前不重复」+ 彩头 EV 口径收口（船长拍板：「按集齐前不重复」「碎片进度不要过度详细的说明，就进行一个大概的剩余残骸预估」）**——**触发**：船长追问「假如按照甲，那么多个蓝图怎么算是随机抽吗、期望是多少」，机制与期望算清后再由船长定这两件。**① 引擎：碎片池改按玩家状态过滤**（`salvage.fragmentPoolOf(state, ctx, tier)`）——旧实现只按"蓝图是否已注册"建池，于是有两个"白吃碎片"的口子：**已学会的蓝图照样在池里**（从市场买书学会后仍一直抽到它的碎片，纯浪费）、**碎片已集齐门槛但没去母港逆向的也照样吃**；现两者一律**移出池子**，三张书全部到手后该档池为空（不再出碎片、不报错、不降级抽别的）。**效果**：凑齐该档三张书恰好 3×门槛片（MK2 **75 片** / MK3 **750 片**），**尾部消失**——旧"纯随机"口径有集邮损耗（精确 DP + 20 万次蒙特卡洛双向校验：MK2 期望 87.9 片 = 硬下限 75 片 **+17.2%**、中位 86 / P90 99；MK3 期望 792.9 片 = 硬下限 750 片 **+5.7%**、中位 789 / P90 824），改后报表里的"整档 166,667 m³ / 1,071,429 m³"就是真实期望（此前那是理想值）；碎片物品描述补一句「集齐前不会重复掉落同一本书的碎片——拿到蓝图后它就不再出现」。**② 报表口径收口（原甲案）**：`tools/salvage-econ.ts` 的「彩头 EV/h」**只算可兑现件**（基础件 3,894 + 低安 MK2 1,086 ≈ **4,979 ISK/h**，占"保底 10% = 8,250"护栏的 **3.4%**，回到护栏内）；碎片不再按"蓝图市价 ÷ 片数"折算（那是收藏折算不是产出、门槛下调后虚高 4 倍并越护栏），改为**一行大概预估**：`碎片进度（不计 ISK；集齐前不重复 → 三张书合计 75/750 片）：凑齐 MK2 三张 ≈166,667 m³ 残骸（炉时 116h/93h）· MK3 ≈1,071,429 m³（744h/595h）`。**回归**：新增 `recycle.test.ts`「碎片池」4 项（空档全在池 / 集齐即移出 / 已学会即移出 / 池空不报错）；**负向验证**：暂时去掉"已学会"这一条 → 2 项如实失败（`expected [...] to not include 'mod-cargo-2'`、`expected [...] to deeply equal []`），还原后全绿；临时探针 `tools/_frag-expect.ts` 已删（公式结论留在设计稿 §四留档）；typecheck ×4 + content:check + 两个经济工具复跑；设计稿 `docs/design/blueprint-rate-20260910.md` 新增 §四、词典「蓝图碎片阈值」按新门槛更新并新增「碎片集齐前不重复」条目【待验收/待发布】。

- 2026-09-10：**敌族特色装备·G 族「烬火流亡」三件收口（船长：设定照既有料确立 + 两件「全走无人机线」）**——**G 族 = 无人机全套专族**，五族专属装备全部落地（A/C/D/E/G 各三件；B/F 因无窝点留空）。①**鱿蜂群导控**（无人机导控·高槽）：放飞无人机单发 **+45% / CPU 32**——比战术导控阵列 MK3（+40% / CPU 45）效果高 5 个点、**CPU 省 13 点**（每点 CPU 出 1.41 个点，MK3 是 0.89）；②**流亡中继桅**（无人机中继天线·高槽）：放飞无人机射程 **+65% / CPU 34**——比中继天线 MK3（+80% / CPU 46）近一档、**省 12 点**（雷鸥 5000 → **8250 m**、蜂鸟 2500 → 4125 m）；③既有「鱿蜂群巢」（+55 m³ / CPU 30）不动。**三件同装只花 96 点 CPU**（梭鱼级 320 里的一小部分），一套补齐无人机流的"带得够多、打得够重、够得着"；数值口径 = **上限低于 D/E**（D 最强、E 略高于 MK3），立身之本是**每点 CPU 更划算的拼装件**。**G 族设定已按既有料固化进词典与设计稿**（被协会长期封锁、打散后残存的流亡舰队：烬火围攻 → 回音残舰"复活"＝残余自动化产线 → 天底"最后的据点"；蜂窝舱段 + 补丁帆；聚落紫；窝点三档 = 残兵头目/聚落营地/藏匿地）。content-check 新增 `droneRangeBonusPct` 值域护栏 (0, 2]。回归 +6（`tests/lair-gear-g.test.ts`：G 族池三件、三件槽位与数值、比 MK3 更划算的性价比断言、导控进战斗的单发加成（含船体 +8% 乘算）、中继进战斗的射程 ×1.65 且机型本体不动、三件同装 CPU 96 且机库/伤害/射程同时生效）；typecheck ×4 + core **762/762** + content:check + desktop build 绿【待验收/待发布】。

- 2026-09-10：**敌族特色装备·E 族「泰坦巨构」后两件补齐（船长逐件过审：档位「略高于 MK3」、仍低于 D 族）**——**E 族三件齐**（`FOE_LAIR_GEAR.E`）：①巨构残骸炮（上一批修订：十秒一发、名义 DPS = MK3 的 109.5%）；②**深层机库**（无人机甲板·高槽）：无人机舱 **+95 m³ / CPU 50**——比无人机甲板扩展 MK3 的 +70 大**三分之一**（1.36 倍），换算 **19 侦察机 / 9 战斗机 / 4 攻坚机**；实测梭鱼级机巢 160 → **255 m³**，能整编 250 m³ 机群一次性放飞（18 架条目全数进 `spec.weapons`，卸下机库即按清单逆序回落）；**说明：真正卡放飞数量的仍是 CPU 带宽（每架 4/7/11/16 点），机库只保证带得够多**。③**巨构骨架**（装甲·低槽）：**结构层容量 +60% ＋ 装甲容量 +30% / CPU 50**——引擎新增 **`ModuleDef.hullHpBonus`**，接上此前全游戏**唯一没有容量模块**的一层（结构层过去只有 C 族的"结构抗性"与船体加固理论技能能碰）；多件加算求和（与甲容同口径）、技能（船体加固理论／重装舰操作 +4%/级）再乘于其上。**三层总血对照**（灰鲭鲨 裸 366）：装甲增厚板 MK3 **435.6** ＜ 巨构骨架 **455.1** ＜ 陵寝装甲层 **461.7** → 落实「略高于 MK3、仍低于 D」。UI：模块详情通用新增「结构容量」行（任何槽位都可能带，与「结构抗性」分成两行——容量 = 血更厚、抗性 = 血更耐打），装甲槽短行加「结构 +60%」；content-check 新增 `hullHpBonus` 值域护栏 (0, 2]。**同批另修一处显示缺口**（独立提交 `d5d3c58`）：`hullResistAdd`（结构抗性）此前在界面上**完全没有呈现**、盾/甲/结构三层抗性行只渲染**第一个非零系**（"三系各 +X%" 的件会被误读成只抗一种）、维修系短行原本**空白**（只剩名字 + CPU）——现统一为逐系 chip 行 + 维修尾缀。回归 +5（`tests/lair-gear-e.test.ts` 共 10 项：E 族池三件、机库 95 m³ 与 1.36 倍换算、梭鱼 255 m³ 整编放飞与卸下回落、骨架甲容 ×1.3／结构 ×1.6、三层总血 MK3 ＜ E3 ＜ D3 的锚点断言、两件加算 ×2.2、与技能乘算 ×1.2）；typecheck ×4 + core **756/756** + content:check + desktop build 绿【待验收/待发布】。

- 2026-09-10：**赏金任务·档位改由「地图级别」封顶（船长拍板：内容侧显式标级 + 硬封顶 + 取消保底 + 单图也受约束；追加确认「E 两张都设为 3」「每个种族至少一张为 3」）**——**触发**：船长「赏金任务出现档位进行调整，假设同族存在多张地图越低级的，出现高档位赏金的概率越低」。**原状**：档位由 `rollLairTiers` **纯随机发放 + 每天三档各至少一张保底 + 抽完再洗牌**（专门让"哪个地点配哪档"没有系统性关联）→ 柯尔边境（A 族最弱图，中安 sec 0.5、威胁 12、奖金 1.2 万）照样会抽到"隐蔽船坞"（深层 ×8），蜃影星系也可能只出"小头目"。**新规则**：①新增 `AnomalyDef.lairLevel`（**1 = 只出外围、2 = 到核心、3 = 全档**）= 该地图的**档位上限**；②发档改为在该卡 `[1..级别]` 内**均匀随机**（硬封顶、逐卡独立掷），三档保底与洗牌一并退役；③级别**只按族内相对强度**（奖金为主、威胁为辅）、**不与区划绑定**（A 族 6 张全在中安，按区划封顶则海盗永远出不了深层）；④**19 张候选卡逐张标级**（L1×5 / L2×6 / L3×8）：A 1/1/2/2/3/3、C 1/2/3/3、D 1/2/2/3、**E 3/3（船长定）**、G 1/2/3，**每族至少一张 3 级**（船长定）；⑤**同星系多张卡的代表卡改取「级别最高」**（并列取奖金最高）——否则"谁进池"由数据顺序决定、档位上限跟着随机漂移（副作用：红环航道的 D 族幽灵舰信号(1 级)、深渊之门的 C 族深渊之门卫队(1 级) 被同星系更高级卡顶掉、**实际永不入板**，船长已知情）；⑥界面：**级别 < 3 的星系**在赏金卡「目标行」显示 `本区上限 外围/核心`（`app-lair-kv` 同款键值样式，未新增样式），避免玩家把"低安图只出外围"当成 bug。**实测分布**（`tools/bounty-econ.ts` 尾部新增「日板档位分布」小节，**真实引擎赞助 120 天**、全图已探索含派系占位）：每天档位期望 **外围 2.36 / 核心 1.93 / 深层 0.72 张**（共 5 席），当天至少一张深层 **54.2%**（旧规则 100%）、三档齐全 41.7%——船长明确接受"深层从每天必有变约两天一次"。**护栏**：`content-check` 新增「窝点级别契约」（候选卡必须显式标级——缺省 3 只是"漏标不误伤"；每族至少一张 3 级；同族内"级别更高而奖金更低"出提示）。**回归**：`bounty-tasks.test.ts` 35 项（改 3 项旧保底用例 + 新增 6 项：级别语义与缺省值、1 级图恒出外围、3 级图三档齐现、2 级图永不出深层、取消保底（世界内无 3 级图 → 30 天零深层）、同星系代表卡取级别最高；**负向验证**：把"取级别最高"改成"取级别最低"→ 该用例如实失败，还原后全绿）；临时探针 `_lair-inventory.ts`/`_lair-tier-sim.ts` 已删除（试算并入 `bounty:econ`）。**同批收尾**：按船长指示修订 2026-09-08 B3.1 公告里过期的「集 100/1000 片」「集 1000 片」→「集 25/250 片」「集 250 片」；另为本批蓝图系数拟第 10 张待审公告卡（`docs/design/announcement-draft-20260910-pending.md`，**未写入 announcements.ts**）；蓝图设计稿补记「常驻（common）蓝图书渠道 **不管**」为船长决定（不再是待办）。typecheck ×4 + core **757/757** + content:check 通过 + desktop build 绿（产物核对「本区上限」「该星系的窝点深度上限」在包内）；设计稿 `docs/design/bounty-tasks-20260910.md` §3.1 已补【待验收/待发布】。

- 2026-09-10：**蓝图获取速率调整（船长拍板：残骸只降门槛、市场蓝图书权重 50%→5% 且「5% 也影响奇货」、蓝图单寿命延长到 6 小时）**——**触发**：船长先复核「D2 那批敌族专属装备是否既无蓝图又不上市场」（答复：已核 0 蓝图 / 0 市场卡 / 0 碎片配方，并顺手把该不变量做成 `content:check`「来源唯一契约」；随后要求调出率。①**残骸侧（只降门槛、概率不动）**：`FRAGMENT_RECIPES.need` MK2 **100 → 25**、MK3 **1000 → 250**，`RECYCLE_CHANCE.fragT2/fragT3`（0.00045 / 0.0007 每 m³）不变 → 整档 MK2 666,667 → **166,667 m³（炉时 463h/370h → 116h/93h）**、MK3 4,285,714 → **1,071,429 m³（2,976h/2,381h → 744h/595h）**，相对「买齐三张书」的差距由 **≈28 倍降到 ≈7 倍**（`tools/salvage-econ.ts` 对比表实测）。②**必须连带的重构**：引擎原先用「片数 == 100 / == 1000」反推档位（`salvage.ts` 两处池拆分、`wreckFlavor.tsx` 星图残骸卡威胁门槛、`tools/salvage-econ.ts` 两处）——**只改 need 会把两个池一起打成空数组（碎片一片不出）**；故 `FRAGMENT_RECIPES` 新增显式 `tier: 2 | 3` 字段、新增 `fragmentPoolOf(ctx, tier)`，全部判据改读 `tier`。③**市场侧**：新增 `balance.market.blueprintWeight = 0.05`（原硬编码 ×0.5，**稀有抽取两处 + 奇货掷骰一处**三处硬编码改读配置）→ 蓝图占稀有供给 **25.2% → 3.2%**、大众档蓝图单行间隔 **1.5h → 11.7h**、高阶档 **6.0h → 40.9h**（六张碎片路线对应书：MK2 10.1/12.3/44.5h、MK3 36.8/41.2/36.9h）；**稀有渠道每窗张数不变（14.70 → 14.62）**，蓝图让出的份额全部回到现货装备；奇货渠道同样 ×0.05 → 舰船蓝图单行 ≈417h（17.4 天）、17 行合计 ≈0.98 张/天（造船线由"随时能买"变"要蹲要攒"，船长已知情选定）。④**蓝图单寿命 6 小时**：新增 `balance.market.blueprintLifeMs`，`orderLifeMsOf` 对"稀有渠道蓝图书"返回 6h（普通稀有单仍 36 分钟；奇货本就是 6h）；有货时间占比大众档 ≈51% / 高阶档 ≈13%。⑤**未动项**：书价、声望门槛、暗市闸（×0.04）、收购侧（玩家回卖）一概不变；**常驻（common）蓝图书渠道 0.425/窗 未动**（民用级/MK1 蓝图仍随手可得，如需一并收口另开）；高级箱（稀有残骸）继续封存。⑥**工具**：`market-rarity-sim.ts` 新增 `--bp-weight` 与按行间隔输出（临时探针 `_bp-rate.ts` 已并入并删除）。**已知偏差**：`announcements.ts` 有两条旧公告写「集 100/1000 片」（过期，**修订需船长批准**，未批前保持原文）；`salvage-econ` 的「彩头 EV」行把碎片按蓝图市价折算，门槛下调后折算值涨 4 倍并越过「≤ 保底 10%」旧护栏（碎片不可售，属口径失真，未改）。**验证**：typecheck 全绿 + core **751/751**（新增/改写 4 条：稀有渠道 5% 占比、奇货渠道 5% 占比、蓝图单寿命 6h、碎片 tier 字段与 25/250 门槛）+ content:check 通过（装备 95 件 / 市场 281 卡 / 来源唯一契约 11 件）+ desktop build 绿（产物核对「蓝图书 6 小时」与碎片提示模板在包内）+ 两个经济工具复跑 + 设计稿 `docs/design/blueprint-rate-20260910.md`（状态：已确认）【待验收/待发布】。

- 2026-09-10：**敌族特色装备·E 族「泰坦巨构」第一件修订：巨构残骸炮（船长逐条指定：先只修订 E1、档位"略高于 MK3" → "装填时间延长到10秒，保证现有的DPS。远端衰减设为0.1。CPU占用下调到22"）**——原状是早期占位件（`1.9 / 5200ms`，名义 DPS 只有攻坚炮台 MK3 的 **21%**、单发只有 37%），按现在这套体系装了等于白占一个高槽。**修订口径**：装填 **10000ms**（十秒一发）＋ 保住草案的名义 DPS（**= MK3 的 109.5%**，1.91/秒）→ 单发 **19.1**（= MK3 单发的 3.72 倍、装填 3.40 倍）；**远端衰减 0.9 → 0.1**（越远越打不中，射程带尽头几近失的）；基础命中 0.62 → **0.70**（仍低于 MK3 的 0.78）；射程带 900~**9600**（比 MK3 的 7350 远 31%、仍短于 D 的 12000）；CPU 44 → **22**（不到 MK3 的一半）；耗弹 12/场不变（十秒一发 ≈ 打满两分钟才耗完，弹药不再是限制）。**与 D 族的分工**：D 守墓者长炮 = 12 km **必中点名炮**（单发 2.24 倍、总输出 MK3 的 90%）；E 巨构残骸炮 = 9.6 km **爆炸换血锤**（单发 3.72 倍、总输出 MK3 的 109.5%，命中与射程都不如 D）——**D 仍是最强敌族**。E2/E3 两件本轮未动，等船长逐件过审。回归 +5（`packages/core/tests/lair-gear-e.test.ts`：E 族池单件、装填/单发/总输出比值（含 +9.5% 上界）、衰减 0.1 与 CPU 22 及与 D 件的分工对比、进战斗条目落地且仍是爆炸单弹种、装填技术学 −4%/级 缩短十秒长装填）；typecheck ×4 + core **749/749** + content:check + desktop build 绿【待验收/待发布】。

- 2026-09-10：**敌族特色装备·D 族「守墓古舰」三件（船长逐件过审：「D 族是目前最强的敌人种族，装备可以对标异星档、可略强」）**——①**陵墓护盾阵列**（护盾·中槽）：护盾层**三系减伤各 +35%**（缺口复合、0.9 封顶），CPU **42**（比护盾扩展器 MK3 的 40 更重）——单系不如专精增强器，胜在不用猜对手弹种；②**守墓者长炮**（炮台·高槽·动能）：**基础命中 100%** 的 12 km 超远程点名炮——`dmgMult 11.5 / 装填 7350ms`，即**射速降到攻坚炮台 MK3 的四成（−60%）、单发 2.24 倍、总输出恰为 MK3 的九成（−10.3%，兑现船长"保证 DPS −10%"）**，射程带 1300~12000、远端命中衰减 0.45（比 MK3 的 0.28 更轻），CPU 62、耗弹 12/场；③**陵寝装甲层**（装甲·低槽）：**装甲容量 +110%**（比装甲增厚板 MK3 的 +80% 还厚四成），代价 = **战斗速度 −25%**（新机制「机动代价」）＋ CPU 52。**引擎新增 `ModuleDef.speedPenaltyPct`（机动代价/速度代价）**：装甲**容量件**可带常驻速度代价（值域 (0, 0.9]，content:check 护栏只许挂在容量件上），进战斗 `speedMps × (1 − 该值)`、**多件不叠加只取最重一件**（与推进器失稳 `hitPenalty` 同口径：重甲不会叠成静止）；**只影响接敌/拉开距离的距离机动、不改命中**，不涉跃迁与工厂循环。UI：模块详情装甲类新增「机动代价」行（红标 chip + 影响面说明），沿用既有 `app-chip.is-cost` 样式、未新增样式。**D 族池 = 三件**（`core/lairs.ts` `FOE_LAIR_GEAR.D`；掉落规则仍走"同族集齐前不重复"）；A/C/D 三族均已逐件过审落地，E（泰坦巨构）、G（烬火流亡）待逐件过审。**顺带清理**：词典里「敌族专属装备」原有两条重复词条（三号早批"每族一件"旧口径 + 本批"每族 3 件"新口径）已合并为一条，旧条删除。回归 +7（`packages/core/tests/lair-gear-d.test.ts`：三系抗缺口复合与 CPU、DPS 比值 ≈0.897 + 射速/单发比值、命中 100% 与射程带落地、甲容 ×2.1、速度 ×0.75 且多件取最重、三件同装 CPU 156 < 195）；typecheck ×4 + core **742/742** + content:check + desktop build 绿【待验收/待发布】。

- 2026-09-10：**组装机「循环制造」上移到卡片级（船长：先提"屏幕过窄时隐藏进度条腾位置"→ 实测回报后改口"进度条不做更改，开关和数量单独领出来，改为针对当前生产卡片，对所有在该卡片工作的 AI 都生效，数量为所有 AI 合计"）**——**前期排查（进度条方案为何被否）**：组装机卡的行宽由网格 `minmax(240px, 1fr)` 决定，**基本恒定在 240~300px**（窗口越宽列越多、卡片反而更窄；窗口极窄变单列时才变宽），所以"挤"与屏幕宽窄不对应、隐藏进度条治标不治本；船长采纳后改为本方案，**进度条一行未改**。**新口径（船长逐条批复，全部取推荐项）**：①**开关**与**目标件数**从逐条制造线**上移到整张生产卡**——一张卡（= 一个蓝图）一个开关、一个目标、一个「本轮全卡合计」；②作用范围 = 该卡**全部制造线（含主控亲自那条）**，打开后新开的线自动继承（引擎**实时读**卡片配置、不往线上写副本）；③目标件数 = **全卡合计**，达标即不再续做，此刻在跑的件**跑完再停**（最多超产 = 同时在跑线数 − 1：目标 20、3 线并行 → 停 21~22 件）；④达成目标/材料不足 → **自动关开关 + 卡片上标停因**（日志一条卡片级汇总「循环制造停止：X（本卡合计 N 件）——已达成目标 …；本卡其余 M 条线跑完当前件即停」）；⑤「关→开」= 开一批新循环（合计与停因清零），开关开着时改目标**不动**已累计合计；手动关 = 完成当前件即止（不写停因）；⑥**老档归并**：逐线 `autoRepeat/repeatGoal/produced` → 卡片（on = 任一为真、produced = 各线之和、只要有一条「无目标」则卡片也无目标），**无版本号变化**、归并后逐线字段不再写回。**涉及**：`state.ts`（新增 `manufacturingLoops` 于 `GameStateV24` + `ManufacturingLoopState`；逐线三位标为兼容只读）、`manufacturing.ts`（`setManufacturingLoop(blueprintId,…)` 改签名 + `manufacturingLoopOf` 视图 + `advanceManufacturing` 卡片级判定/计数/停线结账，同 tick 同卡只结一次账；`ManufacturingView` 的 `autoRepeat/repeatGoal/produced` → `loopOn/loopGoal/loopProduced`）、`save.ts`（新字段读写 + 老档归并，新字段优先防重复计数）、`game/engine.ts`（`setManufacturingLoopAt(blueprintId, on, goal)`）、`panels/Industry.tsx`（卡片级控制条 `.app-belt-loop`，行内三件移除）、`pages/ShipPage.tsx`（AI 指挥中心组装机行改「循环制造 · 本卡合计 12/20 件」）、`styles.css`（新增 `.app-belt-loop/.app-mf-made/.app-mf-why/.app-mf-note`；`.app-mf-loop` 逐线容器退役）；设计稿 `docs/design/assembly-loop-card-level-20260910.md`（状态：已确认并实现）+ 词典「循环制造（原连续生产）」改口径。**验证**：typecheck ×4 + core **727/727**（旧 5 项逐线用例改写为 5 项卡片级用例 + save 归并 2 项）+ content:check + desktop build 全绿；产物核对（控制条 4 类名 + 6 处文案 + `manufacturingLoops/loopProduced/循环制造停止` 均在，`app-mf-loop` 无代码引用）；**负向验证**：关掉「自动关开关+记停因」→ 5 项如实失败、关掉「全卡合计累加」→ 4 项如实失败，还原后全绿。**已知取舍**：允许超产（不砍已扣料的在跑件）且超产件不计入合计（合计 = 停线依据 = 目标数，不会出现"已产 22 / 目标 20"的矛盾数）；开关可先开、后开线（预制循环）；数值零改动（材料/耗时/折扣/AI 效率/核心占用一概不变）【待验收/待发布】。

- 2026-09-10：**收尾与交接（三号 → 一号；船长："那么你进行下备注并收尾，我转交给一号"）**——**注：本条写下后船长又追加了一项改动（组装机循环制造上移到卡片级），见上一条；本条的"唯一代码改动"指当时。**①**本轮唯一代码改动 = 星图画「敌对派系活跃」**（承接下一条④的更正）：当日派系星系在星图上挂**琥珀 `✦` 徽标** + 第二行「剩余 h:mm:ss」，点开星系的行动窗新增一行说明——「敌对派系活跃：该星系全部常驻悬赏奖金 +10%、敌人威胁 +10%，胜利有概率掉稀有残骸 · 头号目标「卡名」· 剩余 h:mm:ss（每天 0 点重选）」。判定走 **core 单点 `factionGalaxyId(state)`**（与战斗、任务中心置顶卡同源，不自算）；倒计时取 `engine.sideTasksView().bountyRemainingMs`（与任务中心「距下批刷新」**同一界**）；**互斥已核**：`spawnBountyTasks` 把派系星系从常规席位抽签池里剔除（`sideTasks.ts` 397~401 行）→ `✦` 不会与 `⚑N` 叠在同一节点，故两者共用同一行位。改动 **2 个文件**：`panels/Expedition.tsx`（徽标 + 行动窗行 + 补引 `factionGalaxyId`）、`styles.css`（新增 `.app-map-bounty.is-faction` / `.app-map-bounty-eta.is-faction` / `.app-map-taskline.is-faction`，琥珀取任务中心「今日置顶」同族 `#f4c95d`，弹窗行正文 `#e3cb96`）；玩家可见文案只写"有概率"，不含掉落数值。**验证**：typecheck ×4 绿、core **725/725**、content:check 绿、desktop build 绿；产物核对（`index-*.js`/`index-*.css`）4 类名 + 2 色值 + 「敌对派系活跃」「每天 0 点重选」文案均在。②**交接现状（给一号）**：三号工作区 `Deepseek-EVE-verify`（分支 verify）已把本批全部改动 **ff 合入 main**，主树无未提交改动；**81 个本地提交未推送**（含本次收尾的 2 个提交；一号后续提交会继续累加）（推送闸门：待船长验收 + 公告批准后，代码与公告**一起**推一次）。③**待船长批的公告候选 = 9 张**，全在 `docs/design/announcement-draft-20260910-pending.md`（1 无人机专用舰让出高槽 / 2 调度学补最高难度档 / 3 手册排布 / 4 名单前小图标与 AI 工作动画 / 5 市场类型按槽位分档 + 行内看自己库存 / 6 界面宇宙底（含设置里换一张）/ 7 活动窗 AI 徽标拆两枚 / 8 驾驶状态窗采掘打捞对齐 AI 动画 / 9 星图赏金任务 + 派系活跃）；批复后按约定写入 `packages/data/src/announcements.ts` 顶部。④**仍待船长核定的数值（本轮未动）**：`FACTION_RARE_DROP_CHANCE`（一号已改为 10% ×1 件，`content:check` 输出仍标"待船长核定"）、点防 PD 六参数（二号设计稿 §八）。⑤**与一号在途改动重叠风险高的文件**（建议错峰）：`panels/Expedition.tsx`、`styles.css`、`panels/ActivityBar.tsx`、`packages/ui/src/index.css`、`pages/MarketPage.tsx`。⑥**可调旋钮一览（值都写在代码注释里，改一行即生效）**：底图蒙层 0.55 / `.wui-panel` 与 `.app-header` alpha 0.88 / 状态窗工作色 `WORK_ACCENT`（采掘 `#b5e35f`、打捞 `#6fe3f0`）与浮动周期 2.4s·−2.5px / 星图徽标配色（悬赏情报 ⚔ `--wui-gold`、赏金任务 ⚑ `#8fa9d8`、派系活跃 ✦ `#f4c95d`）/ 舰队卡舰影列 `FLEET_ART_W=132 / GAP=10 / MAIN_MIN=560`（列表实宽 <702px 整列不渲染）/ 击落演出残留 `DRONE_DOWN_LIFE=760`。⑦**收尾清理**：临时预览目录 `H:\大鲸鱼\_bg-preview`、`H:\大鲸鱼\_aifx-preview`（当时为无视觉通道的替代核验产物）已删除，仓库内无临时探针、`git status` 干净。**交接信号：typecheck ×4 + core 725/725 + content:check + desktop build 全绿 = 随时可接手。**【待验收/待发布】

- 2026-09-10：**星图显示赏金任务 + 当日剩余时间（船长：「普通赏金任务希望能在星图上显示。和星图未探索时告诉玩家这个星系有悬赏的那个样式一样。敌对派系活跃则在对应星系上显示剩余时间」）**——原状：星图只有**未探索剪影**才挂 `⚔N` 悬赏情报徽标（`.app-map-bounty`，金色），已探索星系上不显示任何悬赏/任务标记。改法：①数据 = `engine.sideTasksView().bounty`（当日板，与任务中心同源）按 `task.galaxyId` 归组；**已核代码**：任务生成写死 `state.exploredGalaxies.includes(...)`（`sideTasks.spawnBountyTasks`）→ **任务只落在已探索星系**，徽标不会挂到"不渲染的节点"上 ✓；②星图节点挂 `⚑N` 徽标（沿用 `.app-map-bounty` 同款样式，换符号 + 任务族蓝灰 `#8fa9d8` 区分），下一行 `剩余 h:mm:ss`（`.app-map-bounty-eta`，取当日板 `bountyRemainingMs`，**与任务中心「距下批刷新」同一口径**）；③点开星系的行动窗新增一行：列出该星系的赏金任务（窝点名 + 酬金 + 剩余时间），新增 `.app-map-taskline` 样式；④**口径解读（如与船长本意不符待改）**：把"敌对派系活跃"读作"该星系的窝点当日任务在板上 = 活跃"，倒计时即当日板剩余（每天本地 0 点整板替换）；若指的是另一套派系/巢穴"活跃期"机制（一号在途那批），需换成那套字段。**验证**：typecheck ×4 + core 721/721 + content:check + desktop build 绿（产物确认三个新类名都在）。公告待审稿补第 9 卡【待验收/待发布】。
  - **【2026-09-10 追补 · 口径更正】**上述④的"口径解读"**作废**：实测对照后确认，船长说的"敌对派系活跃"**确实是另一套机制**（一号同日 `6987055`/`1d9d91d` 的派系活跃批）——它**不是**"该星系当日有窝点任务"，而是 `state.sideTasks.faction`：每天选**一个已探索的中安/低安星系**，该星系**常驻悬赏**奖金 +10% / 敌人威胁 +10%、胜利按概率掉稀有残骸、**打赢不下板**（当天可反复刷）、每天本地 0 点重选；`⚑` 徽标那一半（赏金任务 = 窝点）保持不变，本节其余结论（含"任务只落已探索星系"的核对）仍有效。星图侧的落法见下一条【收尾与交接】①。

- 2026-09-10：**状态窗配色统一 + 采集对象同步浮动（船长两条追加：「我的意思是只改左上角玩家舰船动画的颜色」+「既然在活动，最好采集对象也跟着船体上下浮动，或者船体保持不动」）**——①**浮动同步**：原来舰体有 `app-swin-float`（2.4s / −2.5px）但特效层静止 → 激光与舰首脱开；现给 `.app-swin-work` 挂**同周期同位移**的浮动，舰体与采集对象一起上下、瞄准不脱靶（比"舰体静止"自然；若要静止改一行即可）；②**整窗统一到工作色（船长选「方案A」）**：作业场景把**舰体 + 尾焰（currentColor）+ 背景氛围 + 漂移的矿岩/残片 + 激光**全部统一到该工作的颜色（采掘 `#b5e35f` / 打捞 `#6fe3f0`，与 `ui/aiWorkFx.tsx` 的工作动画同源）；`bg-work-mine` / `bg-work-salvage` 两处氛围光由暖褐/蓝灰改为绿/青；漂移物加 `.app-shipwin.is-work-*` 作用域覆盖；③**非作业场景舰体改用角色色**（`toneOf(role)`：工业绿/武装红/重装橙/航运蓝，与战场 `ROLE_ACCENT`、舰队卡族色**同值同源**）——此前状态窗舰体一直是默认白灰，与全站不一致；④**范围澄清（重要）**：船长本意**只改这块动画**，不是全站换色——全站色板未动（当时已量化代价：主题变量 13 个/引用 340 次，但 styles.css 另有 302 处硬编码十六进制（192 种）+ 444 处 rgba，只改变量会"只改一半"）；⑤**语义色未动**（金 = 数值/高级、青绿 = 进度/正常、红/琥珀 = 危险/警告）。**验证**：typecheck ×4 + core 721/721 + content:check + desktop build 绿；产物确认两个工作色、漂移物作用域规则与重染色后的氛围光都在。公告待审稿第 8 卡补两条【待验收/待发布】。

- 2026-09-10：**驾驶状态窗的采掘/打捞动画对齐 AI 工作动画（船长：「左上角驾驶舰船的采矿和打捞动画希望和 AI 指挥中心的 AI 动画近似（比如颜色，以及对什么东西射出激光和扫描）」）**——原状态窗的采掘/打捞场景只有**漂过的矿岩/残片**（`.app-swin-drift` 从右向左飘），**没有"激光打到某物"的动作**，也看不出在采什么。现按 AI 指挥中心工作动画（`ui/aiWorkFx.tsx`）的同一套语言补上：①**采掘**：舰首射出**采掘激光**打在右侧矿岩上（六边矿块 + 裂纹），3 枚矿石碎屑错相位**回流货舱**（飞出舰体后被舰体遮挡 = 吸入感）；②**打捞**：**牵引扇面 + 光束**吸住右侧残片（两块断裂板），上方一道**扫描弧**来回扫过，碎屑被吸回；③**配色与 AI 工作动画同源**：采掘绿 `#b5e35f`、打捞青 `#6fe3f0`（与 `.app-inv-fx.is-mining/.is-salvage` 一字不差）；④**几何**：特效画在已有的 `.app-shipwin-sky` 远景层（viewBox 148×66，在舰后），光束起点取本地坐标 x=104 ≈ 舰首（舰体占 112px、舰首约在 89.6% 处，故光束自舰体内侧射出、被舰体遮住断头）；新元素 x∈[104,145]、y∈[9,43] 全部落在画布内（已核）；⑤**性能**：新增动画只动 `transform`/`opacity`，无 filter/box-shadow（约定第十四章）；原有漂移物件保留（背景活物，与固定靶位并存）。**验证**：typecheck ×4 + core 719/719 + content:check + desktop build 绿；产物确认 5 个新类名与两个色值都在。公告待审稿补第 8 卡【待验收/待发布】。

- 2026-09-10：**活动窗口 AI 徽标拆成两枚（船长：「分成 2 个，一个代表驾驶副船的 AI 数量，一个代表工业的 AI 数量，要能够让玩家辨识」）**——原实现是单一 AI 徽标（`×N` = 副船 + 站内工业合计，点击去处还要按"哪边有活"二选一判断）。拆分口径：①**副船** = `aiCoreShipUsed`（AI 副船任务：采矿/打捞/掩护巡逻），图标沿用 AI 核心 `nav-ai`（**粉**），徽标文字「副船 ×N」，点击 → 「舰船」的 AI 指挥中心（原 `onAiCenter`）；②**工业** = `aiCoreIndustryUsed`（站内 AI 精炼炉/回收炉/制造线），图标用工业页 `nav-industry`（**薄荷**），徽标文字「工业 ×N」，点击 → 「工业」页（AI 炉/线的停止在那儿）。**辨识手段四重**：图标不同（粉 AI 核心 vs 薄荷工业）＋ 边框/底色跟随各自色调（`.is-ship`/`.is-industry` 两处修饰类）＋ 常显文字标签（副船/工业）＋ 悬停明细（各写在做什么，并复用 `aiSlots.tsx` 的 `aiSlotTip` 单点文案说明"AI 核心占用 / 上限怎么来的、副船与站内共用上限"）。计数为 0 的那一枚不显示（无事可看不占位）。**顺带修正口径来源**：两枚数字改取 **core 单点**（`aiCoreShipUsed`/`aiCoreIndustryUsed`，与 AI 指挥中心标题行同源），不再从活动列表 `stop === 'cancel-ai'` 反推（旧写法与新口径一致，但来源更直接）。typecheck ×4 + core 719/719 + content:check + desktop build 绿（产物确认两枚徽标与两个修饰类都在）【待验收/待发布】。

- 2026-09-10：**宇宙背景可见性校准（船长反馈"背景似乎有些太暗了？还是各个窗口不透明导致的？"）**——**诊断结论：主因是面板不透明、蒙层是次要原因**：`.wui-panel` 原为 `linear-gradient(180deg, #0a111b, #080e16)`（**alpha=1.0**），而页面上 90%+ 面积都是面板（其余页面容器 `.app-page-content`/`.page-stack` 实测透明、左侧导航透明），底图只剩面板间 10px 缝隙与侧栏可见；且批图本身约九成纯黑（只有星云核与星点亮），故**单纯调薄蒙层收益有限**。**修法（船长选定「方案A」+ 排除战场）**：①`.wui-panel` 面板底改为 `rgba(10,17,27,.88)` / `rgba(8,14,22,.88)`（packages/ui/src/index.css，全站面板一处生效）；②`.app-root` 蒙层 72% → **55%**；③**战斗画面零影响**（战场不使用 `.wui-panel`，自带不透明径向底 + 星野视差层，已核）；④**可读性量化**：改后面板覆盖区合成亮度 14.9~19.3（单像素最亮 27.5），正文色 ≈212 → 对比充足；无面板处平均 15.5、最亮一张 52.7（星云透出微光、缝隙清楚可见）。前后对比图 `H:\大鲸鱼\_bg-preview\bg-visibility-compare.png`（现状 / 方案A / 方案C 三行同屏）。typecheck ×4 + core 719/719 + content:check + desktop build 绿（产物已确认 0.88 面板与 0.55 蒙层）【待验收/待发布】。

- 2026-09-10：**界面宇宙背景（船长提供 32 张 512×512 图，要求平铺应用、只作游戏背景、不动舰船与战斗背景）**——落地口径（船长三项确认：全部 32 张 / 整界面 / 压暗铺）：①**图源体检**：32 张**逐张量过左右与上下边缘像素差**（平均 1.6~3.2、最差 8.7/255）→ 确认为**无缝贴图**，平铺无接缝；PNG 合计 7.5 MB（照片型图 PNG 编码吃亏），**转 JPEG(q85) 后 0.96 MB** 收进 `apps/desktop/src/renderer/src/assets/space/`（原 PNG 留在 `docs/Small 512x512/` 作源图、不进包）；②**抽图规则**：一次会话只用**同一张**平铺（每张只与自己无缝，混铺必出接缝）→ 启动时随机抽一张并固定，见 `ui/spaceBg.ts`（`import.meta.glob` 取 URL，构建后是 `new URL(..., import.meta.url)` 相对解析，Electron file:// 下可用）；③**落点**：`.app-root` 的 background 层 = 72% 深色蒙层（同色系 #060b13）+ 平铺底图（512px 原始尺寸），URL 由 `main.tsx` 首帧前写入 `--space-bg`（无图时 var 兜底 none，退回原深色底）；④**不动项审计**：战斗画面 `.app-battle-screen`（不透明径向底 + 星野视差层）、序章 `.app-prologue`（纯黑底）均为不透明覆盖层，底图**不会透进去**；舰船/无人机 SVG 与战场演出零改动；半透明弹层（公告/确认框/mask）会自然透出星云（属预期）；⑤**可读性量化**：压暗后 32 张的合成平均亮度 13.6、最亮一张 36.7、单像素最亮 79（界面文字 ≈220）→ 不影响读字；⑥**设置里可更换（船长追加）**：顶栏「设置」新增「宇宙背景」行——显示当前这张的玩家向短名（蓝星云 03 / 星野 04 …）+「换一张」按钮，点了立即换成**另一张**随机底图（`rerollSpaceBg`：排除当前下标重抽，单张时兜底顺延），**本场会话内有效、不写偏好**（"每次启动随机"是船长选定的默认行为）；无图时该行显示「未启用」且按钮置灰。typecheck ×4 + core 703/703 + content:check + desktop build 绿（产物含 32 张 jpg，合计 0.96 MB）；公告待审稿补第 6 卡【待验收/待发布】。

- 2026-09-10：**市场按槽位分档 + 行内改显示自己的库存（船长三项指令；同日按船长指正修正过一版）**——①**类型筛选**：**移除「装备」类型**，改为三档**新类型**「高槽装备 / 中槽装备 / 低槽装备」；判定走 core 归槽单点 `rackOf`（模块显式 rack 优先，缺省按槽位推导）。**装备的功能子分类保持原样**（采集与货舱/武器/护盾/装甲/推进器/无人机装置/支援件/打捞器/目标锁定），三个槽类类型下都挂着同一套子分类 → **槽类 × 功能两级叠加筛选**（如 高槽装备+武器、中槽装备+支援件=命中/闪避件、低槽装备+支援件=伤害/射速件）；商品行的类型文案随之显示槽类（高槽装备等）。②**蓝图子类**：「装备蓝图」按**产物模块槽类**拆成 高槽 / 中槽 / 低槽装备蓝图，保留舰船蓝图与补给蓝图（弹药·修理组件）；手册的蓝图图鉴分组同源生效。③**商品行去掉「站内库存」与「常驻」两处字样**（船长：缠住=常驻的错字），改为显示**玩家自己**这件东西的库存——物品→物品仓库、装备→装备库、蓝图→图书存量、核心→核心库、舰船→机库同型艘数（core「自然库存」`naturalHoldings` 单点 + 舰船单独数机库；已挂单托管量不计）；悬停说明里也不再写「常驻」（只标稀有/限定）。**【同日修正note】**首版误把「高/中/低槽」做成了「装备」的**子分类**（并连带改了手册装备图鉴的分组、还误移了行情详情的「库存池」）——按船长指正全部回退：装备子分类表与手册装备图鉴分组恢复原样，行情详情的「库存池」与标题「持有 N 件」恢复原样，市场/手册说明价格机制的文案也恢复原文；改动只落在**类型层**与**商品行**。**数据级验证**（临时探针，已删）：84 件装备 → 高 31 / 中 24 / 低 29（逐件恰好归一类、无孤儿，与「装备」合并口径一致）；叠加抽样 高槽+武器=11、高槽+采集与货舱=5、中槽+护盾=12、中槽+支援件=9、低槽+装甲=12、低槽+支援件=12（核对手册/装配页槽位一致）；113 张蓝图 → 高 29 / 中 24 / 低 28 / 船 24 / 补给 8。typecheck ×4 + core 700/700 + content:check + desktop build 绿【待验收/待发布】。

- 2026-09-10：**机群战损表现层对接（二号设计稿 §九 派给三号的两项 + 一处连带修复）**——①**击落演出**：被点防打掉的无人机在**原位**出小爆炸（SVG 线稿：冲击环 + 八向爆散线 + 三片碎屑下坠），760ms 自动清除；只动 `transform`/`opacity`、无逐帧 JS、无 filter（约定第十四章）；落点取"本帧即将消失的末位机体"（引擎存活架数已减 1 → 渲染机体数随之减 1，用上一帧机体数定位）；②**战报「机群损失」行**：战报弹层新增「机群损失：机型 ×N（无人机舱清单已扣除，回港需补充）」，数据在**分出胜负那一刻**写进战报快照（`OutroSnap.droneLost`——结算后 `battle` 已清空，届时取不到）；③**连带修复**：`droneDown` 事件此前被表现层当成一次开火 → 会在母舰与敌舰之间画出并不存在的弹道（与"弹道不显示"同源的老坑），现于 fx 消费处提前拦下（不画弹道/闪光）；④引擎侧契约测试补一例（`drone-anim.test.ts`：击落事件必须带 `droneDown + src:'drone' + artId + side:'me'`，且 `arcs.droneLost` 与本场记录一致）；口径入 `docs/design/drone-losses-20260910.md` §九（转"已实现"）；公告待审稿附二同步（演出随点防卡生效、不单独成卡）。typecheck ×4 + core 677/677 + content:check + desktop build 绿【待验收/待发布】。

- 2026-09-10：**补 rank5 训练档底（船长：「对错误时间，按照之前 rank 的规则进行修正」+「无视 48h 栅栏，允许突破 48h」）**——规则依据 = 2026-09-05 定档提交（`8987c3b`）：单级时长 = 档位基础 × rank × 系数，阶梯按「满级总时长」成形 **r1≈0.5h / r2≈1h / r3≈4h / r4≈24h（步长 ×2/×4/×6，低档快高档慢）**，当年 r4 特意压在 48h 红线一半「给未来更高档留量」；当晚系数由 `2^(N-1)`(Σ31) 改为 `[1,2,4,16,64]`(Σ87，×2.81) 且船长拍板"不作红线修正"，但 **rank5 档底始终没补** → 落 `DEFAULT_TRAIN_BASE_MS`(60s)，满级只要 **7.25h**，比 rank3（11.2h）、rank4（67.3h）都便宜，与「低档快高档慢」相反（玩家反馈的「调度学能升到 LV6」排查中发现）。修正：`RANK_BASE_MS` 补 **`5: 742_000`**（阶梯下一级 ×8 按比值外推 = r4 的 67.3h × 4/3）→ **调度学满级 ≈89.7h**（3.7 天），r5 才真正是全游戏最长档；手册给玩家的「难度 N（数值越大整条线练得越慢）」随之成立（文案无需改）。**影响面**：只影响「AI 核心调度学」一个技能；`balance-check` 训练链（会计/贸易谈判/采矿等）不含该技能 → D1 体检 A/B/C 基线不变；存档**零迁移**（在练的级按毫秒保留进度，仅本级剩余时间变长）。**新增契约护栏**：`training.test.ts` 加「每个 rank 档都有专属档底、满级总时长随 rank 严格递增」+ rank5 逐档锚点断言——**负向验证**：去掉 rank5 档底 → 契约测试如实失败（`expected 26,100,000 to be greater than 242,208,000`，即 7.25h < 67.3h），还原后 9/9 绿。口径同步：`training.ts` 头注、`skill-rank-dispersion.md` 阶梯表、`captain-skill-audit-batch.md` ⑩ 处留 2026-09-10 更正note（原文"r5 ≈7.3h、为全游戏最长档"作废）。typecheck ×4 + core + content:check + desktop build 绿【待验收/待发布】。

- 2026-09-10：**排查+修复：玩家反馈「AI 核心调度学能升到 LV6」**——结论：**Lv6 在引擎侧到不了**（①`state.ts` MAX_SKILL_LEVEL=5；②`enqueueSkill` 目标限 1~5；③连锁逐级校验；④读档 `trained`/队列目标一律夹紧 ≤5；另 `ai.ts` 效果公式 `Math.min(5,…)`，即使出现 6 也无经济漏洞）。**玩家看到的是技能页按钮文案 bug**：`SkillsPage.nextLevelAction` 无条件用 `lastQueued + 1` 出文案，技能在 **Lv4 且队列里已排 Lv5** 时写出「追加→Lv6」，点下去被引擎拒绝（「该技能已排队到满级，无法再追加」）——调度学是**全游戏唯一 rank 5 技能**（rank 分布 1:8/2:26/3:25/4:12/5:1）且大家都在推满级，故被玩家发现。修复：①按钮改「已排队到满级」+ 置灰 + title 说明（三处渲染点同步 `disabled`）；②补两道**纵深防御**：`advanceSkillQueue` 在等级已达上限时直接出队并留 warn 日志、`moveQueueItem` 重算目标夹紧 ≤5；③新增 `packages/core/tests/skill-level-cap.test.ts`（4 例，含"注入超限目标"的异常档场景）；**负向验证**：把两道防御去掉 → 测试如实失败（`expected 6 to be 5`、`[5,6] ≠ [5,5]`），还原后全绿。**遗留待船长拍板（本次未动数值）**：`training.ts` 的 `RANK_BASE_MS` **只定义 rank 1~4**，rank 5 落默认 60 秒档 → 调度学练满 5 级仅约 **7.25 小时**，比 rank3（11.2h）、rank4（67.3h）都便宜，与同文件注释「rank5 更在此之上」及手册给玩家的「难度 N（数值越大练得越慢）」口径相反。typecheck ×4 + core 669/669 + content:check + desktop build 绿【待验收/待发布】。

- 2026-09-10：**工业页「精炼炉 / 组装机」卡牌名字前加同款小图标（船长：参考手册图鉴）**——精炼炉·回收炉卡（IndustryPage FurnaceCard）取 `def.kind`（矿石/矿物/气体/冰矿/残骸）；组装机卡（panels/Industry BlueprintCard）新增 `productGlyph` 取**产物键**（物品/弹药 = kind、装备 = 槽位、舰船 = 船族，兜底蓝图）；两处都**替换掉原有的文字符号前缀**（回收炉「⚒」、组装机「◈/▣」）为 SVG 线稿图标（`RowGlyph`，与物品页/货仓列表同一实现）。**顺带补齐 5 个此前缺失的图标键**：wreck／fragment／kit／salvager／target-lock（这些种类/槽位在仓库·货仓·图鉴·装备库里原本静默落兜底圆环徽，色调分别为 旧黄铜／蓝图紫沉一档／维修绿／打捞青／准星黄）；并给 `content:check` 加**图标覆盖契约护栏**（物品 9 类 + 槽位 14 个 + 船族 4 个 + 蓝图；缺图形或缺色调即失败，已做正向/负向验证）。**蓝图书架卡片未动**（船长未提；仍是 ▦/◈ 文字前缀，需要的话同一行即可换）。typecheck ×4 + core 658/658 + content:check + desktop build 绿【待验收/待发布】。

- 2026-09-10：**物品页仓库与货仓的列表模式加行首图标（船长：参考手册图鉴，在名字前加上对应的图标）**——行首小图标组件 `RowGlyph` 由 Handbook 本地实现上移为**共用唯一实现**（`ui/itemView.tsx`，与「图标/列表视图」族同处；检索入口已写进该文件头），键口径与图鉴完全一致（物品取 `def.kind`、装备取 `def.slot`，同一 Glyphs 图标库 + toneOf 色调）；物品页仓库（物品行 + 装备库行）与货仓（货物行 + 船载装备行）各两处加图标，**仅列表模式**生效（图标模式本就是图标网格）；样式沿用图鉴行的 `.app-hand-row-glyph`，未新增任何样式；typecheck ×4 + desktop build 绿【待验收/待发布】。

- 2026-09-10：**AI 指挥中心「执行中」每行加工作动画（船长：给每个 AI 显示一个简单的工作动画、按工作内容差分；并问"是否会大幅消耗性能"）**——船长三项口径均取推荐：**仅执行中列表 / 6 类各一套 / SVG 线稿**。新增唯一实现 `ui/aiWorkFx.tsx`（采矿·打捞·掩护巡逻·精炼炉·回收炉·组装制造；判据全取引擎真值，回收炉与精炼炉同用一处 `item.kind==='wreck'` 判据、文案与动画不分叉），ShipPage 三处行（副船 / 精炼回收炉 / 组装机）插入固定 56×36 槽位 `.app-inv-fx`（行结构走新修饰类 `.app-inv-main.is-aiwork`，不动共享的 `.app-inv-row` 其它页面用法），styles.css 加槽位与 keyframes；**性能结论：不会大幅消耗**（行数个位数、只动 transform/opacity、不碰 filter/box-shadow/宽高、不新建 rAF、`memo` 化使每 tick 整树重渲染时跳过整棵 SVG 子树、切页即卸载）；无视觉通道故做几何自检（六场景包围盒全在 56×36 内，首轮修掉打捞残片贴边 x=56 与扫描弧改三次贝塞尔）+ 预检图 `H:\大鲸鱼\_aifx-preview\`；typecheck ×4 + core 658/658 + desktop build + content:check 全绿；口径与性能纪律入 `docs/design/ai-work-fx-20260910.md`【待验收/待发布】。

- 2026-09-10：**我的舰队卡片左侧舰影列（船长："将每艘船的舰船形象放在对应容器的最左侧展示"；追加："当玩家屏幕宽度不足时隐藏舰船图形"）**——舰队卡新增固定 132 宽舰影列（`ShipSprite` + `engine={false}` 不画尾焰），原卡片内容整体入 `.app-ship-main`；只加修饰类 `.app-ship-card.is-fleet`，同页舰船市场卡片保持原纵向排布；显隐用 `useLayoutEffect` + `ResizeObserver` 量列表容器实宽（<702px = 132+10+560 即整列不渲染，不用窗口宽猜——壳层 168px 侧栏与手机旋转虚拟宽都会让窗口宽失真），舰影 `memo` 化、未加 filter 辉光（约定第十四章第 5 条）；口径与各窗口档位实测入 `docs/design/t5b-fleet-instances.md` 追加节【待验收/待发布】。

- 2026-09-10：**残骸保底线 5→10 + 自然恢复速度 4h→96h（船长拍板：保底线抬高、"大幅下调恢复速度"；mul 分母"不做修改"）**——①`WRECK_FLOOR` **5 → 10**（≤10 不扣密度、进入保底稳态；打捞放干扣减的超出量基准随之改为 d−10，进位收口同步）；②`WRECK_RECOVER_MS` **4h → 96h**（密度 <base 的线性回升需四天回满；>base 衰减仍 48h）——打捞放干一个星系后自然恢复极慢（"耗尽感"）；③**船长明确 `mul = max(0.5, 密度/10)` 分母不改**：（原表述"产出依旧保底 50%"）→ 实际稳态保底（密度 10）系数 = **1.0**（记录在案，如需回到 50% 只需把分母改 20）；④文档/测试同步：release 说明进 salvage.ts 头注与函数注释、词典「残骸密度」词条、b3-salvage.md、salvage 测试断言（超出量 30×2%、收敛到 10、稳态 mul 1.0、回升 96h 与挂起用例）；core 641/641 + typecheck ×4 + content:check + desktop build 绿【待验收/待发布】。

- 2026-09-10：**星系基础残骸量 = 该星系悬赏 ×20 次 + 悬赏注入按敌人数 +20%/敌 + 遇袭按最强卡 ×0.5 + 回收档位阈值等比上调（船长多轮拍板）**——①`wreckBaseDensity` 由「安全等级曲线 10~40」改为**该星系全部可见悬赏卡「完成 20 次」的注入量之和**（两卡求和；无可见卡回退旧曲线兜底）→ 实测 **58（大鲸鱼Ⅳ）~1536（穹顶墓园）**；②`injectWreckDensity` 改为注入给定量，悬赏路径 Δ = **威胁 ×0.4 × (1 + 0.2×敌人数)**（敌人数 = 主舰+僚机+多波全部单位：单敌 ×1.2 / 三敌 ×1.6 / 五敌 ×2.0）；③低安遇袭 = 该星系**最强悬赏卡**注入量 ×0.5（无卡回退旧口径）；④回收档位阈值**等比上调**（系数 21.4 = 新/旧 base 均值比）：险 20→**428**、危 30→**642**；⑤连带：5 张卡变档（红环两卡 险→危、烬火 危→常、天底/裂谷 危→险、碎晶带 险→常）→ **6 张 B3.1 特色池按新档基数重校**（池均价 = m×新档基数 ±3%，21 张全绿）；打捞体积当量 mul 由 1~4 → **5.8~153.6**（产出暴涨、回收炉速成唯一瓶颈，船长确认接受）；回归：salvage/salvaging/recycle 测试按新口径重写与更新（含按卡推导/两卡求和/敌数加成/档位边界 428·642/遇袭基准）；core 641/641 + typecheck ×4 + content:check（B3.1 特色池 21 张全绿）+ desktop build 绿；词典与 b3-salvage.md 口径同步【待验收/待发布】。

- 2026-09-10：**修复：重复清剿中「退出战场」会误停连击（船长定位）+ 停环提示带数字与在线弹窗（船长定：只实行 B）**——①**真凶**：App 战场关闭回调走 `engine.onBattleViewClosed()`，在「连击自动发起的战斗」中退出观看即清 `autoLoopAnomalyId`（旧兜底逻辑"视为收手"）→ 玩家只是退出观看就被停环；修复 = 删除该停环兜底，退出战场仅关闭全屏界面（战斗后台照常推进、清剿照常继续；中止请用「⚑ 撤退」，撤退才停环），`autoSortie` 仅保留"连击战斗默认最小化界面"用途；全量审计其余 6 条停环路径（战败/进退两路撤退/转战采矿/换船/条件不满足）均属设计内；②**B 项**：停环文案统一带「（当前 装甲 X% / 结构 Y%）」（stopAutoLoopReason 与自动撤退两路），并写入一次性提示 `state.autoLoopStopNotice`（同 deliveryNotice 模式：不落档、零迁移）→ 心跳 drain 后经既有 onSystemNotice 弹窗（玩家在线除事件日志外可见；手动撤退不弹）；回归 +2（文案数字/一次性提示写入、未停环不写）；core 636/636 + typecheck ×4 + content:check + desktop build 绿【待验收/待发布】。

- 2026-09-10：**排查：重复清剿「停下」原因（玩家反馈"战损不多却停、平均战斗 5 分钟"）**——工具 tools/loop-stop-check.ts 真实引擎复现（正式入库，改判定后可复跑），结论**现象成立、但非"结构损伤过重"单因**：①**判定口径**=「装甲**或**结构任一 <50%」（2026-09-08 连续作战保险）→ 装甲先吃伤害，实测终局常见「**装甲 0% / 结构 94%**」→ 玩家看结构"几乎没伤"却停环（反馈主因）；②停环前自动维修 repairWithKits(target 0.6) 会**循环烧光货仓修理组件**（装甲 0% 时 40 枚也修不到 50%，全烧后停环）；③另一类=真·战斗内结构 <50% 自动撤退停环（中配打噬口 80/虚海 88 多波长盘复现，停止时结构 26~42%/组件未耗），贴合"单场 5 分钟"观感；④顶配牛鲨 vs 穹顶 96 可连胜 11 场不停，但组件同样被烧光（100→77% 装甲、组件 0）。**待船长拍板的改进项**：A 判定口径（装甲低而结构健康是否继续/分列阈值）· B 停环提示带当前 装甲/结构 数字 · C 自动维修组件节流（单次上限或保留数）· D 停环原因在活动栏醒目提示【排查记录，无行为改动】。

- 2026-09-10：**无人机分类并入「种类」显示（船长："无人机现有分类将其归类子属性一起显示在种类中"；追加：中继天线 MK3 CPU 38→46）**——无人机分类入物品本体（ItemDef.droneClass：scout/combat/assault/sentry = 侦察机/战斗机/攻坚机/哨戒机，与射程分类一一对应），core 单点 `itemKindText()` 输出「无人机 · 侦察机」，市场行种类列/物品仓库/货仓/图鉴/手册详情行与列表 chip 全部走同一单点（残骸特例保留）；顺带补齐上轮遗漏——物品信息行新增「射程上限」行（2500/3000/3500/5000 m，注明中继天线乘入）；content:check 新增无人机分类契约护栏（字段值域 + 射程与分类对应）；回归 +1（四类文案/兜底/非无人机）；**另：中继天线 MK3 CPU 38→46（船长追加）**，并追查补齐上一提交丢失的数据层四件（modules 三件/blueprints 三张/marketCatalog 六行/rarityTier 六键——上轮部分编辑未落盘，本轮逐文件验证）；core 621/621 + typecheck ×4 + content:check + desktop build 绿【待验收/待发布】。

- 2026-09-10：**无人机战斗动画·第一批（三号；船长逐轮拍板 11 次，已验收）**——引擎侧差异化地基：`WeaponSpec.src`（turret/missile/laser/drone/base）+ `artId`，`BattleFx.src/artId`（旧事件缺省回退旧口径），射程弧把逐架无人机条目合并为「机型 ×N」一条（冷却取组内最小）；表现层：新增 `ui/droneArt.tsx`（4 型机体 SVG 线稿 + 编队/阵位/弹点形制，上限 6 架/型），**出击制**演出（机库口放出 → 上凸弧飞抵敌侧**随机阵位**（Y ±48px）→ 到位驻留 220ms 开火 → 立刻掉头沿下凸弧返航；哨戒雷鸥常驻母舰**上方**伴飞恒显 1 架），无人机弹道**起点恒与机体同源**、**瞄准敌舰中心**、小曳光点（提速 ×0.55）/哨戒细曳光条（flyMul 1.6）、命中与闪光小型化；数值：装填 2200→**4400ms** + 单发 **×2**（**净 DPS 不变**，校准口径不变）；机群制（编队巡飞）整套保留于 `DRONE_STYLE` 一行切换；**期间修复 6 项**（弹道不可见/落弹位置偏移/无人机移动 10fps 步进/击毁瞬间绘制尖峰 63ms→0/重进战场已摧毁敌舰复活/战斗结束黑屏——最后一项为渲染体提前 return 之后出现 hook 导致 React 卸载整棵树）；core 641/641 + typecheck ×4 + desktop build 绿；设计稿 drone-combat-animation-20260910.md（含收尾归档与 7 条经验教训）【已验收 · 待随批发布】；损毁系统（无人机可损毁/近防炮/敌方机群预留）为**第二批**，另行排期。

- 2026-09-10：**无人机战斗动画差异化（挂起·移交三号执行；玩家反馈"战斗动画与普通武器同质化"）**——无人机无机体（引擎 v1 口径并入火力、不单独成单位不损毁），fx 事件只带弹型不带武器来源 → 与炮台同色同形弹道。移交内容：①引擎侧 fx 携带武器来源（combat.pushBattleFx 增来源字段：炮台/导弹/激光/无人机/基础舰炮——差异化地基）；②无人机弹道差异化（细密蜂群弹点/独立配色，按 2026-09-10 SVG 视觉纪律）；③可选：机群机体演出（母舰侧小机体列阵/环绕，开火自机群出弹）；④单位化/可击落不在范围。验收=战斗目测。

- 2026-09-10：**无人机射程分类 + 中继天线装置（玩家反馈"无人机射程太短"；船长拍板：先分类按型定射程、前三型大幅缩减哨戒独享远程；新增高槽装置提高射程——命名「无人机中继天线」、百分比制）**——机型射程入物品本体（ItemDef.maxRangeM）：蜂鸟 **2500** / 赤鸢 **3000** / 猎鹰 **3500** / 雷鸥哨戒 **5000**（旧硬编码 2600 兜底，描述"航程极远"兑现）；新装备族**无人机中继天线**（slot drone-relay 高槽；MK1/2/3 = 射程 **+20%/+45%/+80%**、CPU 6/16/46（MK3 船长追加 38→46）、市场 15k/150k/400k；百分比求和乘入机型基础射程、多件线性可叠——蜂鸟带 MK3=4500、雷鸥带 MK3=9000 进激光/导弹带）；蓝图书 3 张随全蓝图化体系（M1 常驻书 / M2 稀有 / M3 稀有+声望 4；材料≈产物×0.42 模板）；全链贯通：types（ModuleSlot+字段）、labels（槽表/中文名/归槽）、combat（装载读机型射程×Σ中继乘数）、UI（Glyphs 图标+配色、装配短效/信息行）、市场 6 行 + 稀有度表 6 键、护栏；校准（drone-relay 矩阵留档）：满技能 **D3 坟场 80→100%（残血 20→39%）**、星髓 91→98% 更稳、穹顶墙保持 0%（巡洋验收口径不变）；蜂鸟压缩致 D1 星髓 80→60%（轻型机贴身分工，预期内）；回归 drone-relay +3；core 620/620 + typecheck ×4 + content:check + desktop build 绿【待验收/待发布】。
