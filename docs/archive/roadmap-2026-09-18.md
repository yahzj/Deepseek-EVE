# 封存卷 · roadmap 批次条目 2026-09-18

> **冻结件**（2026-09-18 当日批次条目）：从 `docs/roadmap.md` **原文搬运、一字未改**（含船长原话照抄、逐问逐答、负向验证与读数）。
> 封存日期 **2026-09-15**（三号 · verify40）· 搬运时原行号区间 2026-09-18 当日条目。
> **检索**：本目录内 grep，或 `git log -S "<关键词>" -- docs/roadmap.md`（历史版本随时可翻）。
> **不要在本卷里追加新内容**——新批次写进 `docs/roadmap.md` 的滚动窗口，越窗后由 `npm run docs:seal` 封存进新卷。

---
- 2026-09-18：**玩家报障「军用修理组件设目标 1000，产出 151 就停了」⇒ 组装机循环制造改按「批」＋ 新增「报障处理」条款（船长两句 · 二号 · d2）**——**①报障与取证（真引擎复现）**：`bp-repairkit-mil` = **3 个/批**，而「循环制造」的 `produced`/`goal` 一直按**批**计数、界面却写"件" ⇒ 玩家那份「151 件」实为 **151 批 = 453 件**；实测材料只够 200 批时引擎跑满 200 批后**材料耗尽停线**（卡片标「已停线：材料不足（缺 晶态胶体…、银纹超金属…）」＋日志汇总），日志那句「本卡合计 200 件」掩盖了**实际到手 600 件** ⇒ 停线本身是设计内行为，坏的是**单位口径**。**②船长原话（照抄）**：「**并不是本地存档。但是同意将UI改成按批制作。建议加入提示，所有玩家反馈的问题，并不在本地存档。**」⇒ **引擎口径不动（仍按批）＝零迁移**，只改文案：`manufacturing.ts`（日志「本卡合计 N 批」· 停因「已达成目标 N 批」· 头注补"单位口径"段）· `Industry.tsx`（目标输入 tooltip →「目标**批数**…一批的产出件数见蓝图说明」）· `ShipPage.tsx`（线行进度「本卡合计 X/Y 批」）。**③新增约定条款（§二）**：「**报障处理：玩家反馈的问题不在本地存档**」——玩家报障说的是他们那份线上档，**本地档与此无关、不许拿它复现/覆写**；处理报障的唯一正道 = 从代码与真引擎复现（探针或确定性用例），结论写清「复现路径 / 根因代码位置 / 玩家可见现象为何长这样 / 改法与影响面」。**④用例**：`manufacturing.test.ts` 新增「多产出蓝图：目标 = 批数」（目标 2 批 ⇒ 到手 6 件 · 停因/日志写"2 批"），旧钉值同步改「批」。**⑤闸门**：typecheck 0 错 · core 166 文件 / **1817 例** · content:check ✅ · ui:rot-check ✅ · 桌面构建 ✅。**⑥如实登记**：批量替换脚本把单元素数组拍平 ⇒ 误改 `ShipPage.tsx`（387 行）⇒ 当场 `git diff --stat` 抓到、`git checkout` 还原后用编辑工具重做。**【已合入 main · 待推送】**

- 2026-09-18：**教程重做：「第一次」任务系列 ＋ 任务中心改版 ＋ 次数链标定（船长 2026-09-17/18 多轮指令 · 一号 · main；本文档由一号按 §8 归档）**——**①船长原话（照抄，分三批）**：**立项**「**我希望将现有教程重做，改成以重要任务的形式发布在任务中心，让玩家自由选择完成。**」＋「**这些重要任务完成后，还有后续的次数任务**」＋「**至少 5 个级别；部分没有内容上限的，按 10 个级别做**」；**第一轮**「母港扫描时间缩短至10秒」「每级的信用点奖励…按照级别的 5 次方给予奖励」「新建一张沙猫级的蓝图，按照沙猫级价值 1W 来设定（但是实际上沙猫市场价格是 0，也不会出现在市场内）」「奖励不明显，建议将奖励单独起一行，并采用金色字体」「重要任务进行到哪一个级别要直接在任务名称内告诉玩家」「任务内容过于简略，建议加入一定量的文本丰富」「任务进度建议采取当前已累计100/1000这样的形式」「点下「扫描探索」的那一刻就关（注意不要出现关闭窗口太快导致误触）」「已经全部完成的重要任务隐藏」「顺便打算制作成就系统…先挂机，可以预留接口」；**第二轮（L10 标定）**「虫洞削减到500，采矿增加到1000万，精炼增加到100万…悬赏次数提高到一万次。打捞提高到五万。挂单按照市场交易收入计数，最高档按1000亿算。造船改为1000条。生产增加到100万。并且任务奖金要写清楚当前这级的具体数额。不能让玩家自己算。各个任务说明里的'相送'没有任何意义…建议删除所有任务中相送相关文案」＋八问答复（技能 L10 = **500**、精炼按**批**、交易收入 = **通过市场获得的信用点·税后·任何交易行为**、老档挂单张数**一次性折算**、收入链**按丙（每级 ×10）**、**都按曲线重排平滑数值**、没提到的保持原样、**只删说明**、奖金写"完成当前级别给的"）。**②落码（一表）**：`firstTasks.ts`（13 条 `FIRST_TASKS` ＋ `CHAIN_TIERS` ＋ `FIRST_UNLOCKS` ＋ `advanceFirstTasks`/`advanceFirstChains`/`claimChainReward`/`chainProgressOf`/`firstTaskBoard`/`nextRewardIsk`）· `firstRewards.ts`（六口袋发放一次性奖励）· `data/firstTaskMessages.ts`（13 封情报信）· `explore.ts`（`HOME_SCAN_WINDOW_MS = 10_000`）· `market.ts`（四处卖出入账累计 `marketIncome`）· `state.ts`（v27）· `save.ts`（v25→v26 老档一次性判定 ＋ v26→v27 挂单→交易收入**按级对齐**折算）· `panels/FirstTasks.tsx`（卡片三轮改版：链名带级别 · 跳转按钮 · `detail` 正文 · 金色奖励行 · 已全完成隐藏）· 引擎/各系统 14 处 `bumpFirst` 落点。**③读数**（`npm run balance:chains`）：13 条「第一次」11 条分钟级（最长 = 造船 15 分）；13 条链顶档 **1.7 天（造船）~ 593.5 天（市场 1,000 亿 @ 现产能，产能 ×10 ⇒ 59 天）**，其中虫洞 500 趟 = 89.3 天（卡坐标 4.3 小时/处）、精炼 100 万批需原矿 1 亿 ⇒ 6 线采矿 231 天、技能 500 级**现不可达**（表内 395 级）。**④验证**：typecheck 全绿 · core **166 文件 / 1816 例** · `content:check` ✅ · `ui:rot-check` ✅ · 双 build ✅ · `flow:newgame` ✅ · `save:migrate` **80/80**（v25→v27：13 条判过 · 资产不变 · 页面全开）· `tools:audit` ✅。**⑤公告**：`ann-first-tasks-20260918`（船长 2026-09-18「通过，按此推送」）。**⑥归档**：工作文档 `first-tasks-rework-20260917.md` 与 `chain-retune-20260918.md` **已删**（细节以 git 历史兜底）；词典新增 **§十 任务中心 ·「第一次」任务系列与次数链** 七条；待审稿 `announcement-draft-20260918-push.md` 作为批准存档保留。**【已合入 main · 已推送】**

- 2026-09-18：**电子舰新特性「压制敌舰射程 15%」＋ 鹦鹉螺槽位 3/4/4 ＋ 敌人期望距离随削减收缩（船长两次追加 · 二号；本文档由三号按 §8 归档）**——**①船长原话（照抄）**：「**鹦鹉螺的槽位改成3/4/4，电子舰新增特性，削减敌人15%的武器射程，可以乘法叠加，与敌人的射程增加效果做加法处理。（比如10000m射程，我方一艘电子舰，对方拥有射程+50%，那么对方实际射程为13500.）射程最短只能削弱到3000m（不足3000m的无法被削弱）。**」＋同日追加「**削减射程后，敌人的期望距离也要随之改变**」。**②口径**：多艘**乘法叠加**（`r = 1 − Π(1 − vᵢ)`：1 艘 15% · 2 艘 27.75% · 3 艘 38.6%）· 与敌方增程**做加法**（净倍率 = 增程倍率 − r）· **地板 3000m** 且"基础 <3000m 完全不削" · **只动最远射程、近界不动**。**③落码**：`types.ts` `ShipDef.foeRangeDebuffPct` · `ships.ts` 两艘电子舰 0.15 ＋ 鹦鹉螺 `slots` 4/4/3 → **3/4/4**（总数仍 11）· `combat.ts` `FOE_RANGE_DEBUFF_FLOOR_M` / `foeRangeDebuffOf` / `effectiveFoeRangeM`（`foeGunMaxRangeOf` 与 `foeDroneRangeOf` 改走它）· `state.ts` 运行态 `meFoeRangeDebuff`（每拍重算、不随档）· `shipInfo.tsx` 船体特性栏一条 · `content-check.ts` 新契约「电子舰特性契约」＋ 官方武装舰高槽弱断言加 `sh-nautilus` 豁免 · `foeDesiredRange(..., r)` 第 4 参缺省 0（旧调用方逐字不变）。**④读数**：用例钉死船长给的数（10000m·1 艘 ⇒ **8500** · 敌 +50% ⇒ **13500**、反证 ≠12750 · 2 艘 ⇒ 7225/12225 · 3500m ⇒ 3000 · 2500m 不削）；**期望距离联动实测**（kite 型敌卡）：稳态 **8350m → 6993m**、敌开火 84 → 40、我方伤害 **0 → 393** ⇒ 电子舰从"少挨打"变成"把敌人拽进自己的火线"；`battle:calibrate --std` 168 格逐字未变。**⑤验证**：core 165 文件 / 1807 例全绿 · `content:check` ✅（两艘电子舰 ⇒ 15% · 其余 41 艘不带）· `ui:rot-check` ✅。**⑥归档**：词典「船体特性」条加第 ⑦ 条 · 「劫掠电子舰」条补该特性与槽位 · 「期望交距」条补削减联动。**【已合入 main · 未推送】**

- 2026-09-18：**重复清剿改「可随时开关」＋ 再开前置（船长四答 · 三号 · verify；本文档由三号按 §8 归档）**——
**①船长原话（照抄）**：「**将重复清缴做成开关，玩家在进行悬赏的过程中也能开关控制是否重复清缴。**」；四答：
**开关入口只改悬赏卡**（活动栏按钮形态那问随之作废）· **开启前置沿用现有忙碌判定** · **战损/耐久未恢复则先挡住**。
**②现状（读码坐实）**：悬赏卡上本就有双向开关，但 `busyOther` 写的是「远征在飞 **且环不是本目标**」⇒
**正在打这个目标但还没开环**被判成忙碌、根本点不动（船长报的正是这个）；活动栏只有单向「停止清剿」。
**③落码**：`Expedition.tsx` 把判据改成「**在飞的是谁**」（`state.expedition.anomalyId !== anomaly.id`）⇒
进行中开关可点（本趟照常打完）＋按钮 disabled/title 镜像前置 · `core/expedition.ts` 新增 **`autoLoopReopenBlockReason`**
（①装甲或结构 <50% ②机群战损未补）并在 `setAutoLoopBounty` 开启前过门、**关闭一律放行** · `stopAutoLoopReason`
新增第三参 `droneFloor`（**只有"机群战损过半"那一路传剩余架数**）· 新随档可选字段 **`autoLoopDroneFloor`**
（`state.ts` + `save.ts` 归一化，坏值 ⇒ null，**零迁移**）· `core/index.ts` 再导出。**④验证**：typecheck 四包 0 错 ·
core **168 文件 / 1835 例**全绿（`loop-notice.test.ts` 既有"低耐久开环"那条按新前置改写为"先健康开环再打到线下"，
＋新增 4 条）· `content:check` ✅ · `ui:rot-check` ✅ · 双构建 ✅ · **真引擎探针五步**（健康开 ok / 关 ok /
装甲 40% 拒 / 战损未补拒 / 补货 ok 且记账清）。**⑤边界**：活动栏不动 · 忙碌判定其余部分不动 · 六类自动停环全保留 ·
不发公告。**⑥归档**：词典「重复清剿」条补该口径。**【已合入 main · 未推送】**
