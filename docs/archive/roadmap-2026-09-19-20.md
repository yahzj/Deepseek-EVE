# 封存卷 · roadmap 批次条目 2026-09-19/20

> **冻结件**（2026-09-19/20 当日批次条目）：从 `docs/roadmap.md` **原文搬运、一字未改**（含船长原话照抄、逐问逐答、负向验证与读数）。
> 封存日期 **2026-09-15**（三号 · verify40）· 搬运时原行号区间 2026-09-19/20 当日条目。
> **检索**：本目录内 grep，或 `git log -S "<关键词>" -- docs/roadmap.md`（历史版本随时可翻）。
> **不要在本卷里追加新内容**——新批次写进 `docs/roadmap.md` 的滚动窗口，越窗后由 `npm run docs:seal` 封存进新卷。

---
- 2026-09-19/20：**英语本地化（中英双语 · id 映射制）—— core 甲案 ＋ 界面批 ＋ 主进程 ＋ P4 读数表（三号 · verify40 · 已合入 main）**——
**①船长原话（要点）**：「希望对游戏进行英语本地化处理」·「所有本地化文本采用 ID 引用，只需要做一份本地化表就能直接进行替换」·
（2026-09-20 甲案裁定）「core 只产出「文案 id + 参数」，渲染层按语言渲染」·「手机允许出现滚动条」。
**②实现口径（约定 §十一之三）**：新文案**先取 id、再写表** —— 调用点 `t(id)`／core 用 `textId`/`errorId`，
中英都在**唯一表** `packages/data/src/l10n/table.ts`（**3,792 条**）⇒ 换语言/加语言只动表。
**③覆盖**：core 引擎文案 **803 处引用**全合规（含事件表 82 条正文 · 战报 · 炉子/市场/队列等多段拼接）·
渲染层 `t()`/`tr()` 约 **3,700 处**（含主进程窗口标题与系统对话框）· 内容数据走 P2 覆盖层 `localizeCtx`。
**④机制**：多段可空尾巴用 `composeLog(lead, segs, firstSegNo)`（`packages/core/src/logParts.ts`）——
**空段不入链**（空 id 会把后面整段丢掉）· 段内自带小词递归成段（`p{n}p{k}`）· 段内容本身要翻译走**两步渲染**（`pNId`）。
**⑤工具**：`npm run l10n:check`（闸门：死引用/占位符/中日韩残留 ＋ 声明制 `l10n-keep`）·
`npm run l10n:list -- <文件>`（逐条列未译）· `npm run l10n:wrap`（批量接线）· `npm run ui:overflow`（P4 读数）。
**⑥P4 读数（船长要的交付物）**：9 个一级页 × 2 语言 × 5 档窗口（桌面 3 ＋ 手机竖/横）× 3 份中后期真档 = **270 组**；
**「一级页不滚」270/270 成立**（桌面/窄窗内容区溢出全 0）；手机档有滚动但**船长裁定允许**。
**⑦两条实测教训（已写进老文档）**：**导航项数随「第一次」前置变**（`FIRST_UNLOCKS`：工业←采集 · 市场←生产 · 星图内页签←扫描；
新档只 8 项可见 ⇒ 界面读数"少东西"先查前置门）· **合并/大批改动后必须重启本地 dev**，否则旧 HMR 缓存会让界面回显 id。
**⑧验证**：typecheck 四包 0 错 · core **1967** 用例全绿 · `l10n:check`（未译 0；54 条 `l10n-keep` 已声明）·
`content:check` · `ui:rot-check` · `build` · `docs:index --check` 全绿。（过程细节见 git 历史；工作件已按 §十五 删除。）

- 2026-09-19/20：**谜质科技树（三线 23 节点 76 级）＋「每趟真实收获」读数（二号 · d2 · 已合入 main）**——
**①船长原话（要点）**：「消耗谜质和信用点。不消耗时间。主要研究对虫洞内的战斗和探索。另外含部分洞外工业科技」·「**谜质单价提高到70W**」·「研究入口放在扫描虫洞界面内。将现有的扫描虫洞分出2个子页面：虫洞探索和谜质科技」·「**加入科技后，初始回合数削减到80回合**」。
**②结果**：三线（探索 6 / 战斗 14 / 工业 3）· 满树 **1,196 枚虫洞谜质 ＋ 17.22 亿信用点**（洞内两条线 1,046 枚）；效果在 core **按 `effect` 关键字聚合**（不认节点 id）；**打捞/采集效率 → 额外堆**新机制；存档 **v29**（`state.research.levels`，老档补空树、零行为变化）。
**③读数（09-20 补 · 真引擎实跑）**：八层全扫上界 **23.8 枚/趟**（层 1/2 恰好 1 个谜质格、层 3 恰好 2 个 ⇒ **保底是承重墙**）；**真约束是回合预算**——42 回合只够扫 2~3 层（实测平均到达 **2.78 层**，层 4 起覆盖率 0%）⇒ 真档编队**每趟安全带出 2.67 枚**（最大 4），**满树 ≈ 450 趟**（与船长原估 3~8 枚/趟基本吻合 ⇒ 总量不必动）。工具 `npm run mt:harvest`。
**④一次结论更正（重要）**：读数首版「层 1 守卫只有 1/3 存活、瓶颈是守卫」**是假结论**——当时照抄 `tools/wormhole-econ.ts` 的技能表，而其中 **6 个技能 id 有 5 个不存在**（零有效技能裸奔）⇒ 真档编队打层 1 守卫 **12/12 全存活**。**遗留缺陷（未修，等船长定）**：`wormhole-econ.ts` 与 `counter-audit.ts` 仍用同一套假 id。
**⑤验证**：typecheck 四包 0 错 · core 全绿 · `content:check`（含「谜质科技契约」满树花费读数行）· `ui:rot-check` · `save:migrate` 全过。**词典已登记**（glossary §十一）。
