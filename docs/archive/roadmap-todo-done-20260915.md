# 封存卷 · roadmap 待办活面的已办结项（2026-09-15 改版时移出）

> **为什么在这里**：2026-09-15 船长要求 roadmap 精简（「删除过时内容，或者将过时内容移入另外一个文档并封存」）。
> 本卷收的是**改版当时已经办结**的部分，原文搬运、一字未改：
> ① 待办活面里凡标 `[✓]` 的列表项（含其续行）——已办结的待办；
> ② 旧版「读法（2026-09-11 补注）」注——它描述的旧结构已被新结构取代；
> ③ 船长反馈单 T1~T11 与 B 段的完整原文（roadmap 里只留一行核对结论）。
>
> **未办项仍留在 `docs/roadmap.md`**（`[ ]` 与未标记说明一律没动）。检索同其它卷：本目录 grep 或 `git log -S`。

---
> **读法（2026-09-11 补注）**：本文件有**两套时间序**——**顶部块** = 最新批次（最新在最上；条目按追加式记录，
> **历史条目不回改**，被后续条目取代的旧口径一律以新条目为准）；往下 `## 置顶` = 船长反馈单 T1–T11 的勾选清单；
> 再往下 `## 变更记录（本文件）` 段 = **更早批次**（越往下越旧，最早到 2026-09-04）。查"某功能做到哪了"看顶部块与
> `docs/glossary.md`；查"某决策的来龙去脉"往 `## 变更记录` 段翻。
> 用途：船长在 2026-09-04 认可本清单"很有启发"，要求保存为本地备忘防止遗忘；
> **每当我被问到"下一步 / 新工作流 / 继续做什么"时，先读本文件再讨论**。
> 状态约定：`[ ] 候选` → `[x] 已采纳/进行` → `[✓] 完成`（完成的保留一行历史并链接文档）。
> 相关基线：V16.2（安全等级色阶+悬赏 22 张长期化），见 docs/design/v16-tiering.md 与 architecture.md。
- [✓] **T1 顶部活动窗口**（批次 B→A 拆分）：顶栏常驻"活动"条 = 训练/采矿/制造/远征/扫描/AI 统一展示 + 统一终止入口（取消制造全额退料、远征仅途中/返航可召回、扫描终止保存进度）；散落各页的停止按钮已收敛；"其他互动窗口"澄清为不另起弹窗（见 docs/design/t1-activity-window.md）。
- [✓] **T2 技能队列（批次 A 主体）**：连锁训练——同技能可逐级多次入队（跳级/重复拒绝）；取消队首保留本级进度、重排自动续接（savedProgress 兼容字段，无版本号）；取消后同技能后续条目自动顺延；技能说明"实际效果"数字段 ⟦金色高亮⟧（6 条文案标记）；各级训练时长与入队后下一级时间展示（见 docs/design/t2-skill-queue.md）。
- [✓] **T3 货仓归属（批次 A）**：货仓页"查看船"选择条（船名 + 驾驶中标记 + 出勤徽标）可切看每艘船货仓；副船只读（装卸/出售仅对驾驶船，已确认 1甲/2甲/3甲）；驾驶船变更自动跟随（见 docs/design/t3-cargo-ownership.md）。
- [✓] **T4 采矿与换船/行程（批次 B）**：点开采先显式"前往矿带"（到带才开掘）；定稿：矿船空船出航跃迁×2（出航单程=返航一半），本地基准单程 120s（本地出航 60s/返航 120s），活动栏与矿带卡按阶段显示剩 X；**换驾驶善后（2026-09-04 船长推翻"换船重采"）**：采矿中直接在舰船页切换驾驶即成功，旧船自动返航倒计时、到港自动卸货入仓库（shipReturns 账本），采矿随之结束；手动停止维持原语义；**远征↔采矿互斥解除（2026-09-04 船长定稿转场语义）**：采矿中「转战出发」（警告含驾驶船型→确认→采矿终止货随船→从矿带星系出发）；远征中「再点确认开采」（取消远征+停连击→从母港开采；交火中不可）；原入口拒绝语义保留、新增两枚转场入口（见 docs/design/t4-mining-trips.md）。
- [✓] **T5 舰队交互（A ✅ / B ✅）**：A 已完成——卖船二次确认（内联确认+醒目警示：装配/货物未清禁用确认）；**船只锁定防误售**（跨会话持久，shipLocks 兼容字段；引擎出售守卫）；船型四色统一（工业绿/武装红/重装橙/航运蓝，舰队卡/市场卡/手册/货仓 chip）。B（重复舰船实例化）已随 v17 完成：uid=船型 id 第 1 艘 / `船型id#N` 递增不回收 + 同型自动编号 + 自由改名（见 docs/design/t5b-fleet-instances.md）。
- [✓] **T6 事件日志配色（批次 A）**：全量分类审计后修正 8 处 kind 错配（无资金动作误标 trade、挂单动作误标 info；语义口径甲经船长确认）；六类色板拉距（trade 绿/info 蓝灰/queue 淡青不再撞色）+ warn 红竖条；过滤开关兼作图例（色点+语义悬停说明）（见 docs/design/t6-log-colors.md）。
- [✓] **T7 星图时间一致性（批次 B）**：展示全部统一为"实际时间"口径（星图边距标签=当前驾驶船跃迁×技能的实际分钟+悬停标称说明；距母港/扫描往返提示同口径；出发后按锁定值显示）；途中换船=中断移动——采矿中换驾驶直接成功（旧船自动返航善后，2026-09-04 定稿）、远征拒绝提示先召回、扫描弹警告确认后终止（进度保留）再切换（见 docs/design/t7-time-consistency.md）。
- [✓] **T8 星系停留与连刷冷却（批次 B·大系统）**：位置模型（awayGalaxy 野外停留 + transit 返航空间站；行程按当前位置计程；矿采返航=最近空间站 seam）；悬赏胜利/扫描完成即停留目标星系（失利自动返航）；"连续出击"半自动环（10s 冷却后自动再出发；货仓装不下/耐久<0.5 且修理组件耗尽/战败即停；修理组件 seam=repairRestore，优先自动修复）；开关落档；悬赏重复冷却 10s×(500/scanRes)^0.5；**2026-09-06 语义更新：任务完成即返航 + 低安移动不暴露**（悬赏胜利/扫描完成自动返航 2×单程不可召回、连击=巡回讨伐、B1 暴露面收敛——见本文件变更记录与 t8/b1 文档头部）。B2 衰减后置（见 docs/design/t8-galaxy-stay.md）。
- [✓] **T10 任务界面（批次 B·讨论；按船长调整先于 T9 实施）**：星图页"悬赏情报"升级为「任务中心」——统一任务目录（任务族框架：当前悬赏族 22 张全机制保留；建站/引导族预留）；排序：距离/星系/奖励/声望/默认（默认=可接取冒泡+名称，次级均按名称）；视图层目录+读现有状态，不建引擎任务队列（见 docs/design/t10-task-center.md）。
- [✓] **T9 副空间站 + 通讯系统（批次 B·大系统；T10 之后实施）**：建站族任务承接（红环/烬火两站、分三档边交边生效：停靠卸货→维修补给→换驾驶+并入空间站清单；采矿返航走"最近空间站"解析；数值=2~4h 采集基调草案）；通讯器对话系统（通用剧本、一次性全文+镜像事件日志、已读不重播、卡内可重看；首抵自动弹建站提示）（见 docs/design/t9-substations.md）。
- [✓] **T11 调试器保留真实战斗（批次 A）**：debugQuick 不再即时判定战斗，战斗走实时引擎（core 分支移除 + 测试改造 8/8 绿；v15-debug.md 同步）。
- [✓] **B1 安全等级玩法化**（2026-09-04 定稿）：低安（sec<0）星系的采矿/停留/远征途中（主控与 AI 副船）按暴露窗口掷遭遇骰；承担=区域一次、停留船优先；在线「伏击待决」60s 可迎战（实时战斗自动打完）或快速脱离，离线直接文字三档（击退缴获/受损 −5~15% clamp5% 不弃船/被抢 ≤30% 货）；高安 sec≥0.5 太平；首次进低安提示 + 手册「航行须知」留档（见 docs/design/b1-lowsec-encounters.md）
- [✓] **B2 长期悬赏经济阀 ✅（2026-09-05 船长确认完成，自待办移除）**：反复刷奖金的经济阀评估收口（现机制 = 声望首胜制 completedBounties，见实施记录）
- [✓] **B3 掉落与残骸回收 ✅（2026-09-05 全链落地，见 docs/design/b3-salvage.md）**：星系残骸密度+打捞器+回收开箱；主控/AI 打捞；回收保底×1.1、彩头≤10%；漂流/整备/富集/回收/提纯 5 技能随载体上线。
- [✓] **B3.1 星系特色残骸产出 + 彩头按星系区分 ✅（2026-09-06 船长计划 → 2026-09-08 收尾完成）**：回收保底加**来源敌群特色**（每卡 recyclePool = 同档 2~3 主矿权重求解，池均价 = 档基数 × m ±3%，m = mSec≤1.45 × mThreat≤1.30 顶格 1.89 ≈155k/h）；彩头试点 6 星系主题件集（EV 等值缩放）；UI：工业页回收卡与星图「残骸打捞」星系卡都标「产出倾向 / 低出率掉落」行（ui/wreckFlavor 共用）；护栏：content-check ±3% 断言 + salvage:econ 逐卡对照；公告/手册/词典同步（见 docs/design/b3-flavor-content.md，状态：已确认收尾）。
- [✓] **C1 V12 P2「战斗小剧场」完成**（2026-09-05 船长确认，自待办移除）：全屏战场入口 + 演出/距离条指挥细化（v12-combat.md）
- [✓] **C2 装备/制造链复查**（2026-09-04 定稿执行）：审计发现自制倒挂（MK1 炮 1.28×~MK3 1.79×）、精炼链断裂（辉云 0.63×/磷光 0.52×）；船长定稿"成品价 ≥ 材料+制造费（蓝图不计）、提高基础价、稀有度溢价提高"——现货价阶梯 ×1.15/×1.4/×1.7 落地（战斗件 MK2/3 同步）+ 精炼回补（同位聚晶 45→55、星髓晶 220→245；辉云 iso 1.7→2.7、磷光 iso 2.4/星髓 0.9、希莫 noc 0.55），14 条精炼链 ≥1.11×（见 docs/design/c2-craft-cost.md）；**2026-09-08 追加（船长拍板）：取消每次制造费**——护栏改"现货 ≥ 材料成本"，自制 ÷ 现货重估 0.17~0.61× 全无倒挂、原 105% 巨灵鲸级转 0.61×（见 c2-craft-cost.md §六）；**卖出侧同步：站内让利吸收**（吸收量与价格挂钩：折 1% 提速 40%、折 10% 封顶 ×5；F = 池 supplyFlow / 单件 1 / rare 0.3 / 奇货 0.1 件每窗，吃簿优先不叠加；批量清货 = 让利换吞吐；见 docs/design/market-sell-absorb.md）；**收购侧同批收尾：档位 + 簿面件数 + 两侧抢单**（2026-09-08 船长确认：收购档位 = 原料池 1.0 / 池耗材 0.6 / common 单件 0.6 / rare 0.65 / 奇货 1.0L；簿面件数 common 单件 ×3、rare ×2/张；越线挂单抢单 30%·e^(−6r) 与 20%·e^(−14s) 每 60s 窗；见 docs/design/market-acquisition-rework.md）
- [✓] **C4 校准轮 ✅（2026-09-05 船长确认完成，自待办移除）**：V17.1 抗性/推进初值 + V18 多炮/多矿枪/无人机件 + V18.1 支援件 18 件初值 对 threat 曲线的复核（battle-calibrate 扩展装配行）
      **（2026-09-04 交接：校准轮由大鲸鱼二号负责**——范围 = battle-calibrate 扩展 V18/V18.1 装配行
      （多炮/多矿枪/无人机件/支援件）+ V18A/V18.1 暂定初值复核 + 24h 策略基线复跑（B/C 精炼流新节奏含回港
      运转）；D1 与之衔接；见 docs/design/refine-cycles.md §四与变更记录；
      **2026-09-05 启动信号：船长实测"数据极度不平衡"——敌射程手短被风筝白嫖 + 双方血量过少秒杀
      （探针矩阵：非单调悬崖胜率/支援件超模/激光偏强/入门陡——草案见 docs/design/c4-calibration-notes.md）
      → 暂缓：等一号战斗技能齐全后一起测试再校准**）
- [✓] E1 图片化沟通（工作流已启用：船长发图/标注 → 实现）
- [✓] **O8 精炼炉运转分档校准 ✅（2026-09-09 一号；船长批准四档参考矿船桶后实现，详见变更记录与本文件顶部 🔁 记录）**——14 条配方批量/周期按四档桶对齐：富凡/灼烧 20/24s（3,000/h）；希莫/辉云/曦棱/氖云/蓝霜 18/11s（5,891/h）；磷光/离子/极光/寒髓 34/10s（12,240/h）；玄晶/星幽 58/8s（26,100/h）；**暗星冰 42/5.8s（26,069/h）**——58/8s 会使 120% 产出倍率净率 135.9% 超护栏（content-check ≤135%），42/5.8s 净率 132.7% 合规；perOre 未动；审核表已同步内容工作簿 content-csv/（items sheet）【待验收/待发布】。
- [✓] **O9 蓝图非舰船耗时单独审核 ✅（2026-09-09 收口：船长拍板全部维持现状，零改动）**：17 张非舰船蓝图全表 + 三处发现（修理组件两线净/h 超锚 3~30 倍 = 低门槛补给福利线**保留**、民用级装备档低于锚 = 练手线**保留**、弹药 OK）已出表 = docs/design/blueprint-buildtime-audit-20260909.md；商讨结论 = 修理组件/民用级装备/弹药 **均不变**（含 A1 同比例放大不降净/h 的表述更正）；舰船 buildSeconds 另议（未立项）。
- [✓] **敌舰配置表（舰级表）· A 族试点 ✅（2026-09-11 二号；船长定案「给敌人单独一套敌舰的配置表，敌舰属性按照配置表再根据实际悬赏等进行修正，不会出现动一艘船其他跟着动」+ 裁决「舰级给绝对值 / 卡上用修正 / 允许混编 / 先试点 / ④按四级 / ⑤精锐词缀可以 / ⑥先不改数值 / ⑦enc-pirate 先保留」，详见 `docs/design/foe-ship-config-pilot-20260911.md`）**——
  ① **新增 `packages/data/src/foe-ships.ts` 舰级表**：A 族四档 = 海盗快艇(brawl) / 劫掠护卫舰(orbit) / 劫掠狙击舰(kite) / 海盗头目舰(elite，预留)；属性一律**绝对值**（血/血层/速度/单发/装填/命中/射程带/衰减/近盲/伤害构成/战术）。
  ② **引擎新增舰级路径**：`AnomalyDef.ships`（编成 = 引用舰级 + 数量 + 波次 + 倍率/覆写），`createFoeSpecs` 分流；**未写 ships 的 20 张卡走旧威胁推导路径，行为逐字不变**；混编与「精锐」词缀实装（原为 2026-09-09 预留位）。
  ③ **6 张 A 族卡转入舰级路径，零难度变化已实测**：改造前后逐单位 **17 字段 + 逐系单发全部逐字一致**（唯一变化 = 舰种名：攻坚重甲舰→海盗快艇、环绕护航舰→劫掠护卫舰、狙击护卫舰→劫掠狙击舰、轻装狙击护卫舰→轻装劫掠狙击舰）；`赤潮` 的 `foeDmgMul 0.27`、`蜃影` 的 `0.30` **随改造退场**（并入舰级单发×倍率）。
  ④ **低安获得海盗族**（船长「我希望低安至少要有一个海盗族。目前都处于非低安区」→ 裁决「直接将星系互换，安全等级也跟着换」+「矿带保留在原位」）：**蜃影星系 0.0 ⇄ 烬火星区 −0.7 互换安全等级**，卡/卡名/星图位置/矿带一律未动 ⇒ 三条通道同时点亮（低安海盗悬赏 + 低安伏击遭遇出现海盗 + 星图「海盗」派系标签）。**连带已修**：两卡 B3.1 特色回收池（危险度档位随 sec 走：烬火 危→常 均价 15.05→11.44、蜃影 →险 32.95→43.10）与主题追加件区划互换（否则烬火的 mk2 成永不掷出的死配置）；⇒ 海盗进低安的收益面 = **那条矿带既会招来海盗、残骸也更值钱**。
  ⑤ **顺带修两处结构缺陷**：删掉 `shipArt.tsx` 22 行硬编码 `FOE_FAMILY`（改读数据侧 `foeFamily`，`演习场靶机` 美术B/数据空的分脑消除）；`烬火42/回音52/天底66/虚海88/穹顶96` 的战术由缺省值改为显式声明。
  ⑥ **新增契约**：content:check「舰级契约」（引用有效/族一致/卡面口径与舰级一致/至少一条主体）与「族→战术契约」（**只登记已裁定的族**，当前仅 A 族 = 鱼龙混杂三战术全允许）；敌速口径契约改造为接受舰级路径。
  ⑧ **头目可配多战术**（船长「头目建议允许多个战术」）：舰级 `tactic` 降级为**默认战术**，编成条目新增 `tactic?` 覆写与 `rangeMinM?`/`rangeMaxM?` **绝对射程覆写**。**口径更正（船长同日「射程和战术仅仅的弱相关，并不实时强绑定」）**：射程与战术**仅弱相关**，不再强绑定——舰级路径下射程是绝对值、不随战术推导，换战术**不必**跟着改射程，射程是**独立旋钮**（旧路径的"战术带×成长"强绑定只留在未写 `ships` 的卡上）；契约放宽为「卡面战术须落在主体**有效战术集合**内」（混编允许狙击头目 + 贴脸杂鱼）；`foe-ship-path` 共 **12 项**测试。
  ⑨ 验证：typecheck ×4 + core **814/814**（新增 `tests/foe-ship-path.test.ts` 9 项，覆盖绝对值口径/混编/精锐词缀/tag 口径/旧路径不变）+ content:check + desktop build 全绿；`content-csv/enemy-roster.csv` 新增「舰级编成」列并修正舰级卡的总血口径（原会落回威胁曲线显示错值）。**改动全在 d2 本地提交，未合入 main（船长：「我的意思是不合并到main」）**【待验收】。
- [✓] **舰种表 · 5 档定案（船长 2026-09-11；二号）**——**①船长原话要点**：舰种**7 档收敛为 5 档**；「**重型巡洋归巡洋舰档、战列巡洋归战列舰档**」（皆为**称号**不是独立档，**子分类本轮不做**）；舰种**基准速度本轮给定** = **护卫舰 340 / 驱逐舰 295 / 巡洋舰 258 / 战列舰 205 / 旗舰 155**（m/s）。**②口径**：档位来源 = **现有 `ShipDef.tier`**（1~5），按**等效质量落档**（= `massKg × (armored ? 0.65 : 1)`；区间 T1 0.4~2.4M / T2 2.4~5.8M / T3 5.8~13M / T4 13~29M / T5 ≥29M —— **沿用仓库 2026-09-09 既有规则，本轮不改任何船的 tier、不重切区间、不改任何数值**）；这是**敌我共用**的基准（玩家的船与敌人的舰级归入同一套舰种、参考同一基准），**实际速度 = 基准 × 倍率**（倍率按舰/按族给，**本批暂未落地**）；基准速度的来由 = 各档**武装舰基线**。**③新增 `packages/data/src/hullClass.ts`**：`HULL_CLASS_NAME`（护卫舰/驱逐舰/巡洋舰/战列舰/旗舰）、`HULL_CLASS_BASE_SPEED`（340/295/258/205/155）、`HULL_CLASS_MASS_RANGE`（T5 上界 `Infinity`）、`equivalentMassOf()`、`hullClassTierOfTier()`（越界兜底到最近合法档）、`hullClassOf()`（按 tier 取名），并自 `packages/data/src/index.ts` 对外导出。**④新增常驻护栏 = `content:check`「舰种契约」**：①每艘船的 `tier` 必须与其**等效质量落档**一致（不一致即报错并点名船名/质量/等效质量/实际 tier/应为 tier）；②每档**恰好**一档命名 + 一个基准速度，且基准速度落在 100~500；③打印一行归类统计。**⑤本批零行为变化**：表只建在数据层，**未接线到任何引擎读取点**，敌我速度一律照旧。**⑥验证**：typecheck ×4 全绿 + core **824/824**（84 文件，新增 `tests/hull-class.test.ts` 7 项）+ content:check 全绿——**归类实测：25/25 艘船全数一致**（护卫舰 5 / 驱逐舰 7 / 巡洋舰 9 / 战列舰 3 / 旗舰 1），**无错标船**、无需船长裁决 tier；**负向验证**——临时把陆龟级 `tier 2→3` → 契约如实失败并点名（「陆龟级重装艇（sh-tortoise）质量 6000000 → 等效质量 3900000，实际 tier 3，应为 tier 2」），还原后全绿。**⑦文档**：`docs/glossary.md` 第八节新增「舰种（质量分级·敌我共用）」与「舰种基准速度」两条。**⑧留待下一批（未授权，勿自行开工）**：倍率落地（实际速度 = 基准 × 倍率）、子分类（型号称号）与 UI 接线。【待验收 · 未合入 main】
- [✓] **敌舰级速度改「舰种基准 × 倍率」+ 舰种档登记（船长 2026-09-11；二号）**——**①船长原话（本批依据）**：「**海盗应该是护卫驱逐巡洋构成，战列级强力+维护成本大，不适合海盗的背景设定**」+ 同日追加裁决「**劫掠护卫舰和劫掠狙击舰下落一档，只有头目是巡洋舰**」（上一批「舰种表」留待的**倍率落地**，本批在**敌人侧**落地）。**②`FoeShipDef` 新增必填 `hullClassTier`**（字面量联合 `1|2|3|4|5`；注释写明**须与 data 包 `hullClass.ts` 的 `HULL_CLASS_NAME` 档位一致**——core 不能反向 import data，故不引 data 的类型）；**③速度字段改造**：原 `speedMps`（绝对值）→ **`speedRatio`（倍率）**，注释写清「= 实际速度 ÷ 舰种基准速度；实际速度 = `HULL_CLASS_BASE_SPEED[舰种档] × 本倍率`（编成条目还有 `speedMul` 时再乘）**后取整**」，四个舰级一律**精确分数、不写小数**。**④四舰级登记表**：| 舰级 | 舰种档 | 倍率 | 实速 | —— 海盗快艇 **T1 护卫舰** `351/340` → **351**；劫掠护卫舰 **T1 护卫舰** `291/340` → **291**；劫掠狙击舰 **T2 驱逐舰** `201/295` → **201**；海盗头目舰 **T3 巡洋舰** `377/258` → **377**（头目 = 海盗舰队顶格，对应「只有头目是巡洋舰」）。**⑤core 如何拿到舰种基准速度**：**最小侵入 = 基准速度表进 `BattleBalance`**（新增 `hullClassBaseSpeedMps`，默认值落在 core `balance.ts` 自称的"唯一调参处"；引擎在 `createFoeSpecsFromShips` 随已有的 `bal` 参数直接读，**零签名改动、零调用点改动**）；data 包 `hullClass.ts` 的 `HULL_CLASS_BASE_SPEED` **直接读该字段（同源引用，不再存第二份字面量）**——两包各写一份调参数字会静默漂移（本仓已有三次"全局常量联动"事故）。**⑥契约（`content:check`「舰级契约」追加两条断言，编号 ⑤）**：(a) 每个敌舰级的 `hullClassTier` 必须落在 **1~5**；(b) **海盗族（family 'A'）不得登记 4 战列舰 / 5 旗舰档**，报错文案 = 「**海盗不配战列级（维护成本大，不符合海盗背景设定）**」（附海盗舰队只用 1/2/3 三档）。**⑦改名取消**：原规格曾拟把 `FOE_SHIP_PIRATE_CORVETTE` 改名「劫掠驱逐舰」，**遵船长追加裁决取消**——它现在是**护卫舰档、名与档一致**，名字保持「**劫掠护卫舰**」不变，全仓无任何引用改动。**⑧零变化铁证**：以 `_` 探针（跑完即删，`_` 探针 0 个）在**改造前后各导一次**舰级路径实建速度——**①四个舰级 351 / 291 / 201 / 377 逐字相同**；②引用舰级的 6 张卡**逐单位速度也逐字相同**（含两条 `speedMul` 卡：`ano-redring-raiders` 204、`ano-shard-bandits` 377、`ano-mirage-hijackers` 235/235）；③两份导出**20 行全等**（`Compare-Object` 无差异）。**⑨验证**：`npm run typecheck` **×4 全绿**（core / data / ui / desktop）+ `npm run test -w @whale/core` **85 文件 / 827 用例全绿**（基线 824 → **+3** = 新增 `tests/foe-ship-path.test.ts`「舰种档与速度倍率」三条常驻用例：四舰级实速 = 基准 × 倍率且逐字等于 351/291/201/377、`speedMul` 乘后取整口径、舰种档 1~5 与"海盗不配战列级"；文件数不变）+ `npm run content:check` 全绿（「舰级契约：6 张舰级路径卡（8 条编成，其中混编 0 张）引用有效、族与卡面口径一致；舰种档 4 个舰级全部落在 1~5，海盗族（A）无 4 战列舰 / 5 旗舰档（海盗不配战列级）」；「舰种契约：25/25 艘船归类与等效质量一致（护卫舰 5 / 驱逐舰 7 / 巡洋舰 9 / 战列舰 3 / 旗舰 1）；基准速度 340/295/258/205/155」）。**⑩负向验证**（两条新断言都真会拦）：临时把海盗快艇登记为 T4 → 契约如实失败并点名「海盗族舰级「海盗快艇」（foe-pirate-skiff）登记了 战列舰（T4）档——**海盗不配战列级（维护成本大，不符合海盗背景设定）**」（同时连带触发敌速口径契约 0.72× 越界）；临时把劫掠狙击舰档写成 6 → 「舰种档 6 越界（须落在 1~5…）」；还原后全绿。**⑪文档**：`docs/glossary.md` 更新「舰种基准速度」（补「**敌人的舰级也登记舰种档，敌速 = 舰种基准 × 倍率**（倍率按舰级给定）」与数值唯一出处）、「敌舰配置表（舰级表）」（速度例外）并新增「**海盗族舰队构成（A 族·不配战列级）**」词条。【待验收 · 未合入 main】
- [✓] **单波次内增援（⚙ 已实现 · 未启用）（船长 2026-09-11 裁决「先完成相应的系统机制，不使用。用作后续机制。」；二号）**——**①形态**：与先例「**敌突进**」（`foeChargeEnabled`，船长 2026-09-10「暂时先取消实装，仅实现功能」）**同款**——机制/参数/总开关/契约/用例全部就位，**任何战斗都不触发**。**②机制**：编成条目 `FoeShipSlot.enterAt`（新类型 `FoeReinforceTrigger`）给**三种入场触发（任一满足即入场）**：`sec` 开战满 N 秒（战斗时钟口径）/ `afterKills` 本场已击毁敌单位数 ≥ N / `hpBelow` **己方（本波敌方编成）**存活剩余总血比例 ≤ N（"残部呼救"；要"玩家残血才来援"须另加字段，**不改本语义**）；写了的单位**开战不进战场**，由 `advanceBattleFor` **每拍**检查、命中才 `seedUnit` 入场（`enterReload` ＝一次装填的哑火窗口，与波次增援同款）。**未写 / 条件全无效 = 开战即在（= 现状，逐字不变）**——刻意不产生"永不入场"的沉默副作用（登记 NULL→写空对象也是"开战即在"）。**③总开关**：`BattleBalance.foeReinforceEnabled` **默认 false**（注释引船长原话）；**建档期即按开关过滤**——关着就**根本不写** `UnitSpec.foeReinforceAt`（与 `foeCanCharge` 同款形态，保证"关了＝零行为变化"）。**④距离重开**：`foeReinforceReopenFrac`（语义**完全沿用 `waveReopenFrac`**；**缺省 0 = 原地入场不重开**）。**⑤存档零迁移（红线）**：**不新增任何存档字段**——"是否已入场"**由 `battle.units` 里有没有该 tag 反推**（`seedUnit` 不覆盖已存在 tag、尸体也留着 ⇒ "在场过"永远是"存在"）；读档续战用同一份编成重新推导。**⑥判胜口径**：**未到触发点的增援不参与判胜**（在场者全灭即胜＝"援军来不及赶到"是合法结局）；替代方案（有未到场援军就不结束）会让 `afterKills` 类卡无限拖到 10 分钟上限且要改判胜逻辑，**未做**。**⑦仅舰级路径**：`enterAt` 长在 `FoeShipSlot` 上，旧威胁推导路径不受影响（有专门用例）。**⑧实现（最小侵入）**：`createBattleState` **零签名改动**（靠 `UnitSpec` 标记跳过未入场者）；`combat.ts` 新增 `normReinforceTrigger` / `reinforceTriggered` / `foeKillCount` / `foeResidualHpFrac` / `resolveReinforcements`，主循环在 `stepBattle` **之前**结算。**⚠ 抓到一个洞并修掉**：`advanceBattleFor` 开头的"读档补缺"seed 循环会把**还没该到的援军直接塞进战场**（三条触发用例首次全红）→ 该循环同样跳过带触发者。**⑨界面提示复用现有机制**：战报日志（同波次转场风格的 `addLog`）+ 入场单位自动出现在敌舰名录/血条/武器弧；**刻意不新增界面标记字段**（那要占新存档字段，与⑤红线冲突）。**⑩契约与守卫**：`content:check` 新增「**增援机制未启用契约**」——开关关闭期间**任何卡写 `enterAt` 即失败**，文案写明"机制已实现但按船长裁决不启用"＋启用流程（让"未使用"**由代码守住**）；**双向负向验证**：关＋写 ⇒ 如实失败并点名；开＋写 ⇒ 通过（启用路径通）。**⑪用例**：新增 `packages/core/tests/foe-reinforce.test.ts` **13 项**（直接构造、**不依赖任何真实卡使用**）：关＝逐字零变化（建档 `toEqual` 全等 + 战斗中行为完全等价 + `createBattleState` 不漏单位）/ 三种触发各自按时入场（含"本拍即触发"）/ 坐标触发一起到场且不重复补入 / 距离重开 0 与 1 两取值（1 = 精确拉回开战距离）/ 条件全无效＝开战即在 / 未到点不参与判胜 / 旧路径隔离 / 读档零迁移。**⑫⚠ 与「多舰补偿系数 `2N/(N+1)`」的关系（已写进设计稿）**：两者解决**同一问题**但**不可叠加**——补偿系数是**数值层补偿**（把逐个被击毁的阶梯衰减折算进单发），本机制是**结构解法**（分批到场 ⇒ 衰减本身不成立）；**若将来启用增援，补偿系数必须下调或退场**，否则**双倍补偿、实际火力明显超标**（启用清单第 4 步即此条）。**⑬验证**：`npm run typecheck` **×4 全绿** + `npm run test -w @whale/core` **86 文件 / 844 用例全绿**（831 → **+13**，文件 85 → 86，差值来源＝新增本机制用例文件）+ `npm run content:check` 全绿（含新契约「增援机制未启用契约：总开关 **关**，0 张卡携带 `enterAt`」）+ `npm run build -w @whale/desktop` 全绿；**零行为变化证据**＝`battle:calibrate --csv` 全 26 卡与改动前**逐行逐字一致**（`Compare-Object` 无差异）。**⑭文档**：`docs/glossary.md` 新增词条「**单波次内增援（⚙ 已实现未启用）**」（照「敌突进」写法）；新增 `docs/design/foe-reinforce-20260911.md`（标题下标状态**已实现 · 未启用（船长裁决）**，含机制/三种触发/开关/距离重开/存档零迁移/判胜口径/**与多舰补偿不可叠加**/启用清单五步）。**本批与「A 族数值落地批」是两个独立批次**（互不依赖、可分别验收）。**改动全在 d2 工作树，未 commit、未合入 main、未推送。****【待验收 · 未合入 main】**
- [✓] **修正 · 赤潮/蜃影 火力重锚（船长 2026-09-11 裁决 = 方案 B；二号）**——**①问题**：A 族数值批③ 的公式「名义总 DPS = 威胁 × `foeDpsPerThreat` × 1.6」对**赤潮 / 蜃影**失效——这两张卡旧带**逐卡伤害压制倍率**（旧 `foeDmgMul` **0.27 / 0.30**），而**基线「总DPS」列从来不是它们的实际火力**（实际/名义 = 0.27 / 0.30，其余四卡 = 0.62）⇒ 按"名义列 ×1.6"重锚单发 = **把压制无声取消**（`×1.6÷0.27≈×5.9`、`÷0.30≈×5.3`，与实测 5.97 / 5.35 逐项吻合），实际火力 7.25→43.25、11.50→61.50 ⇒ **蜃影 100%/49s/残血 50% → 20%/35s/残血 1%**（从"可打"变"打不过"）。**船长定性：这是公式错误，不是执行错误。****②裁决与修法**：方案 B（**锚回原难度位置**），但 **禁止恢复 `foeDmgMul`**（该字段已按船长同日裁决「先移除所有 `foeDmgMul`，按照实际算」整体退休，类型上已删除）⇒ **改用卡上 `dmgMul` 重锚，锚点 = 「改造前的实际火力 × 1.6」**（＝「按照实际算」的落地）：赤潮 目标 7.25×1.6 = **11.60**（缩放 11.60÷43.52 ≈ **0.2665**）→ 重锚单发 头目 **28** / 每杂鱼 **6**（Σ46，纸面火力 **11.50**，−0.86%）；蜃影 目标 11.50×1.6 = **18.40**（缩放 18.40÷61.44 ≈ **0.2995**）→ 头目 **44** / 每杂鱼 **10**（Σ74，纸面 **18.50**，+0.54%）。分布仍按头目 60% / 每杂鱼 40%÷3，取整偏差 **0 / 0**；**只改卡上 `dmgMul`**（`anomalies.ts`），**舰级表/血量/编成/速度/射程一律未动**；**六张卡纸面火力全部落在设计值 ×1.6 的 ±1% 内**（×1.59/1.58/1.62/1.58/1.586/1.609）。**③修正后实测**：赤潮 100%/50s/**100%**、蜃影 **100%**/53s/**89%**（胜率双双回归 100% ✓）。**④⚠ 残留偏差（按指令停下报告、未试凑）**：残血未落到锚点读数（赤潮目标 ~88% 实测 100%；蜃影目标 ~50% 实测 89%）——**根因经探针实测证实 = kite 卡的头目根本不开火**（三张 kite 卡终局交战距离 **3312 / 3809 / 3238 m**，头目射程带只有 **1~2210 m** ⇒ 头目 `foe-0` 开火次数 **全为 0**；三艘杂鱼各 9~15 次）⇒"纸面 60% 火力"完全没落地、两卡实战反而比改动前更松（落地火力 4.5 / 7.5 < 改动前 7.25 / 11.50）。此即**已上报待裁的「kite 卡头目仅 2.21 km 射程」**同源问题（§六 第 3 条），**不在本批授权范围，未动**；给船长三条路：现状保留 / 给这两卡头目条目写射程覆写（火力真落地，届时按同锚点微调）/ 改成"可落地火力 ×1.6"锚点（新口径）。**⑤登记独立待办「名义火力 / 实际火力脱节」**（**全 26 张卡的老问题，不只 A 族**，本批不解决）：名义列（威胁 × `foeDpsPerThreat`）与卡上绝对值推导的实际火力之间**没有任何守卫**，历史压制补丁退出后名义列可能整片失真而无契约/用例报警（本次靠实测才抓到）；建议单独立项：`content:check` 加"名义/实际火力一致性契约"或在 `battle-calibrate --csv` 增「纸面火力/实际火力/比值」三列。**⑥契约与文档**：本次**未新增契约**（属修正，不属新口径）；`docs/design/foe-faction-a-numbers-20260911.md` 头部加**修正记录**、§一 1.1 表两行改为锚定值（11.60 / 18.40，单发 28/6、44/10）、§三 全部改为"改动前 / 初版公式 / **修正后**"三列并重写 §3.3（根因、船长定性、修法与锚点、修正后实测、探针实测表、三条待裁路径 + 独立待办方框）、§六 待办重编号。**⑦验证**：`npm run typecheck` **×4 全绿** + `npm run test -w @whale/core` **86 文件 / 847 用例全绿**（重锚守卫 +3） + `npm run content:check` 全绿（六卡仍在「舰级契约 ⑥a/⑥b」与「敌速口径契约」内，未触碰） + `npm run build -w @whale/desktop` 全绿；重锚回归守卫 = `tests/foe-ship-path.test.ts`「赤潮 / 蜃影 火力重锚」3 条（锁死重锚单发 + 纸面火力 ≈ 旧实伤 ×1.6）。**⑧其余 7 点按船长裁决执行**：改契约口径两点**获认可**（保留）；`combatSeconds: 20` 未动（船长报给船长定夺）；父目录三个 `_` 文件**保留**（上一批零变化铁证）；头目血型/僚机语义/kite 头目射程/窝点派生四点**一律未动**（船长已上报等裁决）。**改动全在 d2 工作树，未 commit、未合入 main、未推送。****【待验收 · 未合入 main】**
- [✓] **A 族数值落地（船长 2026-09-11 确认「先按照你的提议实现」；二号）**——**①船长确认值（四件事同时动）**：设定 = **鱼龙混杂 / 装备较差数值偏低 / 靠数量弥补 / 一个头目强大带一堆杂鱼 / 速度都快（方便突袭）**。**②编成 = 头目舰 ×1 + 杂鱼 ×3**（六张 A 族卡统一为 `FOE_SHIP_PIRATE_WARLORD` ×1 + **本卡原有舰级** ×3，共 **4** 单位；灰霾/蜃影原 `escort: true` 僚机条目按船长 ① **取消**）。**③血量总血守恒**：头目 **60%**、每杂鱼 **40%÷3**，用 `hpMul` 精确分数式表达（如 `(150*3/5)/360`、`(150*2/15)/150`）——六张卡 Σ血**逐字守恒**（150/285/365/430/340/560）。**④名义 DPS 守恒 + 多舰船补偿 `2N/(N+1)`**：总名义 DPS = `威胁 × foeDpsPerThreat(0.8) × 1.6`（N=4）；头目 60% / 每杂鱼 13.3333%；单发仍按既有推导（固定系 `×4s × 0.62 ÷ 有效命中`、光束不消费命中补偿）；**取整用"最后一条吃余数"兜底**，逐卡偏差 0/0/+1/0/−1/0（**全部 ≤1**）。数学：N 个单位逐个被击毁 ⇒ 整场累计输出只有账面的 `(N+1)/(2N)`（**N=4 只剩 62.5%**），本系数正好抵消。**⑤实现（最小侵入）**：`core/combat.ts` 新增 `foeMultiShipCompMul(anomaly)`，在 `createFoeSpecsFromShips` 建档时 `shotDmg = round(舰级单发 × dmgMul × 补偿)`——**零签名改动、零调用点改动**；卡上 `dmgMul` 写"设计单发 ÷（舰级单发 × 1.6）"。⚠ **旧威胁推导路径一律不动**（N=1 天然为 1）→「**旧路径暂未启用该补偿**」= 已知口径不一致，待旧卡迁入舰级路径时统一（已写进词条）。**⑥速度改 `speedRatio`（每档都高于本档舰种基准）**：快艇 `1.15`→**391**、劫掠护卫舰 `1.10`→**374**、劫掠狙击舰 `1.10`→**325**、头目舰 `1.45`→**374**（卡上原有 `speedMul` 变体倍率保留：碎晶带快艇 420 / 赤潮 329 / 蜃影 379）。**⑦射程 A 族统一 −15%**（设定：缴获改装的实弹）：快艇 **1~1883**、护卫舰 **326~4275**、狙击舰 **1255~9619**、头目舰 **1~2210**——四舰级同比例、**卡上不另写覆写**（头目与杂鱼同族同比例）。**⑧战术保持各卡现状**：头目缺省 brawl → **4 张卡用编成条目 `tactic` 覆写**（信标 orbit；灰霾/赤潮/蜃影 kite），并覆写 `dmgMix` 使头目与杂鱼同构成（边境/灰霾 爆炸、赤潮/蜃影 能量）。**⑨契约与文档**：`content:check`「舰级契约」新增 **⑥a**（A 族编成必须是「头目 ×1 + 同族同型杂鱼 ×3」共 4 单位）与 **⑥b**（**头目血量占比 60% ±1%**，按编成实算），报错文案写明依据；**③ 主体定义修订**为"单位数最多的非僚机条目"（混编下卡面口径讲的是那 3 艘杂鱼，原 `mains[0]` 会把头目当主体而误报 4 张卡）；「敌速口径契约」新增 **A 族例外**（旧"战术比率带"kite 0.60~0.85 与「A 族速度都快」直接冲突 → A 族改按"实速 > 本档基准 + 全族提速带 1.00~1.60×"校验）；`docs/glossary.md` 新增「**多舰船补偿系数 `2N/(N+1)`**」与「A 族编成口径」词条并更新「混编」「舰种基准速度」；`tools/battle-calibrate.ts` 的舰级路径读数列改**逐舰级**（单取 `specs[0]` 会静默只报头目）、`总DPS` 列改为含补偿；新增设计稿 `docs/design/foe-faction-a-numbers-20260911.md`。**⑩实测对照（本批核心）**：边境 100%/7s/100% → **100%/23s/100%**；碎晶 100%/11s/100% → **100%/25s/100%**；信标 100%/10s/100% → **100%/18s/100%**；灰霾 100%/34s/96% → **100%/45s/100%**；赤潮 100%/34s/88% → **100%/50s/30%**；蜃影 100%/49s/50% → **20%/35s/1%**。实际期望火力 ×1.59/×1.58/×1.62/×1.58（= 设计的 ×1.6 ✓）与 **×5.97/×5.35**（⚠ 赤潮/蜃影）。**⑪⚠ 如实报告的失控项（未自行回调）**：赤潮/蜃影原带**逐卡伤害压制倍率 `foeDmgMul` 0.27/0.30**（试点期被折进舰级绝对单发而"藏"了起来），而「基线总DPS 列」对这两张卡**从来不是实际火力**（实际/名义 = 0.27 / 0.30，其余四卡 = 0.62）；本批公式按"名义列 ×1.6"重锚单发 ⇒ **压制被无声取消**，`×1.6÷0.27 ≈ ×5.9`、`×1.6÷0.30 ≈ ×5.3` 与实测逐项吻合 ⇒ 蜃影（T48）新名义 DPS 61.4 已是中段最高、难度≈E 段墙（T45 深渊守卫 40%、T88+ 三张墙 0%）。**判断：本批口径是"名义守恒 + ×1.6"，这两张卡实际拿了 ×5.3~6.0、非设计目标**；建议 (A) 认下"硬卡"并同步上抬威胁/奖金/展示时长，或 (B) 恢复"逐卡压制/校准倍率口"把实伤锚回"旧实伤 ×1.6"（倾向 B，但该字段系船长 2026-09-11 另一条裁决退休，**恢复需船长点头，故不动手**）——**待船长裁决**。另记三条已知口径：头目那份血量不随卡面血型走（头目舰是固定装甲型、`FoeShipSlot` 无 `split` 覆写位）、僚机语义取消后杂鱼不再挂「轻装」、kite 卡的头目只有 2.21 km 射程（按船长 ⑤"只改舰级值、不另写覆写"）；**第四条（探针实测）**：**A 族窝点派生卡与主题卡同数值**（`lairAnomalyOf` 继承 `ships`，而舰级路径不读威胁/`hpShare`/`escorts` ⇒ 威胁被放大到 T96 也**不涨血不涨火力**，tier2/3 的多波表更**刷出 0 单位**、演出塌成一波）——试点批起就存在，六张卡 tier1/2/3 全遍历实测，**修法三种皆属新口径，待船长裁决**。**⑫验证**：`npm run typecheck` **×4 全绿** + `npm run test -w @whale/core` **85 文件 / 831 用例全绿**（基线 827 → **+4**：新增「A 族每档实速都高于本档舰种基准」1 条 + 「多舰船补偿」3 条；另按新口径**改写** 2 条旧断言：速度零变化守卫 351/291/201/377 → 提速读数 391/374/325/374、混编单发 60/10/6 → 100/17/10）+ `npm run content:check` 全绿（「舰级契约：6 张舰级路径卡（12 条编成，其中混编 6 张）…A 族编成契约 6 张：头目 ×1 + 同族杂鱼 ×3（共 4 单位）、头目血量 60% ±1%」）+ `npm run build -w @whale/desktop` 全绿；**旧路径零回归**：`battle-calibrate --csv` 非 A 族 **20 张卡 20 行逐字一致**（`Compare-Object` 无差异）；`_` 探针收尾已删（**0 个**）。**改动全在 d2 工作树，未 commit、未合入 main、未推送。****【待验收 · 未合入 main】**
- [✓] **A 族收口修正（船长 2026-09-11 四裁决；二号）**——**①船长原话**：①「**头目血型随卡片走**，其实我们刚刚忘记讨论种族血型了，不过问题不大，**海盗设定鱼龙混杂，那么就什么血型都有**」②「**不保留僚机**」③「我记得之前说过，**头目可以多种战术选择，因此射程方案也是多重**」④「**按甲处理**」。**②血型随卡走**：`FoeShipSlot` 新增 `split?`（缺省走舰级），引擎 **有效 split = 条目覆写 ?? 舰级**；头目舰本体是装甲型 ⇒ **卡面非装甲的四张**（信标 均衡 / 灰霾·赤潮·蜃影 护盾）在头目条目用 `foeLayerSplit(卡面 defProfile)` 覆写（**不手抄数字**），边境/碎晶（装甲）无需覆写；契约改为**逐条主体按有效值**判定血型与构成（只判"主体舰级"会漏掉头目写错——负向验证踩到过，故收紧），报错文案写明「头目血型随卡片走；若要配别的血型请写 `split` 覆写使其与卡面对齐」；**A 族不做族级血型约束**（鱼龙混杂 ⇒ 什么血型都有：六卡 = 装甲×2 / 均衡×1 / 护盾×3）。**③不保留僚机**：代码无需再改（数值批已取消灰霾/蜃影的 escort 条目，三艘杂鱼为主体、不挂「轻装」）；全仓核对 `escort: true` **已无任何真卡使用**（仅测试合成卡用到）；字段**保留**并注释「当前无卡使用；船长 2026-09-11『不保留僚机』」。**④头目射程多重方案（本批最关键）**：探针实测**六张卡的头目 `foe-0` 原先一次都不开火**（射程带 1~2210m vs 实战交战距离 3.2~3.8km）⇒ **四张**（信标/灰霾/赤潮/蜃影）在头目条目写 `rangeMinM`/`rangeMaxM` = **本卡杂鱼的有效射程带**（同卡同带；赤潮/蜃影 用与杂鱼同一个变体倍率常量推导，不手抄数字），覆写后头目开火 **4 / 13 / 13 / 10 次**（杂鱼 1~15 次）；⚠ **边境/碎晶不写**（头目 0 次但**整队 4 艘全 0**——原因是玩家 21~23 秒就把 150/285 血打完、敌人从未进入接触窗口；写覆写只会把 2210m 缩成 1883/1932m、**更打不到**），**此偏离已如实报请船长确认**。覆写后按上批同口径复核锚点（**锚点 = 改造前实际火力 ×1.6**，`foeDmgMul` 仍禁止恢复）——四卡纸面值本就落在锚点 ±1%（信标 ×1.62 / 灰霾 ×1.58 / 赤潮 ×1.586 / 蜃影 ×1.609），**本批未再改 `dmgMul`**。**⑤六卡最终实测**：边境 100%/23s/100% ✓、碎晶 100%/25s/100% ✓、信标 100%/29s/99% ✓、灰霾 100%/47s/89% ✓、赤潮 100%/47s/64% ✓、**蜃影 60%/47s/15% ❌ 未达 100%**。**⑥⚠ 蜃影未达标 = 按指令停下报告、未试凑**：锚点「旧实伤 ×1.6」是**纸面锚**，实收还要过阶梯衰减（4 单位 → **实收积分 = 纸面 × 0.625**）；**改动前蜃影只有 2 单位（衰减 0.75）** ⇒ 覆写让火力落地后**实收积分 ≈ 改动前的 ×1.33**（4 张固定系卡改动前 1 单位 → ×1.6×0.625 = **×1.0**，故它们残血几乎不动 ✓）。三条路待裁：**(1)** 该两卡（或仅蜃影）锚点改「实收积分 ≈ 改动前」= 纸面 ×`0.75/0.625` = **×1.2**（赤潮 8.70 / 蜃影 13.80）；**(2)** 保留 ×1.6 锚点、承认硬卡并上抬威胁/奖金或下调总血；**(3)** 只收窄蜃影头目射程（试凑，不建议）。附风险观察：低技能行（6 项 L3）探针里**灰霾打到 600s 硬上限未分胜负**（头目拿远战带后敌人期望交距被推远，低装备玩家可能够不着；中位技能行无此问题）——建议上线前补"无技能/低技能"对照。**⑦窝点派生按档缩放（甲）**：`lairAnomalyOf` 对舰级路径卡按「**原威胁 → 派生威胁**」的比例**同乘每个条目的 `hpMul` 与 `dmgMul`**（血与火力一起抬；单发/射程/编成/战术不动）；**舰级路径派生卡不套 `LAIR_WAVES`**（旧波表按"每波几队"描述旧路径编队，套了会**刷出 0 单位**——探针实测）⇒ 维持**单波**、每波 ≥1 单位；旧路径三档波次（单波/双波/三波）与僚机加成**逐字不变**。**实测对照**：赤潮 主题 T34 血 340 / 火力 11.50 → tier1 T44 **血 440 / 15.00**、tier2 T54 **血 540 / 18.50**、tier3 T68 **血 680 / 23.00**；蜃影 主题 T48 血 560 / 18.50 → tier1 T62 **血 723 / 24.00**、tier2 T77 **血 898 / 29.75**、tier3 T96 **血 1120 / 37.00** ⇒「**威胁 = 战力标尺**」在舰级路径下重新成立（此前 A 族窝点"标 T96、数值还是主题卡的"）。**⑧契约**：新增「**窝点派生契约**」（①每波单位数 ≥1 ②舰级路径派生卡总血与火力代理值**随 tier 严格递增**且首档高于主题卡；**负向验证：摘掉缩放 ⇒ 36 处错误**并点名卡与档位）；「舰级契约」血型/构成判定改**逐条主体按有效值**（负向验证：把蜃影头目 split 写错 ⇒ 如实失败并点名）。**⑨用例**：新增 `packages/core/tests/foe-lair-scale.test.ts` **5 项**（缩放比例逐条目核对 / 编成与 60% 头目血占比与战术与射程覆写继承 / 舰级路径维持单波且第 2 波不刷空 / 旧路径波次逐字不变）+ `foe-ship-path.test.ts` 扩 **4 项**（`split` 覆写生效 / 六张真卡 24 个单位的有效血型都等于卡面 / 头目覆写分布 / 打不到的头目同卡同带、近战两张不写）。**⑩验证**：`npm run typecheck` **×4 全绿** + `npm run test -w @whale/core` **87 文件 / 856 用例全绿**（847 → **+9**：新增 `foe-lair-scale.test.ts` 5 项 + `foe-ship-path.test.ts` 4 项）+ `npm run content:check` 全绿（含两处新/改契约，均已双向负向验证）+ `npm run build -w @whale/desktop` 全绿。**⑪文档**：`docs/glossary.md` 新增「**血型随卡走（`split` 覆写）**」「**头目射程多重方案**」「**窝点派生按档缩放**」三条、更新「编成条目」（`split` + escort 现状）与「头目多战术」（射程随之多重）；`docs/design/foe-faction-a-numbers-20260911.md` 新增 **§七 收口修正**（四裁决逐条 + 头目开火对照表 + 六卡最终实测 + 蜃影未达标分析与三条路 + 窝点派生对照表）。⚠ 未自行决定项：**边境/碎晶是否也强制写射程覆写**、**蜃影锚点三条路**、**低技能行够不着风险**——均待船长裁决。**改动全在 d2 工作树，未 commit、未合入 main、未推送。****【待验收 · 未合入 main】**
- [✓] **B 族落码批 · 武装拾荒者（船长 2026-09-11 七裁决；二号）**——**①船长原话（本批依据）**：「关于B组，**统一为武装拾荒者，移除靶机**。**确认为新手过渡种族**。**演习场讨伐令改为演习场驱逐令**，驱逐到演习场拾荒的拾荒者。**战术性格统一为 orbit**。**乱射不动**。**速度偏慢**。**舰种按照你推荐的来**。」**②族名与文案**：`ano-training` 改名「**演习场驱逐令**」、文案改写为"协会例行清场令：拾荒船常年在演习场边缘翻捡演习残骸，协会按次悬赏驱逐"（**新手第一课**）；`lairCore` 保持留空（教学卡不作窝点，不变）；全仓"靶机"按**清零口径**处理（见⑩）。**③战术统一 orbit**：演习场/新港 由 brawl 改 **orbit**（占港原即 orbit）；契约「族→战术契约」登记 **B = `['orbit']`**（照 A 族同款）。**④乱射不动**：占港命中 **0.55 保持**，但**从卡级字段移到编成条目** `hitRate`（**乱射是单卡特征、不是族级签名**：舰级一律 0.85）；另两张保持 0.85。**⑤迁移入舰级路径**：新增 **2 个 B 族舰级**——`foe-scav-skiff` **拾荒武装艇**（T1 护卫舰，服务 演习场6/新港10）、`foe-scav-armed` **拾荒火力舰**（T2 驱逐舰，服务 占港16），两者默认战术 orbit、`speedRatio` 均 **< 1**（**速度偏慢**：`0.90`→**306**、`0.92`→**271**；旧绝对速 322 / 337 / 286）；基准取值 = 同档**威胁最低那张卡**的现状建档值（拾荒武装艇 ← 演习场 T6、拾荒火力舰 ← 占港 T16），同档其余卡用精确倍率复现（新港 `hpMul 75/22`、`dmgMul 23/14`，并覆写 `split`（装甲）与 `dmgMix`（8:2））。**⑥零变化锚定（探针铁证）**：改造前后各导一次**逐单位建档值**（24 字段 × 3 行）逐项比对——**仅 11 项差异且全部有意**：`name`×3（舰种名→拾荒两舰级）、`speedMps`×3（**船长"速度偏慢"**）、`tactic`/`foeTactic`×2（**船长"统一 orbit"**）、`cardName`×1（改名）；其余 **21 字段 × 3 行逐字一致**（血/单发/逐系单发/命中/射程带/衰减/近盲/装填/形态/主系/规格/回避/敏捷）。**⑦契约（两处）**：「舰级契约」新增 **B 族档位 < 3**（依据「**确认为新手过渡种族**」+拾荒者小艇；负向验证：把拾荒武装艇临时写成 T3 ⇒ 如实失败并点名，还原后全绿）；「族→战术契约」加 **B = orbit**。**⑧实测（标定参考行 S2 中位技能）**：演习场 100%/7s/100% → **100%/7s/100%**、新港 100%/7s/100% → **100%/7s/100%**、占港 100%/10s/100% → **100%/10s/100%**（**零漂移**）；**期望交距读数**由 brawl 的 441m → **1210m**（orbit 口径生效）。**⑨⚠⚠ P0 如实报告（未自行改口径）**：**战术改判在"新手口径"下把教学卡打死**——裸船零技能（舰船自带**基础舰炮 单发10/射程2500m**）对照：演习场 **orbit 0%/600s（打满硬上限判负）** vs brawl 100%/23s、新港 **orbit 0%/600s** vs brawl 100%/72s、占港 orbit 100%/463s vs brawl 100%/398s。**根因**：`foeDesiredRange()` 给敌人算期望交距用的是**旧路径的全局战术表**（`TACTIC_RANGE[战术] × tacticDesireFactor`），**不看舰级自己的射程带** ⇒ orbit 下敌人想站 ~2.7~3.5km，而拾荒艇自带带只有 1~2200m、新手基础舰炮 2500m ⇒ **距离锁死 2671m、双方全程 0 次开火、600s 判负**（探针逐秒轨迹为证）。这把 A 族收口批"打不到的头目"同类问题推到致命档位（A = 缺 60% 火力，B 教学卡 = **双方完全打不到**）。**三条路待裁**：(1) 舰级路径的期望交距改由**自己的射程带**供给（最对症，但会改变 A 族 6 张的距离行为、需重跑 9 张舰级路径卡实测）；(2) 两张卡改回 brawl（等于不落地 orbit 裁决）；(3) 只写给敌人更宽的射程覆写（治标，玩家仍打不到）。**「裸船零技能可过」这条教学卡口径在 orbit 下已失效，建议优先裁决。**⑩文档：`docs/glossary.md` 新增「**武装拾荒者（B 族）**」「**乱射（单卡特征）**」两条；新增设定卡 `docs/design/foe-faction-b-scavenger-20260911.md`（标**已确认**）；`foe-faction-identity-20260910.md` B 族段由"草案/待拍板"改为**已定案**（并注明旧版两条待拍板均被七裁决取代、"乱射升格族级"被否决）；`faction-tactic` 族谱表、`ship-battle-art/*`（README/验收清单/挂点表）、`test-saves/README`、`make-test-save` 说明、公告待审稿的 B 族命名同步改写。**⑪"靶机"清零口径**：**清零**所有"现状/现行资产/玩家可见/工具说明"处；**保留**历史记录里作为"当时旧名/被修缺陷"的引述（roadmap 历史条目、两张试点/结构设计稿的取证行、`docs/review/A1-bounty-review.md` 当时的文案快照）——改写它们等于篡改历史。另 `脱靶`/`近防炮选靶`/`playthrough-sim` 的"战力基准靶"是**不同词义**，不属本次范围。**⑫验证**：`npm run typecheck` **×4 全绿** + `npm run test -w @whale/core` **87 文件 / 856 用例全绿**（本批未增用例，差值来源见前批：847 → +9 收口批） + `npm run content:check` 全绿（含「舰级契约」「族→战术契约」两行原文，B 档位守卫已双向负向验证） + `npm run build -w @whale/desktop` 全绿。**改动全在 d2 工作树，未 commit、未合入 main、未推送。****【待验收 · 未合入 main】**
- [✓] **A 族收尾 · 蜃影回归 + 多技能行硬上限检查（船长 2026-09-11 二次裁决；二号）**——**①蜃影未达标 → 取方案 (1)**（船长理由：「这批的意图是 **A 族设定落地**，**不是难度重排**；蜃影改动前是"唯一真威胁的墙"（100%/49s/残 50%），掉到 60% 属**回归**，必须修回位」）：**纸面锚点由 ×1.6 下调为 ×1.2**——赤潮 **8.70**、蜃影 **13.80**，仍**只改卡上 `dmgMul`**（赤潮 头目 **21**/杂鱼 **5**、蜃影 头目 **33**/杂鱼 **7**）、**禁止恢复 `foeDmgMul`**、不动舰级表/血量/编成/速度/射程；**口径确立（已写进设计稿）：难度守恒按「实收」判定，不按「纸面」**——N 单位逐个被击毁 ⇒ 实收 ≈ 纸面 ×`(N+1)/(2N)`（N=4 → ×0.625），而这两张卡改动前只有 2 单位（衰减 ×0.75）⇒ 纸面 ×1.6 会实收成改动前的 **×1.33**，要"实收 ≈ 改动前"须纸面 ×`0.75/0.625` = **×1.2**（四张固定系卡不受影响：改动前 1 单位 ⇒ ×1.6×0.625 = ×1.0，实收本守恒、实测残血也几乎不动）。**②目标读数达成**：赤潮 **100% / 43s / 残血 86%**（目标 100%/~88%）、蜃影 **100% / 48s / 残血 45%**（目标 100%/~50%）⇒ 两张卡回到改动前的难度位置 ✓。**③多技能行实测 + 600s 硬上限检查（船长要求，标定纪律）**：六卡 × 三档技能行（无技能 / 低技能=6 项战斗技能 L3 / 中位=全技能 L3）× 5 播种——边境 100%/21s、100%/17s、100%/17s；碎晶 100%/43s、100%/19s、100%/19s；信标 80%/140s **⚠触顶1/5**、100%/16s、100%/18s；灰霾 40%/220s **⚠触顶1/5**、**40%/291s ⚠触顶2/5**、100%/47s；赤潮 80%/167s **⚠触顶1/5**、80%/164s **⚠触顶1/5**、100%/43s；蜃影 40%/60s、80%/167s **⚠触顶1/5**、100%/48s。**⇒ 目标「低技能行没有任何一张卡触 600s 硬上限」未达成，按指令交裁决（未自行回调数值）。** **④根因判断（与数值无关）**：六卡**中位行全 100%**、纸面锚点达标、总血守恒 ⇒ **不是编成/血量分布问题**；触顶只现于弱技能行，根因 = **`foeDesiredRange()` 给敌人算期望交距用的是旧路径的全局战术表**（`TACTIC_RANGE[战术] × tacticDesireFactor`），**不看舰级自己的射程带** ⇒ 弱行玩家（无机动/推进技能 ⇒ 慢）**追不上**按 kite 口径站远的狙击舰（期望交距 ~8.9km）也**够不着**（玩家炮 ~4.6km）⇒ 打不完。**对照实验（关键证据）**：同一装配、同样两行弱技能去打**旧路径 kite 卡**——`幽灵舰信号 T46` **80%/54s（无技能）、100%/46s（低技能）不触顶**；`深渊之门 T45` 与三张 E 段墙**快速判负**、也不触顶 ⇒ **旧路径 kite 卡的"期望交距"与"敌人射程带"同源**（都来自旧战术表），双方都在射程内、战斗必然收敛；**舰级路径把射程带换成绝对值却没同步换期望交距** ⇒ 才有"打不完"。**⑤连带**：这与 **B 族教学卡在新口径下 0%** 是**同一条根因**（见 `docs/design/foe-faction-b-scavenger-20260911.md` §4.1）——**一个修法可同时解决两处**：让舰级路径的期望交距取**自己的射程带**（`舰级/条目 band × desireFactor`）；⚠ 会改变**全部 9 张舰级路径卡**的距离行为（含已验收的 A 族读数），**属新口径 · 待船长裁决 · 本批未动**。**⑥回复确认项**：边境/碎晶**不写射程覆写获认可**，并把「**整队 0 开火 ≠ 头目问题，而是清场过快**（玩家 21~23 秒打完、敌人从未进入接触窗口；写覆写只会把 2210m 缩成 1883/1932m 更打不到）」写进设计稿 §3.3(e)；契约按「逐条主体有效值」更严**获认可**；窝点派生维持单波（含"102 个波次 ≥1 单位"契约）**获认可**；边境/碎晶"对中位行近乎免费局"**登记为待办**（射程/节奏口径，本批不动）；增援批在 `6605df8` **保留**。**⑦验证**：`npm run typecheck` **×4 全绿** + `npm run test -w @whale/core` **87 文件 / 857 用例全绿**（重锚守卫值随裁决同步） + `npm run content:check` 全绿 + `npm run build -w @whale/desktop` 全绿。**⑧文档**：`docs/design/foe-faction-a-numbers-20260911.md` §3.3 重写为「(c-1) 第一版 ×1.6（废弃）/ (c-2) 定稿 ×1.2 + 实收口径 / (d) 修正后实测 / (e) 边境碎晶 0 开火解释」，新增 **§八 多技能行实测 + 硬上限检查**（含对照实验），§六 待办重排（蜃影已结、新增"期望交距口径"与"免费局"两条待办）。**改动全在 d2 工作树，未 commit、未合入 main、未推送。****【待验收 · 未合入 main】**
- [✓] **B 族规格增补（船长 2026-09-11 追加裁决「B 族速度按 0.8 走。没有窝点，排除出赏金范围」；二号）**——**①速度一律 0.80**：`foe-scav-skiff` **272**（340×0.8）、`foe-scav-armed` **236**（295×0.8）；`0.90/0.92` 中间口径**作废**；速度是**有意差异**（与"零变化锚定"分开列：旧绝对速 322/337/286 → **272/272/236**）。**②三卡实测（0.80 下，标定口径 S2 中位技能行）**：演习场驱逐令 **100%/7s/残血 100%**（改动前 100%/7s/100%）、新港商路护航令 **100%/7s/100%**（改动前同）、占港武装通缉 **100%/10s/100%**（改动前同）——**三卡与改动前逐项一致 = 零漂移**、均不触 600s 硬上限；期望交距读数由 brawl 的 441m → orbit 的 **1210m**；另在更弱的 20 项技能行上为 **4s/4s/7s**（弱装配下更快，见④）。**③⚠ 重要连带：0.80 把上一版口径（306/271）下的 P0 死锁解决了**——那版在裸船零技能下演习场/新港是 **0% / 600s 打满判负、双方 0 次开火**；改 0.80 后死锁消失（敌人比玩家慢 ⇒ 距离拔河由玩家主导、距离收得进射程）。**④「偏慢」定位判断（船长要求，如实说）**：**合适，但代价是"敌人来不及参战"**——无技能行探针：演习场/新港 **最近距离 3205m、敌开火 0 次（9s 结束）**、占港 3207m / 敌开火 4 次（19s）；A 族边境卡同情形（0 次）。⇒ 两张小艇卡的敌人**全程一炮未放**（自带带 1~2200m，而战斗在 3205m 就结束），4 秒结束**远短于卡面 `combatSeconds: 20`**；对"新手第一课"而言"能赢、看得懂"达标，但**教学信息量不足**（看不到敌方武器/射程/受击表现）。**建议方向 = 开战距离/先手（新口径、未动）**，**不是把速度提回去**（提速会重回"追不上/够不着"）。**⑤窝点排除核查（交付项）**：**候选张数/族数 `19 张 / 5 族` → `19 张 / 5 族`（前后不变）**——`isLairCandidate()` 早含 `if (foeFamily === 'B') return false`（2026-09-10 起），B 两卡**本来就被排除**（对照：有核心词 + 奖金>0 = **21 张**，差额正是这两张 B 卡）。**⚠ `lairCore` 字段我未删**（按规格"会破坏机制就停下报告"）：探针实证 `data/context.ts` 注册稀有残骸用的是 **`hasLairCore`（不是 `isLairCandidate`）**，注释写明**刻意包含已停用的 B 族**（好让旧档里已存在的 B 族稀有残骸仍能被识别/开箱）；探针确认 `wreck-rare-ano-harbor-escort`、`wreck-rare-ano-abandoned-platform` **现在确实已注册** ⇒ **删字段会让它们不再注册、旧档里这两件变"未知物品"**。**推荐二选一（待确认）**：(1) 保留 `lairCore` 仅作旧档兼容（零风险）；(2) **删 `lairCore` + `context.ts` 注册条件改 `hasLairCore(a) || RETIRED_LAIR_CARD_IDS.has(a.id)`**（语义最干净，我推荐）。**⑥专属件核实**：`FOE_LAIR_GEAR.B = []`（**本就为空**，原「拾荒者拆解臂」随 B 窝点取消一并撤下），且有契约守卫 ⇒ **无需移除**。**⑦唯一机制改动**：`sideTasks.factionPoolOf()`（「派系活跃」候选池）由 `hasLairCore` 改判 **`isLairCandidate`** ⇒ B 族**规则上不参与**；探针核对：三张 B 卡所在星系（母港/新港/碎晶带）security 均 **0.6 = 高安**，本就被"非高安"条件挡掉（**此前从未当选，属侥幸不中**）⇒ 本次改动**行为等价、语义对齐**（候选池仍 16 席）。**⑧契约**：新增 **B 族全族慢速口径**（「敌速口径契约」：B 族实速必须**低于**本档舰种基准、比率落在 **0.60~1.00×**——旧 orbit 带 0.90~1.25 与"全族慢速"直接冲突；与 A 族提速带对称）；「舰级契约」B 档位 <3、「族→战术契约」B=orbit 同前；均双向负向验证。**⑨验证**：`npm run typecheck` **×4 全绿** + `npm run test -w @whale/core` **87 文件 / 857 用例全绿** + `npm run content:check` 全绿（敌速输出含「B 族 3 条按全族慢速口径」+ 实测读数） + `npm run build -w @whale/desktop` 全绿。**⑩文档**：B 设定卡 §一 补裁决 8/9、§二 速度改 272/236（有意差异单列）、§四 改写为「4.1 三卡实测 / 4.2 偏慢判断 / 4.3 引擎口径待裁」、新增 **§六 窝点排除核查**（候选数对照 + lairCore 待裁 + 专属件 + 派系活跃）；`docs/glossary.md`「武装拾荒者（B 族）」补速度 0.80 与"无窝点、不参与赏金派发"；族档 B 段同步。**改动全在 d2 工作树，未 commit、未合入 main、未推送。****【待验收 · 未合入 main】**　**⑪⚠ 后续裁示（2026-09-11 船长/上级）：「速度 0.80 那版增补**不要执行**」**——树内 `speedRatio` 已**回档为 `0.90` / `0.92`（实速 306 / 271，= d2 现值）**，**最终值由三号裁定**。⇒ 本条第 ①②③④ 里的 **0.80 / 272 / 236 及其实测**一律**降为历史留档**（该口径**未落树**）；§⑧ 的契约形状不变（"B 族实速低于本档舰种基准 + 比率落 0.60~1.00×"在 0.90/0.92 下同样成立）。
- [✓] **敌人数值复核 · 多装配对照 + 标准复核配置集（船长 2026-09-11 追问「配置是否都采用了远程风筝磨血才导致 0 损失？」；二号）**——**纯测量批，未改任何数值**。**①船长质疑成立，且是此前报告的疏漏**（把"残血 100%"当成"敌人软"用了）。**②参考行推演设定（首次成文）**：玩家战术档 = **`mid`（= 主武器射程带中点，引擎在 `startBattleFor` 里写死 `desiredRangeFor(me,'mid')`）**；`desiredRangeFor` 另有 `assault`(min×0.6)/`kite`(max×0.95) 两档但默认不用（战中滑杆/出发参数可改）；参考行主武器 = 4× **重型炮台 MK2 动能型 `700~5740m`** ⇒ **玩家期望距离 = 3220m**（实测终局交距 3197~3241m ✓）；敌人期望距离 = `TACTIC_RANGE[战术] × tacticDesireFactor`（brawl `0~2200`→440 / orbit `350~4600`→2688 / kite `1200~9200`→8000），**与舰级自己的射程带无关**。⇒ 3220m 恰好落在**海盗快艇（1~1883/1932）与 B 族小艇（1~2200）射程之外**。**③六组装配 × 六张 A 族卡 + 3 张非 A 对照卡（3 播种，逐格报 胜率/时长/残血/终局与最近交距/我方开火/敌方开火并分列头目杂鱼）**：①参考行 ②近战短射程·无推进（mid 1735）③参考行去推进 ④低技能档 ⑤贴脸高射速·无推进（mid 1890）⑥**远程风筝·导弹MK2（mid 6330）**。**④判据结论**：**边境/碎晶 = "0 损失"是参考装配的产物 ✓**（参考行敌开火 **0 次**；换②近战立刻 **4~5 次**）；**信标 = 敌人够得着也开火（10→44 次）但啃不动**（残血仍 100%）；**灰霾/赤潮/蜃影 = 不是 0 损失**（参考行敌开火 **38/34/39 次**、残血 **90/85/43%**），且换近战/贴脸后**玩家自己 0 开火被反杀（0% 胜率、308~600s）**——**"够不着"是双向的**。⇒ **此前"A 族六卡全 100% 胜率 / 残血 100%"的说法作废**，按三种装配重新表述。**⑤交付项·近战装配下六卡**：边境 100%/100%、碎晶 100%/100%、信标 100%/100%（打了 155s 挨 44 炮）、**灰霾 0%/88%（600s 触顶）、赤潮 0%/0%、蜃影 0%/0%** ⇒ **没有任何一张卡"输在敌人太强"**（三张 0% 是玩家够不着），能对射的三张**贴脸也几乎不掉血** ⇒ **对"A 族要不要加硬度"的结论 = 先修射程/期望交距口径，否则读数都被"谁站在谁的射程外"污染**。**⑥交付项·参考行不具代表性 + 建议固化为标准复核配置集**：参考行三缺陷（距离档单一且偏袒玩家 / 不覆盖"玩家够不着远程敌"的另一端 / 技能档单一）；**建议四行 A0 标准中距·A1 贴甲近战·A2 远程风筝·A3 弱技能行 + 强制列「敌开火次数（头目/杂鱼分列）」**，并建议 `battle-calibrate --csv` 增列「我/敌开火、终局交距」。**⑦文档**：新增 **`docs/design/foe-baseline-audit-20260911.md`**（参考行推演设定 + 6 装配 × 9 卡全矩阵 + 判据结论 + 标准复核配置集）；`docs/design/foe-faction-a-numbers-20260911.md` 头部加**读数更正**（"残血须连同敌开火次数一起读"）；**未改任何数值、未新增契约**。**⑧验证**：`npm run typecheck` ×4 / core **87 文件 / 857 用例** / `content:check` / desktop build 全绿（本批为纯测量 + 文档，四闸不受影响）；`_` 探针 0 个。**改动全在 d2 工作树，未 commit、未合入 main、未推送。****【待验收 · 未合入 main】**
- [✓] **标准复核配置集落地 + 期望交距口径修正（船长 2026-09-11 两裁决：①采纳四行与开火列 ②采纳「期望交距取自身射程带」；二号）**——**①标准复核配置集（裁决①）**：`tools/battle-calibrate.ts` 固化 **A0~A3 四行**（`STANDARD_ROWS` + **`--std`**：A0 标准中距 4×动能MK2+推进+支援/全技能L3 · A1 贴脸近战 4×轻型炮MK1 `250~3220` 无推进 · A2 远程风筝 4×重型导弹架MK2 `900~11760` · A3 弱技能行 = A0 装配 + **6 项战斗技能 L3**（`gunnery`/`kinetic-gunnery`/`reload-drills`/`fire-control`/`targeting-integration`/`shield-operation`，与 A 族 §八 同源逐字沿用））；`--csv` **新增 6 列**——`引擎期望交距`/`终局交距`/`最近交距`/`我开火`/**`敌开火(头目)`**/**`敌开火(杂鱼合计)`**（头目 = 该波首个单位 `foe-0`；分列需**逐单位**计数，故实测段改**分段推进**采样——fx 是 48 条环形缓冲，一次性大步长会裁掉长战事件；引擎内部固定 100ms 步长，分段不改结果，既有 27 列**逐值不变**）；头注释写死**强制纪律：判定敌人强弱必须至少跑满 A0~A3 四行**（单跑 A0 的残血/胜率会被距离口径污染，即"残血 100% 误读成敌人软"的根因）。**②期望交距口径修正（裁决②，本批主体）**：`UnitSpec` 新增可选 **`foeRangeBand`**（**只有舰级路径写**，值为含 `rangeMul`/`rangeMinM`/`rangeMaxM` 覆写后的有效带）；`foeDesiredRange()` 的带改为 **`foes[0]?.foeRangeBand ?? TACTIC_RANGE[战术]`** ⇒ **舰级路径取自己的带、战术只决定带内偏好位置**（`tacticDesireFactor` 0.20/0.55/0.85 **原值不改**），**旧威胁推导路径一字不动**（`TACTIC_RANGE` 注释标注"只服务旧路径"）；混编卡取 `foes[0]`（首个主体单位），要同带就写覆写（"同卡同带"），**不做加权平均**。实算：快艇 440→**377m**、护卫舰 2688→**2498m**、狙击舰 8000→**8364/8885/10102m**、**B 小艇 2688→1210m**。**③验收四判据逐条**：**①弱技能行无 600s 触顶 ✅**（修前低技能行 灰霾 2/5、赤潮 1/5、蜃影 1/5 触顶；修后 **9 卡 × 三档技能行触顶数全 0**，同 seeds/同装配复核）· **②B 族教学卡最弱行回到 100% ✅**（**裸船零技能 25 艘全扫 0 死锁**，修前演习场 **15/25 艘**死锁；且"敌人一炮未放"归零——敌人现在收到 1210m 期望交距并真的开火；`--std` A3 行两张卡 100%/9s/100%）· **③A 族 A0 中位行不劣化 ⚠ 5/6 达成、蜃影不达标**（边境 100/23s/100%、碎晶 100/25s/100%、信标 100/29s/99.3% 均不变；灰霾残血 88.8→**96.8%** 更好；赤潮 80.8% ≈持平；**蜃影 残血 40.1→32.1%、时长 53→65s**——机制 = 修正后敌人**开火次数上升**（头目 9.4→10.8、杂鱼 28.2→32.4）；按船长「不为凑判据反复调数值」**未回调、停下报告**）· **④旧路径 20 张卡逐字不变 ✅**（`--csv` 既有 27 列逐卡比对：旧路径 **20 张零差异**；变化只出现在 3 张舰级路径卡 = 判据③同一现象）。**④P0-2（开战距离/先手）建议留作下一批**（理由：会动全 26 卡含旧路径 ⇒ 与本批判据④冲突；需单变量归因；三种候选修法差异大需船长选型——详见 P0 稿 §7.5）。**⑤测试**：`tests/foe-ship-path.test.ts` 新增 **4 条**期望交距守卫（旧路径 440/2688/8000 逐值锁定 + 舰级路径 441/1210/3550 + 覆写=纠偏旋钮 + **全 9 卡"期望交距必落在自身射程带内"**）⇒ core **87 文件 / 861 用例**。**⑥文档**：`docs/design/p0-foe-desired-range-20260911.md` 状态改**已裁决·已落码**并新增 §7（落码记录 / 实算对照 / 四判据逐条 / 配套交付 / P0-2 分步理由）；`foe-baseline-audit-20260911.md` §四 升级为**已采纳口径**（含落地表）；`foe-faction-a-numbers-20260911.md` §八加**收口**（触顶已消除 + 修后读数）；`foe-faction-b-scavenger-20260911.md` §4.2(a)/§4.3 更正（死锁是**船条件相关**：15/25 艘、**不含起始船**；`0.80` 只能把死锁集 **15→10** ⇒ "0.80 解决死锁"说过头）并标注根因已修；`docs/glossary.md` 新增「标准复核配置集（A0~A3）」「开火列口径（强制列）」「期望交距（舰级路径取自身射程带）」三条。**⑦验证**：`npm run typecheck` ×4 / core 861 用例 / `content:check` / desktop build 全绿；`_` 探针 0 个。**改动全在 d2 工作树，未 commit、未合入 main、未推送。****【待验收 · 未合入 main】**
- [✓] **B 族窝点字段退役 + 旧档兼容白名单（船长 2026-09-11 裁决「`lairCore` 按**方案 2** 执行」；二号）**——**「B 族无窝点」= 三层结构**：**①族规则排除** `isLairCandidate()` 的 `foeFamily === 'B' → false`（2026-09-10 起，**未动**，是另一条独立护栏）· **②字段退役** 删掉 `ano-harbor-escort`（新港商路护航令）/ `ano-abandoned-platform`（占港武装通缉）的 **`lairCore`**（两卡均无 `lairLevel`），原位留退役注释（写明裁决依据 + 兼容去向 + "**不是漏标、勿加回来**"）· **③旧档白名单** 新增 **`packages/data/src/retiredLairCards.ts`**（`RETIRED_LAIR_CARD_IDS`，显式列这两个 id，注释写明"为什么不能直接删字段"），`data/context.ts` 稀有残骸注册条件由 `hasLairCore(a)` 改为 **`hasLairCore(a) || RETIRED_LAIR_CARD_IDS.has(a.id)`**（否则旧档里已获得的 `wreck-rare-ano-harbor-escort` / `wreck-rare-ano-abandoned-platform` 会变「未知物品」）；`data/index.ts` 导出该常量。**④契约护栏**：`content-check` 新增**「退役窝点卡契约」**（4 条：白名单 id 必须存在于 `ANOMALIES` / 这些卡 `lairCore` 必须为空 / 必须仍被 `isLairCandidate()` 排除 / 对应稀有残骸仍可注册），输出行 = `· 退役窝点卡契约：2 张退役卡字段已清、白名单有效、稀有残骸仍可识别（wreck-rare-ano-harbor-escort / wreck-rare-ano-abandoned-platform）`；**三种负向验证实测都会拦**（①白名单写错 id ⇒ 「白名单 id「ano-harbor-escortX」在 ANOMALIES 里不存在」②把 `lairCore` 加回来 ⇒ 「…的 lairCore 应为空——…字段退役后不得加回来」③从注册条件摘掉白名单 ⇒ 两条「旧档稀有残骸 … 未注册…否则旧档里已有的这件的会显示成"未知物品"」）。**⑤旧档兼容实跑（不许只看代码）**：识别层（两件物品定义均可解析：`kind=wreck`、显示名「稀有残骸（新港商路护航令）」/「（占港武装通缉）」）· 存档层（造含这两件的档 → `serializeSaveFile` → `loadSaveFile` ⇒ 两件都在、数量 30/30 不变、名字可解析）· 结算层（`startRecycleRun` ⇒ ✅ ok；`advanceGame` 派时间 ⇒ 炉子结算、残骸耗尽、装备库得到产出；**高级箱 30/30 命中**）；**负向对照实跑**：把定义从 ctx 摘掉 ⇒ 回收炉首行守卫报 **「未知物品：wreck-rare-ano-harbor-escort。」**（= 不补白名单时旧档的真实症状），假 id 同样被拦，还原后正常。**⚠ 未验到的层**：桌面渲染层（UI 的"未知物品"显示分支）未跑实机——本轮验到的是**数据/引擎层**（物品定义解析 + 存档 + 回收结算），UI 只依赖同一份 `ctx.items` 查询。**⑥文档**：`docs/glossary.md` 新增「退役窝点卡白名单」与「退役窝点卡契约」两条；`docs/design/foe-faction-b-scavenger-20260911.md` §六(b) 改写为**三层结构表 + 实跑结论**。**⑦验证**：`npm run typecheck` ×4 / core **87 文件 / 861 用例**（基线 857 + 上一批"期望交距"新增 4 条）/ `content:check`（含新契约行）/ desktop build 全绿；`_` 探针 0 个。**改动全在 d2 工作树，未 commit、未合入 main、未推送。****【待验收 · 未合入 main】**
- [✓] **B 族速度终值落值（`0.80/0.80` = 272 / 236）+ 三号口径逐块合并 + 蜃影裁决归档（船长 2026-09-11 三条裁决：「**B 族速落实 0.8**」/「**按你的执行**」/「**4，不做调整。保留**」；二号）**——**①速度终值落值**：`foe-scav-skiff` **272**（340×0.80）、`foe-scav-armed` **236**（295×0.80）；**中间口径 `0.90/0.92`（306 / 271）作废**（保留痕迹以便追溯）。落点 = `packages/data/src/foe-ships.ts`（两舰级倍率 + 类注释）、`packages/core/tests/foe-ship-path.test.ts`（守卫期望值 272 / 236）、`tools/content-check.ts`（契约带 + 读数）、`docs/glossary.md`（「武装拾荒者（B 族）」条）、族档 `foe-faction-identity-20260910.md`、战术档 `faction-tactic-20260910.md`、B 设定卡 §一 / §二 / §三 / §4.1 / **§九**。**②契约带改为「B 族全族慢速带 `0.75~1.00×`」**（取代上方「B 族规格增补」条目里的 `0.60~1.00×` 口径，**以本条为准**）：下限 0.75 防"慢到玩家白嫖"、上限 1.00 = **慢于基准船**（"偏慢"的可验证表达）；**必须用族级带、不能用战术带**——orbit 常规带 `0.90~1.25×` 与"全族偏慢"直接冲突，0.80 口径下拾荒火力舰 **0.81×** 会被误拦（本批实测踩到）。`content:check` 实测读数 = 拾荒武装艇 **272（0.93×）** / 拾荒火力舰 **236（0.81×）**，三张卡全部通过。**③0.80 前后对照（同一棵树，只切 B 两舰级倍率，5 播种 1/7/13/29/51）**：三卡 A0~A3 四行**逐格胜率 / 时长 / 残血完全相同**（仅最近交距随敌人更慢 **+16~87 m**：演习场 3564→3620、新港 1781→1797、占港 3271→3285 / 9563→9650 等）；**裸船零技能 25 艘全扫：两个口径都是 0/25 死锁、"敌人一炮未放" 0 艘**（用时区间亦一致）⇒ **在当前距离口径（P0-1 已修）下，0.80 对实测读数几乎无影响**；P0-1 修前它的贡献是把死锁集 **15 → 10 艘**（见 B 设定卡 §4.2(a)）。快照 = B 设定卡 **§9.4**；原始读数由临时探针 `_probe-b080.ts` 产出，**已删**（`_` 0 个）。**④三号口径逐块合并（船长「合并」）**：`docs/glossary.md`（保 d2 各条 + 取 verify 两条非 B 新行 + B 速度定稿）·`docs/roadmap.md`（本条目）·B 设定卡（新增 §九：三处分歧裁决表 / 三号落码实测读数 / 回交单四处差异更正 / 前后对照 / 契约口径）·族档与战术档（B 速度 + `foeFalloff` 行）·`merge-note-verify-20260911.md`（§五 由"待裁"改为"**已全部裁决**"+ 逐块合并执行情况）；**取件只取全新文档、逐文件、原始字节 + blob 校验；verify 的代码 / 数据一律未取**（会倒退 d2 的 A 族 / 距离口径 / 舰种表成果）。**⑤蜃影裁决归档**：船长「**4，不做调整。保留**」⇒ 四问全裁（A0 残血 **32%** 接受、保留为 A 族难度天花板 / ②③⑤ 三行 **0%** 不处理、日后归 **P0-2「开战距离 · 先手」**批 / 技能断崖保留 / 仍是最硬海盗窝点目标、与 **E 段墙**一起看）；`docs/review/mirage-20260911.md` 稿首补裁决记录，该稿此后作**实测数据快照**（数值一动须重跑 `battle-calibrate --std` 并同步）。**⑥验证**：`npm run typecheck` ×4 / core **87 文件 861/861** / `content:check`（含「敌速口径契约」B 族 3 条新带值）/ desktop build 全绿；`_` 探针 0 个。**⑦交接提醒（重要）**：d2 仍缺 main 的同日「远端衰减三改 + 回调」批（敌方默认 `foeFalloff` 0.3 → **0.5**、我方动能 0.30·0.28 → **0.5**、激光 0.30·0.35 → **0.1**）——**本批与 A 族各批的标定读数都是在旧 falloff 口径下测的**，同步该批须**整批重标**；词典「远端衰减（falloff）」「彩头分层」两条已带来源标注与提醒。**改动全在 d2 工作树，未合入 main、未推送。****【待验收 · 未合入 main】**
- [✓] **修「battle-calibrate 敌开火列在长战漏计」（船长 2026-09-11：「现在单开一小批修」；二号）**——**根因（工具法，不是引擎）**：`tools/battle-calibrate.ts` 的 `simulateCell` 用 `b.fx.slice(seen)` + `seen = b.fx.length`（**以"环长度"作游标**）累计敌开火，而 `BattleState.fx` 是 **48 条环**（`pushBattleFx`：`if (b.fx.length > 48) b.fx.splice(0, …)`）⇒ **环满之后 `slice` 恒为空**，该场后续开火**全部漏计**（分段推进也救不回来：游标本身失效）。**实测对照（同一场战斗、同一批种子）**：灰霾 T28 ⑤贴脸 600s 真值 **584.0**（头 145.0 / 杂 438.0）↔ 工具法 **48.0**（**漏 92%**）；蜃影 T48 A1 140s **136.0**（33.0/102.0）↔ 48.0；蜃影 A0 65s **54.0**（12.8/40.2）↔ **43.2**。**修法（一行级、按 `BattleFx.seq` 续读）**：被裁掉的永远是**旧事件**（`seq` ≤ 上次最大值），故"`seq` > 上次最大值"的事件**一定还在环里** ⇒ 无损；`lastSeq` 游标替代 `seen`/`slice`。**未动**：我方开火仍取 `stats.meShots`（引擎累计、权威，不受此 BUG 影响）、引擎与任何数值一律未动。**验证**：`npm run battle:calibrate -- --std` 复跑全 26 卡 × A0~A3（读数变化只出现在长战格子，例：蜃影 A3 敌开火 **48.0 → 78.6**、灰霾 A3 **48.0 → 53.8**；短战格子逐值不变），胜率/时长/残血/交距列**逐格不变**（本修复只改计数，不改战斗）。**连带（待办 · 已登记）**：以下文档的"敌开火"列是**旧口径（偏低）**，需按新工具重测后再引用 —— ①`docs/review/mirage-20260911.md` §2.1/§2.3（蜃影 A0 记 10.8/32.4，真值 12.8/40.2）②`docs/design/p0-foe-desired-range-20260911.md` §7.3 判据 3（"头目 9.4→10.8、杂鱼 28.2→32.4"）③`docs/design/foe-faction-a-numbers-20260911.md` 各表敌开火列 ④`docs/design/foe-faction-b-scavenger-20260911.md` §4.2/§九（教学卡多为 0~1 次，量级影响小）。**⚠ 另一处口径不一致（发现即报）**：`docs/design/foe-baseline-audit-20260911.md` §二 的表用的是**另一套正确量级**（灰霾 ⑤ 记 **584（146/438）**，与本批真值 584（145/438）吻合）⇒ 同一仓里**两张表曾用两种计数口径**，重测时以本批 seq 口径统一。**词典**：`docs/glossary.md`「开火列口径（强制列）」条已改写为"我方 stats / 敌方 seq 续读"并写明旧法漏计。**改动全在 d2 工作树，未合入 main、未推送。****【待验收 · 未合入 main】**
- [✓] **P0-2 追加口径复测：推进器档位门槛（船长 2026-09-11：「假设远程型怪，玩家带推进器能获胜，那么就没问题。甚至基于敌人难度，假如仅携带 MK1 推进器就能 100% 获胜，那么就属于设计失衡」；二号）**——**这条口径把判据从"0% 三行要不要救"换成"最低档推进器是否已经够用"，并直接推翻本批先前的"乙 = 收窄敌人带"方向**（收窄到 3400m 会让**不带推进器**也 100% ⇒ 正是船长说的失衡；该取数作废、探针已删）。**复测矩阵 = 4 张 kite 卡 × 3 武器行 × 4 档推进器（无/MK1+30%/MK2+60%/MK3+100%，周期点火 60s/60s、开场即点火），5 播种 1/7/13/29/51**，装配生效逐行验证（`sh-mako` 基础机动 345 m/s、峰值 345/449/552/690 m/s）。**①蜃影 T48 = 门槛健康、不改**：参考行 无推进 0%（140s）· **MK1 0%（90s、我开火 0.0）** · **MK2 100%/65s/残 32%** · MK3 100%/53s/45%；近战 MK1 武器行 MK1/MK2 均 0%・**MK3 60%/68s/残 3%**；贴脸转管炮 MK1 0%・MK2 80%・MK3 100% ⇒ 阶梯与"T48 = A 族天花板"自洽，与船长「保留」裁决一致。**②灰霾 T28 / 赤潮 T34 = 门槛偏低（按新口径即失衡）**：参考行 **MK1 即 100%**（灰霾 81s/残 92%、赤潮 85s/残 70%）、贴脸转管炮 **MK1 也 100%**（150s/79%、156s/12%）⇒ **威胁更低的两张卡反而比 T48 更容易被最低档推进器破解**；⚠ 如实说：MK1 行是**磨胜**（81~160s、挨 67~145 次敌火），非碾压。赤潮近战行例外（MK1 **60%**/残 4%、MK2 才 100%）。**③"不带推进器 = 0%"不是缺陷**（推进器正是正当解法），**唯一遗留 = 可读性**：无推进器行灰霾 **600s ⚠触顶 5/5**、赤潮 373s、蜃影 140s，玩家只看到"失利"⇒ 建议按提案 §四 候选丙（提前脱战 + 战报写明"我方射程 X / 敌典型交距 Y"）单独处理、**不动数值**。**④幽灵舰信号 T46（旧路径对照）= 全 11 格 100%**（连无推进器都 100%/74s/残 53%）⇒ **旧路径 kite 根本没有这道距离检定**，再次印证差异来自舰级路径的"自身带 ×0.85"。**⑤三选一待裁**：甲 治"门槛偏低"（抬灰霾/赤潮难度使 MK1 不够、MK2 才是门槛，手段另出提案）/ 乙 只治可读性（数值不动）/ 丙 接受现状（把 MK1 通关当 T28/T34 的中段定位）。**⑥船长裁定「乙2，事件为 120 秒」→ 已落码**：新增**「无法交战 ⇒ 提前脱战」**——`balance.ts` 新参数 `cannotEngageMs`（默认 **120 000**，设 0 = 关闭）+ `combat.ts` `stepBattle` 结束判定段（与超时判负同一处、同一形态）三条件分支（**开战满 T 秒** + **我方全程一炮未发** + **当前距离在我方最远射程之外且敌已开火**）⇒ `ended='foe'`、`autoEscaped=true`、`escapeReason='cannot-engage'`；`state.ts` 的 `escapeReason` 联合类型扩一个值（可选、缺省不落盘 ⇒ **零迁移**）；`expedition.ts` `settleBattleRetreat` 新增 `'cannot-engage'` 分支 = **与超时判负同源的轻损撤退**（扣损 ×0.5 / **不掷弃船骰** / 下限 5% 保护 / 停清剿 + 提示），战报**与引擎同源**写出"我方主武器最远射程 X m，敌编队停在约 Y m 外（交火 …，一炮未发）"。**落码前后对照（受影响 9 格 · 5 播种）**：蜃影 ②⑤③ **击毁 5/5｜140s｜残 0% → 撤退 5/5｜120s｜残 14%**；赤潮 ②⑤③ **击毁 5/5｜373s｜残 0% → 撤退 5/5｜120s｜残 69%**；灰霾 ②⑤③ **timeout@600s → cannot-engage@120s（残 96%）**；**三卡胜率仍 0%**（数值一个字未动）。**验证**：新增 `tests/cannot-engage.test.ts` **5 例**（触发 / 阈值边界（满 T 那拍不触发、下一拍生效，因 `stepBattle` 在 `lastTickGameMs += dt` 之前调用）/ 我方开过火永不触发 / 接近段不误判 / 关开关零行为）+ 四连全绿（typecheck ×4 + core **88 文件 866 例** + content:check + desktop build）。**边界（如实报）**：只治"我够不着"（双方都没开火、或我方开过火都不触发，仍由 600s 上限兜底）；**AI 副船与低安遭遇的结算路径本就不看 `autoEscaped`**（与超时判负同待遇，本批未改）⇒ 那两条只会提前收场，不新增收益/惩罚；本批**未做**战斗界面状态行、卡面"典型交距"预告、手册须知（需要请点一句）。**改动全在 d2 工作树，未合入 main、未推送。****【待验收 · 未合入 main】**
- [✓] **换星系连带项复核（船长问：「是不是交换星系后，你忘记更新悬赏威胁度了？」；二号）**——**答：威胁值本身没漏**（方案丙当初明确「威胁值一个都不改」，且现值都在新分区带内：烬火围攻战 **42 ≤ 中安顶段 52**、蜃影导航劫持令 **48 ≥ 低安地板 45**）⇒ 这两项**无需改**；**但漏了一个随分区走的作者值**：**`ano-cinder-siege`（烬火围攻战）的 `foeHitRate` 仍是 `0.95`（低安 +0.1）**，而烬火星区已改判中安 ⇒ 应按口径回到 **0.85**。**①为什么漏**：设计稿 §6.3 的"八项连带面"（伏击/扫描/打捞/密度/被抢/销赃/市场价/星图）**全是运行时由 `security` 推导**，而 `foeHitRate` 是**卡片作者值**——改两个 `security` 数字不会带动它；清单里当时**没有第 ⑨ 项**（已补进设计稿 §6.3）。**②机制核对（`combat.ts:1217-1234`）**：旧路径每单位**一条武器组**，形态/命中按**主系**——`kind = 主系 plasma ? beam : fixed`、`hitRate = 主系 plasma ? 1 : (anomaly.foeHitRate ?? bal.foeHitRate)` ⇒ **主系为 plasma 的卡是光束必中，该字段无效**。**③全表体检（21 张可见卡，含分区/威胁/命中/多波次）**：唯一真实不一致 = 烬火围攻战；**被误判的两张已排除**——`ano-abyss-guard`（纯 plasma）、`ano-starcore-boss`（主系 plasma）把 0.85 ↔ 0.95 对调后 **A0~A3 四行读数逐格完全相同**（证明字段不生效）；`ano-titan-wreck` 0.65 = 低命中特例 0.55 + 0.1 ✓；**多波次 4 张（坟场/噬口/穹顶/虚海）全在低安** ✓ 与"低安顶段"口径一致。**④影响面实测（只切该字段，5 播种 A0~A3）**：烬火 0.95 → 0.85 后 **A0 42s/残 93%→95%**、**A3 46s/残 76%→82%**、**A1 74s/79%→78%（基本持平）**、A2 不变 ⇒ **难度几乎不动，但口径归位**。**⑤待船长一句话**：(a) 是否把 0.95 改回 0.85；(b) 是否顺带加一条 `content:check` **「低安命中契约」**（非光束卡：低安必须 0.95、中安/高安必须 0.85）防复发。**⑥落码（船长 2026-09-11：「不用，修正命中就好了」）**：只改 `ano-cinder-siege` 的 `foeHitRate` **0.95 → 0.85**（原位注释写明"烬火星区已中安 ⇒ 撤销低安 +0.1"），**不加契约**；**验证**：typecheck ×4 + core **861/861** + `content:check` + desktop build 全绿，`battle:calibrate --std` 复跑该卡四行 = **A0 100%/42s/残 95.3%**、**A1 100%/74s/残 78.4%**、**A2 100%/42s/100%**、**A3 100%/46s/残 81.6%**（与落码前预测逐格吻合：A0 93→95%、A3 76→82%）；⚠ 该卡**不是窝点/舰级路径卡**，故不受"旧路径逐字不变"约束，但 A0~A3 以外的行未重跑（如需全表刷新请点一句）；两枚审计探针用完已删。**改动全在 d2 工作树，未合入 main、未推送。****【待验收 · 未合入 main】**
- [✓] **A 族批次 · 内容收尾（船长 2026-09-11：「进行收尾」；二号）**——**只收口、不改数值**。**①配置总表同步到落码后现状**：`docs/design/foe-faction-a-numbers-20260911.md` 附表一（四舰级 = 按档重排后的 **156/28 · 364/51 · 480/60 · 900/135** + 档基线/角色修正口径）· 附表二（**边境/碎晶/信标 三张改为「无首领 · 1+2 / 2+2 / 2+2 两波」**，逐条列新倍率与建档值、注明退役的两条头目覆写）· 附表三（三张卡 A0~A3 换本轮实测 **A0 18s / 23s / 24s**，灰霾/赤潮/蜃影逐格不变）· 附表五（挂账 5 条；「期望交距仍取旧战术表」已标记由 P0-1 关闭）全部对齐。**②文档口径**：词典新增「舰级按档重排（档基线 × 角色修正）」「无首领编成（A 族三张）」两条。**③过期措辞清理**：`lairs.ts` 与 `content-check`「窝点派生契约」里「舰级路径**维持单波**／A 族六卡全在 `wave: 0`」按现状改写（三张卡已显式两波、波次按 `slot.wave` 表达）；`foe-ship-path.test.ts` 三条编码旧编成的用例改为按波核对（23 单位）+ 无首领三张射程带一致。**④临时件清零**：本轮探针与建档快照全删，`_` 文件 **0 个**，工作区干净。**⑤验证**：typecheck ×4 + core **88 文件 / 866 例** + `content:check` + desktop build 全绿。**⑥A 族状态 = 已收尾**（六卡：配置 / 读数 / 契约 / 文档四者对得上）。**⑦移交挂账（船长 2026-09-11 处置：**处理 1/2/5、把 3/4 标为已完成**）**：✅**3 已结项**——蜃影 A1 三行 0%（数值按乙2 未动，已由「无法交战 120 秒脱战 + 战报写明射程」可读化）；✅**4 已结项**——E 段墙（坟场 T88 / 穹顶 T96）按设计保留为终局墙，不再作为挂账跟踪；**⑤已处理**——「远端衰减 0.5 / 激光 0.1」批**已自 main 同步进 d2**（见下条 ★）；**①②处理中**（见下条 ★★）。**改动全在 d2 工作树，未合入 main、未推送。****【待验收 · 未合入 main】**
- [✓] **★ 挂账⑤：自 main 同步「远端衰减三改 + 回调」批（2026-09-11；二号）**——**动机**：d2 与 main 分叉，d2 全部 A/B 标定读数都是**旧 falloff 口径**（敌方 0.3 / 我方动能 0.30·0.28 / 激光 0.30·0.35 ×0.8 换算），不校正则两树读数不可比。**同步内容（逐项对齐 main `33a46c1` + 回调 `11e52e2`）**：①`balance.ts` `foeFalloff` **0.3 → 0.5**（动能/爆炸 = 远端命中倍率、能量 = 远端威力倍率）；②`modules.ts` 我方**动能炮台四件 falloff 一律 0.5**（民用 0.3 / MK1 0.3 / MK2·MK3 0.28）；③我方**激光四件 falloff 一律 0.1**（原 0.30/0.35）⇒ 最远端威力 ×0.10；④`combat.ts` `beamPowerFactor` **去掉旧 ×0.8 换算**（新口径 = 近端 ×1 → 最远端 = 该武器 falloff）；⑤窝点专属件：**守墓者长炮 0.45 → 0.6**（回调批；A 转管炮维持 0.35、E 巨构残骸炮维持 0.1）。**连带用例（4 处，全部照 main 的修法移植）**：`v18b2` 公式断言改新口径（远端 = falloff 本身、激光 ×0.10）；`lair-gear-d` 改「专属 0.6 > MK3 0.5」；`wave-battle` 与 `drone-loss` 两张平衡敏感用例把该卡伤害压到 **0.2** 保持用例语义（⚠ d2 已退休 `foeDmgMul` ⇒ 改用**基础单发直写 `foeShotDmg`** 表达，与 main 的写法等价）。**验证**：typecheck ×4 + core **88 文件 / 866 例** + `content:check` + desktop build 全绿。**⚠ 后续（未做，须整批重标）**：d2 全部 A/B 标定读数（A 族附表三、B 族设定卡 §4.1/§九、蜃影稿、审计稿等）仍是旧口径，需按新口径重跑 A0~A3 + 六组后再同步文档。**
- [✓] **挂账① 落码：低威胁卡敌军射程延长到 2600 ／ ② 火力差异只登记口径（船长 2026-09-11：「低威胁卡敌人射程延长到 2600，其他不做更改。火力只登记口径」；二号）**——**①落码**：`foe-ships.ts` 海盗快艇 `rangeMaxM` **1883 → 2600**（船长给定值；原值 = 2215 ×0.85 的"射程 −15%"结果），并**撤下碎晶带劫匪通缉两处 `rangeMul: 2273/2215`**（统一走舰级带 ⇒ 该卡快艇射程带 = **1~2600**，不再走 1.02619 微调）——**只动这两处，其它一律未改**（血量/单发/装填/编成/波次/战术/威胁/奖金全不动）。**实测（A0~A3 · 5 播种，新 falloff 口径）**：边境 A0 **18s → 9s**（我开火 8.6 → 4.4）、A1 9s → **5s**；碎晶 A0 **23s → 14s**（我开火 11.0 → 6.8）、A1 19s → 15s；**两卡 A0/A3 的"敌开火"仍是 0.0**（⚠ 如实报：本改动把敌军**能开火的距离**从 1.9 km 推到 2.6 km，但参考行的战斗在 ~3.2 km 就结束 ⇒ **"敌人一炮未放"的形态没被治好**；要治需开战距离或先手，船长本轮裁「其他不做更改」故未动）。**②②登记**：词典新增「**纸面单发 vs 实收输出**」口径条（含换算关系与"难度守恒按实收判定"的既有口径），**不重标任何数值**。**验证**：typecheck ×4 + core **88 文件 / 866 例** + `content:check` + desktop build 全绿；`battle:calibrate --std` 复跑两卡。**⚠ 读数文档仍待整批重标**（本批与上一批 falloff 同步共同导致 A/B 各文档读数过期，见上一条 ★）。**改动全在 d2 工作树，未合入 main、未推送。****【待验收 · 未合入 main】**
