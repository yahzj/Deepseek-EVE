# 封存卷 · roadmap 批次条目 2026-09-12

> **冻结件**（2026-09-12 当日批次条目）：从 `docs/roadmap.md` **原文搬运、一字未改**（含船长原话照抄、逐问逐答、负向验证与读数）。
> 封存日期 **2026-09-15**（三号 · verify40）· 搬运时原行号区间 2026-09-12 当日条目。
> **检索**：本目录内 grep，或 `git log -S "<关键词>" -- docs/roadmap.md`（历史版本随时可翻）。
> **不要在本卷里追加新内容**——新批次写进 `docs/roadmap.md` 的滚动窗口，越窗后由 `npm run docs:seal` 封存进新卷。

---
- 2026-09-12：**低安运输批：长途运输报酬重标（安全档 × 距离指数）+ 遇袭后自动维修（先修后判）（船长七条裁定 · 二号 · d2）**——**A 长途运输报酬**：船长原话「高安 0.5／中安 0.75／低安 1」＋「保持母港 ⇄ 烬火前哨站 648,000/h，其他按比例削弱」＋「基于距离有一个指数增幅，让 1+1<2」＋「按你推荐的 1.5 来执行」⇒ **单段报酬 = 货仓 × 0.6 × 锚定标称(10) × (有效距离 ÷ 7.75)^1.5 × 行情倍率**；**有效距离** = 沿最短路**逐跳**累加「该跳分钟 × 该跳两端里**安全等级较低（更危险）**那一端的系数」（档位走 `securityZoneOf` 单点）；**航程时间一字未动**、**存档零迁移**。**读数（皇带鱼 36,000 m³ · 行情均值 ×7.5）**：母港⇄烬火 **648,000/h（锚，逐字不变）** · 母港⇄红环 **409,600/h（−37%）** · 红环⇄烬火 **520,200/h**；**超可加**：拆成「母港⇄红环＋红环⇄烬火」只值整段的 **68%**（旧线性口径 97%，且旧口径下低安短线是全表最赚）。落点 `hauling.ts`（`haulEffectiveMinutes`/`haulSecurityMulOf`/新 `haulBaseReward`）· `travel.ts` 新增 **`shortestTravelPath`**（与 `shortestTravelMinutes` **同源同并列取法**，避免路径与分钟两算）· `balance/types` 新增 `haul` 段（锚定常数，漂移由真内容用例拦）· 面板改走有效距离但**不显示档位与系数**（船长定）。⚠ **旧条款作废**：词典「长途运输·时间与行情倍率」条末句「仍与航线长短无关（不挑短线刷钱）」**标注作废**（各线时薪不再相等；但被削的正是安全短线，**仍不存在挑短线刷钱**）。**B 遇袭后自动维修**：船长「因为打算让长途运输会遇袭，所以我需要修改自动维修规则」＋「**先维修，组件不足或者修完后结构 <50% 返港**」＋「用组件，**包括仓库组件**」⇒ 五路收场（击退/受损/被抢/自动脱离/应战胜负）**共用** `settleEncounterTail`：① 先就地用修理组件补到装甲/结构 ≥0.6 或**组件耗尽**（**货舱优先→仓库兜底**、取件序民用→军用）；② **组件耗尽**或**补完结构仍 <50%** 才收手返港待命（主控停作业+即时返航；副船中止任务召回）。**覆盖主控 + AI 副船**（`shipyard.repairWithKitsFor` 逐船单点；重复清剿口径**一字未改**，仍只读货舱；副船「失利且耐久≤30% → 回港花 ISK 修满」保留为兜底）。⚠ **旧条款作废**：2026-09-11 的「**不自动维修**」＋「结构<50% 一律撤退」两条**由船长改判作废**（词典 259 条 + `b1-lowsec-encounters.md` 均已标注）⇒ **只要组件够，被伏击的船不再自动撤退**。**C 运输遇袭保留、只改文案**：实测根因 = `hauling.setLeg` 把 `awayGalaxy` 记为出发星系、而暴露面排除清单未含运输 ⇒ 出发港在低安（烬火星区 sec 0.0）的运输航段会掷伏击（5 游戏日 8 条遭遇、装甲 94%；对照停港不跑运输 = 0 条）；船长裁定**保留行为**，手册「什么会遇袭」/「打捞残骸」、通讯 `msg-lowsec-rules`、首次入低安提示四处文案已改为「**长途运输途中同样会**」（**公告历史条目不回改**）。**验证**：typecheck 四包 ✅ · core 全绿（新增 `tests/haul-security-reward.test.ts` 5 例真内容 + `b1` +4 例）· `content:check` ✅ · desktop build ✅；**负向验证两条真红**（距离指数改回 1.0 ⇒ 超可加红；去掉先修 ⇒ 收场用例红）。**文档**：新增 `docs/design/haul-security-and-ambush-repair-20260912.md`（已确认并实现）＋ 词典两条新增/两条改写。**不做**：航程时间/行情倍率/费率、暴露面、伤害模型、ISK 停站维修、重复清剿口径、历史公告。【待验收】

- 2026-09-12：**P0 结案：采矿船「货仓没满就返航」＝AI 副船满舱判定漏了三条技能乘链（船长「按方案来（主修单点化 + 四处文案）」· 二号 · d2）**——**①根因**：`ai.ts` 原 `freeCargoFor` 是 `inventory.cargoCapacityM3Of` 的**手写副本**，只算 `船体 cargoM3 ×(1+货舱件)`，**漏掉深空物流学(+4%/级)/货舱管理学(+3%/级)/货舰操作(+5%/级)** ⇒ 技能越练、AI 副船越早"喊满"返航，日志却写「货仓已满」（全仓 `cargoM3 × (1+bonus)` 形态只有 `inventory.ts:138` 正本与 `ai.ts:871` 副本两处）。**②复现读数（真引擎 · AI 副船采矿 · 驾驶船沙猫）**：**皇带鱼级旗舰货舰**（货舰三技能全 5）返航只装 **35991/62100 ＝ 57.96%** · **鲸吞级采矿艇**（管理5＋物流5）**4488/6210 ＝ 72.27%** ·（仅管理5）86.72% ·（0 技能）99.73% ⇒ 修复后同配置 **62049（99.92%）/ 6188（99.65%）/ 5168（99.86%）/ 4488（不变）**。**③主控路径已排除**：满舱判定走 `freeCargoM3 → cargoCapacityM3Of`（技能算全），27 艘船 × 3 档实测剩余占比 **中位 0.11%~0.17%、最大 1.02%**；另三条"没满就返航"路径（教学首单沙猫 50/800 ＝ 6.25% · 换驾驶善后 · 低安遇袭结构<50% 撤退）**均设计内且文案诚实**。**④改动（单点化）**：`inventory.ts` 新增 **`freeCargoM3Of`**（容量−占用，**满舱判定唯一口径**）· `freeCargoM3` 委托它 · `index.ts` 导出；`ai.ts` **删除 `freeCargoFor`**（连带其 `familyModules` 导入）⇒ 采矿与打捞判定两处改走单点。**⑤文案（甲案 · 零平衡影响）**：五处「货仓/货舱已满」→「**货舱装不下下一循环/下一轮（余 X m³ ／ 每循环 Y m³）**」（主控采矿 2 · AI 采矿 1 · AI 打捞 1 · 主控打捞 1）。**⑥行为变化**：仅 AI 副船（采矿＋打捞）每趟多带（矿船 ≤+38%、货舰 ≤+72.5%），**每趟用时同步变长**（产出速率不变）；**主控与 0 技能档零变化**；无存档字段 ⇒ **零迁移**。**⑦验证**：typecheck 四包 ✅ · core **106 文件 / 1060 用例**全绿（+1 例：货舱管理 5 ⇒ AI 船装 115 而非 100）· `content:check` ✅ · desktop build ✅；**负向验证真红**（临时还原漏技能口径 ⇒ 用例 `expected 100 to be 115`，还原后全过）；探针 `tools/_p0-mining-recon.ts` 用完即删（`tools/_*` 残留 0）。**⑧文档**：新增 `docs/design/p0-ai-cargo-capacity-20260912.md`（**已确认**）＋ 词典「满仓自动返港」条补判定口径。**⑨本批不做（等船长单独定）**：乙案（拆零循环真正填满＝改产出节奏）· 丙案（教学首单取消特判）· **富矿脉 ×3 顶穿货仓**（沙猫实测 810/800 ＝ 101.25%，触发窗口苛刻）。【待验收】

- 2026-09-12：**收档 + 交接卡：二号工作树会话收尾（船长「写交接卡，准备进行更新交接」· 二号）**——新增 `docs/design/handoff-20260912-pilot2-wrapup.md`（§0 快照 / §1 本会话 13 条交付线 / §2 接手必读口径 10 条 / §3 未决项 7 项 / §4 落点索引 / §5 血泪清单 8 条 / §6 交接三件事）。**当前基线**：`main` = `d2/workspace` = `80d9ad9e`（两树同点 · 内容差异 0 · 均干净）· 四连全绿（typecheck 四包 · core **105 文件 / 1050 用例** · `content:check` ✅ · desktop build ✅）· `_` 探针 **0** · **未推送 14 条**。**P0 未决**：玩家反馈「采矿船货仓没满就返航」已**代码级定位三条路径**（`mining.ts:422` 满舱检查判定＝"装不下下一个完整循环"、**日志却写「货舱已满」**· `mining.ts:473` 教学首单采足 50 即返航 · 换驾驶善后；AI 副船同款判定见 `ai.ts:703`/`851`）——**等船长要玩家的日志原文**定性后再出方案（甲 修日志用词 / 乙 允许部分循环 / 丙 教学取消特判）。【待验收】

- 2026-09-12：**三号会话交接卡（更新交接 · 船长「写交接卡，准备进行更新交接」）**——新增 `docs/design/handoff-20260912-verify-wrapup.md`：**①快照**（`verify` = `main` = `7babb474` · 四连全绿 core **106 文件 / 1055 用例** · 工作区干净 · `_` 探针 0 · 本地产物 `index-CUGF7j5N.js` @19:42 · `origin/main` = `6bb692e1`，**领先 17 条未推**）；**②本会话交付线 11 项逐条附提交号**（斜向菱形 `2c5feb3d` · 后备机库徽标 `0cddbcf6`/`1b99d60f` · 稀有残骸进星系详情 `35116391` · 近防炮丁案+削血 `905302d7` · 对无人机 ×2 `dfe6fb0e` · 删航行下限 `b4a58213` · 防空属性+图鉴 `bf7c56df` · 「90」改判→回滚 `503a3761`→`32d00621`→`529f56a0` · 销档处理 `6e48d426` · 公告口径终结 `7acdcc48` · 工具转正 `8c378e7a`）；**③接手必读口径 8 条**（防空 = 一条属性两义 · 打机群命中口径与"实收/基础"标注纪律 · 警戒机 55 血 · **航行无下限且字段已删** · 体检契约编号 ⑤a~⑤g、新增从 ⑤h 起 · 图鉴间接属性 · **销档不重新入账** · 推送闸门）；**④未决项 3 条**（G 蜂群机闪避 0.45 · 大而慢货舰时薪 +16% · 一号/二号在办项指向各自交接卡）；**⑤血泪清单 8 条**（`git checkout --` 丢未暂存改动 · PowerShell 写中文毁文件 · `edit` 跨行连带删 · fx 48 条环致断言假失败 · 中文输出乱码≠文件坏 · 对照实验前提要复核 · 汇总行别撒谎 · 负向验证必须真红）；**⑥推送与更新状态**（未推 17 条 · GitHub 时通时断的密 retry 法与 `ls-remote` 核对法 · 推后照例写回执）。【交接件】

- 2026-09-12：**近防炮「防空」属性并成一条 + 手册图鉴显示舰船间接属性（船长两条裁定 · 三号）**——**①船长原话**：「给近防炮系列添加一个属性"防空"，将近防炮的对无人机伤害 ×2 写到防空属性里。然后手册图鉴里的舰船信息可以查看舰船的间接属性」。**②属性合并（一条属性两个含义）**：装备表 `ModuleDef` 的 `canHitDrones: true`（布尔）与 `antiDroneDmgMul`（倍率）**两字段并入 `antiDrone`**（**值 = 对无人机伤害倍率**）⇒ ①**能筛到敌方机群** ②**对无人机伤害 ×该值**；建档带进 `WeaponSpec.canHitDrones + antiDroneMul`（同源），开火链"打机群"分支乘该值，**对舰伤害一字不动**。三档均 `antiDrone: 2`。**③界面**：`moduleInfoLines` 新增一行「**防空** · 能打敌方机群　对无人机伤害 ×2」（装配页 / 装备库 / 货舱 / 市场 / 手册图鉴同一数据源）；装备说明文里重复的「对无人机伤害 ×2」**删除**（数字改由属性行承载）。**④手册图鉴**：`shipIndirectLines`（最大速度 / 跃迁速度 / 质量 / 锁定范围 / 信号半径 / 扫描分辨率 / 跃迁充能）原先**仅装配页**显示，现**图鉴·舰船详情与舰船蓝图详情也列出**（同一数据源、`k` 作 React key 故要求两函数键不重名——当前无重名）；**悬停浮层仍不显示**（空间有限，保持克制）。**⑤验证**：typecheck ×4 绿 · core **1048/1048**（105 文件）· content:check 绿（契约①文案与过滤条件同步改为 `antiDrone`、①b 新增"一条属性两个含义"说明、汇总行改「防空属性 ×2」）· desktop build 绿；**负向验证**（把防空值改成 1 ⇒ content:check 与用例如实报错，还原后全过）；`_` 探针 0 个。**⑥文档**：词典「防空（属性）」条改写为"一条属性两个含义" + 新增「舰船间接属性（图鉴可见）」条；设计稿 §六·补2③ 改写（字段合并 + 界面行 + 对照件口径）；`shipInfo.tsx` 头注释同步。**⑦实现细节（供复核）**：属性合并后，行为对照件（`foe-drone.test.ts` 的"无加成"组）**不能再用"摘掉属性"**——那样连"能打机群"都没了、两次跑的开火次数就不同；改为**把值降为 1**（引擎对 `antiDroneMul === 1` 不乘）⇒ 仍能打机群、只是不吃加成。【待验收】

- 2026-09-12：**船长两裁定：①无人机船配装基准（主武器口径收口）②巡洋舰 vs 驱逐舰血量实测（二号 · d2）**——**①无人机船至少要带一件"加射程的高槽装备"**（船长就"主武器口径"三选一给出的答案 ⇒ **维持现行主武器口径**，不加"排除无人机"的特例）：落点 = `drone-relay` 家族（无人机中继天线 MK1/MK2/MK3 = **+20% / +45% / +80%**，CPU 6/16/46；窝点件「流亡中继桅」+65%）。**为什么这条是必要的**：**无人机条目本身就是武器**（`src:'drone'` 也进 `mainWeaponOf` 比较）⇒ 其射程直接决定主武器带与期望交距：雷鸥哨戒机 `200~5000` ⇒ 中距 **2,600m**；挂中继 MK3 ⇒ 带变 `200~9000` ⇒ 中距 **4,600m**。**已并入词典「主武器（战术距离口径）」条**（并注明它同时闭环了 pd-tune 三份对账：3.2 架 = 旧口径回落基础舰炮 1,250m，2.2 架 = 新口径 2,600m，A/B 实测精确复现）。**②巡洋舰 vs 驱逐舰血量（船长实测观察："似乎没有多少血量优势"）—— 实测证实，且玩家的档位阶梯明显平于敌人**：**主战船（armed）**：T1 虎鲨 243 / 鲣鱼 126 → **T2 灰鲭鲨 366 · 梭鱼 378 · 大白鲨 396（中位 378）** → **T3 长尾鲨 438 · 王鲭 441 · 电鳐 456 · 锤头鲨 474 · 牛鲨 510（中位 456）** ⇒ **T3/T2 中位 = 1.21×**，单船对照 **灰鲭鲨 366 → 锤头鲨 474 = 1.30×**；而**价格**是 **48 万 → 1,100 万 = 23×**（牛鲨 1,300 万 = 27×）。**重装线更陡**：陆龟（T2）333 → 玳瑁（T3）540 = 1.62× · 玄武（T4）783。**与敌人对比（关键）**：敌舰档基线 = T1 260 / T2 480 / T3 900 / T4 1,600 / T5 2,800 ⇒ **每档 ×1.85**（T3/T2 = 1.875×），而**玩家 armed 中位每档只 ×1.21~1.56** ⇒ **越到高档玩家越吃亏**——这正是此前"E 段换大船本身不解决生存、只有抗性选择才是主旋钮"的根因（玩家血量阶梯追不上敌人）。**③待船长决定**：要不要**重排玩家舰船的档位血量阶梯**（把 T3 拉近敌人的 1.85× 口径，例如让巡洋中位从 456 抬到 ~660，或改为"给 T3/T4 加结构性优势：槽位/CPU/盾甲比"而非总血）——属**数值体系改动**，按四步闸门需先出方案；本轮**未动任何数值**。**④验证**：纯文档批（探针 `tools/_hull-recon.ts` 用完即删，`tools/_*` 残留 0），四连无回归。【待验收 · 血量阶梯待裁】

- 2026-09-12：**P-41 三份读数对账结案：真因＝「主武器口径」把玩家期望交距推过了 2,500m 的一道门槛（二号 · d2）**——**①三份数字全部自洽**：**0.0 架**（P-41 改前：近防炮打不掉）→ **3.2 架**（敌方近防炮改造当日 `cc86049c`，当时主武器口径仍是旧规则）→ **2.2 架**（今日复跑，主武器口径已是 `416e752` 的新规则）。**②先排除两处**：**(a)** 工具配置**逐字未变**（`SHIP`/`LOAD`/`CARDS`/`SEEDS` 自建立起从未被任何提交改过）⇒ **不是口径漂移**；**(b)** **机巢容量不是原因**（实测王鲭 320 m³ ＋ 2× 甲板扩展 MK3 140 m³ ＝ **460** ≥ 所需 **320**（heavy 20×4 ＋ sentry 40×6）⇒ 10 架全装得下）。**③探针实测（复刻 pd-tune 同配置）**：**主武器 = 雷鸥哨戒无人机**（`[drone] 200~5000`）——`mainWeaponOf` 按"射程最远"选，而**无人机条目射程 5,000m ＞ 舰炮 2,500m** ⇒ 对纯无人机船，**主武器实际是无人机** ⇒ **期望交距 = 带中点 2,600m**，而敌方近防炮打哨戒机的门槛（`PD_SENTRY_RANGE_M`）是 **2,500m** ⇒ **只差 100m 就跨过那道门**。**④A/B 反证（决定性 · 只改"期望交距"、其余逐字不变 · 噬口 80 × 5 种子）**：`2,600m`（现状）⇒ **2.2 架/场**｜`2,400m`（刚进门内）⇒ **4.2 架/场**｜**`1,250m`（旧口径＝基础舰炮 `0~2500` 的带中点）⇒ **3.2 架/场**——**精确复现台账记录的 3.2**。**⑤结论（判为"隐式耦合"，非缺陷）**：一号 `416e752` 改「主武器＝射程最远的武器」本意是修三按钮/射程带口径，**连带把近防炮的实战杀伤改了**（纯无人机船的期望交距从 1,250m 抬到 2,600m ⇒ 哨戒机过半时间在门槛外 ⇒ 战损 −1 架/场）。**⑥待船长一句话（三选一）**：**(a)** 接受现状（无人机的射程当主武器 ⇒ 纯无人机船走中距 2,600m）；**(b)** 明确「**主武器只认舰载武器**（排除 `src='drone'`）」⇒ 纯无人机船回落到基础舰炮 1,250m、哨戒机全程挨打（战损回 3.2~4.2）；**(c)** 给无人机船单独定口径。⚠ 另记一条**设计气味**：**100m 的带差决定 1 架/场**（2,600 vs 2,500），说明"哨戒机是否可选"这条判定对交距**极敏感**——若日后还要动主武器/无人机射程，请连这条一起看。**⑦过程**：探针 `tools/_pd-recon.ts` 用完即删（`tools/_*` 残留 0）；工作区干净。【待验收 · 待船长裁决口径】

- 2026-09-12：**工具更新四批（船长「先更新工具，工具已经落后很多版本」）—— 机械化体检 + 四条更新全落（二号 · d2）**——**①体检**：把 26 个工具里的"内容 id 字面量"与当前数据逐个对照 ⇒ **0 处断链**（命中 47 处全是误报：单位 tag `foe-0`、图标名 `nav-*`/`ico-*`、槽位名、目录路径）⇒ **落后形态是"口径与能力落后"，不是引用失效**；按最后改动时间看 **14/26 个工具停在 09-04~09-10**，而内容在 09-11~09-12 动得最猛。**②批 1 · 旧文案收口**：`make-test-save` 三处旧族名（`G 烬火流亡·天底封锁军` 与两处 `G 流亡`）→ **鱿烬亡军 / 鱿烬**（这是**活生成器**里的旧文案；文档里的同名残留属"历史条目不回改"、正确保留）。**③批 2 · `battle-calibrate` 现代化**：根因 = **全表 27/27 张卡已迁舰级路径** ⇒ 旧路径提案字段 `foeHpOverride`/`foeSpeedMps` 对真卡**一律静默空转**（`--proposal` 读数一格不变 ⇒ 极易被误读成"提案无效"＝**假结论**）。现按卡的**实际路径**落字段：舰级路径卡 ⇒ **卡侧 `hpMul`/`speedMul`**（按比例反算；⚠ 同一舰级常被多张卡共用，故**只动卡侧倍率、绝不动舰级本体**），旧路径卡保留回退；并加**口径自报行**。**实测**：`--solve-hp` 由"27 张卡全空转"变为**真解**——灰霾伏击团 28「现血 430 → **解出 9859**（落卡侧 hpMul；189s / 残血 46%）」· 赤潮劫掠舰队 34「现血 340 → **解出 6120**」，27/27 行都标出落字段。**④批 3 · 测试档注入器补齐**：新增 **`gswarm`（G 族等离子蜂群验收档）**——**四艘同型对照船**（T3 锤头鲨，**只差抗性系与武器**：动能抗 / 等离子抗 / 双抗折中 / 4×近防炮向）+ 声望 13 + 全星系 + 中位技能 + 弹药与 MK3 备件；理由 = 机群只挂**单张卡**（天底静区封锁 66），**只有同型对照**才能把"抗性选择"从船型/火力差里分出来。已按工具纪律**三处登记**（`INJECTORS` + 工具头注释注册表 + `docs/test-saves/README.md` 档案清单），存档 `test-save-gswarm-20260912-164955.json`。**⑤批 4 · `playthrough-sim` AI 升级（判为"部分达成"，如实登记）**：三处改动——**(a)** 新增 `bestCombatShipUid()`/`ensureFlagship()`：**主力战船优先给驾驶位**（在 AI 出勤就**召回**），`doBounty`/`doFarm` 出征前调用；**(b)** AI 副船派发**排除主力战船**（不许把唯一能打的船派去挖矿）；**(c)** 新增 `counterFitFor()`：按目标卡混伤构成换抗性件（沿用混伤批「**盾抗主系 · 甲抗副系**」口径，缺件按市场价买）。**另修一处指标自相矛盾**：终局受阻的"顶配达成"判据旧口径只要求"驾驶船 role=armed + ≥3 武器"，而 debugQuick 下技能秒级毕业 ⇒ **0.1d 就登记"顶配"**、随后 10 天对着打不过的 boss 空转（报告还写"顶配武装（**沙猫级采矿艇**×1 门，自 **0.1d** 达成）"，与事实相反）⇒ 现要求**主力战斗船真的在驾驶位** + **每次换到更强的船重新计时**。**实测（三次复跑）**：顶配登记 **0.1d → 1.3~3.5d**（与牛鲨级突击巡洋舰到手日吻合）、跑得更远（10.06 → **11.3~13.5 天**）、声望 15 → **25**；⚠ **但仍未通关**：黑洞仍在 11~13 天触发，且触发时驾驶位**又回到采矿艇** ⇒ 批 4 判为**部分达成**——指标已与事实对齐、战船确实更早到手，但"AI 长期守住主力战船 + 真破穹顶"需要**更大范围的舰船-角色纪律重构**，属**平衡复核（C4）**范畴，另行立项。**⑥验证**：typecheck 四包 0 错 · core **105 文件 / 1044 用例**全绿 · content:check ✅ · desktop build ✅；`tools/_*` 探针 **0**。【待验收】

- 2026-09-12：**全流程跑通（船长「进行一次全流程跑通」）→ 两个卡关修复 + 追平主树（二号 · d2）**——**①追平**：`main` 合进 d2（落后 7 条），仅 `docs/roadmap.md` 冲突 ⇒ **d2 侧并集解**（我方两条在前、一号「后备机库」两条随后），只删 3 行标记；三条自检全过（BOM=False ＋ 全 CRLF · 全仓文本往返解码乱码 **0** · **与两侧父提交逐行比对各缺 0 行**：d2 侧 579 / main 侧 582）。**②卡关 1（`playthrough-sim` 崩溃）已修**：根因 = `familyBest('armor', armorHpBonus)` 取到**窝点专属装备** `mod-lair-armor-d`（陵寝装甲层，`armorHpBonus 1.1` ＝ 全表最高），而该类装备按「来源唯一契约」**本就没有市场卡**，`tryOne` 却用 **`!` 非空断言**取 `.key` ⇒ `TypeError`（调用方反而已用 `?.` 兜底价格，口径本就不一致）。**时间线**：该件 **2026-09-10** 引入（`089d8179` D 族三件），而工具最后一次改动是 **2026-09-09**（最后留档跑通 `acceptance-boss-20260909`）⇒ **这条验收链自 09-10 起就断了，与今日改动无关**。**修法**（船长「按你说的来」＝甲+乙）：`familyBest` **前置过滤"必须有市场卡"** ＋ `tryOne` 改用 `goodOf` 并**买不到就跳过**；调用方的价格查询一并统一到 `goodOf` 口径。**改后实测**：**不再崩、34 秒跑完 10.06 游戏天并输出完整报告**（此前 exit 1 / 17 行堆栈）。⚠ **但它仍未通关**：AI 自 0.1d 起"顶配武装"始终是**沙猫级采矿艇 ×2 门**（1.06d 曾换驾牛鲨级突击巡洋舰、随后又回到采矿艇），对**穹顶守卫 96 胜率 0%** ⇒ 触发工具自带的「**策略黑洞（终局受阻）**」提前终止，并自判"**疑似平衡项，转 C4 复核**"。**判读（如实说）**：穹顶 0% 与**已验收口径完全一致**——std 表四行（A0/A1/A2/A3）对穹顶守卫**全是 0%**、D 族本就是船长实测确认的「**需要针对性装配的墙**」⇒ 这是**工具目标 vs 平衡口径不匹配**（naive AI 不会针对性配抗性），**不是引擎缺陷**；出路三条：(a) 接受"策略黑洞"为该工具的正常产出 (b) 把 goal 的终局目标改到标准行打得过的档 (c) 给 AI 加针对性配装逻辑。**③卡关 2（`content:import modules` 整表被拒）已修**：根因 = 工作台 `tools/content-schema.ts` 的 slot 枚举**手写 12 个**，而引擎 `ModuleSlot`（`packages/core/src/types.ts:748-764`）有 **15** 个（缺 `cpu` / `drone-relay` / `target-lock`）⇒ 10 行报"非法枚举"、**整表拒绝写入**。**修法**：表头与 `vals` **一起由引擎单点派生**（`MODULE_SLOTS` ＋ `SLOT_LABELS`；`rack` 同改走 `RACK_SLOTS`），并新增体检契约「**内容工作台契约**」（schema 枚举必须 ⊆ 引擎值域、非空、无重复）。⚠ **表头文字有变**（改用引擎官方中文名）⇒ 旧 CSV/xlsx 该列会失配，**导入前须先 `npm run content:export` 重新生成**（工具已有"忽略未知表头列"的提示，不静默）。**改后实测**：7 表往返 `--dry-run` **全部 exit 0「无差异」**（此前 modules 表 exit 1）。**④对账（P-41 三份读数 · 0.0 / 3.2 / 2.2 架）**：已排除两处——(a) **工具配置逐字未变**（`SHIP`/`LOAD`/`CARDS`/`SEEDS` 自建立起从未被任何提交改过）⇒ **不是口径漂移**；(b) **机巢容量不是原因**（实测王鲭 320 m³ ＋ 2× 甲板扩展 MK3 140 m³ ＝ **460** ≥ 所需 **320**（heavy 20×4 ＋ sentry 40×6）⇒ 10 架全装得下、前后一致）。**剩余最可能的机制** = `416e752`「主武器口径改按**射程最远的武器**」改变了玩家期望交距 ⇒ 噬口实际交距变 ⇒ 敌方近防炮打哨戒机的**许可窗口**变（哨戒机须在 2,500m 内才可被打）⇒ 战损 3.2 → 2.2、中位秒 22s → 26s；**待一次性探针确认（打印两处交距），未擅改工具读数行**。**⑤全流程跑通总账**：26 个工具跑了 **24** 个（另 2 个：`content-schema` 是库非 CLI、`make-icon.ps1` 会覆写 `build/icon.*` 未跑）——基础四连 ＋ 网页 build ＋ 经济 7 ＋ 战斗 4（含 `--std` 全表 108 格）＋ 分析 5 ＋ 工作台 7 表 ＋ 真机四场景 ＋ 测试档流水线 **全绿**；真机端到端（Electron 生产渲染）四场景 fps 均值 **60.0 / 60.0 / 60.0 / 59.96** · 最低 **53.98** · **长任务 0** · 堆 12.8MB。**⑥过程留档（一处自家事故，已还原）**：首次解 roadmap 冲突时用 .NET 写盘踩了「**.NET 当前目录 ≠ PowerShell 的 location**」——相对路径读失败后**仍执行了写入**，把 `roadmap.md` 写成 **0 字节**；改用 `git checkout -m` 重建冲突态后重做，**内容零损失**（两侧父提交逐行核对各缺 0 行）。教训已并入 `development-conventions.md` §三。**⑦未做（本轮明确留出）**：天底换系后的**多配置试算**（船长「建议尝试更多配置」）—— 下一小批；pd-tune 交距确认探针。【待验收】

- 2026-09-12：**P-20a 收口：G 族等离子机型上舰（船长就三案选「丙」＝换系）（二号 · d2）**——**①船长裁决**：就我方出的三案（甲＝加架数 3 架 / 乙＝换系 exp+pla / **丙＝换系 kin+pla**）选**丙**。**②改动（一行数据 + 注释同步）**：`packages/data/src/foe-ships.ts` 的 `FOE_G_NADIR_LOCK.drones` 由 `kin ×1 ＋ exp ×1` 改为 **`kin ×1 ＋ pla ×1`**（`foe-drone-g-bee-pla` 由"已备未挂"转为**实挂**）；导入同步（`FOE_DRONE_G_BEE_EXP` → `FOE_DRONE_G_BEE_PLA`）；`foe-drones.ts` 三型注释标注挂载现状。**③读数（实建档 · 改前 → 改后）**：卡总血 **2,040 → 2,040** · 炮台 **114 → 114** · 机群 **24（12 动能 + 12 爆炸）→ 24（12 动能 + 12 等离子）** · 合计 **138 → 138** —— **总量逐字守恒**（条目锚点 80 ＋ 占比 0.30 的守恒拆分**只改构成**）。机型绝对值（射程 7,000 / 装填 2,200 / 命中 0.75 / 三层血 26）与三型美术**一字未改**；**无后备**（船长「无后备」）照旧。**④连锁面（如实登记）**：**(a)** 三种机型只有**两个机位** ⇒ **任何"换系"路线都必然空出一个**：本轮空出的是**爆炸机型 `-exp`**（转「已备未挂」；另两案同理——只有甲案 3 架才三系全挂）；爆炸系在本卡仍由**母舰炮台** `dmgMix { kinetic 8, explosive 2 }` 承担 ⇒ **卡整体三系齐备**。**(b)** 等离子**不会**变"光束必中"：机群是 `kind:'fixed'` 掷命中、走机型自己的 0.75 命中，等离子只改**层位克制**（拆盾 ×1.25 / 对甲·结构 ×1；对照 动能 盾 ×1.5·甲 ×0.5、爆炸 盾 ×0.5·甲 ×1.5）⇒ 机群多一系等离子 = **玩家没法靠"盾抗动能 ＋ 甲抗动能"一味堆抗**。**(c)** 机群血池 52 与架数 2 均**不变**（丙不是甲案 ⇒ 不增架、不增血）。**⑤契约（新增两处，防复发）**：`content-check`「机群与防空契约」新增 **⑤d G 族蜂群机挂载硬守卫**——G 族只要挂了机群，就**必须至少有一架是等离子系**（钉的是**交付物**而非具体组合，日后换组合不误伤；编号取 **⑤d** 而**不取 ⑥**——⑥ 已被「机群火力占比」占用且被设计稿引用）；并新增 **⑤e**「**已备未挂机型**」**预警行**（机型表里有、却无任何舰级引用 ⇒ 列出机型 id，把"漏挂"从**静默消失**变成体检输出里的一行字——这正是 P-20a 本身的教训）。**⑥用例**：`tests/foe-drone.test.ts` 新增 **2 例**（真卡 `ano-nadir-static`：构成 kin+pla 与逐架 12 · 射程/装填/命中照机型表 · 无备用机库；总火力 114+24=138 与总血 2,040 守恒）。**⑦负向验证（真实红→绿）**：把机群临时改成 `kin ×2`（＝等离子没挂上）⇒ 新用例**实测 1 红**、体检**点名报错**（「G 族有 1 条舰级挂了机群，但**没有一架是等离子系**」），且**汇总行按结果分支**打出「⚠ **无机型含等离子** —— 见上方错误」而**不是**假的"✓"（血泪清单：汇总行别撒谎）；还原后全绿。**⑧验证**：typecheck 四包 ✅ · core **105 文件 / 1044 用例全绿**（+2）· `content:check` ✅ 通过（+1 条预警：`-exp` 已备未挂）· desktop build ✅。**⑨文档**：G 族稿 §二/§六/§八/§10.1/§10.2/§10.3（含 §10.3 一处**旧读数订正**——天底行原写「138 → 110 + 28（机）」，与现状不符，改为「**114（炮）+ 24（机）= 138**」）· 待办总表 P-20a 行收口 · 词典 G 条两处。【待验收】

- 2026-09-12：**船长三条台账裁决落账：P-41 收口 · P-40 保留集火 · A2 清档（新二号接手第一批 · 纯台账，零代码零数据）**——**①P-41 收口**（船长「**P41已经完成**」）：机群批标定轮读数已固化 `docs/design/battle-data/p41-drone-readings-20260912.md`，挂着的三件事**按现状不再改**——(a) 敌方近防炮**维持改后读数**（`battle:pd-tune` 噬口猎杀令：每场平均损 **3.2 架**（全为哨戒机）· 战损估值 30,400 ISK · 占卡奖励 **3.6%**；坟场/穹顶仍 0 架）(b) E/G 机群火力占比**维持首版值**（G 族 **0.30**，船长同日已裁「G族无人机仅占30%」）(c) G「烬火围攻战 42 / 回音残舰 52」A2 远程风筝行 0 开火**维持不动**（船长同日已裁「承认短射程族在远距风筝下摸不到人」，属全局既有现象）。**②P-40 保留集火**（船长「**P40选择集火**」）：我方近防炮「**锁定一架打到死**」的集火口径（`BattleState.mePdFocus`）**确认保留**；行为改动的正式读数**不另跑**（沿船长「不跑标定除非说跑」口径，日后需要可单独立项）。**③A2 清档**（船长「**A2清档**」）：审计 A2 的三条收紧清单（①舰船/舰船蓝图价格「预警」升「阻断」②物品基础卖价加比例带契约③改单一真源派生）**从待办台账清出、不再挂账**；`v9-market.md` §十二 与词典「价格权威关系」条目的**技术留档正文保留**（真源/副本/护栏状态/基线读数仍是对照依据），仅把「本次明确不做」改写为**已清档**。**④同批在办**：**P-20a**（G 族等离子机型 `foe-drone-g-bee-pla` 挂载）已出三案待船长裁决——甲＝**加架数 3 架**（kin+exp+pla 各 1；逐架单发 12→**8**、机群血池 52→**78**）／乙＝**换系 exp+pla**／丙＝**换系 kin+pla**；**三案的卡总火力 138 · 炮台 114 · 卡总血 2,040 一律不动**（守恒拆分锚点在位）。**⑤落点**：`foe-pending-index-20260911.md` P-40/P-41 两行 · `v9-market.md` §十二 · `glossary.md`「价格权威关系」条 · 交接卡 §3。**⑥验证**：纯文档批，四连复跑无回归（typecheck 四包 · core **105 文件 / 1042 用例** · content:check ✅ · desktop build ✅）；文档三自检（**无 BOM + CRLF** 与改前一致 · 全仓往返解码乱码 **0** · 与父提交逐行无缺失）。【待验收】

- 2026-09-12：**公告口径终结：本批（敌族/机群/近防炮 + 删下限）不发公告（船长「不写公告。」· 三号）**——**①裁定**：船长今日已两次给出同一结论——早先「**B1，B2，清理剩余待审稿，不发。**」（见 `docs/design/announcement-draft-20260912-foe-overhaul.md` 顶部**已作废 · 不发布**声明），本次再答「**不写公告。**」⇒ **该待审稿永久作废、不写入 `packages/data/src/announcements.ts`**，**不得再请示**（与今日新入册的「销档条目不重新入账」同源）。**②三号自省（如实登记）**：我在两条推送回执里写「若需随本次更新发公告，请示下即可落库并再推一次」——**那是在重新请示一条已销档的事**（与今日「①消档」被纠正的毛病同型）；本条目把两处措辞**就地封死**。**③约定同步**：`development-conventions-changelog.md` 的「销档条目不重新入账」条补一句——**船长裁定"不发"的公告待审稿同样视同销档**（汇报里不得再出现"要不要发公告/可随时落库"这类开放措辞）。**④现状**：`announcements.ts` **未改**（仍只有已发布的旧公告）；本批更新**以代码为准**。

- 2026-09-12：**推送回执：`main` 已推 origin（`fb938ec7..7dd5d172`，6 个提交）（三号；船长「进行推送」）**——**①推送范围**：含三号 4 条（**删除航行时间因子下限**「船长：删除下限」· 近防炮命中口径**回滚**到闪避生效版 + 约定新增「修正链后的数一律标实收」· 那版一度改判的「无视距离的 90」及其 revert 一并入史 · 工具转正 `travel-matrix`）+ 一号 1 条（虫洞设计稿第二轮）+ 合并提交 1 条。**②推送波折（如实登记）**：GitHub 直连今日**时通时断**——先 3 次 `push` 全败（`Failed to connect to github.com:443 after ~21s`），TCP 443 探测 5 次里仅 1 次通；随后按"密retry"策略第 2 次成功（`fb938ec7..7dd5d172 main -> main`）。**③推送后核对**：`git ls-remote origin refs/heads/main` = **`7dd5d172…`**，与本地 `main` 同点，**领先/落后 0/0**（`ls-remote` 在 fetch 亦被网络挡下时仍可用，是此刻的权威核对手段）。**④推送前门禁**：typecheck ×4 绿 · core **1048/1048**（105 文件）· content:check 绿 · desktop build 绿；工作区两边干净、`_` 探针 0 个。**⑤公告口径（仍如实提醒）**：**只推代码**——敌族/机群/近防炮大更的公告待审稿 `docs/design/announcement-draft-20260912-foe-overhaul.md` **仍未写入** `packages/data/src/announcements.ts`（按 §7 须船长批准）；~~若要随本次更新发公告，请示下即可落库并再推一次。~~ ⇒ ⚠ **2026-09-12 船长「不写公告。」：本句作废**——本批**无公告**，该待审稿永久作废、不得再请示（见本条上方 2026-09-12 公告口径终结条目）。**⑥本地验收口径**：主树 `apps/desktop/out/` 已重建（`index-h6tpufYw.js`，含"删下限"后的航行换算）⇒ 重启本地那份即可对比同一航线两艘船的单程时长。【已推送】

- 2026-09-12：**删除航行时间因子下限（船长「删除下限」）——剑鱼级速度是皇带鱼级一倍以上却"长途时间相差不大"的真因（三号）**——**①船长原话**：「我还发现为什么剑鱼级跃迁速度是皇带鱼级速度的一倍以上。但是在长途运输中时间相差不大。」→ 取证后裁定「**删除下限**」。**②根因（真引擎实测）**：`travelTimeFactor` 把因子夹到 `Math.max(minFactor 0.35, …)`，而下限**只卡快船** ⇒ 航行族练到 **3 级**起，**飞鱼级(7.4) 与剑鱼级(6.2) 的单程时间完全相同**（都被抬到 0.35）；满技能时 剑鱼 ÷ 皇带鱼 的时长差从 **2.21× 被削到 1.41×**，且快运线时薪随技能从 0.86× 掉到 **0.55×**（越练越亏）。**③改动**：`travelTimeFactor` 的 clamp **删除**（直接返回乘积）；`balance.travel.minFactor` 与 `TravelBalance.minFactor` 字段**一并移除**（附起因注）；`hauling.ts` / `v12b-travel-warp.md` §一/§六 同步标注作废。**④删后读数**（最长标称航线 34′ ⇒ 运输航段 ×15 = 510′）：剑鱼 ÷ 皇带鱼 = **每个技能档都是 2.21×**；满技能（航行族 5 + 操控学 5）单程 = 皇带鱼 252′ / 剑鱼 114′ / 飞鱼 **95′**（因子 0.187）；时薪比固定 **0.86×**（不再随技能漂移）。**无"压没航程"风险**：最快组合仍保留 18.7% 时间，长途运输另有 ×15 航段倍率。**⑤仍未解决（另议）**：大而慢的货舰时薪仍高 16%（货仓 2.57× vs 时长 2.21×）——删下限后该比值**固定**，要不要动货仓/费率由船长定。**⑥验证**：typecheck ×4 绿 · core **1048/1048**（105 文件）· content:check 绿 · desktop build 绿；**负向验证**（把 0.35 加回去 ⇒ 新用例如实失败 0.35 ≠ 0.128，还原后全过）；**工具转正**：原 `_travel-probe.ts` → `tools/travel-matrix.ts`（`npm run travel:matrix`），仓库内 `_` 探针 **0** 个。**⑦文档**：词典新增「星图航行 · 航行因子（无下限版）」并更新「长途运输·时间与行情倍率」；设计稿 `v12b-travel-warp.md` §一 公式行标注作废 + 补删除说明。**⑧真档取证（只读）**：船长当前档航行族与操控学均为 0 ⇒ 该档内时长差本就完整体现（2.21×），本次改动对**无技能档零影响**。【待验收】

- 2026-09-12：**近防炮打机群命中：口径两度改判后回滚到「闪避生效版」（船长「哦 滚回到上一个闪避生效的版本，你也没说77命中是计算闪避后的啊」· 三号）**——**①经过**：上一版「丁案」= **无视距离、仍减机型闪避**（实收 0.72 / 0.45）；船长追问「不是 90 命中吗」⇒ 我改判为「无视距离的 90」（连闪避也不减，实收 0.90/0.92，带火控+索敌 ⇒ 100%）⇒ 船长随即裁定**回滚**，并指出**汇报疏漏**：我把**实收 77%**（已计入 G 族 0.45 闪避）当裸读数报出，未标明口径。**②处置**：`git revert 503a3761`（回滚提交 `32d00621`）⇒ **终态 = 无视两舰距离、机型闪避照常参与**；`combat.droneHitChance` 函数头补**口径沿革三条**（吃距离 ⇒ 去距离/减闪避 ⇒ 一度去闪避、已回滚），词典与设计稿同步改写，并写明**基础值 vs 实收值必须分开报**。**③终态读数（基础 → 实收）**：MK1/MK2 基础 **0.9** ⇒ 对 E 警戒机 **0.72** / G 蜂群机 **0.45**；MK3 基础 0.92 ⇒ 0.74 / 0.47；火控 5 级 ⇒ 0.855 / 0.585；再乘索敌 MK2（×1.12）⇒ **0.90 / 0.66**（上一版"闪避失效、G 族难打中性格消失"的说法**随回滚作废**）。**④未推送**：改判版**从未推 origin**（推送闸门未批），线上无痕。**⑤验证**：typecheck ×4 绿 · core **1047/1047**（105 文件）· content:check 绿 · desktop build 绿。【待验收】

- 2026-09-12：**推送回执：`main` 已推 origin（`e85604a6..6e48d426`，**10** 个提交）（三号；船长「①消档。然后进行合并和改动推送」）**——**①推送范围**：`origin/main` `e85604a6` → **`6e48d426`**，含三号 4 条（近防炮打机群「丁案」+ 警戒机削血 40%、对无人机伤害 ×2、临时探针转正式工具 `tools/pd-vs-foe-drone.ts`、销档处理与约定新增）+ 一号 2 条（舰队筛选对调、虫洞撤离设计稿）+ 此前一条推送回执。**②推送后核对**：`git ls-remote origin refs/heads/main` = `6e48d426…`，本地 `main` 同点，**领先/落后均 0**；`fetch` 后 `origin/main` 顶端一致。**③推送前门禁**：typecheck ×4 绿 · core **1047/1047**（105 文件）· `content:check` 绿 · desktop build 绿；五个改动文档编码自检（无 BOM / CRLF 一致）+ 全仓往返解码乱码 **0** 处。**④公告口径（如实提醒）**：本次**只推代码**——敌族/机群/近防炮大更的公告待审稿 `docs/design/announcement-draft-20260912-foe-overhaul.md` **仍未写入** `packages/data/src/announcements.ts`（按 §7 须船长审核批准后才落库），故推送内容**不含公告**；~~若需随本次更新发公告，请示下即可落库并再推一次。~~ ⇒ ⚠ **2026-09-12 船长「不写公告。」：本句作废**——本批**无公告**，该待审稿永久作废、不得再请示（见本条上方 2026-09-12 公告口径终结条目）。**⑤本地验收口径**：主树 `apps/desktop/out/` 本地产物已重建（`index-Cxl0ufvI.js`，含本批三条改动的实现与文案标记）⇒ 重启本地那份即可看。【已推送】

- 2026-09-12：**近防炮打机群两处修复：命中改判「丁案」+ 警戒机削血 40%（船长两条裁定 · 三号）**——**①船长原话**：「**按丁修复，近防炮给予一个对无人机伤害加成。**」＋「**将警戒机的血量削弱40%**」（起因 = 船长追问「**我方近防炮不是 90 命中率吗**」）。**②根因（真引擎实测）**：装备表写的 `hitRate 0.9` 是**零距离基础值**，实战走 `(基础命中 × 火控 × 距离衰减 − 机型闪避) × 索敌件`，而近防炮射程 **2,500m** 短于典型交距（3,211~5,545m）⇒ 距离衰减**恒落在下限 ×0.5** ⇒ 实战只剩 **0.27~0.35**；这与船长 2026-09-11 的裁定「**打机群不看两舰间距**」自相矛盾（选靶早已照办、命中还在按两舰间距打折）。**③落码**：新增 `combat.droneHitChance`（= `hitChance` 的距离衰减覆写为 1）并在玩家开火链**分两支**——打机群走它、**打舰一字未动**（"射程短所以对舰吃亏"的性格保留）；E 警戒机三层血 **28/20/44 = 92 → 17/12/26 = 55**（×0.6 逐层取整、合计 −40.2%；**抗性与闪避未动**）。**④读数**：打机群命中 0.9−0.18 = **0.72**（E）/ 0.45（G 蜂群机）；单门混装对泰坦巨构击落 **0 → 0.6**、两门 **0.8 → 1.8**、四门 **4.4 → 7.2**；机群池血泰坦卡 **1,288 → 770**、奥罗 2,760 → 1,650、核心 1,840 → 1,100。**⑤不影响常规难度（A/B 实测）**：标准复核四行 A0~A3（不带防空武器 ⇒ 打不到机群）**逐格读数完全相同**。**⑥验证**：typecheck ×4 绿 · core 全绿（含新增 2 例 + 修 1 例，见下）· content:check 绿（**新增契约⑤d：E 族机型三层血 = 55**）· desktop build 绿；**负向验证**两处（把丁案改回 ×0.5 ⇒ 新用例如实失败；把血量改回 92 ⇒ content:check 如实报 3 处错误）；`_` 探针 2 个（**待"加成幅度"定后一并转正式工具或删除**）。**⑦修掉一条假失败**：`foe-drone.test.ts` 原按"**最终 fx 环**里有没有 `pd` 事件"断言，而 fx 是 **48 条环、丢最旧**——机群被打光后剩下的全是"对舰开火"事件 ⇒ 早期 pd 事件被挤出环 ⇒ **假失败**；改为**逐秒推进 + seq 游标无损收集**。**⑧文档**：设计稿新增「§六·补2」；词典新增「近防炮打机群 · 命中口径（丁案）」「警戒机三层血（削 40%）」两条。**⑨加成幅度已裁**：船长「**那伤害倍率按2倍算**」⇒ 装备表新增 `ModuleDef.antiDroneDmgMul = 2`（三档同倍率）→ 建档带进 `WeaponSpec.antiDroneMul` → 在**打机群**那一支乘（**对舰伤害一字不动**）；契约 = content:check ⑤e，用例 = `pd-damage-ladder.test.ts` 2 例 + `foe-drone.test.ts`「对无人机伤害加成 ×2」（合成对照件同种子：机群掉血**恰好 ×2**、敌舰掉血**逐字相同**）；落码后最终读数：单门混装对泰坦击落 **2.6**（在空 7.0→5.4）· 单门 MK3 **4.4**（3.6）· 两门 **7.2**（1.6）· 四门 10.0（0.2）· 五门纯防空三卡基本清场；核心舱段单门 MK3 **9.2** 架、胜 100%。另：开火节奏（甲案「一次齐射换 K 炮」）**已销档**——船长 2026-09-12 终裁「①消档」（见续接卡 `handoff-20260912-to-new-pilot1.md` §7「挂账 0 条」），本批**不涉及、也不重新入账**；将来若要重启属**新裁定**。【待验收 · 已合入 main（`20e65bd7`）· 主树本地产物已重建】

- 2026-09-12：**后备机库改为「全队合计」（船长：追问「为什么会出现2次」→ 我给出三口径 → 船长选「乙」· 三号）**——**①起因**：船长看到 E 族「奥罗武装残骸群」上出现**两个相同的 `×5`** ⇒ 查清原因 = **备用机库是舰级字段（`FoeShipDef.droneReserve`）⇒ 每艘同型舰各带一间库**，而该卡是**主体 ×1 + 僚机 ×2 = 3 艘**（按现今阵形：第一排＝主体 + 僚机2、第二排＝僚机1）⇒ 同屏出现 3 个 `×5`（若其中一艘已阵亡则剩 2 个）。**②船长选定「乙：显示全队合计」** ⇒ 徽标**只挂一艘（主体）**、数字改为**全队合计**：奥罗 **×15**（5+5+5）· 泰坦残骸勘探 **×7** · 巨构核心勘探令 **×10**；悬浮说明改为「敌方全队机库备用机：前线战损后自动满血补位（打光母舰才是解法）」。**③实现**：`battleViewCore` 新增 `foeHangarTotal`（**先按舰去重再求和**——逐条累加会翻倍）；`BattleScreen` 用 `hangarCarrierTag`（主体优先、否则首个带库的舰）决定徽标挂谁；母舰阵亡后其备用机随池消失 ⇒ 合计自然变小。**④验证**：探针（已删）实测 —— 同舰两条同值 ⇒ 5（非 10）· 奥罗三舰 ⇒ 15 · **真实内容读数**泰坦 7 / 奥罗 15 / 核心 10 / **G 族（天底静区封锁，无后备）0**（不占位）；**负向验证**：把合计改成逐条累加 ⇒ 如实报「同舰同值 10（应 5）· 三舰 20（应 15）」，还原后通过；四连：typecheck ×4 绿 · core **1042/1042** 绿 · content:check 绿 · desktop build 绿；`_` 探针 0 个。**⑤文档**：词典「备用机库（敌机）」的**战斗画面显示口径**改为"全队合计 + 只挂主体"，并记下"每艘各一间库"这个易误读点；本条目。【待验收 · 待合入 main】

- 2026-09-12：**战斗画面「后备机库」改显示位置与形式（船长：「关于战斗画面的后备机库，将其显示在名字边上，采用图标乘以数字的形式」· 三号）**——**①原形态**：敌舰列**下方一行 chip**，每个机型一条「机库 +N」。**②新形态**：**跟在各敌舰名字右边**的徽标 ＝ **`drone-rack` 图标 ×N**（复用图标库现成字形，不新造；配色取 `ICO_TONES['drone-rack']`＝`#ffc46b`），悬浮说明沿用原文案（「机库备用机：前线战损后自动满血补位（打光母舰才是解法）」）；**第二排的舰名在血条标签里 ⇒ 徽标跟在那里**（`HpTri` 的 `label` 由 `string` 放宽为 `ReactNode`）；**无备用机不占位**；旧的列底 chip 行**删除**（样式 `.app-bts-hangar` 由"一行 flex"改为"内联小徽"）。**③口径坑（本批抓住并规避）**：引擎里 `hangar` 是**每舰一个**数值，却盖在该舰 `byArt` 的**每一条**机群条目上 ⇒ 界面若逐条累加会**翻倍**；故抽成纯函数 `foeHangarByTag`（**按舰取 max**）并在注释写明原因。**④验证**：探针（收尾已删）三条全过（同舰两条同值 ⇒ 3 而非 6 · 缺省/0 不出现 · 多舰独立）+ **负向验证**（改成累加 ⇒ 如实报「应为 3 得 6」「多舰读数不符」，还原后全过）；四连：typecheck ×4 绿 · core **1042/1042** 绿 · content:check 绿 · desktop build 绿；`_` 探针 0 个。**⑤文档**：词典「**备用机库（敌机）**」条目补**战斗画面显示口径**与那个翻倍坑；本条目。**⑥已知（未改，属引擎侧口径）**：`battleView` 把按舰的 `hangar` 重复盖在每条机型条目上（同舰挂两机型时同值出现两次）——界面已按"取一次"正确处理；是否把引擎侧改为"只挂在备用机所属机型那一条"由一号定（要改我另开小批）。【待验收 · 待合入 main】

- 2026-09-12：**敌列阵形改为「斜向菱形」（船长：「我想让敌舰排成 2X2 的菱形样式」→「斜向菱形，第二排向右偏移」→「我看了下 是丙」→ 选丙② · 三号）**——**①四步闸门**：集中提问（三种读法 甲/乙/丙 + 竖向两路）→ 船长选定 → 「确认开工」。**②口径（取代 2026-09-11 的"错列雁阵"，旧口径作废）**：按**视觉行序交替分两排**（第 1/3/5… 艘＝第一排；第 2/4/6… 艘＝第二排），同排内按列从左到右；**第二排右移半个列间距**（170 舰宽时 **87px**）、**下移一个舰高**（**78px**）；**第一排严格原位**（＝改动前逐像素）、**单舰编成只有一排 ⇒ 逐像素不变**。**③两条硬约束（决定数值只能这么取）**：**(a)** 舰体是实心的 ⇒ 两排水平错开 < 一舰宽必须竖向拉开 ≥ 一舰高（验算：右移 87＋竖向仅 54 ⇒ 横向重叠 83px、竖向重叠 24px，实打实叠住）；**(b)** 上方只有 `LAY.TOP`(54px) 留白（09-10 给无人机上凸弧留的）⇒ 第二排不能上抬 78（溢出被裁）故**往下**排——好处是第一排完全不动（船长 09-11 明确不喜欢"前排被顶下去"）。**④读数（贴脸）**：单舰 y93 不变 · 2 舰 764/93＋851/171 · 3 舰 717/93＋804/190＋851/111 · **4 舰（斜向菱形）677/93＋764/171＋851/93＋938/171** · 5 舰 590/677/764/851/938；**编队总宽 4 舰 431**（改前"一排 4 舰"是 692 ⇒ 窄 38%）。**⑤实现**：`battleViewCore` 新增 `foeFormationOf`/`foeColLeft`，`layout()` 改**网格**（两排 × 列）并返回 `formation`；`foeBarGeom` 入参由"抬升量"改为**逐舰真实舰底**（堆叠基准必须用真实舰底，**用抬升量会算高一整排压到第二排**——首次探针即抓到此坑）；DOM 改"外层定宽块＋两个 `.app-bts-shipRow`"（排高固定、第二排 `marginLeft = shift`、逐舰 `marginLeft = dx`），**删除**旧的 `top:-raise` 与 `foeSink`；旧常量 `RANK_STAGGER`/`RANK_BY_WAVE`/`foeRaiseOf` 一并删除（`core/foeShipEliteOf` 保留为只读查询）。**⑥验证**：几何探针（已删）全 **26 卡 × 5 距离 × 2 尺寸口径**自检 —— 总宽口径 / **舰体两两零重叠（660 对）** / 不溢出泳道顶 / 第一排逐像素不变 / 血条零重叠 全过；**DOM 实测**（真实 Chrome 引擎、关掉入场动画）：舰体 **170×78 未被挤压**、二排相对一排 **右移 87.0 / 下移 78.0**、舰体两两重叠 **0 对**、最高舰顶 y55.0 ≥ 0；并比过三种结构写法（只有"外层定宽块"给出恰好 87，另两种因列内居中只有 43.5 ⇒ 已排除）。四连：typecheck ×4 绿 · core **1033/1033** 绿 · content:check 绿 · desktop build 绿；`_` 探针 0 个。**⑦残留取舍**：编队向下多占一个舰高（窄窗可能裁第二排血条）· 尸骸收拢时幸存舰**可能换排**（与"整批演完一起收拢"同源）· 第二排浮空舰名改由血条标签承载。**⑧文档**：新增 `docs/design/ship-battle-art/battle-formation-diamond-20260912.md`；旧稿 `battle-formation-20260911.md` 顶部加**作废声明**；词典条目改写为「敌列阵形（斜向菱形）」；本条目。【待验收 · 待合入 main】

- 2026-09-12：**收尾归档（船长「进行收尾归档，然后合并」）**——本轮（**敌族 / 机群 / 近防炮**三条线）**全部落码并合入主树**：①新增**续接卡** `docs/design/handoff-20260912-foe-overhaul-wrapup.md`（当前状态 / 交付清单 8 项 / 接手必读关键口径 / 未决待办 / 关键落点）②新增**公告待审稿** `docs/design/announcement-draft-20260912-foe-overhaul.md`（4 条 + 1 备选要点；**未写入 `announcements.ts`**，等船长批准）③清理核对：`_` 探针 **0** 个 · 两侧工作区干净 · `main` 与 `d2/workspace` **同点**。**基线**：typecheck 四包 ✅ · core **97 文件 / 993 用例** ✅ · `content:check` ✅ 全绿 · desktop build ✅；文档自检（BOM 口径 / 全仓往返解码乱码 **0** / 合并父提交逐行覆盖零缺失）全过。**未推送**（等验收 + 公告审核后代码与公告一起推）。【待验收】

- 2026-09-12：**巨构核心勘探令降为 L1**（船长「**把核心卡降为 L1**」· 二号 · d2）——`ano-core-section.lairLevel` **3 → 1**；语义 = **档位上限**（日板发档在 `[1..级别]` 内均匀随机）⇒ 本卡**只出外围档**；派发上**不再顶掉同星系另外两张 L3**（奥罗荒环的日板/派系活跃**代表卡**改由 `泰坦残骸勘探 60` / `奥罗武装残骸群 62` 并列 L3 中**取奖金高者** ⇒ **62 上位**）。**⚠ 两点如实提醒**：①**84 那张（奖金 100 万）退出日板/派系活跃的代表位**，但它仍是奥罗荒环的**常驻悬赏**（可直飞去做）✓；②**L1 也意味着它不再有"核心档/深层档"派生**（派生 = 更硬的版本），与卡面注释原写「**E 族代表位**（族内最高级目标）」**正好相反** ⇒ 若您要的是"不顶掉别人、但保留深层版本"，改为「**保留 L3 + 改派发规则**」（同域按级分槽/轮换）即可，一句话我改。**读数**：窝点级别分布 **L1×6 / L2×6 / L3×8**（5 个敌族各至少一张 3 级 ✓）；**四连全绿**（typecheck 四包 · core 97 文件 993 用例 · content:check ✅ · desktop build）。【待验收】

- 2026-09-12：**巨构核心勘探令回档 L3 + 派发代表位改为随机抽取**（船长「**好像是我理解错误了，先回档为 L3。派发代表位改为随机抽取。**」· 二号 · d2）——**①回档**：`ano-core-section.lairLevel` **1 → 3**（当天曾按上一句「把核心卡降为 L1」改过，同日回档；卡面注释留过档说明）⇒ 窝点级别分布回到 **L1×5 / L2×6 / L3×9**。**②机制改动（本次真正的解法）**：**同星系"派发代表位"由"级别最高（并列取奖金最高）"改为「随机抽取」**，落点两处 —— (a) **赏金日板** `spawnBountyTasks`：同星系代表卡改**蓄水池抽样**（第 n 张以 `1/n` 概率替换当前代表 ⇒ **每张等概率**；旧口径由"级别 + 奖金"定死，低级别卡**永不入板**）(b) **派系活跃候选池** `factionPoolOf`：同星系由"取第一张（= 数据书写顺序）"改为**等概率抽一张**。两处都消费 `state.rng` ⇒ **确定性可复现**（同种子同结果）。**③用例**：`bounty-tasks.test.ts` 那条「代表卡取级别最高（级别低的永不入板）」按新口径改写为「**代表位随机抽取**——30 天里两张都出现过」（旧断言在新口径下必然失败）。**④验证**：typecheck 四包 ✅ · core **97 文件 / 993 用例全绿** ✅ · `content:check` ✅ · desktop build ✅。【待验收】

- 2026-09-12：**巨构核心勘探令降为 L1**（船长「**把核心卡降为 L1**」· 二号 · d2）—— ⚠ **本条已被同日下一条「回档 L3 + 代表位改随机」取代**（船长「好像是我理解错误了」）：保留作过档记录，口径以新条目为准。

- 2026-09-12：**E 族三卡全部集中到奥罗荒环 + 三特色池按 B3.1 规则重校**（船长「**将所有巨构移动到奥罗荒环**」· 二号 · d2）——**①落点**：`泰坦残骸勘探 60` 与 `巨构核心勘探令 84` 由**深渊之门**迁到**奥罗荒环**（`奥罗武装残骸群 62` 本就在那）⇒ **E 族三卡同域**；**深渊之门恢复为纯 C 族异形星系**（其 lore「古老跃迁门…守卫森严」**不涉巨构，故文案一字未改**）；奥罗荒环 lore「环状小行星废墟带，**旧战争留下的武装残骸**在此游荡」正好承接 ✓。**②连带红灯（已修）**：三卡集中后奥罗的**回收档基数**成为共同分母（**92.4**）⇒ 三个特色池均价越界（**150.00 / 37.50 / 160.35** vs 目标 **124.9 / 125.7 / 130.9**；连**没移动**的奥罗卡也红 ⇒ 证明该契约按**星系档**定目标）⇒ 按您 B3.1 的定价规则（`m = (1+0.45×max(0,−sec)) × (1+0.004×威胁)`、`池均价 = m × 档基数 ±3%`）**重校三个池**：泰坦 星髓晶37 : 同位聚晶63 = **125.3**（+0.3%）· 奥罗 类银28 : 同位聚晶30 : 星髓晶42 = **125.0**（−0.5%，并补入星髓晶呼应"巨构都在奥罗"）· 核心 同位聚晶60 : 星髓晶40 = **131.0**（+0.1%）⇒ `content:check` **3 红 → 全绿**。**③⚠ 待船长注意的派发后果**：同星系日板「取级别最高（并列取奖金最高）」⇒ 奥罗荒环日板**只会出「巨构核心勘探令 84」**，`泰坦残骸勘探 60` 与 `奥罗武装残骸群 62` 都不会出现（此前只有 60 被顶掉，您当时裁「接受、不调整」）⇒ **三选一**：(a) 接受（三卡同域、日板只出最高级）(b) 把核心卡**降为 L2**（让 60 仍出板）(c) **改派发规则**（同域按级分槽 / 轮换，让三张都能出）。**④验证**：typecheck 四包 ✅ · core **97 文件 / 993 用例** ✅ · `content:check` ✅ · desktop build ✅。【待验收】

- 2026-09-12：**合入主树（船长「允许合并」）**——`d2/workspace` → `main` **fast-forward**（`main` 由 `bad5413` 前进到 **`d9ee4f3`**），两侧 HEAD 一致、工作区干净。**合并前先在 d2 侧解 3 处冲突**（主树期间 10 个新提交）：①`docs/roadmap.md` = **两侧条目并集**（只删冲突标记、两侧条目全保留）②`packages/core/src/expedition.ts` = **并集**（保留 d2 的「无法交战 `cannot-engage`」战报分支 + 采主树新文案「手动撤退 = 即刻回港」）③`BattleScreen.tsx` 撤退按钮 title = 采主树新文案。**合并后验证**：typecheck 四包 ✅ · core **97 文件 / 993 用例全绿** ✅ · `content:check` ✅ · desktop build ✅；**三条自检全过**（全仓 422 文本文件往返解码**乱码 0** · roadmap 对**两个合并父提交**逐行覆盖**各缺 0 行** · BOM 口径一致）。**推送仍按闸门挂起**（等船长验收 + 公告稿审核后代码与公告一起推）。【待验收】

- 2026-09-12：**敌方近防炮改造（船长八条裁决）+ 修「出击型机群永远不会被反击」的开火许可 bug**（船长原话：「**敌人防空火力受舰船级别影响。越大的舰船防空火力越强。改为集火制度。优先攻击哨戒和攻坚无人机，给攻坚无人机添加 25% 全抗性。侦查和普通战机相同权重抽取。允许命中下限 10%。基础命中率提高到 70%**」· 二号 · d2）——**①改前读数（P-41 标定轮）**：三张代表卡（噬口 80 / 坟场 88 / 穹顶 96）**机群战损 0 架 · 战损估值 0 ISK · 占奖励 0.0%** ⇒ 近防炮**没有牙**。**②定位到一处真 bug**：`resolvePointDefense` 旧代码拿到"出击型打一次换一次"的反击令牌后，**又要求"哨戒机在 2,500m 射程内"才放行**（`if (!sentryInRange) return`）⇒ 两条开火许可被 **AND** 在一起，等于把出击型那条路**整条掐死**（与船长 2026-09-11 口径「出击型：攻击一次换一次**无视射程**的反击」直接矛盾）⇒ 改为**两条独立许可（OR）**：a) 反击令牌（无视距离）b) 哨戒机在射程内。**③八条逐条落码**：`pdAcc` **0.5 → 0.7** · 新增 `pdHitFloor` **0.10**（`命中 = clamp(下限, 1, acc − 闪避)`，修掉"专属机闪避 0.55 > acc ⇒ 永久免疫"）· 新增 **`pdTierMul` 舰种档系数 [1.0 / 1.5 / 2.0 / 3.0 / 4.0]**（T1…T5，伤害 = `pdDmg × 系数`；为此 `UnitSpec` 加 **`hullClassTier`**，敌侧由编成条目所引舰级写入）· **集火制度**（新增 `BattleState.pdFocus`：锁定一架直到击落才换靶——旧口径每拍随机换靶 ⇒ 伤害摊薄到整群、几乎打不掉）· **选靶优先级「哨戒机 → 攻坚机 → 其余等权」**（侦察机与战斗机同权；⚠ **前提：哨戒机必须在近防炮射程 2,500m 内**——船长 2026-09-12 追加「**优先攻击哨戒机的前提是哨戒在射程内**」，代码里由 `aliveDroneIndices` 的 `if (!sentriesInRange) return others` 保证：拉开距离时哨戒机既不可选也不占优先级、集火锁同步解除）· **攻坚机 25% 全抗性**（三层 × 三系全 0.25，取代原"甲抗爆炸 0.15 / 结构抗动能 0.05"）。**④判定频率保留**（船长问「不是改成反击型了吗，判断频率还有用吗」⇒ 答：**反击制只管"能不能开火"，频率管"开火后多久掷一次命中"**；实测令牌速率 ≈0.23 次/秒 ≪ 上限 2 次/秒 ⇒ **当前不是瓶颈**，真实作用 = 还手速率上限 + 大步长时的补算上限）。**⑤改后读数（`battle:pd-tune` 同配置）**：噬口猎杀令 **每场平均损失 3.2 架（全是哨戒机——正是"优先打哨戒"的直接效果）· 战损估值 30,400 ISK · 占卡奖励 3.6%**（改前 0 架 / 0.0%）⇒ **有牙了**；坟场/穹顶仍 0 架（那两张玩家 0% 胜率、17~18 秒被打死，战斗太短）。**⑥验证**：typecheck 四包 ✅ · core **94 文件 / 977 用例全绿** ✅（新增 `tests/pd-rules.test.ts` 5 条）· `content:check` ✅ · desktop build ✅；`tools/pd-tune.ts` 里那行过期口径说明同步改新。⚠ **待办**：近防炮改造后的 `--std` 全表复跑 + 词典/设计稿的整段改写。【待验收】

- 2026-09-12：**船长两条裁决落码：G 族无人机占比 0.35 → 0.30 + 承认"短射程族在远距风筝下摸不到人"**（二号 · d2）——**①占比 30%**（船长「G族无人机仅占30%」）：守恒拆分只改构成 ⇒ 机群 28→**24**（2 架各 12）、炮台 52→**56**，**条目总量仍 80、整卡总量仍 138**；`content:check` 守恒契约 + core 全绿。**②承认口径**（船长「承认"短射程族在远距风筝下摸不到人"」）：P-41 第 3 项**维持不动**，登记为**已承认的既有口径**（A/B/C/G 在 A2 远程风筝行 6,330m 处大量 0 开火，属"玩家在敌射程外白嫖"的全局现象，不再当问题提）。**③同批**：`--std` 全表固化（`docs/design/battle-data/std-table-20260912.txt`，4 行 × 27 卡 = 108 格全部跑到；逐行 0 开火统计 = A0 9 张 / A1 3 张 / A2 15 张 / A3 9 张）。【待验收】

- 2026-09-12：**修「天底封锁看不到敌方无人机」：G 蜂群机机型资产补登记 + 未登记机型加兜底**（船长实测反馈 · 二号 · d2）——**①现象**：船长实测天底静区封锁战斗，**看不到敌方无人机**。**②定位**：机群**机制**在跑（引擎建了 2 架机群武器、`foeDrones` 视图有数据），缺的是**渲染层机型资产**——`apps/desktop/src/renderer/src/ui/droneArt.tsx` 的 `DRONE_MODELS` 里只登记过 **E 族警戒机**，G 三型（`foe-drone-g-bee-kin/-exp/-pla`）**从未登记** ⇒ `droneModelOf()` 返回 `undefined`、敌侧 `model` 拿不到 ⇒ **机体不画**（只见到弹道）。**③修法**：补登记 G 三型（族色**聚落紫** `#cd9fdd`；机体 = **蜂窝六边舱段 + 拼装焊接痕**，与族外形口径一致；三型共用同一机体——口径「**颜色按弹型、机型不夺颜色语言**」，三系差别在弹点颜色与弹种；弹点 = 小弹点 len 8 / 宽 1.8 表达"一群致命"）。**④顺带堵类**：新增 `DRONE_MODEL_FALLBACK` + `droneModelOrFallback()`，并把敌侧/我方的 `droneModelOf(...)!` 两处非空断言改为走它 ⇒ **未登记机型 = 画"未知机型"灰机体 + 名字回退成机型 id**（把"漏登记"从**静默消失**变成**一眼可辨**）。**⑤验证**：typecheck 四包 ✅ · core 93 文件 / 972 用例 ✅ · desktop build ✅（渲染层改动 ⇒ **需重启客户端**才可见）。【待验收】

- 2026-09-12：**G 族蜂群机射程 2,800 → 7,000m**（船长 2026-09-12：「**敌方蜂群攻击范围提高到 7000**」· 二号 · d2）——**①动因**（上一问的落点）：蜂群原射程 2,800m = 我方侦察机档的"**近身护航**"口径，而**机群是挂在母舰单位上的武器条目**（不是独立单位）⇒ **只有母舰所处距离 ≤ 机群射程才开火**；而 G 族天底静区封锁的**期望交距 ≈ 3,858m**（射程带 502~6,604 × orbit 0.55）⇒ 敌人站定位置时**蜂群够不着**，与族格「**全 orbit：蜂群远距压制**」直接矛盾（挂上去等于白挂）。**②落法（一行）**：`foe-drones.ts` 的 `maxRangeM: 2800 → **7000**`（**只改射程**）——火力总量不受影响（天底那条仍由守恒拆分钉在 **138 = 炮 110 + 机 28**）；`minRangeM 1`（无近盲）与 `falloff 1`（带内不衰减）照旧。**③两族口径互不看齐**：E 族警戒机维持 **5,000m**（船长 2026-09-11「无人机射程设为 5000」）+ 受击增程 ×4 = 20,000m；**G 族没有受击增程**（船长只批"2 架、无后备"）。**④新契约守卫**：「机群与防空契约」新增 **G 族蜂群机射程定值 7,000m**（低于本族卡期望交距即报错，防日后被调回"够不着"的值）+ 汇总行读数。**⑤影响面（如实说）**：现只有**天底静区封锁**一张卡挂 G 蜂群 ⇒ 该卡的蜂群**从开战起就能持续输出 28 点/轮**（此前要玩家贴到 2.8km 内才挨打）= **该卡实战难度上升**，属船长裁决下的**有意变化**；炮台对射距离与其它卡零影响。**⑥验证**：typecheck 四包 ✅ · core **93 文件 / 972 用例全绿** ✅ · content:check ✅ 全绿（含新守卫读数）· desktop build ✅。【待验收】

- 2026-09-12：**P-43「各族舰级补完」落码 —— G 族四档舰级 + 蜂群上舰 + 四张隐藏模板迁移；全表 27/27 张敌军卡进舰级路径**（船长六裁决 2026-09-12：「**G组分5档。从护卫舰到战列舰。战列舰暂时空置。蜂群目前挂在巡洋舰上。2架。无后备。速度口径按照1.05算。隐藏模板迁。名字按照你推荐的修改。**」＋ 四条编成追加裁决「**战列舰先建壳体**」「**烬火围攻战按 2 护卫舰**」「**残响残舰按 1 驱逐 2 护卫**」「**天底封锁舰按 1 巡洋 2 驱逐**」· 二号 · d2）——**①范围实测（先纠口径）**：P-43 行写的"13 张卡"= 登记当时的 **A 6 + G 3 + 模板 4**，而 A 族 6 张早已迁完 ⇒ **实际只剩 7 张**（G 三卡 + 四张 `hidden` 遭遇模板）；D/E/B/C 全在舰级路径。**②G 族四档舰级**：**围攻残兵舰 T1（357）· 残响残舰 T2（310）· 天底封锁舰 T3（271）· 亡军战列舰 T4（215，空置壳体）**——全族 `speedRatio` = **1.05 定值**（船长口径），角色系数按档基线 × 角色（如 T1 260 × 0.60 = 156 / 39.5 × 0.71 = 28）。**③三张卡迁入舰级路径（编成按船长裁决）**：烬火围攻战 42 = **2 护卫** · 回音残舰 52 = **1 驱逐 2 护卫** · 天底静区封锁 66 = **1 巡洋 2 驱逐**；血量权重 = **档基线比**（天底 900 : 480 : 480）；**不写 `escort`**（船长 2026-09-11 裁决②「不保留僚机」）。**④蜂群（船长「挂在巡洋舰上。2 架。无后备」）**：只挂**天底封锁舰**，机型 `foe-drone-g-bee-kin` ×1 + `-exp` ×1（三系先落两系，等离子留后续）；**不写 `droneReserve`** ⇒ 无后备。火力走**守恒拆分**（`droneFireShare` 0.35 + `firepowerAnchor` **80**，写在**巡洋那一条目**）：机群 28（2 架各 14）+ 炮台 52，再加两个驱逐条目各 29 ⇒ **本卡炮台 110 + 机群 28 = 138 = 迁移前实测值**。**⑤迁移守恒（探针前后逐单位比对）**：三张卡**总血 1,585 / 2,035 / 2,040** 与**炮台总单发 98 / 121 / 138**、射程带（437~5,745 / 464~6,103 / 502~6,604）、命中（0.85 / 0.85 / 0.95）、衰减（0.5）、三层血型（均衡）**逐项一致**；**唯一有意差异 = 速度**（307 / 316 / 327 → 357 / 310 / 271，船长 1.05 口径）+ 天底多出**机群血池 52**（26×2，占总血 2.5%）。**⑥四张隐藏遭遇模板迁入 A 族舰级**（船长「隐藏模板迁」）：血量 / 单发 / 射程 / 命中 / 衰减 / 实速**逐项守恒**（靠 `speedMul` 反算 + 射程与命中/衰减覆写）⇒ 旧档遭遇**零漂移**、且不再吃 170/90 兜底体积。**⑦顺带新增一个小口**：`FoeShipSlot.falloff`（远端衰减覆写，与 `hitRate` 同款；**缺省 = 舰级值 ⇒ 现有卡零行为变化**）——迁移守恒所需。**⑧契约**：新增 G 族守卫（**档位 1~4、T4 只能是那条壳体、不配 T5** · **速度定值 1.05×**）；四张 `hidden` 旧模板**豁免**「A 族编成契约（头目+杂鱼）」与「A 族全族提速带」（归属 A 族但属旧档兜底、编成是守恒迁移而非族设计——契约里写明理由）。**⑨收口读数**：**全表 27/27 张敌军卡都在舰级路径**（旧威胁推导路径**再无真实卡**）· 舰级 **20 条** · 族→战术 **47 处** · 敌族登记 **27 张全登记** · 机群火力占比条目 5 处（锚点 3 张卡）。**⑩用例**：`foe-ship-path` 舰级表补 4 条 G（含档位与实速期望）＋ 舰级路径卡数 20 → **27**；`foe-lair-scale` 的"旧路径派生"夹具改为**合成卡**（真卡已全在舰级路径，旧分支仍需守卫）。**⑪验证**：typecheck 四包 ✅ · core **93 文件 / 972 用例全绿** ✅ · content:check ✅ 全绿 · desktop build ✅。【待验收】

- 2026-09-12：**F 族废弃融合批落码（P-15 · 8 项全落）**（船长 2026-09-11「**废弃F族，将F族融合进A族**」· 二号 · d2）——**①数据**：四张旧遭遇模板（`enc-pirate-1..4` = 流窜海盗快艇 10 / 伏击劫掠队 22 / 狂徒巡逻编队 40 / 深空屠夫舰队 70）补 **`foeFamily: 'A'`**——由"未登族 → 兜底 F 形"改为**显式登记**；签名表本就回落到 A 口径 ⇒ **战斗零变化**。**②类型**：`FoeFamily` 保留 `'F'` 并注「**已废弃·字母位空置**」（防旧内容表/旧导出解析出错；存档与导出都不落族字段）。**③三表退役留档**（不删，`Record<FoeFamily,…>` 需键完整）：`FOE_LAIR_TIERS.F` / `FOE_LAIR_GEAR.F` / `FOE_SUB_DMG.F`。**④美术退役留档**（不删，已无卡引用）：`FOE_ACCENT.F` / F 族形 / `FOE_FAMILY_LABEL.F` / `shipMounts.F`；核查星图只把 `FOE_ACCENT` 遍历成 `defs` 渐变（无 F 族文字残留）。**⑤兜底改判**：全仓唯一缺省点 `foeFamilyOf()` 的 `?? 'F'` **改指 `'A'`**（仅防崩；正常路径由新契约保证每卡有族）。**⑥契约**：删 `content-check` 的 `def.foeFamily !== 'F'` 特例，**新增「敌族显式登记契约」**三条（每张敌军卡必须有 `foeFamily` · 不许写 `'F'` · 须为合法族字母 A~E/G）⇒ 实测 **27 张敌军卡全部显式登记、`'F'` 空位无卡占用**；族→战术契约 **38 → 42 处**（四张模板并入 A 族校验）。**⑦工具**：`content-schema` 族枚举注记 F 已废弃；`mixed-damage-review` 的 `sectionF()` 核查后确认该站位**已是推进器档位对照段、全文件无 F 族计算** ⇒ 第 8 项按"无残留"收口并加注（**不删段**：删了会丢掉推进器探针）。**⑧顺带修名**：`make-test-save` 测试路径说明里「F 制式巡逻（低安遭遇伏击）」→「遭遇模板（F 族已废弃、四张模板归 A 族：低安遭遇伏击）」、同句旧族名「G 烬火流亡」→「G 鱿烬亡军」；`modules.ts` / `encounters.ts` 头注释同步。**⑨验证**：typecheck 四包 ✅ · core **93 文件 / 972 用例全绿** ✅ · content:check ✅ 全绿 · desktop build ✅；**新契约负向验证如实拦下**——临时摘掉 `enc-pirate-2` 的 `foeFamily` ⇒ 体检 **❌ 1 处错误**并指名该卡（「每张敌军卡必须显式登记族」）、`git checkout` 字节级还原后 **27 张全登记、✅ 通过**。【待验收】

- 2026-09-12：**新增纪律：合入与批量文档写入的编码纪律**（船长「允许新增纪律」· 二号 · d2）——见本文件「修复 `docs/roadmap.md` 被合并转码污染」一条的**根因与三条自检**：①取历史版本首选 `git checkout <rev> -- <path>`（不经变量中转）②必须经变量时用 .NET 显式 UTF-8（本机 `pwsh` 工具实为 **Windows PowerShell 5.1**，禁 `Set-Content`/`Out-File` 裸用；含中文的 `.ps1` 探针须带 BOM 再跑）③**合入/批量写文档后三条自检**（BOM 与行尾一致 · 全仓**往返解码**乱码 0 · 与两侧父提交比「父提交有的行还在」）④乱码与丢行按**事故**处理、只许用干净父提交逐行还原。落点 = `docs/development-conventions.md` §三新增小节 + `AGENTS.md` §4 同步一句 + `development-conventions-changelog.md` 一条。【待验收 · 待发布】

- 2026-09-12：**修复 `docs/roadmap.md` 被合并转码污染（449 行乱码）+ 找回两次合并丢掉的 19 行**（二号 · d2）——**①现象**：本文件顶部条目读到乱码，实测**文件字节本身**即「UTF-8 文本被按 GBK 解码后又存成 UTF-8」的转码产物（坏行 **449/548**、乱码字 **10,460**；形态 = 中文变成生僻字串）。**②溯源**（`git log` 逐提交计数）：`9f6b94f` 及之前为 **0 乱码**，`7ccf95f`（「合入主树前先把 main 合进 d2」）首次出现、`eaa00ce` 延续 ⇒ **出在两次合并解冲突的写入路径**（经控制台文本管道搬运中文），与内容无关。**③范围核查**：全树 **485 个文本文件**扫描 ⇒ **只有本文件中招**；两次合并涉及的全部文本文件（22 + 5 个）再按「**两侧父提交都有、合并结果却没了**」逐行审计 ⇒ 仅本文件（另一处 pending-index 的 P-21 行属**撞号改号 P-43 的正常改写**，非丢失）。**④还原法（可复现）**：两次合并的**两侧父提交都干净**（main `9a58edd` / d2 `9f6b94f`）⇒ 对每个坏行做「干净候选 → UTF-8 字节按 GBK 解码」的**模拟损坏比对**，**449 行全部唯一命中**；另有 **19 行乱码不含窄特征字**（漏检）⇒ 改用「**不在两侧父提交中**」定位、再做同样的模拟比对，**19/19 精确配对**后删除乱码副本（干净本已在位）。**⑤自检（全过才落盘）**：最终 **548 行** · 乱码 **0** · **A 侧内容缺 0 行 / B 侧内容缺 0 行** · **无多余行**；抽查「置顶引言 / A1 待办 / `## C. 战斗与舰船系统余项` / `## D. 经济·数值精调` / `## E. 基础设施` / 段落续行」均已归位。**⑥顺带找回的正文**：`## C./D./E.` 三个分节标题、A1 待办行、引言那句「先读本文件再讨论」、以及 **15 条段落续行**——两次合并把它们从结果里丢了（**两侧本来都有**）。**⑦教训（本文件级）**：**合并解冲突时不要经控制台文本管道搬运中文**——本环境是 Windows PowerShell，默认编码非 UTF-8，`git show` → 字符串 → 写回这条路径会**静默转码**；改为**`git checkout <rev> -- <path>` 直接物化文件**，或用 .NET 显式 `UTF8Encoding` 收输出。临时探针已删（工具纪律）。【待验收】

- 2026-09-12：**G 族族名落地 + 14 处「（X 族）」内部编号清理（文案批 · 待审稿转已落码）**（船长「**允许修改文案**」· 二号 · d2）——**①玩家可见 4 处落码**：星图「敌对派系」短名 `流亡 → 鱿烬`（`shipArt.tsx` `FOE_FAMILY_LABEL.G`，喂远征页族标签）· 两件 G 族专属件说明文「烬火流亡（G 族）窝点专属：…」→「**鱿烬亡军**窝点专属：…」· **已发布公告**那句「…、烬火流亡的无人机套件…」→「…、**鱿烬亡军**的无人机套件…」· 顺带发现并改**第 4 处**：G 族卡「天底静区封锁」卡面描述「也是**流亡舰队**最后的据点」→「也是**鱿烬亡军**最后的据点」。**②14 处内部编号清零**（A3/C3/D3/E3/G2）：专属件说明文里的「海盗（A 族）/ 异形生物（C 族）/ 守墓古舰（D 族）/ 泰坦巨构（E 族）/ 烬火流亡（G 族）」一律**去掉「（X 族）」**只留族名（违反「开发用语不进玩家可见范围」那一条）。**③内部注释同步 6 处**：`lairs.ts` ×3（窝点词表 / 专属件表 / 副系签名表）· `types.ts` 族字母表 · `shipMounts.ts` 美术锚点注释 · `lair-gear-g.test.ts` 头注。**④物品名保留不动**（P-36 三号建议）：鱿蜂无人机 / 鱿蜂群导控 / 流亡中继桅——「流亡」作处境描述仍成立；若要随族名统一（如「鱿烬蜂无人机」）一句话即改。**⑤验证**：全仓 `烬火流亡` **0 处**、`（X 族）` **0 处**；typecheck 四包 ✅ · core **93 文件 / 972 用例全绿** ✅ · content:check ✅ 全绿 · desktop build ✅。**⑥文档**：G 设定卡 §8.1/§8.2、总表 P-19/P-32~P-36、词典 G 族条逐条转「已落码」。【待验收】

- 2026-09-12：**「架数变多、总火力不动」落成条目级总火力锚点**（船长：「架数变多、总火力不动。我顺便想问下无人机的血量属性会变化吗」· 二号 · d2）——**①新增 `FoeShipSlot.firepowerAnchor`**（条目设计总单发锚点，**须与 `droneFireShare` 成对写**）：写了它 ⇒ 该条目的实收总单发以锚点为准，再按占比拆机群/炮台（逐架/逐单位摊分、余数补前面）；**为什么需要**：机群架数是**舰级**属性，A5 口径下"每架无人机都是一门炮"⇒ 架数一变总量就变，且**取整步长 = 架数**（7 架时总量只能按 7 递增，216 这类目标**根本取不到**）。**②E 族锚点** = 改架数前总量：泰坦 **216** · 奥罗 **72 + 82**（两条目各一）· 核心 **672**。**③实测（常态出击 5/7/10 + 备用 100%）**：齐射 **216 / 154 / 672 全部钉住**（炮 86 / 61 / 269；机群 7 架 ×19~18 / 15 架 ×5~8 / 10 架 ×40~41）；机群总血池 644 / 1,380 / 920（含备用 1,288 / 2,760 / 1,840）；窝点派生 T1/T2/T3 = ×1.3/1.6/2 ⇒ 281·346·432 / 201·246·308 / 872·1,072·1,344。**④踩到并修掉的坑**：锚点初版**没有随窝点档位放大** ⇒ 派生卡"血涨、火力不涨"（探针发现）⇒ 在 `lairs.lairAnomalyOf` 里让锚点随 `scale` 同乘（与 `hpMul`/`dmgMul` 同层）。**⑤契约**：`content:check`「机群与防空契约」⑥ 改为两套口径——**写了锚点 ⇒ 实收总单发必须等于锚点合计**（3 张卡命中）；没写锚点 ⇒ 沿用"比例前后总量不变"对照；并校验"锚点与占比成对"。**⑥血量问题的答复**：**机型属性一字未改**（三层血 28/20/44 = 92 · 回避 0.18 · 甲层动能抗 0.15 · 命中 0.65 · 装填 4,400ms · 射程 5,000m→增程 20,000m）；变的是**架数**⇒ **机群总血量池**随架数变（上表）。**⑦验证**：typecheck 四包 ✅ · content:check ✅ 全绿 · core **6 失败 / 931 通过**（＝基线）· desktop build ✅。**⑧文档**：E 族稿 §11.8 实测表与锚点口径段重写（含窝点派生表）；词典新增「**总火力锚点（`firepowerAnchor`）**」并更新「备用机库（敌机）」。【待验收 · E 族机群编成】

- 2026-09-12：**E 族常态出击数按舰种档 + 备用机库 100%**（船长：「将 E 族**可以常态出击的无人机数量，按照舰船等级设置，巡洋 5，战列 7，旗舰 10**。**备用机库为出击数量的 100%**。修改后将每个悬赏更新后的数据列给我看，包括无人机属性」· 二号 · d2）——**①取值**：`FoeShipDef.drones.count` 按**舰种档**定死——**奥罗残骸段（T3 巡洋）5 架 · 巨构残段（T4 战列）7 架 · 核心舱段（T5 旗舰）10 架**；`droneReserve.count` = **同数**（备用 = 出击数 100%），补位间隔沿用 10s / 10s / 12s。**②实测（引擎建档）**：泰坦残骸勘探常备齐射 **311**（炮 124 / 机 187 = 7 架，占比 39.9%/60.1%）· 奥罗武装残骸群 **202**（炮 80 / 机 122 = 15 架，3 舰各 5）· 巨构核心勘探令 **861**（炮 344 / 机 517 = 10 架）；备用机库 7 / 15 / 10 架，满库存总火力 **493 / 317 / 1,371**；窝点 T3 派生按档 ×2（628 / 407 / 1,722）。**③⚠ 必须点明的一处连带**：A5 口径下**机群架数本身就是火力项**（每架按 `dmgMul × 机型单发` 计）⇒ 架数上调把每卡总火力推高（泰坦 216→**311**、奥罗 154→**202**、核心 672→**861**）；若船长要的是"架数变多、**总火力不动**"（每架更轻的分布式蜂群），加一个**卡级总量锚点**即可（已写入设计稿待裁）。**④验证**：typecheck 四包 ✅ · content:check ✅ 全绿（备用机库 3 条启用 / 出击上限 0 条；架数与备用数契约通过）· core **6 失败 / 931 通过**（＝基线）· desktop build ✅。**⑤文档**：E 族稿 §11.8 更新取值与实测表（含连带说明）；词典「备用机库（敌机）」同步。【待验收 · E 族机群编成】

- 2026-09-12：**敌机两条出击机制：备用机库启用（乙）+ 单次出击上限就位待启用（甲）**（船长「先都做，但是**本轮只采用乙**。**甲留作后续其他机制**」· 二号 · d2）——**①船长问答链**：「**能否限制敌机单次出击数量或者给敌机添加备用机库（损坏后补充敌机）**」→（我答"两条都能做、且是两种不同的东西"）→ 船长定「**先都做，但是本轮只采用乙。甲留作后续其他机制。**」。**②现状修正**：引擎里**从来没有"分批放飞"**——一舰带 N 架就 N 架**同一拍齐射**、被击落即永久停火（A3），故两条都是**新增机制**而非调参。**③乙 · 备用机库（启用）**：舰级 `droneReserve: { count, respawnMs }` ⇒ 常备之外多带同机型备用机（建档展开成**额外武器条目**、开局 `inHangar`：不出战/不可被防空选中/不计存活架数）；前线战损 ⇒ **排期一架**、`respawnMs` 后**满血**放出（池条目 `inHangar`/`readyAtMs`/`maxS~maxH`，随战斗状态存档）；母舰阵亡则整群停。**账目口径**＝备用机条目带 `WeaponSpec.reserve`（**库存深度**，不是常驻齐射）⇒ 总火力/守恒核对一律排除之，视图单列 `foeDrones[].hangar`。**④甲 · 单次出击上限（不启用）**：舰级 `droneLaunch: { maxAloft, cycleMs?, keepDps? }`，`combat.aloftDroneSet` 按战斗时钟轮换批次窗口（**不新增存档字段**），`keepDps` ⇒ 在空架装填 ×(k/在场架数) ⇒ 平均 DPS 守恒。**契约锁死**：`content:check`「机群出击契约」**禁止任何舰级写 `droneLaunch`**（与"敌突进/单波增援"同款纪律）。**⑤E 族取值（乙）**：巨构残段 +2（总 4）/ 奥罗残骸段 +2（总 4，3 舰 ⇒ 6 架）/ 核心舱段 +3（总 6），补位 10s/10s/12s；实测**常备齐射 216 / 154 / 672 一字不变**，满库存总火力 346 / 246 / 1,074。⚠ **这是难度改动**（库存翻倍 ⇒ 清不干净机群，必须打母舰），且与**受击增程**叠加压力更大；数值为**首版**，待船长实测后调。**⑥改判备案**：乙与 2026-09-11 的 A3 裁定「**打光为止不补充**」相反 = **船长新裁定优先**，仅对写了 `droneReserve` 的舰级生效（其它族行为一字不变）；设计稿 §五·补2 与词典两处已改写。**⑦验证**：typecheck 四包 ✅ · content:check ✅ 全绿（新增「机群出击契约」读数行：备用机库 3 条启用 · 单次出击上限 0 条）· core **6 失败 / 931 通过**（＝基线；`foe-drone` 新增 4 条：建档/分批（合成舰级）/补位满血/缺省零变化）· desktop build ✅（渲染层仅注释与"在库机不画"的既有口径）。**⑧文档**：E 族稿新增 **§11.8**（甲乙对比 + 实测 + 改判备案）· 机群系统稿新增 **§五·补2** · 词典新增「**备用机库（敌机）**」「**单次出击上限（敌机 · 机制待启用）**」两条并改写「敌方机群」条目的旧表述。【待验收 · 敌机出击机制】

- 2026-09-12：**E 族追加修订：核心舱段升旗舰级 + 泰坦卡缩为单舰 + 三卡火力 +20% + 衰减 0.3**（船长三条裁定 · 二号 · d2）——**①船长原话**：「**核心舱段，调为旗舰级**。① **泰坦残骸勘探，敌人数量缩减为 1**。然后将**三个悬赏整体火力上调 20%**，**衰减降低为 0.3**。」**②核心舱段 → T5 旗舰**：`hullClassTier 4 → 5`；按甲案「档基线 × 角色倍率」重排 = **T5 档基线 2,800/420 × 1.40/1.30 = 3,920/546**（原 T4 口径 2,240/312），**卡侧 `hpMul`/`dmgMul` 反算吸收 ⇒ `ano-core-section` 卡读数不变**（血 3,400／总单发 672）；⚠ 旧裁定「T4 上限、T5 不开」作废，E 族现为**敌舰里唯一 T5**（A 禁 4/5、B 禁 ≥3、C 上限 4、E 无档位禁令）；速度 = 5 旗舰基准 155 × `speedRatio 0` = 0（静物）⇒ 档位改动不影响实速。**③泰坦卡缩为单舰**：巨构残段 ×2 → **×1**，**总血 1,585 逐字守恒**（990.625 + 594.375 合并到唯一一具），tag 由 `foe-0`+`w0-foe-1` 变为只有 `foe-0`；⚠ **机群 4 → 2 架**（架数登记在舰级上，单舰只带自己的 2 架）⇒ 每架单发 34/33 → **65/65**（机群总额守恒）。**④三卡火力 +20%**：泰坦 180 → **216**（炮 86／机 130）· 奥罗 128 → **154**（+20.3%，取整；炮 61／机 93）· 核心 560 → **672**（炮 269／机 403）；比例与构成不变、**血量一律不动**。**⑤远端衰减 0.3**：三条舰级 `falloff 0.5 → 0.3`（⚠ 方向 = `falloff` 是**射程上限处的威力系数** ⇒ 0.5→0.3 是**远端收得更紧**，与"火力 +20%"合成"近强远弱"）；⚠ **警戒机机型仍 `falloff: 1`**（射程带内恒定，族格原设定）——本批只改舰级，若要机群也吃远端衰减需单独裁。**⑥验证**：typecheck 四包 ✅ · content:check ✅ 全绿（T5 档位无契约冲突；E 族射程带/受击增程读数照旧）· core **6 失败 / 927 通过**（＝基线；`foe-ship-path` 舰级档位表 `foe-core-section` T4 → T5）· 实建档探针逐卡核对（含窝点 T3 派生：泰坦 433 / 奥罗 308 / 核心 1,344）。**⑦文档**：E 族稿新增 **§11.7 追加修订**（裁定表 + 修订后实建档表）· §11.1 表核心舱段行改 T5 旗舰与 3,920/546 · 全族同口径行补"远端衰减 0.3" · 词典「泰坦巨构」同步第三修订。【待验收 · E 族修订】

- 2026-09-12：**E 族「核心舱段级」代表卡落码 —— 全部舰级补齐代表卡**（船长「你先完成所有代表卡」· 二号 · d2）——**①查账口径**：把 `FOE_SHIPS` 每条舰级"被哪张卡引用"全表扫过 ⇒ **15 条舰级里只有「核心舱段」没有任何卡引用**（其余 14 条都有）⇒「所有代表卡」＝**就这一张**（G 族三张卡是"没有舰级"的另一笔账 = P-39 挂起项，不是缺代表卡）。**②新卡**：「**巨构核心勘探令**」`ano-core-section` · **威胁 84** · **深渊之门**（E 族两卡的 lore 系统）· **L3** · 窝点核心词 **`巨构核心`**（避开据点档位词「核心舱段」本身，防窝点名读成「核心舱段·核心」）· 玩家可见描述走 E 族口径（"几乎完好的巨构／残存自动程序仍在运转／能拆开它的人"）。**③编成与数值**：**精锐核心舱段 ×1（单舰）**——裁定 8「一具接近完好的巨构为目标」，体量感靠它自己、不配僚机；实建档 **血 3,400｜总单发 560（炮台 224 + 机群 3 架 ×112，占比 40%/60%）｜速度 0｜射程 100~10,000｜交距 5,545m｜构成 5:5｜战术 orbit**；奖金 **1,000,000**（80 档 85 万 ~ 88 档 110 万之间）· standing **11/4**（同噬口猎杀令）· `combatSeconds 150`（族格"撑到最后"）。段位对齐参照：噬口猎杀令 80 = 血 2,766／单发 501、坟场守墓者 88 = 血 3,960／单发 350。**④配套登记四处**：卡（`anomalies.ts`）· 稀有残骸（`lairCore` 自动生成 `wreck-rare-ano-core-section`）· 稀有度表（`wreck-ano-core-section: 1`）· 特色池与主题追加件（`salvageFlavors.ts`：池 **同位聚晶 45 : 星髓晶 56 = 160.35**，对 m×危档基数 = 1.7095×92.4 = 157.96 偏差 **+1.5%**；追加件 `mod-drone-tac-2` + `mod-armor-plate-2`）。**⑤验证**：typecheck 四包 ✅ · content:check ✅ **全绿**（B3.1 特色池 **22 张**、窝点契约 **20 张**、覆盖 5 个敌族）· core 全量 6 失败 / 927 通过（＝基线）· desktop build ✅。**⑥⚠ 一处副作用待船长定**：同星系规则"每星系最多一张、代表卡取级别最高（并列取奖金最高）"⇒ 本卡（L3 · 100 万）会在深渊之门**顶掉同系统「泰坦残骸勘探 60」的日板位**（后者不再上板）。三条出路：①接受（E 族三张卡只有两个 lore 系统，必然有一张让位）；②把泰坦残骸勘探迁到别的深空系统（会顶掉那里的卡）；③改"同星系代表卡"规则（船长此前已注"若要两张都能上板，需另定规则"）。**⑦文档**：E 族稿新增 **§11.6**（查账口径 + 代表卡数值表 + 副作用三条出路）· §六 第 3 条与 §11.1 表改"已落码"· 待办总表 **P-14 收口** · B3.1 稿定稿表补行（21 → 22 张）· 词典「泰坦巨构」条目同步。【待验收 · E 族代表卡】

- 2026-09-12：**逐卡「机群／炮台火力占比」旋钮**（船长：「**允许调整敌舰的无人机/炮台火力比例。这个要根据每个悬赏卡制定**」· 二号 · d2）——**①背景**：A5 口径下机群与炮台**同吃一条 `dmgMul`** ⇒ 只能同比例缩放、**卡上无旋钮**可改相对占比（占比由「机型 `dmg`÷舰级 `shotDmg`」× 架数隐式决定；实测两张 E 卡机群占 **14.4% / 21.9%**）。**②七项裁定**（船长逐条点选）：**守恒拆分**（写"机群占比 s"，总火力不动）· **编成条目级**（`FoeShipSlot.droneFireShare`，缺省回落 `FoeShipDef.droneFireShare`）· 基准 = 该条目**现口径实收总单发 T**（机群先取、余额给炮台）· **0~1 闭区间且两侧各保底 1**（无 0 伤害条目）· **不显示**给玩家 · 只含「机群 vs 母舰武器组」（敌侧近防炮照 B3 不动）· **本批只做机制**（E 族两张卡暂不写）。**③算法**（`combat.droneFireSplitOf`）：`T = N×(炮台旧单发 + Σ机群旧单发)` → `D = clamp(round(T×s), 架数, max(架数, T−N))`、`G = T−D` → 机群摊到逐架（余数补**前面的架次**）、炮台摊到逐单位（余数补**前面的单位**），两者 Σ 精确；**未写 s ⇒ 返回 null、走旧算法**（零行为变化，硬保证）。**④契约**：`content:check`「机群与防空契约」⑥（0~1 · 只有"会展开机群"的条目/舰级可写 · **守恒实测**：写了比例的卡其 Σ 单发必须与"同卡去掉比例"逐字相等）。**⑤用例**：`tests/foe-drone.test.ts` 新增 5 条（缺省零变化 / 守恒跨 s∈{0,0.3,0.5,1} / s=0.5 机群与炮台各 417 且均摊 / 边界保底 s=1 与 s=0 / 条目级两条目互不影响）。**⑥验证**：typecheck 四包 ✅ · core **6 失败 / 927 通过**（＝基线；新增 5 条）· content:check ✅ 全绿（本批无卡写该字段 ⇒ 契约静默通过）· desktop build ✅（**不涉渲染层**）。**⑦文档**：机群系统稿新增 **§五·补**（裁定表 + 算法 + 契约 + 不做清单）；词典新增词条「机群火力占比（`droneFireShare`）」。**⑧下一步**：等船长逐卡给值（E 族泰坦残骸勘探 60 / 奥罗武装残骸群 62 / 核心舱段代表卡）。【待验收 · 数值机制】

- 2026-09-12：**受击增程改判「只触发一次 · 对所有敌舰生效」**（船长三次裁定 · 二号 · d2）——**①船长原话**：「**每个敌人都会单独触发一次射程增加的文字提示，理论上应该只触发一次，对所有敌舰生效**」。**②改法**：状态由"按敌单位 tag 的倍率表"改为**标量** `BattleState.foeDroneRangeBuff`——命中**任一**带该机制的敌舰 ⇒ 给**整支敌队**盖章 ⇒ **所有敌舰的机群**同时 ×4（不再逐舰各算一份）；提示**整队只推一条**（`markFoeDroneRangeBuff` 仅在 1 → 倍率 时返回 true），文案去掉单舰名，改「**巨构残存程序过载：警戒机群解除射程限制**」；`foeDroneRangeOf(b, w)` 签名随之简化（不再需要 tag），视图层 `foeDrones[].rangeBuff` 也改成整队一个值 ⇒ **所有敌舰的机群一起后撤**（与"全队生效"的画面一致）。**③验证**：typecheck 四包 ✅ · core **6 失败 / 922 通过**（＝基线；受击增程用例改为断言"标量 = 4 / 两艘母舰的机群都读到 20,000m / 提示恰好 1 条 / 再推进一整段仍 1 条"）· content:check ✅ 全绿 · desktop build ✅（渲染层改动 ⇒ **须重启客户端再验**）。**④文档**：E 族稿 §11.5「作用面（三次裁定改判）」「提示条数」两行 + 实现落点段、词典「受击增程（敌机）」同步。【待验收 · E 族机制再修】

- 2026-09-12：**提示位统一（战斗窗口正上方）+ 敌机几何单一算法（错位修复）**（船长两条实测反馈 · 二号 · d2）——**①船长原话**：「**日志内不用显示提示，将该提示放入战斗画面内显示**（和**敌方增援**统一下系统，**显示位置改为战斗窗口正上方**）」+「**射程增加后，敌无人机的落点位置出现错位**」。**②提示位统一**：受击增程**不再写 `addLog`**，改推 `BattleState.notices`（逐条带战斗时钟 `atMs`、只留最近 4 条）；UI 把「波次增援」（`waveClearAt` 推导）与引擎提示合到**同一处提示位** `.app-bts-notices`（战场窗口**顶部居中**、多条纵向排列、4 秒后自动消失），`styles.css` 的 `.app-bts-wave-hint` 由"绝对定位挂在敌舰上方"改为容器内相对排布（外观/闪烁动画沿用既有）。**③错位根因与修复**：表现层有两套敌机阵位算法——机体走 `foeDroneStation`（增程后已外推）、**弹道起飞点仍走旧的 `droneStationFrom`（未外推）** ⇒ 机体后撤了、弹道还从我舰旁发出（船长看到的错位）。现**统一为 `foeDroneStation` 一处**：机体阵位、弹道起飞点（含返航段路径起点）、击落爆炸点三处共用同一函数；`foeDroneBuffedOf(tag)` 提供该舰增程态查询。**④验证**：typecheck 四包 ✅ · core **6 失败 / 922 通过**（＝基线；受击增程用例改为断言"提示进 `battle.notices`、日志不再出现"）· content:check ✅ 全绿 · desktop build ✅（**渲染层改动 ⇒ 须重启客户端再验**）。**⑤文档**：E 族稿 §11.5「提示」行与实现落点重写；词典「受击增程（敌机）」同步。

- 2026-09-12：**E 族射程重定 + 新机制「受击增程」（船长七项裁定 · 二号 · d2）**——**①船长原话（第二次修订稿）**：「E 族射程按照**最低 100**，**最高根据 7000~10000** 设定，**无人机射程设为 5000**，添加新机制，**受到攻击后，大幅提高无人机射程（提高 400%）**」；七项细节逐条点选：**×4**（提高到 400%）· **仅母舰本体被命中触发**（打机群不触发）· 该舰**全部**机群共享 · **本场永久** · **不封顶** · 射程分配 **7,000 / 8,500 / 10,000** · **解除期望距离钉住** · **要玩家可见提示**。**②数值**：三条舰级射程带 **100~7,000（奥罗）/ 100~8,500（巨构）/ 100~10,000（核心舱段·精锐）**；警戒机 **4,500 → 5,000m**；撤掉三条舰级的 `desireRangeM`（射程既定 ⇒ 交距回归族规）= 带内插值 `min+0.55×(max−min)` ⇒ **3,895 / 4,720 / 5,545m**（此前钉在 ~513m）。**③机制**：`FoeShipDef.droneRangeMulOnHit`（E 族三条写 4）→ 建档时带到 `UnitSpec`；**命中敌舰本体**那一刻在 `BattleState.foeDroneRangeBuff`（按敌单位 tag 记**倍率**、跨读档保持）盖章 ⇒ 该舰全部机群 5,000 → **20,000m**（全图可达）；射程**只有一处算法** `foeDroneRangeOf(b, tag, w)`（开火射程门与界面同源，避免"打得着却画得近"）；触发时写一条战斗日志「…残存的自动程序过载——警戒机群解除射程限制，转为远程拦截。」，表现层 `foeDroneStation` 把机群阵位沿"我舰→敌舰"外推（55%、上限 260px，击杀落点同源）。**④契约与用例**：`content:check`「机群与防空契约」新增 ⑤（E 族射程带下限 100 / 上限落 7,000~10,000 · E 族机型射程 = 5,000 · 受击增程只允许带机群的 E 族舰级且 ≤4）；`foe-drone.test.ts` 新增 5 条（真触发 ×4 与日志 / **只打机群不触发**（构造"整场够不着母舰"的仗：母舰钉 4,000m + speedRatio 2）· 无字段零行为变化 · 字段不下发 · **本场永久**不回落）、`foe-ship-path.test.ts` 新增 1 条（射程带 + 交距 + 钉住值已撤）。**⑤验证**：typecheck 四包 ✅ · core **6 失败 / 922 通过**（6 条＝基线）· content:check **✅ 全绿** · desktop build ✅（渲染层改动 ⇒ **须重启客户端**）。**⑥已知取舍（船长已确认）**：核心舱段 10,000m ＞ 纯动能玩家上限 7,350m（须激光 MK3 / 导弹 / 巨构残骸炮对射）· 交距 513m→3,895~5,545m 是本批最大难度改动 · 触发后无死角（解法 = 清机群或撤退；机体仍会扑到玩家舰旁 ⇒ 近防炮照打）。**⑦读数**：按船长「标定轮暂缓」本批不跑，留待 P-41。【待验收 · E 族射程与机制批】

- 2026-09-12：**B3.1 裂谷特色池重定价（船长「重定价池子」· 二号 · d2）——`content:check` 首次全绿（0 处）**。**①问题**：content:check 的 B3.1「敌群特色回收池」常年挂唯一一处红——裂谷畸变体猎杀令（`ano-chasm-aberrations`）池均价 43.33 ÷ 档基数 92.4 = 0.469、目标 m=1.565（**−70%**）。**②根因**：2026-09-10 基础密度改口径（档阈值 ×21.4 上移）后该池是按**险档**目标 43.18 重校的，但裂谷深带残骸基础密度 **1,206 ≥ 危线 642** ⇒ 本卡实为**危档**（基数 92.4）⇒ "池按险档定价、星系按危档结算"；设计稿 `b3-flavor-content.md` 定稿表本来就写「裂谷深带 = 危 / m 1.56 / 保底≈128k」，是**代码侧漂了**。**③裁定与落法（甲案）**：`salvageFlavors.ts` 池改 **同位聚晶 53 : 星髓晶 47 = 144.30（−0.2%）**（矿物按设计稿主题规则「深渊/裂谷 → 星髓 + 同位」），`recycleNote` 同步改「同位聚晶与星髓晶富集」；`b3-flavor-content.md` 新增「重校记录」台账。**④读数**（`salvage:econ` 逐卡）：m=1.565｜144.30 ÷ 92.4 = 1.562（**−0.2%**）✓｜保底 ≈ **128,831 ISK/h**（与定稿表"128k"逐字对回）｜**21 张特色池超差 0**。**⑤影响面**：仅该卡残骸回收保底（约 ×3.3），不动档位阈值与其它 20 张池子、不涉战斗数值。**⑥验证**：content:check ✅ **全绿** · typecheck 四包 ✅ · core 6 失败 / 916 通过（＝基线）。**⚠ 口径更新**：本分支的 content:check **期望态由"恰好 1 处"改为 0 处**。【待验收 · 经济重定价】

- 2026-09-12：**E 族数据批收尾（船长四条裁定 · 二号 · d2）**——**①三条舰级齐备**：巨构残段 T4（已有）· 新增 **奥罗残骸段** T3（990/124 · 命中 0.95 沿用本卡）· 新增 **核心舱段** T4 **精锐**（2,240/312 · 警戒机 ×3）⇒ 设定裁定 8「**核心舱段级代表位**」的**舰级已就位**（代表卡待出）。**②全族速度倍率 0**（船长「**族速度倍率设为 0**。依靠无人机攻击炮台范围外敌人」）⇒ 三条舰级实速全 0（静物残骸，火力由警戒机群投送），`content:check` 的 **E 族族格速带下限 0.65 → 0**（0~0.90×），实测读数两条均 `0(0.00)`。**③全族构成 50% 动能 + 50% 爆炸**（船长「该系敌人伤害比例为 50% 爆炸 50% 动能」），并按船长「**E 族单独调整，包括 E 族赏金任务的伤害比例**」把**窝点派生卡**一并纳入 ⇒ 族签名表 `FOE_SUB_DMG.E` 爆炸升副系首位 + 新增 **`LAIR_SUB_DMG_SHARE_BY_FAMILY = { E: 0.5 }`**（**E 族窝点派生卡也是 5:5**，不套全局 6:4）+ 混伤契约新增 **④ E 族特例**（常驻卡 5:5 与窝点卡 5:5 双校验）+ `tests/mixed-damage.test.ts` 新增用例（并反证 D 族仍 6:4）。**④战术对齐 orbit 但期望距离不改**（船长「战术进行调整，**但是期望距离不改，因为射程未定**」）⇒ **移植主树既有机制**进 d2：`FoeShipDef.desireRangeM` / `FoeShipSlot.desireRangeM`（条目 > 舰级），`combat.foeDesiredRange` 优先采纳；三条舰级各钉住现状交距（513 / 516 / 513）。**⑤奥罗武装残骸群迁入舰级路径**（与泰坦残骸勘探同款处理）：总血 **1,740 逐字守恒**（主体 1,740×5/11 + 僚机 1,740×3/11 ×2）· 单发 **129 → 128**（母舰 46 + 僚机 27×2 ／ **机群 3 舰 ×2 架 = 28**，按 A5「机群计入总火力、母船单发让位」）· 速度 **412 → 0** · 构成 **5:5** · 战术 `orbit` + `desireRangeM 516`；旧 `escorts 2` 改**显式编成条目**（`escort: true` ⇒ 显示名带「轻装」前缀；tag 仍是 `foe-0/1/2`）。**⑥顺带清一笔账（必须先说清）**：本分支**此前数条提交把 10 个文件整体按 prettier 默认样式重排**（加语句分号 / 双引号 / 按 80 列折行，≈5,000 行纯格式噪声）——与主树（无分号 + 单引号 + 手写折行）风格冲突，合入时会在 combat/types/state/content-check/BattleScreen 这些**两侧都在改**的文件上炸出大片无意义冲突 ⇒ 以 merge-base（`8c4c6a6`）为基准逐 hunk 做 token 级比对重建：**纯格式段丢弃 2,113、语义段保留 253**，各文件对主树 diff 从 1,000~3,600 行降到 **30~584 行**（提交 `c467218`）。**⑦验证**：typecheck 四包 ✅ · core **915 通过 / 6 失败**（＝基线，main WIP 同源）· content:check **1 处**（仅剩船长已定不改的 C 族裂谷）· 定向用例 `mixed-damage` 9/9 ✅。**⑧文档**：E 族设定稿新增 **§十一 落码记录**、§六 1~4 项标"已落码"；词典「敌方混伤」补 E 族特例、「泰坦巨构」条目过期段改已落码；待办总表 **P-12 / P-13 收口**、**P-14 记「舰级已就位、代表卡待出」**。**⑨剩余**：核心舱段代表卡（威胁 84 · 深渊之门 · L3）· E/D 族规契约化（P-21）· 标定轮（P-41，船长指示暂缓）· G 族挂起（P-39）。【待验收 · E 族数据批】

- 2026-09-12：**清理多余文档与中间产物（船长「清理多余的备注和文档信息」· 一号 · main）**——**①中间产物直接删除**：`apps/desktop/release` 434.8 MB · `.npm-cache` 297.1 MB · `out` 9.5 MB · `web/dist` 2.2 MB · `content-csv` 0.1 MB ⇒ **合计回收 743.8 MB**（均在 `.gitignore` 内、随时可重建）。**②多余文档归档不删除**：28 份已结案文档 `git mv` 入**新增的 `docs/design/archive/`** —— **12 份公告待审稿**（顶部均有船长批复：已写入 `announcements.ts` / 明判不发 / 整体作废）+ **16 份历史交接卡**（已被 2026-09-12 交接件取代）⇒ `docs/design/` 直接子级 **154 → 126 份**。**③新增 `docs/design/archive/README.md`**：归档判据 + 逐份结案状态台账 + 取回方法。**④留在一线仅 4 份**：一号与二号最新交接卡 + 09-12 当日作废待审稿两份（终局裁定留档）。**⑤源码注释按船长口径不动**（755 处「口径」类注释记录的是取值依据，且不在玩家可见范围，已核玩家可见文案无开发话语残留）。**⑥验证**：归档前全仓核对这 28 份**零引用**（无死链）· `git status` 全部识别为**重命名 R**（历史保留、可 `git mv` 取回）· typecheck 四包 0 错 · core **105 文件 / 1042 用例全绿** · `content:check` ✅ 全绿 · 桌面与网页 build ✅；文档写入自检（BOM 无 · CRLF 一致 · 行数增量 = +1）通过。【待验收 · 待推送】

- 2026-09-12：**推送回执：main 已推 origin（`6565dadb..e85604a6`，14 个提交）**（船长「进行推送」· 一号 · main）——**①推送方法**：本机 7897 代理 + openssl 后端**一次成功**（`git -c http.proxy=http://127.0.0.1:7897 -c http.sslBackend=openssl push origin main`）；**一次性参数、未改任何 git 全局或仓库配置**（仓库内仅有既有的 `http.sslbackend=schannel`）。⚠ 直连 GitHub 仍不稳：推送后**不带代理的 `git fetch` 实测失败**（`curl 28 Failed to connect to github.com:443 after 21132 ms`）——与交接卡 §1 记录的现象一致。**②上线内容 14 条**：一号 2 条（清理多余文档与中间产物 `45d365ba` · 其 roadmap 条目 `875a6e6d`）· 三号 4 条（敌列阵形改「斜向菱形」`2c5feb3d` · 后备机库改显示在名字边上 `0cddbcf6` · 后备机库改「全队合计」`1b99d60f` · 两次合入 `2f02695a`/`e85604a6`）· 二号 4 条（P-40 收口 `94ffb4d6` · 两次合入 `ea84ea85`/`c474871f` · 上次推送回执 `ececaa25`）· 交接件 3 条（一号 `293aef9b` · 二号 `40fc83fd` · 三号合入 `e65deac6`）· 另含三号 1 条随档。**③远端核对（最硬证据）**：经代理 `git ls-remote origin refs/heads/main` = `e85604a6d31f6cd4b62ccc4d10e72f82a553f6e7` = **本地 `main` 逐字节一致**，未推送 **0** 条；三树状态：`main` 干净 · `verify` 与 main 同点 · `d2/workspace` 领先 2 条（二号在途：台账落账）。**④本回执为推送后新提交**，留待下一次推送一并带上。

- 2026-09-12：**我的舰队：类别与级别筛选对调（船长「将我的舰队中，级别和类别筛选对调，玩家先选择类别，再选级别」· 一号 · main）**——**①四步闸门**：先取证（读到既有实现＝`ShipPage.tsx` 三行：状态 / 级别 / 类别，**类别仅在选中具体级别后出现**，且换级别会复位类别）→ 按 §5.2 摆出**与 2026-09-11 旧裁定的冲突**（旧原话「级别筛选单列一行，**当选择级别后**，出现舰船类别（工业战斗那些）筛选」，方向正好相反）→ 集中提问三条 → 船长逐条点选：**确认对调** · **始终出全五档 + 各类别** · **两行常显**。**②实现**：工具条改为 **第 1 行 状态 · 第 2 行 类别（`SHIP_SUBS`） · 第 3 行 级别（`SHIP_TIER_SUBS`）**，**两行都常显、不再隐藏、不再级联重置**；判定由「级别为上位（级别=全部时类别不参与）」改为**两维各自独立与其它条件取「与」**；文件 `apps/desktop/src/renderer/src/pages/ShipPage.tsx`（筛选逻辑 + 工具条 DOM + 两处注释）。**零数值/存档/战斗改动**。**③实测（无头 Chrome + 9 艘跨五档四类验收档，14 项断言全过）**：三行 top **266/309/352**（类别确在级别之前）、类别行 **5 标签** · 级别行 **6 标签**（全档常显）、工具条 bottom **398** 与首卡 top **408** 无重叠；读数链 **9 艘** → 类别 **采矿舰 3 / 货运舰 3 / 武装舰 2 / 重装舰 1**，级别 **T1 2 / T2 3 / T3 2 / T4 1 / T5 1**，组合「**货运舰 × T2 = 1**」、空组合「**货运舰 × T4 = 0**」、再叠状态「待维修」仍 0、复位回 **9**；**三档窗口（1920×1080 / 1366×768 / 1024×768）零横向溢出 · 一级页无纵向滚动 · 无整页横纵溢出**。**④已知取舍（船长已确认）**：两行全档常显 ⇒ **空组合可选**——25 艘船表里 20 格有 **7 格为空**（采矿舰 × T4/T5 · 武装舰 × T4/T5 · 货运舰 × T1 · 重装舰 × T1/T5），选到即 0 结果；若日后想自动收敛，改「级别行随类别收窄」或「换类别复位级别」即可。**⑤文档**：词典「我的舰队检索」条目改写（**旧条款标注作废** + 新裁定 + 实测读数 + 空组合清单）；本条目。**⑥验证**：typecheck 四包 ✅ · core **105 文件 / 1042 用例全绿** ✅ · `content:check` ✅ · 桌面与网页 build ✅；`_` 探针 0 个。【待验收 · 待推送】

- 2026-09-12：**敌舰体火力上限 = 150 DPS（船长「按照 DPS 上限 150 算」· 一号 · main）**——**①起因**：讨论「威胁 ↔ 赏金」时发现两条不对称——`foeHpOfThreat` 的 `t = min(1, (T−6)/90)` 把**敌血钳在威胁 96**（威胁 100/150/300 血量一律 1152），而**敌火力 `威胁 × foeDpsPerThreat` 线性不封顶**（威胁 300 ⇒ 240 DPS）⇒ 抬威胁只会得到"更脆更毒"的敌人。船长裁定：**火力改为 DPS 上限**。**②取证（真引擎逐卡建档，非估算）**：全表 **27/27 张卡都在舰级路径**（旧威胁推导路径现无真实卡）· **21 张是多单位**（N=2~11，多舰补偿 `2N/(N+1)` 实测跨度 **1.333~1.833**，最大 = 噬口猎杀令 N=11）· 舰体总 DPS 最高 = **虚海守望者 131.25**。**③实现**：`BattleBalance.foeDpsCap`（**150**）· 新增 `foeHullDpsOf` / `foeDpsCapScaleOf`，在 `createFoeSpecsFromShips` 建档时按**整卡舰体总 DPS** 判顶，越线则**全卡舰体单发等比例缩放**；**机群不计入**（另有受击增程/备用机库/A5 守恒，且船长已裁定不吃多舰补偿）· **被 `droneFireShare`/`firepowerAnchor` 拆分的条目不参与**（那条链自带总火力锚定，再钳会让锚点失准）· 缺省/0 = 不钳制。**④零行为变化（正测）**：27 张卡**无一越线** ⇒ 建档逐字不变；**负向验证**：人为把坟场守墓者 `dmgMul` ×4 ⇒ **350 DPS 被钳到 150、两艘等比例（同为 300 单发）**；`foeDpsCap=0` ⇒ 恢复 350（开关有效）；**机群边界**：巨构核心勘探令机群 **182.5 DPS 未被压**（确证机群不吃钳制）。**⑤留给将来的门槛**（每单位威胁触顶值 = `150 ÷ 0.8 ÷ 补偿`）：**N=1 187.5 · N=2 140.6 · N=3 125 · N=4 117.2 · N=11 102.3** ⇒ **战列舰/旗舰级的多单位卡一出生就会被管住**，且完全不动现有内容。**⑥用例**：新增 `tests/foe-dps-cap.test.ts` **5 条**（现值与全表零越线 · 越线钳制与等比例缩放 · 开关 · 机群不吃 · 补偿参与判顶）；core **106 文件 / 1055 用例全绿**。**⑦验证**：typecheck 四包 ✅ · `content:check` ✅ · 桌面与网页 build ✅。**⑧未做（等船长）**：解除**血量**钳制（`foeHpOfThreat` 去掉 `min`）——实测它会同时影响**窝点派生档**（L1 4~5 张 / L2 7~8 张 / L3 12 张卡的派生血量被钳）；以及**旧数据规则**的落地（「超过一次大更新版本的旧数据不可靠」，判据待船长定：存档版本 v24 / 里程碑批次 / 原话口径）。【待验收 · 待推送】

- 2026-09-12：**解除敌血钳制（船长「解除血量钳制，火力钳制设定为 150」· 一号 · main）**——**①改动（一行）**：`combat.ts` 的 `foeHpOfThreat` 去掉 `min(1, …)` —— 旧式 `t = min(1, (T−6)/90)` 让**威胁 ≥ 96 的敌血一律冻结在 1152**（100/150/300 全同），而敌火力却线性不封顶 ⇒ 抬威胁只得到"更脆更毒"的敌人。现 `t = max(0, (T−6)/90)`：**血量随威胁继续增长**，火力由 `foeDpsCap = 150` 封顶（上一批）。**②真值读数**：威胁 96 = **1152**（不变）· 100 = 1230 · 120 = **1652** · 132 = 1928 · 176 = 3074 · 192 = **3540** · 300 = 7295；对照旧口径这些值**全是 1152**。**③口径澄清（重要，我原先搞错过）**：`foeHpOfThreat = 参考火力 f(威胁) × D(T)`，**f 随威胁分段**（`foeRefFire`：≤16 → 2.1 · ≤40 → 9.7 · 其余 → 12.8），`D(T) = 5 + 85×t^1.6`；故威胁 6 = `2.1×5 → 11`（与校准矩阵"6:11"一致），**不是** 12.8×5=64。**④受益面**：**现 27 张卡威胁全部 ≤ 96 ⇒ 逐字零变化**（`t ≤ 1` 时 `min` 本就不生效）；受益的是**今后 threat > 96 的新卡**（战列舰/旗舰级）。⚠ **窝点派生卡不走这条曲线**（它们用"基础卡血量 × 派生比例"），故派生档也不受影响——这一点我先前的说法有误，已在此更正。**⑤工具同步**：`tools/battle-calibrate.ts`（`dExpect` 与 `--solve-hp` 的 `dTarget`）与 `tools/faction-audit.ts`（`killSeconds`）各自复写了同一条曲线且**自带 `min(1,…)`** ⇒ 三处一并去钳制，避免工具读数与引擎不符（单点纪律）。**⑥速度/射程成长的钳制保留**（`foeRefSpeedMps` 与 `growT` 的 `min(1,…)` 未动，避免敌人"又快又远又硬"）。**⑦用例**：`tests/foe-dps-cap.test.ts` 由 5 条扩到 **9 条**（新增：≤96 逐字不变 · >96 继续增长 · 受益面与派生档澄清 · 速度钳制保留）；**负向验证**：把 `min(1,…)` 临时加回 ⇒ **1 条如实变红**，还原后逐字节一致、恢复全绿。**⑧验证**：typecheck 四包 ✅ · core **106 文件 / 1059 用例全绿** · `content:check` ✅ · 桌面与网页 build ✅。【待验收 · 待推送】

- 2026-09-12：**交接卡：一号 → 新一号（2026-09-12 夜）**（一号；船长「写交接卡，准备继承新的一号」）——**新增 `docs/design/handoff-20260912-to-new-pilot1.md`**：给**零上下文**的接手人用的单页起点，§0 一分钟开工 → §11 逐项打勾，半小时进入可交付状态。**①内容**：工作区/推送闸门/铁律 · 船长工作方式的**今天新增经验**（选项作答、会连续改口⇒以最新一句为准、「归档 ≠ 消档」、玩家反馈优先取证、推送是独立闸门）· 今天新增的单点地图（`mainWeaponOf` / `ensureRackBays` / `cpuBudgetOf` / CPU 显示 / 蓝图书价 / 近防炮攻击 / 目标距离 / 倾销惩罚）· 本会话 6 批清单 · 验证闭环与**当前读数基线（core 1042/1042 · 105 文件）** · **9 条真实踩过的坑**（`edit` 吞 BOM、PowerShell 改源码毁文件、roadmap 插入必须按行锚点、文档三自检、旧条款走 §5.2、既有用例随数值适配、探针纪律、字段名先确认）。**②挂账口径**：明确写「**当前 0 条**」——船长已把最后两条**消档**，接手人不得再把它们翻出来问。**③互补件**：同日收档卡 `handoff-20260912-pilot1-wrapup.md`（记每批起因/真因/读数），本卡记「接手要知道的一切」，两份不重复。**【待推送 · 交接就绪】**

- 2026-09-12：**P-40 收口：我方近防炮的选靶口径与敌方侧对齐（哨戒射程前提 · 优先级 · 集火）（二号；船长「(a)(b)(c) 一起做」＋「现在就补优先级与集火」）**——**①先查清真相**：P-40 原文说「我方侧现无哨戒机概念」**已经过期**——`pickFoeDroneTarget` 里那条 `role === 'sentry' && dist ∉ 本武器射程 ⇒ 跳过` **早在 2026-09-12 近防炮改版批就已落码**（代码注释原文「对称规则（P-40）」），只是**待办索引没同步**（还挂着「—未开工」）。**②本轮补的是原文没要求、但口径该齐的两条**：**优先级**（哨戒 0 → 攻坚 1 → 其余 2、同档等权随机）与**集火**（锁定一架直到被击落才换靶）。**③一处关键发现（否则优先级形同虚设）**：原 `pdPriorityOf` 只按**机型 id** 查 `PD_PRIORITY_BY_ART`，而那张表登记的是**我方无人机 id**（`drone-sentry` / `drone-heavy`）⇒ **我方侧打敌方机型永远查不到**。现改为：查表命中优先，**查不到按 `role` 兜底判档**（`sentry` → 0 / `assault` → 1 / 其余 2）⇒ 敌方日后加哨戒/攻坚机型**自动生效**，不必回来改表。**④集火状态**：新增 `BattleState.mePdFocus`（按**武器槽**存 `{tag, idx}`，与敌方侧 `pdFocus` 同口径、索引轴不同），并**登记进 `save.ts` 的 `BATTLE_FIELDS`**（`runtime`、零迁移——正好走一遍 A3 刚立的登记表）。**⑤顺带修两处过期注释**：`combat.ts` 中 2026-09-10 的「默认**排除哨戒机**／近防炮**不打**哨戒无人机」已被 2026-09-12 改判（哨戒机**优先**、前提是进射程）⇒ 注释与代码相反，已改。**⑥验证**：新增 `tests/pd-player-target.test.ts` **5 例**（合成哨戒/攻坚/侦察机型：哨戒机 4,000m 打不到 · 1,000m 优先中选 · 只有哨戒且够不着 ⇒ 返回 null 不空放 · 攻坚优先于侦察 · **集火**连续两次打同一架、被击落才换靶 · 按武器槽各锁各的）；**负向证据**：加 `role` 兜底**之前**该文件实测 **2 红**（哨戒机与攻坚机都选不中）⇒ 补上后转绿（真实红→绿过程，强于人工扰动）；全量 core **104 文件 / 1038 用例**全绿（+5）· typecheck 四包 · content:check 绿 · 桌面 build 绿。**⑦已知取舍（如实登记）**：**(a)** 真数据里敌方**没有** `role: 'sentry'`（E 警戒机 = combat、G 蜂群机 = scout）⇒ **优先级与哨戒射程前提在真战斗中是空转**，靠合成用例钉住；**(b)** **集火是行为改动**：我方近防炮从"每次反击随机抽一架"变为"锁定一架打到死" ⇒ 面对多架敌机（E 警戒机 5~10 架 / G 蜂群机 2 架）实际击落更快；**本批未跑读数**（按船长「不跑标定除非说跑」口径），需要正式读数可另立；**(c)** 哨戒"进射程"的判定：敌方侧是硬编码 `PD_SENTRY_RANGE_M = 2_500`，我方侧走**本武器 `maxRangeM`**（近防炮同为 2,500 ⇒ 现数值重合）——**日后若出更远的防空武器，两侧口径会分叉**，已在注释写明。【待验收 · 待发布】

- 2026-09-12：**收档：当日后续六批（位数组修复 → 装配页 CPU 显示 → 主武器口径 → 公告清账 → 近防炮蓝图对齐 → 近防炮伤害）**（一号；船长就最后一批裁定「**不调整，收档**」）——**①六批清单与读数**见续接卡 `docs/design/handoff-20260912-pilot1-wrapup.md` §七（含每批起因、真因、提交号与实测读数）。**②终态**：三树 `main` / `d2` / `verify` 均干净、无未提交改动、无 `_` 探针；相对 `origin/main` 一律 **0 落后**（领先 main 11 · d2 8 · verify 10）——**全部本地提交、一条未推**（船长同日指示先不推）。**③验证口径**：全量 core **1037/1037**（104 文件）· typecheck 四包 · content:check 绿（蓝图价格口径 94/94 合规 · 防空契约 · 产物说明契约）· 桌面与网页版 build 绿。**④挂着未做（等船长）**：近防炮**开火节奏**（反应式 + 消费制 ⇒ 一场只开 3~20 炮；E 族 7~10 架机群仍打不完，需另裁「一次齐射换 K 炮 / 窗口内按自身装填连续开火」）· **推送** · 二号「星系机制通讯」批的公告。**⑤证据留存（仓库外，可复跑）**：`_ui-saves`（四态存档）· `_ui-shots`（截屏 + 读数 + 几何）· `_ui-probe.mjs` / `_ui-geom.mjs`。**【已收档 · 待推送】**

- 2026-09-12：**近防炮伤害与射程补偿：单发 3/5/6 → 10/16/17，对舰名义 DPS 反超同档主炮 1.24~1.28 倍（一号；玩家反馈「近防炮攻击好像偏低了」→ 船长裁定「丙：提高近防炮伤害，且因为其射程更短，威力应该提高」）**——**①取证（真数据真引擎）**：旧值 `dmgMult 0.5/0.8/1.05` ⇒ 单发 3/5/6、名义 DPS 2.00/3.57/4.62，**只有同档主炮的 39~44%**；打机群更差——**反应式 + 消费制**（一次敌机齐射只换一炮）⇒ 一场只开 **3~20 炮**（同场主炮 51~117 炮），对 E 族警戒机群（7 架 644 血）整场只打出 93 伤害（14.4%）、**0 击落**；两侧口径也不对称（敌方近防炮单发 5×舰种档 1.0~4.0 = 5~20，且有"哨戒机在射程内即可开火"的第二条许可）。**②改动（`data/modules.ts` 三行 + 三段说明文）**：`dmgMult` → **1.65 / 2.65 / 2.85**（单发 **10 / 16 / 17**、名义 DPS **6.67 / 11.43 / 13.08** = 同档炮台 5.19 / 9.24 / 10.54 的 **1.28 / 1.24 / 1.24 倍**）；**射程 2,500m、命中 0.9/0.9/0.92、装填 1.5/1.4/1.3s、CPU 12/26/44、价格与防空属性一律未动**；说明文改为「射程短、贴到近处威力很足」（旧文写「威力远不如主炮」已不成立）。**③作废旧条款（§5.2 处置）**：`docs/design/foe-drone-system-20260911.md` §六 表的「单发 = 同档主炮的 ~40% ⇒ 对舰明显偏弱、不是主炮替代品」**已就地标注作废**，并追加「六·补、单发口径改判（2026-09-12）」；词典新增「近防炮（玩家侧 · 攻击口径）」行。**④实测效果（同批卡同种子）**：近防炮整场对机群伤害约 **×1.85**（泰坦残骸 MK3 93 → **173**、核心舱段 144 → **265**、天底静区蜂群卡 44 → **52** 满分清场 2/2）；核心舱段卡首次**击落 1 架**。**⑤已知仍受限（如实登记）**：E 族 7~10 架机群仍打不完（该场只开 ~6 炮）——瓶颈是**开火节奏**不是单发；若要再进一步，需另裁「一次敌机齐射换 K 炮 / 窗口内按自身装填连续开火」这类节奏口径（本次未动）。**⑥验证**：新增 `tests/pd-damage-ladder.test.ts` **4 例**（三档单发/DPS 绝对值 · 对舰 DPS 反超同档炮台 1.2~1.3 倍且射程更短 · 防空属性与射程 ≤2,500m 不退让 · 旧口径作废核对）；全量 core 绿 · typecheck 四包 · content:check 绿（防空契约与产物说明契约均过）· **⑦既有用例适配（如实登记）**：`tests/foe-drone.test.ts` 的「备用机库：前线战损 ⇒ 满血补位」原断言「必须存在**存活的**补位机」（旧平衡下的偶然现象——四门近防炮 150 秒打不光整群）；新口径下整群（含补位机）可能被打光 ⇒ 该断言改为核对**补位确实发生**（`inHangar` 随机库战损同步减少）+「存活者三层血为满值」，「满血补位」的机制断言未放松、只去掉"必须留下活口"这条与数值绑定的隐含前提。桌面与网页版 build 绿。**【待验收 · 待发布】**

- 2026-09-12：**推送回执：`main` 已推 origin（`d5db9f1..6565dadb`，**13** 个提交）（一号；船长「推送，2 和 3 消档」）**——**①推送方法**：直连 GitHub 今日不稳（TCP 时通时断、TLS 被要求重协商后断链，四次重试全败）⇒ 改走**本机 7897 端口的代理** （`git -c http.proxy=http://127.0.0.1:7897 -c http.sslBackend=openssl push origin main`）**一次成功**；未改任何 git 全局/仓库配置。**②本次上线内容（13 条）**：一号 6 条（装配位数组修复 `7d702e1` · 装配页 CPU 显示 `07cf900` · 主武器口径 `416e752` · 公告清账 `6eec137` · 近防炮蓝图对齐 `b7b8667` · 近防炮伤害 `05cf6859`）+ 二号 6 条（A2 价格权威文档 / A3 战斗字段登记表 / A3 后续六项随档 / 注释修正 / 两次合入）+ 消档 1 条 ``6565dadb`（近防炮开火节奏与二号通讯批公告两条待办**消档**）。**③验证（推送前对最终 HEAD 复跑）**：全量 core **1037/1037**（104 文件）· typecheck 四包 · content:check 绿 · 桌面与网页版 build 绿。**④远端核对**：`git ls-remote origin refs/heads/main` = 本地 HEAD = `6565dadb`；三树（main / d2 / verify）均干净。**⑤后续**：本条回执为推送后新提交，留待下一次推送一并带上。【已推送 · 回执留存】

- 2026-09-12：**A2 价格权威：只写文档、不改护栏（审计第五条 · 收尾）（二号；船长裁定「丁：只写文档，不改护栏」）**——**①先修正了审计初判**：价格在数据侧确有 **5 份字段 + 1 套 CSV 工作台**，但逐条核过之后，真相**不是"同一价格五份副本"**，而是两件不同的事：**(一) 本该相等的三处**——`blueprints` 书价（94 处，✅ 已有硬契约）· `ships.priceIsk`（25 处）· `shipBlueprints.priceIsk`（24 处），后两者**仅预警不阻断**（2026-09-11 船长「3 改为预警」）；**(二) 本不该相等的一处**——`items.baseSellPriceIsk`（35 处）是**另一个概念**（基础卖价/保底，进采掘结算 · 打捞定价 · 工业估值共 14 个读取点），与行情锚 `marketCatalog.basePrice`（272 处 = **真源**）**本就允许不等，且完全没有对账**；另有 `content-csv` 三张表是**事实上的第三份**（导入后必须跑 `content:check`）。**②实证（一次性探针，已删）**：34 件对齐样本的"物品基础卖价 ÷ 行情锚" = 最小 **0.857**（动能弹 6/7）· 中位 **1.000** · 最大 **1.083**（富凡晶石 13/12）· **28/34 恰好 1.000** · 越出 `[0.6, 1.6]` **0 件** ⇒ 现状健康，**故本轮不动数据、不动护栏**。**③交付（纯文档）**：设计稿 **`v9-market.md` 新增 §十二「价格权威关系」**（真源 / 副本 / 护栏状态 / 基线读数 / **"本次明确不做"三条清单**，供日后收紧时立项）· 词典新增「**价格权威关系（真源 = `marketCatalog`）**」条目（同内容精简版，含实测基线）。**④验证**：纯文档批，仍复跑四连确认无回归——typecheck 四包 · core **1033/1033**（103 文件）· content:check 绿 · 桌面 build 绿；临时探针 `tools/_price-gap.ts` **已删**（`tools/_*` 残留 0）；文档三自检通过（往返解码 0 乱码 · BOM/行尾一致 · 与父提交逐行 0 缺失）。**⑤审计五条至此全部落地**：A1（安全等级口径收口，已改）· B2+B3（抽取单点化，已改）· B1（远端衰减合一，已改）· A3（战斗字段登记表 + 编译期守卫 + 七项"重载即丢"全修，已改）· **A2（价格权威：文档化）**。【待验收 · 待发布】

- 2026-09-12：**近防炮三张蓝图按全表口径对齐（一号；船长就 B3 体检预警裁定「乙」）**——**①起因**：`content:check` 的「蓝图价格口径」常驻 **6 条预警**（书价 3 处 + 料/价 3 处），全表 94 张里**只有近防炮这三张**不合规。**②根因（漏对齐，非有意偏离）**：2026-09-11 机群批 S4 补档（`011f8e3`）加这三张书时，**书价直接照抄了产物现货价**（= 档位系数 ×1）、材料按"造得出"配 ⇒ 料/价 15.8~17.6%（同族锚 45%）。**③本批改动（`data/blueprints.ts` + `data/marketCatalog.ts` 两处同值）**：书价 **120,000 → 236,000**（= 118,000×2）· **315,000 → 787,500**（= 315,000×2.5）· **690,000 → 2,760,000**（奇货 ×4）；材料抬到锚 45% ⇒ 材料总价 **20,800 → 52,800**（44.7%）· **54,400 → 141,400**（44.9%）· **109,200 → 308,000**（44.6%），材料种类不变（三钛合金 / 类晶体胶体 / 超噬矿）。**④读数**：体检由「6 条 ⚠」变为 **94/94 合规、硬契约 0 · 系数不符 0 · 料/价出带 0**；档位分布随之 民用·基础·MK1 37 / MK2 28 / MK3 25 / 奇货 4（共 94 张、单独覆盖 6 张=弹药线）。**⑤玩家可见影响**：三档蓝图买价上调 2~4 倍、自造料钱 ×2.6~2.8（与全表「书价 ≈ 成品 ×2、蓝图是长期投资」的设计一致；此前近防炮漂到"买书≈买成品"、自造成本仅成品价 ~18%）。**⑥未动**：成品价（118,000/315,000/690,000）与三张书的 `buildCostIsk`（2026-09-08 起已不收取的历史遗留字段）。**⑦文档**：词典「蓝图价格口径」行补 (c) 条与档位分布数字。**【待验收 · 待发布】**

- 2026-09-12：**A3 后续：七项"重载即丢"的缺陷全修（船长裁定「六项全修」）（二号）**——**①背景**：A3 登记表把现状如实登记后，暴露出七项"重载即丢"的**真缺陷**（与 2026-09-11 `hullEscapeFrac` 同类）；船长裁定全修。**②改动（登记表 `runtime` → `persist` + 补清洗器）**：`repair`（维修装置快照 + **预载组件账本**：过去重载后**组件凭空消失、战后无从退回**）· `dronePools`（我方机群生存池：过去重载后**无人机不再会被击落**）· `foeDronePools`（敌机生存池：过去重载后**敌方机群整支消失** = E/G 族第二套火力报废）· `droneLost`（击落记录：过去**可反复重载规避战损**）· `droneLoadAtStart`（开战机群快照：战后"战损过半"判定失效）· `foeDroneRangeBuff` / `foeGunRangeBuff`（两条受击增程：本场永久的机制状态被重置）；另修 `escapeReason` **只认 `'hull' | 'timeout'`** ⇒ 补 **`'cannot-engage'`**（无法交战脱战的场次重载后不再退化成"结构撤退"口径，战报与结算措辞随之正确）。**③清洗口径**（新增 5 个清洗器，一律"坏值丢弃、不崩、零迁移"）：计数表（键非空 + 值 ≥0 整数）· 单架生存池（三层血齐备才收；机型/闪避/抗性/备用机字段按形状带过）· 我方池（字符串键还原为数字下标）· 敌机池（tag → 逐架数组，空数组丢键）· 维修态（units 须 `moduleId` + `kitId` 齐备；`kits`/`pulses`/`kitsUsed`/`nextPulseAtMs`/`kitsUsedByType` 逐项兜底）。**④读数**：登记表 **24 项 persist + 7 项 runtime**（运行态只剩：敌突进三字段 · 反应式防空的受击时刻 · 提示条 · 点防冷却 · 集火锁定）。**⑤验证**：`tests/save-battle-fields.test.ts` 两例扩写——七项**逐项写死抽样**（含 `repair.kits` 整块一致、敌机备用机字段 `inHangar`/`readyAtMs`/`maxS~maxH`、`escapeReason = 'cannot-engage'`）+ runtime 一例改为仍不入档的四项（`pdFocus` / `pdCd` / `notices` / `foeChargeOn`）；**负向验证沿用同一条用例**（本轮已实测：persist 字段误登记为 runtime ⇒ 往返用例红；登记表塞不存在的键 ⇒ 编译期 `TS2353`）；全量 core **1033/1033**（103 文件）· typecheck 四包 · content:check 绿 · 桌面 build 绿。**⑥已知取舍**：**(a)** 这是**行为改动**——存档形状多了 7 个**可选**键（旧档读入零迁移 ✓、新档战中重载状态更完整 ✓）；**(b)** `foeChargeOn` / `foeChargeCdUntilMs` **仍不随档**（"重载即重置突进循环"是 2026-09-10 起的既有口径，本轮未动）；**(c)** 抗性表按形状带过、不做逐项校验（坏形状退化为"无抗性"，不崩）。【待验收 · 待发布】

- 2026-09-12：**公告待审稿批量清账：剩余待审稿一律不发布，两份稿整体作废留档（一号；船长「B1，B2，清理剩余待审稿，不发。」）**——**①作废范围**：一号那份 `docs/design/announcement-draft-20260912-pilot1-batches.md`（9 张卡：卡 1/2/3/5/7/8 早先已裁不发布、**卡 9 位数组修复本次一并作废**）与二号那份 `docs/design/announcement-draft-20260912-foe-overhaul.md`（「敌人重做」单条）——两份稿的标题/状态行改为**全部作废 · 不发布**，「待船长裁定」段收口为「不发」。**②不受影响**：早先已批准的卡 4（协处理器）与卡 6（稀有残骸保底）仍逐字生效，`packages/data/src/announcements.ts` 保持 **29 条**不变（本次**零数据写入**）。**③留档口径**：稿子按本仓既有惯例保留原文备查（同目录另有 20 余份历史待审稿同此处置），今后不得再写入数据层。**【已结案】**

- 2026-09-12：**A3 战斗字段清单收口：登记表 + 编译期完整性守卫（审计第四条 · 纯重构 · 零行为变化）（二号；船长「继续」）**——**①病根**：载入侧 `cleanBattle` 是一份**手抄白名单**（15 项），与 `BattleState`（**31 个字段**）并行维护 ⇒ 加字段忘了收录就**静默丢状态**（2026-09-11 就漏过 `hullEscapeFrac`：连续作战保险凭空失效、该撤退的场次继续打到弃船）。**②落法**：新增 **`BATTLE_FIELDS` 登记表**——`BattleState` 的每个字段都必须登记为 `{ kind: 'persist' }` 或 `{ kind: 'runtime'; why: '…' }`（运行态**必须写明理由**），并以 **`satisfies Record<keyof BattleState, BattleFieldSpec>`** 把"漏登记 / 多登记"变成**编译期错误**；`cleanBattle` 改为**按登记表组装**（只带出 persist 字段，原有清洗逻辑逐字未改，undefined 照旧被 JSON 丢掉）；导出 `BATTLE_PERSIST_KEYS` 供用例逐字段守"重载不丢"。**③读数**：**17 项 persist + 14 项 runtime**（运行态逐条注明理由：敌突进循环 · 维修装置运行态 · 我方/敌方机群生存池 · 两条受击增程 · 提示条 · 点防冷却与集火锁定 · 开战机群快照 · 已停用字段）。**④验证**：新增 `tests/save-battle-fields.test.ts` **2 例**——**逐 persist 字段真存档往返比对**（含 `hullEscapeFrac` / `autoEscaped` / `escapeReason` / `waveIdx` / `waveClearAt` / `ammoIds` 写死抽样；`fxSeq` 是**派生字段**（按 fx 环尾部重算）故单独断言派生式）· runtime 字段**有意不入档**（塞值后重载必须消失）；**负向验证两条都如实红**——把 `hullEscapeFrac` 误登记成 runtime ⇒ 往返用例红（`expected undefined to be 0.5`）；往登记表塞一个不存在的键 ⇒ **编译期报错**（`TS2353: Object literal may only specify known properties`）；还原后全绿；全量 core **1033/1033**（103 文件，+2）· typecheck 四包 · content:check 绿 · 桌面 build 绿。**⑤本轮只做"登记 + 守住"，不改持久化范围**（分类仍照现状）⇒ 由此**暴露出一批"重载即丢"的真缺陷，待船长裁定后另立批次**：`repair`（维修装置 + **预载组件账本**丢失 ⇒ 组件凭空消失且战后无从退回）· `droneLost`（本场击落记录清零 ⇒ 可反复重载规避战损）· `dronePools`（我方无人机重载后不再会被击落）· `foeDronePools`（敌机机群整支消失 = E/G 族第二套火力报废）· `escapeReason` 只认 `'hull' | 'timeout'`（**丢掉 `'cannot-engage'`**）· `droneLoadAtStart`（战后"战损过半"判定失效）与两条受击增程（重载后回到未触发、会重复弹提示）。【待验收 · 待发布】

- 2026-09-12：**B1 远端衰减合一：`beamPowerFactor` 收进 `distFactor`（审计第三条 · 纯重构 · 零行为变化）（二号；船长「下一步」）**——**①先把前提证死**：两份实现**逐字相同**，唯一差别是 `beamPowerFactor` 多一层 `Math.max(0, …)`；而 `t` 已 clamp 到 `[0,1]` ⇒ `1 − t(1−falloff) ≥ falloff ≥ 0` **当且仅当 `falloff ∈ [0,1]`** ⇒ 该层恒不生效。**②合并**：`beamPowerFactor` 改为**委托** `distFactor`（保留名字与"**威力**衰减"的语义标签，与动能/爆炸的"**命中**衰减"并列；`hitChance` 与 `foeGunPowerFactorOf` 本来就都走 `distFactor`）⇒ 全仓"远端衰减"自此**只有一处实现**。**③加前提守卫**（新契约「**远端衰减取值域契约**」）：扫 **56 处** `falloff`——`MODULES` · `FOE_SHIPS` · `FOE_DRONES` · 我方 `DRONES`（⚠ 此前**没从 data 包导出**，本次一并补上）· 卡面条目覆写（`ANOMALIES_FLAVORED[].ships[].falloff`）· `balance.battle.foeFalloff` ⇒ 任一处越出 [0,1] 即报错（越界会让合并前提失效、静默产生负命中/负威力）。**④验证**：`tests/combat.test.ts` **+1 例**（6 组射程带 × 10 个距离**逐点 `toBe` 相等**，含退化带 `max ≤ min`、`falloff` 取 0 / 0.1 / 0.3 / 0.5 / 1、距离取负值与超远值）；**负向验证两条都如实红**——把 `beamPowerFactor` 偏 0.01 ⇒ 等价用例红（`expected 1.01 to be 1`）；把一处 data `falloff` 改成 1.5 ⇒ 体检**点名报错**（「敌机 foe-drone-e-alert = 1.5」）；还原后全绿；全量 core **1031/1031**（102 文件，+1）· typecheck 四包 · content:check 绿 · 桌面 build 绿。**⑤顺带修一处自相矛盾**：体检汇总行原来在有越界时仍打印"全部落在 [0,1]" ⇒ 改为按结果分支（有错时打印"有 N 处越界"）。**⑥已知取舍**：**(a)** 保留 `beamPowerFactor` 这个名字（`index.ts` 导出、界面 `shipInfo.tsx` 在用），只是实现委托——删名会牵连 UI 与 3 个用例文件，收益不值；**(b)** `distFactor` 仍**不夹下界**（与改前逐字一致），因为前提已由契约守住；**(c)** 审计清单里 **A2（价格权威）与 A3（战斗字段清单）**未动，按顺序排在最后。文档：词典「远端衰减（falloff）」条目补"单一实现 + 取值域契约"，并清掉其中一条**已过期的 d2 未同步警告**。【待验收 · 待发布】

- 2026-09-12：**B2+B3 抽取单点化：`pickOne` / `pickWeighted` 收口 26 处手写（审计第二条 · 纯重构 · 零行为变化）（二号；船长「下一步，」＝按 A1 → B 的顺序推进）**——**①病根**（审计结论）：`rng.ts` 只有 `nextInt`，于是"**均匀取一个**"在 6 个文件手写了 **18 处**（`salvage` 9 · `combat` 3 · `expedition` 2 · `market` 2 · `encounters` 1；另有 2 处数量骰/下标骰），"**按权重取一个**"手写了 **6 套**（`events` / `expedition` / `mining` / `market` / `salvage` / `salvaging`），边界写法还不一致（4 处 `r < acc`、2 处 `r <= acc`）。**②新增两个单点函数**（`packages/core/src/rng.ts`，并从 `index.ts` 导出）：`pickOne(rng, arr)` 与 `pickWeighted(rng, arr, weightOf, opts?)`。**关键契约 = "抽几次随机数"也一致**：两者的 `nextRandom` 消耗次数与手写式**逐一相同**（**连空表 / 总权重 ≤ 0 的路径也"先抽再判"**——`nextRandom` 把 `count` 记进存档，序列一挪位离线结算结果就全变）；权重按 `max(0, w)` 夹紧（`mining` 原本即如此）；`bound: 'lt' | 'lte'` 只为逐字复刻原算术形式。**③替换 26 处**：18 处均匀取（含 `combat` 3 处 `Math.min(len-1, …)` 兜底写法与 `salvage` 9 处）+ 2 处数量骰改 `nextInt` + 6 处加权取（各自保留原有的"`total ≤ 0` 早退在抽之前"与无中选兜底：`expedition`→首事件 · `mining`→主产物 · `salvage`→池首矿种 · `salvaging`→池首 · `market`→不生成 · `events`→订单事件）。**④验证**：新增 `tests/rng-pick.test.ts` **6 例**——用**手写参照实现**逐位对照（结果与 `rng.count` 都相等，500 次采样 ×2 种边界）· **空表也消耗一次**（最关键契约）· 总权重 ≤ 0 / 负权重夹紧 · 分布 sanity；**主判据 = 既有种子确定性用例**：全量 core **1023/1023**（101 文件，+6）一次通过（离线结算 / 战报 / 掉落 / 市场抽取全按种子断言，序列一移位必红）。**⑤负向验证（如实登记，一条没红）**：**(a)** `pickOne` 改成"空表早退不抽" ⇒ 「空表也消耗一次」如实红（`expected +0 to be 1`）；**(b)** 把 `pickWeighted` 的 `inclusive` 写死 false ⇒ **用例没红**——因为 `lt` 与 `lte` 只在"抽到的 `r` **精确命中**累计边界"时不同（`r = k/2³² × total` ⇒ 概率 ≈ 2⁻³²），用例构造不出该情形；已把实情写进 `rng.ts` 与用例注释，并保留 `bound` 选项。**⑥已知取舍**：**(a)** 两个单点函数**故意不做空表早退**（会挪位 rng 序列），调用方自行判空；**(b)** `pickWeighted` 的权重函数被调两次（求和 + 遍历），现有 6 处都是小表纯计算、无性能问题；**(c)** 本轮**只动抽取**——`hitChance` / `distFactor` 衰减函数合一（B1）按顺序留到下一批。文档：词典新增「抽取单点（`pickOne` / `pickWeighted`）」条目。【待验收 · 待发布】

- 2026-09-12：**主武器口径修正：贴脸/中距/风筝与「我方射程带」改按「射程最远的武器」（一号；船长转玩家反馈「战斗界面下方的快速选择贴脸 中距 风筝，现在的距离不正确」→ 裁定「甲」）**——**①根因（口径 bug，不是数值问题）**：三按钮、出发前战术、战斗界面射程带都取 `weapons.find(w => w.kind === 'gun') ?? weapons[0]` 当「主武器」，而**激光是 `beam`、基础舰炮（恒在的兜底武器）是 `fixed`** ⇒ 纯激光/无人机船回落到基础舰炮（2,500m）；**近防炮是 `turret` 槽（`kind = gun`，2,500m）** ⇒ 装了它的船一律被 PD 抢位（该件自己的设定写的就是「占一个高槽、**不是主炮替代品**」）。**"现在"才变错的原因 = 近防炮**：此前玩家高槽只有炮台/导弹架这类正经主武器，旧写法恰好取对。**②甲（船长裁定）**：主武器 = **射程最远的武器**（并列射程取名义火力大的，再并列保持武器表顺序）⇒ 新增 core `mainWeaponOf` 单点；三条公式**一字未动**（贴脸 = 近端×0.6 / 中距 = 带中点 / 风筝 = 最远×0.95）。**③单点化四处**：`desiredRangeFor`（三按钮与出发前战术）、`battleZonesFor`（战斗界面「我方射程带」）、`steadyDistance`（解析口径的双方中点，经 `desiredRangeFor`）、`battleArcsFor`（战场弧新增 `isMain`，界面不再自己 find——弧端米数刻度照它画）。**④实测读数（真数据真引擎）**：激光船（4,600m）**200/1250/2375 → 200/2300/4370**，射程带显示「基础舰炮 0~2500」→「轻型激光炮 MK1 0~4600」；近防炮 + 激光 **200/1251/2375 → 200/2300/4370**；近防炮 + 导弹架 MK2（11,760m）**200/1251/2375 → 540/6330/11172**，射程带「近防炮 1~2500」→「重型导弹架 MK2 900~11760」；**炮台船与纯导弹船一字不变**（旧口径恰好取对）。**⑤验证**：新增 `tests/main-weapon-range.test.ts` **7 例**（激光船不回落到基础舰炮 · 近防炮不抢位 · 只有近防炮时主武器就是它 · 并列射程取火力大的 · 射程带与三按钮同源 · 炮台/导弹船回归 · **真数据**战场弧 `isMain` 与三按钮同源）；**负向验证**：把 `desiredRangeFor`/`battleZonesFor` 临时改回旧口径 ⇒ 3~4 例如实红（1250 vs 2300、2500 vs 4600），把弧上标记改回「第一个 gun」⇒ 真数据例红（2500 vs 4600），还原后全绿；全量 core **1024/1024**（101 文件）· typecheck 四包 · content:check 绿 · 桌面与网页版 build 绿。**⑥文档**：词典新增「主武器（战术距离口径）」行（含旧口径作废说明与实测读数）。**【待验收 · 待发布】**

- 2026-09-12：**A1 安全等级口径收口（审计第一条）：扫描保留 0.5、只修文案 · 掉落 0.0 并入低安档 · 主题件档位加断言（二号；船长「按顺序挨个来，A1先细说扫描/掉落和低安的关系」→ 三问三答「乙：保留 0.5，只修文案」＋「甲：0.0 并入低安档」＋「乙：仍手写，但加断言拦住」）**——**①先把三条线讲清**（审计结论）：**伏击** = `sec ≤ 0`（今日已定）· **扫描窗口延长** = `sec < 0.5`（含全部中安；`explore.ts` 三处硬编码：算时长 / 提示文案 / 读档上限）· **掉落** = 分三段：池均价溢价 `mSec = 1 + 0.45×max(0,−sec)`（连续函数，sec=0 处本就无溢价）· MK2 完好舰体层 `sec < 0` · 主题件档位**逐卡手写且无契约**（09-11 互换安全等级时靠人手改过两张）。**②改动**：**(a) 扫描文案**——`explore.startScan` 此前在 `sec<0.5` 时一律打印「该星系为**低安**」（把中安也叫低安＝名词错，上批漏改），现按**两件事**分两支：扫描偏慢仍按 0.5、**遇袭只发生在低安**（`sec ≤ balance.encounter.lowSecMax`）；**(b) 掉落并入低安档**——`salvage.recycleProfileOf` 的 `lowSec` 由 `sec < 0` 改 **`sec ≤ 0`** ⇒ 烬火星区/回音荒区（均 0.0）从此吃 MK2 完好舰体层；两张卡主题件由中安档 `modules` 改到低安档 `mk2`（`ano-cinder-siege` 烬火围攻、`ano-echo-haunt` 回音残舰），`balance.ts`/`salvage.ts` 注释同步；**(c) 新断言**——体检新增「B3.1 主题件档位与星系分区一致」（中安只允许 `modules`、低安只允许 `mk2`，分区**读 `securityZoneOf` 单点**）。**③顺带修两处我自己的漏改**：体检「日板席位可行性」原先**内联重算**分区（旧边界 `v >= 0`）⇒ 改读 `securityZoneOf`（并补 import）；体检里两处过期注释/描述（触发器 kind 注释、机制信登记表的 face 描述仍写 `sec < +0.5`）。**④验证**：`tests/salvaging.test.ts` **+1 例**（sec=0.0 掷 MK2 层 / sec=0.1 中安不掷）；**负向验证两条**——`lowSec` 临时改回 `sec<0` ⇒ 新例如实红（`expected +0 to be 4`）；把一张中安卡（幽灵舰信号）临时改挂低安档 ⇒ 体检**如实红并点名**（「B3.1 主题件档位与星系分区不符：幽灵舰信号（galaxy-redring，中安）挂了低安档 mk2」）；还原后全绿；全量 core **1017/1017**（100 文件，+1）· typecheck 四包 · content:check 绿 · 桌面 build 绿。**⑤已知取舍（如实登记）**：**(a)** 扫描慢与遇袭**从此是两条线**（0.5 vs ≤0）——按船长裁定保留，已在提示文案与词典里写明"各系统用哪条线"；**(b)** 主题件档位**仍逐卡手写**（只加断言）⇒ 以后再改安全等级，体检会报错但仍需手工改卡（船长选的口径）；**(c)** 池均价（B3.1 ±3%）**未受影响**：mSec 在 sec=0 处本就无溢价，且主题件增删由引擎按均价反比缩放（EV 守恒）。文档：`b1-lowsec-encounters.md` 阈值小节改写为"已定案"· 词典「高安/中安/低安」条目补"各系统用哪条线"。【待验收 · 待发布】

- 2026-09-12：**装配页 CPU 显示改口径：超载给负数 + 「差 N」并标红，顶条显示「超 N」，刚好装满不再算红（一号；船长转玩家反馈「装备的 CPU 大于剩余 CPU 时，卡片上只会显示剩余 CPU 到 0，不会显示负数，玩家无法看到自己还差多少 CPU，也没有变红的警示」→ 裁定「按甲来」）**——**①根因**：装配浮层候选卡的差异段与顶部 CPU 条都把剩余**钳到 0**（`Math.max(0, 预算 − 占用)`），且段色写死为中性青⇒ 超预算时只显示「CPU 剩 15→0」，看不出差多少、也不变红；若当前已满载/超载（旧档），两栏都钳成 0 ⇒ **该段整段不显示**（卡片上完全没有 CPU 提示）。**②甲（口径）**：剩余**允许为负**——**超载** ⇒ 候选卡 `CPU 剩 11→−9（差 9）`**红**（复用既有 `.dseg.is-down` `#ff9288`）、顶条 `−15 / 130` + 右侧「超 15」**红**（`is-full`）；**刚好装满**（= 0，**合法、装得进**）从红里分出来 ⇒ 顶条右侧「刚好装满」**琥珀**（`is-warn`）。**判据 = "装后是否超预算"，不是"数值有没有变化"**：已超载的船换同耗件照红、换成更轻但仍超载的件也照红；两栏各用自己的预算（协处理器装卸前后预算不同）。**③连带（同页同一量的显示一致性）**：无人机装入弹层的「余 CPU」由钳制值改为**原值**（可为负），**钳制值仍用于"还能装几架"的功能判定**（不改行为）。**④文案**：浮层说明补「红色的「CPU 剩 …（差 N）」= 装后超预算，这件装不上（先卸件，或低槽装一件「协处理器」扩容）；红色的属性段只是数值下降，照样装得上」；顶条悬停补「剩余 0 = 刚好装满（装得进，但加不了新件）；负数 = 超载（装不上，先卸件或装协处理器扩容）」。**⑤真机实测（无头 Chrome + 网页版同一份 renderer；鲸吞级 CPU 130，四态存档）**：有余量 `58 / 130 · 45%`（青 `is-ok`）· 接近满载 `11 / 130 · 8%`（琥珀）且**同一浮层里合法卡 `CPU 剩 11→8`（青 `rgb(147,216,210)`）与超载卡 `CPU 剩 11→−9（差 9）`（红 `rgb(255,146,136)`）同框** · 刚好装满 `0 / 130 · 刚好装满`（琥珀边框 `rgba(232,180,90,.85)`，**不再红**）· 旧档超载 `−15 / 130 · 超 15`（红边框 `rgb(217,106,106)`），其卡片 `CPU 剩 −15→−18（差 18）` 红、而同框的「协处理器 MK3」`−15→42` 青（**扩容这条出路看得见**）。**⑥跨窗口尺寸几何**（1600×1000 / 1366×768 / 1024×768 × 两态）：条内 `scrollWidth = clientWidth`（552/552 · 431/431 · 240/240）、尾字不越条右边界、一级页无纵横滚动条、浮层固定 590×485 且无内滚。**⑦验证**：typecheck 四包 · core **1016/1016**（100 文件，含同期二号批次）· content:check 绿 · 桌面与网页版 build 绿。**本条为纯显示改动，不涉引擎判定**（装配仍由 `equipment.cpuOverloadText` 按原口径拒绝，toast 文案与数字与卡片一致）。**【待验收 · 待发布】**

- 2026-09-12：**战斗画面提示显示时长 ×3（4 秒 → 12 秒）（二号；船长「顶部的「静滞阵列解除限幅：静滞卫舰炮台射程 +50%」这类文字显示持续时间变为300%」）**——改动 = `apps/desktop/src/renderer/src/panels/BattleScreen.tsx` 的 `NOTICE_LIFE_MS` **4,000 → 12,000ms**（**唯一出处**：全仓只有这一处读它；样式表 `.app-bts-notices` 只有定位与排版、**没有自带动画时长**，故改这一个数即生效）。**口径**：只作用于**引擎推来的战斗提示**（`battle.notices` —— 受击增程「巨构残存程序过载：警戒机群解除射程限制」/「静滞阵列解除限幅：静滞卫舰炮台射程 +50%」、敌方增援提示等）；①「敌方增援正在接近…」那条按**增援窗口**（`waveClearAt`）常显、与固定时长**不同源**，**本次未动**（要一起改说一声）。⚠ 生命期读的是**战斗时钟**（`battle.lastTickGameMs − n.atMs`）而非真实时钟 ⇒ 快进/加速下墙钟感受会更短（与改动前同口径）。**验证**：typecheck 四包 · core **1016/1016**（100 文件）· content:check 绿 · 桌面 build 绿；纯界面常量、core 零改动，故不新增用例（仓内 UI 无测试框架，按既有口径 = 构建 + 单点复核）。**文档**：D 族设定稿 §十 补一行提示时长。【待验收 · 待发布】

- 2026-09-12：**D 族静滞卫舰「受击增程（炮台）」：挨打后射程 +50%、仅影响所有静滞卫舰（二号；船长「给D族静滞卫舰加入类似E族挨打加炮台射程的效果，不过仅影响所有静滞卫舰。挨打后射程增加50%。」→ 三问三答：「甲：任一静滞卫舰挨打 ⇒ 全体静滞卫舰 +50%」＋「乙：只延长最远射程，旧距离内读数不变」＋「甲：弹战斗画面提示」）**——**①四步闸门**：先核事实（静滞卫舰 = `FOE_D_STASIS`，T3 · 速度 129 · **光束必中** · 射程带 **2,062~12,000m** · 只出现在**穹顶守卫 96**：静滞卫舰 ×2 + 守墓长舰 ×1）再提问，三条口径由船长选定后落码。**②新开一套，不复用 E 族机群那条**：`FoeShipDef.gunRangeMulOnHit: 1.5`（只登记 `FOE_D_STASIS`）→ `UnitSpec.foeGunRangeMulOnHit` → **标量** `BattleState.foeGunRangeBuff`（一次触发、本场永久）——E 族的 `droneRangeMulOnHit` 作用于"整支敌队的机群"，本条**只作用于带该字段的敌舰**（= 所有静滞卫舰），两套状态互不覆盖。**③口径**：**触发** = 任一静滞卫舰**本体被命中一次**（打机群不算、未命中不算），**只推一条**画面提示「静滞阵列解除限幅：静滞卫舰炮台射程 +50%」；**射程** = 只延长最远射程 **12,000 → 18,000m**、近界 2,062 不动；**折减** = **原射程内一字不变**、延长段**按同斜率**继续线性衰减（18 km 处 ≈ ×0.20，而非趴在 `falloff`=0.5 的平台）。**④单一出处**：新增 `foeGunRangeMulOf` / `foeGunMaxRangeOf` / `foeGunPowerFactorOf`，**开火射程门 · 命中与伤害衰减 · 战斗界面底部射程标签**三处共用（`hitChance` 加可选 `dfOverride`，缺省零行为变化）。**⑤未动**：E 族的机群增程、`rangeBuff`（机群阵位后撤旗标）一字未改——不把炮台增程混进无人机视觉。**⑥验证**：新增 `tests/foe-gun-range.test.ts` **6 例**（真卡真舰级：建档只有静滞卫舰带倍率 · 射程 12,000→18,000 且守墓长舰 7,391 不变 · **折减在原区间与 `beamPowerFactor` 逐字相等**、18 km 处 ≈ ×0.1987 且 < 0.25 · **真引擎触发**（60 秒内自然打中本体 ⇒ 盖章 + 提示恰一条、续打不重复）· 负向「只有守墓长舰的卡跑满 60 秒绝不触发」· **界面同源**（底部标签出现 `2,062~18,000` 带、聚合带 maxM = 18,000））；`content:check` 新增「机群与防空契约」**⑤c-2**（只允许 D 族静滞卫舰 · 倍率 ≤ 2 · **必须真出现在某张卡编成里**）+ 汇总行读数；**负向验证**：把数据侧那一行临时摘掉 ⇒ **6 例中 5 例如实红**，还原后全绿；全量 core **1012/1012**（99 文件，+6）· typecheck 四包 · content:check 绿 · 桌面 build 绿。**⑦已知取舍（如实登记）**：**(a)** 静滞卫舰是**光束必中** ⇒ 延长到 18 km 意味着**18 km 内必中**，穹顶守卫 96 的远程压力明显上升；**(b)** **未跑标定**（按船长口径「不跑标定除非船长说跑」）——穹顶守卫 96 的实测读数会变，要重标由船长立项；**(c)** 只推一条提示、不逐舰推（该型舰共享状态，与 E 族三次裁定同口径）。文档：D 族设定稿新增 **§十「受击增程（炮台）」**（含口径、落点、实测读数）· 词典新增「受击增程（炮台）」条目【待验收 · 待发布】

- 2026-09-12：**装配位数组按船型布局补齐——修玩家报障「装了 1 个 CPU 后，装其他装备显示『该 Y 槽位不可用（第 X 位）』」（一号；船长转玩家反馈 → 裁定「按推荐来」= 甲 + 丙）**——**①定性：真 bug，且是同批引入的回归（与协处理器本身无关）**。`emptyFitted()` 造出的位数组恒为 **1/1/1**、与船型布局无关；只有 `fitModule`（装时按布局补齐）与载入修复链 `repairDeprecatedModules` 会把它拉长。2026-09-11 协处理器批次把装配页装入从 `fitModuleTo`（会补齐）改成原子换装 `swapModuleTo`（提交 `2d92718`），而 `swapModuleAt` **不补齐**、界面却按 `Math.max(船型布局, 数组长度)` 画满槽位格（`FitPage.tsx:677`）⇒ **每类槽位只有第 1 位能装**。玩家「先装 CPU」的实况 = 协处理器装进能用的第 1 位、接着装别的件落在第 2 位被拒——**文案逐字复现：「该低槽位不可用（第 2 位）。」****②甲（core 单点）**：新增 `ensureRackBays` + `wantedBaysOf`，`fitModule` 与 `swapModuleAt` 共用「补齐到船型布局」（与修复链的位对齐同口径、幂等、只补空位、不动已装件）。**③丙（应用层）**：`engine.start` 原先只在「读到存档」分支里跑修复链 ⇒ **新档首局同样不对齐**（初始船 鲣鱼级 高3/中2/低1、沙猫级 高2/中1/低1：除第 1 位全被拒）；改为 try/catch 之后**无条件跑一次**（仍在离线结算前，读档路径顺序不变）。**④实测（真数据真引擎，牛鲨级 高5/中3/低4）**：新造/新买的船入手时 1/1/1、界面画 4 个低槽格；修前低槽第 2/3/4 位与高槽第 2/5 位**全部**被拒，修后**全部成功**并把数组补齐到 5/3/4（真越界到第 5 位仍照旧被拒）。**⑤给玩家的救急（修前即可用）**：重开一次游戏——读档过修复链即对齐，不影响存档内容。**⑥验证**：新增 `tests/fit-bay-align.test.ts` **4 例**（1/1/1 下按位号装布局内任何位 · 真越界仍拒且文案不变 · 两条命令同口径 · 与修复链一致且幂等不丢件）；**负向验证**：临时撤掉 `swapModuleAt` 的补齐 ⇒ **4 例全红**（首例如实报出玩家原话），还原后全绿；全量 core **1010/1010**（99 文件，+4）· typecheck 四包 · content:check 绿 · 桌面与网页版 build 绿。**⑦顺带核过（都不是缺陷）**：引擎**无同类装备数量上限**（装配只校验槽位 + CPU 预算：牛鲨级不装协处理器可装 4 件低槽装甲、5 门高槽炮）；协处理器与其它件抢低槽位属**设计内取舍**（低槽件 4 → 3）。**【待验收 · 待发布】**

- 2026-09-12：**低安口径归一：伏击只掷低安（sec ≤ 0，含 0）+ 分区边界同步（二号；船长「将伏击掷骰阈值降低为0，之后修改通讯。」→「0也算低安」；本条修订同日上一条「星系机制通讯」里的低安触发口径）**——**①起因**：船长指出刚交付的那封「航线须知」把「安全等级不到 +0.5」当低安讲是错的（全仓分区口径见 `sideTasks.ts` 的 `SecurityZone`：高安 ≥ 0.5 / 中安 0~0.5 / 低安 < 0）。**②实测定位到一处真漂移**：`encounters.rollLowSecAmbush` 的到点遇袭率公式 `p = 0.05 + 0.15 × min(1.5, max(0, 闸门值 − sec))` 把**闸门值（0.5）当成了零风险参考点** ⇒ 实际 sec=0 → 12.5%、−1.0 → **27.5%**，与同段注释写的定义（sec=0 → 5%、sec=−1 → 20%）**差 7.5 个百分点**。**③改动**：`EncounterBalance.highSecSafe: 0.5` → **`lowSecMax: 0`**（判据由 `sec >= 阈值 return` 改 **`sec > 阈值 return`** ⇒ 低安含 0）；公式零点随之归位 ⇒ **曲线回到注释定义**（sec=0 → 5%、−0.5 → 12.5%、−0.9 → 18.5%、−1.0 → 20%）；`securityZoneOf` 的边界由 `v >= 0` 改 `v > 0`（**中安 = 开区间 (0, 0.5)**，0 归低安）；通讯 `lowSec` 触发器与体检死触发器守卫**都改读同一个值**（此前各写死一份 0——负向验证时才暴露）。**④影响面（全 20 星系）**：**会掷 12 个**（烬火 0.0 · 回音 0.0 → 5%；奥罗 −0.2 → 8%；星髓 −0.5 · 天底 −0.5 → 12.5%；裂谷 −0.6 → 14%；深渊 −0.7 · 蜃影 −0.7 → 15.5%；星噬 −0.9 · 虚海 −0.9 → 18.5%；暗星坟场 −1.0 · 穹顶墓园 −1.0 → 20%）；**中安 4 个不再掷**（红环 0.1 · 碎晶 0.2 · 灰霾 0.4 · 灯塔 0.4）。**⑤连带**：日板抽地点的分区把**烬火星区与回音荒区（均 0.0）由中安池移入低安池**（席位数 中安 2 + 低安 3 不变）；首次提示与在场记录只在低安建立。**⑥本批未动（待船长定）**：**扫描窗口延长**仍按 `sec < 0.5`（中安扫描照样变慢、但不会遇袭）；**残骸回收池溢价与完好舰体 MK2** 仍按 `sec < 0` ⇒ **0.0 的两个星系算低安却不吃低安掉落档**。**⑦文案同步**：手册「航行须知 · 安全等级」行改写为「sec ≥ 0.5 高安；0 < sec < 0.5 中安；sec ≤ 0 低安——**只有低安**会遇到拦截与伏击（中安与高安不会）」；通讯那封改写为「航线须知：低安空域」（正文四段：只有低安会动手 / 越低越频繁且扫描越慢 / 负安全等级残骸更值钱 / 半结构自动脱离）；代码注释、设计稿 `b1-lowsec-encounters.md`（含文末「阈值改动」小节）、词典条目同步。**⑧验证**：`tests/b1.test.ts` **+3 例**（中安 0.2 不掷且不记在场/不发首提 · 边界 **sec=0 掷、0.1 绝不掷**（到点率拉满）· **曲线零点实测**：sec=−0.5 得 12.5% 带内（2000 次采样，旧口径为 20%））、`bounty-tasks.test.ts` **+1 例**（分区边界 0 → 低安）、`comms.test.ts` 低安触发器用例改为 ≤ 0；**负向验证**：把 `lowSecMax` 临时改回 0.5 ⇒ **4 例如实红**（b1 三条 + 通讯一条，末条证明触发器真的读同一个值），还原后全绿；全量 core **1006/1006**（98 文件，+4）· typecheck 四包 · content:check 绿 · 桌面 build 绿。【待验收 · 待发布】

- 2026-09-12：**推送回执：`main` 已推 origin（`a444bec..24f562c`，4 个提交）（一号；船长「可以推送了」）**——**①推送内容**：`48044d2` 收尾归档（三树盘点 + roadmap 去重 + 一号续接卡 + 公告待审稿）· `456bd4c` 公告发布（`2026-09-12-cpu-coprocessor` 协处理器 / `2026-09-12-rare-wreck-pity` 稀有残骸保底与说明，共 29 条）· `8a63189` 星系机制通讯（二号：两条触发器 `lowSec`/`foeFamily` + 四封信 + 体检契约）· `24f562c` 合并提交。**②验证（推送后对 `24f562c` 复跑）**：typecheck 四包全绿 · core **1002/1002**（99 文件）· `content:check` 全绿 · 桌面与网页版 build 全绿；`git ls-remote` 核对远端 `refs/heads/main` = `24f562c`、本地 `main` = 本地 `origin/main` = `24f562c`、**待推送 0**。**③状态**：三树（main / d2 / verify）无未提交改动、无 `_` 探针；本次推送包含一号 09-11~09-12 全部批次与二号「星系机制通讯」批。**④如实登记**：二号「星系机制通讯」批随本次推送一并上线，**该批未配公告**（不属一号待审稿件 8 张卡的范围）——是否补公告由其经办人按 §7 判定。**⑤后续**：本条回执与后续文档提交留待下一次推送一并带上。【已推送 · 回执留存】

- 2026-09-12：**星系机制通讯：探索到带特殊机制的星系后收到一封讲解（二号；船长「罗列下目前的特殊机制，并在玩家探索到该具备特殊机制的星系后在通讯界面发一封通讯给玩家，和玩家讲解下对应机制」→ 四问四答：「只发星系，但是在无人机相关的通讯里提到近防炮（或者提示有防空属性的武器）」＋「各合并一封」＋「补送」＋「协会对口部门」）**——**①先交清单再动工（四步闸门）**：把现有特殊机制按作用范围分**三层**交船长过目——**星系级**（奥罗荒环＝E 族巨构残骸带 / 烬火·回音·天底＝G 族鱿烬亡军无人机群 / 安全等级不到 +0.5 的空域 / 红环航道＝全图唯一前哨站选址）、**全局战斗级**（敌方近防炮 · 多舰补偿 `2N/(N+1)` · 窝点 L1~L3 · 残骸密度与回收池）、**族级族格**（A/B/C/D 的战术与火力性格）；船长裁定**只发星系级**。**②两条新触发器，都不写死"哪几个星系"**：`lowSec`＝已点亮星系里存在 **sec < +0.5** 者（阈值**刻意与既有低安判定同源**：`encounters.ts` 暴露面掷骰「高安 sec ≥ 0.5 不掷」、`explore.ts` 扫描窗口惩罚 `sec < 0.5` ⇒ **安全等级被改判时自动跟随**；缺省安全等级按高安 `?? 1`，与 `secOf` 同口径）；`foeFamily`＝已点亮星系里存在**任意一张该族敌卡**（读 `AnomalyDef.foeFamily + galaxyId` ⇒ **敌卡搬家自动跟随**）。**③四封信**（一触发面一封）：`msg-auro-megastructure`（测绘处·剧情：巨构残骸带／警戒机群＝第二套火力且**打掉会从机库补位**／本体挨打后机群 **5,000 → 20,000m** 本场不收回／近防炮专打无人机、清机群只有带防空属性的武器）、`msg-exile-swarm`（航线安全·剧情：蜂群自巡洋舰放出 · **7,000m** · **打光不补** · 薄而快；敌近防炮随舰级且**优先挑哨戒机与攻坚机**）、`msg-lowsec-rules`（航线安全·提示：就地作业会被拦而**航行途中不会** · 安全等级越低扫描越慢（最差 10 → 22 分钟）· **负安全等级星系残骸更值钱** · 结构半数自动脱离）、`msg-redring-outpost`（基建部·委托：前哨站选址与分档交付）。**④同批收掉前一批遗留的界面口径错位**：`battleArcsFor` 敌方逐带聚合原读敌机武器 `maxRangeM`（机型原始射程）⇒ 改读 `foeDroneRangeOf`（与开火射程门同源），底部射程标签随受击增程同步变化（提交 `a444bec`，已推送）。**⑤验证**：新增 `comms.test.ts` **4 例**（lowSec 阈值：0.5 与缺省高安不算、0.2 与 −0.7 算 / 送达幂等 / foeFamily 读数据（**把 G 卡搬到另一星系判定跟着卡走**）/ 老档补送）；`content:check` 新增「星系机制通讯登记契约」（四封必须都在 + 触发面互不相同）与两条**死触发器守卫**（数据里必须真存在 sec<+0.5 星系 · 该族必须真有敌卡）；**负向验证如实**——低安信初稿写「最后一条保命的口径」被体检**当场拦下**（玩家可见文案含开发用词「口径」），改「规矩」后通过；另一条自作用例初稿期望"只补一封"如实红（该星系 sec −0.7 同时满足两个触发面）⇒ 改两封。全量 core **1002/1002**（98 文件，+4）· typecheck 四包 · content:check 绿 · 桌面 build 绿。**⑥已知取舍（如实登记）**：**(a)** 低安信的触发面是**机械阈值**（全仓 15 个星系在 `sec < +0.5` 线内，最早可能在碎晶带 0.2 就收到），不是"蜃影/星噬/虚海三个深低安"——理由是伏击与扫描惩罚的判定线就是 0.5，只认三个深低安会让其余十二个同样会掷伏击的星系**失声**；要改成 sec<0 或指定星系，改 `comms.ts` 一行；**(b)** 全局机制（近防炮集火制度 · 窝点 · 回收池）**不单独发信**，只在无人机相关两封里点到；**(c)** 教程期间导航被锁，引导期点亮的机制信要等引导走完才读得到（内容不丢）。文档：设计稿 `docs/design/comms-20260911.md` §17（含三层清单存档与触发器口径）【待验收 · 待发布】

- 2026-09-12：**公告发布（船长「**只说明4,6.**」）：公告数据写入两条——`2026-09-12-cpu-coprocessor`（低槽协处理器）与 `2026-09-12-rare-wreck-pity`（稀有残骸保底与说明）（一号）**——**①裁决与范围**：一号 09-11~09-12 批次待审稿共 **8 张卡**（`docs/design/announcement-draft-20260912-pilot1-batches.md`），船长裁「只说明4,6」⇒ **只发卡 4、卡 6**，两条**逐字照批**写入 `packages/data/src/announcements.ts` 最上方（现共 **29 条**）；**其余 6 张不发布**（卡 1 市场砸盘与挂单 / 卡 2 低安遇袭重做 / 卡 3 战斗距离与撤退 / 卡 5 蓝图与舰船价格 / 卡 7 界面筛选合集 / 卡 8 长途运输），待审稿件已逐卡标注状态留档备查，日后要发需重新递审。**②发布的两条内容**：卡 4 = 新装配件「协处理器」三档 +25/+35/+45（自身不占 CPU、多件全额叠加、卸下即收回扩容且超载拒绝卸下、MK1/MK2 稀有订单与蓝图、MK3 仅奇货无蓝图）；卡 6 = 稀有残骸保底（当日派系活跃星系连续未出则第 20 次肃清必留）+ 回收卡只列实际掉落 + 回收炉出货记录显示装备名称 + 窝点与保底掉落都进当地残骸场（打捞必得、回站开高级箱）。**③验证**：`data` typecheck 绿 · 公告 id 唯一性核对（29 条、0 重复、新两条置顶）· `content:check` 绿 · 全量 core **998/998** · 桌面与网页版 build 绿。**④文档**：待审稿件状态标注、续接卡 `handoff-20260912-pilot1-wrapup.md`（公告状态 + 待办 #1 结项）、本条目。【待发布】

- 2026-09-12：**收尾归档：三树盘点 + roadmap 去重 + 一号批次续接卡与公告待审稿（一号；船长「检查三个工作区，是否有未清理文件，然后进行收尾归档并根据推送准备公告」）**——**①三树盘点（全干净）**：`main`(`80e57d2`) / `d2/workspace`(`80e57d2`，与主树同点) / `verify`(`42db4bf`，落后主树 129 提交、无独有提交）——三树 `git status --porcelain` **全空**、`git stash` **0 条**、`_` 前缀临时探针 **0 个**（`tools/` 与全树扫描）；主树 `main` = `origin/main`（**已推送，无待推送提交**）。**②发现并清理一处合并残留**：`docs/roadmap.md` 曾有**两个「变更记录」段**（合并时两侧条目并集写成了两段）⇒ 跨段重复 **22 条**（整行完全一致）+ 1 个多余表头；已合并为一段（24 + 117 = **141 条**，每条唯一），**纯删除 23 行、0 行新增、0 内容丢失**；自检三条全过（BOM 口径与改前一致 · 行尾 CRLF 不变 · 全树往返解码乱码 0），并用**多重集比对**证明"原有行零丢失、无凭空新增"（`git diff --numstat` = `0 23`）。**③收尾归档件**：新增 **`docs/design/handoff-20260912-pilot1-wrapup.md`**（一号批次续接卡：当前状态与验证快照 / 11 条交付线（含主要提交）/ 接手必读口径 5 条 / 待船长项 5 条 / 关键落点 / 本次收尾两件事）。**④公告待审稿**：新增 **`docs/design/announcement-draft-20260912-pilot1-batches.md`**（**8 张卡**，覆盖一号 09-11~09-12 全部玩家可见改动；已按第十二章写：第三人称「玩家」、无开发/验收话语、每卡 3~5 条；**未写入 `announcements.ts`，待船长逐卡批准**）。**⑤验证**：本批**纯文档**（唯一代码动作为零）——按船长口径不需四连，仍复跑确认无回归：typecheck 四包 · core **998/998**（98 文件）· `content:check` · 桌面与网页版 build 全绿；`_` 探针已删。【待验收 · 待发布】

- 2026-09-12：**倾销惩罚定稿：每层 −10%、收购量摘掉压力项、地板 0.1、挂卖量同步削减（一号；船长「如果是这样的话，价格惩罚提高到10%，移除压力项。」→ 两问两答：「甲：只从收购量里移除」＋「下调到0.1，且砸盘时，挂卖单的量进行同步削减。」）**——**①先摆冲突（§5.2）**：「移除压力项」若照字面**整体移除**，会作废 `docs/design/v9-market.md` §二已确认的「q > poolTarget 淤积 → 收购压价（倾销砸价）；q 接近 0 → 供应断货、价格走高」——船长选**甲**：**只从收购量里移除**，价格侧压力项**保留**（V9 那条规则继续成立）。**②四项改动**：**(a)** `shockPerTrigger` **0.08 → 0.10**（每层 −10%，衰减半程仍 6 分钟）；**(b)** 池商品收购阶梯量 **不再乘 `pClamped`**（`qty = flow × 阶梯系数 × 补偿倍率`）⇒ 收购量只随惩罚层数放大、不再被池量淤积反向压制；**(c)** `minPriceRatio` **0.2 → 0.1**；**(d)** 新增 `dumpSellVolumePerLayer = 0.08` 与单点 `market.dumpSellVolumeMul()`——**每层未衰减惩罚把 NPC 供应单量 −8%**（下限 10%，只在砸盘方向，只作用于池商品供应阶梯）。**③读数（真引擎 40 窗连续倾销 ore-veldspar：基准 12 ISK、阈值 4,992 单位/窗）**：**温和倾销**（每窗 5,000；池量 1.20× 目标、shock −0.805）⇒ 均衡价 **2 ISK = 0.167×**（上一版口径 0.333×、最初 0.500×；已跌破旧的 0.2× 地板 ⇒ 地板下调确实生效）、本窗新铺**收购单 ×1.64**、**供应单 ×0.43**；**巨量倾销**（每窗 50,000；池量 2.07× 目标、shock −0.701）⇒ 价格同 **0.167×**、**收购单 ×1.56**、供应单 ×0.66 —— **深砸不再把买家压少**（改前深砸会掉到 ×0.80，这正是你问的那个点）。停手后仍 6 分钟半衰、30 分钟回基准。**④验证**：`tests/market-dump-penalty.test.ts` 改写为 **5 例**（每层 −10% / 40 窗稳态 ≈ −0.92 且价格已跌破旧 0.2× 地板 / 收购量只随层数放大 ×1.6~1.9（不再含压力）/ 挂卖量同步削减且倍率 ≥0.1 / 倍率函数方向与地板）；**既有用例同步**：`market.test.ts` 的冲击衰减用例改按新幅度取窗口（+10% 起算 ⇒ 24 窗口 = 4 个半程 < 1%）；**负向验证**：把 `dumpSellVolumePerLayer` 置 0 ⇒ 2 例如实失败（供应量 15,655 反超基准 12,727、倍率 1 而非 0.68），还原后全绿；全量 core **998/998**（98 文件）· typecheck 四包 · content:check 绿 · 桌面与网页版 build 绿；`_` 探针已删。**⑤已知取舍**：**(a)** 收购量摘掉压力后，池量淤积**只压价、不再压收购量**（"池满=没人要"只剩价格那一半）；**(b)** 挂卖削减只对**池商品供应阶梯**生效（单件/稀有/奇货供应单恒 1 张，无处可削）；**(c)** 两侧倍率都**只在砸盘方向**生效（买入顶价时买卖量都不动）；**(d)** 价格地板 0.1× 配合 10%/层（稳态 −0.92）意味着**深砸会贴着地板走**（实测 0.167×，取整后 2 ISK/12 ISK）。文档：设计稿 `v9-market.md` §二/§清单/钳制行同步、词典「冲击动量（倾销惩罚 · 砸得越狠买家越多）」条目改写【待验收 · 待发布】

- 2026-09-12：**倾销惩罚加重 5%→8% + 「砸得越狠、来收货的买家越多」（一号；船长「提高倾销惩罚，同时每层惩罚还会提高系数一半的订单量。」→ 三问三答：「甲：每层 8%、衰减仍 6 分钟」＋「**修改为每层提高 8% 买单数量**」＋「乙：所有商品都适用」；澄清原话「我想表达的意思是。砸得越狠越卖来买的人越多」）**——**①惩罚加重**：`balance.market.shockPerTrigger` **0.05 → 0.08**（每层 −5% → −8%），**衰减半程仍 6 分钟**（不延长）。**②买家变多（本次新增机制）**：新增 `dumpBuyVolumePerLayer = 0.08` 与单点 `market.dumpBuyVolumeMul()`——**每层未衰减惩罚把 NPC 收购单（买单）挂单量 +8%**；层数 = `|shock| ÷ shockPerTrigger`（**不取整**，衰减中的半层按比例算，量随行情平滑回落）；**只在砸盘方向生效**（`shock < 0`；玩家买入把价顶上去时不动买单量）；**所有商品适用**——池商品收购阶梯 3 档、单件平价品（3 × boost）、稀有（2 × boost）、奇货（boost）、开局铺簿（`seedCommonBook` 两档）**五处共用同一倍率**。**③读数（真引擎 40 窗连续倾销，ore-veldspar 基准 12 ISK、阈值 4,992 单位/窗）**：每窗 −8%、衰减每窗 −10.9% ⇒ **稳态 shock −0.723**（旧 5% 时 −0.458），均衡价 12 → **3 ISK（0.250×）**（旧口径停在 0.500×）；**本窗新铺收购单量：干净一窗 12,665 → 饱和后 18,408（×1.45）**（倍率 1.723 × 库存压力 0.90）——即"砸得越狠，接货的人越多"；停手后仍 6 分钟半衰、30 分钟回基准（与旧口径一致）。**④验证**：新增 `tests/market-dump-penalty.test.ts` **4 例**（每层 −8% / 40 窗稳态 −0.73 且价格高于 0.2× 硬地板 / 饱和后本窗收购单量 ×1.4~1.8 / 倍率函数与"买入方向不放大"）；**负向验证**：把 `dumpBuyVolumePerLayer` 置 0 ⇒ 2 例如实失败（倍率 1 而非 1.72、−0.4 冲击下倍率 1 而非 1.4），还原后全绿；全量 core **997/997**（98 文件，+4）· typecheck 四包 · content:check 绿 · 桌面与网页版 build 绿。**⑤已知取舍**：**(a)** 倍率**只在砸盘方向**生效（买入方向不加量）——按"惩罚"语义取的方向，若要对称放大一句话可改；**(b)** 层数**不取整**（不卡整层，量随衰减连续回落）；**(c)** 价格硬边界仍是 `[0.2×, 5×]` 基准（本次未动，8% 的稳态也用不到它）；**(d)** 池商品"越砸量越小"的库存压力项**保持不变**（压力仍在 0.4~1.6 区间），本次是**叠加**在它之上的惩罚补偿。文档：设计稿 `v9-market.md` §二 与 §清单段更新（±5% → ±8% + 新增"买家变多"条）、词典新增「冲击动量（倾销惩罚）」条目【待验收 · 待发布】
