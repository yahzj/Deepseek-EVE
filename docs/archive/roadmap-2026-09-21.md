# 封存卷 · roadmap 批次条目 2026-09-21

> **冻结件**（2026-09-21 当日批次条目）：从 `docs/roadmap.md` **原文搬运、一字未改**（含船长原话照抄、逐问逐答、负向验证与读数）。
> 封存日期 **2026-09-15**（三号 · verify40）· 搬运时原行号区间 2026-09-21 当日条目。
> **检索**：本目录内 grep，或 `git log -S "<关键词>" -- docs/roadmap.md`（历史版本随时可翻）。
> **不要在本卷里追加新内容**——新批次写进 `docs/roadmap.md` 的滚动窗口，越窗后由 `npm run docs:seal` 封存进新卷。

---
- 2026-09-21：**修船长两条：玄武级「旗舰」→「战列舰」· 力场/充能装置的叠加衰减改「同族同池」＋补登收敛分组（二号 · d2）**——
**①船长原话（照抄）**：「**我发现几个问题玄武级是战列，不是旗舰。护盾充能立场不是多件衰减吗。**」＋两问两答（**取 A：同族合并计数，MK2+MK3 也衰减** · **取「玄武级重装战列舰 / Leatherback-class Heavy Battleship」**）。
**②玄武级改名**：它是 **T4**（`tier: 4`）而 `SHIP_SIZE_CLASS[4] = 战列舰`，「**旗舰**」是 **T5** 档名（皇带鱼/邓氏鱼那一档）⇒ 名实不符。改动 3 处**玩家可见**（`ships.ts` 的 `name` · `l10n.ts` 的英文译名 · `shipBlueprints.ts` 两张图的说明「重装旗舰，货舱 15,200 m³…」→「**重装战列**」）＋ 2 处断言（`l10n-overlay.test.ts` 的英文名 · `content-check.ts` 的两处注释）＋ 1 处英文词典表（`glossary-en.md` 两行）＋ 现存设计文档 12 处（price-ladder / t4-battleship / v10-content / battle-sprite-tier / wormhole-extraction-endgame / test-saves README / 玄武 SVG 样例；**archive 与成稿公告件不动**——那两处是发布时刻的快照，且 09-13 的公告正文里本来就把玄武列在「战列舰档」）。**子分类 `subClass` 不动**（仍是 2026-09-16 船长定的「武装货舰」）。
**③衰减口径改「同族同池」**：两件装置的收敛计数原按**件 id**（`seen.get(d.id)`）⇒ **同型**多件衰减而 **MK2+MK3 混装各算一件**：实测力场 `1×MK2 = 10%` · `2×MK2 = 18.69%` · **`MK2+MK3 = 20.00%`**（混装比同型两件还高 ⇒「换个档装」就是绕开惩罚的最优解，与船长「有叠加惩罚」自相矛盾）。现收敛键改走 `equipment.stackingOf(d).kind`（力场 `'shield-field'` / 中槽充能 `'shield-charge'`）⇒ **各档混装同池**：力场 MK2+MK3 = **18.69%**（与同型两件逐字同额，冷却仍取最短 8 秒）、`2×MK2+MK3 = 28.69%`（按第 3 件折减）；中槽充能 MK1+MK2 由 32% → **29.38%**。**船级不对齐一处修一处**：`stackingOf` 里两族各给一个 kind，界面自动按 `weighted` 标「多装递减」＋「同类第 N 件：按 X% 生效」。**多艘船各带一件仍各自独立、可叠加**（2026-09-20 原裁定未变）。**中槽那件同批改同一把尺**（理由：两件是同类"按周期脉冲"机制，留两套口径只会让面板与数值各读各的；已在汇报里明示）。
**④顺带补登收敛分组（船长那问的另一半）**：这两件原先**掉在 `stackingOf` 的 `flat` 兜底里** ⇒ 装配页/说明短行把"多件"标成**「全额叠加」**，而引擎**一直在按 EVE 曲线折减** —— **面板与实际不符**（这才是「不是多件衰减吗」的直接观感来源）。归 `weighted` 后自动改标「多装递减」。
**⑤存档零迁移**：只改计数口径与标注，不动任何字段、不升版本；`save:migrate` 76/76 全过。
**⑥验证**：typecheck 四包 0 错 · core **2015 例全绿** · `content:check` ✅ · `ui:rot-check` ✅ · `ui:tip-check` ✅ · `l10n:check` ✅ · `save:migrate` **76/76** ✅ · 桌面 build ✅。用例：`tests/shield-field.test.ts` 的「MK2＋MK3 同族同池也衰减」**改写**（原断言写死 0.2 = 旧口径，是本批被我改红的唯一存量断言）＋ 新增三件用例；`tests/shield-charge.test.ts` 新增「档次混装也同池」；`tests/v181.test.ts` 的 `stackingOf` 分组用例补两族（并断言**两族互不同池**）。**词典已登记**（glossary 力场那条追加 2026-09-21 修正段）。
