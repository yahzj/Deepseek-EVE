# 封存卷 · roadmap 批次条目 2026-09-15

> **冻结件**（2026-09-15 当日批次条目）：从 `docs/roadmap.md` **原文搬运、一字未改**（含船长原话照抄、逐问逐答、负向验证与读数）。
> 封存日期 **2026-09-15**（三号 · verify40）· 搬运时原行号区间 2026-09-15 当日条目。
> **检索**：本目录内 grep，或 `git log -S "<关键词>" -- docs/roadmap.md`（历史版本随时可翻）。
> **不要在本卷里追加新内容**——新批次写进 `docs/roadmap.md` 的滚动窗口，越窗后由 `npm run docs:seal` 封存进新卷。

---
- 2026-09-15：**虫洞文案修正：回合耗尽提示改写 + 「撤离拦截」漂移叫法统一为「撤离战」（船长报障 · 三号 · verify）**——**①船长原话（照抄）**：「**虫洞内回合耗尽后的提示：回合已走不动：只能撤离（撤离拦截照打——打赢才算把货仓带回去）。文本不符合虫洞探索的游戏设定**」。**②查实三处不符（逐条对词典/手册）**：**(a) 术语漂移**——「撤离拦截 / 撤离拦截战」是**施工期叫法**，而词典八之一、手册「撤离」条、设计稿、以及船长 2026-09-13 原话（「玩家可以无条件开始撤离，但是依旧需要打撤离战」）**一律叫「撤离战」**；**(b) 冒号前的口语化措辞**——「回合已走不动」不是设定语言（手册与日志口径是**「回合耗尽」**）；**(c) 赌注说少了**——原文「打赢才算把货仓带回去」只提货仓，而实际口径是**撤离战打不赢 = 全损**（编队与随行战利品一起留在洞里，手册「全损」条：船被打沉，该船与它带回的货一起丢）。**③改动（纯文案，零逻辑零数值）**：**主条** `Wormhole.tsx` 回合耗尽提示 ⇒ 「**回合已耗尽：只能撤离（撤离战照打——打不赢就是全损，编队与随行战利品一起留在洞里）。**」（用词全部对齐手册/词典：回合耗尽 · 只有撤离 · 撤离战 · 全损 · 随行战利品）；**术语统一**（同族 8 处）——`Wormhole.tsx` 撤离按钮 tooltip「第 2 层起要打撤离战」· 结算标签「打赢了撤离战」· 撤离确认 toast 与**页顶警告条**都补上"打不赢就是全损"（页顶那条原本已有「（打输 = 本趟全损）」⇒ 未重复改写）· `wormholeBattle.ts` 战斗标题两处 `'撤离拦截'` → `'撤离战'`（顶部标题注释同步）· `wormholeFoes.ts` 敌名后缀 → 「撤离战」· `items.ts` 谜质物品说明「节点战、守卫战与撤离拦截战」→「…与撤离战」（**这条是新加的内容契约抓出来的**）· `wormhole.ts` 注释 · `tools/wormhole-econ.ts` 注释与政策行 · 两份设计稿。**④加内容契约（防复发）**：`tools/content-check.ts` 的**陈旧术语黑名单**新增一组 `[/撤离拦截/, '施工期漂移叫法（现行口径：撤离战——词典/手册/设计稿同一叫法）']`——玩家可见文本再出现即红（体检扫描 6 个源码根；当前 144 个源文件 · 黑名单 7 组 · 白名单 1 条专名）。**⑤历史记录不改**：`docs/roadmap.md` 里三处旧条目（含二号/一号当时的工作记录）**按"历史如实留档"原则一字未动**——它们在描述当时的代码叫法，不是现行口径（本条目即口径分界）。**⑥文档**：词典八之一「撤离与全损」条补两条——术语唯一叫法（撤离战，含"漂移已改回 + 契约拦住"）＋「回合耗尽 ≠ 只能靠撤离」（2026-09-13 改裁定：撤离随时可发起，只是照打撤离战）；本条目。**⑦验证**：typecheck 四包 **0 错** · core **144 文件 / 1545 用例全绿**（`wormhole-battle.test.ts` 那条日志断言随标签同步改名）· `content:check` **✅**（新黑名单生效：先抓出 `items.ts` 1 处，修完即过）· `ui:rot-check` **✅** · 桌面 ＋ 网页 build ✅。**【待验收 · 本地提交未推送】**

- 2026-09-15：**修「内容工作台还是旧矿物名」：重导工作台 ＋ 固化「改名后必重导」口径（船长报障 · 一号 · main）**——**①船长原话（照抄）**：「**依旧还是有三钛合金一类旧名称存在。**」**②根因（不是游戏里，是工作台那份）**：`content-csv/`（`items.csv` / `belts.csv` / `ships.csv` / `skills.csv` + `content-workbench.xlsx`）是**从 data 包派生的"第三份"**，且**已 gitignore**（只在本机）；文件时间戳停在 **2026-09-13 17:42**，而 2026-09-14 的矿物改名批（三钛合金→钛钢合金 等 7 项，见 `docs/design/ore-mineral-rename-20260914.md`）**只改了 `packages/data/src/*.ts`、没重导工作台** ⇒ 他手上那份仍写着旧名。⚠ 这也是 `content:check` **抓不到**的一类：陈旧术语契约只扫 `.ts/.tsx`（`apps/desktop/src` + `packages/*/src`），完全不看工作台文件。**③处理**：重跑 `npm run content:export`（7 张表 854 条：skills 79 / items 73 / modules 140 / ships 43 / anomalies 32 / belts 17 / market 470）⇒ 复核 **旧矿物名（含简写 三钛/类银/类晶体/类胶/超噬/富凡/希莫非特/灼烧岩）0 处**，新名齐备（橄榄岩 / 辉长岩 / 赤环岩 / 钛钢合金 / 银纹超金属 / 晶态胶体 / 重钨合金；`min-nocxium` 说明里的「稀有矿物」也已按 09-12 口径改成「稀有**原材料**」）。**④固化口径（防再犯）**：`tools/content-export.ts` 头注加一条硬提醒「**任何玩家可见的改名/文案改写之后都要重跑本工具**」；词典「原矿 / 原材料（术语）」条补同一条流程纪律（点明"工作台是第三份、不会自动跟、体检扫不到"）。**⑤表头开发者列标签**：已按船长 2026-09-15「按照新名词改名」裁定执行（`矿石→原矿` · `矿物→原材料` · `ISK→信用点`，并加旧表头别名兜底），见下一条。**⑥验证**：`content:check` ✅（不受影响，工作台不入体检范围）· typecheck 四包 0 错 · 本批**只改工具注释与文档**（无 .ts 逻辑改动、无存档改动）。**【待验收 · 本地提交未推送】**

- 2026-09-15：**命中公式改判：回避「从初始命中中扣」（旧口径"先乘衰减再减回避"作废）（船长裁定 · 三号 · verify）**——**①船长原话（照抄）**：「**改为从初始命中中扣。**」（起因：船长追问「扣除的命中是最终命中还是基础命中？会一起吃距离衰减吗」——查实旧口径是"先乘衰减、再减回避"，即回避＝不随距离缩水的固定点数）。**②改动（一处公式）**：`packages/core/src/combat.ts` 的 `hitChance` —— 旧 `(武器基础命中 + 攻方加成) × 距离衰减 − 守方回避` ⇒ 新 **`(武器基础命中 + 攻方加成 − 守方回避) × 距离衰减`**；后续 ④ × 索敌件/加力失稳 → ⑤ 夹到 [0,1]（`hitMin` 0 / `hitMax` 1，仍可完全脱靶）**一字未动**。**③零变化的两处（已核）**：**打机群**（`droneHitChance` 把距离衰减固定为 1 ⇒ `(b−e)×1 ≡ b×1−e`）、**能量/光束必中**（开火分支短路，压根不掷命中）。**④语义与读数**：**df = 1（贴到近端）新旧完全等价**，越远回避越被衰减稀释 ⇒ **远距离命中整体上升**（对称：双方打对方都更准）。敌动能炮（基础 0.85 / 远端衰减 0.5）实算：回避 **0.34** 时 df 0.7 = 25.5% → **35.7%**、df 0.5 = 8.5% → **25.5%**；典型回避 **0.22** 时 df 0.7 = 37.5% → **44.1%**、df 0.5 = 20.5% → **31.5%**。**⑤校准读数（前后对照，日志落 `tools/_ui-artifacts/`）**：`battle:hit-profile` —— 哨戒无人机（衰减 0.35）5,000m **52% → 58%**、7,000m 66% → **70%**；攻坚炮台 7,350m 45% → **51%**；「三型命中不随距离」那几条**一字未变**（df=1）。`battle:calibrate --std`（`calib-before.log` / `calib-after.log`）—— **128 格中 54 格有变**，按档平均：A0 胜率 82.5% → 84.4%（+1.9）· A1 60.6% → 59.4%（−1.3）· A2 90.6% → 90.6%（0）· A3 74.4% → 76.3%（+1.9）；平均残血漂移均在 ±3.5 个百分点以内（A1 −3.5 最大）。单格最大移动：A3 泰坦残骸勘探 60% → 100%、A0 守墓巡哨 40% → 80%、A1 回音残舰 80% → 60%（单格 5 种子、RNG 链重排 ⇒ 判强弱看 A0~A3 同向，见工具头注）。**⑥敌方伤害补偿 `foeHitCompMul` 按「甲」不动**（船长确认）：它（0.62）是按**旧公式**折出的锚 ⇒ 同一补偿下敌方期望承伤上升（典型 22%：中距 +17.6%、远端 +53.7%；本船 34%：中距 +40%、远端 +200%）；**要重锚就改那一个数**（对齐中距锚 ≈ 0.527、远端锚 ≈ 0.403），已在该字段注释里写明沿革与两个候选值，等船长看读数定。**⑦用例**：`tests/combat.test.ts` **新增 1 条钉子**「命中公式（2026-09-15）：回避吃距离衰减——`(基础 − 回避) × 衰减`，近端新旧等价」（三条断言：df=1 等价 / 远端 = 0.18（旧公式给 0.04）/ 回避的边际效果 = 回避值 × 该距离衰减）；按新口径**改写 2 条**旧读数断言：`tests/drone-hit.test.ts`（哨戒远端 `(1.1+0.16)×0.35−0.12` → `(1.1+0.16−0.12)×0.35`）、`tests/pd-damage-ladder.test.ts`（近防炮打舰 `0.9×衰减−0.18` → `(0.9−0.18)×衰减`）。**⑧负向验证（真红后还原）**：把公式改回旧口径 ⇒ 上述 3 条**同时红**（新钉子报远端 0.04 ≠ 0.18），还原即 144 文件 / 1545 用例全绿。**⑨验证**：typecheck 四包 **0 错** · core **144 文件 / 1545 用例全绿** · `content:check` ✅ · `ui:rot-check` ✅ · 桌面 ＋ 网页 build ✅。**⑩文档**：`combat.ts` 公式注释改写（含新旧两条公式与语义差别、连带登记）· `balance.ts` `foeHitCompMul` 注释扩写（口径沿革 + 两个重锚候选值）· `docs/design/v12-combat.md` §三 公式表（顺带修正两行陈旧内容：信号半径/扫描分辨率**不参与公式**、`effEvasion` 一行删除）· 词典新增「**命中公式（回避吃距离衰减 · 2026-09-15 改判）**」条 · 本条目。**【待验收 · 本地提交未推送】**

- 2026-09-15：**虫洞货仓：谜质悬停补「具体效果」＋ 玩家可见名「背包」→「货仓」（船长两条 · 一号 · main）**——**①船长原话（照抄）**：「**虫洞中的背包，鼠标移动到谜质上，不显示谜质具体效果。且不应该直接叫背包，展示给玩家看的叫货仓。**」**②谜质悬停（缺口定位）**：货仓格与临时空间格里的每一件本来就有 `title`，但谜质那一支只写「名称（占 2×2 格，拖到别的物品上可换位）· 本趟增益生效中」——**没带该台装置的具体效果**；而探索页的「这一格的谜质是『X』：效果」与读数条的「谜质 N 台」悬停**早就有** `device.text` ⇒ 只有**格子**这一处漏了。改法：格子渲染处取出 `wormholeMatterDeviceOf(itemId)`，title 改成「**名称：效果**（占 2×2 格…）· 状态」，文案**仍取 core 单点 `device.text`**（与取回谜质的 toast、读数条同源，界面不自写效果文案）。**③改名（只动玩家可见层）**：页签（`TAB_LABEL.bag`）、页签字样（`货仓 N/M 格`）、读数行、三处「打赢才把**货仓**与货柜带回去」、两处「去**货仓**页处理」、临时空间两条提示（「离开**货仓**页前必须清空/要放回货仓或丢掉」）、`activity.ts` 活动栏读数（「回合 N/M · **货仓** X 类物资」）、`wormhole.ts` / `wormholeSalvage.ts` / `wormholeBattle.ts` 的日志与错误（「**货仓**放不下：已占 N / 共 M 格」「**货仓** N 类物资入港」「编队失联、**货仓**内容全部丢失」「先到「**货仓**」页把它们放回货仓或丢弃」）、手册词条『**货仓**与产出』与临时空间规则条、待审公告稿一条。⚠ **内部标识符 `bag` / `run.bag` / `BagSlot` 与注释里照抄的船长原话一律不动** ⇒ 零存档、零接口、零数值。**④为什么不算推翻旧裁定**：船长 2026-09-13 原话本就写着「**类似背包英雄那种需要管理的背包格（没有背包，货仓直接代表背包大小）**」——玩家可见的「背包」是当时的漏改，本批把它归位（口径写进词典八之二）。**⑤验证**：typecheck 四包 **0 错** · core **144 文件 / 1544 用例全绿**（唯一断言玩家可见串的用例 `wormhole-salvage.test.ts` 的「背包放不下」→「货仓放不下」同步）· `content:check` ✅（文案纯净契约通过）· `ui:rot-check` ✅ · 桌面＋网页 build ✅；全仓再搜玩家可见「背包」**仅剩注释与船长原话**。**⑥文档**：词典八之二「背包」条改写为「货仓」并记改名裁定 · `docs/design/announcement-draft-20260913-wormhole.md` 第 5 条同步。**【待验收 · 本地提交未推送】**

- 2026-09-15：**市场右栏高度重新分配：上下限只加在「订单详细窗口」、富余全给「我的挂单」（船长裁定 · 三号 · verify）**——**①船长原话（照抄）**：「**保证订单详细内容不会被压缩的基础上，多余的给我的订单（也就是保证窗口的高度有一个上限和下限）**」；同日澄清：「**我的意思是，保证订单详细窗口的上限和下限，我的订单不用上下限控制**」（起因：玩家反馈「市场详细界面太高，能否缩短」，随附实测截图）。**②现行口径（纯样式 + 一处注释：零逻辑、零数值）**：`styles.css` 右栏两条——**① 「市场详情」= 上下限都在它身上**：**上限 = 内容自然高**（`flex: 0 1 auto`，`flex-grow: 0` ⇒ 窗口再高也不把它拉长、底部不留白）；**下限 = `min-height: 400px`**（≈ 压缩底：面板头 37 ＋ 内边距 20 ＋ 折线图 64 ＋ 报价 22 ＋ 买卖盘 5 档 105 ＋ 交易面板 ≈120 ＋ 块间距 24；取 400 略高于实测底 ⇒「压到下限时不出内滚」，再矮由**面板体内滚**兜底、绝不裁内容）。**② 「我的挂单」= 不加任何上下限**：`flex: 1 1 auto` ＋ `max-height: none` ＋ `min-height: 0` —— 窗口有富余就把富余全给它，窗口不够就先让它让位（可见行数仍由 `MarketPage.MyOrders` 量成「整行」写进面板体、超出的在面板体内滚）。配套：**`.app-mkt-chart-wrap` / `.app-mkt-chart-empty` 由「flex 吃富余（1 1 auto，64~130px）」改成「常规高 130px、可缩到 64px（0 1 auto + height: 130px）」**——折线图不再靠「抢富余」决定高度（该历史口径的**压缩侧仍然保留**）；窄窗媒体查询里照旧把「我的挂单」的上下限清成 `none / 0`。**③旧口径作废（§5.2 登记）**：2026-09-14 船长「甲：富余全给市场详情、我的挂单 ≤ 40%」**已作废**；**同日我先给挂单设的 150px 下限也已按澄清撤回**（那次误把"上下限"记在了挂单头上）。新口径写进 `styles.css` 该段注释与 `MarketPage.tsx` 档数自适应的注释（含「面板体高度改由内容决定后，档数只会向上收敛到上限 8、不会反向层层下探」的说明）。**④验证**：`ui:rot-check` **✅**（旋转 / 窄窗契约不受影响）· typecheck 四包 **0 错** · 桌面 ＋ 网页 build **✅** · `content:check` **✅**（未触碰任何文案）· 主线产物 `index-*.css` 里三条规则逐条核验在包内。**⑤已知取舍**：a) 窗口很高时「我的挂单」会很高（订单少时下方留白）——这是「多余的给我的订单」的必然结果；b) 窗口**很矮**时「我的挂单」先被压（它没有下限），压到极矮时订单列表只能靠面板体内滚看；详情窗口保住 400px 下限；c) 两个数（详情下限 400px / 折线图下限 64px）各自都是**一个数**，要改说一句即可。**【待验收 · 本地提交未推送】**

- 2026-09-15：**默认期望交距：中段 → 射程带高位（星图 0.8 · 洞内维持 0.5）（船长裁定 · 一号 · main）**——**①船长原话（照抄）**：转玩家报障「**玩家反应，进行赏金任务时，敌我的初始距离非常近，对远程武器不利**」⇒ 我实测后他回「**解释清楚了，是期望距离的问题。**」并选定「**默认期望抬到射程带高位**」＋追加「**虫洞维持中段**」。**②排查（先把事实钉准，附截图证据）**：玩家截图（我方 基础舰炮 + **巡航导弹架 MK3×4（1,400~14,880）** · 敌方 **3× 守墓长舰 1,062~7,391 m** = 任务中心的**虚海守望者**窝点卡）里「**期望 8,140 m**」正好 = 该武器射程带中点 ⇒ 默认档没被"按星系记忆"带偏。真引擎实测（同卡同装配同种子）：**t=0 交距 16,368 m**（= 双方最远射程 14,880×1.0 + 缓冲 max(100m,10%) ⇒ **开局不近**、双方都够不着）→ 5s 14,888 → 10s 13,408 → 20s 10,448 → **30s 起 8,127 m**（停在我方期望中距）⇒ 玩家观感来自**稳态**：进战斗约半分钟后交距必然收拢到"射程带中点"，对 14,880 的武器只用到**射程的 55%**；要打远端只能**在战斗里**拖距离条/点「风筝」，且拖过之后**按星系永久记住**。**③口径（现行）**：`desiredRangeFor(me, tactic, bal, midPos = bal.desireBandWormhole)` —— 中距档 = **射程带内可传参位置**；新增两个旋钮 `balance.battle.desireBandStarMap = 0.8` / `desireBandWormhole = 0.5`。**星图 = 0.8 高位**（导弹架 ⇒ **12,184 m**）、~~**洞内维持中段 0.5**~~ ⇒ ⚠ **本句 2026-09-15 当天由船长「这个是我口误，可以回滚那句」更正为"取消分档、洞内也 0.8"，见上一条**（洞内**近战怪开局**另立 `wormholeBrawlOpenBand = 0.5`，保留）；**贴脸（min×0.6）/风筝（max×0.95）两档一字不动**；**开战距离公式不动**。**④同一单点的 7 个使用方（逐条登记，避免漏改）**：星图默认期望（`startBattleFor`）· 星图编队战（`startFleetBattleFor` 非虫洞支）· 「中距」按钮（`battleTacticDesire`，按"当前是否在洞内交火"分档）· **AI 副船**（`desireM === null`）· **预估胜率**的稳态距离（`steadyDistance(..., midPos)`，按卡 id `wh-*` 判洞内）· 出发前/战斗中切战术出口 · 工具读数（`foe-hp-table` 等）。**⑤读数（真引擎 A/B，同卡同装配同种子）**：a) **导弹流**（截图那套）旧 8,127 m ↔ 新 12,171 m，**我开火/敌血曲线逐点相同**、我方全程 100%/100% ⇒ 纯安全余量（离敌方射程上限从 **736 m → 4,783 m**）；b) **光束流**（T3 + 4×激光 MK3，必中+远端衰减）**旧档 t≈20s 进入敌方射程 ⇒ 交火 20 秒「遭重创」⇒ 远征失利且弃船（长尾鲨级已损毁，货仓与装备一并遗失）**；**新档全程停在敌方射程外 ⇒ 100%/100% 零伤害**（我方 8,387 m 处仍打得到，只吃衰减）。⇒ 对"我方射程远于敌方"的配置，这是**实质性的生存转折**。**⑥用例**：`main-weapon-range.test.ts` **新增 1 条**（锁 `desireBandStarMap = 0.8` / `desireBandWormhole = 0.5`；导弹架 900~11,760 ⇒ **星图 9,588 / 缺省（洞内）6,330** / 风筝 11,172 / 贴脸 540）；`wormhole-battle.test.ts` **新增 1 条**（洞内默认期望 = 射程带中点，**这条是本批补的护栏**——原先改洞内档不会红）；`desire-per-galaxy.test.ts` **三处断言改按星图默认档**（默认档 / AI 副船 `null` / 老档回落），文件头口径同步。**⑦反向验证**：`desireBandStarMap` 改回 0.5 ⇒ 新用例如实红；洞内档改成 0.8 ⇒ 洞内锁定用例红；复原即全绿。**⑧五闸门**：typecheck 四包 **0 错** · core **144 文件 / 1544 用例全绿**（基数含同期并入的其它批次）· `content:check` ✅ · `ui:rot-check` ✅ · 桌面＋网页 build ✅。**⑨已知取舍与气味（如实登记）**：a) **纯炮台/光束流**站位变远 ⇒ 命中随距离下降 + 光束远端衰减 ⇒ **自己输出变低**（长盘磨不动会撞"打满上限判负"；追踪导弹无此代价）；b) **射程优势被放大**——我方射程远且更快时可稳定站在纯近程敌人射程外白嫖（D 族这类"不 brawl"的族不会扑上来），这是"打远程怪就该用远程武器"的设计意图，但确实让高威胁远程卡更容易无伤刷；要收可调 ①0.8 → 0.7 ②给敌方内推留权重 ③给最远端白嫖加约束；c) **洞内不受影响**；d) **预估口径同源分档** ⇒ 不会出现"界面按中点估、实战站远端"的错位，但改前历史读数不可直接对比。**⑩文档**：新设计稿 `docs/design/desire-band-20260915.md`（状态：已确认）· 词典两条就地改写（「目标距离（按星系独立保存）· 胜率预估口径」与「主武器（战术距离口径）」）。**【待验收 · 本地提交未推送】**

- 2026-09-15：**星系扫描改「无人扫描艇」：不占主控 · 不牵动舰船 · 不暴露 · 完成待查看（船长定案 · 三号 · verify）**——**①船长原话（照抄）**：「**进行修正，玩家扫描星系将不再占用玩家的主控活动（也不显示在主控活动里，而是在 AI 活动的图标右侧显示一个进度条，当扫描完成后这个进度条依旧存在并高亮，直到玩家进入星图界面查看后才移除）。**」**②确认过程（四步闸门）**：集中提问三问 ＋ 顺带一问，船长回「**确认**」⇒ 按推荐项 **1A / 2A / 3甲** 落码（第 ④ 问「扫描虫洞要不要一起改后台」未点 ⇒ **默认不动**）。**③修前现状（核过）**：星系扫描（`state.scanning`）是货真价实的主控活动——开扫自身挡 11 项，**反向有 14 处** `if (state.scanning.active) return '扫描探索中：先终止扫描'`（采矿/打捞/远征/长途运输/快递/亲自开炉×2/亲自开线/掩护巡逻/返航空间站/建站交付/换驾驶/进洞/扫描虫洞）；`shipBusyLabel` 报「扫描探索中」⇒ 换驾驶、派副船、进洞同一把尺挡住，界面还有"换驾驶会终止扫描"二次确认；它还**牵动舰船位置**（开扫清 `awayGalaxy`，完成 → 自动返航段 → 自动停靠最近已建成站 + 自动卸货）。**④三条口径**：**(1A) 双向放行**——扫描期间能采矿/打捞/远征/长途/快递/掩护巡逻/亲自开炉·开线/进洞，反过来这些在跑也能派扫描艇；唯一保留的互斥 = **一次只派一艘**（单槽，换目标先召回、进度按星系各自保留）。**(2A) 不牵动舰船**——无人艇不出发/不返航/不停靠/不卸货，**自动返航段取消**，`awayGalaxy`/`dockedSite`/`transit`/货仓全程不动。**(3甲) 不再暴露**——扫描退出"就地作业暴露"清单（不会因它挨打、也不会被"结构过低自动停手"连带停扫），但**「探索发现」事件池（12 条专属事件）与事件倒计时加速照旧保留**（口径 = 扫描艇把信号传了回来）。**⑤界面（船长原话直落）**：`activityOverview` **不再生成 `kind: 'scan'`** ⇒「玩家活动」列表里没有「扫描探索」行了；活动窗头部 **AI 两枚徽标（副船 / 工业）右侧**新增**扫描条**（`app-activitybar-scan`，定宽 200px ＋ 名称省略号防头部跳动）：进行中 = 星系名 ＋ 进度 ＋ 百分比（点击跳「星图」页）；**完成待查看 = 满格 ＋ 金色高亮 ＋ 「✓ 已完成」**，点击或**进「星图」页的「星图」标签**即收起（`App.tsx` 的 `useEffect([page, mapTab])` → `engine.ackScanView()`）。⚠ **完成时人已经停在星图页** ⇒ 那一拍不触发、高亮留着，等他离开再回来或点一下才收（免得"新点亮的星系还没看见，条就没了"）。**终止入口搬家**：活动栏那个「终止扫描」随行消失 ⇒ 搬到星图页那条扫描读数里（口径 = 「召回扫描艇」，已扫部分保留）；`ShipPage` 的换驾驶二次确认**删除**（扫描已不影响换船）。**⑥存档（零迁移）**：新增两个**可选**字段 `scanning.awaitingView?: boolean` / `lastGalaxyId?: string | null`（`createInitialState` 给初值 ＋ `save.ts` 归一）；**老档"自动返航段"一次性收口**——老档 `returning === true` 时窗口完成早已点亮（`finishScan` 先落地）⇒ 读档直接按"已收尾"处理（`active=false`、时间字段清零），**情报不丢、舰船位置不动**（"到港停靠 + 自动卸货"那套作废），并**补亮 `awaitingView`** 让玩家看一眼；`returning` 字段保留但运行期恒 false。**⑦连带改动**：进洞门槛的"主控活动全表"**十档 → 九档**（「扫描星系」出表，两个方向都放行：扫描在跑能进洞、进洞后能派扫描艇、别的活动在跑也能派）；词典八之七与「扫描探索」条同步改写、新增「扫描条」条；设计稿 `docs/design/system-scan-unattended-20260915.md`（已确认 · 已落码）。**⑧明确不动**：`RETURN_LEG_MUL` 仍是悬赏/打捞/巡回讨伐的返航旋钮（星系扫描不再用它）· 低安「扫得慢」不变 · 续扫进度与 22 分钟读档上限不变 · **「扫描虫洞」的一切口径不动**（仍占主控、仍暴露、仍"遇袭不中断"）。**⑨顺带发现的口径漂移（已登记 · 船长已裁定）**：2026-09-14 那条「**虫洞扫描与星图扫描遭遇期望一致**」（`encounters.ts` 共用 `kind: '扫描'` 档 ＋ 低安 ×1.5）——星系扫描改无人化后不再暴露 ⇒ 这条"一致"事实上只剩虫洞扫描一边；**船长 2026-09-15 裁定「不要动虫洞」** ⇒ **保持现状**（虫洞扫描照旧暴露、照旧"遇袭不中断"），本条即口径记录，不再双边对齐。**⑩验证**：typecheck 四包 **0 错** · core **143 文件 / 1530 用例全绿**（改写 12 处旧互斥与返航段断言，新增无人化三口径 / 老档收口 / 进洞门槛对照 / 忙态徽标对照）· `content:check` **✅** · `ui:rot-check` **✅** · 桌面 ＋ 网页 build **✅**；**负向验证 5 处各自点名后还原**——a) `startScan` 加回"采矿中不许开扫" ⇒ 「不占主控」用例红；b) `shipBusyLabel` 加回 `'扫描探索中'` ⇒ `t3` ＋ `exploration ①` ＋ `wormhole-activity-lock ⑤` **三条同时红**；c) `encounters.ts` 加回扫描暴露 ⇒ `t25` ＋ `b1` 红；d) `finishScan` 不置 `awaitingView` ⇒ `exploration` 3 条 ＋ `t8` ＋ `debugQuick` 红；e) `save.ts` 不认老档 `returning` ⇒ 老档收口用例红。**⑪本轮不做**：公告稿（属"口径修正"不是新系统，按 §7 未写待审稿；要补一句请说）· 星图页扫描读数与顶部扫描条的"两处同源"合并（现为同源调用的两条渲染路径）。**【待验收 · 本地提交未推送】**
