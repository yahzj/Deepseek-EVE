# 封存卷 · roadmap 批次条目 2026-09-22

> **冻结件**（2026-09-22 当日批次条目）：从 `docs/roadmap.md` **原文搬运、一字未改**（含船长原话照抄、逐问逐答、负向验证与读数）。
> 封存日期 **2026-09-15**（三号 · verify40）· 搬运时原行号区间 2026-09-22 当日条目。
> **检索**：本目录内 grep，或 `git log -S "<关键词>" -- docs/roadmap.md`（历史版本随时可翻）。
> **不要在本卷里追加新内容**——新批次写进 `docs/roadmap.md` 的滚动窗口，越窗后由 `npm run docs:seal` 封存进新卷。

---
- 2026-09-22：**二号合入批（二号 · d2/workspace → main · 已推送）**——**①修船长报障**：虫洞「时序核心」来回拖会**重复加回合**（两半病根一起修，`9ba00d65`）；**②四份设计稿入库**（随 `e637885d` 合入并按 §8 刷新 `docs/INDEX.md`）。**③验证**：typecheck 四包 0 错 · core 全绿 · `content:check` ✅ · `build` ✅。

- 2026-09-22：**本会话收尾批：建站交付纳入统一切换 ＋ 开放项 13 条销账 ＋ 情报信按船长风格重写（一号 · main · 待推送）**——
**①船长原话（照抄）**：「**建设空间站的运输也加入可以打断其他行为的切换里，不需要先暂停其他活动**」（建站交付批）·「**1甲。2甲。3甲。4甲。5甲。6甲。7不动。8不动。9.保持。10不动…11，不动…12没有问题就不动。13不动。**」（13 条开放项裁定）·「**情报信的文案你没根据我的第一次任务的文本风格重新写吗？**」（情报信批）·「**英文本地化依旧交给三号**」·「**全部可以消档，都已经完成。**」（设计稿销账）。
**②建站交付纳入统一切换**：主控活动 **9 → 10 项**（＋`siteDeliver`）：判据 = `transit.active && transit.delivery !== null`；`AUTO_HALT_KINDS` 七项；`cannotInterruptReason` 第③条改「只认**不带**交付批次的 transit」；`startSiteDeliverTrip` 删 7 条跨活动硬拒改走 `applyActivityGate`，本入口两道前置（货仓无空位 `.020` / 仓库无可装载建材 `.021`）**提到判据之前**；`haltActivityForSwitch('siteDeliver')` = 返母港、**本趟建材留在船上**（船长选「甲」）；`activity`/`wormhole` 忙态与进洞自动停名单同源补入；界面 `Expedition.tripBusy` 忙态名单整段删除。提交 `4a56deec`。
**③开放项 13 条销账**：**1甲**（`core.onboarding.001` 改为「立足已稳，该往更深处打听了」——发布点在前 11 条之后）· **4甲**（老档收口 `firstTaskAutoClaim` 先补发"当前那条的起手道具"，零新字段零版本变更）· **5甲**（`core.ai.008` 补「到「装配」页装一台」）**已落码**；**2/3/6/7/8/9/10/11/12/13 按船长裁定"不动 / 保持"销账**（其中 10 = `tools/` 不纳入 typecheck，理由是「已有用旧工具前要更新的护栏」；11 = 死文件 `content-validate.ts` 留场，前提"不影响游戏运行"已核成立）。
**④情报信按船长风格重写**：13 封通讯正文 39 行整篇重写（共事口吻「我们/你」· 少破折号 · 情报后带一句建议 · 术语加「」· 物品用正式名 · 三行 = 情报/建议/随信附），顺带纠三处（强化采集器 MK1 · 钛钢合金/银纹超金属 · 新船先入舰船仓库再转舰队），主题「市场挂单」→「市场交易」。提交 `da8764ce`。
**⑤设计稿销账（船长 2026-09-22：「全部可以消档，都已经完成」）**：`wormhole-extraction-endgame-20260912`（F1~F5 已全落，剩余仅 F3c 经济复跑 / F4d 拆解配方[船长"暂时不用"] / 词典与公告）· `wormhole-exclusive-20260913`（L0~L4＋R1~R4 全落码）· `wormhole-ships-review-20260913`（重跑工具实测：**明显强于同档中位 10 艘 / 同档 4 / 偏弱 0**，抗性 10/15、族武 3/15）· `wormhole-fleet-optimum-20260913`（读数留档，后续盘面/威胁批已覆盖）· `p0-2-engagement-20260911`（§八 提前脱战已落码；其余提案随批结清）· `c4-calibration-notes`（09-05 已逐项拍板）· **W2 验收存档**（不再配）。
**⑥验证**：`typecheck` 四包 0 错 · core **182 文件 / 2079 例全绿** · `content:check` ✅ · `l10n:check` ✅ · `ui:rot-check` ✅ · `build` ✅ · `flow:newgame` ✅ · `save:migrate` ✅（真档只读）。

- 2026-09-22：**主控活动窗口对玩家开放（三号 · verify-activity-win → main · 船长令）**——
**①船长原话（照抄）**：「**不用设缩放下限，可以对玩家开放了**」。
**②改动**：撤掉那道"只有开启调试模式才可见"的开关（2026-09-20 立，`ActivityScreen.tsx` 的 `activityWinEnabled()` → `debugEnabled()`）——函数整条删除，判定只剩"主控在不在做本窗口认得的活动"（`activityKindOf`，`App.tsx` 与组件同源，页面让位 `is-win-hidden` 也走它，避免"窗口不渲染、页面却已让位"的空白主区）；窄屏按船长明示**不设缩放下限**、照现有等比缩放（`--win-scale`）不变。⇒ 所有玩家在开采/打捞/长途运输/扫描虫洞时都会看到活动窗口，可最小化到左上角小窗（「⤢」还原）。
**③同步更新**：探针 `tools/activity-win-probe.ts` 的头注与 A0 节改成**读玩家侧形态**（不再动任何开关；判据改为"活动一起来窗口就该在"）；归档文档 §6 与上一条的回滚说明同步改口径（原先写的"只在调试开关下可见"已作废）。
**④验证**：typecheck 四包 0 错 · core 183 文件 / 2091 例全绿 · `l10n:check` ✅ · `content:check` ✅ · `ui:rot-check` ✅ · `build` ✅ · `docs:index` ✅ · 探针 `ui:actwin` 三视口：**玩家侧（不动任何开关）**五态窗口皆在、嵌入=是、填满活动栏下那块=是、页面让位=是、无文档溢出；收起链六步全对。

- 2026-09-22：**技能页改版「技能科技树」（一号 · main · 已推送 · 公告 `ann-skilltree-20260922`）**——
**①船长原话（照抄）**：「**我希望参考大鲸鱼根目录下的QQ图片20260922133622图中的样式**」·「**1乙 ，将同类效果的技能做成上下级关系。2将现有的技能再进行细分。3乙。4.甲。5乙。6乙。7.做一个导航用于切换不同类型的技能书，其他先不做。8，甲。**」·「**甲，lv1**。**不同类型的舰船不应该是上下级关系。采矿属于工业舰，武装舰操作 · 装甲舰操作是战斗。**短名规则先不采用，先试试看全名的效果。」·「**有前置的技能效果是否相关？如果不相关最好单开一条线。**」·「**技能不用强行并列同一列。可以按照rank将部分单个技能放在后面（只是没有连线）**」·「**能否将测试中的技能页面放入EXCEL中，将其位置转换成坐标。我来手动调整图标位置？**」·「**还有为什么部分筛选的样式不统一？为什么是白底的**」·「**然后调整下子类的顺序，技能数量多的窗口优先靠前。**」·「**没问题了，可以将技能科技树替换掉原先的技能目录。**」·「**对了 要写一份技能界面大改的公告**」（＋审核「**可以**」）。
**②数据（`packages/data/src/skills.ts`）**：`SkillDef` 新增 `branch`（**技能书**）与 `prereq`（**真前置**，数组 ⇒ 支持汇合，门槛 `PREREQ_MIN_LEVEL` = **Lv1**）；**23 本技能书**（新增常量 `SKILL_BRANCHES`，7 大类之内再切细）；**真前置 28 条技能 / 28 条连线 / 54 条根**——**连线判据 = 效果同族**（二者说明里互相点名「与 X 乘算叠加 / 同效果 / 同享」＝同一乘区），**效果不同轴的一律不连**（先按此拆掉了 26 条凑链）；**4 条技能换大类**（采矿舰入门学 · 采矿舰操作 → 工业；武装舰操作 · 装甲舰操作 → 战斗）；船长在坐标工作台回稿的**两条改名**（采集器入门学 → **采矿舰入门学** · 空间站协议学 → **空间站维修协议**）与**三条 rank**（动能炮术/导弹发射学 → 2 · 空间站维修协议 → 4，**该对上下级方向随之翻转**）。
**③规则（`core/engine.ts`）**：`enqueueSkill` 加真前置校验（新词条 `core.engine.019` 写明"还差哪条、要几级"）· `skillLockMissing` 单点（界面置灰与入队同一把尺）；**老档零迁移**（只看已练等级，不回收、不加迁移键）；`content:check` 新增**技能书与前置契约**四条（登记且与大类一致 · 前置真实 · **不得成环** · **父 rank ≤ 子 rank**）。
**④界面**：技能页改为**六边形蜂窝**技能树（SVG 线稿 ＋ 真前置连线 ＋ 层内短连线）· **垂直位置 = rank** 且层压紧（没有 T1/T2 就整体上移）· **前置优先左上角、下级挂其正下方**、无连线的孤立技能按 rank 垫后（不画线）· 节点 = 图标 ＋ 全名（≤4 字/行）＋ `Lv x/5` · 5 态色 ＋ 在练角标 · 未解锁压暗 ＋ 虚线（不画 ▽）· **技能书导航**（一级大类 `.app-tasktab` ／二级技能书 `.app-subtab`，**按技能条数多的靠前** ＋「全部」档）；**升为正式「技能」页**，旧技能目录 `pages/SkillsPage.tsx` 退役（训练队列面板与 `⟦数值⟧` 高亮搬进 `pages/skillShared.tsx`），调试入口「技能测试」下线。
**⑤工具**：新增 **`npm run skilltree:export`**（把 82 个节点坐标导出成 `content-csv/skilltree-workbench.xlsx` 供船长手调，改完按 id 回写 `packages/data/src/skillTreePositions.ts` 覆盖表）＋ **`npm run skilltree:layout-check`**（正式工具：不越界 / 同行不重叠 / 每个前置都挂着子且子不往回长；读数 `23 本 / 82 格 · 不合格 0 处`）。
**⑥验证**：typecheck 四包 0 错 · core **183 文件 / 2089 例全绿** · `content:check` ✅ · `l10n:check` ✅ · `ui:rot-check` ✅ · `build` ✅ · `flow:newgame` ✅（教程「第一次学习技能」照旧 2.0 分钟 ⇒ 前置没卡教程）· `save:migrate` ✅。

- 2026-09-22：**三号界面批：窗口改「嵌入主区」＋ 四类操作自动收起 ＋ 退出战场不再误停连击（三号 · verify-activity-win → main · 已推送）**——
**①船长令**（原话见提交 `2c654408` 与当时的工作文档，该文档已按 §8 归档删除）：窗口不再盖住主内容区，改为**嵌进主区**；四类操作完成后自动收起；顺带修回「战后战报弹不出来」。
**②范围**：`App.tsx` · `panels/Announcements.tsx` · `styles.css` · `ui/ActivityScreen.tsx` · `ui/WinBox.tsx` ＋ 工具 `tools/activity-win-probe.ts`。
**③验证**：typecheck 四包 0 错 · core 全绿 · `content:check` ✅ · `ui:rot-check` ✅ · `build` ✅。

- 2026-09-22：**离线跨 0 点：「敌对派系活跃」按哪一天算（船长之问 → 船长选「甲」· 二号 · d2 · 已推送 origin/main）**——**①船长原话（照抄）**：「假设玩家挂机活跃敌人星系后离线，第二天再上线，那么会计算前一天刷了多少活跃敌人吗？还是说刷了多少敌人只在上线时计算此时刷了多少敌人（因为换天了，活跃敌人的星系换了）」→ 三案后裁「**甲**」。**②先查明现状（探针实测，非推断）**：整段离线共用"离线末刻"这一把墙钟 ⇒ 日板在**分片第一片**就换成**上线日**那版 ⇒ 长窗口 20:00→次日 00:00:25 跨天时该星系稀有残骸存量 **0**（战报 56 条）、不换板时 **30**（战报 51 条）⇒ **连 0 点之前那几个小时也一场都没吃到昨天的加成**；唯一例外 = 离线前已在打的那一场（出发即锁定 `exp.factionActive`，实测钱包正是 ×1.1）。**③改法（`core/simulation.ts` 分片循环，约 6 行）**：`wallMs` 每片 `+= step`，传 `{ ...advOpts, nowWallMs: wallMs }` ⇒ 0 点前的片按离线前那版判、0 点后才换新版；**分片本身一个字没动**（片长/片数/每片一次 `advanceGame` 不变、末片墙钟仍 = 离线末刻）。**④边界（为什么不切"大推进"那条路）**：只有重复清剿开着时离线才会再出发（`advanceAutoLoopBounty` 只有在线心跳与离线分片两个调用点）；大推进那条切段会让"20 分钟资源/快递板只按末窗刷一次"（船长 2026-09-05 定）从 **1 次变 2~3 次**（实测：大推进 8 小时 = 1 次、分片路径 = 24 次），故故意不切，注释写明将来若那条路也能再出发要一起改。**⑤用例（`tests/offline-loop.test.ts` +3，确定性判据）**：① 离线前那版活跃 = 目标星系 ⇒ 0 点前那段照吃加成（稀有残骸 > 0，靠"连开 10 场未出必掉"的保底）② 对照：两版都不是它 ⇒ 恒 0 ③ 上线日那版 = 目标星系 ⇒ 0 点后那段吃今天这版；红→绿已验证（改回旧写法 ⇒ ① 报红）。**⑥闸门**：typecheck · core 2105 用例 · `content:check` · `l10n:check` · `ui:rot-check` · `skilltree:layout-check` · `save:migrate` 76/76 · `save:roundtrip-audit` 丢键 0 · `build` 全绿。**【已推送 origin/main】**

- 2026-09-22：**洞内战斗内嵌进虫洞探索界面（三号 · verify-activity-win → main）**——
**⚠ 本批的战斗侧已于 2026-09-22 按船长令回滚，改动全部封存备用**：
船长原话「**界面回滚，战斗界面回滚到全屏显示。但是现有改动进行归档备用。**」＋回滚范围答问「**只回滚战斗相关，保留主控活动窗口**」
⇒ 战斗恢复**全屏覆盖层**（窗口壳 / `bare` 模式 / 内嵌虫洞面板三处撤掉；`save:whbattle` 脚本与验收夹具、探针的"战斗壳几何"与"洞内战斗宿主"两节一并删除）；
**主控活动窗口按船长口径留在 main**（嵌入主区 + 四类自动收起 + 点小窗还原；它随后于同日**对玩家开放**，见下一条）。
归档入口 = 分支 `archive/window-embed-20260922`（= 回滚前 main）+ 标签 `archive-window-embed-20260922` +
`docs/design/archived-window-embed-20260922.md`（清单 / 复活方式 / 别随回滚删掉的独立修复）。
以下为封存时的落地记录，供日后复活对照。
**①船长原话（照抄）**：「**虫洞内的战斗因为舰船比较多，能否改为内嵌在虫洞探索界面内？**」；选择题回话：**战斗中能关面板（关了就挪回主区）**·**战斗开始时若面板没开自动叫起面板**。
**②落地**：`panels/Wormhole.tsx` 新增 `battleSlot`（战斗节点由 `App.tsx` 传入 ⇒ **全仓仍只有一个战斗组件实例**：它内部有阶段机 live→outro→report 与慢镜快照，两份实例会各跑一套）· `panels/BattleScreen.tsx` 新增 `bare`（不带窗口壳，宿主 `.app-wh-battle` 负责尺寸背景）· 战斗中面板**主体整块让给战场**（CSS `.app-wh-run.is-battle > :not(.app-wh-head):not(.app-wh-battle)`），只留顶部读数条（深度/已探/回合/货舱/谜质）——**取代 2026-09-13「战斗中整块不渲染面板」那条旧裁定**（面板比主区更宽：1060 vs 887，1024 宽屏时 942 vs 471，正合"舰船比较多"）· 交火中「✕ 关闭」不再禁用、**也不再等于离洞**（只收界面、`attending` 保持 true，战场由主区接管继续打；`whDismissed` 记住"本场已收起"，战斗结束自动复位）· 自动开面板改用**电平判据**（`run.battle && attending === true`）而非"开战上升沿"——上升沿在"载入时战斗已经在打"时不可靠（实测：边沿触发、state 也置了真，面板仍是关的），远征战那条上升沿规则未动。
**③文案**：删死键 `ui.Wormhole.009`/`.014`（"交火中不能离开"已不成立）；新增 `.378`（交火中关闭的悬停说明）· `.379`（交火中按钮文字「✕ 关闭面板」）。
**④新工具**：`npm run save:whbattle`（`tools/make-whbattle-save.ts`）——用引擎自己的 `wormholeStartBattle` 造一份"洞内战斗进行中"的可载入真档（双方各 2 艘），既是探针夹具也是给船长的验收测试档（约定 §9）。
**⑤验证**：typecheck 四包 0 错 · core 全绿 · `l10n:check` ✅ · `content:check` ✅ · `ui:rot-check` ✅ · `build` ✅ · 探针 `ui:actwin` 三视口：⓪自动开面板=是 · ①战场在面板内 1036×521 且**填满读数条之下那块=是** · ②主区无第二份战场、页面不让位 · ③点「✕ 关闭」⇒ 面板收掉、**战场挪回主区**、这一趟照常推进。
**⑥过程教训（已写进工具注释）**：手造洞内战斗是**无效夹具**（搬来的远征战斗对象对不上这一趟编队/威胁 ⇒ 战斗组件一渲染就抛错、整块面板被 React 收回，一度误判为宿主接线有问题），正解是用引擎自己的入口开真战斗；放旧的夹具会被**离线结算**吃掉瞬时态（这局战斗被当场打完）⇒ 探针加载时把存档时间戳改成"刚刚"。

- 2026-09-22：**玩家报障「进洞打层末战斗，直接出现结算画面、战斗却还在后台跑、要等它打完才能操作」· 战斗观战屏回归修复（三号 · verify-activity-win → main）**——
**①报障原话（照抄·经船长转达）**：「**玩家反应，在虫洞内进入层末的战斗，会直接出现结算画面，但是战斗似乎依旧在后台跑，要等战斗结束才能操作，是否是之前修改战斗窗口导致的？**」
**②诊断（是窗口版造成的，且回滚已断掉那条链路）**：发布版把战斗与战报都做成了**主区里的窗口**（`origin/main:styles.css` 的 `.app-winbox .app-battle-screen{position:static;z-index:auto}`），层级低于虫洞面板的模态遮罩（`.app-modal-mask` z-index 120），而面板在**整场战斗期间不渲染**（`Wormhole.tsx` 的 `run.battle && attending` 早退，2026-09-13 裁定）⇒ 玩家把战斗窗口一收（船长 2026-09-20 明确允许「虫洞内的战斗允许最小化」）就**既看不到战斗、也够不着面板**，只能干等；战斗一结束面板回来，若这局判负就是**结算页**，而战报因为在主区窗口里、被面板压住**根本看不到**。
**③回滚顺手带出的新缺陷（本批修的就是它）**：战斗恢复全屏后"挂载＝上屏"，`onClose` 只把 `battleOpen` 置假而 `inBattle` 仍真 ⇒ 点「← 退出战场」**点了没反应**（与按钮自己的悬停文案矛盾）；更糟的是 `battleOpen=false` 会让"引擎结算"与"宿主卸载"发生在**同一拍** ⇒ 子组件没机会渲染 report 阶段，**整场战报（含沉船名单）丢失**。
**④修法（船长 2026-09-22「按你推荐来」= 甲＋乙；丙不做）**：**甲 · 挂载≠上屏**——`App.tsx` 新增 `battleMounted`（**开战上升沿**置真，只由子组件的收尾回调 `onDone` 撤销；**不能**写成 `inBattle || battleOpen`，那样结算同拍就卸载）＋ `visible` 单独管上屏（退出战场=真关屏，浮动入口随时回来）＋ 战报到达时 `onReport` 问宿主"要不要上屏"（`battleWatchedRef` 记"这一场玩家看过"：看过 ⇒ 把屏叫回来，**没看过的自动化战斗不打扰玩家**）· **乙 · 换场复位**——`battle.startedAtGameMs` 一变就把 `stage` 打回 `live` 并清 `outroRef`（治"旧战报还挂着时又开一场 ⇒ 新战斗在幕后台跑"那类现象；`undefined` 守卫必须有，否则刚进的战报会被立刻打回 live）。**丙（战斗中让虫洞面板只读渲染）按船长口径不做**，2026-09-13 那条"战斗中整块不渲染"照旧。
**⑤验证（真档真点读数；读数不是观感结论）**：临时探针造"洞内战斗进行中"的可载入真档（`wormholeStartBattle` 正规入口 + 先在 Node 里预跑到快收场）⇒ ①开战自动上屏=是 · ②点「退出战场」后**战场从屏上消失、浮动入口出现**（旧缺陷：点了没反应）· ③隐藏期间战斗照常推进（我方开火 28→38、交火 30s→39s）· ④**保持退出态到这一场打完 ⇒ 战报自己回到屏幕上**（「⚔ 大捷」+ 正文「第 3 层地点交火结束：39s…」，旧写法这一下整场战报全丢）· ⑤点战报「关闭」⇒ 屏与挂载一起收干净。闸门：typecheck 四包 0 错 · core 183 文件 / 2091 例全绿 · `l10n:check` ✅ · `content:check` ✅ · `ui:rot-check` ✅ · `build` ✅ · `docs:index` ✅ · 活动窗口探针 `ui:actwin` 三视口复跑全绿（App 改动没碰活动窗口）。

- 2026-09-22：**手动精炼双技改为「手动与 AI 核心驱动同享」（船长两条令 · 二号 · d2 · 已合入 main · 待推送）**——**①船长原话（照抄）**：「**炉心熔炼学好像只对主控生效？现在希望改成对所有都生效。并修改文案**」＋（同日追问炉膛扩容学后）「**一起开放。**」。**②船长判断属实（代码核实）**：`core/industry.ts` 的 `startRefineRun` 里两条技能的加成**原先整段写在 `worker === 'pilot'` 分支内** ⇒ AI 核心驱动的精炼炉一点都不吃（技能说明里也写着"AI 核心驱动不受此技能影响"）；2026-09-08「工业自动化 → 产线节拍学」是同款改法的先例。**③落值**：两条一起挪出该分支（分支随之取消）⇒ 对所有劳动者生效；**每级值一点没变**（−4% 周期 / +6% 批容）、乘算顺序不变（先 ÷核心效率 → 再吃技能 → 最后乘产线节拍学）；中英说明照抄「产线节拍学」句式改成"（手动与 AI 核心驱动同享）"；满级 AI 精炼线产出 ≈ **×1.63**（1/0.8 × 1.3）。**④已知副作用（船长已知情）**：批容上限 10 → 13 ⇒ AI 线「可炼量不足一批」的开工门更严（要放宽是另一件事）。**⑤验证**：单测两半各自"先红后绿"（stash 掉代码 ⇒ `expected 15000 to be 12000` / `expected 10 to be 13`）；typecheck 四包 0 错 · core 2093 用例全绿 · `content:check` ✅ · `l10n:check` ✅ · `ui:rot-check` ✅ · `save:migrate` ✅ · `build` ✅。**【已合入 main · 待推送】**

- 2026-09-22：**虫洞「时序核心」来回拖 ⇒ 重复加回合 · 定位与修复（船长报障 · 二号 · d2 · 已推送 origin/main）**——两处根因：① `wormholeSyncMatterTurns` 的**非幂等**（旧的非对称钳制让"拖出去再拖回来"净赚 +10 回合）⇒ 重写为**花费守恒**口径（`spent = turnsTotal − turnsLeft` 固定，改 `turnsTotal` 后 `turnsLeft = clamp(want − spent)`）；② `save.ts` 手写白名单重建 `wormhole.run` 时**漏了 `turnsBase` / `turnsTechBonus`** ⇒ 每次读档这两个字段消失、回合基准回退（与同日打捞器那条同源，见下条"随档字段两处落笔"）。验证 = core 单测 `wormhole-matter.test.ts` ⑤b/⑤c ＋ 真界面探针 `npm run wh:turn` 双证。**【已推送 origin/main】**

- 2026-09-22：**刷新可重复领取补发的打捞器 · 修复（船长报障 · 二号 · d2 · 已推送 origin/main）**——**①船长原话（照抄）**：「**玩家刷新可以重复领取补发的打捞器**」。**②根因**：`core/save.ts` 的 `normalizeState` **手工白名单**重建 `importantTasks` 时漏了补发去重键 `salvagerGift` ⇒ 读档即丢 ⇒ 下一拍 `backfillSalvagerIfMissing` 又发一台（刷新一次 +1 台，日志也再写一条）。**③修法**：白名单补一行（只在 `true` 时落键、**零迁移、存档版本仍 v31**）；补发判定与"无条件全员发"的临时补丁本身不动。**④交付的重头是护栏**（见 `docs/glossary.md`「随档字段的两处落笔」条）：类型穷尽用例 ＋ "引擎跑过的档往返不许丢键"自动护栏 ＋ 正式工具 `npm run save:roundtrip-audit`（76 份真档逐份往返：75 通过 · 丢键 0 · 类型变化 0；1 份失败是 v17 早于可迁移下限，与本次无关）。**⑤负向验证**：把白名单那行改回漏键 ⇒ **三条用例同时红**（含不认识打捞器的自动护栏），改回后全绿。**⑥⬜ 仍待船长定**：**已被刷新多领的那几台要不要回收**——现按既有口径「已发出的装备不回收」**未回收**；要收就另开一单（把装备库 `mod-salvager-1` 扣回 1 台）。**【已推送 origin/main】**

- 2026-09-22：**筛选与标签页文案补齐 ＋ 新增「英文语境残留中文」扫描工具（三号 · verify-activity-win → main · 船长报障）**——
**①船长原话（照抄）**：「**之前关于筛选选项的文案和标签页的文案，还未完成吧？我这边发现有遗漏**」；范围回话：「**可以，按你推荐来**」（= 本批做真漏译＋工具口径，通讯与成就页随后按序做）。
**②诊断（确实没做完，且静态闸门保不住这一块）**：硬闸门（无死引用/占位符/死键）一直是过的，但 `l10n:check` 把这块长期标成「**未译读数（报告口径，不阻断——P3 界面批逐页消化）**」⇒ 实测**58 条未声明中文串**（`ui/itemSubs.ts` 28 · `panels/Industry.tsx` 14 · `panels/Shipyard.tsx` 11 · `pages/FitPage.tsx` 5）。更要紧的是**静态工具看不见的三类**：a) **模板串拼出来的档**（`itemSubs.ts` 的 `label: \`${s.label}蓝图\``：没有字面量可扫，英文下整句中文）；b) **直接渲染 `.label` 而没走 `subText`** 的筛选档（字面量在表里、渲染处无字面量）；c) **半译句**（前缀走 `tr(id)`、尾巴是字面量拼上去）。
**③落地**：`panels/Shipyard.tsx`——「学会：/子类：/图纸：」三行 ＋「全部」档 ＋ 读数行（含「匹配/当前 N 张」两条尾巴）＋ 空态句全部接线，两处筛选档改走 `subText`；`panels/Industry.tsx`——「去组装机」「无需图纸」「已学会」、顶栏读数行、取消制造线提示接线，并新增 **`kindLabelText` / `ownedWhereText`** 把内容层联合 key（'舰船'/'装备'/'零件'/'消耗品'、'仓库'/'仓库＋机库'）映射成文案（这两个字段此前**有些卡片直接把 key 当文案渲染**）；`pages/FitPage.tsx`——机群「再开还差几架」整句与其**悬停说明**（原先中文写死的 `title`）；`ui/itemSubs.ts`——补上**缺失的 `l10n-keep-start`**（此前只有收尾标记、区间没生效，28 条**已带 id** 的数据键被误报成未译）。新增 **9 条**词条（`ui.Shipyard.002~006` · `ui.Industry.156/157` · `ui.FitPage.175/176`），其余**复用既有同串条目**（`ui.IndustryPage.001`/`108`、`ui.Industry.127`/`129`/`039`/`145`/`015`、`ui.labelsText.019`、`ui.MarketPage.178`、`ui.itemSubs.037`、`ui.ItemsPage.001`），不造重复 id。
**④读数**：`l10n:check` 未译 **58 → 0**（已声明不译 42 → 84，全部是数据/键，逐条可复核）· typecheck 四包 0 错 · `content:check` ✅ · `ui:rot-check` ✅ · `build` ✅。
**⑤新工具（临时探针转正）**：`tools/l10n-en-scan.ts`（**`npm run l10n:scan`**）——无头 Chrome + `locale=en` **逐页并逐页签**（选择器含 `.app-tasktab` 与 `.app-subtab` **两族**；首版只认前者，把「造船厂」整页漏扫，实测踩到）扫**可见**残留中文，连类名一起报，专治静态工具看不见的那三类。首跑读数：星图 0 · 舰船 11 · 装配 8 · 物品 3 · 市场 4 · 工业 6（**组装机 61 · 造船厂 70** 在页签里）· 技能 0 · 任务 0 · 成就 81 · 通讯 49。⚠ 读数不等于结论：船名/舰长名等**玩家数据**与刻意保留的中文不该译，逐条判在船长。
**⑥挂账（按船长核定的顺序）**：**通讯英文本地化**（49 处，今日已挂在本批名下）→ **成就页数据侧补 id**（81 处：`packages/data/src/achievements.ts` 的 `name`/`note` 是裸中文、**没有 id 字段**，要成批定译名，先送船长过目）。

- 2026-09-22：**手册本地化（三号 · verify-activity-win → main · 船长令「先进行手册的本地化」）**——
**①读数**（`npm run l10n:scan` 的手册专项 · 英文语境真档）：**314 → 6 处**（物品图鉴 56→6 · 装备图鉴 140→**0** · 舰船图鉴 43→**0** · 蓝图图鉴 35→**0** · 技能速查 40→**0**）；**玩法说明（指南正文）本来就是 0**。
**②病根全是「接线」，不是数据翻译**（故不用成批造译名）：a) `panels/Handbook.tsx` 的 `kindName` / `slotName` / `roleName` 原先**直读 core 的中文名表**（`ITEM_KIND_LABELS` / `SLOT_LABELS` / `SHIP_ROLE_LABELS`）⇒ 卡片副标题、槽位/角色 chip、详情行、筛选档一起漏；b) 分组顺序表 `orderOf()` 把单点表的 **`id` 丢掉了**（`{ key, label }`）⇒ 渲染处只能回落中文 label；c) 蓝图按舰级的档是**拼接标签**（``label: `${s.label}蓝图` ``，无 id——静态扫描看不见）；d) `ui/shipInfo.tsx` 的推进器周期后缀**原样吃 core 的中文整句**（`thrusterCycleText` / `thrusterCycleFullText`）。
**③修法**：新增 `ui/labelsText.ts` 的 **`slotText`**（槽位→id 映射 15 项：采集器/打捞器/推进器/目标锁定**复用既有 id**，其余 11 项新登记 `ui.labelsText.049~059`）与 **`shipTierText`**（舰级整档模板，复用 `ui.labelsText.014~018`）；`Handbook` 的 kind/role/slot/技能分类一律改走 `kindText` / `shipRoleText` / `slotText` / `skillGroupText`；`orderOf()` 原样带上 `id`/`idParam`、渲染处走 `subText`；补 5 条**舰级蓝图整档模板** `ui.labelsText.060~064`（给拼接档补 id）；推进器周期改为**取 core 的秒数 ＋ 渲染层模板**（新登记 `ui.shipInfo.182/183`，core 一个字节没动）。
**④剩下 6 处**：物品图鉴里「Cargo Expander MK2**蓝图碎片**」这类**碎片物品名**——名字在数据侧拼出来（`ItemDef` 无 `nameId`），与 AI 核心等物品名同属"**数据侧命名**"批，本批未动（已记在挂账里）。
**⑤闸门**：typecheck 四包 0 错 · `l10n:check` 未译读数仍 **0** · core 184 文件 / 2093 例全绿 · `content:check` ✅ · `ui:rot-check` ✅ · `build` ✅ · `docs:index` ✅。

- 2026-09-22：**界面配色（皮肤）六套可切换（船长令 · 一号 · main · 已推送 `fd409dd9` · 公告已发）**——**①船长原话（照抄）**：「**玩家反应，现在的界面看着太吃力，我打算添加几套配色，用于让玩家切换（至少有一个是白色皮肤）。**」；后续三轮口径：「按你推荐来进行」→「**继续做剩余的颜色**」→「**都做**」→「**亮白的亮度依旧有点刺眼**」；两条报障：「**字都没办法辨认**」「**各种 svg 图标都失效了**」；一条裁示：「**面板透明度的没问题了，可以消档**」。**②做法（单一色表）**：颜色字面量收进 `packages/ui/src/index.css` 的 `:root` ＋ `[data-theme='…']` 块（**481 个色 token × 六套，键集完全一致**）；语义色阶（族色/物品/导航/图标/作业/伤害/角色）单点化到新文件 `ui/tones.ts`（值 = `rgb(var(--wui-tone-*))`）；`styles.css` **974 处字面量改写 944 处**（余 85 处在 `box-shadow`/`text-shadow`/`filter` 里**有意保留** —— 影子在亮底下仍是黑的），渲染层 30 处内联色全部收口。六套 = 深空（默认）· 亮白 · 高对比 · 暖色护眼 · 纯黑夜间 · 色盲友好；入口 = 设置面板新增「界面配色」一行（本机 `whale-idle:ui-theme` ＋ 跟随系统，**不进存档**），首帧前写 `data-theme`（不闪）；字号上限 125% → 150%。**③色板来源（按参考图实测取色）**：船长给的四张参考图用探针解 PNG 按面积数主色 —— 深空 ← SpaceIdle（面板 `#182232` 51% · 宇宙底近黑 · 白字 `#f3f3f3`）· 亮白 ← Kittens Game 亮色（内容 `#f2f2f2` 66% · 页面灰 `#cbcbcb`/`#d9d9d9` · 近黑字）。**④三次事故真因（各配一条可复跑契约）**：a) **裸 `var()` 当色值用** —— 色板 token 是**空格三元组**、必须包 `rgb(...)`，而仓里原有 **481 处**是旧写法 ⇒ 那些声明**整条失效、`color` 回落黑**（深色皮肤上黑字压深底 = 「字都没办法辨认」；亮色皮肤上碰巧正常 ⇒ 只在深空暴露）⇒ 修 470 处，守门人 = `ui:theme-check` **⑥**；b) **`ui/tones.ts` 的 `toneVar()` 漏包 `rgb()`** ⇒ 图标描边整条失效（「各种 svg 图标都失效了」；填充型 `currentColor` 图标不受影响 ⇒ "各种"而非"全部"）⇒ 守门人 = **⑥-b**（CSS 与渲染层 TS/TSX 一起查，并点名"拼接式色值串缺 rgb() 包装"）；c) **长尾"面"色被判成"字"** —— 角色判据只看 token 名前缀，而长尾 token 叫 `x24`/`x74` 看不出语义 ⇒ 把**导航栏底色提亮**、其上文字掉到 1:1（三套新主题各约 400 处不合格）⇒ 判据改为**看它在深空里亮不亮**（亮 ⇒ 字/图标；暗 ⇒ 面/槽/边，一律不许提亮）。**⑤顺带修掉的历史问题**：3 个 token 引用了却从未定义（`--wui-dim` ×10 / `--wui-green` ×1 / `--wui-warn` ×2，那些声明一直在静默失效）· 亮底"凹槽"用纯黑半透明会变近白（新设 `--wui-well`）· 导航角标 10px 黑字压亮红底仅 **2.4:1**（新设 `--wui-badge-bg`/`--wui-badge-ink`）· 日志时间列（2.2~2.8:1 · 每页 ×140）与 `app-dim` 系列抬到 ≥4.5:1 · 通讯页发件人色调**写死在数据侧**（亮白下 1.7:1）改为引用色阶 token ＋ `content:check` 势力色调形参放宽。**⑥读数**（新工具 `npm run ui:contrast`：真实底像素，9 个一级页 ＋ 设置面板 × 六套）：不达 AA —— 深空 **1** · 亮白 **7** · 高对比 **0** · 暖色 **2** · 纯黑 **1** · 色盲 **2**（余下个位数皆是 13px 导航角标的**取样误差**，色板层实测该对 5.87:1）；色板层最差正文：深空 5.09 / 亮白 4.86 / 高对比 16.4 … **六套全部 ≥4.5:1**。**⑦工具（均正式入库）**：`ui:contrast`（对比度体检，含小元素 3×3 中位采样修正）· `ui:theme-check`（**六条契约**：引用齐全 / 各主题键集一致 / 深空基线冻结 / 无残留字面量 / 前景×底色对比度 / 无裸 var()＋渲染层版）。**⑧验证**：typecheck 四包 0 错 · core **185 文件 / 2110 例** · `content:check` ✅ · `l10n:check` ✅ · `ui:rot-check` ✅ · `ui:tip-check` ✅ · `ui:theme-check` ✅ · `build` ✅。**⑨发布**：公告 `ann-theme-box-skills-20260922`「界面配色、货柜拆解与技能扩充」（船长批准，一条覆盖三件事）。**⑩面板透明度一条销档**：星云底图每次启动随机抽 ⇒ 面板 0.88 下对比度会随"抽到哪张图"漂移（实测极端 1.2:1 ↔ 9:1）；我一度改到 0.94/蒙层 0.72，但那是船长 2026-09-10 选定的「方案A」，按 §5.2 已改回原值，船长裁示**维持现状、销档**。

- 2026-09-22：**货柜拆解改用「存档种子 ＋ 第 N 次拆解」的独立子流（船长令 · 一号 · main · 已推送 `fd409dd9` · 公告已发）**——**①船长原话（照抄）**：「**所有货柜的开启，能否采用随机数种子固定？**」→ 四答（目的 **防刷** · 种子按存档种子＋序号现算 · 范围 **只这四种货柜** · 老档首次拆解补种写回）→ 关键裁定：「**允许玩家通过改变开箱顺序来微调。**」→ 定案「**照此开工 ＋ 全局混算**（四种货柜共用一条拆解计数）」。**②最终口径**：种子 = `hash(存档种子, 第 N 次货柜拆解)`，`N` = 全局货柜拆解计数（安全/图纸/贵重品/军用**混算**，存存档、老档缺省 0）；`wormholeUnboxRoll` **不再消费 `state.rng`** ⇒ 开箱前去跑商/打架/触发事件都**不影响**本箱结果（防刷成立），读档重开 `N` 回到同一值 ⇒ 同一箱结果相同（**刷不了**），而**先开哪一箱由玩家决定** ⇒ 同一箱落在不同 `N` 上开出不同东西 ⇒ **顺序仍可微调**（船长要的）；一箱多件（军用 1~3 件 / 贵重品 10~20 件）由同一条子流派生 ⇒ **整箱固定**。**③落码**：`core/state.ts` 的 `RngState.box?: number`（**可选 ⇒ 零迁移**）· `core/save.ts` 按可选字段读写（**老档不带就不写回**，保持"存档往返逐字节一致"）· `core/wormholeSalvage.ts` 的 `wormholeUnboxRoll` 改走本地子流并把计数推进放在函数内（与既有 `nextRandom` 改 `rng.count` 同风格 ⇒ `industry` 调用点**零改动**）。**④相比初版作废两条**（因船长裁定而简化、记录防再犯）：~~序号落在"那一件货柜"上~~（不需要货柜实例身份）· ~~老档懒迁移~~（计数器从 0 起即可）⇒ **存档结构零改动、堆叠语义不变**。**⑤用例 5 条**（`packages/core/tests/box-seed.test.ts`）：同 (种子,N) 逐字同结果 · 不同 N 结果会变（60 个序位 >3 种）· 拆解**不推进全局流**（且"先干别的 100 次随机"后结果仍一致）· 四种货柜混算一条序列 · 结果仍落在该族池内（池与权重未动）。**⑥被既有契约逮到一次**：第一版让存档读回时**总是写 `box: 0`**，破坏"存档往返逐字节一致"⇒ 改可选字段后通过（老契约有效，记一笔）。**⑦验证**：typecheck 0 错 · core **185 文件 / 2098 例**（含新 5 例）· `content:check` ✅ · `salvage:econ` 读数未漂移 · **`save:migrate` 77/77 份真档通过**（不倒退 · 不凭空完成 · 资产不变）· `build` ✅。

- 2026-09-22：**新增六条技能（工业 T4/T5 ＋ 战斗舰种操作 T4/T5）（船长令 · 二号 · d2 · 已合入 main ＋ 公告已发 `fd409dd9`）**——**①船长原话（照抄）**：「添加一个精炼系T4的精炼炉周期缩短技能，每级4%。添加一个制造系T5技能，缩短所有零件生产周期每级4%。添加战舰操作系T4技能，护卫舰操作…驱逐舰操作…添加战舰操作系T5技能，巡洋舰操作…战列操作…」；逐条裁定：闪避/命中加**百分点**（各 +2pp/级）· 单发伤害与容量**相对乘算** · 抗性只对**已有条目**相对乘算（上限 90%、没有抗性的层不动）· 舰种操作**只对主控在驾的那艘**生效 · 两条工业技能名 =「**炉温精调学**」（前置 炉心熔炼学）/「**零件流水线**」（前置 批量生产学）· 战舰四技能前置 = **武装舰操作 ＋ 装甲舰操作**。**②落值**：`furnace-precision` 炉温精调学（工业 `b-refine` r4，精炼炉周期 −4%/级 ⇒ 满级 ×0.8，**吞吐 +25%**）· `parts-line` 零件流水线（`b-craft` r5，零件类蓝图周期 −4%/级）· `frigate-ops` 护卫舰操作（r4，闪避 +10pp）· `destroyer-ops` 驱逐舰操作（r4，单发 ×1.25 ＋ 命中 +10pp）· `cruiser-ops` 巡洋舰操作（r5，单发 ×1.15 ＋ 已有抗性 ×1.1）· `battleship-ops` 战列操作（r5，护盾/装甲/结构 ×1.15 ＋ 已有抗性 ×1.1）。**判据 = 舰种 `tier`**（T1 护卫舰 / T2 驱逐舰 / T3 巡洋舰 / T4 战列舰），与按**类别**判的「武装舰/装甲舰操作」互不冲突、可叠加 ⇒ 技能总数 **82 → 88**，存档结构不变（v31）。**③接线点**：`core/industry.ts`（炉温精调学进周期乘区，与炉心熔炼学同乘区）· `core/manufacturing.ts`（`spec.partTier !== undefined` 才吃）· `core/combat.ts` 五处（容量乘链 / 已有抗性相对乘 / 闪避加百分点 / 单发乘链 / 命中加百分点）。**④技能树排布的新结构问题与裁决**：四条舰种技能是**全仓第一批"同书两条前置"**的节点 ⇒ 排布体检「每个前置都要挂得住 ＋ 子不回头」对两个父**结构上不可能**同时成立（一列只能承一个父：设父列 pA < pB，子要挂 pA 须 x=pA，而"不回头"要求每个子 x ≥ pB ⇒ pA ≥ pB，矛盾）⇒ 船长选「**乙**」：**数据与排布算法都不动，只给体检加"多前置合流豁免"**（`tools/skilltree-layout-check.ts`：子全是共享子的父跳过这两条，但**整族必须有一条向下生长的边**；独占子照旧严格判 ⇒ 单前置的书读数逐字未变）。**⑤验证**：单测"先红后绿"（抽掉三处引擎改动 ⇒ 6 条引擎用例全红）· typecheck 四包 0 错 · core **2105 用例** · `content:check` ✅ · `l10n:check` ✅ · `ui:rot-check` ✅ · `skilltree:layout-check` ✅（23 本书 / 88 格 · 全 0 处）· `save:migrate` 76/76 ✅ · `build` ✅。**⑥发布**：公告 `fd409dd9`「界面配色、货柜拆解与技能扩充」（船长批准）已写入 `announcements.ts`。

- 2026-09-22：**全流程模拟 renovate ＋ 三条目标收尾（船长令 · 二号 · d2 · ✅ 船长 2026-09-22 消档）**——**①船长原话（照抄）**：「**更新下全流程工具，进行一次全量跑通测试，设置几个目标：完成所有悬赏，虫洞的成就完成，赚到10亿。**」＋追问两答（虫洞成就 = **六枚里程碑**：层深 2/3/4/5 ＋ 击破层末守卫 2/4；跑法 = 默认快进 `debugQuick`）。**②三条目标最终读数（30 天档 · 真流程不直接改状态）**：`bounties` **20/23**（⬜）· `whach` **全拿**（层深 5 · 守卫 47 · 第 20.03 天）· `isk1b` **第 16.00 天**（✅）。**③口径核实（别再改错）**：目标文案写"27 张卡"，数据实测 `ctx.anomalies` 共 **42 张 = 可见 23 ＋ 隐藏 19**；"27" = 23 张悬赏 ＋ 4 张隐藏遭遇卡（`enc-pirate-1..4`）——**遭遇卡不走 `completedBounties`** ⇒ 工具判据 = **23 张可见卡**是对的。**④本轮累计修掉 21 处缺陷**（每处都有实测证据）：策略层 4 处（`ensureFlagship` 换驾不配装 ⇒ 炮全装在采矿艇上；战力打分口径错 ⇒ 改用 `tier×100 + 槽位×2`；升级流程跑不到；只装驾驶船）· 虫洞专项 8 处（**最要紧 = 引擎 `buildEvalState` 只复制一艘船** ⇒ 拿它跑整队 `startFleetBattleFor` 时另外 3 艘不存在、4×T3 满配也"第 1 层打不过"，新写 `buildFleetEvalState()`；另新增 `ensureWhFleet()` 凑齐 4 艘 T3 等）· 编队质量与政策 7 处 · 关掉整条目标的两个盲区 2 处。**⑤工具**：`npx tsx tools/playthrough-sim.ts --goal bounties,whach,isk1b`（默认快进；`--max-days 30` 等档位照旧；旧档 `boss`/`tril`/`collect`/`all` 保留）——报告含三目标逐项读数、未达成时**还差哪几张卡**（威胁/声望门槛/实时预估胜率）、虫洞逐层读数、编队战力明细、进洞门未过原因、悬赏战报节选。**⑥验证边界**：本轮只改 `tools/playthrough-sim.ts` 与 `docs/design/**`（未碰 `packages/**`、`apps/**`）；全程走真流程。**⑦第 3 条目标（悬赏收全）已定性为配平问题**并成文上报 ⇒ 船长 2026-09-22 裁定**消档**（见下条）。

- 2026-09-22：**悬赏最后 3 张卡 = 配平问题（不是 bug）· ✅ 船长 2026-09-22 消档 = 接受 20/23 现状**——**结论**：虫洞「完成所有悬赏（23 张可见卡）」与「虫洞六枚里程碑（层深 5＋守卫 4）」在"单主控出征 ＋ 编队上限 4 艘"前提下**互斥**；三轮共**五种策略方案**实测**无一能同时达标**（A 全炮塔装：20/23 ＋ 虫洞 6/6 ＋ 10 亿第 16.0 天 ✅；B 主控全换导弹：22/23 但虫洞掉到 2/6；C/D/E 更差）。**机制根因（已定位到代码，非工具缺陷）**：① **射程墙**——剩余 3 张里两张是 `kite` 战术，引擎的敌方期望距离 = 「跟随我方最大射程」（`combat.ts` 的 `tactic === 'kite'` 分支 `mainMax × 0.95`）⇒ 只装动能炮塔 MK3（7,350 m）时敌停在约 **10,102 m**，引擎写「⚔ 无法交战：一炮未发…已脱离交火并返航」（脱战不写胜负 ⇒ 报告里"出征 N 次、零结局行"）；够得着须上导弹 MK3（14,880 m）或激光 MK3（10,500 m）。② **火力墙**——虫洞层末守卫硬拼伤害，而远射武器"伤害/秒/槽"更低（**炮塔 MK3 1.745 ＞ 激光 1.05 ＞ 导弹 0.915**）⇒ 换远射后虫洞立刻 6/6 → 2/6。③ 两个目标**共用同一批船**（虫洞编队上限 `WORMHOLE_MAX_SHIPS = 4`，本模拟能稳定养出的满配就是 4 艘 T3）⇒ 单主控没有余力再养"远射专用队"。**船长裁决**：三条方向（提编队上限 / 给 kite 留条够得着的路 / 接受现状）中选**接受现状**（按 20/23 交付），**不调平衡**；「想全拿就得取舍」视为设计意图。

- 2026-09-22：**采矿 vs 精炼 速度配平 · 读数入库（船长问题 · 二号 · d2 · ✅ 船长 2026-09-22 消档 = 不改数值）**——**船长原话（照抄）**：「我打算添加新技能，用于平衡采矿和精炼的速度。以鲸王+3MK3采集器满技能作为满配标准。现有的精炼炉应该加强多少才能达到 2 精炼炉搭配 1 鲸王的挖掘速度？」。**工具**：`npm run balance:mine-refine`（`tools/mine-refine-balance.ts`，正式入库；读数存档 `tools/_ui-artifacts/mine-refine-balance.txt`）。**核心读数**：**没有一个单一倍数能同时配平所有矿**——采矿速度**几乎不分矿种**（满配**纯挖掘** 普通 **195,353** / 低品级 **253,588** / 气冰 **244,059** 单位/时），而**往返把有效到货压到 ~1/3 且三档趋同**（**79,071 / 87,724 / 88,277**）；精炼侧按矿种**差 6 倍**（周期 10s~60s、批容恒 100）⇒ 同样"2 台 AI 炉（alpha）"：低品级**已过剩 1.51×**（132,597 vs 87,724）、虚空母矿**还差 ≈4 倍**（22,100 vs ~88,000）。**三条可选路线**（①一条全局技能/参数 ②收窄精炼周期的矿种跨度＋一条小幅全局技能 ③让采矿速度也分矿种）＋ 一条口径提醒（**配平基准算不算往返**：按纯挖掘要 ×1.9~4.4、按有效到货只要 ×0.66~1.2，差 2~4 倍；建议按**有效到货**）。**船长裁决**：**消档**（不改数值、不加新技能；读数与工具留档备查）。

- 2026-09-22：**通讯本地化（三号 · verify-activity-win → main · 船长令「你继续本地化」）**——
**①读数**（`npm run l10n:scan` · 英文语境真档）：通讯页 **49 → 0 处**（三批做完：发件人/主题行/送达时间 → 立场片/类型片/跳转说明 → 正文）。
**②病根**：通讯文案在**数据侧**——`CommsMessageDef.subject`（`firstTaskMessages.ts` 13 封 + `messages.ts` 17 封）与 `commsFactions.ts` 的势力/部门 `name` 都是中文，core 还把发件人拼成「势力名 · 部门名」（`comms.ts` 的 `resolveCommsSender`）再交给界面。
**③做法（data/core 一个字节没动）**：新增**渲染层单点** `ui/commsText.ts`——`commsSenderText()`（按**部门中文名**映射，13 条）、`commsSubjectText()`（按**消息稳定 id** 映射，30 条）、`commsClockText()`（用 core 导出的 `COMMS_DAY_MS` 自己算"第几天 + 时:分"，句子走 `ui.comms.044`）；查不到一律原样回落（新剧本漏登记看得见）。词条 **44 条**落 `ui.comms.001~044`，专名以游戏既有英文为准（Cinder Sector / Redring Corridor / Auro Waste Ring / Deadarmy / Low-sec，均取自 `packages/data/src/l10n.ts` 既译）。接线两处：`panels/CommsReader.tsx`（右栏 + 送达弹窗同源）与 `pages/CommsPage.tsx`（左栏列表）。
**④正文（第三批）**：「第一次」13 封任务信的 **39 行正文**英译（`ui/commsText.ts` 的 `COMMS_BODY_EN`，按 `isEn()` 取用；**行数与中文逐行对齐**，不符则整段回落中文、绝不错行）；**物品/技能/舰船名一律用游戏既有官方英文**——用临时脚本从「内容 id → `packages/data/src/l10n.ts` 英文表」导出对照后逐条核对（Peridotite / Tritanium Alloy / Silvervein Supermetal / Reinforced Mining Laser MK1 / Basic AI core / Skipjack-class Frigate / Flyingfish-class Courier / Sandcat-class Mining Corvette / Civilian Hull Repair Unit / Civilian Repair Kit / Accelerated Learning / AI Core Operation / Reprocessing / Refining）；强调段落的高亮判定改为**比对中文原文**（英译整段替换后不能拿译文比对，否则高亮丢）。
**④之补（第四批）**：协会侧**8 封短札正文**（welcome / survey-memo / industry-shift / refinery-note / salvage-crew / site-thanks / redring-outpost / first-ship，共 27 行）＋ **13 条部门与势力简介**（悬停说明 `fromBrief`，`commsBriefText()`，中文即键、已声明 `l10n-keep`）；探针 `l10n:scan` 新增**通讯逐封点开扫**（列表页只显示主题/发件人/时间，正文要选中才渲染——只扫默认那封会漏其余信件）。**逐封读数**：28 封已投递信件里 **21 封正文全英文**；剩 **7 封长设定文**（Archive reboot 12 行 · Low-sec 5 行 · wormhole unlock 5 行 · ambush-retreat 4 行 · Auro 巨构 4 行 · Deadarmy 蜂群 4 行 · Cinder 3 行 = **37 行**）留待下一批。
**⑤闸门**：typecheck 四包 0 错 · `l10n:check` 未译读数仍 **0** · core 184 文件 / 2093 例全绿 · `content:check` ✅ · `ui:rot-check` ✅ · `build` ✅ · `docs:index` ✅。
