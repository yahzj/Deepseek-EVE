# 封存卷 · 词典「三、装备 / 制造 / 经济」词条详解（2026-09-15 改版时移出）

> **为什么在这里**：船长 2026-09-15 裁定词典走「**乙：口径瘦身**」——词条正文只留**现行口径 + 出处日期 + 关键数值**，
> 「船长原话照抄 / 逐问逐答 / 实测读数 / 推演过程」一律外移进本卷（**原文搬运、一字未改**）。
> 词典里**没有丢信息**：被移走的每一个字都在下面。检索：本目录 grep 或 `git log -S "<关键词>" -- docs/glossary.md`。
>
> **进度**：第一批 = 原 glossary 第 128~131 行（4 条）；后续按章推进，每批追加一节。
## 第一批（原 glossary 第 128~131 行）

### 原第 128 行

| 目标距离（按星系独立保存）· 胜率预估口径 | 2026-09-11 船长：「**玩家每个星系设定的目标距离独立保存，预估胜率的战斗按照那个距离决定。如果没有，采用射程中段距离。**」（起因 = 船长问「预估战斗中采用的是默认的预期距离吗」——答：是，旧口径只认单一全局值、且预估恒用中段）。**口径**：`state.expedition.desirePrefByGalaxy`（键 = 星系 id，值 = 米；**可选字段、零迁移、随档保留**，`save.ts` 只收"键非空 + 值正有限"）；**单点读写** = `combat.desirePrefOf(state, galaxyId)` / `combat.setDesirePrefOf(state, galaxyId, m)`。**写入**：战斗内拖距离条 / 点「贴脸·中距·风筝」（`setBattleDesire` 写**本场所在星系**＝远征目标卡星系）· 出发时显式 `opts.desireM`（写**目标星系**）。**读取**：①**实战开战** `startBattleFor`（远征 / AI 副船按卡星系；遭遇战显式传 `desirePrefOf(state, enc.galaxyId)`——遭遇模板自带 galaxyId 是模板产地、不是玩家所在星系）；②**玩家可见胜率**——**MC N=21 预估**（`buildEvalState` 把该表带进评估快照 ⇒ 每一局模拟都按该星系设定打）与**稳态解析**（派系活跃卡 / 窝点卡的 `steadyPreview`：设定过就用设定距离、钳到开战距离内）。**回落**：该星系没设过 ⇒ **默认档**——⚠ **2026-09-15 船长两次裁定（起因＝玩家报「进行赏金任务时，敌我的初始距离非常近，对远程武器不利」）**：**星图与洞内统一 = 主武器射程带 0.8 处**（`balance.battle.desireBandMid`；导弹架 1,400~14,880 ⇒ **12,184 m**，旧"中点 8,140"作废）。沿革：当天先落成"星图 0.8 / 洞内维持中段 0.5"分档，船长随即更正「**这个是我口误，可以回滚那句**」⇒ **取消分档**；但**洞内"近战怪开局距离"另立一档**（`wormholeBrawlOpenBand = 0.5` 中段，船长选定「乙」）⇒ 2026-09-13「进去就得挨打」的张力**保留**。**解析口径同源**（`steadyDistance(..., bal.desireBandMid)`，不再按 `wh-*` 分辨）；**贴脸/风筝两档不受影响**。**作废**：旧全局字段 `desirePrefM`（老档不再沿用、一律回落；类型里留兼容占位，读档不回填）。**已知取舍**：**只有主控吃**（船长 2026-09-11 追加裁决）——AI 副船开战显式传 `desireM = null`＝强制射程中段；主控远征、低安遭遇与胜率预估才读该星系设定；战斗界面暂无「本星系目标距离」提示（船长同日裁「不用更改」） | core/combat.ts `desirePrefOf`/`setDesirePrefOf`/`startBattleFor`/`steadyPreview`；core/expedition.ts `setBattleDesire`/`beginBattleAt`/`startExpedition`；core/encounters.ts；core/winEstimate.ts `buildEvalState`；core/state.ts；core/save.ts；tests/desire-per-galaxy.test.ts（6 例）+ expedition.test.ts |

### 原第 129 行

| 主武器（战术距离口径） | **2026-09-12 船长裁定「甲」**：**主武器 = 射程最远的武器**（并列射程取名义火力大的，再并列保持武器表顺序）——单点 = `combat.mainWeaponOf`。**读这一处的有四处**：三条战术距离（贴脸 = 主武器近端×0.6 / **中距 = 带内 `desireBandMid`（⚠ 2026-09-15 船长：星图与洞内统一 **0.8**，"洞内维持中段"是口误已回滚；旧"带中点"作废）** / 风筝 = 主武器最远×0.95；战斗中三按钮与出发前战术同源）、战斗界面「我方射程带」（`battleZonesFor`）、战场弧的主武器米数刻度（`battleArcsFor` 的 `isMain`）、无星系偏好时的预估回落默认档（`desiredRangeFor` → `steadyDistance`）。**作废旧口径** `weapons.find(w => w.kind === 'gun') ?? weapons[0]`（2026-09-04 初版）：**激光是 `beam`、基础舰炮（恒在的兜底武器）是 `fixed`** ⇒ 纯激光/无人机船会回落到基础舰炮（2,500m）；**近防炮是 `turret` 槽（`kind = gun`，2,500m）** ⇒ 装了它的船被近防炮抢位（与该件自己的设定「占一个高槽、**不是主炮替代品**」相冲）。触发时机 = 近防炮上线后玩家开始装它。**实测（真数据真引擎）**：激光船 200/1250/2375 → **200/2300/4370**（射程带「基础舰炮 0~2500」→「轻型激光炮 MK1 0~4600」）；近防炮 + 导弹架 MK2 200/1251/2375 → **540/6330/11172**；炮台船与纯导弹船**一字不变**（旧口径恰好取对）。**⚠ 2026-09-12 船长追加口径（无人机船配装基准）**：**无人机船至少要带一件"加射程的高槽装备"**——即 `drone-relay` 家族：无人机中继天线 MK1/MK2/MK3 = **+20% / +45% / +80%**（CPU 6/16/46）、窝点件「流亡中继桅」+65%。**理由**：**无人机条目本身就是武器**（`src:'drone'` 也进 `mainWeaponOf` 的比较），其射程直接决定主武器带 ⇒ 雷鸥哨戒机 `200~5000` ⇒ 中距 **2,600m**；挂中继 MK3 后带变 `200~9000` ⇒ 中距 **4,600m**。**顺带闭环一笔对账**：近防炮战损台账的 **3.2 架/场**（敌方近防炮改造当日）正是**旧口径**下"无人机船回落到基础舰炮 `0~2500` ⇒ 中距 **1,250m**"的读数，今日 **2.2 架/场** 是新口径 2,600m 的读数（A/B 实测：只把期望交距设回 1,250m ⇒ **精确复现 3.2**；设 2,400m ⇒ 4.2）。**设计气味（供日后注意）**：哨戒机能否被敌方近防炮选中取决于它与 `PD_SENTRY_RANGE_M = 2,500m` 的相对位置 ⇒ **100m 的带差就能换 1 架/场** | core/combat.ts `mainWeaponOf` / `desiredRangeFor` / `battleZonesFor` / `battleArcsFor`（`isMain`）/ `steadyDistance`；renderer `panels/BattleScreen.tsx`（三按钮 + 弧端米数刻度）；tests/main-weapon-range.test.ts（7 例） |

### 原第 130 行

| 推进器周期点火（加力推进） | 2026-09-10 船长：矢量推进器**不再常驻提速**——改为**点火 60 秒 → 冷却 60 秒**、**开场即点火**；点火期战斗机动 ×(1+爆发倍率)（**MK1 +30% / MK2 +60% / MK3 +100%**，多件按 EVE 曲线收敛），冷却期回基础速度。周期由**战斗时钟推导**（`thrusterPhase`，不占存档字段），引擎与战斗界面**同源**；战斗界面底部「装填冷却」行显示倒计时。失稳代价 `hitPenalty` **也只在点火期生效**（2026-09-10 船长追加：冷却期不失效稳，`stepBattle` 用 `meAtk` 把 `hitMul` 置 1）。动机 = 原常驻加成让玩家全程快于近战敌 → 距离拔河停在玩家期望距离而近战敌够不着（复核 §9）。**周期口径文案单点（2026-09-11 船长：「推进器现在有持续时间和冷却时间，这点希望在推进器的说明内讲清」）**：秒数**唯一出处 = `balance.battle.thrusterBoostMs/thrusterCooldownMs`（或**该件自己的两个覆盖字段**）**，界面一律经 core `thrusterCycleText()`（短缀「点火 60 秒 / 冷却 60 秒」）/ `thrusterCycleFullText()`（全缀「60 秒点火 / 60 秒冷却，开场即点火」）取值，**不许写死秒数**；落点 = 换装卡与装备库/手册网格短行（`moduleShortEffect`）、模块信息卡「加力推进」行（`propTailOf(mod)`）、装配页「机动速度」行（基础值与点火期值并列）。**⚠ 2026-09-14 船长（新增「微型跃迁引擎」）改判：周期自此「全局默认 + 件可覆盖 + 逐单位判定」**——`ModuleDef.thrusterBoostMs/thrusterCooldownMs` **不写 = 用全局 60/60**（三档矢量推进器照旧）、写了 = 该单位改用这套窗口（微型跃迁引擎 = **点火 10 秒 / 冷却 60 秒**）；**一船多件取点火最短的那件**（`createPlayerSpec` 的 `propCycle`）；引擎按 `thrusterPhase(battle, bal, unitThrusterCycle(u, bal))` **逐舰**判定点火与失稳，战斗界面那一条冷却格显示**我方首舰（主控）**的周期。⇒ **旧说法「全队共用一条时间线、秒数只能来自 balance」作废**；旧稿见 `docs/design/micro-warp-engine-20260914.md` §三。 | combat.ts thrusterPhase / unitThrusterCycle / createPlayerSpec.thrusterBoost＋propCycle / stepBattle 距离步进与开火失稳（**逐单位**）/ battleArcsFor.thrusterCycle；balance.ts thrusterBoostMs/thrusterCooldownMs（全局默认）；labels.ts thrusterCycleSeconds/thrusterCycleText/thrusterCycleFullText/thrusterCycleOfModule；shipInfo.tsx moduleShortEffect / propTailOf；FitPage.tsx 机动速度行 + diffSegs 点火期速度段；modules.ts mod-prop-*（无覆盖）· **mod-mwd-1/2/3（10/60 覆盖）**；BattleScreen.tsx 底部冷却格；tests/thruster-charge.test.ts |

### 原第 131 行

| 敌速基准（速度中位线船只） | 2026-09-10 船长裁决：「采取固定锚定，参考按照速度中位线的船只进行参考」（并选「不压反提」）——**敌速不再按威胁段参考船分档锚定，改为对齐单一基准船**：船池 25 艘按 `maxSpeedMps` 排序取中位 = **长尾鲨级导弹巡洋舰（船速 272 / 敏捷 0.54）→ 基准战斗机动 165.2 m/s**（= 272 ×0.6×(1+(0.54−0.5)×2×0.15)，无技能、不装推进器）。**比率 = 敌战斗机动 ÷ 165.2**，其中敌战斗机动 = `foeSpeedMps` ×0.6×0.94（敌敏捷固定 0.3）。**全 26 张敌军卡逐卡显式给 `foeSpeedMps`**（brawl 低段缓坡 1.10/1.15/1.20 → 1.29~1.48×；orbit 0.96~1.20×；kite 维持 0.69~0.80×），`balance.foeSpeedCapMul` 1.2→1.55；**旧公式（段参考船 × m_base × 战术系数）降级为"未逐卡标定的新卡"兜底默认**。⚠ 口径换算说明：旧表分母是**阶梯**（低段 220/250 比中位船低、高段 300/320 比中位船高），换单一 275.3 后低段敌速被抬高、高段被压低——**"高段变慢"只是换分母，不是数值被削**（穹顶守卫同一份 356：对旧分母 1.05×、对新分母 1.22×）。护栏 = `content:check` **「敌速口径契约」**（每卡必须显式声明 + 比率落在战术带 brawl 1.05~1.55 / orbit 0.90~1.25 / kite 0.60~0.85 内） | data/anomalies.ts `foeSpeedMps`；balance.ts foeRefSpeedTable / foeSpeedCapMul；tools/content-check.ts；tools/battle-calibrate.ts `--proposal`；docs/design/enemy-speed-retune-20260910.md |

