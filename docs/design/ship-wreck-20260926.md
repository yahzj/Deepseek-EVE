# 玩家舰船残骸（含加固结构插件回收接口）（2026-09-26）

状态：**进行中** —— 设计已获船长确认（「确认」），落码中；归档按 AGENTS.md §8
（船长验收 + 合入 main 后当批做：关键内容并入 roadmap/词典 → 删本文件 → 重跑 `docs:index`）。

## 船长原话（照抄）

> 「**准备添加新机制，玩家舰船被摧毁后，如果是在非虫洞的正常星系内，在该星系生成一个
> '<被摧毁的舰船名称>的残骸'该残骸存在48小时，玩家如果在该星系打捞，优先打捞该残骸
> （比稀有残骸优先级还高）。打捞后玩家按照一定概率和比例回收被摧毁舰船的部分装备。
> 除此以外没有其他资源。**」

> 「**留一个接口，给之后舰船插件的。之后会添加一个加固结构的舰船插件，有加固结构的插件，
> 玩家有概率能够回收该舰船。**」

## 集中提问六答（船长选定）

| # | 问题 | 裁定 |
| --- | --- | --- |
| 1 | 触发面 | **主控 + AI 副船**（虫洞内一律不生成） |
| 2 | 残骸记账 | **只记装配快照**（各槽位装了什么件 ＋ 无人机），货舱/弹药不记 |
| 3 | 回收口径 | **逐件单独掷骰**，命中即原样回收；**概率按槽位分档** + 至少掷回 1 件 |
| 4 | 打捞节奏 | **玩家残骸独占最高优先级**：有它就每轮从中捞 1 件 |
| 5 | 计时 | **按游戏时间**，与入侵残骸同款 **48h 线性衰减** |
| 6 | 同星系多残骸 | **各记各的**、各算各的 48h，打捞按最新那条优先 |

## 设计总结（已确认）

### 一、一句话

船在正常星系被打没 ⇒ 该星系留一具写着船名的残骸，48 小时（游戏时间）线性衰减；
在那儿打捞时它排在最前面（压过稀有池），每轮从它身上捞 1 件、逐件掷概率回收装备；
**没有矿物、没有信用点、没有其它资源**。

### 二、触发面

| 损毁路径 | 调用点 | 是否生成 |
| --- | --- | --- |
| 主控远征失利遭追击（正常星系） | `expedition.ts` | ✅ |
| AI 副船远征失利遭追击（正常星系） | `ai.ts` | ✅ |
| 虫洞内被击沉 / 失联 | `wormholeBattle.ts` ×2 | ❌ |
| 低安遭遇战 | `encounters.ts` | ❌（结构 5% 底线，根本不丢船） |

**实现要点**：`loseShip()` 加**可选**入参 `wreckGalaxyId`，**只由两处正常星系调用点传入** ⇒ 虫洞那三处不传、不可能误生成。

### 三、打捞四级序

```
①★ 玩家舰船残骸（新） ← 有就每轮从中捞 1 件
① 稀有池（捞完为止）
② 普通池 ⓐ 同池内入侵残骸优先  ⓑ 否则按各组数量比
```

- 有玩家残骸时它**独占**该轮（不碰两池、不参与组比）；手选打捞对象也压不过它。
- **掷完所有件 ⇒ 残骸消失**；**48h 到点 ⇒ 无条件消失**。

### 四、回收概率（逐件掷 · 分档 · 保底）

| 件类 | 回收率 |
| --- | --- |
| 武器（炮台 / 导弹 / 激光 / 无人机导控 / 中继） | **60%** |
| 装甲 / 护盾 / 货舱扩展 | **40%** |
| 无人机舱各型 | **25%** |
| 其它（支援件等兜底） | **40%** |

**保底**：整具残骸至少掷回 1 件。**不吃技能加成**（打捞学管"每轮多捞多少 m³"，与"这件能不能捞回"两回事）。

### 五、加固结构插件接口（本次只留口子）

| 位置 | 字段 | 说明 |
| --- | --- | --- |
| `ModuleDef` | `hullRecoveryChance?: number` | 带此字段的件 = 加固结构插件（0~1 整船回收率）。**现无一件带此字段 ⇒ 恒不触发** |
| `ShipWreckRecord` | `reinforceChance?: number` | 损毁那刻把该船装着的加固件数值**求和存下**（日后插件改值不影响已生成的残骸） |

**唯一判定单点** `salvage.hullRecoveryChanceOf(rec)` = `clamp(0, 1, Σ)` ⇒ 日后加插件只改数据表 ＋ 新增那件模块，本机制零改动。

**流程**：玩家残骸被捞的**第一次**先掷整船回收 —— 命中 ⇒ **整艘船回港**（连同当时装配与无人机）、跳过逐件掷骰、残骸消失；
未命中 ⇒ 记"已掷过"标记（**一具只掷一次**，防反复刷概率），此后走逐件回收。

**船长三点裁定**：① 回**母港舰队**、耐久/装甲按残骸时刻打折（结构 30% / 装甲 50%）、**不自动成为驾驶船**、
货舱货物不回；② **同一艘船**（保留自定义船名与装配）；③ 回收率**加算、封顶 60%**（数值等插件定稿时拍）。

### 六、边界 / 不做

- **不做**：虫洞残骸 · 低安遭遇 · 货舱/弹药回收 · 跨星系打捞 · 市场/图鉴新物品（残骸是**星系账**，不是物品）。
- **不改**：稀有池与普通池的既有语义（只是前面多插一档）· 既有三类残骸的产出与价格 · 技能效果。
- 已知取舍：这是给玩家的**一次止损**（丢船从"装备全没"变成"至少捞回一半上下"）。若觉得太宽松，
  只降武器档（60% → 40%）即可，一个常量的事。
- 存档：`state.shipWrecks` 为**可选字段，零版本迁移**（照 2026-09-25 `weekendWrecks` 成例；老档缺键 = 无残骸）。

## 落码清单（实测读数）

| 文件 | 改动 |
| --- | --- |
| `core/shipWrecks.ts`（**新建**） | 本机制全部判定：注入口 `noteShipWreck` · 48h 线性衰减 `advanceShipWreckDecay` · 逐件掷骰与保底 `trySalvagePlayerWreckOf` · 加固件接口 `hullRecoveryChanceOf` / `reinforceChanceOfFitted` · 回收率分档 `WRECK_RECOVERY_RATE`（武器 0.6 / 装甲护盾货舱 0.4 / 无人机 0.25 / 兜底 0.4）。**只依赖 `state`/`types`/`rng`**，刻意不引 `equipment`/`instances`（避免成环） |
| `core/state.ts` | `ShipWreckRecord` 类型 ＋ `GameState.shipWrecks?` / `shipWreckSeq?`（**可选字段，零版本迁移**——照 `weekendWrecks` 成例） |
| `core/save.ts` | 清洗器登记 `shipWrecks`（装配/无人机逐位净化、两栏衰减数缺一不可）＋ `shipWreckSeq`；**空表不落字段** |
| `core/types.ts` | `ModuleDef.hullRecoveryChance?`（加固结构插件接口，现无一件使用） |
| `core/salvaging.ts` | 四级序第 ①★ 档：**在"手选对象的存量闸"之前**（残骸里的件与任何池的存量无关）＋ `autoTargetPickOf` 返回 `null` 让位 ＋ 捞回物入账（模块进装备库 / 无人机进仓库）＋ `volumeM3 = 0`（不占货舱、不触发满仓返航） |
| `core/shipyard.ts` | `loseShip` 加**可选** `wreckGalaxyId`（**只有传了才生成**；快照在 `delete state.fleet[]` **之前**抓）＋ `restoreShipFromWreck`（整船回收：回母港、结构 ×0.3 / 装甲 ×0.5、不自动上驾驶位） |
| `core/expedition.ts` · `core/ai.ts` | 两处正常星系调用点传星系 id（`exp.foeGalaxyId ?? anomaly.galaxyId` / `anomaly.galaxyId`）；**虫洞那三处不传** |
| `core/salvage.ts` | `advanceWreckDrift` 带上 `advanceShipWreckDecay`（第三本账共用同一条漂移推进） |
| `core/index.ts` | 导出 16 个符号 ＋ 两个类型 |
| `data/l10n/table.ts` | `core.salvaging.030/031` · `core.shipyard.033` · `ui.weekend.110/111`（中英双语） |
| `panels/Expedition.tsx` | 打捞面板**置顶残骸读数卡**：一具一张（船名 ＋ 可回收件数 ＋ 剩余小时） |
| `tests/ship-wreck.test.ts`（**新建**） | **17 条**用例，覆盖设计稿验收清单九项 |

### 实测读数（`npm run …` 全绿）

- `typecheck` 四包 **0 错** · core **229 文件 / 2521 用例全绿**（新增 17 条）
- `content:check` ✅ · `l10n:check` ✅ · `ui:rot-check` ✅ · `save:roundtrip-audit` ✅（89 份往返、**丢键 0**）· `docs:index --check` ✅ · 桌面 `build` ✅（2.24 秒）
- **概率实测对账**：一具 2 件残骸（武器 ＋ 装甲）第一轮"先出武器"= **73.3%**（400 种子）
  ↔ 理论 `0.6 + 0.4×0.6×½ = 72%`；**若档次写错（武器按 0.4 算）应为 58%** ⇒ 14pp 的差距足以分辨
- **保底实测**：单件残骸必回 1 件（`pityUsed` 置位）；一具残骸**只掷一次**整船回收
- **优先级实测**：残骸立着期间连捞 2 轮、稀有池 **2 件一件不少**；残骸清空后下一轮才出稀有残骸
- **衰减实测**：24h 时 `decayAccMs` = 半程仍在；48h 到点条目消失；打捞中的星系挂起衰减

### 一处实现期的发现（值得记）

`pullOneWreck` 里原有「手选对象已捞干 ⇒ 本轮不出」那道**存量闸**排在玩家残骸之前 ⇒
手选了一个捞干的组时，玩家残骸会被跳过。按船长「**优先打捞该残骸（比稀有残骸优先级还高）**」的字面口径，
**残骸里的件与任何型号池的存量无关** ⇒ 已把残骸那一支**提到存量闸之前**，并由用例钉住。

## 待裁决点

- 无。六问六答与加固接口三点（回收后去向/同一艘船/加算封顶 60%）均已按裁定落地；
  **待船长验收**观感（打捞面板那组读数卡的样式只做了读数，好不好看请船长判）。
