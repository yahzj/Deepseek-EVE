# 洞内货仓操作体验四条（2026-09-22）

状态：**进行中**（船长已确认口径，逐条落码）

> 船长原话（照抄）：
> 「货舱内，残骸和稀有残骸按照手册内的颜色做出区分。当货舱背包过长时，给货舱背包窗口单独加一个滚动条。
> 货舱内，玩家按住ctrl+鼠标点击能将物品快速转移到临时空间或者货舱背包内。货舱下方的物品列表内，
> 物品和物品之间的分隔不明显，建议不同类型的物品之间加个分隔线。」

> 口径确认（船长答，照抄）：「**①甲②甲③甲④只格区**」
> ① Ctrl+点击＝**整件搬**（一整堆散货条 / 一个货柜）· ② 放不下 ⇒ **拒绝并提示**（沿用现有校验与 toast）
> ③ **形状货柜也支持**（走 core 自动摆放）· ④ 只让**格区**独立滚动（下方清单保持现状）

## 范围与不做

- 只动**洞内货仓页**（`panels/Wormhole.tsx` 的货仓/临时空间两板 + 下方清单）与必要样式；不碰结算/容量口径。
- 不改 core 的摆放规则（自动摆放一律走既有 `findFreeSpot` 路径）；不新增搬运逻辑，Ctrl+点击只是**同一条搬运入口的快捷方式**。
- 不改存档结构。

## 已定位的接口（落码用）

| 用途 | 入口 |
|---|---|
| 拖动/点选落位（既有单点） | `panels/Wormhole.tsx:2942 dropAt()` → `engine.wormholeHoldDropAt` / `engine.wormholeBoardTransfer` / `engine.wormholeHoldSwap` |
| **临时空间 → 货仓**（自动找位，**已存在**） | `engine.wormholeTempStow(id)`（`game/engine.ts:2315`）→ core `wormholeTempStowPiece(state, ctx, id)`（`wormholeSalvage.ts:991`）· ⚠ 方法名里 "Temp" 说的是**来源板是临时空间**，不是"搬进临时空间"（易读错，落码时以注释为准） |
| **货仓 → 临时空间**（自动找位，**没有现成单点**） | 候选甲：`engine.wormholeBoardTransfer(from, to, id, x, y, grab)` ＋ core `findFreeSpot(...)`（`wormholeHold.ts:346`）在临时板上现算一个空位；候选乙：core `wormholeTempAddShape(...)`（`wormholeSalvage.ts:951`，**待核签名**：若它按 itemId 往临时空间加一件，正是我们要的原语） |
| 自动找空位的 core 原语 | `findFreeSpot(...)`（`wormholeHold.ts:346`） |
| 容量单点 | `wormholeHoldCapacityOf(state, ctx)`（含谜质「舱段扩展器」+8 格/台；2026-09-22 已让背包页读数改用它） |
| 残骸色调单点 | `ui/tones.ts` 的 `TONES` / `ui/Glyphs.tsx` 的 `toneOf`（手册图鉴同一套；普通残骸＝旧黄铜，稀有另档） |
| 清单分组键 | 清单现有排序用的同一把尺（`ctx.items.get(itemId).kind` 与既有 `ItemDef.kind` 家族） |

## ⚠ 落码前必须先定的一件事（避免写出第二套搬运逻辑）

**「货仓 → 临时空间」目前没有现成的 core 单点入口**（反向的 `wormholeTempStowPiece` 有）。
两条候选都**复用既有原语**，但语义/风险不同：

- **甲（推荐）**：`wormholeBoardTransfer + findFreeSpot` —— 与拖拽走**同一个搬运函数**，形状/实占格原样带过去，
  只是落点由我们现算；好处是"拖动与 Ctrl+点击"在 core 侧完全同源，失败口径也一致。
- **乙**：`wormholeTempAddShape(...)` —— 更"高层"，但**签名未核**（是否按 itemId 加、是否处理散货条都待查）；
  若它只服务形状件，散货条还得另找路 ⇒ 会分叉出两套。

⇒ 我按**甲**落（同源优先）；若你更想用乙，说一声我改。

## 四条落法

1. **残骸 / 稀有残骸配色照手册**：格子块与清单行的取色改用**同一单点**（`toneOf` / `TONES`），不新造色。
2. **格区独立滚动**：`.app-wh-hold-board`（格区容器）加 `max-height` + `overflow-y: auto`，滚轮只滚格区。
3. **Ctrl+点击整件搬**：两板的件 `onClick` 里判 `e.ctrlKey || e.metaKey` ⇒
   货仓件走 `engine.wormholeTempStow(id)`、临时空间件走 `engine.wormholeHoldStowItem(itemId)`；
   失败 `onToast(..., true)`（与拖动同一条文案 id）；成功后 `setDragId(null)` 清选中态。
4. **清单类型分隔线**：按同一分组键在"类型变化处"给行加一个类名（如 `is-group-start`），样式复用仓里既有分隔线家族。

## 验证口径

- 闸门：typecheck 四包 0 错 · `ui:rot-check`（样式改动必跑）· `content:check` · `build` · `l10n:check` 未译读数仍 0。
- 读数（无头）：① 格区出现独立滚动条且不再撑高面板（量格区 `scrollHeight > clientHeight` 与面板高度）；
  ② Ctrl+点击前后**两处格数**（`wormholeHoldInfo().used` 与背包页读数）同步变化、且失败时给出 toast；
  ③ 分隔线出现在类型切换处（量相邻行的分隔线样式）。
