# 通讯（通讯录）系统 · 设计稿（2026-09-11）

> **状态：已确认**（船长六问全选「甲」，并以「继续通讯录工作」开工）。
> 经办：一号。落点：`packages/data/src/messages.ts` · `packages/core/src/comms.ts` ·
> `apps/desktop/src/renderer/src/pages/CommsPage.tsx` · `ui/Glyphs.tsx` · `styles.css`。
>
> **v2（2026-09-11，船长裁定「继续通讯 v2」；世界观见 `docs/design/npc-factions-20260911.md`）**：
> 发件方改为**势力 + 部门**引用（新增 `data/commsFactions.ts`）、世界观铁律入契约、界面按参考图
> 换成「圆角机身 + 内嵌屏幕」线条语言。详见本文 §3.1 / §5 / §8。

## 1. 一句话

左侧导航新增**「通讯」**一级页（邮件形图标）：NPC 以**发消息**的形式补充剧情与任务提示，
玩家在此查看**历史通讯记录**；有未查看的消息时，导航图标**闪烁**并挂**未读计数**。

## 2. 船长已定六条（逐条）

| # | 问题 | 裁决 | 落法 |
|---|---|---|---|
| ① | 消息从哪来 | **甲：数据表 + 触发条件** | `packages/data/src/messages.ts` 一张 `COMMS_MESSAGES` 表，每条带 `trigger`；core 每帧廉价判定、条件满足一次即送达（幂等） |
| ② | 与既有 T9 通讯剧本（建站介绍/庆贺）的关系 | **甲：合并（统一到通讯页）** | 剧本**照旧**在抵达时弹通讯器浮层（既有体验不动），同时**镜像进通讯页**成为一条历史消息；通讯页 = 全部 NPC 通信的唯一收件箱 |
| ③ | 消息里带任务怎么做 | **甲：只给提示 + 跳转** | 消息可带 `hint`（一句提示 + 一个跳转目标页）；**不在通讯页里接取/完成任务**，点击提示只是跳到对应页面 |
| ④ | 未读口径 | **甲：逐条已读 + 图标闪烁 + 未读计数** | 未读 = 已送达且未读；点开某条即标已读；导航图标在有未读时**闪烁**（`prefers-reduced-motion` 退化为静态红点）+ 右上角**未读数字徽标** |
| ⑤ | 版式 | **甲：双列，列表内部滚动，一级页不滚** | 左列 = 消息列表（内部滚动），右列 = 正文（通讯器造型）；整页高度 = 主窗口内容区，**整页无滚动条**（约定第六章） |
| ⑥ | 玩家能否回复 | **预留回复选项接口，但本期不启用** | 类型与数据字段（`replies`）与界面分支都留好，开关常量 `COMMS_REPLIES_ENABLED = false`；玩家侧**不出现**任何回复控件（不做空按钮） |

## 3. 数据模型

```ts
/** 触发条件（core 判定；幂等：满足一次即送达，重载/复算不重复送） */
type CommsTrigger =
  | { kind: 'start' }                                  // 开局：首次推进即送达
  | { kind: 'day'; days: number }                      // 游戏内第 N 天（24 小时 = 1 天）
  | { kind: 'explored'; count: number }                // 已探明星系数 ≥ N
  | { kind: 'galaxy'; galaxyId: string }               // 探明指定星系
  | { kind: 'skill'; skillId: string; level: number }  // 某技能达到 N 级
  | { kind: 'isk'; amount: number }                    // 现金 ≥ N
  | { kind: 'siteBuilt'; siteId: string }              // 指定副站建成

interface CommsMessageDef {
  id: string                                  // 稳定 id（已读/送达按 id 记账）
  factionId: string                           // 发件势力（v2：不再写自由文本发件人）
  deptId?: string                             // 发件部门（缺省 = 势力本部，只显示势力名）
  signer?: string                             // 具名联系人（悬停说明，不改发件人写法）
  kind: '剧情' | '提示' | '委托'               // 内容类型（须落在势力/部门白名单里）
  subject: string                             // 主题
  body: readonly string[]                     // 正文（逐段）
  trigger: CommsTrigger
  hint?: { text: string; page: CommsJumpPage; tab?: string }   // 提示 + 跳转（裁决③）
  replies?: readonly CommsReplyDef[]          // 预留（裁决⑥，本期不启用）
}
```

**§3.1 v2：发件方 = 势力 + 部门**（`docs/design/npc-factions-20260911.md`）
- `data/src/commsFactions.ts` 的 `COMMS_FACTIONS`：`id / name / species(恒「章鱼人」) / alignment / tone / glyph / brief / kinds[] / departments[]`；
- **玩家看到的发件人写法不变**，仍是 `势力名 · 部门名`（协会 `深空工业协会 · 航行管制`、打捞队工会 `打捞队工会 · 老陈`）；
  拼装与降级都在 core 的 `resolveCommsSender` 一处（解析不到 → 显示原文 id，不崩）；
- 内容类型走**两级白名单**（势力 `kinds` ⊇ 部门 `kinds`），契约强制；
- 4 份建站剧本挂靠 `dshi · 基建部`（`commsFactionId`/`commsDeptId`/`commsSigner`，均可选；未挂靠回落 `title` 原文）。

剧本合并（裁决②）：`DialogueScriptDef` 增可选 `subject`（已有 4 份剧本就地补一句主题），
剧本送达/播放时记入同一收件箱，正文 = 各发言句（`发件人：内容`）。

## 4. 存档字段（零迁移）

| 字段 | 含义 | 归一化 |
|---|---|---|
| `state.commsDelivered: Record<string, number>` | 消息 id → 送达时的游戏内毫秒 | 只收「id 非空 + 数值 ≥ 0」；缺失 = 从未送达（老档天然如此） |
| `state.commsRead: Record<string, boolean>` | 消息 id → 已读 | 只收 `true`；缺失 = 未读 |

两字段均可选语义：**不写迁移版本**，老档读入后通讯页为空收件箱（除"开局信"会在下一次推进时补送）。

## 5. 界面

- **导航项**：`NAV_ITEMS` 末位新增 `{ key: 'comms', label: '通讯', icon: 'nav-mail' }`；
  图标 = 邮件 SVG 线稿（24×24 / 1.7 描边 / currentColor，与既有导航图标同语言），
  `NAV_TONES['nav-mail']` 取同族新色（青白蓝，与 `nav-items` 金黄、`nav-market` 橙区分）。
- **未读提醒**：`.app-nav-item.is-unread` 上图标走 `app-comms-blink`（**只动 transform/opacity**，约定第十四章），
  `@media (prefers-reduced-motion: reduce)` 下退化为静态红点；徽标复用 `.app-ann-dot` 家族做法，新增 `.app-nav-badge` 显示未读数。
- **双列**：`.app-comms-grid` = 左 `minmax(210px, 290px)` 列表 + 右弹性通讯器；
  列表 `overflow-y: auto`（唯一滚动区），通讯器的**屏幕**自身 `overflow-y: auto`（长文在屏幕内滚）。
- **通讯器造型（v2：机身 + 内嵌屏幕）**：右列通讯器 = **机身式大圆角外框 + 内嵌屏幕圆角框**（双层线）；
  **机身**由 SVG 线稿画（`rect` + `vector-effect="non-scaling-stroke"`，描边恒定，跟随面板边框色），
  **内嵌屏幕的圆角框由屏幕容器自己画**（`.app-comms-screen` 的 `border-radius` + CSS `outline` 线段描边，
  只描边不改盒形，**不是 CSS 拼形状**）——这样"文字容器"与"圆角框"是同一圈，**窗口任意尺寸都不会错位**；
  **禁止 CSS 拼形状**（约定第六章 / 第九章）。
  屏幕内**三层信息**：大标题（右侧内容类型小片）→ 细分隔条（发件人 + 立场小片 + 送达时间）→ 正文；
  屏幕**下方**通栏胶囊「前往」（带 `hint` 的消息才出现，钉在机身下檐口，不参与屏幕的弹性布局）。
  > **几何口径（2026-09-11 船长实测反馈后定）**：机身内工作区 = 上 15 / 侧 19 / 下 54（机身檐口 15 +
  > 按钮与屏幕间距 8 + 按钮 31）；屏幕内缩因此**与窗口高度无关**。此前内框画在 SVG 里按百分比拉伸、
  > 屏幕容器内边距却是固定值 ⇒ 机身 900px 高时内框在 39px 处、屏幕框整圈落在框外（文字看着在框外）。
- **立场 / 内容类型小片**：复用全仓 `.app-chip` 家族（不另造样式），按发件势力 `tone` 着色（内联 `color`/`border-color`）；
  立场小片的悬停 = 势力 brief + 部门 brief（「这是谁」）。
- **交互**：点列表某条 → 右侧显示正文并**标已读**；标题行右侧「全部标记已读」；
  空收件箱 = 一句占位说明（复用 `app-exp-idle` 家族样式）。
- **不做**（船长对参考图的裁定）：**人像照片式头像**、未解锁占位行、扫描线/人字纹/噪点/电量格/旋钮等装饰、任何新增动画。
  v1 的「14 顶点不规则外框」按 v2 换成上面的圆角机身语言（历史做法已作废）。
  > 2026-09-11 船长追加：**左上角要有章鱼头 SVG**（代表官方章鱼人）——这是**势力代表符号**，与上一条禁的"人像头像"不冲突。

## 6. 不做的事（边界）

- 不在通讯页里**接取/推进/交付任务**（裁决③：只提示 + 跳转）。
- 不做**玩家回复**（裁决⑥：接口预留、开关关闭）。
- 不做**主动删除/归档**消息（历史记录永久保留；后续若要再加）。
- 不改既有 T9 剧本的弹层行为，也不改建站流程。

## 7. 验收口径

1. 新档推进一次 → 收到「开局信」，导航图标闪烁 + 未读计数 1；
2. 点开即已读、计数递减、全部已读后图标停止闪烁；
3. 触发型消息（探明星系数 / 指定星系 / 技能等级 / 现金 / 副站建成）在条件达成后**恰好送达一次**（重复推进不重复送）；
4. 建站剧本播放后，通讯页能看到同一条历史记录；
5. 存档往返：送达与已读状态原样保留；老档读入不报错、通讯页为空；
6. 一级页整页无滚动条，列表内部可滚（v2 起：屏幕内部也可滚，面板体与整页不滚）；
7. `content:check`「通讯消息契约」全绿（id 唯一、触发器字段可解析、跳转目标页合法、引用星系/站点 id 存在）。

## 8. v2 落点与验证（2026-09-11，同批追加）

**新增/改动**
| 处 | 内容 |
|---|---|
| 新增 `data/src/commsFactions.ts` | `COMMS_FACTIONS`（协会 8 部门 + 打捞队工会 1 队）+ `buildCommsFactionCatalog` / `buildCommsDeptCatalog` |
| `data/src/messages.ts` | 7 条消息改 `factionId + deptId + kind`（发件人写法不变）；**欢迎信改「接到你这条船上」**（船长 2026-09-11 选词） |
| `data/src/dialogues.ts` | 4 份剧本挂靠 `dshi · 基建部`（+ 具名 `commsSigner`） |
| `data/src/blueprints.ts` | 重型动能炮描述「也会忌惮**你的船**」→「也会忌惮**你**」（玩家就是那条船；同批扫全仓仅此两处） |
| `core/src/types.ts` | 新增 `CommsFactionDef` / `CommsDeptDef` / `CommsKind` / `CommsFactionAlignment`；`CommsMessageDef` 换发件方字段；`CommsEntryView` 增 `factionName/alignment/kind/signer/fromBrief/tone/glyph`；`SimContext` 增 `commsFactions` |
| `core/src/comms.ts` | 新增 `resolveCommsSender`（发件人拼装 + 降级不崩）；送达日志与收件箱视图改走它 |
| `tools/content-check.ts` | 「通讯消息契约」扩展：**势力契约**（`species` 恒章鱼人、部门 id 势力内唯一、立场/色调/图标合法、白名单两级）+ **发件方引用契约**（势力/部门必须存在、`kind` 在白名单、每势力至少被引用一次）+ **世界观铁律扫描** |
| `CommsPage.tsx` / `styles.css` | 通讯器改「圆角机身 + 内嵌屏幕」；屏幕三层信息 + 屏幕下方通栏「前往」；立场/内容类型小片（复用 `app-chip`） |

**验证**
- typecheck ×4 全绿；core **829/829**（84 文件；`comms.test.ts` 10 → 16 例，新增 6 例覆盖发件方解析、缺部门、势力解析失败降级、部门 id 写错回落、剧本挂靠、剧本未挂靠回落）；
- `content:check` 全绿（含新契约读数：`7 条消息 + 4 份剧本；势力档案 2 个（协会 8 部门 / 打捞队工会 1 部门）`）；
- **契约负向验证 5/5**：临时探针逐条注入坏数据（物种改成人类 / 部门白名单去掉「提示」/ 部门 id 不存在 / 文案出现「智能 AI」/ 文案回退「你的舰船」）→ 契约**逐条如实报错**，还原后全绿；探针已删（仓库 `_` 探针 0 个）；
- **实机界面核验**（无头 Chrome + `web/dist` 预览 + 合成存档）：8 条条目、发件人写法 8/8 正确（`打捞队工会 · 老陈` 已按新部门名）、未读徽标 4 → 点开消息后 3、立场小片「官方」、内容类型小片「提示」且按势力色调着色、通栏「前往」文案完整、**一级页与面板体均不可滚**（`listOverflow=auto` / `screenOverflow=auto`）；
- 桌面与网页版构建均通过并含新文案。

**公告**：已批准的通讯公告（`2026-09-11-comms`）**不改**（发件人写法与「导航新增通讯」口径都没变），本批新增内容**不另发公告**。

## 9. 收尾修正（2026-09-11，船长实测反馈）

**反馈**：「中间消息窗口，文字所处的容器大小位置不对，未处于圆角方框内」。

**根因（实测取数）**：内嵌屏幕的框原来画在 SVG 里（第二个 `rect`，`preserveAspectRatio="none"` 按百分比拉伸），
而屏幕容器的内边距是固定像素 ⇒ 机身一变高，两者就错位：**机身 900px 高时内框在 39px 处、屏幕容器从 20px 起，
屏幕框整圈落在内框外面**（顶部差 19px、底部差 21px），文字因此看着不在圆角框里。

**改法**：**内嵌屏幕的框改由屏幕容器自己画**——去掉 SVG 的内框 `rect`，屏幕容器用 `border-radius: 10px`
（CSS 圆角，`outline` 只描边不改盒形，**不算 CSS 拼形状**）；机身仍由 SVG 画（非缩放描边、跟随面板边框色）。
机身内工作区内边距改为**上 15 / 侧 19 / 下 54**（下 = 檐口 15 + 与按钮间距 8 + 胶囊按钮 31，让屏幕上下居中），
通栏「前往」改为 **`position: absolute` 钉在机身下檐口**（左右比屏幕各内收 8px），不再参与屏幕的弹性布局。

**核验（无头 Chrome，三个真实视口高度 560 / 722 / 900 / 1000 各测一遍）**：
- 屏幕相对机身内缩**恒定** = 上 15 / 下 54 / 左 19 / 右 19（**与窗口高度无关**，即"错位"根因消除）；
- 机身 900px 下：正文段落、标题、分隔条、两枚小片 **7/7 全部落在屏幕框内**（`outsideCount=0`）；
- 「前往」按钮始终在机身内、且在屏幕下沿之外；
- 机身与整页均不滚，只有屏幕自身滚（`deviceScrollable=false` / `screenScrollable=false`）；
- 极端矮窗（560px 高，屏幕被压到 53px）下内容整体在屏幕内滚动，属于既有的二级窗滚动口径，非描边错位。

## 10. 大窗口（全屏）观感优化（2026-09-11 船长：「全屏这类大窗口模式下，消息界面显得很空，有无办法优化」→ 裁定「在丙的基础上，让下方跳转在全屏下更大」）

**实测依据**（2560×1440 全屏，内容区 1963×1002）：屏幕可视高 933px 而内容仅 **167px**（填充率 **17.9%**，空出 767px）；
屏内正文行长只占屏幕宽 **31.4%**（500 / 1591px）；左列 290px 内 8 条共 443px（列表底也空 559px）。
⇒ "空"的成因 = **框大内容小**（设备框拉满整宽整高，而文字短、字号固定 12.5px）。

**处方（船长定 = 丙：大窗口放大字号 + 下方跳转更大；不限宽、不改双列比例）**
- 断点 `min-width: 1500px`：屏内正文 12.5 → 14.5px、大标题 15 → 18px、发件人 12 → 13.5px、送达时间 11 → 12.5px、
  页头说明 13.5px；左列条目内边距与发件人/主题字号同步放大；屏内边距 18/24/20、段间距 10；
  **「前往」胶囊**：内边距 7/18 → **11/26**、字号 13 → **15px**（提示文字 13.5px）、左右内缩 27 → 31px、底部仍钉在檐口 15px；
- 断点 `min-width: 1900px` 再跳一档：正文 → 16px、标题 → 20px、胶囊内边距 **14/32**、字号 **16px**（提示文字 15px）；
- **机身下檐口同步抬高**（54 → 80 → 90px），否则按钮变高会顶进屏幕框（屏幕内缩是固定值）。
- 字号一律走 `calc(var(--ui-fs, 1) * Npx)`，与"设置 → 字体大小"**叠加而不互相覆盖**。

**核验（无头 Chrome，五个窗口尺寸各测一遍）**

| 窗口 | 屏内正文 | 「前往」高 | 屏幕填充率 | 几何 |
|---|---|---|---|---|
| 1280×800 | 12.5px | 33px | 84.1% | 内缩 15/72/19/19，无重叠、无滚动 |
| 1600×900 | 14.5px | 44px | 68.9% | 内缩 15/80/19/19，无重叠 |
| 1920×1080 | 16px | **51px** | 44.6% | 内缩 15/90/19/19，无重叠 |
| 2560×1440（全屏） | 16px | **51px** | 23.6%（原 17.9%） | 按钮左右内缩 31px、距底 15px；机身/整页/屏幕均不滚 |
| 全部尺寸 | — | — | — | 屏幕可视区内文字与小片 **0 个越框**；「前往」始终在机身内且在屏幕下沿之外 |

## 12. 窄窗口排版（2026-09-11 船长：「当屏幕狂赌缩小时，深空工业协会 · 航线安全的容器过窄。建议屏幕窄的情况下，后面的官方标签和时间另外起一行」）

**改动（3 条，均为自适应，不改宽屏观感）**
1. **分隔条允许换行**（`.app-comms-head`）：`flex-wrap: wrap`，官方小片与送达时间在一行放不下时**整体另起一行**（时间仍靠右），
   发件人 `flex: 0 1 auto; min-width: 0` —— 换行**优先于**把发件人截成省略号；
2. **窄窗左列收窄**（≤1180px）：`minmax(210px, 290px)` → `minmax(140px, 190px)`，把宽度让给通讯器；
3. **真窄窗改单列**（≤1023px）：双列已放不下（实测 1024 宽时屏幕仅 259px、三件挤成三行）⇒ 列表收成顶部一条
   （固定高、**横向滚动**、条目 `flex: 0 0 auto`），通讯器拿满宽。

**走过的弯路（留档，别再犯）**
- 先试过给 `.app-comms-body-col` / `.app-comms-device` / `.app-comms-screen` 加 `min-width: 0` 想让它"真正压缩"：
  结果 1024 宽下屏幕**塌到 187px**、900 宽下**只剩 63px**，文字直接**溢出屏幕框**（`allInsideScreen=false`）——
  压过头比挤更糟，已全部回退。**结论：窄窗该做的是"改布局（单列）"，不是"硬压双列"。**

**核验（无头 Chrome，五个窗口尺寸）**

| 窗口 | 布局 | 屏幕宽 | 分隔条 | 越框 |
|---|---|---|---|---|
| 1600×900 | 双列（列表 290） | 663px | 三件**同一行** | 无 |
| 1280×800 | 双列（列表 290） | 343px | **官方 + 时间另起一行**（时间靠右 0 偏差），发件人未截断 | 无 |
| 1024×760 | **单列**（列表在上，高 68px、横滚） | 389px | 官方同行、时间另起一行 | 无 |
| 1023×760 | 单列 | 388px | 同上 | 无 |
| 900×700 | 单列 | 265px | 三件分两行 | 无 |

全部尺寸：`pageScrollable=false`（一级页不滚）、列表内部滚动（宽窗纵滚 / 单列横滚）、发件人**未出现省略号**。

**已知残留（留档）**：全屏（2560 宽）下屏幕填充率仍只有 23.6%——绝对剩余高度 683px。
船长本次选的是"放大字号 + 按钮加大"（丙），**未选限宽居中（甲/乙）**；若日后仍觉空，
再考虑的候选是：①屏内正文限宽约 680px 居中；②通讯器限最大宽 900~1000px（会改双列比例，需船长另定）。

## 11. 官方章鱼人头像（2026-09-11 船长：「在消息界面的左上角，标题和来源左侧，绘制一个 SVG 章鱼的头像用来代表官方吧。该头像之后都用于代表官方章鱼人」）

**图形**：`ui/Glyphs.tsx` 新增 **`faction-octopus`**（24×24 viewBox / `currentColor`，与既有 `nav-*` 同语言）——
圆钝外套膜头部（顶端小凸起）+ 两只**实心圆眼** + 一条外套膜线 + 下方 **6 条腕足（3 对）**；
`NAV_TONES['faction-octopus'] = '#9fd8ff'`（与协会同色调，便于"官方 = 青白蓝章鱼"一眼认出）。

**尺寸与线宽（2026-09-11 船长两次复看后的最终值）**
- 船长先反馈「有些过于小了」⇒ 头像盒 38 → 56px、盒内图形 26 → 50px；
- 船长再澄清「不是放大窗口，而是**放盒内图形**且**盒内图形线条过粗**」⇒
  **头像盒不再动**，改为 ①几何按 **1.4×** 放大（图形高 11.7 → **21.1 格**，铺满视框；含描边占 y 0.4~21.6、x 4.6~19.1，不溢出）
  ②本组写死 **`strokeWidth="0.7"`** 覆盖 `Glyph` 默认的 1.7 ⇒ 50px 显示下描边由 **3.5px 降到 1.46px**
  （与导航图标同视觉粗细：19px 下 1.7 单位 ≈ 1.35px）；着墨率 40% → **15%**。
- 缩放用一次性脚本做坐标变换（`_scale-octopus.ts`，跑完即删）：按 SVG 命令逐类解析（M/L/C/H/V 与相对形式），
  **数字正则必须带小数部分**——上一版把 `6.28` 拆成 `6` 与 `28`，把路径写坏了（已回退重做）。

**复用规则（这条是"之后都用于代表官方章鱼人"的落法）**
- 数据侧新增常量 **`FACTION_OCTOPUS_GLYPH = 'faction-octopus'`**（`data/src/commsFactions.ts`），
  **协会与打捞队工会的头像都用它**——所有 NPC 势力物种皆为「章鱼人」，**只靠 `tone` 色调区分行会/立场**；
- **契约强制**：`content-check`「通讯消息契约」新增一条——势力 `glyph` 必须是 `faction-octopus`
  （写别的图标会打破"一个符号代表章鱼人"的口径，体检直接报错）；图标名同时纳入既有线稿图标名白名单。

**落点与尺寸**
- 屏幕内：**标题行左侧**的头像盒（**56px；1500 / 1900px 断点 66 / 76px**），圆角 11px、描边与着色取势力 `tone`，
  盒内图形 **50px**（占盒约 3/4——2026-09-11 船长复看"有些过于小了"后由 38/26 提上来，1600 宽下实测盒 68px、图形 50px）；
  标题与来源（发件人 + 立场小片 + 送达时间）整体右移，**标题列与来源分隔条同宽**（分隔条压在标题正下方）；
- 列表行内：原通用邮件图标 → **章鱼头 18px**（同族不同行会靠色调区分）；
- 悬停说明 = 势力 brief + 部门 brief（「这是谁」）。

**核验（无头 Chrome + 合成存档）**
- 结构：`pathCount=8` / `circleCount=2` / `filledDots=2`（两只实心眼）；
- 几何（最终值复测，1600 宽）：头像盒 **68px**、盒内图形 **50px**、描边 **0.7 单位 = 1.46px**（导航图标参考 1.35px）、
  实心占位 x 4.6~19.1 / y 0.4~21.6、图形高 **21.1 格**、着墨率 **15%**；
  在标题与来源**左侧**（不重叠）、**完整落在屏幕框内**（距屏幕上沿 15px / 左沿 24px）、正文右缘距屏幕右沿 24px（无越框）；
- 取形自检（当前模型读不了图，改用**光栅化灰度字符图 + 数值判据**）：图形**居中**（逐行墨迹中心恒定 33.5~34.0）、
  宽度剖面 = 头部穹顶（5→30 列）；左右镜像差 7.1%（网格离散误差）；底部可见 **3 对腕足**；
- 眼睛按取形结果**放大一档**（r 1.02 → 1.35）：26px 头像下原眼径仅约 1.1px 几乎看不见，现约 **2.9px**；改动只改半径，未再重跑取形；
- 屏幕可视区内文字与小片无越框，机身/整页不滚。
