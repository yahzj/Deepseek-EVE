# 带括号文案重审（跨件对比清零 + 跨件对比契约）· 工作文档

> **状态：进行中**（三号 · `verify40` · 2026-09-16）
> 归档指向：船长验收 + 合入 main 后，关键内容并入 `docs/development-conventions.md` §十三（文案纪律）
> 与 roadmap 一条，**本工作文档删除**。

## 一、船长原话（照抄 · 两轮）

1. 「**文案中依旧还有类（总输出 ≈ 攻坚炮台的八成）这种文案，建议重新审查一遍所有带有括号的文案。**」
2. 第一轮（同日早些）：「审查装备和其他物品的文案……这类明显不适合让玩家看到或者重复数据的部分修改并列给我看」→ 裁决「行，按你推荐来」。

**为什么第一轮漏了**：那一轮清的是**常规装备/物品**那一批（43 对替换），而这两条在
**窝点/虫洞专属装备**（2026-09-10 之后新增的那批）里，且都塞在括号或破折号后
（「射速只有攻坚炮台的四成、单发威力是它的两倍（总输出 ≈ 攻坚炮台的八成）」）⇒ 正则没覆盖到。

## 二、审查方法（可复现）

`tools/_probe-parens.ts`（临时探针，跑完已删）：**用编译器 API 抽全部"带括号的玩家可见串"**——
口径与 `content-check` 同款（`apps/desktop/src/*` · `packages/{core,data,ui}/src` 的**文本节点**，
注释不算）：**1676 条 · 86 个文件**。再按三类筛：

| 类 | 判据 | 结果 |
|---|---|---|
| **跨件对比** | 括号/破折号里点名别的装备 + 「N 成 / N 倍 / 相当」 | **6 条**（全在 `modules.ts`） |
| **开发词** | 「已落码 / 量级 / 契约 / 口径 / 留档」等 | **2 处**（装备说明 1 · 教程悬停 1 · 界面标签 1） |
| 极值/自比 | 「最高 / 最低 / 约为原来的 N 成」 | 4 条，**判为合规**（自比或技能上限，非跨件） |

## 三、改了什么（8 处）

| 处 | 改前 → 改后 |
|---|---|
| `modules.ts:1241` 掠袭近程导弹巢 | 「射程只有**巡航导弹架的三成**」→「射程短到只能近身用」 |
| `modules.ts:1295` 酸液喷吐器 | 「（单发略高于**攻坚激光炮**、装填更短，**总输出相当**）」→「走"以射速换单发"的路子：装填偏慢，每一发都重」 |
| `modules.ts:1322` 陵寝守备炮 | 「射速只有**攻坚炮台的四成**、单发威力是**它的两倍**（**总输出 ≈ 攻坚炮台的八成**）」＋「（必中量级）」→「射速极慢、单发极重」＋「（锁定即命中）」；「装填极慢，打空一发就是七秒」→「装填一发要七秒，打空就是七秒空窗」 |
| `modules.ts:1425` 掠袭破片炮 | 「单发是**攻坚炮台 MK3 的 1.37 倍**」→「单发威力极重」 |
| `modules.ts:1543` 孢子导弹巢 | 「单发是**导弹架 MK3 的 1.6 倍**」→「单发极重」；「（**已落码**：逐目标独立结算）」→「（逐舰各结算一次）」 |
| `modules.ts:1574` 陵卫连装炮 | 「单发是**攻坚炮台 MK3 的 1.79 倍**——一轮打出两发弹药」→「一轮打出两发、单发极重」 |
| `ui/shipInfo.tsx:945` 无人机信息卡行标 | 「生存（**契约**）」→「生存（**三层血**）」（行内就是盾/甲/结构三个数） |
| `panels/TutorialGuide.tsx:398` 教程悬停 | 「（历史上每一步都**留档**）」→「（每一步都能回看）」 |

**口径**：装备说明**只讲本件**——射速/单发/射程各自定性即可；**绝对值不进文案**（界面卡上本来就有
`单发伤害` / `装填` / `射程` 读数），也不再拿别的件当标尺。

## 四、新哨：跨件对比契约（防第三次）

`tools/content-check.ts` 新增「**跨件对比契约**」（与「陈旧术语契约」同一遍扫描、同一个文本节点口径）：

- 判据 = **只认"点名了别的装备/舰种"的对比**：类名词（炮台/激光炮/导弹架/导弹巢/近防炮/稳定器/
  计算机/阵列/甲板/扩展/推进器）后 12 字内出现「的 N 成 / 的 N 倍 / 相当 / 同档」，或指代词
  「它/该件/原型」＋「N 成 / N 倍」；
- **刻意放过三类**（第一轮已判合规）：**自比**（「满级间隔约为原来的六成」）· **跨玩法对比**
  （「收益约为亲自探索的四成」）· **数值读数**（「≈15.5 万 信用点/h」）。

## 五、验证

- [x] **存量清零**：新哨跑出 **0 命中**（147 个源文件 · 判据 2 条）；
- [x] **反证**：把「单发是导弹架 MK3 的 1.6 倍」塞回 `modules.ts:1543` ⇒ 体检报
      `跨件对比契约：… packages\data\src\modules.ts:1543（…导弹架 MK3 的 1.6 倍…）`；复原即全绿；
- [x] `npm run typecheck` 四包 0 错 · `npm run test -w @whale/core` **156 文件 / 1697 例** ·
      `npm run content:check` ✅ · `docs:index` 269 份 · 双 build ✅；
- [x] 探针 `tools/_probe-parens.ts` 按纪律**跑完即删**（审查口径写在本文件 §二，可随时重做）。

## 六、边界项（船长当日裁「甲/甲」· 已落码）

| 项 | 原文案 | 裁决与落法 |
|---|---|---|
| `ships.ts` 「（限定奇货）」×5 ＋「（限定奇货，需高声望）」×1 | 舰船说明尾注 | **甲：删**（与第一轮删掉的「（市场稀有）/（市场专供）」同类）。同批**一并清掉同类残留**：「（仅可制造）」×2 ·「（稀有现货或蓝图自造）」×1 ·「市场限量现货」半句——都是渠道信息，市场页/舰船仓库本来就看得到 |
| `ships.ts:1119/1181` 「护盾占比**全批**最高」（＋「（不再靠总血厚）」） | 洞内族舰说明 | **甲：改** ⇒ 自述「**盾厚甲薄**」；同处「（不再靠总血厚）」是**版本对比**（开发语）一并删 |
| `ships.ts:926/1249` 「火控与回避**双高**」「机巢与无人机战力**双高**」 | 电子战艇 / 无人机作战舰 | 随上一问一并改自述：「长项在火控与回避」·「机巢大、机群强」 |
| `skills.ts:82` 「（与**推进器**点火加成乘算…）」 | 矢量喷口操控 | **保留**：讲叠加规则，不是强弱对比（契约判据也不拦它） |
| `Handbook.tsx:272` 「收益约为**亲自探索**的四成」 | 手册·三种处置 | **保留**：跨玩法对比，玩家要靠它做决定 |
| `tuning.ts` 的 `where` 串 / `droneRoles.ts:132` 错误串 | 开发面 | **保留**：`tuning.ts` 无界面消费点（仅工具/调试）；`droneRoles` 那句是数据自检报错 |
| `announcements.ts:219` 「MK2 的制造蓝图书按**限定奇货档**供应」 | 已发布公告 | **保留**：讲的是"图纸走哪个渠道"，不是物品档位标签，且属已发布条目 |

**残留复核**：`限定奇货 / 仅可制造 / 稀有现货` 在源码里只剩**注释**（`ships.ts:251` 字段行注释 ·
`marketCatalog.ts:456` 段头 · `market.ts:6` 段头）与上面那条公告；产物实证（桌面包逐字扫）
`护盾占比全批最高 = 0` · `火控与回避双高 = 0` · `（总输出 ≈ 攻坚炮台的八成）= 0`。

## 七、归档待办（船长验收 + 合入后当批做）

1. `docs/development-conventions.md` §十三（文案纪律）：把"**跨件对比**"从"审查经验"升级为**明文条款**
   （只讲本件 · 绝对值不进文案 · 自比与跨玩法对比不算），并把本轮的**新哨**登记进去；
2. `docs/roadmap.md`：本批一条（1676 条审查口径 + 6 条跨件对比清零 + 新契约）；
3. 本工作文档删除 + 重跑 `npm run docs:index`。
