# 谜质科技批 · **续接卡**（二号 · d2 · 2026-09-19）

> 用途：船长要求「准备交接卡」⇒ 把本工作线的**状态 / 待裁决 / 已销账 / 边界 / 下一步**一次写死到盘上，
> 供压缩后或换会话接力用（先读 `AGENTS.md` → `docs/catalog.md` → 本卡 → 工作文档）。
> 工作文档（细节与逐条原话）：`docs/design/matter-tech-20260919.md`（§一~§九 ＋ 批次 1~6）。

## 一、现在在哪

- **两树同步**：main = d2 = `c9f63199`（写卡时两边都干净）。
  ⚠ 主树随时可能有**一号在途改动**（他最近与我在同一片区域作业：`Wormhole.tsx` / `game/engine.ts` /
  本批工作文档）⇒ **合入前必须确认主树干净**，冲突时两边都留（本批已撞过一次：批次 5 撞号 ⇒ 我那节改号批次 6）。
- **本批（谜质科技）全部已合入 main**；**未推送 origin**（推送闸门：待船长验收）。
- 我这条线的角色：**二号**（`H:\大鲸鱼\Deepseek-EVE-d2`，分支 `d2/workspace`）。

## 二、交付物一览（都已与 main 同步）

| 区块 | 落点 |
| --- | --- |
| 科技树数据 | `packages/data/src/matterTech.ts`（**23 节点 / 76 级**；满树 **1,196 谜质 ＋ 17.22 亿信用点**，洞内两条线 1,046） |
| 研究核心 | `packages/core/src/matterTech.ts`（等级/费用/前置/研究/按 `effect` 聚合/倍速档位） |
| 存档 | `state.research.levels` ＋ **v29** ＋ 迁移（老档空树、零行为变化） |
| 洞内接线 | 回合（锚定器 +10/级，趟内生效）· 威胁 · 三层抗性 · 命中 · 回避 · 敌命中 · 射程 · 装填 · 单发 · 近盲 · 无人机回收 · 战后维修 · 货仓格 · 打捞/采集效率 · 扫描间隔 −5%/级 |
| 洞外工业 | 货柜拆解 −25%/级 · 虚空晶 +10%/级 · 残骸保底 +5%/级 |
| 倍速 | 独立时间轴（`battleClockNowMs` / `battleShowWindowMs`）＋ 战斗窗口控件（**记住上次选择**） |
| 战场远端 | `battleMaxDistanceM`（距离上限随**当前**双方射程现算、只增不减）＋ 界面 `farM` 四处同尺 |
| 反存档摸图 | 进洞/深入**现掷种子**（`freshLayerSeed`）；族/原型照库存项兑现 |
| 界面 | `panels/MatterTechTab.tsx`（子页 + **三行悬停** + 二级详情窗）· 「扫描虫洞」拆两子页 · 准备页读数据 +3 项 · 资源任务**每单位均价** |
| 体检 | 「谜质科技契约」（含**满树花费读数行**）＋「效率档位契约」＋ 炮台受击增程**舰级→指定件**契约 |
| 测试档 | `docs/test-saves/test-save-mt-lab-20260919-201207.json`（谜质 ×2000 · 信用点 3B · 声望 60 · 层 2 站在 3 舰敌卡上 · 货仓刻意空着＝纯科技读数） |
| 文档 | 工作文档 `docs/design/matter-tech-20260919.md`（§八 文案调整台账 · §九 留给三号 · 批次 1~6 进度） |

## 三、复现命令（读数，改值后重跑即更新）

```
npm run typecheck                 # 四包 0 错
npm run test -w @whale/core       # 1893/1893（173 文件）
npm run content:check             # 含「谜质科技契约：… 满树花费 1,196 谜质 ＋ 17.22 亿 …」
npm run ui:rot-check              # 旋转口径（UI/样式改动必跑）
npm run ui:tip-check              # 悬停接线（改悬停必跑）
npm run flow:newgame              # 开局流程（一号新加的闸门）
npm run l10n:check                # 英文词典（新文案会进"未译读数"报告，不阻断）
npm run wormhole:econ             # 洞内逐层经济读数（本批前后一字未变）
npx tsx tools/make-test-save.ts mt-lab   # 重造实验档（会先备份真档）
```

## 四、待船长裁决（卡在他那儿，**别自己拍**）

1. **自动探索吃科技**（船长已同意方向，折减系数待定）——**一号经办**，三案：
   甲 不折减 / 乙 固定 50% / 丙 随进度 `0.4 + 0.3×科技进度`（一号推荐丙）。
2. **谜质总量 vs 每趟产出**（我提的）——满树 **1,196 枚**；按既定规则（**每层保底 1 个谜质格**、
   **1 台谜质储存器 = 1 枚**）估：一趟 3~8 枚 ⇒ 满树约 **150~400 趟**。三条路供选：
   ① 总价降一档（如 T3/T4 的谜质档）② 提每趟产出（每层 2 个谜质格／1 台 = 2 枚，会同时加快装置获取）
   ③ 先不动，等我出**"每趟真实收获"读数**再定 —— **我可以随时跑这份读数**（用 `mt-lab` 实跑、逐层统计）。
3. **「三联读数」→「编队读数」改名**是否保留（一行可回退；旧名还在 `docs/glossary.md` 与几份旧文档，
   按 §八 归档时更正）。
4. **科技树英文**（三号）：`EN_MATTER_TECH` 目前只有 `name`，说明（含新增作用域「仅洞内战斗生效」）
   与悬停三行的词典条目待补；接线点 = `packages/data/src/context.ts` 一处。
5. **公告**：`ann-matter-tech-20260919` 已按批准稿写入（一号）⇒ **是否推送/发布**等船长。

## 五、已销账（**别再重复问**）

- 倍速公式：`per^级` ⇒ 1 级 ×2、2 级 ×4（原 `1+Σ` 会算成 ×3/×5）✓
- **纯科技死线**：`applyMatterPlayerBuffs` 的 `devices === 0` 早退 ⇒ 已删（只点科技也全生效）✓
- **战斗快照死线**：`wormholeMatterBattleModsOf` 未吃科技袋 ✓
- 谜质行价 **70 万** ✓ · 回合基数 **100→80** ✓ · 效率→额外堆 ✓ · 扫描间隔 −5%/级 ✓ · 工业三件 ✓
- 「关闭虫洞准备界面后停在星图远征页」✓（`openWormhole` 不再偷切标签）
- 导弹残段：射程 **11,000** ＋ 自己的受击增程件「**巨构齐射观瞄**」（与 D 族「守墓远距观瞄」**两件独立**）✓
- 资源任务显示「均价 N 信用点/单位」（普通正文色）✓
- 取消固定种子：进洞/深入**现掷**（读档摸不到图）✓

## 六、已知取舍 / 边界（留档，**别当 bug**）

- **战场远端**：上限只由射程决定，「有地方可退 ≠ 一定退得掉」（每拍仍是速度拔河，没动）。
- **洞内 8 层对参考编队仍判「墙」**（`wormhole:econ`）：本批之前就有的基线，科技只给玩家加 buff，不改基线。
- **战场远端对洞外同样生效**（船长裁定「甲：洞内洞外同一套」）。
- **自动探索仍用固定种子**（`item.seed`）：船长选「甲」时已知悉；日后要一并现掷 ⇒ 改 `wormholeAuto` 一处。
- **一号修的回合公式**（`turnsBase + 装置 + 当前科技整份`）覆盖了我上批的错误实现（`+ 科技增量` 会吞掉
  整份锚定器加成，玩家报的「138 → 38」即此）⇒ **不要再按旧公式改回去**。
- 悬停是**纯文本**渲染 ⇒ 文案里不许写 `**` 强调记号（`content:check` 文案纯净契约会红，本批被它抓过两次）。

## 七、下一步（若由我继续）

1. 船长要读数 ⇒ 跑「**每趟真实收获**」（`mt-lab` 实跑，逐层统计谜质枚数与最终入仓枚数），据此议第 4 节第 2 条。
2. 按船长裁决动数值（总量 / 每趟产出），并同步体检读数行与工作文档。
3. **验收后的归档三步**（§八）：工作文档关键内容并入 `roadmap` / `glossary`（含「三联读数」改名与
   谜质科技新词条）→ **删工作文档与本续接卡**（或按旧例移入 `docs/design/archive/`）→ `npm run docs:index`。
4. 推送 origin（**待船长批准**；公告同批）。

## 八、干活纪律速查（本会话踩过的坑）

- **提交消息**一律 `$msg | npm run commit:msg '--' --add`（消息从 stdin 进，绕开 PowerShell 双引号陷阱；
  要传 git 参数时分隔符必须写成 `'--'`）。
- pwsh 是 **Windows PowerShell 5.1**：`.NET` 文件 API 一律**绝对路径**、写盘用 .NET 显式 UTF-8；
  没有 `??`；双引号串里反引号是转义符（细则见约定 §三）。
- 临时探针一律 `_` 前缀、用完即删；改 `tools/*.ts` 必须**真跑一次那个工具**（`tools/` 不在 typecheck 覆盖内）。
- UI 改动跑 `ui:rot-check`（＋改悬停跑 `ui:tip-check`）；观感审查权在船长，**不许自己截图当结论**。
