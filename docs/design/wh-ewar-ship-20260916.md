# 劫掠电子舰 + 劫掠捕获网（**已暂停** · 待续接）· 工作文档

> **状态：暂停**（船长 2026-09-16「暂停」＋「先将劫掠电子舰之外的内容合并检查后推送」）
> 代码 WIP **未提交**：`git stash@{0}`（名字：`WIP 劫掠电子舰 + 劫掠捕获网（船长 2026-09-16 暂停…）`），
> 另在 `tools/_ui-artifacts/_wip-ewar-20260916.patch`（gitignore 目录）留了一份补丁备份。
> **恢复方式**：`git stash pop`（或 `git apply tools/_ui-artifacts/_wip-ewar-20260916.patch`）。

## 一、船长原话（照抄，按时间序）

1. 「**新增A族敌人劫掠电子舰，添加挂载件冲锋，并额外加装一件新的挂载件，劫掠捕获网：降低目标90%移动速度，
   并关闭所有类型推进器。在自身第一次开火时发动。动画效果为一根蓝色的光速连着命中舰船。添加进深层的海盗战团里。**」
2. （闸门确认时）「**血量修正为0.7**」＋「**补充一点，劫掠捕获网还会让目标闪避强制为0，射程降低500米**」
3. 「**在玩家第一次遭遇劫掠电子舰之后。结束虫洞或回到主界面时，给玩家发送一封通讯，介绍劫掠电子舰的捕获网。**」
4. 「**暂停**」→「**先将劫掠电子舰之外的内容合并检查后推送**」

## 二、已确认口径（四问四答 + 两条修正）

| 项 | 口径 |
|---|---|
| 新舰 | **T1 护卫舰** · 血量系数 **0.7**（= 260 × 0.70 = **182**）· **闪避提高**（舰级覆写 `evasion`，建议 **0.30**，现统一 0.12）· 单发建议 30 · 速度 374（340×1.10，满足 A 族「每档高于基准」）· 射程带 326~4275 · orbit |
| 挂载件 | 冲锋沿用 `foe-mount-charge-pirate`（×1.6 / 30s）＋新件 **`foe-mount-capture-web`「劫掠捕获网」** |
| 捕获网 · 触发 | **自身第一次开火那一刻**发动（**不看命中**），只钉**它这一发的目标** |
| 捕获网 · 效果 | ① 战斗机动 **×0.1**（−90%）② **关闭所有类型推进器**（含微型跃迁引擎）③ **闪避强制为 0** ④ **武器射程 −500m**（两端各减、近界下限 1m） |
| 捕获网 · 时长/解除 | **本场永久**；**击杀发动者即解除**；多艘**不叠加** |
| 编成 | 「海盗战团」（deep 卡）改为 **头目×1 + 电子舰×1 + 快艇×2**（单位数仍 4 ⇒ 总威胁预算不变） |
| 动画 | **蓝色连线**：从发动者连到被钉舰（SVG 线稿；持续到效果解除） |
| 通讯 | **首次遭遇劫掠电子舰之后**、**结束虫洞或回到主界面时**发一封介绍捕获网的通讯 |

## 三、WIP 已落地的部分（在 stash 里，**未提交**）

1. `types.ts`：`FoeMountId` 增 `foe-mount-capture-web`；`FoeMountDef.web`（四层效果参数）；
   `FoeShipDef.evasion`（舰级闪避覆写，缺省 0.12 ⇒ 老卡零变化）。
2. `foeMounts.ts`：新件 id 常量 + `FOE_MOUNTS` 条目（`slowMul 0.1 / noThruster / noEvasion / rangeDownM 500`）
   + `ResolvedFoeMounts.foeCaptureWeb`。
3. `combat.ts`：舰级路径读 `ship.evasion ?? 0.12`（两处）；挂载件解析结果写入 `UnitSpec.foeCaptureWeb`
   （注释里那句被 PowerShell 反引号吃掉过，`stash pop` 后**先看一眼 1962 行附近**再继续）。
4. `state.ts`：`BattleState.meWebDebuffs` / `foeWebFired` / 新接口 `BattleWebDebuff` / `BattleFx.web`。

## 四、待办（续接点，按序做）

1. **引擎**：`applyMeWebDebuff(spec, d)`（减速 ×0.1 / 清 `thrusterBoost` / `evasion=0` / 每条武器射程 −500）
   用在三处：`buildMyUnitSpecs` 每拍重建、视图 `battleArcsFor`、触发当拍的 `gtgt.spec`（让这一发就看到闪避 0）；
   触发点 = 敌方开火循环 `pickTarget()` 之后（`foeCaptureWeb && foeWebFired[tag] !== true`）；
   每拍清理"发动者已死"的条目；`pushBattleFx({ web: true, tag: 发动者, to: 被钉者 })`。
2. **存档登记**：`save.ts` 把 `meWebDebuffs` / `foeWebFired` 标 `runtime`（不入档）。
3. **数据**：新舰 `foe-pirate-raider`「劫掠电子舰」（T1 · 血 182 · 单发 30 · 闪避 0.30 · 速度 374 · orbit）
   + `FOE_SHIPS` 登记；「海盗战团」编成改 头目×1 + 电子舰×1（挂冲锋件 + 捕获网件）+ 快艇×2。
4. **界面**：蓝色连线（SVG 线稿，遵守 §6；从发动者锚点到被钉舰锚点，持续到解除）+ 被钉舰负面标记 + 悬停文案。
5. **通讯**：新触发器 `{ kind: 'foeShipSeen', shipId }`（随档标记 `state.foeShipSeen`，置位点 = 开战扫描该卡编成）
   + 消息级新字段 `holdWhenBusy`（洞内/交战中不投递 ⇒ 「结束虫洞或回到主界面」才送达）
   + 一封介绍捕获网的通讯（发件方建议「深空工业协会 · 情报库」；文案按 §5/§12 第三人称、无开发话语）。
6. **契约**：挂载件契约扩「捕获网件」（只允许挂在该卡电子舰上）；`evasion` 值域护栏；新舰纳入 A 族敌速口径。
7. **用例**：首次开火即发（不看命中）· 四层效果各自生效 · **击杀发动者后解除** · 不叠加 · 洞外/其它卡零影响 ·
   特效事件（蓝线数据源）· 通讯触发与"洞内不投递"。
8. **闸门 + 文档 + 提交**（本批全绿后再合并推送）。

## 五、已归档/已推送的相邻批次（与本事无关，勿混）

- 敌方挂载件体系（冲锋 / 受击增程挂载化）· A 族洞内海盗冲锋 ×1.6/30s · 洞内首波 `foeMounts` 修复 ·
  族哈希恒 A 真 BUG 修复 · 距离条两端显示双方速度 · 面板速度口径对齐 · 海盗护卫舰速度 337→374。
- 这些都在 `8360916e / 9aa8ae1a / c444c3ef` 三条提交里（随本批推送）。
