# 归档续接卡 · 2026-09-10 批次（稀有残骸解禁 + 高级箱数值 + 工业页产出显示 + 窝点档位 + 蓝图速率）

> 用途：本批次收尾归档。船长随时可据此复盘；**下一位接手人（含被压缩后的一号）先读这一页**，
> 再按 §4「续接要点」看代码与文档。
> 状态：**待验收/待发布**——全部改动为**本地提交、未推送**（推送闸门：待船长验收 + 公告批准后，
> 代码与公告一起推一次）。

## 1. 本批交付（按时间顺序，均为船长当日拍板）

| # | 事项 | 关键口径 | 提交 |
|---|---|---|---|
| 1 | **蓝图获取速率调整** | 碎片门槛 MK2 100→**25** / MK3 1000→**250**（概率不动）；蓝图书权重 50%→**5%**（稀有 + 奇货两渠道）；蓝图单寿命 36 分钟→**6 小时** | `f1e385f` |
| 2 | **碎片池「集齐前不重复」** | 已学会（含市场买书）或已集齐门槛的模块移出池子 → 凑齐该档三张书恰好 3×门槛片，尾部消失 | `8f0f2c4` |
| 3 | **赏金任务档位改由「地图级别」封顶** | 新增 `AnomalyDef.lairLevel`（1=只出外围 / 2=到核心 / 3=全档），19 张候选卡逐张标级（E 族两张都 3、每族至少一张 3 级）；**取消三档保底**；同星系代表卡取级别最高 | `0b8bed7` |
| 4 | **工业页产出显示拥有数** | 精炼炉：「♨ 产出：」标题 + 每种产物缩进一行 + `（仓库 N）`；组装机：产物行尾 `（装备库/仓库/机库 N）` | `9363256`、`60b0c54` |
| 5 | **稀有残骸解禁** | 回收炉不再拒收；卡片回归回收网格（带「高级箱」徽标）；全部"暂不受理/先封存"文案改口 | `01c2ae6` |
| 6 | **高级箱数值定稿 + 每炉锁死 1 件** | 命中率 25/40/55% → **5% / 8% / 10%**；炉次新增 `lockUnits`：稀有残骸起炉锁 30 m³（= 1 件），3 批后定额停炉 → **一炉恒等于一件 = 一箱** | `42cc3a2` |

## 2. 关键数值（复跑命令与实测口径）

| 指标 | 值 | 复跑命令 |
|---|---|---|
| 碎片路线整档（三张书） | MK2 166,667 m³（炉时 116h / 93h）· MK3 1,071,429 m³（744h / 595h）→ 约为"买书等价工时"的 **7 倍** | `npm run salvage:econ` |
| 蓝图书占稀有供给 | 25.2% → **3.2%**；单行间隔 1.5h/6.0h → **11.7h / 40.9h**；稀有渠道每窗张数不变 | `npx tsx tools/market-rarity-sim.ts --windows 2880` |
| 日板档位分布（120 天真实引擎） | 外围 2.36 / 核心 1.93 / 深层 0.72 张；当天至少一张深层 **54.2%**（旧 100%） | `npm run bounty:econ` |
| 稀有残骸日供给 | **≈9.8 件/天**（全清日板 8.4 + 派系活跃 1.4） | `npm run bounty:econ` |
| 高级箱 | 专属命中 **5/8/10%**；未命中必给主题件；另附矿物 300/120/40 单位；**每炉一箱（= 一件）** | `npm run bounty:econ` |
| 集齐一族三件专属装备 | ≈60 / 37.5 / 30 箱 → **≈3.1~6.1 天/族**（五族 ≈15~30 天） | `npm run bounty:econ` |
| 「非命中收益是否被砍」的答复 | **没砍反而升**：深层卡每箱可兑现 ≈2.1 万 → **3.5 万 ISK（+66%）**、外围有主题池卡 ≈9.6 万 → **11.9 万（+26%）**（命中给不可售的专属件、未命中给可售主题件） | `npm run bounty:econ` |

## 3. 实机测试存档（船长可直接加载）

- 文件：`docs/test-saves/test-save-rarebox-20260910-224146.json`（生成器已注册 `rarebox` case）
- 双保险备份：`docs/test-saves/user-backup-20260910-224146.json`（注入前的真档副本）
- 加载：游戏内先「存档管理 → 备份」→ 退出 → 用测试档替换 `%APPDATA%\whale-idle\save.json` → 启动即测；
  测完用游戏内备份恢复。
- 注入内容与建议测试路径：见 `docs/test-saves/README.md`「档案清单 → 稀有残骸·高级箱实机验证（rarebox）」。
- 已自检（引擎读档核对）：稀有残骸 11 件（330 m³，五族）、普通残骸 100+100 m³、碎片 20 / 250、
  基础 AI 核心 2、驾驶船 白鲨级、存档版本 24、无残留在炉台数。

## 4. 续接要点（下一位接手人看这里）

1. **代码入口**：`packages/core/src/salvage.ts`（碎片配方/池、`RARE_BOX_GEAR_CHANCE`、`RARE_BOX_MINERAL_UNITS`、`rollRareBoxExtra`）、
   `industry.ts`（`startRecycleRun` 起炉与 `lockUnits`、`advanceRefining` 每批扣料与定额停炉）、
   `lairs.ts`（档位常量、`FOE_LAIR_GEAR`、`lairLevelOf`）、`sideTasks.ts`（日板抽地点与逐卡掷档）、
   `data/anomalies.ts`（19 张窝点卡的 `lairCore` / `lairLevel`）。
2. **契约（体检护栏，别绕过）**：`npm run content:check` 里的「窝点契约」「窝点级别契约」「来源唯一契约」
   三块——候选卡必须显式标级、每族至少一张 3 级、专属装备无蓝图/无市场卡/无碎片配方/不进常规池。
3. **改这些常量后必须复跑的对照**：`npm run bounty:econ`（日板档位分布 + 稀有残骸高级箱对照）、
   `npm run salvage:econ`（碎片路线与彩头 EV）、`npx tsx tools/market-rarity-sim.ts`（市场稀有/蓝图结构）。
4. **存档兼容**：本批只新增**可选字段**（`lairLevel`、`lockUnits`），老档零迁移；`lockUnits` 缺省 = 不限制。
5. **待船长定/待批的尾巴**：
   - **公告待审稿 11 张**（`docs/design/announcement-draft-20260910-pending.md`）：其中第 10 张（蓝图速率）、
     第 11 张（稀有残骸解禁）为本批所拟；**未写入 `announcements.ts`**，等船长逐条批复。
   - 本批已按船长指示修订了 2026-09-08 B3-1 公告里过期的「集 100/1000 片」→「集 25/250 片」。
   - **常驻（common）蓝图书渠道**（0.425/窗）船长明确「不管」，维持原样。
   - 高级箱的**主题件兜底金额**（外围有主题池卡每箱 ≈11.9 万 ISK、全清日板口径每天可达百万级）
     船长尚未表态是否收口——若要拧，旋钮是「主题件兜底概率」或「矿物单位」。
6. **本地提交状态**：本批 6 个提交 + 此前若干批次均**未推送**（推送闸门）。工作区干净（除本轮临时探针已删）。
