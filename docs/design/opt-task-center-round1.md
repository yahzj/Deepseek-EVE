# 优化轮 1：任务中心分类 / 建站物资明示 / 连击战斗最小化（状态：已确认）

> 船长 2026-09-04 体验优化点（T1–T11 收口后的打磨轮之一）。实现即交付，无 core 行为改动（除 UI 标记）。

## 1. 任务中心选项卡分类（多归属）
- 四个选项卡：**重要任务 / 悬赏任务 / 资源任务 / 快递任务**，任务可跨分类出现
  （建站 = 重要（长期目标）+ 资源（分档提交）→ 两页都显示同一站点卡）；
- 悬赏页保留原排序（距离/星系/奖励/声望/默认）；快递分类已就位（暂无内容，占位文案）；
- 当前选中页存 localStorage（whale-idle:task-tab）。

## 2. 建站任务"所需物资"明示
- 站点卡新增物资栏：所需物资名称（红环：希莫非特/灼烧岩；烬火：蓝霜冰）+
  产出来源矿带名 + 声望门槛 + 单单位体积，注明"任意组合、按档累计"；
- 每档行统一显示该档建材总量（不再只显示当前档）。

## 3. 连击自动战斗界面默认最小化
- 引擎侧无改动；desktop wrapper 记录"本场远征由连续出击自动发起"（autoSortie 标记，
  远征结束后复位）；App 的"自动切入全屏战场"对该标记放行——自动连击期间战斗以
  右上角「⚔ 战斗中 · 进入战场」小浮钮呈现，玩家想观战随时点入；手动出击照旧自动弹。

## 涉及
- renderer：Expedition.tsx（选项卡重构 + 物资栏 + 档位总量）、App.tsx（自动弹战场守卫）、
  game/engine.ts（autoSortie 标记/查询）、styles.css；
- core 零改动；存档零改动。

## 验证
- core 244/244 绿；typecheck/build 通过；
- Electron CDP：四选项卡齐全且按序；重要/资源页各见 2 张站点卡；物资栏含具体矿名；
  悬赏页排序选择器在；快递页空态文案；0 异常。
