# 谜质科技 · 洞外工业新增 T4「工业多核调度」（工业 AI 工位上限 +1/级 · 5 级）· 2026-09-20

> **状态：进行中**（已实现，四闸门全绿，待船长验收）
>
> **船长原话（照抄）**：「**谜质研究的洞外工业，添加T4科技，增加工业AI上限，每级+1，最高5级**」

## 一、新节点（数据表条目）

| 字段 | 值 |
|---|---|
| id / 名 | `mt-industry-ai` · **工业多核调度**（EN：Industrial Multi-core Dispatch） |
| 支 / 层 | `industry`（洞外工业线）· **T4** |
| 效果 | `industryAiSlots` · `per: 1` · **maxLevel: 5** ⇒ **每级 +1 站内工业 AI 工位，满级 +5** |
| 费用（谜质） | `30 / 60 / 90 / 120 / 150`（T4 档步长 30，与既有两张 T4 同阶） |
| 费用（信用点） | `1.2 亿 / 2.4 亿 / 3.6 亿 / 4.8 亿 / 6 亿` |
| 前置 | `mt-industry-void`（虚空精炼技术 ≥1，同支 T3 —— 契约要求"前置在更低层"） |
| 说明 | 「每级提高站内工业的 AI 专用工位上限 1 个（满级 +5；每个工位仍占一枚实体核心）。」 |

## 二、效果接线（三处，缺一处就"点了没反应"）

1. **关键字登记**：`core/types.ts` 的 `MatterTechEffect` 联合新增 `'industryAiSlots'`（写错/漏登记 ⇒ 类型报错 + 体检判红）。
2. **消费点**：`core/ai.ts` 的 `industryAiBonus` = **谜质科技 `matterTechSum(state, ctx, 'industryAiSlots')` ＋ 技能表 `balance.aiCore.industrySkillSlots`**（两支**相加**）。
   内容体检的「谜质科技契约」⑤ 靠 core 源码里的 `'industryAiSlots'` 字面量判"登记了没接线" ✓。
3. **反向护栏（新加）**：内容体检「AI 扩容契约」补一条——**谜质科技节点说明里承诺「专用工位」的，`effect` 必须是 `industryAiSlots`**（与技能那条"说明里承诺工业专用工位 ⇒ 必须在扩容表里"同款），并把读数行扩成三个来源。

## 三、读数

| 项 | 改前 | **改后** |
|---|---|---|
| 满级站内 AI 工位 | 共用 5 ＋ 技能 15 = **20** | 共用 5 ＋ 技能 15 ＋ **科技 5 = 25** |
| 谜质科技树 | 23 节点 · 76 级 · 满树 **1,196 谜质 ＋ 17.22 亿信用点** | **24 节点（探索 6 / 战斗 14 / 工业 4）· 81 级 · 满树 1,646 谜质 ＋ 35.22 亿信用点** |
| 其中洞外工业线 | 150 谜质 | **600 谜质**（新节点自己 450 谜质 ＋ 18 亿信用点） |

⚠ **这条 T4 是本树里最贵的一件**（既有两张 T4 各 90 谜质/2 级，本节点 5 级按同一步长外推 ⇒ 450 谜质）。
若你要压价，我给的三个可选（都只改数据表，一句话就改）：
① **降步长**：`20/30/40/50/60 = 200 谜质`（ISK 同比例降到 0.8 亿/级）；
② **改 3 级**（每级 +1 ⇒ 满级 +3，与"每级+1、最高 5 级"不符，需你改口径）；
③ **保持现值**（与"T4 就该贵"一致，且它是**永久 +5 工位**、摊到长期很便宜）。

## 四、验证

- `npm run typecheck` 四包 **0 错**；
- `npm run test -w @whale/core` **177 文件 / 1,993 用例全绿**——新增 2 例：
  `matter-tech.test.ts`（节点表 24 · 工业 4 · 新节点的 层/效果/每级/满级/费用/前置）·
  `ai.test.ts`（**点满科技 ⇒ `industryAiBonus` +5；与技能两支相加以至 20；只点科技不练技能也能开站内产业、副船仍被共用上限拦**）；
  ⚠ 那条例用**真上下文**（`buildSimContext()`）——`makeTestCtx` 是精简世界、没有 `ctx.matterTech` 目录，用它测会恒为 0（踩过）。
- `npm run content:check` ✅（谜质科技契约读数：24 节点 · 81 级 · 1,646 谜质；AI 扩容契约读数：**共用 5 + 技能 15 + 谜质科技 5 = 25**）·
  `npm run l10n:check` ✅（英文名补进 `l10n.ts` 的 `EN_MATTER_TECH`）。
- ⚠ **既有 l10n 缺口（不是本批引入）**：谜质科技各节点的 **`note` 全树只有中文**（`EN_MATTER_TECH` 只覆盖 `name`）⇒ 英文界面下说明仍显示中文。要不要整树补 EN，等你定。
- UI 无改动（科技页按分支/层渲染，数据驱动）。

## 五、归档待办（§8）

- `docs/glossary.md` §十一「谜质科技树」：节点数 23→24 · 满树花费 1,196→**1,646 谜质 / 17.22→35.22 亿** · 工业线补第 4 条 ·
  「工业自动化基础（工业扩容）」那条把"满技能站内工位 = 20"改成 **25**（含科技那 5）。
- roadmap 一条（可并进谜质科技那条）。

---

_维护：本件是工作文档，验收后按 AGENTS §8 归档（关键内容并入 roadmap/词典 → 删文件 → 重跑 docs:index）。_
