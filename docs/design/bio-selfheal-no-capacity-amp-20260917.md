# 生体甲壳板：无消耗自愈不吃装甲容量加成（2026-09-17）

> **状态：进行中**（工作文档；船长验收 + 合入 main 后按 AGENTS.md §8 归档三步：并入 roadmap / 词典改写 → 删本文件 → 重跑 `docs:index`）

## 一、船长原话（照抄）

1. 「**生体甲壳板的维修量，我希望不吃装甲容量的加成（文案已经在三号处修正**」
2. 追加（同日，看到第一件的落码后）：「**损管腔也一同修改，并修正损管腔文案**」

## 二、§5.2 冲突与改判（旧裁定作废范围）

- **旧口径（2026-09-16 船长定，本次为例外）**：「**统一吃，并在相关说明中提及（提高维修量等）。**」
  ⇒ 维修装置的每跳修复量与修理组件**同一把尺**，都随**额外护甲/结构加成**放大
  （出处：`docs/archive/roadmap-2026-09-16.md`「维修量『统一吃』层容量加成」条 · 词典「船体维修装置（战后消耗口径）」/「遇袭后自动维修」两条 · 设计稿 `hull-repair-module-20260909.md`）。
- **新裁定（2026-09-17，两件）**：**「生体甲壳板」（装甲自愈 6）与「生体损管腔」（结构自愈 4）的无消耗自愈都为平值**，
  不吃对应层的容量加成 ⇒ 09-16 那条**继续对"耗组件"的维修装置与修理组件有效**（不是全类回滚）。
- **范围**：**恰好这两件**（都是 `repairFree` 无消耗自愈）；后来再有新件要不要开例外，**等船长点名**，
  不许"顺手补齐"（字段注释与用例都写了这句）。
  ⚠ 第一轮按船长字面只改了甲壳板；他在同一天追加「损管腔也一同修改」⇒ 本批扩到两件（见 §四·历史）。

## 三、落码（数据字段驱动，不在引擎里硬判物品 id）

| # | 文件 | 改动 |
|---|---|---|
| 1 | `core/src/types.ts` | `ModuleDef` 新增可选字段 **`repairIgnoresCapacityAmp?: boolean`**（注释写明：修复量按**平值**结算；只对无消耗自愈有意义；**现持有人 = 两件**；新件等船长点名） |
| 2 | `data/src/modules.ts` | **`mod-lair-armor-c`（生体甲壳板）＋ `mod-lair-dc-c`（生体损管腔）**各写 `repairIgnoresCapacityAmp: true` |
| 3 | `core/src/combat.ts` | `preloadRepairFor` 的**无消耗自愈分支**：带该字段的件 ⇒ `armorPerPulse = round(repairArmorHp × 曲线权重)`、`hullPerPulse` 同理，**不再乘 `amp.a / amp.h`**；耗组件分支一字未动（仍吃 `amp`） |
| 4 | `core/tests/lair-gear-c.test.ts` | 一条用例钉三件事：① 甲壳板 **6**（甲容 +20% 也不放大）② 损管腔 **4**（结构 +75% 也不放大）③ 对照：**耗组件装置 12（甲）/ 18（结构）照旧吃** |
| 5 | `data/src/modules.ts`（文案） | **损管腔文案按船长「并修正损管腔文案」重写**：去掉「修复量随结构容量加成放大」（机制已作废）与风味句 ⇒「**结构层三系减伤各 +25%；交火中每 5 秒自修 4 点结构（不吃组件）。**」——**句式对齐三号重写后的生体甲壳板**（同样不带"窝点专属"前缀）· `copyLen` **26**（与甲壳板同长）· 括号只放规格 · 不写原因解释 |

## 四、历史：范围是怎么从一件扩到两件的

- 第一轮（本文件初版）按船长的字面**只改甲壳板**，并在当天 13:0x 推送（提交 `9d5586a2`）；三号在途的文案也**只改了甲壳板**。
- 同日船长追加「**损管腔也一同修改，并修正损管腔文案**」⇒ 第二轮扩到两件（数据字段 ＋ 用例 ＋ 损管腔文案）。
- ⚠ **同时点**：三号那批（`b930a063` 合入 main）已把**甲壳板**的文案按新硬规矩重写为
  「装甲层三系减伤各 +10%；交火中每 5 秒自修 6 点装甲（不吃组件）。」（26 字，且**带上了本例外**）；
  核过**没有第二套机制**（全表只有 `repairIgnoresCapacityAmp` 一个字段，无重复实现）。

## 五、验证（2026-09-17 实测）

- [x] `npm run typecheck` 四包 **0 错**
- [x] `npm run test -w @whale/core` **163 文件 / 1,775 例全绿**（`lair-gear-c.test.ts` +1 条 = 三件事一起钉）
- [x] `npm run content:check` ✅ —— 文案契约读数：**核对 533 条说明 · 存量待改写 246 条（基线宽限）· 超线报红 0 条 ·
      括号原因解释 存量 0 / 报红 0**（新写的损管腔文案 `copyLen = 26` 通过）
- [x] **读数（真引擎 `preloadRepairFor` · 一次性探针跑完即删）**：同一档装配
      （甲壳板 ＋ 装甲增厚板 MK1〔甲 +20%〕· 损管腔 ＋ MK1 维修装置 ＋ 几丁质骨架层〔结构 +75%〕）下
      —— **生体甲壳板 6/跳**（改前 **7**：6 × 1.2）· **生体损管腔 4/跳**（改前 **7**：4 × 1.75）
      · **耗组件装置 12（甲）/ 18（结构）**（照旧吃加成，09-16 那条对它们仍然有效）
- [x] **负向验证两次各自精准**：① 拿掉甲壳板的字段 ⇒「甲壳板的自愈量不该吃装甲容量加成:
      expected 7 to be 6」；② 拿掉损管腔的字段 ⇒「损管腔的自愈量不该吃结构容量加成: expected 7 to be 4」；
      两次还原即 7/7 全绿

## 六、待裁决 / 已知取舍

1. **文案**：损管腔那句已按船长指示重写（26 字 · 句式对齐三号重写后的甲壳板）；
   甲壳板那句由三号那批（`b930a063`）重写并已合入 ⇒ **两件现在都只写"是什么/有什么用"**，与引擎一致。
2. **范围封口**：现持有人恰好两件；日后若再有"无消耗自愈"件要不要同口径，**等船长点名**（字段注释与用例都写了这句）。
3. 归档时同步：词典「船体维修装置（战后消耗口径）」条补该例外（两件）· roadmap 一条 ·
   设计稿 `hull-repair-module-20260909.md` 就 09-16 那条加例外注。
