# 续接卡 · 全流程模拟工具（2026-09-21 · 目标轮 1/20）

> 用途：本会话上下文有限，**随时把状态固化在这里**，压缩/中断后照它继续。
> 权威状态以代码与 `docs/design/playthrough-sim-20260921.md` 为准，本卡是"下一步做什么"的索引。

## 一、当前已提交（d2/workspace）

- `ea546c1f` 工具 renovate 第一批（目标制 + 虫洞模块 + 九处缺陷）
- `a105ac11` 第二批（自造舰船路 + 三处虫洞策略修正）
- 工作文档：`docs/design/playthrough-sim-20260921.md`（含实测读数与卡点分析）
- **未合入 main**（船长 2026-09-21 明确选"先不合，等主树干净"）

## 二、目标（本轮要达成的三件事）

① 完成所有悬赏（**目标文案写 27 张**；数据核实 = `ctx.anomalies` 42 张里 **23 张可见 / 19 张隐藏**，
   "27" = 23 张悬赏 ＋ 4 张隐藏的遭遇卡 `enc-pirate-1..4`；遭遇卡不走 `completedBounties`）
   ⇒ **工具判据取 23 张可见卡**（正确口径）
② 虫洞六枚里程碑：层深 2/3/4/5 ＋ 击破守卫 2/4
③ 赚到 10 亿 ISK

**现状（第七轮末 · 三条里 2 条已达成）**：① **19/23**　② ✅ **六枚全拿**（层深 5 · 守卫 17，第 16.02 天）　③ ✅（第 15.50 天）

## 三、剩余卡点（第七轮查清）

悬赏最后 4 张（35 天档末态，报告已逐张列出）：

| 卡 | 威胁 | 声望门槛 | 当前预估胜率 |
|---|---|---|---|
| 穹顶守卫 `ano-vault-sentinel` | 96 | 13 | **0%** |
| 坟场守墓者 `ano-gravekeeper` | 88 | 12 | **0%** |
| 虚海守望者 `ano-voidedge-warden` | 88 | 12 | **0%** |
| 蜃影导航劫持令 `ano-mirage-hijackers` | 48 | 5 | 2% |

⇒ 前 19 张能拿下；后 3 张是**终局档**、第 4 张预估仅 2% ⇒ **纯战力问题，不是策略/政策问题**。
声望已到 **39**（远超 T4 现货门槛 20）⇒ 下一轮第一件事：**查"声望 39 为何没买 T4"**
（很可能是 `ensureWhFleet` 的选型条件把它挡掉了：它只取 `rarity === 'rare'`，
而 T4 玄武级是 `exotic`）—— 那是唯一还能在战力上再上一档的路。

```
（第七轮之前记录的旧环状分析 —— 其中"声望上不去"这一环**已被打通**：
 第七轮修掉"声望 ≥13 就停止打悬赏"的盲区后，声望从 15 涨到 39、首胜从 10 涨到 19。
 现在只剩最后一环：终局档 3 张 + 蜃影 1 张需要 T4 级战力。）
⇒ 声望 39 ≥ T4 现货门槛 20 ⇒ **下一步查 `ensureWhFleet` 为何没买 T4**
```

## 四、**当前状态（2026-09-21 本轮终）**

| 目标 | 现状 |
|---|---|
| ① 悬赏 23 张可见卡（= 目标文案"27 张"减 4 张隐藏遭遇卡，**已核实数据：42 张里 23 可见 / 19 隐藏**） | 10/23 |
| ② 虫洞六枚 | **3/6**（层深 2/5 · 守卫 **52**/4） |
| ③ 10 亿 ISK | ✅ 第 12.34 天 |

### 本轮关键突破（编队终于能满员活着打）

| 读数 | 本轮前 | 本轮后 |
|---|---|---|
| 进洞门真跑后的在场 | **1/1 艘** | **4/4 艘** |
| 第 2 层到达残血 | 6% | **49%** |
| 守卫账本 | 14 | **52** |
| 中低槽 | `高5/5 中1/2 低1/4`（空 3/6） | 逐槽铺满（`fillRack`） |
| 无人机 | **0 架** | 每舰 3~8 架（`ensureDroneLoads`） |

四处修复：
1. **`autoFitGear` 每族只装一件** ⇒ 中低槽大量空着（实测四舰 `炮 20/44 槽`）⇒ 改 `fillRack` 铺满。
2. **无人机从来没装上船** ⇒ 新写 `ensureDroneLoads()`（走引擎 `refillDroneLoadTo`）。
   ⚠ 无人机**不需要无人机甲板就能放飞**（`combat.ts:1398` 判据只是 `bayLimit > 0 && cpuLeft > 0`）。
3. **`WH_MAX_TRIES` 40 → 120**：实测 30 天档打满 40 次、后半程整段不进洞。
4. 新增 `whFleetDigest()` 诊断（逐舰高/中/低槽 + 无人机 + **CPU 已用/上限**）。

### 已排除的假设（别再走一遍）

- ❌ **CPU 不是瓶颈**：实测 `CPU 60/444`、`114/488`，四舰合计 294/1868 ⇒ 空槽与 CPU 无关。
- ❌ **换武器不是重点**：按"伤害/秒/槽"算，动能炮塔 MK3（1.745）＞激光 MK3（1.05）＞导弹 MK3（0.915），
  **现在装的已经是最高 DPS 那条线**（僵尸级炮塔 MK3 = `mod-turret-kin-3`）。
- ❌ **声望没有别的来源**：全仓只有 `expedition.ts:565` 一处加声望、且只在 `firstBlood` ⇒
  环状卡点成立（战力 ⇒ 硬悬赏 ⇒ 声望 ⇒ T4 ⇒ 战力）。

### 现在的精确卡点（下一步照这个做）

**层深 2 → 3 上不去，缺的是"过第 2 层的余量"**（第六批已把第 2 层到达从 40% 抬到 **61%**）：

- 清掉第 2 层守卫约吃 **36%** 血 ⇒ `61% − 36% ≈ 25% < 撤退线 45%`
  ⇒ 下一拍 `wantExtract`（<45%）命中 ⇒ **撤离，仍然拿不到第 3 层**。
- 也就是**还差约 20 个百分点**。三个可用方向（按性价比）：
  ① **抬升过第 2 层的血量**：确认 `fillRack` 铺的是高档件、把 `repairWhFleet` 的节流从"每天一次"
     放宽到"每次进洞前一次"（现在 0% 甲的船要等到第二天才修，中间那些趟只能靠选船闸挡掉）；
  ② **降低单场战斗的掉血**：更多无人机 / 更高档护盾装甲 / 更高 DPS；
  ③ **改撤退口径**：撤退线是 45%（`wormholeHpFrac() < 0.45`），与进洞门同源。
     若允许"残血也下潜一层、下去立刻撤"（层深入账 + 撤离都不需要打赢），
     则只要**清掉本层守卫时还没死**就够了 —— 这是**最低成本**的一条，但要把
     "下潜后立刻撤"做成明确分支（现在是"清守卫 → 下潜 → 下一拍撤"，中间那拍会被 <45% 截住）。

**下一步（按优先级）**：
1. **【最高杠杆，未做】放宽撤退线 / 让"清完守卫就必下潜"优先于血量检查**（上面第 ③ 条）：
   把 `wantExtract` 的血量判断**排到"`bossCleared === depth` 且下一层里程碑未入账"之后**
   —— 那时下潜一层是**净赚**（层深 +1、且下一拍就撤），没有理由因为残血放弃。
2. **`repairWhFleet` 节流放宽**：改成"每趟进洞前一次"（用 `whStats.entries` 变化做键，而不是按天）。
3. **确认 `fillRack` 铺的是高档件**（`mod-shield-ext-3` / `mod-armor-plate-3`），不是 MK1。
4. **无人机选型复核**：当前按"单发伤害降序"（三档每 m³ 伤害都是 0.6，够用）。
4. **跑 45~60 天档**复看层深能否推到 3+（每趟 +1 层深，四趟收齐四枚层深里程碑）。
5. 三条目标全达成后：按 §8 落文档，并**问船长可否合入 main**（船长本轮明确"先不合，等主树干净"）。

**已提交（d2/workspace）**：`ea546c1f` → `a105ac11` → `75ad77b5` → `c7043b1d` → `6ea46774`
→ `2193e156` → `86bf4865` → `40e795d0` → `ca4e51be`（+ 续接卡本文件）。

**当前稳定读数（12 天档 · `a0a4e4d0`）**：现金 **8.08 亿** · 声望 15 · 层深 **2** · 守卫 3 · 星系 20/20。
（25 天档对照：进洞 120 次 · 层深 2 · 守卫 57 · 阵亡 0 · 首胜 10/23 · 10 亿 ✅ 第 12.55 天。）

**第四轮（本轮）的结论**：又试了四处，**全部退化并已回退**，只留"刻度定论"这一处：
- **船体维修装置**（`mod-hullrep-2`，战斗中每 5s 修甲/结构各 18）+ 修理组件补货：
  **单元面看是对的**（健康编队清一层只掉 6% · 到场 4/4 艘），但整体掉到进洞 **85** 次 / 守卫 **35**
  （对照不装时 **120** 次 / **57**）—— 疑与"买模块+买组件"挤占现金流有关，**未查清，先不启用**。
  ⚠ 这是**下一轮最值得单独隔离验证**的一件事（它机制上确实对症：层深卡点就是连续多场战斗的累计掉血）。
- 残船准入闸阈值 **0.6** ⇒ 进洞掉到 35 次 / 守卫 21（太严；**0.25** 是实测最优，现口径）。
- 维修 `force` 绕过节流 ⇒ 退化。
- "按层记净掉血"读数 ⇒ 与护盾重置口径冲突（`wormholeHpFrac` 含护盾、每场重置）⇒ 该读数**不可用**，已回退。

**⚠ 刻度定论（本轮唯一确定的结论，写死在代码注释里）**：
`armorPct` / `durability` 取值 **0~1，`1` = 满**（原始值实测 `甲1.00/结1.00`、`甲0.00/结0.05`；
引擎 `repairCostIsk` 亦按 `1 - armorPct` 算缺失量）。**日志里不要做换算** ——
我先前乘 100 又误读，白花一整轮。

**第五轮（本轮）的结论：两个"隔离实验"也都退化，已全部回退**（基线仍是 `a0a4e4d0`）：

| 实验（一次只改一处） | 机制面预期 | 12 天档实测 |
|---|---|---|
| **只装船体维修装置**（`mod-hullrep-2`，不动组件采购） | 少掉血 | 守卫 **4**（好转，基线 3）但现金 **534M**（基线 808M，**−34%**）⇒ 两个目标反向，不采纳 |
| **弹药选 MK2**（`ammoPref` + 备货 2000） | 单发 **+33%**（纯增益） | 现金掉到 **4.5 万**、层深 0 ⇒ **前期经济被打崩**，未查清（疑与买弹挤占早期现金有关） |

⇒ 两个方向都证明：**不带门槛的"多花钱换战力"会把「10 亿」那条目标拖垮**（三条目标要同时达成）。

**✅ 第五轮找到的解法（已验证有效，两批提交）**：给这类支出加**现金门槛**
`SPEND_FOR_POWER_ISK = 8 亿`（`tools/playthrough-sim.ts`），**只在钱充裕之后才开始换战力** ——
理由是本模拟经济**前紧后松**（第 12 天 8 亿、第 60 天 286 亿）。

| 读数（25 天档同 seed） | 第五轮基线 | +MK2 弹药（`18d7c305`） | +维修装置（`fe61d234`） | +修主控闸/撤退线（`3c5ce9cd`） |
|---|---|---|---|---|
| 击破守卫 | 57 | 104 | 109 | **114** |
| 撤离收口 | 56 | 103 | 108 | **113** |
| 10 亿达成 | 12.55d | 12.50d | 12.48d | **11.84d** |
| 现金 | — | 82.1 亿 | 85.0 亿 | **89.9 亿** |
| 层深 | 2 | 2 | 2 | **2（仍未动）** |

⇒ **守卫翻倍、经济与 10 亿目标都更好**。第六轮又修掉一个真 bug：

- **主控绕过了准入闸**（`wormholeFleetPick` 把主控**无条件置首**，而只有 `rows` 过闸）
  ⇒ 实测进洞读数是 `甲0.00/结0.26 …`、`甲0.00/结0.43 …` —— **甲被打到 0 的主控照样带队进洞**。
  修法：主控与僚舰同一把尺（甲/结构 ≥ 0.6）。修后进洞读数全部是 `甲1.00/结1.00`。
- 撤退线 0.45 → **0.2**（层深记在下潜那一刻 ⇒ "清完守卫还活着就下潜"是净赚 +1 层深）。

**✅ 层深卡点已解决（第六轮查清、第十三批修掉）—— 层深 2 → 4，六枚里程碑 ✅ 由 3 枚增到 5 枚**

机制（已核引擎）：
1. **层末守卫每趟都会重生**（`bossCleared?: number` 是 **run 级**字段，`wormholeStartRun` 每趟新建）
   ⇒ "重进再清"不产生额外层深，层深只能靠**同一趟里连清 N−1 个守卫**再 `wormholeDescend`。
2. 每清一层守卫掉约 **36~39%** 血 ⇒ 越深越吃紧。
3. ⚠ **真正的自锁是这一句**（`doWormhole` ③c，第十三批已删）：
   `if (whDepthBanked.has(run.depth + 1)) 撤离` —— 层深 N+1 只要被写进 `whDepthBanked`
   （**哪怕是别趟写的**），之后每一次"清完第 N 层守卫"都会被直接撤掉、永不再尝试下潜
   ⇒ 实测 25 天档层深账本**永远停在 2**。
   删掉是安全的：层深里程碑走 `peakFirst`（只记峰值、只升不降）⇒ **再往下闯对成就毫无损失**，
   最坏只是丢一趟船（4 艘约 5000 万，而第 25 天有 90 亿）。现口径 = **"能下就下"**。

**⚠ 新的卡点：层深 4 → 5（"深渊彼岸"）**
- 40 天档读数：尝试 **260** 次 · 深入 **199** 次 · 守卫 **199** 个 · 阵亡 0 · 现金 **108.1 亿**
  · 10 亿第 12.28 天 —— 但**层深稳定停在 4**。
- **不是次数不够**（已把 `WH_MAX_TRIES` 120 → 600，趟数翻倍而层深没动）⇒ 是**第 4 层过不去**：
  能连清到第 4 层，但没人能连清第 4 层守卫。
- ⇒ 下一轮应聚焦"**第 4 层的战斗本身**"：读第 4 层守卫的敌人构成/威胁值，
  与 4×T3 满配的实际输出对比，判断是**配平墙**（该如实上报）还是**配装/编队还能再优化**。
  这已经是纯战力问题，参数与政策层面能动的都动过了。

**下一轮怎么做（纪律，已升级）**：
1. **一次只改一处**，每处跑 **12 天档（快）与 25/40 天档（准）对照**，退化立刻回退。
2. **要花钱换战力，一律走 `SPEND_FOR_POWER_ISK` 闸**（已验证；不带闸必退化，两例为证）。
3. 层深 5 是最后的 1 枚；先量第 4 层的敌我强度对比，再决定是优化还是如实上报平衡项。
4. 参数与配装都已试遍；战力档位仍受**声望**锁（T4 现货要 20、T4 蓝图要 25，实测 60 天只到 15），
  而声望只由悬赏首胜发放 ⇒ 要打通这个环得先打下剩下 13 张威胁 58~96 的硬悬赏，
  而那又要求战力 —— 现实路径是**用一次性蓝图自造 T3 把 4 舰编队做厚**
  （`ensureWhShipBuild` 已实现，但端到端尚未实测到：编队一直 4/4 所以没触发）。

## 五、不要重犯的坑（都踩过）

- ⚠⚠ **`armorPct` / `durability` 的取值是 0~1，`1` = 满**（不是 0~100！第四轮我用原始值实测确认：
  `甲1.00/结1.00`、`甲0.00/结0.05`；引擎 `repairCostIsk` 亦按 `1 - armorPct` 算缺失量）。
  ⚠ **日志里不要做换算** —— 我先前把日志写成 `Math.round(f.armorPct * 100)`，
  又把"×100 后是 26"读成"存的是 26"，据此按 0~100 去调准入闸（`>= 0.25` 本来是对的），白花一整轮。
- ⚠ **这个模拟的经济是紧平衡的**：任何"多花钱换战力"的改动都会拖垮「10 亿」那条目标
  （第五轮两例：维修装置 ⇒ 现金 −34%；MK2 弹药 ⇒ 现金掉到 4.5 万）。**三条目标要同时达成**，
  所以改策略后**必须同时看现金与层深两个读数**，只看层深会被误导。
- ⚠ **"残船准入"阈值调过三轮，每轮读数都要留档、别凭感觉调**：
  不筛 ⇒ 混进 `甲0%` 废船、拉低整队读数；筛 25 ⇒ 编队凑不满、**整天不进洞**
  （进洞 0 次 · 现金 5 万 · 首胜 6/23）；筛 1 ⇒ 折中可用（现口径）。
- ⚠ **维修节流也是三轮**：不节流 ⇒ 每拍都修、整段时间卡死（进洞 0 次 · 现金 46k）；
  一天一次 ⇒ 修不过来；**一天 48 次**是现在的口径。
- ⚠⚠ **一次只改一处、每处都跑 12 天档对照**：第三轮我把"残船闸 + 维修预算 + 门缓存"三处一起改完
  才发现模拟整体退化（现金 3 万、首胜 6/23），只能 `git stash` 回到上一个好提交逐个排查
  —— 三处齐改根本看不出是哪一处坏的。
- `buildEvalState` **只复制一艘船** ⇒ 整队真跑必须用我自己写的 `buildFleetEvalState`。
- 层深里程碑记在**下潜那一刻** ⇒ 政策必须是"清守卫 → **先下潜** → 再撤"，反过来会自锁。
- `mark()` 按**文本**去重 ⇒ 每拍调用且文本含变动数字的日志会刷屏（自己按天节流）。
- 报告 `milestones.slice(0, 400)` 上限会截掉后期读数（曾因此误判"模拟卡死"）。
- 一次性蓝图**不能 `learnBlueprint`**（引擎明确报错）⇒ 只能直接 `startManufacturing`。
- 损失读数用 `state.wormhole.lastFleetLost` 增量（模拟当天买替补，名单/艘数对比都会漏报）。
- ⚠ **一处未查清**：16.22d 那趟"4 艘在场 → 1 艘在场"而两个损失口径都报 0；未查清、不当结论。
