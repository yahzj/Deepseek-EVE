# 工作文档 · 势力图鉴（手册独立图鉴页签）（2026-09-26 · 三号）

> **状态：进行中** —— 四步闸门已走完（集中提问 → 设计总结 → **船长「按你推荐来」＝确认** → 落码）；
> 闸门全绿、验收档已出，**待船长真机验收**；归档按 §8（验收 + 合入 main 后当批做：
> 关键内容并入 `docs/roadmap.md` → **删本件** → 同时删掉 `hostile-factions-handbook-20260925.md`（挂起备忘，
> 内容已全部接手）与 `faction-codex-20260926.md`（本日先落的那节，已被本批取代）→ 重跑 `docs:index`）。
> **船长原话（照抄）**：「**敌族图鉴单独列出吧，放在蓝图图鉴下方，叫'势力图鉴'，并且敌族图鉴可以查看玩家
> 遭遇过的敌人介绍（和之前悬赏卡鼠标悬停查看的相同）。玩家能在敌族图鉴里查看该势力的专属装备和舰船。**」
> ＋ 集中提问四答（① 未遇敌人＝**「？？？」占位、遇过解锁** ② 专属内容＝**按"遇到过该势力"解锁** ③ 与航行须知那节＝**搬过来、删掉那节** ④ 形态＝**网格/列表六势力 → 点开详情三块**）
> ＋ 「**按你推荐来**」（＝ H 族明写「暂无专属舰船」· 图纸列出并标注类别 · 编号 325~391 不动）。

## 一、落地

| 项 | 形态 |
| --- | --- |
| 入口 | 手册左栏 **「蓝图图鉴」下方**新增 **「势力图鉴」**（key `factions`），与既有五档图鉴同一套：网格/列表切换、搜索、详情二级窗口 |
| 落地页 | **六张势力卡**：势力名 ＋ 副行「已遭遇 N/M」；**未解锁** ⇒ 卡名与敌人行显示「**？？？**」，详情里两块改成「遭遇该势力的敌舰后解锁」 |
| 详情三块 | **① 势力简介**：五段（基调/外观/战斗风格/招牌手段/出没之处，复用本日那批文案）· **② 敌人**：逐舰级一行（舰种 · 战术 · 武器系 · 主副伤 · 舰载机 · 特殊装置 · 精英档，**与悬赏卡悬停同一份内容**）· **③ 专属装备与舰船**：专属件（名＋短效果）· 专属舰（名＋舰级）· 「图纸」一栏 |
| 搜索 | 势力名 / 已遭遇的舰级名 / 专属件名（别页同款行为） |
| 航行须知 | **删掉**本日先加的那一节；文案 id（`ui.Handbook.325~391`）**改名不动、继续用**（编号即顺序：325 节名 ＋ 每族 11 格） |

## 二、解锁判据（**零新增存档字段**）

- **遭遇记录本就有**：`state.foeShipSeen`（开战那一刻 `combat.noteFoeShipsSeen` 写入，洞内洞外同一出口；
  已有随档往返与清洗器，通讯触发器也在读它）。
- **族解锁** = 该族任一舰级遇过（船长裁定②：遇过任一舰 ⇒ 该族**简介＋敌人＋专属**三块一并打开）。
- **舰级解锁** = 该 id 在 `foeShipSeen` 里。
- 老档没有该字段 ⇒ **未遇态**（零迁移，符合"新图鉴从零解锁"）。

## 三、数据与单一来源

- **新增登记表** `packages/data/src/factionCodex.ts`（图鉴的唯一依据）：
  收录顺序 A/C/D/E/G/H · 每族的**虫洞专属件（`mod-wh-*`）/ 专属舰（`sh-wh-*`）/ 图纸（`bp-*`·`sbp-*`）** ·
  值构造 `buildFactionCards()`（喂 `FOE_SHIPS` ＋ `state.foeShipSeen` ⇒ 六张卡）。
- **敌人**不重列：`data/foe-ships.ts` 的 `FOE_SHIPS`（30 条，每条带 `family`）**现算**；
  舰名与简报走**存档卡片**（已过 l10n 覆盖层 ⇒ 当前语言）＋ `foeBrief` 的结构化出口；
  未遇舰级只借 `FOE_SHIPS` 的 id/family（**不读它的中文名**，界面显示占位）。
- **新增契约**（`content:check` 的「**势力专属登记契约**」）：`mod-wh-*`（28）· `sh-wh-*`（15）·
  `exclusive` 无人机（4）**必须恰好落在一族**；未登记 / 重复登记**报红**。
  ✅ 本批实测：A 18 / C 18 / D 18 / E 18 / G 19 / H 3 件，**无重复、无遗漏**。
  ⚠ 契约**上线当天就抓到真缺口**：4 架专属无人机（鱿蜂 / 巢卫攻坚 / 构件哨戒 / 墨潮重袭）原先没登记 ⇒ 已补。
- **复用而非新写**：`ui/foeBrief.ts` 新增只读出口 `foeBriefLinesOfShip()`（悬停行为**一字未改**）；
  资料三块用既有 `moduleShortEffect` / `shipTierText` / `tr(ui.Handbook.*)`。
- **补一枚族徽**：H 墨潮帮此前**没有族徽**（`fam-*` 只有 A/C/D/E/G）⇒ 新增 `fam-h` ＋ 逐主题 token
  `--wui-tone-fam-h`（7 处：深空 / 亮白 / contrast / warm×2 / night / cb），对比度由 `ui:token-check` 守着。

## 四、验证读数

| 闸门 | 读数 |
| --- | --- |
| `typecheck` | 四包 **0 错** |
| `test`（core） | **229 文件 / 2509 用例全绿** |
| `content:check` | ✅ 通过（含新契约：6 族 96 件**全覆盖、无重复**） |
| `l10n:check` | ✅ 通过（新增 16 条 `ui.codex.*` ＋ 1 条 `ui.itemSubs.041` 全被引用） |
| `ui:rot-check` | ✅ 通过（含 TDZ 体检 —— 本批**上移过一段代码**，专门复跑过） |
| `ui:theme-check` | ✅ 通过（7 套主题 token 齐备、**对比度达标**） |
| 桌面 build / 网页 build | ✅ / ✅ |

**验收档**：`npx tsx tools/make-test-save.ts faction` ⇒ `docs/test-saves/test-save-faction-20260926-121938.json`
（已把六族敌舰全标"已遭遇"，共 29 条；`docs/test-saves/README.md` 有同名条目）。

## 五、已知取舍

1. **未解锁的族仍占位**（卡片显示「？？？＋ 已遭遇 0/N」）——按船长裁定①；**收录范围**里没有 B/F，
   所以图鉴里看不到这两族（它们也没专属件池）。
2. **H 族无专属舰**：按裁定甲在该栏明写「暂无专属舰船」；它的专属**装备** 3 件（含专属无人机）照列。
3. **图纸**：装备图纸（`bp-wh-*` / `bp-lair-*`）与舰船图纸（`sbp-wh-*`）**按裁定甲**列在第三块末尾并标「图纸」；
   因为数量多，这一栏**只给名称**（不展开效果）——若希望也能看效果，说一声我加。
4. **不在本批**：不改敌舰数值/掉落/解锁条件/存档结构；不给图鉴加"族群关系图""时间线"这类新段落。

_维护：本件按 §8 归档；关键内容并入 `docs/roadmap.md` 一条精简条目后删除本文件。_
