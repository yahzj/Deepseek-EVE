# D/E 族数值批：D 族远端 0.2 · E 族构成改爆炸 60/动能 40（2026-09-19 · 二号）

> **状态：进行中**（已落码并合入 main，等船长实测/复核）
>
> **船长原话（照抄）**：「将D族单独调整，远端改为0.2。并且奥罗残骸段的命中率降低为0.75。所有E族的默认伤害比改为爆炸60%，动能40%，导弹残段依旧是100%爆炸。近盲倍率改为0.3。射程下限改为3000米。并且将D族的虫洞卡片列出，我要进行修改」
>
> **两处范围追问后的裁定**：①「近盲 0.3」= **E 族四条统一**（实际只动导弹残段：1 → 0.3）；②「射程下限 3000m」= **只给导弹残段**（E 族另三条保持 100）；③ 抬下限后敌方**期望交距顺其自然**（公式自动跟随，不钉 `desireRangeM`）。

## 一、改动清单（逐项落点）

| # | 改动 | 落点 | 备注 |
|---|---|---|---|
| 1 | **D 族远端衰减 0.5 → 0.2** | `foe-ships.ts` 四条舰级（幽灵舰 / 守墓长舰 / 静滞卫舰 / 守墓王座舰） | 卡面无覆写 ⇒ **7 张 D 卡**（洞外 4 + 洞内 3）自动跟随；**窝点专属件「守墓者长炮」（玩家侧 0.6）不动** |
| 2 | **奥罗残骸段命中 0.95 → 0.75** | `foe-auro-hulk` | 与巨构残段/核心舱段（0.65）同侧，族内不再有"最准"的一档 |
| 3 | **E 族默认构成 → 爆炸 60 % / 动能 40 %** | 舰级三条（巨构残段 / 奥罗残骸段 / 核心舱段）**＋ 卡面 `dmgMix` 5 张**（泰坦残骸勘探 / 奥罗武装残骸群 / 巨构核心勘探令 / 巨构残响 / 巨构压境） | **主系由动能变爆炸**；卡面 = 主体构成（体检「构成契约」） |
| 4 | **导弹残段依旧是 100 % 爆炸** | `wh-titan-missile` 卡面 + 舰级 `dmgMix { explosive: 10 }` | 复核确认，未动 |
| 5 | **E 族近盲统一 0.3** | `foe-missile-hulk`：`blindDmgMul` 1 → **0.3** | 「导弹无视近盲」这条特性**作废**；`falloff: 1`（命中不随距离衰减）保留 |
| 6 | **射程下限 3000 m（只给导弹残段）** | `foe-missile-hulk`：`rangeMinM` 100 → **3000** | 期望交距随之 8,295 → **9,600 m**（= 3,000 + 0.55 × 12,000，船长选顺其自然） |
| 7 | **E 族窝点派生卡不再走 5:5 特例** | `lairs.ts`：`LAIR_SUB_DMG_SHARE_BY_FAMILY` **清空**（表保留）；`FOE_SUB_DMG.E` 序改 `['kinetic','explosive','plasma']` | 主系变爆炸后，副系须取**动能**（旧序会落到等离子）；派生窝点现与主题卡**逐值一致**（爆炸 6 : 动能 4） |

**契约同步**：`content-check`「构成契约」E 族 5:5 特例分支**删除**（E 三条舰级登记进 `FOE_SHIP_MIX_AUTHORITY_IDS`）；**白名单分支补上"派生窝点仍按全局 6:4"校验**（原来在 `continue` 前会漏掉这条覆盖）；D 族静滞卫舰折减用例改为**读舰级现值**（不再写死 0.5）；`mixed-damage.test.ts` 的 E 族用例改写为 60/40（含"导弹残段依旧纯爆炸"与"D 族窝点仍 6:4"两条对照）。

## 二、校准读数（`battle:calibrate --std` · A0~A3 × 全卡 × 5 播种）

**169 格中 36 格有变化，全部落在 D 族与 E 族卡上**（其余族一格未动 ⇒ 改动隔离干净）。摘录：

| 格 | 变化 |
|---|---|
| `A0 幽灵舰信号`（D） | 残血 70.6 % → **82.4 %** |
| `A3 守墓巡哨`（D·洞内） | 胜率 80 % → **100 %** · 残血 39.3 % → **61.6 %** |
| `A2 静滞哨链`（D·洞内） | 胜率 0 % → **100 %** · 残血 0 % → **24.6 %** |
| `A0 穹顶守卫`（D） | 时长 9s → 14s · 终局交距 9,772 → 8,642 m |
| `A0 奥罗武装残骸群`（E） | 胜率 80 % → **100 %** · 残血 32.5 % → 40 % |
| `A3 巨构残响`（E·洞内） | 胜率 20 % → **60 %** · 残血 7.2 % → 21.3 % |
| `A0 导弹残响`（E·洞内） | 残血 1.1 % → **8.9 %** · 交距 6,626 → 6,694 m |

方向上 D 族整族**明显变软**（远端砍到两成，长程对轰局全面好转），E 族因「命中下调 + 主系变爆炸（玩家层位克制与抗性分布跟着变）」有升有降——需要船长按族格判断是否到位。

## 三、待办 / 待裁决

- **船长复核上表读数**：D 族这次是"远端 0.5 → 0.2"的直给（远端威力只剩两成），若觉得太软可回调 0.25/0.3——一句话我就复算。
- **D 族三张虫洞卡**（`wh-grave-watch` 守墓巡哨 / `wh-grave-sentry` 静滞哨链 / `wh-grave-throne` 陵墓王庭）的明细已按船长要求列出（编成/条目覆写/射程带/构成/战术/挂载件），等新数值。
- 验收后归档：结论并入 `docs/glossary.md`（E 族族格构成、导弹残段"无视近盲"作废、D 族远端 0.2）+ `docs/roadmap.md` 一条，删本工作文档，重跑 `docs:index`。
