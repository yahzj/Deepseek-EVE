# B 族「武装拾荒者」族特色 · 设定定案（2026-09-11 · 已确认 · 代码待落码）

> **状态**：**设定已定案**（船长裁决齐备，共 9 + 4 条）· **代码/数据落码未执行**——
> 船长 2026-09-11 指示：「你只进行设定和文档补充，不对 D2 工作区内容进行实际修改」。
> **本稿是 B 族的权威设定记录**；落码清单见 §七、验收口径见 §八。
>
> **素材来源**：二号交接卡（D2 工作区 `docs/design/handoff-faction-b-20260911.md`）＋ 二号 B 族残稿（D2 未提交的
> `foe-faction-b-scavenger-20260911.md`，仅供取证，本稿为三号重写的权威版）＋ 三号对**本工作区**
> （`Deepseek-EVE-verify` @ `d6eac01`）现状的逐字段实测核对（§五 表内「现状」列为实测值）。

---

## 一、船长裁决 · 九条（族格层）

| # | 裁决原话 | 落地含义 |
|---|---|---|
| 1 | 「**统一为武装拾荒者，移除靶机**」 | 族短名保持「**拾荒**」（`FOE_FAMILY_LABEL.B`）、族全名「**武装拾荒者**」；全仓清理"靶机"字样（卡面文案 / 美术注释 / 族谱稿 / 词典 / 测试档说明 / 公告待审稿），历史留档类引述除外（见 §六） |
| 2 | 「**确认为新手过渡种族**」 | 定位写死：**威胁 6~20 专用**，不延长到中段；舰种**只登记 T1 护卫舰 / T2 驱逐舰**，不配 T3 及以上（契约守卫） |
| 3 | 「**演习场讨伐令改为演习场驱逐令**，驱逐到演习场拾荒的拾荒者」 | `ano-training` 卡名改「**演习场驱逐令**」＋描述改写为"协会例行清场"语义（玩家可见文案，须符合设定） |
| 4 | 「**战术性格统一为 orbit**」 | 三张卡全 orbit（现 brawl ×2 要改）；两个舰级默认战术 = orbit；契约「族→战术契约」登记 **B = `['orbit']`** |
| 5 | 「**乱射不动**」 | 保持占港 `hitRate 0.55`（另两张 0.85）。**乱射是单卡特征、不是族级签名** ⇒ 写在该卡的**编成条目**上，舰级一律 0.85 |
| 6 | 「**B 族速度按 0.8 走**」 | 两个舰级 `speedRatio` 一律 **0.80**（相对本档舰种基准） |
| 7 | 「**舰种按照你推荐的来**」 | 拾荒武装艇 **T1 护卫舰**（服务 6 / 10）、拾荒火力舰 **T2 驱逐舰**（服务 16） |
| 8 | 「**没有窝点，排除出赏金范围**」 | 删两卡的 `lairCore`（`lairLevel` 若有）⇒ B 族不再参与赏金任务派发、不参与「每族至少一张 3 级」的窝点级别契约（**候选 19 → 17 张 / 5 族 → 4 族**） |
| 9 | 「**完成后我们先回到 A**」 | B 族落码后**不开新批次**，先回 A 族收口验收 |

**族别通则（船长在 A 族批次一并定，B 族同用）**：
- **血型不做族级约束**（「海盗设定鱼龙混杂，那么就什么血型都有」）⇒ 血型**逐卡自定**；
- **射程与战术仅"弱相关"、不实时强绑定**——射程是独立旋钮。

---

## 二、本次补充裁决 · 四条（2026-09-11，三号问、船长答）

| # | 事项 | 船长裁决 | 含义 |
|---|---|---|---|
| 10 | **P0：orbit 下教学卡双方打不到** | 「**单独给 B 族添加期望射程修正**」 | **不动全局期望交距算法**（A 族 6 张的距离行为保持零漂移），只为 **B 族**提供自己的"想站的远近"⇒ 设计规格见 §四 |
| 11 | **B 族速度取值** | 「**0.80（272 / 236）——按交接卡**」 | 以交接卡为准；二号残稿的 `0.90 / 0.92`（306 / 271）**作废** |
| 12 | **d2 在途 B 族原型处置** | 「**复制到你的工作区**」 | d2 在途的 B 族原型（2 个舰级 + 3 卡迁移 + 2 处契约）作为**取数来源**，**不在 d2 上继续改**（见 §七 取数口径） |
| 13 | **B 族要不要"硬度"** | 「**不要：三张都保持新手向**」 | 三张卡维持新手难度（T6 / T10 / T16），教学后第一道坎交给 A 族；本批不做额外加压 |

---

## 三、B 族族格（定案）

| 维度 | 定案 | 依据 |
|---|---|---|
| 族名 | 短名「**拾荒**」· 全名「**武装拾荒者**」 | 裁决 1 |
| 定位 | **新手过渡族**（威胁 6~20），非中段强度族 | 裁决 2 |
| 舰种 | **T1 护卫舰 / T2 驱逐舰**（不得 ≥ T3） | 裁决 7 |
| 战术 | **统一 orbit** | 裁决 4 |
| 速度 | **偏慢**：`speedRatio 0.80`（比本档基准慢两成） | 裁决 6 / 11 |
| 火力签名 | **动能 8 : 2**（教学卡保持纯系，见下）；**乱射**（命中 0.55）为**单卡**特征 | 裁决 5 |
| 血型 | **逐卡自定**（不做族级约束） | 族别通则 |
| 窝点 | **无**：不参与赏金任务派发，无专属件 | 裁决 8 |
| 专属件 | **已是空表**：`core/lairs.ts` `FOE_LAIR_GEAR.B = []`，窝点词表标注「已停用 · 留档」⇒ **无需移除任何件** | 三号核对 |
| 与美术 | 拾荒者沿用低威胁方块化船体（**去掉"靶机"命名与注释**），黄黑警告斜纹保留 | 裁决 1 |

**⚠ 一处与交接卡表述的更正（实测）**：交接卡 §二 把三张卡的火力签名都写作「动能 8 : 2」，
但**教学卡 `ano-training` 实测无 `dmgMix`（纯系）**——这与既有「敌方混伤契约：教学卡保持纯系」一致，
且二号原型已按此把**舰级基准设为纯动能**、另两张卡覆写 8:2。本研究以**纯系教学卡**为准。

---

## 四、B 族专属期望射程修正（设计规格 · 取值待标定）

### 4.1 问题（P0，实测证据）

`foeDesiredRange()` 给敌人算"想站的远近距离"用的是**旧路径的全局战术射程表**
（`TACTIC_RANGE[战术] × tacticDesireFactor[战术]`），**完全不看舰级自己的射程带**。
orbit 口径下敌人想站 **~2.7~3.5 km**，而：

- 拾荒武装艇**自己的射程带只有 1~2200 m**；
- 新手**基础舰炮只有 2500 m**（这是教学卡的设计受众）。

⇒ **双方都够不着、距离锁死在开战距离**，逐秒轨迹显示**双方全程 0 次开火、600 秒打满判负**：

| 卡 | orbit（裁决 4 之后） | brawl（改判前） |
|---|---|---|
| 演习场**驱逐**令 T6（**裸船零技能**） | **0% / 600s（打满上限判负）** | 100% / 23s / 残血 100% |
| 新港商路护航令 T10（裸船零技能） | **0% / 600s** | 100% / 72s / 残血 99% |
| 占港武装通缉 T16（裸船零技能） | 100% / 463s / 残血 91% | 100% / 398s / 残血 79% |

### 4.2 规格（建议实现，字段名待定）

- **只在 B 族生效**：`foeDesiredRange()` 的优先级取
  **编成条目覆写 > 舰级覆写 > 旧全局表**（默认路径不变 ⇒ **A 族零漂移**，无需重跑 A 族 6 张的距离行为）。
- 载体二选一（落码时定）：**舰级字段**（如 `FoeShipDef.desireRangeM`）或**编成条目字段**
  （如 `FoeShipSlot.desireRangeM`）；前者表达"这个舰种的习惯交战距离"，后者表达"这张卡的特殊打法"。
- **取值待标定**（候选 1000 / 1300 / 1600 m）——约束两条：
  ① 必须落在**拾荒舰自己的射程带内**（1~2200 m），否则等于让敌人"想站在自己打不到的地方"；
  ② 必须落在**新手基础舰炮 2500 m 内**，否则教学卡依旧打不赢。
- **契约建议（新增）**：① 新字段的取值必须落在**该舰级射程带内**；
  ② **B 族三张卡必须显式声明该修正**（防日后回落成"双方锁死"）。

### 4.3 为什么不用另两条路（留档）

- 「把教学卡/新港改回 brawl」= **不落地裁决 4**（族统一 orbit），且 B 族与其余卡口径不一致；
- 「给这两张卡覆写更宽射程」= 治标：敌人有理由站远，但**玩家仍然打不到**，锁死不解决。

---

## 五、两个舰级与三张卡（定案值）

### 5.1 舰级（`packages/data/src/foe-ships.ts`，照 A 族四舰级写法）

| 舰级 id | 舰级名 | family | 舰种档 | 基准速度 | `speedRatio` | **实速** | 默认战术 | 服务卡 |
|---|---|---|---|---|---|---|---|---|
| `foe-scav-skiff` | **拾荒武装艇** | B | **1 护卫舰** | 340 | **0.80** | **272** | orbit | 演习场驱逐令(6)、新港商路护航令(10) |
| `foe-scav-armed` | **拾荒火力舰** | B | **2 驱逐舰** | 295 | **0.80** | **236** | orbit | 占港武装通缉(16) |

基准取值口径（与 A 族试点同款）= **迁移前"该舰级所服务的最低威胁卡"的现状建档值**：
拾荒武装艇 ← 演习场驱逐令（T6）；拾荒火力舰 ← 占港武装通缉（T16）。
同档其余卡用**精确倍率**复现原值（新港护航：`hpMul 75/22`、`dmgMul 23/14`）。

### 5.2 三张卡（现状 = 本工作区实测 · 目标 = 落码后）

| 卡 | 威胁 | 战术（现状 → 目标） | 总血 | 命中 | 射程带 | 构成 | 窝点字段（现状 → 目标） | 编成（目标） |
|---|---|---|---|---|---|---|---|---|
| 演习场**驱逐**令<br>`ano-training` | 6 | brawl → **orbit** | 22 | 85% | 1~2200 | **纯动能**（教学卡不写混伤） | 无 `lairCore` ✓（**补 `foeFamily: 'B'`**——本工作区现为缺失） | 拾荒武装艇 ×1 |
| 新港商路护航令<br>`ano-harbor-escort` | 10 | brawl → **orbit** | 75 | 85% | 1~2200 | 动能 8 : 2 | **`lairCore: '新港拾荒团'` → 删** | 拾荒武装艇 ×1（`hpMul 75/22` · `dmgMul 23/14` · `split: armor` · `dmgMix 8:2`） |
| 占港武装通缉<br>`ano-abandoned-platform` | 16 | orbit ✓（不变） | 292 | **55%（乱射）** | 366~4815 | 动能 8 : 2 | **`lairCore: '占港拾荒团'` → 删** | 拾荒火力舰 ×1（条目覆写 `hitRate 0.55`） |

**速度**（有意改动，唯一允许漂移的数值项）：322 → **272**、337 → **272**、286 → **236**。

---

## 六、"靶机"清零口径

**清零范围** = 描述**现状 / 现行资产 / 玩家可见文本 / 开发工具说明**的位置：
`ano-training` 卡面 description、`apps/desktop/src/renderer/src/ui/shipArt.tsx` 与 `shipMounts.ts` 的 B 族注释、
`foe-faction-identity-20260910.md` 的 B 族段、`faction-tactic-20260910.md` 族谱表、`ship-battle-art/*` 资产与挂点表、
`test-saves/README`、`make-test-save` 说明、公告待审稿。

**有意保留** = **历史记录里作为"当时旧名 / 被修缺陷"的引述**（`docs/roadmap.md` 历史条目、
`foe-ship-config-pilot-20260911.md` / `foe-ship-config-table-20260911.md` 的取证行、`docs/review/A1-bounty-review.md` 的当时快照）——改写它们等于篡改历史。

**不属于本次范围的"靶"**（不同词义）：`脱靶`（命中率语义）、`近防炮选靶`（无人机目标选取）、
`playthrough-sim` 的「战力基准靶」（合成基准敌，非 B 族）。

---

## 七、落码清单（**下一批执行 · 本次未做**）

> 本次（2026-09-11）只出设定与文档；以下条目**全部待落码**。

1. **取数**：二号把在途改动提交后，三号在 verify 执行 `git merge d2/workspace`（或按文件复制 B 族相关 hunk）。⚠ **两边并行的冲突面**：`anomalies.ts` / `content-check.ts` / `foe-ships.ts` / `docs/roadmap.md`；⚠ **已实测到一处跨分支冲突**——d2 的 `foeDmgMul` 整体退休 vs main 侧两张测试仍在用它（`tests/drone-loss.test.ts` / `tests/wave-battle.test.ts`），合并后 2 例失败，落码批需一并改为**条目级 `dmgMul`**。
2. **两个舰级**：按 §5.1 写入 `foe-ships.ts`（速度 0.80、默认 orbit、舰种 T1/T2）。
3. **三张卡迁移**：加 `ships` 编成；补 `ano-training` 的 `foeFamily: 'B'`；除速度/战术/卡名外**逐字保持原值**（先做零变化基线，再改数值，两步分开验收）。
4. **窝点退出**：删两卡 `lairCore`（`lairLevel` 若有）；`lairGear` 已为空表，无需移除。
5. **B 族期望射程修正**（§四）：新增字段 + `foeDesiredRange()` 优先级 + B 族三卡声明 + 取值标定。
6. **契约**：① 族→战术契约登记 **B = `['orbit']`**；② 舰级契约追加 **B 族舰级不得 ≥ T3**；③ **窝点级别契约重写**（19 张 / 5 族 → **17 张 / 4 族**，依据「B 族没有窝点、排除出赏金范围」）；④ 新增期望射程修正契约（§4.2）；⑤ 复核「增援未启用契约」（B 族不带 `enterAt`，应照旧通过）。
7. **全仓核实"按族硬编码"假设**（`lairs.ts`、`sideTasks.ts`、`tools/bounty-econ.ts`、词典、设计稿、公告待审稿）——**若发现族数减少会破坏机制（三档称呼按族分配、专属件按族给、候选不足报错等）⇒ 停下报告，不自行决定**。
8. **文案**：卡名与描述改写（裁决 3）＋ §六 靶机清零，交付时**给出搜索命令与结果**。
9. **文档**：`foe-faction-identity-20260910.md` B 段标记已定案（本批已改）、`docs/glossary.md` 登记词条（本批已加）、`docs/roadmap.md` 追加批次记录。
10. **收尾**：`_` 前缀探针收尾删除（0 个残留）。

---

## 八、验收口径（落码批缺一不可）

- `npm run typecheck`（core / data / ui / desktop 四 workspace）＋ `npm run test -w @whale/core` ＋ `npm run content:check`（**粘贴「舰级契约」「族→战术契约」「窝点级别契约」三处输出原文**）＋ `npm run build -w @whale/desktop`。
- **零变化基线**：改造前后各导一次**逐单位建档值**（血三层 / 单发 / 命中 / 射程带 / 衰减 / 近盲 / 装填 / 形态 / 主系 / 规格 / 回避 / 敏捷 …）逐字比对；**允许的差异只有四项**：舰级名、速度（0.80）、战术（orbit）、教学卡名。
- **多装配复核**（重点）：速度 0.80 的真实影响必须用**近战装配 + 不带推进器 + 低技能**复核，**不能只看参考行**；**必须逐单位输出"敌方各开火次数"**（头目 / 杂鱼分列）——「残血 100%」可能只是"敌人够不着"，不是"敌人软"。
  **标准复核配置集（采用二号 2026-09-11 复核件 `foe-baseline-audit-20260911.md` 的六组，全部 `sh-mako` 灰鲭鲨 4 高槽）**：
  ①参考行（4×动能MK2 + MK2推进器 + 支援，期望距离 **3220m**）· ②近战短射程（4×轻型炮 MK1，**无推进器**，1735m）· ③参考行去推进器（3220m）· ④低技能档（战斗技能 L3，3220m）· ⑤贴脸高射速（4×转管炮，无推进器，1890m）· ⑥远程风筝（4×导弹架 MK2，6330m）。
  **A 族同批复核已证明这条纪律的必要性**：参考行 3220m **恰好落在低威胁敌人射程带之外**（海盗快艇 1~1883 / 拾荒武装艇 1~2200 都够不着）⇒ 六张 A 族卡里**五张的敌人 0 开火**、"残血 100%" 是**距离的产物**；换成近战组后同一张卡立刻变成"敌人狂开火、玩家 0 输出"（灰霾/赤潮/蜃影）。
  ⇒ **B 族三张卡必须六组全跑**，尤其 ②⑤（这两组才会暴露"贴脸够不着"与"机动差被追死"）。
- **实测对照**：三张卡改造前后的**胜率 / 时长 / 残血**，并点名 brawl→orbit 的影响。
- 靶机清零的搜索命令与结果。

---

## 九、风险与取证留档

| # | 事项 | 状态 |
|---|---|---|
| 1 | **前置阻塞**：B 族所需基础设施（舰种表 / 舰级路径 / 敌速接入 / 多舰补偿 / 契约）全部在二号**未合入 main** 的提交里 | 已确认；取数口径见 §七.1 |
| 2 | **d2 在途仍有 B 族原型**（与交接卡「二号不再动 B 族」表述不符）——2 个舰级 + 3 卡迁移 + 2 处契约均在工作树未提交 | 已核实；按裁决 12 **只取数、不在 d2 改** |
| 3 | **d2 与 main 已分叉**（main 领先 17 / d2 领先 9），合并必经 `anomalies.ts` / `content-check.ts` / `foe-ships.ts` / `roadmap.md` 四处 | 已核实；落码批按"谁先谁后"约定 |
| 4 | **`foeDmgMul` 退休与 main 侧测试冲突**（实测 2 例失败） | 已取证；落码批一并修（见 §七.1） |
| 5 | 「窝点级别契约」重写后的人数与族数变化 | 待落码批复核（§七.7 的停手条件） |

---

_维护：本稿是 B 族（武装拾荒者）的权威设定记录；落码时同步 `foe-faction-identity-20260910.md`、`docs/glossary.md` 与 `content:check` 的族→战术 / 舰级 / 窝点级别三处契约。_
_定稿：三号（`H:\大鲸鱼\Deepseek-EVE-verify` @ `d6eac01`）· 2026-09-11 · 设定已确认、代码待落码。_
