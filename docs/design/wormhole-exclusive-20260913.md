# 终局玩法「虫洞」· **掉落物设计**（虫洞专属装备 / 舰船 / 一次性图纸）· 设计稿

> **状态：设计稿 · 船长逐条裁定已并入 §0**（2026-09-13 二号起草；**待船长最终点头后落码**）
> **本稿范围（船长 2026-09-13 强调两次）**：「**你只做掉落物设计，具体哪边掉一号负责**」
> ⇒ 本稿只定 **掉什么、多少、按族按深度怎么分稀有度**；**"从拾取点 / 战斗残骸 / 事件 / 层末守卫哪边出"不写、不碰**，由一号接线。
> **施工期铁律**照旧：虫洞对玩家不可见（入口走调试开关、数据走 `unreleased` 闸门、可见文案不得出现「虫洞」）。

---

## §0 船长裁定链（本稿的每一处设计都能追到一句话）

| # | 船长原话（照抄） | 落到本稿哪一条 |
|---|---|---|
| 1 | 「一号在推进虫洞战斗系统，你这边负责设计虫洞产出物。」 | 本稿 = 产出物设计 |
| 2 | 「虫洞装备跟着虫洞种族类型走…虫洞只有 A/C/D/E/G 几个种族划分」 | §2 五族签名；§3 装备按族成池 |
| 3 | 「**除了装备，还有舰船**，但是**舰船只能依靠蓝图制造，不会出成品**」 | §4 舰船；§5 只出**一次性图纸** |
| 4 | 「**不掉永久图纸**」 | §5 全部 `singleUse`；不进逆向研究表；**蓝图碎片也不进洞** |
| 5 | 「**我的避开是指，同族以内，不要有重复定位的**」 | §3.2 同族占用矩阵（逐件核过） |
| 6 | 「**护卫，驱逐，巡洋都可以有**，你干脆都安排设计吧」 | §4 **每族 3 艘**（护卫・驱逐・巡洋），共 15 艘 |
| 7 | 「**所有舰船不掉成品，只掉一次性图纸**」 | §5.1 |
| 8 | 「你只做掉落物设计，**具体哪边掉一号负责**」 | §6 只给权重与深度门槛，不指定来源 |
| 9 | 「**不符合的问题清档**」 | §1 边界：**本设计不含任何存档迁移**；不符合同族不重复规则的条目**已清掉重排**（§3.3 共 8 处） |

---

## §1 范围与边界

**做**：虫洞专属装备 30 件（每族 6）· 虫洞专属舰船 15 艘（每族 3）· 一次性图纸 45 张 · 按族按深度的掉落权重表 · 施工期闸门与契约。

**不做（明确划线）**：
1. **不指定来源**——拾取点 / 战斗残骸 / 事件点 / 层末守卫怎么挂、概率在哪一步触发，**一号负责**；
2. **不做存档迁移**——本设计新增的是"内容"（装备/船/图纸/权重表），不改存档结构；接线若需扩背包（装备与图纸进包）由一号按其批次决定，**遇到不符合的旧档按船长口径清档，不写迁移路径**；
3. 不掉 ISK、不给声望、不改星图残骸场（沿用设计稿「收口对等清单」的 4 条"有意不做"）；
4. 洞里不直接掉虚空晶（Q13/Q14/Q15 照旧）；**不掉永久图纸，也不掉蓝图碎片**（碎片能逆向出永久配方，同属这一禁忌）；
5. 不碰既有 27 艘船、不碰 `sh-megalodon`（T4 巨齿鲨级）/ `sh-dunkleosteus`（T5 邓氏鱼级）两个模子——它们继续挂着不动（船长本轮改口径：专属舰船**从护卫到巡洋为止**，不做 T4/T5）。

---

## §2 五族签名（承接既有身份，不另起一套）

| 族 | 既有身份（出处） | 洞内选靶模式 | 掉落池签名 |
|---|---|---|---|
| **A** 海盗 | 劫掠支队 | `noncombat`（专抢非战斗船） | 掠夺：**大货舱 + 快 + 中近程** |
| **C** 异形 | 巢群游猎 | `smallest`（打最小的） | 生体：**结构/装甲高、护盾薄、近程高伤、自修** |
| **D** 守墓 | 守墓巡哨 | `top-output`（BOSS 一律 `largest`） | 重装：**三系抗、必中远程、最慢** |
| **E** 巨构 | 泰坦巨构 | 随机 | 平台：**机库 + CPU（无人机承载）** |
| **G** 亡军 | 鱿烬亡军封锁 | 随机 | 幽灵：**低信号、快、远锁定、无人机射程** |

> ⚠ **E 族依赖**：洞内敌卡现有四张（A/C/D/G，`packages/data/src/wormholeFoes.ts`），**E 族还没有敌卡** ⇒ E 族掉落池暂时**无来源**，需一号补一张 E 族洞内敌卡（其文件）。本稿照常设计 E 族内容。

---

## §3 虫洞专属装备：每族 6 件（共 30 件）

### 3.1 逐族清单（槽位 · 字段 · 量级锚）

> **量级锚口径**：族专属一类件的既有规律 ≈ **MK3 同字段件的 2 倍量级**（实证：守墓者长炮 ×10.35 vs 攻坚炮台 MK3 ×5.13 ≈ 2×）。下表"锚 → 建议"即 MK3 现值 → 本件建议值。
> **每件都带一个明确代价**（沿用既有设计语言）：高 CPU 占用，或一个副作用（失稳 / 减速 / 只对某系有效）。

**A 海盗**（窝点套已占：转管炮・导弹巢・货舱+甲容量）

| 件 | 名称 | 槽位 | 字段 | 锚 → 建议 | 定位与代价 |
|---|---|---|---|---|---|
| 1 | 裂罅熔流炮 | 激光炮 | 武器（等离子） | 激光炮 MK3（10.5 km・命中 100%・4.0 s・×4.2） | 中远程持续熔蚀（A 族首个激光件）；代价＝装填偏慢。**弹型合规**：A 族卡池含等离子卡（混合族） |
| 2 | 赃物机库 | 无人机甲板 | `droneBayBonusM3` | 无人机甲板 MK3 +70 → **+85**（审核下调：原 +95 与窝点 E 族「深层机库」同值） | 海盗自带护航机库；代价＝CPU 高 |
| 3 | 掠袭加力器 | 推进器 | `speedBonusPct` | 推进器 MK3 +100% → **+120%** | 打了就跑；**代价＝开火失稳 `hitPenalty` 0.05** |
| 4 | 赃物折射涂层 | 支援件 | `evasionGapPct` | 陀螺 MK3 0.2 → **0.28** | 保命保货；代价＝不提供任何抗性 |
| 5 | 赃物扫描阵 | 支援件 | `hitBonusPct` | 索敌 MK3 0.16 → **0.24** | 压住速射炮散布 |
| 6 | 掠夺者护盾笼 | 护盾 | `shieldHpBonus` | 护盾扩展器 MK3 0.6 → **0.9** | A 族唯一防护件；代价＝不回充速度（纯池） |

**C 异形**（窝点套已占：甲自修・结构抗+结构自修・近程必中喷吐）

| 件 | 名称 | 槽位 | 字段 | 锚 → 建议 | 定位与代价 |
|---|---|---|---|---|---|
| 1 | 生体棱镜束 | 激光炮 | 武器（等离子） | 激光炮 MK3（10.5 km・100%・4.0 s・×4.2）→ **8.6 km・必中・5.6 s・×7.6** | **审核修正**：原"几丁质穿刺炮（炮台·动能）"**越出 C 族弹型**（C 卡池 = 等离子 8 / 爆破 2，无动能）⇒ 改等离子；与本族窝点件（酸液喷吐器 2.8 km 近程必中）**射程带不同** |
| 2 | 活性甲壳层 | 装甲 | `armorHpBonus` | 装甲增厚板 MK3 +80% → **+160%** | 与窝点的"自修"、本套的"甲抗"三条不同维度 |
| 3 | 甲壳棱镜层 | 装甲 | `armorResistAdd` 三系 | 装甲镀层单系 0.25 → **三系 0.3** | 全游唯一的族专属**装甲抗性**件 |
| 4 | 生体脉搏加速器 | 支援件 | `reloadCutPct` | 射速计算机 MK3 0.12 → **0.18** | 配合近程高频 |
| 5 | 孢子导弹巢 | 导弹架 | 武器（爆破） | 导弹架 MK3（14.9 km・0.92・6.0 s・×6.04） | 中程覆盖；代价＝命中低、装填长 |
| 6 | 几丁质骨架层 | 装甲 | `hullHpBonus` | 巨构骨架 +60% → **+75%** | C 族结构容量（窝点给的是结构**抗性**） |

**D 守墓**（窝点套已占：三系盾抗・12 km 必中长炮・甲容量）

| 件 | 名称 | 槽位 | 字段 | 锚 → 建议 | 定位与代价 |
|---|---|---|---|---|---|
| 1 | 陵卫连装炮 | 炮台 | 武器（动能） | 攻坚炮台 MK3（7.35 km・0.78・2.94 s・×5.13）→ **5.4 km・0.86・1.6 s・×4.6** | **审核修正**：原"陵寝齐射巢（导弹＝爆破）"**越出 D 族弹型**（D 卡池 = 等离子 8 / 动能 2，无爆破）⇒ 改动能速射；与本族窝点件（守墓者长炮 12 km 必中慢炮）**射程带与节奏都不同** |
| 2 | 陵墓护盾芯 | 护盾 | `shieldHpBonus` | 护盾扩展器 MK3 0.6 → **0.9** | 与窝点的盾**抗性**不同维度；代价＝CPU 高 |
| 3 | 守墓者丧钟 | 锁定装置 | `lockDmgBonus` | 锁定阵列 MK3 0.2 → **0.3** | 集火 + 被锁目标受击加深（与"打最大的"同一语言） |
| 4 | 陵寝棱镜炮 | 激光炮 | 武器（等离子） | 激光炮 MK3（10.5 km・命中 100%・4.0 s・×4.2） | D 族第二把武器（窝点只给了动能）；代价＝最慢 |
| 5 | 守墓者速装填机 | 支援件 | `reloadCutPct` | 射速计算机 MK3 0.12 → **0.18** | 专补必中长炮 7.35 s 的短板 |
| 6 | 陵墓弹道铭文 | 支援件 | `damageTypeBonusPct` | 稳定器 MK3 单系 0.15 → **0.22** | 动能单系单发加成 |

**E 巨构**（窝点套已占：极慢重锤炮・机库扩容・甲+结构容量）

| 件 | 名称 | 槽位 | 字段 | 锚 → 建议 | 定位与代价 |
|---|---|---|---|---|---|
| 1 | 巨构损管阵列 | 装甲 | `hullResistAdd` 三系 | 生体损管腔 0.25 → **0.3** | E 族结构抗性（E 原来只吃伤害） |
| 2 | 巨构导控塔 | 战术导控 | `droneDmgBonus` | 窝点 G 族 0.45 → **0.5** | E 是机库族，导控补"打得疼" |
| 3 | 巨构协处理器 | 协处理器 | `cpuBonus` | 协处理器 MK3 +45 → **+90** | **全族唯一 CPU 件**（第 1 批那件已清掉，避免自撞） |
| 4 | 巨构近防阵列 | 炮台 | `antiDrone` | 近防炮三档 ×2 → **×2（同值）** | ⚠ 落码时按**船长既有硬规则**改回：防空武器**射程恒 2 500 m**、**防空值恒 ×2**（见 §3.4 偏差记录）；改由**单发 1.3 倍 + 射速更快**立身 |
| 5 | 巨构护盾矩阵 | 护盾 | `shieldResistAdd` 三系 | 陵墓护盾阵列 0.3 → **0.32** | E 族首个护盾件 |
| 6 | 巨构稳态器 | 支援件 | `damageTypeBonusPct` | 稳定器 MK3 单系 0.15 → **0.22** | 爆破单系（E 是爆破族） |

**G 亡军**（窝点套已占：专属无人机・无人机伤害・无人机射程）

| 件 | 名称 | 槽位 | 字段 | 锚 → 建议 | 定位与代价 |
|---|---|---|---|---|---|
| 1 | 亡军蜂巢坞 | 无人机甲板 | `droneBayBonusM3` | 深层机库 +95 → **+110** | G 族缺的正是**容量**（窝点只给了伤害与射程） |
| 2 | 亡军火控残响 | 支援件 | `hitBonusPct` | 索敌 MK3 0.16 → **0.24** | 炮台命中 |
| 3 | 幽灵弹道校正器 | 支援件 | `damageTypeBonusPct` | 稳定器 MK3 0.15 → **0.22** | 单系单发 |
| 4 | 残兵结构层 | 装甲 | `hullResistAdd` 三系 | 生体损管腔 0.25 → **0.28** | G 是残兵，结构向；G 族唯一防护件 |
| 5 | 亡军残炮 | 炮台 | 武器（爆破） | 攻坚炮台 MK3（7.35 km・0.78・2.94 s・×5.13） | G 族首个实体武器（捡来的残炮） |
| 6 | 幽灵推进器 | 推进器 | `speedBonusPct` | 推进器 MK3 +100% → **+115%** | 幽灵/侦查族的机动 |

### 3.2 同族占用矩阵（**逐件核过：同族以内不重复定位**）

| 族 | 窝点套（3） | 本套（6） | 冲突 |
|---|---|---|---|
| A | 转管炮(动能近程)・导弹巢(爆破中程)・货舱+甲容 | 激光・机库・速度・闪避・命中・盾容量 | **0** |
| C | 甲自修・结构抗+结构自修・激光近程必中 | 炮台・甲容量・甲抗・射速・导弹・结构容量 | **0** |
| D | 三系盾抗・动能必中长炮・甲容量 | 导弹・盾容量・锁定・激光・射速・动能单发 | **0** |
| E | 极慢重锤炮・机库・甲+结构容量 | 结构抗・无人机伤害・CPU・防空・盾抗・爆破单发 | **0** |
| G | 专属无人机・无人机伤害・无人机射程 | 机库・命中・单发・结构抗・炮台・速度 | **0** |

> 「同槽不同维度不算重复」是刻意的族套装结构：例如 C 族装甲槽三件（自修 / 容量 / 抗性）三条路线，字段各不相同。

### 3.3 已按"同族不重复"清掉重排的 8 处（过档）

| 族 | 原条目 | 问题 | 清掉后 |
|---|---|---|---|
| A | 虚空赃物舱 | 与窝点「赃物强化舱」同槽同字段 | 赃物机库 |
| A | 裂隙掠夺炮 | 与窝点「转管炮」同为近程炮 | 裂罅熔流炮 |
| C | 酸蚀喷吐器·洞生型 | 与窝点「酸液喷吐器」同槽同弹型 | 生体棱镜束（激光·等离子，射程带与窝点件错开） |
| D | 陵寝长炮·洞生型 | 与窝点「守墓者长炮」同槽同定位 | 陵卫连装炮（动能速射，与窝点件错开射程带与节奏） |
| D | 守墓相位盾 | 与窝点盾抗、与本套盾容量双重撞 | 陵墓弹道铭文 |
| E | 巨构残响炮 | 与窝点「巨构残骸炮」同槽同定位 | 巨构损管阵列 |
| E | 巨构协处理器（第 1 批那件） | 与本套 CPU 件**自撞** | 巨构导控塔（CPU 件全族只留一件） |
| G | 鱿蜂群导控 / 流亡中继桅（2 件） | 与窝点 G 两件同槽同字段 | 亡军残炮 / 幽灵推进器 |

### 3.4 落码偏差记录（**合同/既有硬规则把设计稿的 6 处改了**，2026-09-13）

| # | 设计稿 | 落码后 | 依据 |
|---|---|---|---|
| 1 | 巨构近防阵列：射程 2 800 m · 防空 ×3 | **2 500 m · ×2** | 《机群与防空契约》：船长 2026-09-11「近防炮射程按 2500m 算」+ 2026-09-12「那伤害倍率按 2 倍算」⇒ **防空值全游统一 ×2**，专属件也不破例；该件改以**单发 1.3 倍 + 射速 1.2 s**立身。⚠ 若要 ×3 请给新裁定，契约会先红 |
| 2 | 裂罅熔流炮 / 陵寝棱镜炮：近盲 600 / 1 000 m | **minRangeM = 0** | 激光炮契约：光束无近盲（`minRangeM === 0`、`hitRate === 1`） |
| 3 | 孢子导弹巢：falloff 0.6 | **falloff = 1** | 导弹架契约：追踪命中、命中不随距离衰减（说明里的"射程尽头"表述同步删掉）。⚠ 原 D 族那件导弹已按 **§3.5 审核**整件换成动能炮台 |
| 4 | 几丁质骨架层 / 巨构损管阵列 / 残兵结构层：只有结构族字段 | **各带一点甲族字段**（甲容量 +20% / +20% / +18%） | 装甲槽契约（V17.1 拆族）：装甲槽必须声明 `armorHpBonus` 或 `armorResistAdd` —— 与既有「巨构骨架 = 甲 0.3 + 结构 0.6」同款搭车 |
| 5 | 巨构协处理器说明写「协处理器 MK3 是 +45」 | 改成「制式协处理器只到它的一半」 | 《产物说明契约》：`处理器` 紧跟名字里的 "MK3" 会被解析成一个凭空数值 3（实测抓到） |
| 6 | 装备 30 件分 L1/L2 两批落 | **合并为一批落** | 船长 2026-09-13「开工，不过**装备就全部做进来**」 |

### 3.5 按族审核记录（2026-09-13 · 船长「进行审核」）

**审核依据（数据权威）**：洞内敌卡（`wormholeFoes.ts`）与悬赏卡的 `dmgMix` ——

| 族 | 卡池弹型（实测） | 既有窝点套弹型 | 结论 |
|---|---|---|---|
| **A** 海盗 | 动能 / 爆破 / **等离子**（混合族，6 张卡三分） | 动能 + 爆破 | 混合族 ⇒ 等离子件**合法** |
| **C** 异形 | **等离子 8** / 爆破 2 | 等离子 | 必须等离子/爆破 |
| **D** 守墓 | **等离子 8** / 动能 2 | 动能（副系） | 必须等离子/动能 |
| **E** 巨构 | 动能 5 / 爆破 5 | 爆破 | 动能/爆破均可 |
| **G** 亡军 | **动能 8** / 爆破 2 | 无人机件（无弹型） | 必须动能/爆破 |

**逐件核对结果（每族 6 件 × 5 族）**：

| 族 | 弹型合规 | 同族不重复（vs 窝点套 3 件） | 槽位分布 | 裁定 |
|---|---|---|---|---|
| A | ✓（等离子在 A 混合池内） | ✓ 0 冲突 | 高 2 / 中 4 / 低 0 | 保留；**机库 +95 → +85**（原与窝点 E 深层机库同值） |
| C | **✗ 修 1 件**（动能炮台越界） | ✓ 0 冲突 | 高 2 / 低 4 | 炮台→**生体棱镜束**（激光·等离子·8.6 km） |
| D | **✗ 修 1 件**（爆破导弹越界） | ✓ 0 冲突 | 高 3 / 中 1 / 低 2 | 导弹→**陵卫连装炮**（炮台·动能·5.4 km 速射） |
| E | ✓ | ✓ 0 冲突 | 高 2 / 中 1 / 低 3 | 保留 |
| G | ✓（动能主系 + 爆破副系，正好覆盖本族 8:2） | ✓ 0 冲突 | 高 2 / 中 2 / 低 2 | 保留 |

**量级锚复核（逐件对 MK3 真值，不是"约 2 倍"一句话）**：

| 字段 | MK3 真值 | 本套 | 倍数 |
|---|---|---|---|
| `speedBonusPct`（推进器） | +100% | +115% ~ +120% | 1.15~1.2× |
| `evasionGapPct`（陀螺） | 0.2 | 0.28 | 1.4× |
| `hitBonusPct`（索敌） | 0.16 | 0.24 | 1.5× |
| `reloadCutPct`（射速） | 0.12 | 0.18 | 1.5× |
| `damageTypeBonusPct`（稳定器·单系） | 0.15 | 0.22 | 1.47× |
| `shieldHpBonus`（盾扩展器） | 0.6 | 0.9 | 1.5× |
| `armorHpBonus`（装甲增厚板） | 0.8 | 1.6 | 2.0× |
| `droneBayBonusM3`（无人机甲板） | +70 | +85（A）/ +110（G） | 1.2× / 1.6× |
| `cpuBonus`（协处理器） | +45 | +90 | 2.0× |
| `lockDmgBonus`（锁定阵列） | 0.2 | 0.3 | 1.5× |
| 武器单发（炮台 / 激光 / 导弹） | ×5.13 / ×4.2 / ×6.04 | 本套四门 4.6~9.4 | 0.9~2.2×（快炮单发低、慢炮单发高） |
| `armorResistAdd` 三系 | 装甲镀层 MK3 = **单系 0.55** | 三系 0.3 | 单系 0.55×、**覆盖率 1.6×**（三系合算） |
| `hullResistAdd` 三系 | 生体损管腔 0.25 | 0.28 ~ 0.3 | 1.1~1.2× |

⇒ **口径修正**：本套不是"一律 2× MK3"，真实分布是 **1.2~2.0×**（抗性件按覆盖率算 ≈1.6×），
与窝点套同档；**没有一件超过 2×**（防数值膨胀）。上表取代 §3.1 里"≈2 倍量级"的粗口径。

---

### 3.6 A 族审核（船长 2026-09-13 逐件给定）与**待落机制**

**已落码**（本轮直接改进数据）：

| 原 | 改后 | 落码 |
|---|---|---|
| 裂罅熔流炮（激光·等离子·11.5 km·必中 1·×8.8·CPU 62） | **掠袭破片炮**（**炮台**·爆炸·7.3 km·命中 0.8·×7.04·CPU 48） | ✅ 弹种改爆炸 ⇒ 槽位随武器形态改炮台（激光槽契约要求等离子） |
| 赃物机库（+85 机库·CPU 46） | **掠袭机库**（+30 机库·**CPU 25**） | ✅ |
| 掠袭加力器（失稳 0.05·CPU 38） | 速度 +120% 不变；**失稳 0.35**（命中 ×0.65）·**CPU 18** | ✅ |
| 掠夺者护盾笼（盾容量 +90%） | **掠袭者护盾笼**（盾容量 **+80%**） | ✅ |
| 赃物折射涂层 / 赃物扫描阵 | 船长要求"改低槽装甲 + 全抗性 −15%"与"射程 −15%" | ⏳ **待落机制**（见下） |

**⏳ 待落机制（4 项：引擎当前没有对应字段 ⇒ 需新字段 + 战斗侧接入 + 界面呈现 + 契约调整）**：

**船长 2026-09-13 补充口径（两处语义已定死）**：
1. 「破片炮额外造成 50% 的动能伤害是**附加伤害，和弹种无关**，按照武器预设，**是消耗爆炸弹药**」
   ⇒ 主伤害仍是**爆炸弹**（吃爆炸弹药、走爆炸克制），**额外那一段固定是动能**（不受弹种影响）；
2. 「无人机攻击间隔 −8% 是**无人机出击周期**」⇒ 减的是无人机的出击/攻击节拍（`DroneSpec.reloadMs`），
   不是放飞间隔、也不是装填。

| # | 船长要求（哪一件） | 建议字段（语义按上） | 已核实的落点（file:line） |
|---|---|---|---|
| 1 | 掠袭破片炮「**额外造成 50% 的动能伤害**」 | `secondaryDamagePct?: number` + `secondaryDamageType?: DamageType`（缺省 kinetic） | `combat.ts`：`WeaponSpec`（:83）随 `createPlayerSpec` 携带 → 我方开火结算处**追加第二段** `applyDamage(..., 主段实收 × pct, 副系)`；**胜率预估器**（:4408-4436）同步加副段（否则"卡面混伤、预估按纯系算"会骗人）；克制倍率走各系各自的 `typeLayerMult` |
| 2 | 掠袭机库「**无人机出击周期 −8%**」（原话"无人机攻击间隔"） | `droneCycleCutPct?: number` | `combat.ts:771 droneReloadOf(def)`（→ `DroneSpec.reloadMs`，即出击节拍）乘 `(1−cut)`；聚合口径与 `reloadCutPct` 同款（加算、设上限）；无人机装置契约的"必须且只能给 bay/dmg 一个"扩为"bay/dmg/周期 三选一" |
| 3 | 掠袭折射涂层「改为**低槽装甲**，添加负面效果，**全抗性 −15**」 | `allResistPenaltyPct?: number`（负抗：三层抗性同减，下限 0） | `combat.ts:482 mergeResist` 之后统一扣减（:604/:606/:620 三处 base 合成处）；**装甲槽契约**（V17.1 要求"甲容量或甲抗"）需为"携带负抗件的装甲件"开例外；`evasionGapPct` 留在装甲槽属跨族 ⇒ 需登记进 `content-check` 的跨族白名单 |
| 4 | 掠袭者护盾笼「**射程削减 25%**」／赃物扫描阵「**射程削减 15%**」 | `rangeCutPct?: number`（多件**取最重一件**，与推进器 `hitPenalty` 同口径） | `createPlayerSpec` 里我方武器射程字段（gun/beam 两路）乘 `(1−cut)`；预估器同步（:4411 起的循环）；**多件取最重**避免叠成"射程归零" |

> ⚠ 这 4 项都落在 `packages/core/src/{combat,equipment,types}.ts`（**一号在管的战斗线**）
> ⇒ 二号**未擅自改战斗代码**，请船长定：① 由二号落这批机制；② 还是交一号落、二号只落数据。
> 数值（50% / 8% / −15% / −25%·−15%）已按船长给定值登记在此，落码时照抄。
> **落码时必带三件**（本仓规矩）：① 预估器同步（不许卡面与预估两套口径）；② 模块信息卡加对应一行；
> ③ 补用例 + 负向验证（抽掉字段 ⇒ 用例真红）。

### 3.7 审阅表（Excel 内容工作台，2026-09-13）

船长「现在这样修改太麻烦」⇒ 改用**仓库既有的内容工作台**（`tools/content-export.ts` 一套）：

- 产物：`content-csv/content-workbench.xlsx`（7 张 sheet；`modules` **134 行 × 48 列**、`ships` **42 行 × 42 列**）
- **新内容位置**：`modules` 表**最后 30 行** = 虫洞专属装备；`ships` 表**最后 15 行** = 虫洞专属舰船
  （表头已冻结 + 全表自动筛选 ⇒ 用 `id` 列筛 `-wh-` 即可只看新内容；**筛选不会让导入丢行，但不许删行**）
- **回写**：`npm run content:import modules content-csv/content-workbench.xlsx`（改完自动跑 content:check + typecheck + diff 摘要）
- **本轮补全的列**（此前审阅表看不见这些字段）：
  模块侧 9 类 —— 结构容量 / 结构抗性三系 / 锁定加深 / CPU 扩容 / 防空 / 备弹 / 机动代价 / 修复系四件 / 无人机中继射程 + 未上线闸门；
  舰船侧 5 项 —— 命中加成 / 船体武器族加成三系 / 船体无人机加成 / 未上线闸门

### 3.9 新机制**已落码**（船长 2026-09-13「你来实现新机制」）

**引擎新增 9 个模块字段**（`packages/core/src/types.ts`；缺省不写 ⇒ 既有装备零行为变化）：

| 字段 | 语义（船长口径） | 引擎落点 | 已挂装备 |
|---|---|---|---|
| `secondaryDamagePct` + `secondaryDamageType` | **附加伤害段**：主段结算后按**主段实收** ×比例再打一段**固定弹种**伤害，**与所耗弹药无关** | `WeaponSpec` 同步 → 我方开火结算处追加第二段 `applyDamage`；**胜率预估器同步** | 掠袭破片炮 0.5/动能 |
| `ammoPerShot` | **每次攻击耗弹发数** | 预载与实战扣弹统一走「门数 × 本值」 | 陵卫连装炮 2 |
| `rangeCutPct` | **全武器射程削减**（多件取最重） | `rangeOf()`：`射程 ×(1−cut)×(1+该系加成)`，下限 500 m | 护盾笼 0.25 / 扫描阵 0.15 |
| `rangeTypeBonusPct` | **按系武器射程加成**（按系加算） | 同上 | 校正器 动能 +0.22 |
| `damageBonusPct` | **通用单发加成**（只进炮台/光束） | 与按系稳定器同链乘入 `perShot` | 亡军火控 0.06 |
| `reloadPenaltyPct` | **装填惩罚**（× (1+值)；多件取最重） | `createPlayerSpec` 的 `reload` 计算 | 协处理器 0.12 |
| `allResistPenaltyPct` | **全层抗性削减**（三层同减、下限 0；多件取最重） | 抗性合成与调谐**之后**统一扣 | 折射涂层 0.15 |
| `droneCycleCutPct` | **无人机出击周期折减**（= 船长澄清的"攻击间隔"） | `droneReloadOf()` ×(1−Σ)，与整备学乘算 | 掠袭机库 0.08 |
| （跨槽）`speedBonusPct` | 速度加成**不再只认推进器槽** | 聚合改走 `allFittedModules` | 生体脉搏 +0.10 / 几丁质骨架 +0.05 |

**契约同步**（`tools/content-check.ts`）：① 装甲槽放开为"防护类任一项"（结构容量 / 结构抗 / 全层抗削减 / 机动加成，仍禁同件并给甲容量+甲抗）；② 护盾槽**取消"容量与抗性互斥"**；③ 跨族字段白名单加 9 项 + 登记 6 条虫洞件；④ 界面（`ui/shipInfo.tsx`）为 9 个新旋钮各加信息行（含跨族那几条）。

**用例**：`packages/core/tests/wormhole-gear-mechanics.test.ts`（10 条：九项旋钮各一条 + "缺省不受影响"一条）；测试床 `helpers.ts` 的 `moduleDef` 补上 10 个新字段透传。
**负向验证真红**：抽掉 `droneReloadOf` 里的周期折减 ⇒ ⑧ 如实报「expected 4000 to be 3680」；还原后 10/10 绿。

⚠ **附加伤害段：口径已定 + 实战已验**（船长 2026-09-13：「**伤害各自吃各自的制（克制）效果**」）

**落码口径**：副段基数 = **武器原伤害**（含锁定加深、**不含**主段已吃的克制）× `secondaryDamagePct`，
然后**两段各吃各自的层克制**（`typeLayerMult`）。⇒ 与**胜率预估器**一直用的算法一致（此前实战走的是
"主段实收"基数，两边不同源，本次一并校正）。

**实战 A/B 读数**（工具同款装配：长尾鲨 × 5 门掠袭破片炮 vs 真实敌卡 `wh-pirate-scout` 第 3 层 ·
强度 ×10；只把 `secondaryDamagePct` 在内存里置 0；探针用完即删）：

| 种子 | 关闭（主段·爆炸） | 打开（+副段·动能 50%） | 增量 |
|---|---|---|---|
| 11 | 开火 1／命中 1 · 我伤 **220** | 开火 1／命中 1 · 我伤 **550** | **+330（+150%）** |
| 22 | 开火 5／命中 4 · 我伤 **880** | 开火 5／命中 4 · 我伤 **2200** | **+1320（+150%）** |
| 33 | 开火 1／命中 1 · 我伤 **220** | 开火 1／命中 1 · 我伤 **550** | **+330（+150%）** |

**为什么是 +150% 而不是 +50%**——`typeLayerMult` 的克制表是三系两极（`combat.ts`）：
`动能 盾×1.5 / 甲×0.5`、`爆炸 盾×0.5 / 甲×1.5`、`能量 盾×1.25 / 其余×1`。
本例敌舰**先吃盾**：主段爆炸只拿 **×0.5**，副段动能拿 **×1.5** ⇒ 副段每点效率是主段的 **3 倍**
（0.5 × 1.5 ÷ 0.5 = 1.5 ⇒ 相对主段 +150%）。

⚠ **由此带来的数值波动面（须知）**：同一件炮的"额外 50%"，实收增量随目标层型在 **+17% ~ +150%** 之间摆：
- 打**盾厚**目标（先吃盾）：主段爆炸 ×0.5、副段动能 ×1.5 ⇒ **+150%**；
- 打**甲厚**目标（先吃甲）：主段爆炸 ×1.5、副段动能 ×0.5 ⇒ **+16.7%**。
⇒ 这是"各自吃各自克制"的必然结果（**不是 bug**）；若日后觉得波动太大，另一条口径是
"副段 = 主段**实收** ×50%，再吃副系克制"（实测该目标 +75%、摆幅收敛到 +25%~+75%），
改法只有 `combat.ts` 一处。**本批按船长裁定保留"各自吃各自"**。

**测试床注**：`wormhole-gear-mechanics.test.ts` 的 ① 仍只守"字段进条目 + 两段各吃各系克制"——
core 包**不能 import `@whale/data`**（会成环），故工具同款的真实战斗装配只能走 `tools/`（本次探针路径），
需要长期看住的话可把该探针转正成 `tools/wormhole-gear-audit.ts`（等船长定）。

### §3.10 层位克制表改判（2026-09-13 船长）——附全套读数

- **裁定原话**：「将克制系统进行修改，爆炸对护盾改为\*0.75，动能对装甲改为\*0.75」（同日追问：「有无测试过针对有不同抗性的敌人伤害」）；
- **落码**：`packages/core/src/combat.ts` 的 `typeLayerMult` —— 动能对甲 `0.5 → 0.75`、爆炸对盾 `0.5 → 0.75`（克制/劣化比 3:1 → **2:1**）。同源自动跟随：UI 速查 `layerMultText`、`tools/content-check.ts` 产物说明对账、`packages/data/src/items.ts` **四条玩家可见弹药说明**、`packages/core/tests/combat.test.ts` 两处钉值；
- **体检工具（新入库）**：`tools/counter-audit.ts` —— ① 表 + 三血型有效倍率（旧→新）② `applyDamage` 抗性矩阵 ③ **敌卡抗性现状全量扫描** ④ 真实战斗"同卡只改血型"对照 ⑤ 齐射数新旧对拍 ⑥ 颗粒度敏感度；
- **读数（同一次改动）**：

  | 口径 | 动能 | 爆炸 | 能量 |
  |---|---|---|---|
  | 对 balanced 血型有效倍率 | 1.0050 → 1.0875（**+8.2%**） | 0.9950 → 1.0800（**+8.5%**） | ±0 |
  | 对 armor 血型 | 0.8250 → 0.9625（**+16.7%**） | +4.3% | ±0 |
  | 对 shield 血型 | +5.6% | 0.8750 → 1.0000（**+14.3%**） | ±0 |

  真实战斗（动能装配 4×T3 打威胁 96 卡；**同卡只改三层配比**，血/伤各 ×4、开局交距 1500m、3 播种）：
  盾型 98.6s / 开火 157 次 · 甲型 105.3s / 174 次 · 均衡型 102.8s / 167 次（**3/3 全胜**）；
  齐射数新旧对拍（同一血池）：盾型 **179 → 157 轮** · 甲型 **205 → 170 轮** · 均衡型 **193 → 164 轮**（−12%~−17%）；
- **"针对不同抗性的敌人"实测结论（回答船长当日追问）**：**敌人侧没有抗性这条链路** ——
  ① 现网 **31 张敌卡 / 76 条敌舰单位**，`createFoeSpecs` 出来的 `resists` **全部为空**（敌舰卡的 `FoeShipDef` 根本没有抗性字段，建档处写死 `resists: {}`）；
  ② 战斗中 `BattleUnitRt` **无抗性字段**，我方打敌舰的两处 `applyDamage(rt.hp, {}, …)` 是**硬编码空抗性**；
  ③ 抗性只在两条链上生效：**我方挨打**（`spec.resists`）与**机群池**（`droneDefense` 的盾/甲/壳抗）。
  ⇒ 所以"不同抗性的敌人"目前打不出差异，能测的只有**血型（盾/甲/均衡）+ 层克制**；
  `applyDamage` 抗性矩阵证明函数本身吃抗性且**只作用于本层**（单发 1000：无抗 1000/375/0 · 盾动能抗 0.3 ⇒ 1000/38/0 · 甲动能抗 0.3 ⇒ 1000/263/0）；
- **⚠ 副作用 + 处置（船长 2026-09-13 裁定「① 最小调整夹具」· 已执行）**：颗粒度越粗越敏感 —— "每层 30 血 × 单发 30"的夹具里同一改动让破层 **4 发 → 3 发（−25%）**，两处既有用例因此翻盘（**都是"我方在该场被打沉"**）：
  ① `target-lock.test.ts`「集火弹道」——诊断：我方 **0 次开火**、敌方 **4 发**把我方打到 0/0/0 ⇒ **处置 = 夹具船每层血 30 → 90**；
  ② `b1.test.ts`「低安遭遇应战存活」——结算后 `durability = 0`，夹具用**开局沙猫级**（护盾 **18** 点）⇒ **处置 = 夹具在 `lowWorld()` 里注册抬硬的沙猫级（400/700/700）**，实测以 **5% 结构**活下来。
  **两处都只动测试夹具、内容数值一字未动**（被测行为 = 集火弹道 / 应战结算且船还在，均不变），复测 **30/30 全过**、全仓 **1201/1201**。
  **折中值实测**：逆克制改 0.6 ⇒ b1 过、target-lock 仍挂（它不是幅度问题而是尺寸问题）⇒ 折中不是解法。真实量级下同一改动是 +8.2%（balanced）。

---

## §4 虫洞专属舰船：**每族 3 艘（护卫・驱逐・巡洋），共 15 艘**

### §4.0 舰种子分类与数值重排（**2026-09-13 船长定 · 已落码**；下表**取代下方全部旧表**）

**船长口径（原话）**：「希望根据种族有几个大方向的强力增幅，其他方面甚至允许少量削弱。但是平均强度要高上10%，并且多出一个槽位（上中下选择其中一个）」＋「削弱幅度提高到30%」＋「并将现有的舰船名字进行调整。（比如炮艇）」＋子分类**进界面**；D 族巡洋舰**无子分类**；G 驱逐舰**改名**。

**口径落地**：Σ血（盾+甲+壳）= 官方同档同角色中位 × 族签名比；**15 艘平均 1.149（+14.9%）**；
CPU = max(官方中位 ×1.10, 现 CPU + 40)；**每艘 +1 槽位**（按子分类定槽）；抗性按角色补（武装舰 盾动能 0.5 / 重装舰 甲爆炸 0.5，C·D·E 另加第二层）；
**削弱落点** = 货舱 / 机动 / 回避 / 信号 / 分辨率 / 三层血占比（**上限 −30%**），总量仍达标。

| 舰船（新名） | id | 子分类 | 盾/甲/壳血 | CPU | 槽 高/中/低 | 速/机动/回避 | 信号/锁定/分辨率 | 货舱/机巢 | 比官方中位 |
|---|---|---|---|---|---|---|---|---|---|
| **掠袭电子舰** | `sh-wh-a-frigate` | 电子舰 | 130/60/55 | 165 | 3/**3**/1 | 350/0.675/0.18 | 44/**46500**/**990** | 1260/10 | 1.07 |
| **掠袭炮艇** | `sh-wh-a-destroyer` | 炮艇 | 175/130/130 | 250 | **5**/3/2 | 300/0.62/0.12 | 72/36000/456 | 3080/30 | 1.15 |
| **掠袭重型突击巡洋舰** | `sh-wh-a-cruiser` | 重型突击巡洋舰 | 340/205/230 | 390 | **6**/3/3 | 225/0.385/0.12 | 110/40000/525 | 3640/50 | 1.15 |
| **幼虫截击舰** | `sh-wh-c-frigate` | 截击舰 | 35/90/130 | 170 | 2/**3**/3 | **432**/**0.837**/0.10 | 71/26000/600 | 630/10 | 1.12 |
| **甲壳截击舰** | `sh-wh-c-destroyer` | 截击舰 | 30/150/220 | 215 | 2/**4**/4 | **331**/**0.675**/0.06 | 138/24000/460 | 1330/30 | 1.20 |
| **巢群重型突击巡洋舰** | `sh-wh-c-cruiser` | 重型突击巡洋舰 | 75/415/470 | 280 | 3/4/**5** | 166/0.294/0.04 | 204/25000/400 | 2940/50 | 1.20 |
| **哨戒电子舰** | `sh-wh-d-frigate` | 电子舰 | 105/75/75 | 165 | 2/**3**/3 | 300/0.495/0.07 | 70/**51000**/**840** | 770/10 | 1.12 |
| **陵卫指挥舰** | `sh-wh-d-destroyer` | 指挥舰 | 100/145/165 | 220 | 2/**4**/4 | 154/0.383/0.045 | 140/**46500**/**651** | 2700/**45** | 1.23 |
| **陵寝巡洋舰** | `sh-wh-d-cruiser` | **（无子分类）** | 180/365/455 | 300 | **3**/5/5 | 175/0.40/0.04 | 200/28000/350 | 9000/50 | 1.25 |
| **构件鱼雷舰** | `sh-wh-e-frigate` | 鱼雷舰 | 115/60/80 | 200 | **4**/2/2 | 315/0.70/0.105 | 68/32000/640 | 1000/30 | 1.12 |
| **机库无人机作战舰** | `sh-wh-e-destroyer` | 无人机作战舰 | 230/90/115 | 290 | 4/**4**/3 | 275/0.60/0.10 | 80/37000/540 | 1960/**105** | 1.15 |
| **巨构无人机作战舰** | `sh-wh-e-carrier` | 无人机作战舰 | 410/160/200 | 440 | 5/**5**/3 | 250/0.50/0.10 | 105/42000/500 | 2660/**145** | 1.14 |
| **幽影侦察舰** | `sh-wh-g-frigate` | 侦察舰 | 140/45/65 | 175 | 3/**4**/1 | **383**/0.82/**0.27** | **22**/**47500**/700 | 490/10 | 1.10 |
| **亡军后勤舰** | `sh-wh-g-destroyer` | 后勤舰 | 230/75/115 | 260 | 4/4/**3** | 310/0.68/0.147 | 58/44000/620 | **3480**/**45** | 1.11 |
| **亡军鱼雷舰** | `sh-wh-g-cruiser` | 鱼雷舰 | 345/185/225 | 410 | **5**/5/3 | 285/0.493/0.105 | 101/48000/580 | 3200/60 | 1.12 |

**九个子分类的强增 / 削弱 / 加槽位**（增强已按船长「增幅偏少」上调）：

| 子分类 | 强增 | 削弱（≤ −30%） | +1 槽 |
|---|---|---|---|
| 电子舰 | **命中 +0.10~0.12 · 回避 +20%~+55%**（真吃战斗公式）· 分辨率 +50%（**经济向**：悬赏冷却 ≈ −18%）· 锁定 +50%（**纯展示**） | 货舱 −30% · 结构血占比 −25% · 机动 −10% | 中 |
| 炮艇 | 族武 动能 +0.15 · 命中 +0.03 | 货舱 −30% · 护盾血占比 −20% · 分辨率 −20% | 高 |
| 重型突击巡洋舰 | 族武 +0.15 · 甲/壳血占比提高 | 机动 −30% · 货舱 −30% · 信号 +20% | 高（C 族为低——见下） |
| 截击舰 | 速度 +35% · 机动 +35% · 命中 +0.04 | 货舱 −30% · 护盾血占比 −30% · 信号 +15% | 中 |
| 指挥舰 | **机巢 +50% · 无人机伤害 +0.08 · 命中 +0.05**（真吃战斗公式）· 分辨率 +55%（经济向）· 锁定 +55%（纯展示） | 速度 −30% · 货舱 −25% · 回避 −10% | 中 |
| 鱼雷舰 | 族武 爆炸 +0.15 · 命中 +0.03 · 结构血占比提高 | 回避 −25% · 信号 +30% · 机动 −15% | 高 |
| 无人机作战舰 | 无人机伤害 +0.10 / +0.14 · 机巢 +50% · CPU +15% | 货舱 −30% · 命中 −0.02 · 护盾血占比 −15% | 中 |
| 侦察舰 | **回避 +35% · 命中 +0.06**（真吃战斗公式）· 信号 −35% · 锁定 +25%（纯展示，叙事用） | 货舱 −30% · 甲/壳血占比 −25% | 中 |
| 后勤舰 | 货舱 +45% · 机巢 +50% · 回避 +5% | 命中 −0.03 · 甲血占比 −20% | 低 |

> ⚠ **同日修正（船长指出「锁定/分辨率不是间接属性吗，并不直接影响战斗」）**——代码核实结论：
> **`lockRangeM` 锁定范围 = 纯展示**（全引擎零消费：仅 `types.ts:254` 声明、`shipInfo.tsx:388` 展示）；
> **`signatureM` 信号半径 = 纯展示**（`combat.ts:268` 注释明写"公式不消费"）；
> **`scanResMm` 扫描分辨率 ≠ 战斗属性**，只进 `expedition.ts:981` 的**悬赏重复冷却**（`10s × √(500/分辨率)`）。
> ⇒ 原先挂在"电子舰 / 指挥舰 / 侦察舰"上的锁定·分辨率·信号强增**是空的**，已改为**真吃战斗公式**的属性：
> **掠袭电子舰** 命中 0.15 → **0.25**、回避 0.18 → **0.216**；**哨戒电子舰** 命中 0.10 → **0.22**、回避 0.07 → **0.109**；
> **陵卫指挥舰** 命中 0.06 → **0.11**、新增**无人机伤害 +0.08**；**幽影侦察舰** 命中 0.14 → **0.20**（回避 +35% 原本就真）。
> 分辨率加成**保留**但改记为**经济向**；锁定/信号保留为**叙事/展示**项，不再计入"强增"。
> **另有 4 项经核实"真吃战斗"**：速度/机动（`combat.ts:395 combatSpeed`）、命中（`hitChance`）、机巢（可带架数）、CPU（装配）。
> **⏳ 待船长定**：要不要把**锁定距离 / 信号半径接进战斗**（新机制，例如"超出锁定距离不能开火""锁定耗时 = f(锁定, 分辨率, 信号)"）？
> 现状是这两项对战斗**零影响**，而装配界面会显示它们。

### §4.8 二轮调整与三条新机制（**2026-09-13 船长四条裁定 · 已落码**）

**① 重型突击巡洋舰（A/C）——"厚"的正式定位**

| 项目 | A 掠袭重型突击巡洋舰 | C 巢群重型突击巡洋舰 |
|---|---|---|
| 三层血 | 775（340/205/230）⇒ **855（375/225/255）＝×1.1** | 960（75/415/470）⇒ **1055（85/455/515）＝×1.1** |
| 抗性 | 护盾动能 0.5 保留；**其余 0 抗一律 → 0.25**（三层三系） | 甲爆炸 0.5、壳动能 0.25 保留；**其余 0 抗一律 → 0.25** |
| 族武加成 | **移除**（原 动能 +0.15） | **移除**（原 能量 +0.15） |

> 船长原话：「重突可以，不过要移除额外火力加成」＋「除去已有的抗性外，将其他 0 抗性提高到 25 抗。生命值按照 1.1 加成」。

**② D 组（陵墓）全体：特色由"血量厚"改为"高护盾比"**

| 舰船 | 三层血（改后） | 护盾占比 | 抗性 |
|---|---|---|---|
| 哨戒电子舰（D 护卫） | 195 → **260（170/35/55）** | **65%**（全批最高） | 盾三系 0.25 · 甲爆炸 0.5 · 壳三系 0.25 |
| 陵卫指挥舰（D 驱逐） | 410 → **385（230/75/80）** | **60%** | 同上 |
| 陵寝巡洋舰（D 巡洋） | **1000 → 920（510/155/255）** | **55%** | 同上 |

> 船长原话：「指的是 D 组全体」＋「D 组特色换成高护盾比」。⇒ **"最厚"头衔移交给 C 重突（1055）**；D 组靠**护盾占比 + 盾抗**立足。
>
> **同日修正（船长：「D 组现在不是重装种族了，改成高盾占比的，是否移除这个种族的削弱部分」）**——
> **种族级削弱（速度/机动）已移除**：陵卫指挥舰 速 154 → **220 m/s**、机动 0.383 → **0.45**（原按"D = 陵墓重装、坐镇后方"给的 −30%/−15%，身份改了 ⇒ 前提消失）。
> **保留的是子分类级/身份级代价**：货舱 −25%（指挥舰，与 A 族电子舰同口径"为设备/机巢让舱"）· **甲/壳血占比低（甲 19~20% / 壳 21~22%）＝ 高护盾比的天然短板**（盾一破就很脆）。
> **为什么这条重要**：编队接近速度 = 全队存活舰 `combatSpeed` 的**平均**（`combat.ts:3871`）⇒ 单舰的 −30% 速度会**让全队替它买单**（2 舰编队接近速度 −13%），把"全舰 +15% 伤害"的光环吃掉一部分；移除后该连带惩罚归零。

**③ 三条船体固有新机制（新字段 + 接入点 + 实测读数）**

| 舰船 | 效果 | 新字段 | 接入点 | 实测读数（临时探针，跑完即撤） |
|---|---|---|---|---|
| 侦察舰 / 电子舰（G 护卫 · A 护卫 · D 护卫，共 3 艘） | **虫洞扫码范围 +1 圈**（**编入队伍即生效、可叠加**） | `wormholeScanRadiusBonus: 1` | `wormholeEnter` 建档时喂 `wormholeMakeGrid(seed, depth, Σ编队加成)`；`wormholeScanBonusOf(ctx, shipIds)` 求和 | 3 艘编队 ⇒ **加成 3 · scanRadius 4**；单艘 ⇒ **1 · 2**（`WORMHOLE_SCAN_RADIUS_BASE = 1`；1 圈 7 格 → 2 圈 19 格） |
| 陵卫指挥舰（D 驱逐） | **全舰单发伤害 +15%**（编队光环，**多艘取最高、不叠加**） | `fleetDamageBonusPct: 0.15` | `startFleetBattleFor` 建完各舰规格后，对**全队每条武器**统一 ×1.15 | 规格值实测：主舰 `13 → 14.95`、`动能 129 → 148.35`、僚舰 `9 → 10.35` ⇒ **精确 ×1.15、全队生效** |
| 掠袭炮艇（A 驱逐） | **动能武器射程 +30%** | `weaponRangeBonusPct: { kinetic: 0.3 }` | `createPlayerSpec` 的 `rangeBonus` 合并（与模块 `rangeTypeBonusPct` **同链加算**） | 同型动能炮：炮艇 **7,462 m** vs 无加成对照舰 **5,740 m** ⇒ **×1.30** |

**⏳ 待接线（一号）**：`wormholeDescend(run, seed, scanBonus)` 新增了可选入参（默认 0 = 旧行为）——面板在**深入下一层**时把 `wormholeScanBonusOf(ctx, run.fleet)` 传进去，扫描加成才能在深层继续生效；`wormholeEnter` 侧已在引擎内接好。

**④ 2026-09-13 末轮：两艘巡洋舰的槽位点名 + 删除一条旧弱断言**

| 舰船 | 槽位（前 → 后） | CPU | 说明 |
|---|---|---|---|
| 陵寝巡洋舰（D 巡洋） | 4/5/5（提议）→ **4/4/4**（12 槽） | **300**（保持现值，不涨） | 船长「中槽和低槽各 −1」· CPU「回滚」⇒ 保持现值 |
| 巢群重型突击巡洋舰（C） | 5/4/6（提议）→ **4/3/5**（12 槽） | **280**（保持现值，不涨） | 船长「高中低槽各 −1」 |

> **同时删除一条旧弱断言**（船长「删掉」）：`content-check` 里的 **`role==='armored' ⇒ low >= high + 1`**。
> 溯源：该弱断言 2026-09-04 随 V18A 槽位落库（提交 `68bb806f`），依据是 `docs/design/v18-slots.md` §四
> 布局草案表（陆龟 2/2/3、玳瑁 2/3/3、玄武 2/3/4 的低槽偏多形态 + 注"甲厚（低槽多）"）——
> **是"草案精神"的提炼，不是独立裁定**；且草案数值早已被后续平衡批突破（玄武现为 4/4/6）。
> **保留"武装舰 `high >= low + 1`"半条**（现行 42 艘全部通过）。
> ⇒ 影响：D 巡洋 4/4/4、C 重突 4/3/5 得以成立（`low >= high + 1` 下 4/4/4 会违规）。
>
> **另行登记（待定）**：陵寝巡洋舰高槽 4 ⇒ **能装下 D 族 3 门高槽武器**（连装炮/丧钟/棱镜炮 + 1 通用位）✓ 该问题随之解决。
>
> **武器输出能力对比**（同船同技能、引擎口径）见 `tools/wh-weapon-dps.ts`：7 组配对中 5 组 DPS 为 **1.28~2.05× MK3**
> （陵寝棱镜炮 ×1.32 · 生体棱镜束 ×1.28 · 巨构近防阵列 ×1.40 · 亡军残炮 ×1.61 · 陵卫连装炮 ×2.05）；
> **掠袭破片炮**主段 ×0.85、**含副段 ×1.28**；**孢子导弹巢 ×0.69**（命中 0.5 + 装填 7.6s，定位在"覆盖/全体攻击"，
> 机制未落地前偏弱，⏳ 待船长定是否补偿）。



- `ShipDef.subClass`（新字段，`ShipSubClass` 联合类型；**只有这 15 艘写**）· `content:check` 加白名单契约（取值 + 非 `sh-wh-*` 不得写）；
- **进界面**：`ui/shipInfo.tsx` 的「定位 / 档次」行改成 `子分类 · 角色 · 舰种 T档`；`pages/ShipPage.tsx` 两处船只卡片（舰队列 + 市场/图纸列）在角色徽标前加**子分类徽标**（复用同级 `app-chip app-role-chip` 样式）；
- **15 张舰船图纸同步改名 + 重写说明**（蓝图名 = 新船名；说明按子分类一句话，说明契约核对通过）；
- **一处契约让步**：`role: 'armored'` 要求"低槽 ≥ 高槽 + 1"⇒ **C 族巡洋的 +1 槽位从高槽改到低槽**（3/4/5，高槽不再 +1）；
- **工具**：`tools/wh-ship-tune.ts`（入库；⚠ **只对未调过的基线运行**，检测到 `subClass` 即拒绝，防二次叠加）。

> ⚠ **下方 §4.1~§4.6 的旧表（旧船名 + 旧数值）已作废**，仅作历史留档；一切以本节为准。

### 3.8 C/D/E/G 四族审核（船长 2026-09-13 逐条给定）

**✅ 已落码（纯数据，四连绿）**

| 族 | 件 | 改动 |
|---|---|---|
| C | 生体脉搏加速器 | 装填 −18% → **−6%**；CPU 32 → **42** |
| C | 孢子导弹巢 | 命中 0.8 → **0.5**；单发 ×12.1 → **×9.68**（降 20%） |
| D | 陵卫连装炮 | 装填 1.6 s → **2.8 s**；单发 ×4.6 → **×9.2**（翻倍） |
| D | 守墓者速装填机 | CPU 34 → **54**（+20） |
| D | 陵墓弹道铭文 | 动能 22% → **18%**，并追加**能量 18%** |
| G | 亡军火控残响 → **亡军火控** | 命中 24% → **12%**（图纸同步改名） |
| G | 幽灵弹道校正器 | CPU 30 → **60**（+30） |
| G | 幽灵推进器 | 速度 +115% → **+85%** |
| C | **移除**活性甲壳层（含图纸） | 由「攻坚无人机」掉落替换 ⇒ C 族 = **5 件装备 + 1 件无人机掉落** |
| E | **移除**巨构稳态器（含图纸） | 由「哨戒无人机」掉落替换 ⇒ E 族 = **5 件装备 + 1 件无人机掉落** |

**⏳ 待落机制 / 待定（本批未动战斗代码）**

| # | 船长要求 | 类别 | 说明 |
|---|---|---|---|
| 1 | C「换成**攻坚无人机**」/ E「添加**哨戒无人机**」 | ✅ **已落码**（2026-09-13） | 船长追加裁定：「**是种族特色的虫洞专属无人机，不是通用无人机**」⇒ 新设两型专属机 **`drone-wh-c-heavy` 巢卫攻坚**（单发 15·血 240·甲/壳抗 0.35）/ **`drone-wh-e-sentry` 构件哨戒**（单发 24·血 60·命中 1.15）；`exclusive` + `unreleased` + 闸门前缀 `drone-wh-`；射程受 09-10「射程分类」硬钉（3500 / 5000）；**剩：① 一次掉几架（建议 10）② 来源接线 = 一号** |
| 2 | C 孢子导弹巢「**对所有敌方同时攻击**」 | ✅ **已落码**（2026-09-13） | ModuleDef.hitsAllFoes → WeaponSpec.allFoes；开火路径**逐目标独立掷命中 + 各吃各自层克制**（副目标不吃锁定加深）；**胜率预估器同步**（×在场敌舰数）；演出层逐目标推弹道。**实测**：最后一波 3 艘同时掉血（0/156/72 · 0/136/72 · 30/158/72） |
| 3 | C 生体脉搏加速器「**舰船速度 +10%**」 | 新机制（待确认） | 支援槽携带 `speedBonusPct`：需确认聚合是否**跨槽生效**（`familyModules` 是否按槽族过滤）+ 跨族登记 |
| 4 | C 几丁质骨架层「移除甲容量 +20%，改为**速度 +5%**」 | 契约例外 | 装甲槽将**既无甲容量也无甲抗** ⇒ 与 A 族「折射涂层」同类，需为"纯结构/速度件"开例外 |
| 5 | D 陵卫连装炮「**每次攻击消耗 2 发弹药**」 | 新机制 | 弹药消耗倍率（打靶与退款都要按倍率算） |
| 6 | E 巨构损管阵列「**改为结构类的**：结构抗动能/爆炸 +30%、结构值 +35%、CPU −10」 | 契约例外 | 去掉甲容量 ⇒ 装甲槽例外（同 #4）；抗性由三系改**两系**（去 plasma）；CPU 48 → **38** |
| 7 | E 巨构协处理器「添加负面**装填 +12%**」 | 新机制 | 负装填（现有 `reloadCutPct` 只收正值且语义是"÷(1+x)"，不是 ×1.12） |
| 8 | E 巨构护盾矩阵「动能/能量抗性 +32%、**护盾上限 +42%**、CPU +20」 | 契约例外 | 护盾槽契约规定"容量件与抗性件互斥"（V17.1 拆族）⇒ 需开例外；抗性去 explosive；CPU 50 → **70** |
| 9 | G 亡军火控「**伤害 +6%**」 | 新机制 | 通用伤害加成字段（现有只有按系 `damageTypeBonusPct`） |
| 10 | G 幽灵弹道校正器「**动能武器射程 +22%**」 | 新机制 | 射程加成（与 A 族"射程削减"同批：`rangeCutPct` / `rangeBonusPct` 一起落） |
| 11 | G 残兵结构层 → **鱿蜂结构层**，「**提高无人机 80% 的结构**」 | 新机制 + 改名 | 无人机血量加成字段（`droneHpBonusPct`）+ 无人机池接入 |
| 12 | **「所有鱿蜂换成鱿蜂」** | 改名 | 范围：`drone-exile-bee`（鱿蜂无人机）、`mod-lair-drone-tac-g`（鱿蜂群导控）、`mod-lair-drone-relay-g`（流亡中继桅）+ 词典 + **公告一处**（`announcements.ts`，已发布文案 ⇒ 改前请船长点头）；**id 一律不动**（避免动存档/契约） |

> ⚠ **#1–#12 与 §3.6 的 A 族 4 项同属一批"新机制"**，全部落在 `packages/core/src/{combat,equipment,types}.ts`
> 与 `packages/data/**`；二号**未动战斗代码**，等船长定归属后一次性落，落码带齐：预估器同步 / 信息卡加行 / 用例 + 负向验证。

---

### 4.1 档位阶梯（**从既有 27 艘船实测反推，本套严格落在同一条阶梯上**）

| 档 | 质量区间（等效质量，`hullClass.ts`） | 既有槽位 | 既有 CPU | 既有三层血合计 | 既有速度 |
|---|---|---|---|---|---|
| **T1 护卫舰** | 0.4~2.4M | 6~8（武装 3/2/1 ~ 4/2/2） | 110~165 | 126~243 | 320~360 |
| **T2 驱逐舰** | 2.4~5.8M | 7~10 | 145~235 | 333~396 | 285~300 |
| **T3 巡洋舰** | 5.8~13M | 8~12 | 185~390 | 648~799 | 185~275 |

> `armored`（重装）族的等效质量 ×0.65 ⇒ 可挂更大绝对质量而不越档（既有先例：陆龟级 6.0M 落在 T2、玳瑁级 11M 落在 T3）。**C 族与 D 族走 `armored`**。
> 契约：`armed` ⇒ 高槽 ≥ 低槽+1；`armored` ⇒ 低槽 ≥ 高槽+1；`slots` 每类 1~7、总数 ≥3；`priceIsk` 必须为 **0**（定制船口径，无市场行）。

### 4.2 逐族清单（15 艘）

**A 海盗（掠夺 · 快 · 货多 · 中近程 · `armed`）**

| 档 | 船名 | 槽位 | CPU | 三层血（盾/甲/结构） | 质量 | 速度 | 货舱 | 签名 |
|---|---|---|---|---|---|---|---|---|
| T1 | 掠私艇级护卫舰 | 3/2/1 | 120 | 78/39/47（164） | 1.3M | 350 | **1800** | 同档货舱最大 |
| T2 | 掠私驱逐舰 | 4/3/2 | 205 | 190/95/115（400） | 4.2M | 300 | **4400** | 货舱导向 |
| T3 | 掠袭巡洋舰 | 5/3/3 | 350 | 340/160/195（695） | 8.5M | 265 | **5200** | 火力与货舱兼顾 |

**C 异形（生体 · 结构/装甲高、盾薄、近程 · `armored`）**

| 档 | 船名 | 槽位 | CPU | 三层血 | 质量 | 速度 | 货舱 | 签名 |
|---|---|---|---|---|---|---|---|---|
| T1 | 幼虫艇级护卫舰 | 3/2/2 | 130 | 40/70/90（200） | 2.1M | 320 | 900 | 贴脸必中 |
| T2 | 甲壳驱逐舰 | 3/3/3 | 175 | 60/190/250（500） | 5.4M | 245 | 1900 | 装甲层最厚 |
| T3 | 巢群巡洋舰 | 3/4/4 | 240 | 90/380/470（940） | 12.2M | 195 | 4200 | 三层血最高 + 自修 |

**D 守墓（重装 · 三系抗 · 必中远程 · 最慢 · `armored`）**

| 档 | 船名 | 槽位 | CPU | 三层血 | 质量 | 速度 | 货舱 | 签名 |
|---|---|---|---|---|---|---|---|---|
| T1 | 哨戒艇级护卫舰 | 2/2/3 | 125 | 70/55/70（195） | 2.3M | 300 | 1100 | 必中远程点名 |
| T2 | 陵卫驱逐舰 | 2/3/4 | 180 | 110/180/230（520） | 5.6M | 220 | 3600 | 三系抗 + 均衡 |
| T3 | 陵寝巡洋舰 | 2/5/5 | 260 | 200/400/500（1100） | 12.8M | 175 | 9000 | 三层血最高 + 三系抗 |

**E 巨构（平台 · 机库/CPU · 无人机承载 · `armed`）**

| 档 | 船名 | 槽位 | CPU | 三层血 | 质量 | 速度 | 货舱 | 签名 |
|---|---|---|---|---|---|---|---|---|
| T1 | 构件艇级护卫舰 | 3/2/2 | 160 | 70/35/45（150） | 1.6M | 315 | 1000 | CPU 高 + 小机库 |
| T2 | 机库驱逐舰 | 4/3/3 | 250 | 175/88/105（368） | 4.6M | 275 | 2800 | 机库容量最大 |
| T3 | 巨构母舰 | 5/4/3 | 400 | 320/160/190（670） | 9.2M | 250 | 3800 | 机库 + CPU 双高 |

**G 亡军（幽灵 · 低信号 · 快 · 远锁定 · `armed`）**

| 档 | 船名 | 槽位 | CPU | 三层血 | 质量 | 速度 | 货舱 | 签名 |
|---|---|---|---|---|---|---|---|---|
| T1 | 幽影艇级护卫舰 | 3/3/1 | 135 | 65/32/40（137） | 1.0M | 365 | 700 | 信号最低 + 最快 |
| T2 | 侦查驱逐舰 | 4/4/2 | 220 | 165/82/100（347） | 3.4M | 310 | 2400 | 锁定最远 + 扫描最高 |
| T3 | 亡军巡洋舰 | 4/5/3 | 370 | 300/145/175（620） | 7.4M | 285 | 3200 | 无人机射程 + 指挥 |

### 4.3 族内三档各占一个位置（不重复）

| 族 | 护卫（T1） | 驱逐（T2） | 巡洋（T3） |
|---|---|---|---|
| A | 快 + 货 | 货舱导向 | 火力 + 货 |
| C | 贴脸必中 | 装甲最厚 | 三层血最高 |
| D | 必中远程 | 三系抗均衡 | 三层血最高 + 三系抗 |
| E | CPU 起手 | 机库最大 | 平台顶点 |
| G | 侦查最快 | 锁定最远 | 无人机指挥 |

> 无人机三条线刻意分开：**王鲭级＝伤害（既有）/ E 族＝承载（机库+CPU）/ G 族＝射程与侦查**，互不重复。
> 逐船还要给的字段（落码时逐艘填全）：`agility` / `evasion` / `hitBonus` / `powerBonus` / `lockRangeM` / `signatureM` / `scanResMm` / `warpSpeedAus` / `droneBayM3` / `weaponFamilyBonus` 或 `droneDmgBonus` / `shieldResist` / `description`（**玩家文案，无开发口径**）。

---

## §5 一次性图纸（**舰船的唯一获取形态**）

### 5.1 三条口径

1. **舰船只出一次性图纸，永不出成品**（船长本轮再次强调）——造船靠图纸 + 材料 + 工位，造一艘**吃掉一张**；
2. **装备**：本体可直接掉（立即变强）+ 一次性图纸也可掉（更稀有，自己造一件）；
3. **一律不掉永久图纸** ⇒ 不新增 `learnedRecipes` 通路；**不进 `FRAGMENT_RECIPES`**（硬契约：一次性图纸不得被碎片逆向成永久配方，`content-check:285-289`）；**蓝图碎片不进洞**。

### 5.2 一次性图纸的三条既有保障（无需新机制）

- **不上市场**：`BlueprintDef.singleUse` / `ShipBlueprintDef.singleUse` 都走豁免（`content-check:279-284` / `:300-303`），不要求市场卡；
- **重复获取有效**：`recipeCapability` 明确「书在架上就是可用——再获得一张 = 又能造一次」（`manufacturing.ts:100-103`），所以"每张图纸 = 一件产物"成立；
- **舰船蓝图的定价契约会自动跳过**：无市场行的定制船 ⇒ `content-check:2927` 直接 `continue`。

### 5.3 材料与耗时

- **材料必须是矿物**（硬契约 `content-check:274-277`，原矿不能直接当料）⇒ 以**虚空晶**为主 + **该族特色矿物**（与残骸回收的族池呼应）；
- 料/价按既有的 **45% 锚**（30%~60% 带内）；
- 耗时：装备图纸按同档蓝图的既有值；舰船图纸按该档既有舰船蓝图值。

---

## §6 掉落权重与深度门槛（**只给"掉什么、多稀有"；挂哪边出 = 一号**）

### 6.1 池的构成

**装备按族成池**（船长口径「虫洞装备跟着虫洞种族类型走」）：每个族一条池 =
**装备本体 ×6 · 装备一次性图纸 ×6**；其中 **C / E 两族 = 「装备 ×5 + 制式无人机 ×1」**
（船长 2026-09-13 裁定：C 移除「活性甲壳层」、E 移除「巨构稳态器」，**由无人机掉落替换** —— 见 §3.5 审核记录末两行）。

**无人机替换物 = 两型「族专属机型」（2026-09-13 船长：「是种族特色的虫洞专属无人机，不是通用无人机」⇒ 已落码）**：

| 族 | 替换掉的原件 | 替换物（玩家名 / id） | 关键数值 vs 制式锚点 | 体积 | 一次掉几架 | 来源 |
|---|---|---|---|---|---|---|
| **C 巢群** | 活性甲壳层（含图纸，已移除） | **巢卫攻坚无人机** / `drone-wh-c-heavy` | 单发 **15**（制式 12）· 三层血 **240**（194）· 弹种**爆炸**（拆甲）· CPU 12 · 命中 0.78 · 闪避 0.08 · **甲/壳抗性 0.35、盾 0.25** | 20 m³ | **建议 10 架**（同窝点先例）· ⏳ 待确认 | 一号接线 |
| **E 巨构** | 巨构稳态器（含图纸，已移除） | **构件哨戒无人机** / `drone-wh-e-sentry` | 单发 **24**（制式 20）· 三层血 **60**（46）· 弹种**动能**（拆盾）· CPU 18 · 命中 1.15 · 衰减 0.40（制式 0.35）· 闪避 0.20 | 40 m³ | **建议 10 架** · ⏳ 待确认 | 一号接线 |

- **船长两条裁定（2026-09-13）**：①「C 组无人机护甲和结构抗性提高 10%」⇒ 甲/壳抗性 `0.25 → 0.35`（护盾维持 0.25）；②「**E 组哨戒不做更改**」⇒ E 型按上表落码，且**「哨戒机可被攻击」机制保持关闭**（不重开）。
- **口径**：两型都是 `exclusive`（无蓝图、不上市场、不入常规掉落）＋施工期 `unreleased`＋id 前缀 `drone-wh-` 已纳入 `content:check` 的「虫洞不可见闸门」。两型都**符合族弹型权威**（C = 爆炸在族内、E = 动能在族内）。
- **射程 = 专属豁免（船长 2026-09-13「专属无人机开豁免」）**：C 型 **3,600 m**（档位标准 3500）、E 型 **6,000 m**（档位标准 5000，哨戒区间上限）。落实方式 = `content:check` 的「射程分类」档位钉对 `exclusive === true` 机型**跳过断言并登记打印**（其余两道约束照旧：`DRONE_ROLE_SPECS[cls].rangeM` 区间 + 硬边界 500~20000 m）。这是对船长 2026-09-10「射程分类 1/4~4/4」那条钉子的**定向豁免**（仅专属机型，制式四型仍按 2500/3000/3500/5000 硬钉）。

> ⏳ **接线现状核对（2026-09-13 二号，主树含一号在途文件）**：一号已做**族池派生**（packages/core/src/wormholeSalvage.ts 的
> WormholeFamilyPool = modules / moduleBlueprints / shipBlueprints，从 ctx 目录按 id 前缀派生 + wormholeFamilyPoolGaps 查齐备），
> **两处仍缺**：① 池里**没有「无人机」这一类**（drone-wh- 全仓只有 2 条物品定义 + 闸门前缀，无池/掉落引用）⇒
> **C 巢卫攻坚 / E 构件哨戒 两型专属无人机不掉**；② panels/Wormhole.tsx **未给 wormholeDescend 传 scanBonus** ⇒ 扫码加成只在第 1 层生效。
>
> **精确落点（照抄即可）**：
> `	s
> // ① wormholeSalvage.ts：池加"无人机"一类（架数复用 salvage.ts 的 RARE_BOX_DRONE_UNITS = 10）
> export interface WormholeFamilyPool { modules: string[]; moduleBlueprints: string[]; shipBlueprints: string[]; drones: string[] }
> //   wormholeFamilyPoolOf 里：drones: pick(ctx.items.keys(), 'drone')   // C = drone-wh-c-heavy、E = drone-wh-e-sentry
> //   ⚠ 无人机是**选择性替换物**（只有 C/E 两族有）⇒ **不进"五族齐备"判据**，只做归属/孤儿检查
> // ② Wormhole.tsx：深入下一层时带上扫码加成
> import { wormholeScanBonusOf } from '@whale/core'
> const r = wormholeDescend(run, seed, wormholeScanBonusOf(ctx, run.fleet))
> `
>
> ⚠ **接线状态**：数据侧机**已落码**（`packages/data/src/items.ts`，物品总数 36→38、无人机 5→7）；
> 但**族池掉落表仍未接线**（全仓除 `blueprints.ts` 的图纸→装备映射外无任何地方引用 `mod-wh-*`，§6.3「只给权重、不给来源」照旧）
> ⇒ 接线时：**C/E 两张池各多一条无人机条目**（一次 ×10 架），其余三族不变。

**舰船图纸走全族共用池**（2026-09-13 二号建议，**待船长按族审核时拍板**，理由三条）：
① **图纸是"文件"不是残骸**——残骸认族（从谁身上扒下来的），文件不认族才合直觉；
② 五族都有船可拿，不会因为某族暂时没有洞内敌卡就废掉它那 3 艘船；
③ 装备的"跟着族走"完全不受影响。
⇒ 共用池按深度分层抽：**护卫 / 驱逐 / 巡洋** 的档位权重见 §6.2 第二表。
（若船长要"船也认族"，则 E 族那 3 艘必须等 E 族洞内敌卡到位才能有来源。）

### 6.2 按深度的稀有度（草案）

| 层 | 装备本体 | 装备图纸 | 舰船图纸 |
|---|---|---|---|
| 层 1~2 | **不出专属** | — | — |
| 层 3~4 | 60% | 25% | 15% |
| 层 5~6 | 45% | 30% | 25% |
| 层 7+ | 30% | 35% | 35% |

**舰船图纸的档位内权重**（同一条池里抽）：

| 层 | 护卫舰图纸 | 驱逐舰图纸 | 巡洋舰图纸 |
|---|---|---|---|
| 层 3~4 | 50% | 33% | 17% |
| 层 5~6 | 35% | 40% | 25% |
| 层 7+ | 25% | 35% | 40% |

### 6.3 硬口径

- **按族抽**：掉的是"哪一族的池"，由**打的是哪一族**决定（船长「虫洞装备跟着虫洞种族类型走」）；
- **确定性**：所有抽取都走 `(seed, 层, 节点, 下标)` 散列 ⇒ 同种子同结果，可用例钉；
- **只给权重，不给来源**：谁触发、触发几次、几堆，**一号接线时定**。

---

## §7 施工期闸门（**本批必须一并做，否则"对玩家不可见"会破**）

**实测证据**：`Handbook.tsx:466 / :474 / :486` 的**装备图鉴 / 舰船图鉴 / 蓝图图鉴都是直接遍历全目录**的（只有物品图鉴在 2026-09-13 修成走 `visibleItemDefs`，`Handbook.tsx:456-458`）⇒ 本批新增的装备、舰船、图纸**当天就会出现在玩家图鉴里**（搜索框能搜到）。

**要补的四件事**：
1. `ModuleDef.unreleased` / `ShipDef.unreleased` / `BlueprintDef.unreleased`（语义同 `ItemDef.unreleased`）+ 单点可见判据（照 `itemReleased` / `visibleItemDefs` 的样式）；
2. **三处图鉴改走可见目录**（Handbook 的装备/舰船/蓝图三个 tab）；
3. `content:check` 的「虫洞不可见闸门」扩两条：**新装备/新船/新图纸必须标 `unreleased`**、**玩家可见文案不得出现「虫洞」**（现有闸门只扫敌卡与物品）；
4. 既有遗留：两艘模子船的图鉴描述是开发口吻（「战列舰壳体（模子）…数值待定案」，`ships.ts:692 / :719`）且**已对玩家可见** ⇒ 一并改成玩家文案（或同样标 `unreleased`）。

**上线动作**＝删 `unreleased`（与公告同批、船长拍板后）。

---

## §8 落码分批（每批四连 + 负向验证）

| 批 | 内容 | 规模 |
|---|---|---|
| **L0** | 闸门四件事（§7）——**先落这个**，否则后面每一批都在漏 | 判据 + 3 图鉴 + 契约 |
| **L1** | A/C/D/E/G 各 2 件 = **10 件装备**（先落"全空字段"那一批） | 10 装备 + 10 图纸 |
| **L2** | 其余 20 件装备（补齐每族 6 件） | 20 装备 + 20 图纸 |
| **L3** | **15 艘舰船**（每族护卫/驱逐/巡洋）+ 15 张一次性舰船图纸 | 15 船 + 15 图纸 |
| **L4** | 掉落权重表 + `tools` 校准（每层期望掉落 / 每族配平）+ 词典与公告待审稿 | 工具读数 |

### 落码进度（2026-09-13）

| 批 | 状态 | 读数 |
|---|---|---|
| **L0 闸门** | ✅ 已落码 | `ModuleDef/ShipDef/BlueprintDef/ShipBlueprintDef` 四个 `unreleased` 字段 + 引擎层单点过滤（`engine.ships/modules/blueprints/shipBlueprints`，另有 `all*` 全目录给"已持有"路径）；手册三处图鉴自动随闸门；契约加两条 |
| **L1+L2 装备 30 件 + 图纸 30 张** | ✅ 已落码（**合并为一批**，船长「装备就全部做进来」） | `content:check`：**装备可见 104/134**、**装备图纸可见 94/124**（各 30 条被挡）· 蓝图价格口径「一次性图纸豁免 30 张」 |
| **L3 舰船 15 艘 + 一次性舰船图纸 15 张** | ✅ 已落码（船长「舰船先进行落码」） | 舰船 **42 艘**（护卫 10 / 驱逐 12 / 巡洋 14 / 战列 4 / 旗舰 2）· 舰种契约 **42/42 一致** · 闸门读数「**90 条**全部标 unreleased」· 可见目录 **舰船 27/42 · 舰船图纸 24/39**（各 15 条被挡） |
| L4 权重表 + 校准 | ⏳ 未落码 | —— |

**L3 落码遗留（按族审核时一并定）**：
1. ~~**舰船命名**：既有 27 艘是海洋生物系，这 15 艘按族签名命名，是否统一？~~ ⇒ **船长 2026-09-13 裁定：「不用统一到海洋生物」**——族签名命名（掠私艇级/幼虫艇级/哨戒艇级/构件艇级/幽影艇级…）**保留**。
2. **`tools/battle-calibrate.ts` 的参考船按"速度中位数"现算**（`[...SHIPS].sort(by speed)[floor(len/2)]`）：船数 27 → 42 后**参考船由长尾鲨级换成王鲭级**，基准战斗机动 165.2 → **166.0 m/s（+0.5%）**（探针实测，用完即删）。该工具**只打印、不写任何游戏数据**，故实际影响 = "下次用它重标敌速时锚点微移 0.5%"；**建议改成按 id 钉死参考船**（属一号的工具，二号未动）。
3. E 族依赖**只剩装备**：E 族 6 件装备按族成池，仍需一张 E 族洞内敌卡才有来源（舰船已改走共用池，不再依赖）。

---

## §9 待办与跨线依赖

1. **E 族洞内敌卡**（一号）：没有它，E 族池无来源；
2. **掉落来源接线**（一号）：本稿只给池与权重；
3. **数值复核**：装备量级锚（≈2× MK3）与 15 艘船的阶梯，落码时用 `battle:calibrate` / `battle:drone-vs-gun` 复核，不手算拍板；
4. **词典**：新术语（虫洞装备 / 一次性图纸……）按规矩**经船长批准后**才写入 `docs/glossary.md`；
5. **公告**：虫洞上线公告另稿（`announcement-draft-20260913-wormhole.md`），本批不动。

---

_维护：本稿由二号 2026-09-13 起草；所有设计条款都能追到 §0 的船长原话。改动请在此追加并注明日期与出处。_
