# 英语本地化（可玩骨架）· 工作文档（2026-09-19）

状态：**进行中**（P0/P1 已完成；P2 名称与说明 100%；**P1b「ID 制改版」已落码**；P3 界面批进行中；P4/P5 未开始）
分支：`verify40`（三号）

> ⚠️ **船长 2026-09-19 追加令（覆盖常规「完成即合入」纪律）**：原话「**继续翻译工作，翻译完成钱不要合并**」（"钱" = "前" 的笔误）
> ⇒ **翻译全部完成前不合入 main**：全部改动只在 `verify40` 本地提交，完成后交船长验收，再一次性合入。
> 为控制日后合并冲突，我会**定期把 verify40 rebase 到 main**（rebase 不动 main，仍不算合入）。

## 船长原话（照抄）
「希望对游戏进行英语本地化处理。」（2026-09-19）

**追加令（2026-09-19，同日改判架构）**：「**建议所有本地化文本采用ID引用的，这样只需要做一份本地化表就能直接进行替换**」
⇒ 两问两答确认口径：**id 自动语义化**（域 + 文件短名 + 序号，如 `ui.itemsPage.014`）+ **界面层与内容层并入同一张表**
（content 段 `ship./mod./item./…`，core 文案留 P5 但同表 `core.*`）。

四步闸门记录：集中提问 7 条 → 船长七问七答（全按推荐口径）→ 设计总结 → 船长「**可以**」（确认）。

## 已确认口径（七条，冻结）

| # | 项 | 裁定 |
|---|---|---|
| 1 | 范围 | **可玩骨架**：UI 界面 + 全部名称与短说明；手册词条 / 通讯 / 公告等长文列第二批（船长接受英文下这些页中英混排） |
| 2 | 切换 | **设置里可切 + 默认跟随系统**（`navigator.language`）；语言存 `localStorage`，**不进存档** |
| 3 | 老档日志 | **保持现状**：`state.logs[].text` 存的是渲染好的中文 ⇒ 老日志永远中文；「新日志跟语言」在引擎文案批兑现 |
| 4 | 翻译来源 | **三号出术语表 + 全量英文初稿，船长终审** |
| 5 | 专名 | **直译**（长尾鲨级 = Thresher-class · 钛钢合金 = Tritanium Alloy · 鲣鱼级 = Skipjack-class） |
| 6 | 排版 | **先出逐页溢出读数、船长逐页定**；「一级页不滚」红线**默认维持** |
| 7 | 铁律 | 只有**玩家可见文案**做中英双语；开发侧（文档 / 注释 / 汇报 / 思考）仍全文中文（`AGENTS.md` §1 已按船长特批改判 + changelog 记账） |

## 架构（两条取舍，为免上千处改动）

- **内容层**（`packages/data`）：新增**按 id 索引的英文覆盖表** `data/src/l10n/en/*.ts`（`{ name, description }`）＋
  `localizeDef / localizeCtx(ctx, locale)`；`buildSimContext(locale?)` 增可选参数。
  **`locale === 'zh'` 时原样返回** ⇒ 现有工具 / 测试 / 模拟逐字不变。英文只覆盖 `name/description`，id 与数值一字不动。
- **界面层**（renderer `i18n/`）：`L10nProvider` + `useL10n()`；**文案一律按 id 引用**（`t('ui.itemsPage.014')`），
  中英两列都只写在 `packages/data/src/l10n/table.ts` 的 `L10N` 表里 ⇒ **源码零文本，换语言 = 换表**；
  **缺 id 显示 id 本身**（便于定位漏登记，不再回退中文）。批量接线与造 id 由 `npm run l10n:wrap` 代劳。
  `fmtNum / fmtIsk / fmtM3` 走当前 locale（替换 353 处硬编码 `toLocaleString('zh-CN')`）。
- **字体**：`[data-locale="en"]` 切 Latin 优先栈（`Segoe UI / Inter / system-ui` → CJK 兜底），中文侧一字不动。
- **校验工具**：新增 `npm run l10n:check` —— ① 英文表 id 集合 = 中文表（缺失 / 孤儿都红）② UI 词典无死 key ＋
  列出未译可见串（报告）③ 英文串禁残留 CJK（白名单除外）④ 占位符 / 单位一致性。
  `content:check` 的**禁用词契约按语言分叉**（中文侧仍禁 EVE/ISK/NPC；英文侧另立清单）。

## 分期（每批独立提交 + 四闸门 + 船长验收）

| 批次 | 内容 | 状态 |
|---|---|---|
| P0 | 术语表 + 专名译名表（`docs/glossary-en.md`）⇒ 船长审后冻结 | **已冻结**（船长 2026-09-19 五问五答：`credit` · `Enigma / Enigma Device` · `Leatherback-class` · `Chen's Crew No.1` · `Whaleswallow-class`）。专名已含：舰船 43 · 星系 20 · 矿带 17 · 势力 13 · **物品 86 · 技能 79 · 异常点 27 · 敌舰/敌卡 40**；剩装备 142 + 蓝图 135（P0b 尾批） |
| P1 | i18n 骨架（locale 偏好 / `t()` / 数字格式 / 字体栈 / `l10n:check`） | **已落码**（见下「P1 落码记录」；请船长点开设置面板切一次看效果） |
| P2 | 内容英文：名称 + 短说明 ≈600 条 | **已完成**（名称 100% · 说明 649/649；见下「P2 内容进度」） |
| P1b | **ID 制改版**（唯一表 + 批量接线器 + 体检器改判据） | **已落码**（见下「P1b 落码记录」；旧词典已删） |
| P3 | 界面 chrome 英文 ≈600~900 条 | **基本完成**：机制（`l10n:wrap` 造 id + 写表 + `l10n-keep` 豁免 / 嵌套护栏 / 模板插值 / 多行提示）已落码；界面批 1~17 已接线 **1,861 条 `ui.*`**（2,345 处调用）⇒ 全部一级页与二级面板、悬浮层、美术标签均已双语；未译读数（**含 JSX 文本节点**）1,862 → **1,225**，其中 **1,058** 为 P5 长文（远征 404 · 虫洞 338 · 手册 316），界面侧残余约 **167** 条多为「逻辑用中文串 / 联合 key」等**故意不译**项 |
| P4 | 逐页溢出读数 → 船长逐页定排版 | 待接 |
| P5 | 长文第二批 + 引擎文案 | **基本完成**：**手册页 100% · 远征页 461 处 · 虫洞页 290 处**（三份长文缺译全部清零，共剩 65 处跨行碎片待人工改写）；core 引擎日志与命令文案待做 |

## P1b 落码记录（2026-09-19 · **ID 制改版**，船长当日追加令）
**新增**
- `packages/data/src/l10n/table.ts`：**唯一本地化表** `L10N: Record<id, { zh, en }>`（当前 **99** 条 `ui.*`）。
  头部注释写明 id 规则、术语权威（`docs/glossary-en.md`）、两条硬口径（英文禁残留中日韩 · 中英占位符集合一致）。
  由 `packages/data/src/index.ts` 导出（`localizeCtx` 与渲染层读的是**同一张表**）。
- `tools/l10n-wrap.ts` + `npm run l10n:wrap`（已登记台账）：**批量接线器**——AST 认四类安全位置
  ① JSX 文本子节点 ② 展示类属性字符串初值（`title`/`placeholder`/…）③ JSX 三元/逻辑分支 ④ 旧写法 `t('中文')` 的参数，
  换成 `tr('ui.<文件短名>.<序号>')` 并**把中英两列追加进表**；英文由 `--en=<json>` 提供，
  **缺译的一律不包**（绝不产出 `en: ''` 半成品）；同一中文串**复用既有条目**（一条文本一个 id）；
  跨行 / 标点碎片（正文与 `{表达式}` 交错）列「需人工拆句」清单，脚本不猜。

**改**
- `apps/desktop/src/renderer/src/i18n/locale.tsx`：`import { L10N } from '@whale/data'`；新增 `textOf(id, locale)`
  （缺 id ⇒ 返回 id）；`t()/tr()` 参数由中文源串换为 **id**。
- `App.tsx` 导航 9 项标签、`ItemsPage/CargoPage` 的 `KIND_EMPTY` 取值改为 id 字符串（渲染处 `t(...)`）；
  75 处 `t('中文')` 调用点改写为 `t('ui.…')`。
- `tools/l10n-check.ts`：判据换成 **ID 不变量** —— ① id 形态 `<域>.<短名>.<三位序号>` ② 调用点不得再传中文源串
  ③ 死引用（源码用了表里没有 ⇒ 界面显示 id）④ 英文禁残留中日韩 ⑤ 占位符对齐 ⑥ 值形态；
  另出「未接线条目 / 未译读数 / 同中文串多条目」报告（不阻断）。
- `apps/desktop/src/renderer/src/i18n/dict.en.ts` **删除**（旧「中文源串当 key」词典，已被唯一表取代）。
- 临时探针 `tools/_l10n-migrate-ui.ts`、`tools/_l10n-rewrite-ids.ts` 用毕删除。

## P2 落码记录（2026-09-19 · 机制批）

**新增**
- `packages/data/src/l10n.ts`：`Locale` / `EnText` / `EnTable` 类型 · `EN_SHIPS`（舰船 43 条**名称**，口径 = `docs/glossary-en.md` §四）·
  `overlayMap(src, en)`（**只改 `name` / `description`**；一条都没命中时返回原对象 ⇒ "没翻译"与"翻过"在引用上可分辨）·
  `localizeCtx(ctx, locale)`（`locale==='zh'` **原样返回同一个 ctx**）。
- `packages/core/tests/l10n-overlay.test.ts`（5 例）：① zh 缺省 = 显式 zh 逐字相同 ② en 覆盖生效（长尾鲨级 → `Thresher-class Missile Cruiser`、
  玄武级 → `Leatherback-class Heavy Flagship`）③ **除 name/description 外逐字段深比一字不动**（去掉两字段后整体序列化比对，新增字段也管）
  ④ 覆盖表 id 必须都存在于内容表（写错的 id 会让英文界面冒中文）⑤ 覆盖表只许写 name/description 两个字段。

**改**
- `packages/data/src/context.ts`：`buildSimContext(locale: Locale = 'zh')` —— 组装完的 ctx 过一层 `localizeCtx`；
  缺省 `'zh'` ⇒ 与改动前**逐字一致**（工具 / 测试 / 模拟全走缺省）。
- `packages/data/src/index.ts`：导出 `localizeCtx / overlayMap / EN_SHIPS / L10nLocale / EnText / EnTable`。

**验证**：typecheck 四包 0 错 · **core 全量 170 文件 / 1842 例全绿**（含新 5 例）· `content:check` ✅ · `l10n:check` ✅ ·
`ui:rot-check` ✅ · 构建 ✅。

**下一步（P2 内容批 / P2b）**：① 渲染层接线（locale 变化时用 `buildSimContext('en')` 重建引擎 ctx ⇒ 舰船页立刻显示英文名）；
② 按批追加覆盖表内容：装备 142 名称 → 物品 86 → 技能 79 → 异常点 27 → 敌舰/敌卡 40 → 蓝图（派生规则）→ **各表短说明**。

## P1 落码记录（2026-09-19）

**新增**
- `apps/desktop/src/renderer/src/i18n/locale.tsx`：`Locale` 类型 · `detectSystemLocale()`（首选 zh\* ⇒ 中文，其余 ⇒ 英文）·
  `loadLocale()/saveLocale()`（`localStorage: whale-idle:locale`，与既有 `:ui-zoom`/`:ui-fs` 同族）·
  `L10nProvider` + `useL10n()`（组件内）· `tr()` + `currentLocale()/isEn()`（组件外）· `interpolate()`（`{name}` 占位符）。
  Provider 顺手把 `data-locale` 与 `lang` 挂到 `<html>`（供 CSS 与系统级排版用）。
- `apps/desktop/src/renderer/src/i18n/dict.en.ts`：英文词典，**以中文源串为 key**（当前 45 条：启动 2 · 导航 9 · 设置面板 17 · 常用名词 17）。
- `apps/desktop/src/renderer/src/i18n/fmt.ts`：`fmtInt/fmtNum/creditUnit/fmtCredits`（按当前语言；中文侧 `zh-CN` 与 `en-US` 千分位一致，差别只在单位词）。
- `tools/l10n-check.ts` + `npm run l10n:check`（已登记进 `tools/README.md` 台账，工具数 31→32）。

**改**
- `App.tsx`：挂 `useL10n()`；**设置面板新增「语言 / Language」一行**（中文 / English 两态，即时生效，当前项高亮）+
  面板全部文案走 `t()`；导航 9 项标签走 `t(item.label)`；顶栏「手册 / 设置」两按钮走 `t()`。
- `main.tsx`：两处 `root.render` 都包 `L10nProvider`（含性能采集那条分支）；启动/失败文案走 `tr()`。
- `styles.css`：新增 `html[data-locale='en'] body { font-family: 'Segoe UI', Inter, … }`（英文换 Latin 优先栈，中文侧一字不动）。

**验证**：`typecheck` 四包 0 错 · `npm run l10n:check` ✅（无死 key · 占位符对齐 · 无残留中日韩 · 值形态合规）·
`ui:rot-check` ✅ · `content:check` ✅ · 构建 ✅ · `docs:index --check` ✅。
**未译读数（工具报告口径，不阻断）**：渲染层仍含中日韩的字符串字面量 **1,929** 条，Top：`Handbook.tsx` 332 ·
`Expedition.tsx` 240 · `Wormhole.tsx` 146 · `shipInfo.tsx` 146 · `ShipPage.tsx` 128 ⇒ 由 P3 逐页消化。

## P2 内容进度（**已全部完成**）

| 表 | 名称 | 说明 |
|---|---|---|
| 舰船 43 | ✅ | ✅ |
| 装备 142 | ✅ | ✅ |
| 物品 86（+ 碎片派生 · 残骸 26 派生） | ✅ | ✅ |
| 技能 79 | ✅ | ✅ |
| 异常点 / 敌卡 42 | ✅ | ✅ |
| 星系 20 · 矿带 17 · 站点 2 · 势力 13 | ✅ | 星系/矿带/站点 ✅ · 势力无说明字段 |
| 旅行事件 8 · 谜质科技 23 | ✅ | **两表都没有说明字段**（无需翻译） |
| 蓝图 135 | ✅（派生） | ✅（逐条手译） |
| 舰船蓝图 57 | ✅（派生） | ✅（45 条手译 + 12 条 `sbp-once-*` 按本体派生） |

⇒ **名称 100% · 说明 649/649**（P2 完成）。下一步 = **P3 界面 chrome**（≈600~900 条界面串包 `t()` + 词典）。

## 边界（本次不做）

- 日志与命令错误提示不进第一批（P5）；老档历史日志保持中文。
- 不改存档结构（语言不进存档）· 不动战斗 / 经济数值 · 不做其它语言。
- 开发侧文档 / 注释 / 汇报仍中文。
- 长文第一批不译 ⇒ 英文下手册 / 通讯 / 公告页中英混排（船长已接受）。

## 续接卡（2026-09-19 · 三号写；接手/验收先读这一节）

**⚠ 2026-09-19 傍晚更新：已并入 main（二号「乙组」筛选表收编 + 纯科技修复 + 洞内倍速记忆）**
- 合并方式：**把 main 并进 verify40**（不是 rebase——45 个提交逐个 rebase 冲突太重）；冲突 11 处全部按
  「保留 ID 制、采纳他的结构」解；他新写的旧式中文 key（`t('已满级')`）已转 id。
- 合并后又接线 21 处（他收编进 `ui/itemSubs.ts` 的筛选表标签等），清 3 条死条目。
- 合并后现状：表 **2,816 条** · 渲染层接线 **3,405 处** · 未译读数（含 JSX 文本）**308**。

**一句话状态**：**内容与界面已全部双语**；剩 4 件事——渲染层碎片/漏项 **≈90 条** · **core 引擎文案 ≈600 处（最大一块）** · 收尾核对 · **P4 溢出读数表**。

**已完成**
- P0 术语表冻结 · P2 内容 100%（名称 + 649 条说明）· P1/P1b ID 制（唯一表 + `l10n:wrap` + `l10n:check`）；
- P3 界面：全部一级页、二级面板、悬浮信息层、美术标签；手册 / 远征 / 虫洞三份长文**缺译清零**；
- `content:check` 的市场类型契约已改成「认 id + 核中文列」（ID 制带来的连带修正）。

**未完成（按优先级）**
1. **core 引擎文案 ≈600 处**（`packages/core`：`addLog(` **316** 处 · 命令错误串 `error: '中文'` **301** 处）。
   需要先定口径：建议 **core 只产出「文案 id + 参数」**，由渲染层按当前语言渲染（老档日志按船长口径保持中文，
   新日志跟语言）；改动面大，**开工前请船长确认口径**（属"系统级改动"⇒ 四步闸门）。
2. 渲染层碎片 57 处 + 漏项 47 条（工具 `npm run l10n:wrap` 的「需人工拆句 / 缺译未包」两个清单，逐个整段改写）。
3. **P4 逐页溢出读数表**（英文比中文长 30~60%）：用 `npm run ui:geom` / `ui:probe` 在 en 语言下逐页量，
   交船长定排版（一级页「不滚」红线是否维持由船长裁决）。
4. 收尾核对：`l10n:check` 的「未译读数」里 **≈250 条为故意不译**（`l10n-keep`：类型联合 key、
   `AUTOPERF_*` 开发协议串；以及逻辑比较用中文串）——建议收尾时把这几类登记进工具白名单，让读数归零可核。

**命令速查**
- 造/写表：`npm run l10n:wrap`（干跑列清单；`--en=<json>` 才落盘；`--only=<file>` 限单文件）；
- 体检：`npm run l10n:check`（ID 不变量 + 未译读数）；四闸门：`npm run typecheck` · `npm run test -w @whale/core` ·
  `npm run content:check` · `npm run ui:rot-check` · `npm run build` · `npm run docs:index -- --check`。

**坑（都踩过，别再踩）**
- 造 id **先查该段最大号**（`ui.<段>.<三位数>`；撞号 typecheck 报 TS1117 / 体检器报死引用）；
- **嵌套模板 / 模板里套三元中文串**：工具只包内层、外层转人工（外层要按参数整句拼）；
- 片段改写要**整段替换**（只往后接 `{tr(...)}` 会把中文留在页面上——我犯过一次）；
- 多行 `title` 用真换行（`\n`），JSON 里写 `\n` 转义、**不是** `\\n`；
- 英文值禁残留中日韩（唯一放行：语言自称 `en === zh`，如「中文」）。

**等船长三处裁决**：① **游戏英文名**（暂用 `Whale · Deep Space Idle`）② `ui.ShipPage.106` 与手册页的
「入门向」设计侧表述 ③ 存档管理页中文笔误「两**讨伐**确认」。

**纪律**：船长令「翻译全部完成前不合入 main」——全部改动只在 `verify40` 本地（**44 个提交**），
完成后交船长验收再一次性合入。

## 待归档

- 归档时：`docs/glossary-en.md` 转为长期权威（术语与译名）· roadmap 一条 · 词典可能加「英文译名口径」条目 ·
  `steam-store-draft-20260911.md:159` 那条旧裁定标注作废 · 删除本工作文档。
