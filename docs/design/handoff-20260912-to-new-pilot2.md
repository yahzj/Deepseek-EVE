# 二号工作树 · 会话交接卡（2026-09-12 · 交接给**新的二号**）

> **给接手人**：你是**二号**，工作树 = `H:\大鲸鱼\Deepseek-EVE-d2`（分支 `d2/workspace`，永不直接写主树
> `H:\大鲸鱼\Deepseek-EVE`）。**开工顺序** = 本卡 → `AGENTS.md`（尤其 §2 四步闸门 / §3 验证闭环 /
> §4 工作区与合入纪律 / §5.1 **禁子代理** / §5.2 冲突先提醒 / §1「验收视图默认 = 本地」）→
> `docs/roadmap.md` **顶部变更记录（最新在前）** → 再按任务类型读对应设计稿 → 最后才看代码现状。
> 旧二号（本卡作者）在本会话把「敌人重做」后续与一轮**代码审计五条**做完了；下面把状态、口径、坑与
> 未决项一次交清。

## 0. 当前快照（2026-09-12 收尾）

| 项 | 值 |
|---|---|
| 工作树 / 分支 | `H:\大鲸鱼\Deepseek-EVE-d2` / `d2/workspace` |
| HEAD | `e65deac6`（**与主树 `main` 同点**；本卡提交后 +1） |
| origin/main | `6565dadb` ⇒ **本地未推送 6 个提交**（写本卡前；提交本卡后 7 个，含一号若干条） |
| 四连基线 | typecheck 四包 **0 错** · core **105 文件 / 1042 用例**全绿 · `content:check` ✅ · 桌面 build ✅ |
| 工作区 | **干净**；`tools/_*` 临时探针 **0** |
| 验收视图 | **船长默认看的是本地那份**（AGENTS.md §1，2026-09-12 新定）⇒ 改完主动提醒他**重建/重启本地**，不要假定他看线上 |

## 1. 旧二号本会话交付线（全部已合入 main）

| # | 交付 | 主要提交 | 推送状态 |
|---|---|---|---|
| 1 | **敌人重做收尾**：E 族巨构集中到奥罗荒环 · G 族舰级与机群 · 近防炮八条改版读数 · 公告《敌人重做》 | `a444bec` 起一串 | 已推（一号代推） |
| 2 | **战斗界面射程标签随无人机射程变化**（船长实测反馈） | `a444bec` | 已推 |
| 3 | **战斗画面提示显示时长 ×3**（4 秒 → 12 秒） | `86aacd2` | 已推 |
| 4 | **D 族静滞卫舰「受击增程（炮台）」**：挨打后 12,000 → 18,000m，仅该型舰 | `f32fc71` | 已推 |
| 5 | **星系机制通讯**：4 封信（奥罗/鱿烬/低安/红环）+ 两条新触发器 `lowSec` / `foeFamily` + 体检登记契约 | `8a63189` | 已推（**本批无公告**，一号拟稿后船长裁定**消档**） |
| 6 | **A1 安全等级口径收口**：低安 = `sec ≤ 0`（含 0）· 扫描保留 0.5 只修文案 · 掉落并入低安档 · 主题件档位加断言 | `107244d` → `5766296` | 已推 |
| 7 | **B2+B3 抽取单点化**：`pickOne` / `pickWeighted` 收口 26 处手写 | `45b7348` | 已推 |
| 8 | **B1 远端衰减合一**：`beamPowerFactor` 委托 `distFactor` + 取值域契约（56 处 `falloff`） | `d5db9f1` | 已推 |
| 9 | **A3 战斗字段登记表 + 编译期守卫**，随后**七项"重载即丢"全修** | `dd2a49a` · `35c34b8` | 已推 |
| 10 | **A2 价格权威：只写文档**（真源/副本/护栏状态/基线读数） | `dc155d6` | 本地 |
| 11 | **P-40 收口**：我方近防炮补优先级（`role` 兜底判档）+ 集火（`mePdFocus`）+ 两处过期注释 | `94ffb4d` | 本地 |

**审计五条（A1 / B2+B3 / B1 / A3 / A2）至此全部落地**——起因是船长问「检查下游戏系统类是否有类似的功能
却各自单独一套代码的」，明细见 roadmap 对应条目。

## 2. 接手必读口径（本会话新增或改动的，**别再踩**）

1. **低安 = `sec ≤ 0`（含 0）**（船长 2026-09-12「0也算低安」）——但**扫描慢是另一条线**：
   `sec < 0.5`（含中安；船长裁定「保留 0.5、只修文案」）⇒ 两条线**并存且已写明**，别再统一它们。
2. **掉落三段**：① 池均价 `mSec = 1 + 0.45×max(0,−sec)` 是**连续函数**（sec=0 处本就无溢价）；
   ② MK2 完好舰体层 & ③ 主题件档位**按低安线 `sec ≤ 0`**（烬火/回音两张卡已改低安档 `mk2`）。
   主题件档位**仍逐卡手写**，由体检「主题件档位与星系分区一致」拦——**改了星系安全等级就要手工改卡**。
3. **战斗字段**：新增 `BattleState` 字段**必须在 `save.ts` 的 `BATTLE_FIELDS` 登记**
   （`satisfies Record<keyof BattleState, …>`，漏登记/多登记 **typecheck 直接报错**）；
   现值 **24 persist + 7 runtime**；`runtime` 必须写理由。`fxSeq` 是**派生字段**（按 fx 环尾部重算）。
4. **远端衰减只有一处实现** `distFactor`（`beamPowerFactor` 只是委托）；前提 = 所有 `falloff ∈ [0,1]`，
   由体检「远端衰减取值域契约」守住（56 处）。
5. **抽取只有 `pickOne` / `pickWeighted`**；两者**不做空表早退**——"空表/总权重 ≤ 0 也先抽一次随机数"
   是**刻意契约**（`nextRandom` 的 `count` 进存档，序列一挪位离线结算全变）。别好心加早退。
6. **近防炮两侧口径**（P-40）：敌方侧优先级按**我方机型 id** 查表；我方侧**按 `role` 兜底判档**
   （`sentry` → 0 / `assault` → 1）⇒ 敌方日后加机型**自动生效**。集火状态：敌方 `pdFocus`（按点防舰）
   / 我方 `mePdFocus`（按**武器槽**），两者都是 `runtime`、零迁移。
7. **公告纪律**：`packages/data/src/announcements.ts` 现 **29 条**；《敌人重做》在表里，
   **星系机制通讯批没有公告**（一号拟稿 → 船长「消档」）。

## 3. 挂着的未决项（**等船长裁决，别自己开工**）

> **⚠ 2026-09-12 新二号接手时的状态更新（船长三条裁决）**：下表 **P-41 三项** 与 **P-40 取舍 (b)** **已由船长收口**
> （原话「**P41已经完成**」·「**P40选择集火**」），**A2 三条收紧清单** 已「**清档**」（不再挂账）；
> **P-20a 尾巴已收口**（船长就三案选「**丙**」＝换系 ⇒ 蜂群机 = 动能 + 等离子；爆炸机型转「已备未挂」，
> 体检有预警行 + 硬守卫）。其余各行照旧等裁决。

| 项 | 内容 | 卡在哪 |
|---|---|---|
| **P-41 三项** | ①敌方近防炮要不要"有牙"（读数：打不掉我方无人机、占奖励 0.0%）②E/G 机群占比首版值是否微调 ③G 两张卡 A2 行零开火是否动射程 | 读数已固化 `docs/design/battle-data/p41-drone-readings-20260912.md`，等船长按读数定 |
| **P-40 取舍 (b)** | **集火是行为改动**（我方近防炮从"随机抽一架"→"锁定打到死"）⇒ 多架敌机时击落更快；**本批未跑读数**（船长口径：不跑标定除非他说跑） | 要不要正式读数，等船长 |
| **A2 三条收紧清单** | ①舰船/舰船蓝图价格"预警"升"阻断" ②物品基础卖价加比例带契约 ③改单一真源派生 | 船长裁定「丁：只写文档」，清单已留档在 `v9-market.md` §十二 |
| **甲案（dormant）** | 敌机**单次出击上限**（`droneLaunch`）已就位但休眠 | 船长「先都做，本轮只采用乙」 |
| ~~**P-20a 尾巴**~~ | ✅ **已收口（2026-09-12）**：船长就三案选「**丙**」＝换系 ⇒ `foe-drone-g-bee-pla` **已上舰**（天底封锁舰 2 架 = **动能 + 等离子**，卡总火力/总血守恒不动）；代价 = 爆炸机型 `-exp` 转「已备未挂」（体检预警行 + 「G 族机群必须含等离子系」硬守卫） | 已办结 |
| **美术/表现** | E 族核心舱段「接近完好」变体 · 备用机补位动画 | 未立项 |

## 4. 待办索引与关键落点

- **待办总表**：`docs/design/foe-pending-index-20260911.md`（P-xx 逐条，含状态列；P-40 已收口，其余按表内状态）
- **战斗/敌族**：`packages/core/src/combat.ts`（近防炮区 ~3000-3260 / 我方开火 ~3320+）· `data/foe-ships.ts` · `data/foe-drones.ts` · `data/anomalies.ts`
- **存档**：`packages/core/src/save.ts`（`BATTLE_FIELDS` 登记表在 `cleanBattle` 上方）· `state.ts`（`BattleState`）
- **平衡**：`packages/core/src/balance.ts`（`encounter.lowSecMax` 等）· 工具 `npm run battle:calibrate -- --std` / `battle:pd-tune` / `battle:drone-vs-gun`
- **体检契约**：`tools/content-check.ts`（通讯契约 · 机群与防空 · 主题件档位 · 远端衰减取值域 · 日板席位…）
- **文档**：`docs/roadmap.md`（变更记录，最新在最上）· `docs/glossary.md`（术语权威）· `docs/design/v9-market.md` §十二（价格权威）
- **本轮新增用例**：`tests/{pd-rules,pd-player-target,foe-gun-range,rng-pick,save-battle-fields,comms,b1,salvaging}.test.ts`

## 5. 血泪清单（本会话真踩过的坑，按发生顺序）

1. **与主线合并必冲突 `docs/roadmap.md`**：两侧都往"变更记录"顶部加条目 ⇒ 固定套路是**在 d2 侧并集解**
   （我的条目在前、一号的随后，只删三行冲突标记），解完**必做三条自检**（BOM/行尾 · 全仓往返解码 0 乱码 ·
   与**两侧父提交**逐行"父提交有的行还在"）。**别用编辑器手抄**，用 .NET `WriteAllText` + `UTF8Encoding`。
2. **write/edit 工具写出的是 LF**，仓内是 CRLF ⇒ 提交前扫一遍改动文件、归一 CRLF（否则 git 会警告、
   行尾自检也不过）。我在本会话被这个坑了三次。
3. **体检汇总行别撒谎**：我新加的契约汇总行一开始**在有错时仍打印"全部落在 [0,1]"** ⇒ 已改成按结果分支。
   写任何"汇总/读数"行时都要问一句"有错时它说什么"。
4. **负向验证没红也要如实登记**：`pickWeighted` 的 `lt`/`lte` 边界差是测度零（≈2⁻³²）⇒ 我把用例断言
   写死成"可观测范围内一致"，并在注释里写明"负向验证实测不会红"。**不许假装用例守住了它守不住的东西。**
5. **用例写错比代码错常见**（本会话 4 次）：把船采到满舱返航（不再算"就地作业"⇒ 无暴露）· 集火锁定**跨调用
   生效**（换距离前没清锁定）· `fxSeq` 是派生字段却按"值相等"断言 · `makeTestCtx()` 里**没有真船**导致
   `startBattleFor` 返回 null（要用 `buildSimContext()`）。写用例先问"夹具是否真支持这条路径"。
6. **子代理一律不许开**（§5.1）：审计、读码、实现、验证全部自己干；做不完就分阶段并如实汇报。
7. **报表三件套**：结论先行 · 已知取舍（含"没跑什么"）· 变化与验证读数；`_` 探针用完即删（本会话删过 1 个）。

## 6. 交接时先做这三件事

1. **核对基线**：`git -C d2 log --oneline -1` 与主树同点？`git status` 干净？四连复跑一次（约 1 分钟）？
2. **读**：本卡 → `AGENTS.md` → `roadmap.md` 顶部 5 条 → 按船长本次派活读对应设计稿。
3. **等派活**：§3 的未决项**不要自行推进**；船长是唯一验收人，任何改动走**四步闸门**（集中提问 → 中文
   设计总结 → **等显式确认** → 实现），并在实现后把决策落进 `docs/design/…`（标注状态）。

---

_起草：二号（`H:\大鲸鱼\Deepseek-EVE-d2`）· 2026-09-12 · 状态：**交接件，待船长转交新二号** ·_
_基线：`d2/workspace` = `main` = `e65deac6`（写卡时）· 四连全绿 · 工作区干净 · `_` 探针 0_
