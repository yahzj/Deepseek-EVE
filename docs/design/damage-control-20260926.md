# 损伤管制装置（DC）批 —— 2026-09-26

**状态：进行中**（功能已落码并推送；**待办：分类改判（等三号）** · 公告待批 · 三条遗留）

## 一、船长原话（照抄）

1. 「**添加新装备，舰船损伤管理MK1~MK3，低槽，效果是大幅增加结构的全伤害抗性，分别+30/40/50，
   且当舰船第一次结构低于1时，将结构恢复到1（避免一次死亡）。损管只能装备一件**」
2. 「**全名为损伤管制装置**」
3. 「**入侵「重复出击」也挂 0.5 撤退保险。损害管制好像什么都不消耗也不太合适吧？维修组件新增新的
   消耗品，损管修理组件，触发损管效果时需要消耗一份。**」
4. 「**改为1秒内结构锁定1会不会更复杂？**」→（裁定）「**丁**」
5. 「**损管修理组件应该消耗更高级的资源，价值提高到 50 万每件，蓝图基础 100 万。**」
6. 「**做完后给我一个存档 我要测试**」
7. 「**损管装置和修理装置是同一类型装备分类，不是装甲。三号正在修改，等他改完你修改损管装置的分类**」

## 二、已落地（提交 `873cd3e2` · `2a24aea1` · `140e483b` · `f35d74e6`；远端 `main` = `f35d74e6`）

| 项 | 现状 |
| --- | --- |
| 三件 | `mod-dc-1/2/3`：**`slot: 'armor'` ＋ `rack: 'low'`**（⚠ 见待办 1）· 结构层三系 `hullResistAdd` **+0.30/0.40/0.50** · CPU 30/38/46 · 稀有档 62 万 / 280 万 / 950 万 · 蓝图 124 万 / 700 万 / 2,850 万 · 无声望门槛 |
| 消耗品 | `repairkit-dc`「损管修理组件」：1 m³ · **50 万/件**（市场 55 万）· 蓝图 100 万（**不入"整批 ×2.2"，已登记 `BLUEPRINT_PRICE_OVERRIDES`**）· 材料 = 冥铁 500 ＋ 星髓 1,000 ＋ 重钨 1,500 ＋ 同位 1,000 = 82.5 万/批（料/价 50%） |
| 免死 | **丁案**：致死那一刻起 **结构锁定 1、持续 1 秒**（`DC_LOCK_MS`）；判定挂在**敌方→我方的唯一入伤口**（敌机群／敌光束／敌炮台三处共用 `cappedFoeDamage` 之后再过 `applyDcGuard`）⇒ **逐段夹伤**，同拍多段破不了；**每场一次**（`BattleState.dc[tag]`）· **启动消耗 1 枚组件**（仓库优先；关掉"从母港仓库取用"则走本舰货仓）· **没组件不启动**（只记 `core.combat.003`）· 不强制撤退 |
| 唯一 | `ModuleDef.unique`（V18.1 取消同类唯一后首次）＋ `fitModule` 拒装（`core.equipment.028`） |
| 撤退保险 | 入侵「重复出击」也挂 `hullEscapeFrac = 0.5`（判据 = 循环开着 ＋ 本场 `foeGalaxyId` 是被占星系） |
| 观感 | 日志 `core.combat.002/003` ＋ 战报尾巴（远征胜/败/弃船三处已接）＋ 战斗内提示走既有 `battle.notices` |
| 用例 | `tests/damage-control.test.ts` **8 例**（窗口 · 只一次 · 组件不足 · 唯一 · 入侵循环保险 · 战报尾巴 · **随档往返**） |
| 工具 | `make-test-save.ts` 新增注入器 **`dc`** ⇒ `docs/test-saves/test-save-dc-20260926-092228.json`（主控低槽第 3 位就地换成 MK3，原 `mod-hullrep-2` 退回装备库）；README 有同名条目 |

## 三、待办 1：**分类改判（等三号）**（船长第 7 条）

- 现状：三件都是 **`slot: 'armor'`（装甲类）＋ `rack: 'low'`**；船长裁定应归**与「修理装置」同一类型**。
- 「修理装置」现在 = `mod-hullrep-*`：**`slot: 'support'`（支援件）＋ `rack: 'mid'`**；`support` 家族按约定
  **必须显式标注 `rack`** ⇒ 若直接对齐，本线将是 **`slot: 'support'` ＋ `rack: 'low'`**（低槽不变，
  与船长第一条令「低槽」不冲突）。
- **等三号把分类方案落地**（他们可能新增一个 slot 值或重排支援件分组）⇒ 我按他们的方案改这三件的 `slot`
  （若他们动的是 `labels.ts` 的 slot→rack 映射或新增 `ModuleSlot` 值，我这边只改数据；core 侧由他们出）。
- **改前已核不受影响的两处**：① `equipment.stackingOf` 对 `hullResistAdd` 的判定走
  `{ group: 'gap', kind: 'hull-…' }`（**与 slot 无关**）⇒ 改分类不会把本件标错收敛组；
  ② `unique` 判据只看标记、不看 slot ⇒ 唯一性照旧。
- 改完要跑：`typecheck` · `content:check`（slot/rack 契约）· `l10n:check`（若新增 slot 名要补表中英）·
  验收档重生成一份（`npx tsx tools/make-test-save.ts dc`）。

## 四、待办 2：公告

待审稿：`docs/design/announcement-draft-20260926-damage-control.md`（`ann-damage-control-20260926` ·
标题「新装备：损伤管制装置」· tag 内容 · 五条）＋ 备选第 6 条（重复出击自动脱离）**等船长定**；批准后才写入
`announcements.ts`。

## 五、待办 3：三条遗留（已报备船长）

① AI 副船战报那一行（ai 战报是链式 id 拼装，加一段要动链）；② 装配预设/读档"裁掉多余件"（本件是新物品，
旧档/旧预设不可能有两件，装配层已从源头拦住）；③ 没另做"锁定中"状态灯（改走 `battle.notices`）。
