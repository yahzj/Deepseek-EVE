# 敌舰配置表 · A 族试点批次（2026-09-11 · 设计总结，待船长确认）

> **船长裁决（2026-09-11）**：
> ① **舰级给绝对值** ② **卡上用修正** ③ **允许混编** ④ **先试点**
> ⑤ 追加需求：「**我希望低安至少要有一个海盗族。目前都处于非低安区**」
>
> 状态：**设计总结（第 2 步）→ 等船长确认后实现（第 4 步）**。未动任何数据。

---

## 一、低安现状诊断（船长的判断已被证据坐实，且比预想更严重）

### 1.1 A 族六张卡全在 高安/中安，低安一张都没有 ✅ 判断正确

| 卡 | 威胁 | 星系 | 安全等级 | 区划 |
|---|---|---|---|---|
| 边境海盗前哨 | 12 | 柯尔边境 | +0.5 | 高安 |
| 碎晶带劫匪通缉 | 20 | 碎晶带 | +0.2 | 中安 |
| 信标猎手悬赏 | 22 | 灯塔长廊 | +0.4 | 中安 |
| 灰霾伏击团清剿令 | 28 | 灰霾带 | +0.4 | 中安 |
| 赤潮劫掠舰队 | 34 | 红环航道 | +0.1 | 中安 |
| 蜃影导航劫持令 | 48 | 蜃影星系 | 0.0 | 中安 |

### 1.2 更严重的一层：**低安的伏击遭遇里也没有海盗**

- `encounters.ts:56` 原文：「伏击者来自**事发星系可见的悬赏敌群**池（hidden 遭遇模板不算）」。
- 低安十一张卡的族系 = **C 异形 4**（深渊之门45 / 裂谷58 / 星髓72 / 噬口80）· **D 守墓 3**（坟场88 / 虚海88 / 穹顶96）· **E 巨构 2**（泰坦60 / 奥罗62）· **G 流亡 2**（烬火42 / 天底66）。
  ⇒ 在低安被伏击，来打你的是**异形 / 古舰 / 巨构 / 流亡者**，**没有海盗**。
- 四张 `enc-pirate-*`（说明写着"低安遭遇模板"）**已退役**：`encounters.ts:9` 原文「旧 hidden 四档模板**退役**，仅作旧档遗留遭遇的战斗兜底」；且它们的 `galaxyId` 全指向 `galaxy-hub`（**sec=1 高安**），只是占位。

### 1.3 星图派系也是空白的

`Expedition.tsx:796-801`：「敌对派系归属——**由该星系的悬赏卡推导**」（`foeFamilyOf(a.id)`）。
⇒ 低安十个星系在星图上**永远不会显示"海盗"标签**。

### 1.4 ⭐ 杠杆：一张低安海盗卡点亮三条通道

因为「伏击者取自该星系可见悬赏敌群」+「星图派系由悬赏卡推导」，
**只要在低安放一张 A 族悬赏卡**，就同时得到：
**① 低安可接的海盗悬赏 ② 低安伏击遭遇里出现海盗 ③ 星图上该星系显示"海盗"派系标签**。

### 1.5 低安星系占用情况（放卡选址用）

| 低安星系 | sec | 现有卡 |
|---|---|---|
| **奥罗荒环** | **−0.2**（最接近中安的低安） | 奥罗武装残骸群 62（E 巨构） |
| 星髓迷宫 | −0.5 | 星髓虫群 72（C） |
| 天底静区 | −0.5 | 天底静区封锁 66（G） |
| 裂谷深带 | −0.6 | 裂谷畸变体猎杀令 58（C） |
| 深渊之门 | −0.7 | 深渊之门卫队 45（C）+ 泰坦残骸勘探 60（E）← **唯一已有两张的低安星系** |
| 烬火星区 | −0.7 | 烬火围攻战 42（G） |
| 星噬之口 | −0.9 | 噬口猎杀令 80（C） |
| 虚海边缘 | −0.9 | 虚海守望者 88（D） |
| 暗星坟场 | −1.0 | 坟场守墓人 88（D） |
| 穹顶墓园 | −1.0 | 穹顶守卫 96（D） |

**我的建议**：放 **奥罗荒环（sec −0.2，威胁段 50~55）** —— 它是"海盗由中安向低安渗透的第一站"，
语义与位置都最顺；且该星系已有 E 族，加一个 A 族会让星图那一片显示两族，信息更丰富。

---

## 二、试点批次范围（**7 张卡：A 族现有 6 张 + 新增低安海盗 1 张**）

### 第 0 步 · 修掉两处结构性缺陷（顺带，试点必做）
1. **族系单一真相源**：删掉 `shipArt.tsx` 里 22 行硬编码 `FOE_FAMILY`，`foeFamilyOf` 改读**数据侧** `anomaly.foeFamily`
   ⇒ 顺带修掉 `演习场靶机` 的分脑（美术侧 B / 数据侧空）。
2. **战术显式化**：把靠默认值生效的 5 张（`烬火42/回音52/天底66/虚海88/穹顶96`）补成显式声明
   （`combat.ts:823` 的 `?? 'orbit'` 是个静默陷阱，已在本会话抓到一次）。

### 第 1 步 · 新建 `packages/data/src/foe-ships.ts`：敌舰配置表（A 族试舰级）

| 舰级 id | 舰级名 | 定位 | 设计要点 |
|---|---|---|---|
| `foe-pirate-skiff` | 海盗快艇 | **杂鱼** | 血薄、速度快、单发低；靠**数量**站住 |
| `foe-pirate-raider` | 劫掠巡洋舰 | 中坚 | 现有 A 族中段的对应物 |
| `foe-pirate-warlord` | 海盗头目舰 | **头目** | 血厚、单发高、命中好；挂**精锐**词缀 |
| `foe-pirate-scrap` | 缴获改装舰 | 装备差的体现 | 数值偏低但带**爆炸副系** |

字段：舰级 id / 名 / 族 / 血（绝对值）/ 结构·装甲·护盾分布 / 三系抗性（可空）/ 速度（绝对值）/
单发 / 装填 / 命中 / 射程带 / 远端衰减 / 近盲带 / 伤害构成 / 默认战术 / 默认词缀。

### 第 2 步 · 六张现有卡改为"引用舰级 + 修正"（**这一步做成零难度变化**）

> **关键取舍**：把**现有实测数值原样搬成舰级基准**，卡上只写编成与修正
> ⇒ 这一步**纯属数据重组，难度一点不动**，跑一遍全表实测应当**逐张一致**。
> 在这个"零变化基线"之上，再按您的 A 族设定调整数量/头目/速度 —— 两次改动分开验收，出问题好定位。

### 第 3 步 · 新增低安海盗卡（满足第 ⑤ 条需求）

编成（**正是"头目强大带一堆杂鱼"的样板**）：
```ts
waves: [
  { units: 3, composition: ['foe-pirate-skiff'] },   // 杂鱼群先上
  { units: 1, composition: ['foe-pirate-warlord'] }, // 头目压阵
]
```

### 第 4 步 · 契约与工作台
- `content-check` 新增三条：**舰级引用有效性** / **族→允许战术范围** / **战力校验**（按舰级反算实际战力与卡面威胁比对，偏差超阈值报错）。
- `tools/content-schema.ts` + `content-export/import` 工作簿与 `content-csv/enemy-roster.csv` 增加"舰级 / 编成 / 混编"列。

---

## 三、涉及文件

`packages/core/src/types.ts`（`AnomalyDef` 改造 + 新 `FoeShipDef`）·
`packages/core/src/combat.ts`（`createFoeSpecs` 改为按舰级建档 + 支持混编）·
**新增** `packages/data/src/foe-ships.ts` · `packages/data/src/anomalies.ts`（7 张卡）·
`apps/desktop/.../shipArt.tsx`（族系改读数据 + 新增舰级的形与色）·
`tools/content-check.ts` · `tools/content-schema.ts` · `tools/battle-calibrate.ts`（`--csv` 列）·
`docs/glossary.md`（舰级/混编术语登记）· 本设计稿转"已确认"。

**其余 19 张卡本轮不动**（C/D/E/G/F/B 族留到试点验收后）。

---

## 四、待确认（请您拍板）

| # | 问题 | 我的建议 |
|---|---|---|
| ① | **低安海盗卡放哪个星系？** | **奥罗荒环**（sec −0.2，海盗向低安渗透的第一站） |
| ② | **威胁段定多少？** | **50~55**（与低安现有的 42/45 衔接，不抢 58 裂谷的位置） |
| ③ | **卡名？** | 待您给（我不自拟玩家可见的专名） |
| ④ | **A 族舰级用 3 个还是 4 个？** | 4 个（快艇 / 巡洋舰 / 头目舰 / 缴获改装舰） |
| ⑤ | **"头目"要不要实装"精锐"词缀**（代码里已预留未实装） | 要——正好在试点里实装，头目名号一眼可辨 |
| ⑥ | **六张现有卡这一步要不要做成"零难度变化"？** | **要**（先纯重构、再调特色，分开验收） |
| ⑦ | **四张退役的 `enc-pirate-*` 怎么处置？** | 建议**登记为 A 族并留着当兜底**（它们已是退役状态），或**删除**（我不自决） |

**请确认**（尤其 ①②⑥ 三条定了我就能开工）。
