# V9 设计：市场系统（已实现）

> 状态：已按用户确认的 V4/V5 方案实现（核心 + 数据 + 界面 + 存档迁移 + 平衡工具）。
> 关联旧设计对话要点：冲突订单**内部消化不瞬消**、冲击动量**叠加无上限**（用户最后确认）、
> 市场只分两栏（常驻供应 / 稀有订单）、所有旧"空间站商店/直购"并入市场、
> 蓝图改消耗品制（学习一次永久可造，重复书只能卖）。

## 一、目录与稀有度

- 两栏：
  - **常驻供应（common）**：矿石/矿物走"库存池"模型；单件平价品（MK1 装备、低级蓝图、
    掘洞级、基础 AI 核心 25k）随刷随买。
  - **稀有订单**：rare（MK2 装备、MK2 蓝图、造船蓝图、鲸吞级、开拓级）低频出现；
    exotic 限定奇货（鲸王级、鲸王蓝图、伽马/贝塔/阿尔法核心）同栏展示、极低概率 + 天价。
- 订单寿命：常驻 20 分钟 / 稀有 9 分钟 / 限定 4 分钟（`balance.market.orderLifeMs`）。
- 价格锚点：单件商品 basePrice = 旧商店/蓝图价（供应价 = 平价 ×1，收购价 = demandMultiplier，
  防套利）；池商品 basePrice = 旧空间站收购价（均衡价），收购平价、供应微溢 6%（保价差）。

## 二、价格机制（池商品核心玩法）

- 均衡价 L = basePrice × 库存压力 × (1 + 冲击动量)。
- 库存压力：q > poolTarget 淤积 → 收购压价（倾销砸价）；q 接近 0 → 供应断货、价格走高；
  池以 30 分钟半程向目标回归（模拟站内吸收/补给）。
- 冲击动量（隐藏数值）：单窗口净成交量超过参考量 ×2（参考量 = 每窗口稳态流量的一半）
  触发一次 ±5% 偏移，**可叠加、无上限**（已按用户确认）；以 6 分钟半程衰减。
  输出价钳制在 basePrice 的 20% ~ 500% 之间（仅防归零/溢出，不是叠加上限）。
- 池商品买卖价差：收购 ≈L / 供应 ≈L+max(1, 6%)（整数价差保底 1 ISK，避免低价商品
  取整后买卖同价）。

## 三、订单簿与内部消化

- NPC 簿：npcBuy（收购单，玩家卖出的对手盘）/ npcSell（供应单，玩家买入的对手盘）；
  单 = {价, 量, 到期时刻}，每 60 秒窗口（tickMs）补单/过期/撮合，离线大推进同样适用。
- 玩家市价卖出 = 立即按价从高到低吃穿收购簿；**吃穿后的剩余自动按边际价转限价卖单**
  （货已锁定 escrow，不浪费）。
- 玩家市价买入 = 立即按价从低到高吃穿供应簿；簿吃穿后提示可稍等补给或挂限价买单。
- 挂单：买入不预扣（成交时才扣钱、货直接入对应库存）；卖出先锁货（escrow）。
  撤单：物品退回物品仓库 / 装备退回装备库 / 蓝图退回蓝图书架 / 舰船退回机库（保留耐久）。
- **内部消化**：NPC 新订单与簿上对侧订单"撞价"（新收购价 > 最低供应价等）时不瞬时成交，
  进消化队列，按 digestPerWindow=15%/窗口 随时间推进消化——大冲突订单消化得慢（不瞬消）。
- 订单成交即时把货/钱入账并写"trade"日志；成交完毕的挂单自动清理。

## 四、蓝图消耗品制（V9 重大改动）

- 旧"买蓝图一次永久可造"（blueprints: string[]）→ v8→v9 迁移**无损平移**为
  `learnedRecipes`（已学会配方）。
- 新流程：市场 npcSell 蓝图书 → 买入进 **blueprintStock（蓝图书架）** → 「学习」消耗 1 本
  → 永久可造；重复蓝图书只能市价回卖（半价收购线），**不存在复制刷书**。
- 界面：工业页新增「蓝图书架」（学习/出售），制造台卡片 = 未学会（市场求购，现货自动买书学习；
  无货自动挂收购单）/ 已学会（开始制造）。

## 五、商店并入市场（旧入口全部删除）

- 空间站商店（舰船直购）→ 舰船页改为「舰船市场」：现货买入；无现货自动挂收购单（到货入机库）。
- 蓝图直购（工业页购买按钮）→ 市场购书 + 学习（见上）。
- 基础 AI 核心直购 → 市场常驻供应 25k（AI 页按钮改为市场买入，缺货自动挂收购单）。
- 装备/物品/矿石所有"卖出"按钮 → 按市场收购簿即时成交（声望加成规则保留：物品类 +1%/点，上限 +15%）。

## 六、可售约束

- 舰船：非驾驶中、无 AI 任务、货仓空、无装配才能出售；市价卖或挂限价卖单。
- 物品：矿石/矿物、装备（装备库）、蓝图（书架）按自然库存取货；AI 核心不可回卖。

## 七、存档（v8 → v9）

- version 9；新增 market / orders / escrowItems / escrowShips / learnedRecipes / blueprintStock；
  移除 blueprints。迁移 8 已实现；normalizeState 全字段容错。
- 老档读取后市场为空 → 首次推进（引擎 ensureMarket）按目录铺开盘簿、开盘价即均衡价。

## 八、实现与验证清单

- packages/core：state/types/balance/market（引擎）/ industry（卖/买船）/ ai（核心购买）/
  manufacturing（学习制）/ engine（advanceMarket 接入）/ save（迁移+容错）/ index 导出。
- packages/data：marketCatalog（31 张商品卡）、SimContext.marketGoods。
- apps/desktop：市场页（两栏 + 我的挂单）、舰船页改市场现货、工业页蓝图书架、App 底部菜单第 8 项。
- 测试：core 126 项通过（新增 market.test 14 项：开盘簿/市价单/剩余转挂单/撤单退回/冲击/消化/
  蓝图学习回卖/卖船约束）。
- 验证：typecheck 全绿；npm run build 通过；Electron 冒烟跑通（真实 v8 档在线迁移 v9、
  市场开市并随窗口走动）；npm run balance 24h 三策略体检正常输出（市场价略低于旧固定价，量级一致）。

## 九、已知取舍（记录）
- ~~离线大推进时市场在"到达时刻"开盘，首段离线的挂单要等在线首窗口~~ → 已修复（A1，2026-09）：
  未开市档的开盘基准 = 离线起点（advanceMarket 传 openAtGameMs），整段离线按 60s 窗口推进
  （订单过期/池回归/补单/我的挂单撮合全部生效）。
- 冲击动量按"单窗口净量"触发一次（一次最多 ±5%），持续砸盘则逐窗口叠加（无限，衰减兜底）。
- 市场价格 = 界面第二栏数据源：收购/供应双价 + 涨跌指示 + 站内库存显示。

## 十一、桌面体验更新（2026-09）
- 市场页：手动挂单表单（挂单买/挂单卖）、有货冒泡、稀有单剩余寿命徽章、常驻栏补给倒计时、每栏搜索+类型过滤；
- 日志面板：可右滑隐藏（细把手展开）+ 六类日志开关（偏好存 localStorage）；
- 离线简报：启动时若离线结算 ≥1 分钟，浮出可关闭简报卡（时长/钱包/收获/技能/事件摘录），统计在桌面胶水层完成，不进存档。

## 十、贸易税与市场技能（2026-09-03 用户确认：销售税 5%、不收挂单费、双技能合计减税 80%）

- **税制**：只设单一「贸易税（销售税）」。玩家每一笔**卖出成交**（市价卖出、限价卖单成交、
  市价/挂单卖船成交）按成交金额征税 5%，入账时自动扣除；买方、挂单/自动转挂单动作一律免费
  （参考 EVE 销售税；**取消经纪人费/挂单费**——用户拍板）。
- 到账 = 毛额（声望加成计入）− round(毛额×税率)；日志写"税后入账 X ISK（贸易税 Y ISK）"。
- **减免技能**（数据驱动，新增「贸易」组，rank 2）：
  - 会计学 `accounting`：每级 −8% 贸易税；
  - 贸易谈判学 `trade-negotiation`：每级 −8% 贸易税（用户建议改名，替代"经纪关系学"）；
  - 减免**线性相加**：税率 = 5% × (1 − 8%×(会计学级+贸易谈判学级))；双满 → 1%（合计减免 80%）。
- 配置在 `balance.market`：salesTaxRate / taxSkillAId / taxSkillBId / taxCutPerLevel（**不进存档**，
  无版本迁移）；结算点：settleSell 与 sellAtMarket（含卖船路径，统一税后净入账语义）。
- 平衡影响（24h 体检税后）：A≈1.0M / B≈3.0M / C≈3.1M ISK（税约 -5~9%，温和的 ISK 回收阀）。
- UI：市场页顶部显示当前税率与技能减免（会计学/贸易谈判学等级实时折算）。
