# 舰船插件（2026-09-26）

状态：**进行中** —— 设计已获船长确认（「确认」），落码中；归档按 AGENTS.md §8
（船长验收 + 合入 main 后当批做：关键内容并入 roadmap/词典 → 删本文件 → 重跑 `docs:index`）。

## 船长原话（照抄）

> 「**在工业-组装机的门类筛选中，添加舰船插件的新分类，打算依靠黑匣来生产舰船插件。舰船插件是一种类似
> 装备的东西，同样装备在舰船上，但是不可拆卸，不可替换。装有插件的舰船无法放入舰船仓库。玩家打捞自己的
> 舰船残骸时，总能回收舰船插件。**」

> 「**选项2 的基础上，改为越低级的船插槽越多。T1~T5分别为5/4/3/2/1个插槽**」

> 「**找章鱼人用声望兑换，玩家完成入侵后根据贡献获得一定量声望。旧的声望门槛改为计算玩家获得的累计声望。
> （反正40声望时候全部功能都解锁了）**」

> 「**玩家回收按插件数量直接回收成黑匣。**」

> 「**最大护盾/装甲/结构的增加为固定值，不是百分比**」

> 「**五①增加被选中的权重，②单纯增加CPU③不吃。没有闪避插件，是减少被攻击的权重的插件。
> 护盾固定值改为+40。装甲固定值+80，但是减少20m/s的速度.结构固定值+60。协处理插件，CPU+80。
> 推进插件固定速度+40m/s**」

## 一、13 件插件与数值（船长逐条定）

| # | 插件 | 效果 | 数值 | 落点 |
| --- | --- | --- | --- | --- |
| 1 | 护盾强化插板 | 最大护盾 ＋固定 | **+40** | 新字段 `shieldHpAdd` |
| 2 | 装甲强化插板 | 最大装甲 ＋固定，代价 速度 −20 m/s | **+80 / −20** | 新字段 `armorHpAdd` ＋ **复用** `speedPenaltyPct` 的思路（绝对值另立字段） |
| 3 | 结构强化插板 | 最大结构 ＋固定 | **+60** | 新字段 `hullHpAdd` |
| 4 | 中层舱段插件 | 中槽 ＋1 | +1 | 新字段 `midSlotsAdd` |
| 5 | 下层舱段插件 | 低槽 ＋1 | +1 | 新字段 `lowSlotsAdd` |
| 6 | 协处理插件 | CPU 预算 ＋ | **+80** | **复用** `cpuBonus`（协处理器 MK1/2/3 = 25/35/45） |
| 7 | 火力强化插件 | 单发伤害 ＋ | +12% | 复用 `damageBonusPct` |
| 8 | 瞄具插件 | 命中 ＋ | ×1.12 | 复用命中乘区 |
| 9 | 推进插件 | 速度 ＋固定 | **+40 m/s** | 新字段 `speedAddMps` |
| 10 | 射程插件 | 射程 ＋ | +25% | 复用 `weaponRangeBonusPct` |
| 11 | **靶标插件** | **增加被选中权重** | ×2 | 新字段 `targetWeightMul` |
| 12 | **隐匿插件** | **减少被选中权重** | ×0.4 | 同上 |
| — | ~~闪避插件~~ | **船长改判：不做**（"没有闪避插件"） | — | — |

## 二、性质与槽位

- **就是装备**（同一套 `ModuleDef`），只是**不可拆、不可替换**；
- **插件槽绑船型**：**T1=5 · T2=4 · T3=3 · T4=2 · T5=1**；只有 T1~T5 有，无档船（采矿/货舰）**无插件槽**；
- **不可拆的落法**：装配页插件槽**无卸下按钮**；装了插件 ⇒ **不许进舰船仓库、不许挂市场卖**；
  **唯一失去途径 = 船被打沉**（现状：船沉时装配件一并遗失、不回装备库）。

## 三、产线 · 声望 · 市场 · 打捞

- **图纸**：**找章鱼人用声望兑换**；声望来自**击败入侵后的贡献占比**，单场 **0~15 点**；旧门槛数值**一律不动**；
- **生产**：组装机造，**每件消耗黑匣 ×1**；组装机门类筛选**新增「舰船插件」一档**；
- **市场**：插件与图纸**当前不可见**，但**市场卡照做并备注**（`unreleased` ＋ 备注），日后要开只删标记；
- **打捞**：自己残骸里的插件**按数量直接回收成黑匣**；与"其余件逐件掷骰"**两本账互不干扰**。

## 四、口径明细（船长裁决）

| 问题 | 裁决 |
| --- | --- |
| 被攻击概率增加 | **增加被选中的权重**（不是"更容易被打中"） |
| 被攻击概率减少 | **减少被选中的权重**（**不是闪避加算**） |
| CPU 增加 | **单纯增加 CPU 预算**（不是占用 CPU 作为代价） |
| 多件递减 | **不吃**——不可拆的固定件，装 5 件就是 5 件全额 |
| 固定值分档 | **不分档**，全船同一套固定值（已知后果：T1 收益远大于 T5，船长接受） |

## 五、不做 / 边界

- **不做**：闪避插件（船长改判）· 插件的拆卸与替换 · 插件上市 · 无档船的插件槽；
- **不动**：现有高/中/低槽位布局与槽位契约 · 旧声望门槛数值 · 黑匣现有掉落（旗舰击毁必掉 ×1）；
- **唯一碰存档结构的地方**：两条 +1 槽会把某类槽位顶到 **8 格**，而清洗器现为 `slice(0, 7)` ⇒ 同步抬上限。

## 落码清单（待填实测读数）
