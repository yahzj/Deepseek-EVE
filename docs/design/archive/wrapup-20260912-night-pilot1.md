# 会话收尾归档（2026-09-12 深夜 · 一号 · 主树 `main`）

> **给接手人**：本卡记的是**本会话（一号，深夜班）**做完的两件事、船长裁定、以及**挂着的坑**。
> 开工顺序仍是：本卡 → `AGENTS.md` → `docs/roadmap.md` 顶部 → 按派活读对应设计稿 → 最后看代码。
> ⚠ 本会话**没有**写 `AGENTS.md` / `development-conventions.md` / 词典 / roadmap
> ——船长 2026-09-12 明确「**我没强调过要写入约定或规则的，不要写入**」，故决策一律只落在
> **设计稿 + 提交信息 + 本卡**三处。

---

## 0. 一言以蔽之

本会话处理了两件事：

1. **赏金任务「分波分流」议题 → 船长裁定回退**：赏金维持现有配置、只按威胁加强敌方；
   **敌方火力钳制也暂时关闭**（`balance.battle.foeDpsCap` 由 150 改为 **0**）。**已落码**。
2. **一次性图纸（新机制）→ 实现完成、待验收**：玩家**无法学会**、**只能制造一次**的图纸。
   **已落码 + 10 条用例 + 负向验证**，但**当前数据里一张都没有**（船长「只做机制」）。

---

## 1. 裁定一：赏金回退 + 火力钳制关闭（**已落码**）

**船长原话**：「回退到赏金维持现有配置，仅按照威胁加强敌方。并且火力钳制也暂时关闭。」

**落点**：`packages/core/src/balance.ts` 的 `foeDpsCap: 150 → 0`（写 0 = 关闭；机制与用例都保留，
恢复只需写回 150）。类型注释（`types.ts`）与用例（`tests/foe-dps-cap.test.ts`）同步。

**顺带查实的一件旧账（重要，别再被它误导）**：
> 该钳制**不是空转的防线**——它开着时**真在压卡**：`巨构核心勘探令 3 档` 单波舰体 159.4 DPS 被压到
> 150；单条目更重的卡（穹顶守卫、虚海守望者、核心舱母舰条目）也都在被压。
> 而代码注释与旧用例里那句「**27 张卡全部未越线 ⇒ 零行为变化**」是**测试夹具的假绿**：
> `foe-dps-cap.test.ts` 的 `hullDps()` 夹具**只统计第一条条目**，多条目卡（穹顶 = 静滞卫舰 ×2 +
> 守墓长舰 ×1）被漏算。**已就地登记为待办**（见 §4），**未修**——修它会让 7 张多波卡的实际难度改变
> （属平衡改动，等船长排期）。

**关于「分波分流」**：本会话一度按其规则（单波 >150 DPS ⇒ 新增波次、按各波 DPS 占比抽走、
末了升序、新波从种族塞最低级敌人并吃威胁加成）写完了实现，但**遇引擎耦合阻塞**（分波会改变整卡
单位数 ⇒ 引擎的「多舰补偿 `2N/(N+1)`」随之改变**每个条目的实收单发**，归一后仍有 12%~78% 系统漂移），
按纪律**实验代码全部回退**（不留半成品）。相关审核读数与裁定见
`docs/design/handoff-20260912-bounty-wave-split.md`（该卡已被船长裁定作废，见其顶部声明）。

---

## 2. 裁定二：一次性图纸（**已实现 · 待验收**）

**船长原话**：「能否实现一次性图纸？既玩家无法学会，只能制造一次的图纸」

**七条口径（船长逐条裁定，实现与用例逐条对齐）**：

| # | 裁定 | 落地 |
|---|---|---|
| 1 | **甲**：仍是"书"，只能造一次 | 进蓝图书架、**不可学习**（引擎拒 + 界面不给按钮）；到组装机直接用 |
| 2 | **乙**：已永久学会同名配方 ⇒ 书**不用也不消耗** | `recipeCapability` 返回 `learned`：一本不动、不记名额 |
| 3 | **甲**：**开工那一刻**扣书 | 与材料同一时刻扣除；取消/失败**不退**（书 = "一次制造资格"） |
| 4 | **乙**：**可卖** | 本批不加出售限制（能否卖取决于有无市场卡；本批也没加市场卡） |
| 5 | **只做机制** | 未加来源、未上市场、未改掉落 |
| 6 | **不进**逆向研究 | 引擎天然不涉及（不在已学会表）；`content:check` 新增契约拦"出现在碎片配方表" |
| 7 | **成品完全一样** | 产出路径完全复用现有制造链（属性/入库/可卖逐字相同） |

**状态：`待验收`**——船长 2026-09-12 明确「一次性图纸先不验收」，且**当前数据里没有任何一张**
一次性图纸，故**界面上看不出效果**（要看效果需先在数据里挂一张）。

**关键实现点（供复核）**：
- 可用性**单点** = `manufacturing.recipeCapability`（`ok` / `learned` / `exhausted` / `no-book`）；
  判定顺序是「**书在架上就可用**」优先于「名额已用尽」——否则**重复获得的图纸会被误拒**
  （本批用例抓到过这个洞）；
- 名额记录 = `GameState.spentOneTimeRecipes?: string[]`（**老档缺省 = 空，零迁移**）；
- **一次性图纸不循环**（"只能一次"）⇒ 该卡的「循环制造」开关整块不渲染；
- 舰船蓝图同样支持（`ShipBlueprintDef.singleUse?`）。

**验证读数**：typecheck ×4 绿 · core **107 文件 / 1070 用例**全绿（新增 10 条）·
`content:check` 绿（含两条新契约）· 桌面 build 绿 ·
**负向验证**：关掉"开工扣书" ⇒ 4 条红；去掉"名额用尽"判定 ⇒ 1 条红（各自还原后全过）。

---

## 3. 本会话改动文件一览（两条提交）

**提交 A · 火力钳制关闭（含赏金卡留档）**
`packages/core/src/balance.ts` · `packages/core/src/types.ts` ·
`packages/core/tests/foe-dps-cap.test.ts` · `docs/design/handoff-20260912-bounty-wave-split.md`

**提交 B · 一次性图纸**
`packages/core/src/types.ts` · `state.ts` · `save.ts` · `manufacturing.ts` · `market.ts` · `index.ts` ·
`apps/desktop/src/renderer/src/panels/Industry.tsx` · `tools/content-check.ts` ·
`packages/core/tests/helpers.ts` · `packages/core/tests/one-time-blueprint.test.ts`（新增）

---

## 4. 挂着的坑与待办（**别自己开工，等船长**）

| 优先 | 项 | 说明 |
|---|---|---|
| **P1** | **`hullDps()` 夹具盲点** | `tests/foe-dps-cap.test.ts` 只统计第一条条目 ⇒ 多条目卡漏算，曾把"全表未越线"变成**假绿**。**修它 = 动 7 张多波卡的难度**（属平衡改动），故本会话**只登记、不修**。 |
| **P2** | **一次性图纸的来源** | 船长本批「只做机制」⇒ 来源（高级箱 / 赏金奖励 / 活动）**待裁定**；定下来只需往数据里挂 `singleUse: true` 的蓝图 + 一条产出渠道。 |
| **P2** | **一次性图纸实机验收** | 数据里挂一张即可看效果（界面上现在完全不可见）。 |
| **P3** | **赏金分波分流** | 卡片与审核读数已留档；若将来重启，需先裁「引擎补偿口径」（逐波算 vs 加绝对单发入口）。 |
| **P3** | 推送欠账 | 本会话两条提交**只本地**；`origin/main` 落后若干条，**船长说推才推**（直连不稳走 7897 代理）。 |

---

## 5. 推送与更新状态

- 本会话**未推送**任何东西（推送闸门：只本地 commit，船长说推才推）。
- 接手后核对：`git log --oneline -3` · `git rev-list --count origin/main..main` · 三树状态。
- GitHub 直连时通时断：有效做法 = **密 retry**（每 3~10 秒一次、最多 10~20 次），
  远程核对用 `git ls-remote origin refs/heads/main`。

---

_起草：一号（`H:\大鲸鱼\Deepseek-EVE`，主树 `main`）· 2026-09-12 深夜 · 状态：**收尾归档件** ·_
_基线：typecheck 四包 0 错 · core **107 文件 / 1070 用例**全绿 · `content:check` 绿 · 桌面 build 绿 ·
工作区 `_` 探针 0 · 改动文件全部 **CRLF + 无 BOM**_
