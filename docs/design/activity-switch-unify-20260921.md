# 主控活动切换：现状统计 ＋ 统一方案（2026-09-21 船长令）

> **状态：进行中**（口径已定 · **第 1 步「判据单点」已落码**；第 2 步「9 个入口接线 ＋ 统一文案 ＋ 9×9 矩阵用例」待做）
>
> **船长原话（照抄）**：「**关于主控切换不同活动，现在依旧很混乱。有的需要玩家先取消当前活动才能切换，
> 有的又可以直接切换，有的还需要警告后切换。你帮我统计下。我希望统一为能够直接切换（自动取消当前活动），
> 像长途运输这种高收益高周期的才加一个警告。**」
>
> **同日追加与三答**：「**处在战斗中的时候也设置为不可取消**」· 「**1 写进警告。2 丢弃。3 纳入。**」
> ⇒ ① **远征 / 快递投送写进"警告"档** ② **亲自开炉/开线的取消 = 当前那批进度丢弃** ③ **换驾驶与进洞纳入同一条单点**。
>
> **本件范围**：主控活动切换的**判据统一**（判据单点 ＋ 各入口接线 ＋ 文案统一 ＋ 矩阵用例）。
> **不做**：各活动自身的玩法数值 · 活动栏的展示排版 · AI 副船任务（不占主控）。

## 一、主控活动全表（9 项 ＋ 3 个特殊态）

| # | 活动 | 状态字段 | **可自动取消？** | 取消代价（现行语义） |
|---|---|---|---|---|
| 1 | 采矿 | `mining` | ✅ `miningHalt` | 即时返港，**本趟货留在船上**（到港卸） |
| 2 | 打捞 | `salvaging` | ✅ `salvageHalt` | 即时返港，**本趟残骸留在船上** |
| 3 | 长途运输 | `hauling` | ✅ `haulingHalt` | 即时返港停靠本段出发站，**本段报酬拿不到**（报酬到站才结） |
| 4 | 扫描虫洞 | `wormholeScan` | ✅ `wormholeScanHalt` | 进度保留（下次补扫剩余窗口） |
| 5 | 掩护巡逻 | `standby` | ✅ `cancelStandby` | 召回（船回港） |
| 6 | 亲自开炉精炼 | `refineRuns`（worker=pilot） | ✅ 取消该 run | 料与进度按炉子既有"停炉"语义（**待核**：进度是否保留） |
| 7 | 亲自开线制造 | `manufacturingRuns`（worker=pilot） | ✅ `cancelManufacturing` | 同上（**待核**） |
| 8 | **远征** | `expedition` | ❌ **在途不可停** | 只能等它打完/召回（`recallExpedition` 是"召回"，但要打完当前场） |
| 9 | **快递投送** | `sideTasks.deliver` | ❌ **投送不可取消** | 只能到站结算 |
| — | **战斗中**（**2026-09-21 船长追加**：「**处在战斗中的时候也设置为不可取消**」） | `expedition.battle` / `standby.battle` / `wormhole.run.battle`（＋低安遇袭的在途一场） | ❌ **不可取消** | 只能打完（洞内另有"不可撤退"那条既有裁定） |
| — | 人在虫洞里 | `wormhole.run` | ❌（是"主控被洞占着"） | 只能撤离/打完 |
| — | 换港返航 | `transit` | ❌（瞬时到站） | 等抵达 |
| — | 星图扫描 | （**已不占主控**） | — | 无人扫描艇，2026-09-15 起不占活动位 |

## 二、现状行为统计（三档并存 ⇒ 就是"混乱"的来源）

**A 档 · 硬拒（必须先手动停掉当前活动）** —— **绝大多数入口都是这一档**：

| 想开始 ↓ / 当前是 → | 采矿 | 打捞 | 长途运输 | 远征 | 掩护巡逻 | 亲自开炉/开线 |
|---|---|---|---|---|---|---|
| 采矿 | — | 拒 `core.mining.007` | 拒 `core.mining.005` | 拒 `core.mining.008` | 拒 | 拒 |
| 打捞 | 拒 `core.mining.006` | — | 拒 `core.state.005` | 拒 `core.salvaging.004` | 拒 `core.salvaging.005` | 拒 |
| 长途运输 | 拒 `core.hauling.007` | 拒 `core.hauling.008` | 拒 `core.hauling.001` | 拒 `core.hauling.009` | 拒 `core.hauling.010` | 拒 |
| 远征 | 拒 `core.expedition.02x` | 拒 `core.expedition.023` | 拒 `core.expedition.021` | 拒 | 拒 | 拒 |
| 掩护巡逻 | 拒 `core.location.0xx` | 拒 | 拒 `core.location.001` | 拒 | 拒 `core.location.023` | 拒 |
| 亲自精炼 | 拒 `core.state.033` | 拒 `core.state.014` | 拒 `core.state.005` | 拒 | 拒 `core.state.035` | 拒（同族互斥） |
| 亲自制造 | 拒 `core.state.033` | 拒 `core.state.014` | 拒 `core.state.005` | 拒 | 拒 `core.state.035` | 拒 |
| 快递投送 | 拒 `core.sideTasks.010` | 拒 `core.sideTasks.011` | 拒 | 拒 | 拒 `core.sideTasks.012` | 拒 |
| 换港返航 | 拒 `core.location.008` | 拒 | 拒 `core.location.001` | 拒 | 拒 | 拒 |

**B 档 · 直接切（自动停掉当前活动）** —— 只有两处：

| 入口 | 自动停掉什么 | 出处 |
|---|---|---|
| **换驾驶**（舰船页换当前驾驶船） | 长途运输（`cancelHaulingOnSwitch`）＋旧船的采矿/打捞转"自动返航"（`retireMiningShip` / `retireSalvageShip`） | `shipyard.changeShip` |
| **进洞**（准备页「进入虫洞」） | **扫描虫洞 / 开采 / 打捞 / 长途运输**（四个 `*Halt`） | `wormhole.ts`（2026-09-15 船长令） |

**C 档 · 警告后执行（两段确认）** —— 只有长途运输这一族（2026-09-20 船长令）：

| 断点 | 行为 |
|---|---|
| 活动栏「停止运输」· 长途运输页「停止运输」· 舰船页「换驾驶」· 虫洞「进洞」 | 第一下只警告（「确认中断（拿不到本趟报酬）」），第二下才执行 |
| 进洞准备页 | 琥珀色警告条「进洞会先自动停掉：扫描虫洞、开采、打捞、长途运输」 |

**⇒ 一句话统计**：现状是 **"除换驾驶与进洞外，一律硬拒"**；而"换驾驶/进洞"这两处又是**静默自动停**
（只有进洞准备页有一行警告）；**警告态只存在于长途运输**。三档混用，且同一件事（切活动）在不同页面
表现不同 —— 与你说的"混乱"完全一致。

## 三、统一方案（按你的口径：能直接切就自动取消；只有高收益高周期的给警告）

### 规则（三条，覆盖全部入口）

1. **可自动取消的当前活动**（上表 1~7）⇒ 开始新活动时**自动停掉它**，写一条日志
   「已自动停止〔××〕（原因：开始〔××〕）」，然后照常开始新活动。**不再要玩家先手动停**。
2. **高收益 / 高周期的那几项**（**建议 = 长途运输**；见 §四 待你定是否加 远征/快递）⇒
   **两段确认**：首击弹警告（写清"将停止××、损失 = 本段报酬拿不到"），二击执行——沿用 2026-09-20 那套
   `ui.Hauling.033/034/035` 的样式与话术，**不新造交互**。
3. **不能自动取消的**（远征在途 / 快递在途 / 人在洞里 / **战斗中** / 换港返航途中）⇒ **仍然拒**，但文案统一改成
   "这趟不能中断：等它结束 / 先撤离 / 先打完这一场"（现在散在 9 个 errorId 里的措辞统一收口）。
   ⚠ **战斗中**（**2026-09-21 船长追加**）的判据单点建议 = `cannotInterruptReason(state)`：把三处战斗槽
   （`expedition.battle`（远征实时战）· `standby.battle`（掩护巡逻战）· `wormhole.run.battle`（洞内战））
   ＋在途低安遇袭场次一起判 ⇒ **战斗一律不可被"切活动"打断**（连站内的"亲自开炉/开线"也拒：主控正在打仗）。

### 落地要点（实现层面）

- **单点**：新增 `core/activity.ts` 的 **`haltCurrentFor(state, ctx, next)`**（把"要停谁、代价是什么、日志怎么写"
  收成一处，返回 `{ halted: ActivityKind | null; warn?: string }`），9 个 `start*` 入口改为调用它 ⇒
  **以后加减活动只改一处**（现在的 30+ 处 busy 检查是散在各模块的）。
- **警告名单**：`WARN_ON_HALT: ActivityKind[]`（初值 `['hauling']`，见 §四）。
- **文案**：统一日志 1 条 ＋ 统一警告头 1 条 ＋ 各活动"代价"短语（采矿/打捞"本趟货留在船上"·
  长途运输"本段报酬拿不到"· 扫描虫洞"进度保留"· 掩护巡逻"召回"· 开炉/开线"停炉/停线"）⇒ 约 8~10 条新 id（中英）。
- **界面**：启动按钮在首击遇到"会中断"时改成确认态（与长途运输那四点同款）；不能中断的仍置灰＋理由。
- **用例**：`tests/activity-switch.test.ts` 新增一张**矩阵用例**（9×9：可自动取消的必须切成功且日志正确 ·
  不可取消的必须被拒且理由统一），把这张表钉死。

## 五、落地进度（2026-09-21）

**第 1 步 · 判据单点（已完成）**：新增 `packages/core/src/activityGate.ts`
- `MainActivityKind`（9 项）· `AUTO_HALT_KINDS`（6 项，直接切）· `WARN_KINDS`（**长途运输 ＋ 远征 ＋ 快递投送**）·
  `INTERRUPTIBLE`（远征/快递 = false ⇒ 警告之后仍是拒）· `HALT_COST` / `KIND_LABEL`（统一文案的两个词表）；
- `mainActivityOf(state)`（现在占主控的是哪一项）· **`cannotInterruptReason(state)`**
  （**战斗中**：远征实时战 / **主控船的低安遭遇战** / 洞内战 → 拦；副船遇袭**不**拦（不占主控）· 人在洞里 · 换港返航途中）；
- **`gateMainActivity(state, next)`** → `{ action: 'ok' | 'halt' | 'confirm' | 'reject', current?, message?, interruptible? }`；
- ⚠ **只出判据、不执行停机**（停机仍由各入口用既有取消函数落地 ⇒ 语义一份、不造模块环）；
- 用例 `tests/activity-gate.test.ts` **6 例全绿**（三档分类 · 副船遇袭不拦 · 三处战斗槽 · 洞里/返航）。

**第 2 步 · 待做（下一步就干）**：
1. **9 个入口接线**（`mining` / `salvaging` / `hauling` / `wormholeScan` / `standby`(goStandbyAt) / `refine` /
   `manufacturing` / `expedition` / `deliver`）：把各自的 busy 检查块换成 `gateMainActivity(...)`：
   `halt` ⇒ 调既有取消函数（`miningHalt`/`salvageHalt`/`haulingHalt`/`wormholeScanHalt`/`cancelStandby`/
   `stopRefineRun`/`cancelManufacturing`）＋写一条统一日志；`confirm` ⇒ 由界面两段确认；`reject` ⇒ 统一理由。
   ⚠ 顺序纪律：**先过完"新活动自己的前置校验"再调 gate**（免得"先停了玩家的活、再说开不了"）。
2. **换驾驶 / 进洞**（现已自动停）改走同一条 gate（进洞准备页那行琥珀警告保留）。
3. **统一文案**：日志 1 条 ＋ 警告头 1 条 ＋ 拒绝 3 条（战斗/洞里/返航）≈ 8~10 条新 id（中英）。
4. **9×9 矩阵用例**：把"可自动停的必须切成功且日志正确 · 不可中断的必须被拒且理由统一"钉死。

## 六、待你定

> 已全部定完（警告名单含远征/快递 · 开炉开线丢弃进度 · 换驾驶与进洞纳入单点）。**无待定项**。

---

_维护：本件是方案（未动码）。你点头后：先落 `activity.ts` 单点 → 9 个入口改调用 → 统一文案 → 矩阵用例 →
四闸门；随后按 §8 归档。_
