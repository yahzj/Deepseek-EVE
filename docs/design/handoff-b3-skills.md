# B3 技能挂接（2026-09-05 船长拍板：打捞相关技能一并完成 → 已实施）

> 状态：**①/② 已实施 ✅，③ 已确认关闭 ✅**（船长指令直接落地；数据/文案与引擎触点由二号一并完成）。
> 技能目录：66 → **67**（新增「残骸回收学」）；core 379 tests 全绿；typecheck ×4 + content-check 绿。

## 背景

B3 残骸打捞-回收全链已落地（星系密度 → 打捞作业/任务 → 回收开箱）。
本文件定方向与数值口径，实施记录如下。

## ✅ 已实施

### ① 漂流物打捞学（探索组 · rank3 · 现存技能扩展）
- 文案更新：`残骸打捞与战利品收集：远征缴获的物资数量与残骸打捞量每级 +12%（主控与 AI 同享）`；
- 引擎触点：`pullOneWreck` 的捞取体积当量 ×(1 + 0.12×级)——主控打捞作业与 AI 打捞任务共用该函数，单点生效；
- 影响：只缩短积攒时长，不改回收炉时 EV 锚（`salvage:econ` 行不变）。

### ② 残骸回收学（工业组 · rank3 · 新增）
- `salvage-recycling`，文案：`精炼炉残骸回收（开箱批）的周期优化：批周期每级 −4%（手动与 AI 核心驱动同享，至少保留 60%）`；
- 引擎触点：`startRecycleRun` 批周期 = 25s ÷核心效率 后再 ×(1 − 0.04×级)（Lv5 = −20% → 20s/批；60% 为长线下限）；
- 提示：回收周期缩短会抬高满级炉时产能（1440→1800 m³/h → 保底 EV ≈ ×1.25）——这是技能投资回报，基线锚仍按无技能口径体检。

### ③ 压缩技术不扩展到残骸 ✅（确认关闭，无代码改动）
- 乙案下残骸计数 = 体积（m³ 即数量），压缩语义自相矛盾；运输“压舱感”由打捞船/货舰分工承担。

## 回归
- `salvage:econ` 基线行（无技能）不变；打捞量加成/回收周期加成已有确定性测试（core 379 全绿）。

