# 2026-09-20 交接开放项：船长逐条裁定 ＋ 落地（2026-09-22）

> **状态：进行中**（船长 13 条裁定已全部收到：**3 条落码、10 条"不动"**；本批自测全绿，待船长验收。
> 按 §8，归档时把 `docs/roadmap.md`「2026-09-20 一号交接开放项」里对应条目逐条关闭，然后删本件。）
>
> **船长原话（照抄）**：「1甲。2甲。3甲.4甲。5甲。6甲。7不动。8不动。9.保持。10不动，因为已有之前的使用旧工具
> 之前要更新的护栏。11，不动。如果其不影响游戏运行。12没有问题就不动。13不动。」
>
> **范围**：`docs/roadmap.md` §2026-09-20 一号交接开放项 的 13 条（当批上报、当时未获答复）。
> **不做**：这 13 条之外的任何事项；本批不含公告（内容类改动按 §12 判定为内部修整，不单独成条）。

## 一、逐条裁定与落地

| # | 事项 | 裁定 | 本批动作 |
|---|---|---|---|
| 1 | 「寻找人类」发布日志的口吻 | **甲 = 改写中英** | ✅ 落码见 §二 |
| 2 | 老公告追改 / 另发公告 | **甲 = 不动**（历史记录照旧） | 无 |
| 3 | 「重要任务」页未发布态占位 | **甲 = 不加占位** | 无 |
| 4 | 老档补采集器（起手道具） | **甲 = 收口时补发当前那条的起手道具** | ✅ 落码见 §三 |
| 5 | AI 打捞被拒文案 | **甲 = 补「去装配页」** | ✅ 落码见 §二 |
| 6 | 进洞 vs 长途运输 | **甲 = 保持**（自动停运 ＋ 两段确认 ＋ 琥珀警告条） | 无 |
| 7 | 「第一次采集原矿」跳转按钮 | **不动**（仍跳「星图 · 矿带开采」，正文已指路） | 无 |
| 8 | 虫洞深层盘重建 | **不动**（已中招的层不自动重建） | 无 |
| 9 | 谜质 T4「工业多核调度」定价 | **保持**（30/60/90/120/150 谜质 · 合计 18 亿信用点） | 无 |
| 10 | `tools/` 纳入 typecheck | **不动**——船长理由：**已有"用旧工具之前要先更新"的护栏**（工具头部的版本自检登记 ＋ `tools:audit`） | 无 |
| 11 | 死工具 `tools/content-validate.ts` | **不动**（前提 = 不影响游戏运行；已核：见 §四） | 无 |
| 12 | 里程碑"彻底追溯" | **不动**（现行下界口径无问题） | 无 |
| 13 | 链条耗时比 4:2:1 | **不动**（方案留档，未实行） | 无 |

## 二、文案改动（两条，中英同改）

| id | 旧 | 新 |
|---|---|---|
| `core.onboarding.001` | ◆ 重要任务发布「寻找人类」：完成方法未知——先在这座城市活下去，再慢慢打听。 | ◆ 重要任务发布「寻找人类」：完成方法未知——**立足已稳，该往更深处打听了**。 |
| `core.ai.008` | 该副船没有打捞器：先在**其高槽**装上打捞器（MK1/2/3）再派打捞任务。 | 该副船没有打捞器：先到**「装配」页**给它装一台（MK1/2/3）再派打捞任务。 |

- **为什么改第 1 条**：这条日志的发布时点 = **序章结束 ＋「第一次」顺序段 11 条全完成**（扫描/采矿/精炼/
  悬赏/维修/打捞/技能/AI/生产/挂单/造船）——那时玩家已经建成第一艘自造船，旧文案却是"刚落地"的口吻
  （活下去、慢慢打听），与处境不搭。新文案保留「打听」这个动作词、去掉"活下去"的早期框架。
- **为什么改第 5 条**：主控那条 `core.salvaging.003` 早已写「先到「装配」页…」，副船这条漏了指路；
  "其高槽"对玩家也不够明确（哪艘船、哪个页面）。
- 两处**中文兜底串**（`core/onboarding.ts` / `core/ai.ts`）与 `packages/data/src/l10n/table.ts` 的 zh/en
  **同步改写**（core 侧字面量 = 无表时的兜底，两边必须一致）。

## 三、老档收口补发起手道具（engine · 零字段零版本变更）

**病灶**：起手道具只在"某一条**轮到**时"发（`firstRewards.claimFirstTask` 收尾那一次点击），而 v30→v31 的
**老档一次性收口**只补"判据已满足却没点过"的那几条 ⇒ 老档若正卡在**带起手道具的那六条**之一、且自己那条
判据还没满足，收口循环一条都不走，它自己那条的起手道具就**永远拿不到**。

六条与各自起手：采集原矿→强化采集器 MK1 · 打捞残骸→打捞器 MK1 · 指派 AI 副船→基础 AI 核心 ·
生产→150 三钛＋50 类铁 · 第一条船→沙猫级蓝图 · 虫洞→采集器＋打捞器各一台。

**改法**（`core/engine.ts`，`state.firstTaskAutoClaim === true` 那一段）：**先**
`grantStartRewardsForCurrent(state, ctx)` 一次（自带 `started` 去重、重复调用不叠加），**再**跑原来的
"已满足 ⇒ 照点击走完"循环。⇒ **零新增存档字段、零版本变更**（复用 v30→v31 的那一次性标记）。

**边界（船长已知情并选定此口径）**：已经在旧代码下升过 v31 的档救不回来（那批档的标记已删）——
它们的起手道具只能自购（采集器市场 31,200 ISK 有售、`bp-miner-1` 可造）。

**实测（船长手头 4 个档逐个查过）**：
- 3 个 09-20 老测试档（v30）：15 条「第一次」**全部完成** ⇒ 收口循环无条目可走，补发也是空操作（无损失）；
- 船长在玩的档（`save-20260921-201652.json`，v30）：已完成扫描/采矿/精炼/悬赏/维修/打捞 6 条，
  **当前轮到「第一次学习技能」**——该条**没有起手道具** ⇒ 船长本人不受此病灶影响；
- 受影响的形态 = 停在上述六条之一且未满足判据的老档（例如"扫完描还没挖第一趟矿"）。

**用例**（`tests/first-tasks.test.ts` ＋2 例）：① 卡在「第一次采集原矿」的老档 ⇒ 走一拍就拿到采集器 MK1、
写 `started`、删收口键，再来几拍不叠加；② 当前那条**已满足**的老档 ⇒ 起手道具 ＋ 完成奖励（1,000 单位橄榄岩）
＋ 推进到下一条，三件事一次走完。

## 四、第 11 条的核对结论（"不影响游戏运行"这个前提成立）

- `tools/content-validate.ts` **不在任何 npm script** 里（`content:check` 跑的是另一个文件 `tools/content-check.ts`）；
- `packages/*/src` 与 `apps/desktop/src` 对它**零引用**（那些命中只是注释里提到工具名）；
- `tools/` **不在任何 tsconfig / 构建入口**里（桌面构建产物不含它）；
- ⇒ 它既不参与游戏运行、也不参与任何闸门，只是一个**已失效的死文件**（`ITEM_KIND_ORDER` 全仓没有导出）。
  按船长裁定：**留着不动**。

## 五、本批读数（2026-09-22）

- `npm run typecheck` 四包 0 错；
- `npm run test -w @whale/core` **182 文件 / 2079 例全绿**（本批 ＋2 例）；
- `npm run l10n:check` ✅ · `npm run content:check` ✅ · `npm run flow:newgame` ✅；
- 无界面/样式改动 ⇒ 未跑 `ui:rot-check`；无存档结构改动 ⇒ 未跑 `save:migrate`、未升存档版本。

## 六、情报信风格重写 ＋ 英文本地化交给三号（2026-09-22 同日追加）

**① 情报信重写**（船长追问：「**情报信的文案你没根据我的第一次任务的文本风格重新写吗？**」）
⇒ 13 封通讯的正文 **39 行按船长的任务文本风格整篇重写**（原先只对齐了顺序与「随信附」几行）。
- 落点：`packages/data/src/firstTaskMessages.ts`（口径写在该文件头注释里）；
- 风格六条：共事口吻（我们 / 你）· 少破折号 · 情报后带一句建议 · 页面与技能名加「」· 物品用内容表正式名
  · 三行分工 = 情报 / 建议 / 随信附（写清拿去干什么）；
- 顺带纠三处：采集器 → **强化采集器 MK1**（正式名）·「三钛 / 类铁」→ **钛钢合金 / 银纹超金属**
  ·「新船入机库待命」→ **新船先入舰船仓库，在「舰船」页转入舰队**；主题「市场挂单」→「**市场交易**」
  （与该条判据放宽为"任何一笔市场交易"对齐；**任务标题**要不要跟着改仍待船长定）；
- 读数：`content:check` ✅（30 条消息契约全过）· `l10n:check` ✅ · `typecheck` 0 错 · core **182 / 2079 全绿**。

**② 英文本地化 ⇒ 交三号**（船长原话：「**英文本地化依旧交给三号**」）

本批新查明的一处缺口（归三号，一号不动）：
- **通讯消息整类没有英文**：`ctx.messages`（**30 条**，含 13 封「第一次」情报信 ＋ 开场简报 ＋ NPC 来信）
  的 **主题 / 正文 / 前往提示** 只有中文 ⇒ 英文界面下通讯页整封显示中文；
- 判据：`packages/data/src/l10n.ts` 的 `localizeCtx` 覆盖了 `ships / modules / items / skills / blueprints /
  shipBlueprints / galaxies / belts / stations / commsFactions / matterTech / travelEvents`，**没有 `messages`**；
- 做法建议（供三号选型）：**甲（推荐）** 新增一张 `EN_COMMS_MESSAGES`（`Record<messageId, { subject; body: string[]; hintText? }>`）
  ＋ 一个 `overlayMessages()`（`overlayList` 同款思路，但字段含数组），在 `localizeCtx` 里加一行 ⇒ 改动集中在数据层；
  **乙** 走 id 映射表（`msg.<id>.subject` / `msg.<id>.body.N`）——更贴现行 id 制，但要改 `core/comms.ts`
  与 `CommsPage` 的读取方式，面更大；
- 13 封情报信的中文刚在 2026-09-22 重写（提交 `da8764ce`）⇒ **英文请照新版中文译**，术语以 `docs/glossary-en.md` 为准。

---

_维护：本件记录 13 条裁定的落点与读数 ＋ 情报信重写与三号本地化交接。船长验收后按 §8 归档：把 roadmap
那份开放项清单里对应的 13 条逐条关闭（其中 10 条标"船长 2026-09-22 裁定不动"即可销账）＋ 把 §六② 并入
roadmap 的「三号交接开放项」→ 删本件 → `npm run docs:index`。_
