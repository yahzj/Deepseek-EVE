# 弹药定名批：全族统一到《系+弹药》· 工作文档

> **状态：进行中**（三号 · `verify40` · 2026-09-16）
> 归档指向：船长验收 + 合入 main 后，关键内容并入 `docs/glossary.md`（弹药相关词条）与 roadmap 一条，
> **本工作文档删除**。

## 一、船长原话（照抄）

「**将弹药 爆破导弹改名为爆破弹药，其他的弹药也进行类似的改名**」

确认时另问「范围怎么定」⇒ 船长选 **甲**：物品名 + 蓝图名 + 武器/技能/手册/教程/界面里的旧名引用
**一并改**，并把旧名加进「陈旧术语契约」防回潮。

## 二、映射表（现行数据）

| 现名 | 新名 | id（**一律不变**） |
|---|---|---|
| 动能弹 | **动能弹药** | `ammo-kinetic-l` |
| 爆破导弹 | **爆破弹药** | `ammo-explosive-l` |
| 能量弹药 | 不动（已合规） | `ammo-plasma-l` |
| 动能弹 MK2 | **动能弹药 MK2** | `ammo-kinetic-2` |
| 爆破导弹 MK2 | **爆破弹药 MK2** | `ammo-explosive-2` |
| 能量弹药 MK2 | 不动 | `ammo-plasma-2` |
| 动能弹 / 爆破导弹 生产线蓝图（各 2 张） | **动能弹药 / 爆破弹药 生产线蓝图** | `bp-ammo-*` |

**id 与市场 key 都不动**（市场行的 `key = refId = 物品 id`，名称直接读物品表 ⇒ **市场零改动、存档零迁移**）。
**“导弹架”这个武器名不动**——被打出去的东西才是弹药（船长的口径正是"导弹 → 弹药"这一层）。

## 三、落码清单（21 处）

**数据（`packages/data/src`）**
1. `items.ts`：6 件弹药（3 名称 + 3 描述）＋ 段头沿革注释；
2. `blueprints.ts`：6 张生产线蓝图（4 名称 + 4 描述）；
3. `modules.ts`：轻型动能炮「吃动能弹」· 轻型导弹巢「发射爆破导弹」· 抗性件「动能弹的噩梦」·
   掠袭破片炮「与所耗的爆炸弹无关」（**旧的散装叫法「爆炸弹」一起对齐**）＋ 弹药段头注释；
4. `skills.ts`：导弹发射学「爆破导弹单发伤害每级 +5%」；
5. `ships.ts`：掠食者级说明「爆破导弹威力额外加成」＋ 同处注释；
6. `tutorialSteps.ts`：教学结算「动能弹 ×120」；
7. `marketCatalog.ts`：消耗品池标定注释里的旧名（改前读数行）；
8. `announcements.ts`（**已发布公告 2 处**，沿用 2026-09-16「一起改」的裁定）：
   「首门炮台与动能弹 120」→ 动能弹药 · 「动能炮击与**爆破导弹类**敌舰」→「与**导弹类**敌舰」
   （后者说的是敌舰武器形态、不是那件弹药，改"导弹类"避免与旧名同形）。

**引擎（`packages/core/src`）**
9. `onboarding.ts`：任务②奖励日志「动能弹 ×N」→ 动能弹药（＋两处注释）。

**界面（`apps/desktop`）**
10. `ui/shipInfo.tsx`：`turretAmmoText()` 后缀「弹」→「**弹药**」（炮台配弹文本）· 导弹架短效文案「爆破导弹 · 射程…」→「爆破弹药 · 射程…」·
    弹头行 chip 标签「爆破导弹」→「爆破弹药」· 生成式 chip `${DMG_LABEL[x]}弹` →「…**弹药**」；
11. `pages/FitPage.tsx`：同上 chip 生成式 + 硬编码标签；
12. `panels/BattleScreen.tsx`：战斗内武器行弹种 chip 同款改法；
13. `panels/ImportantTasks.tsx`：重要任务卡「动能弹 ×120」；
14. `panels/Handbook.tsx`：伤害克制矩阵三行改用**伤害系名**做功名（动能 / 高爆 / 能量），弹名进正文——
    顺带清掉两处散装旧称（「爆炸弹」·「等离子弹」）；
15. `panels/Expedition.tsx`：两处悬停「动能弹拆盾 ×1.5…能量弹各层均衡」→ 动能弹药 / 能量弹药。

**用例（内部，随改保持自洽）**：`tests/helpers.ts` · `ammo-mk2.test.ts` · `wormhole-gear-mechanics.test.ts` 的弹药 fixture 名。

## 四、加的黑名单（防回潮）

`tools/content-check.ts` 的「陈旧术语契约」新增 3 组：`动能弹(?!药)` · `爆破导弹` · `等离子弹`。
⚠ **`动能弹` 必须带负向断言 `(?!药)`**：它是新名「动能弹药」的**前缀**，裸写会把新名一起判红
（这条写成注释钉在代码里；反证见下）。
⚠ 「等离子弹」是 **V18B-2 改名时的存量漏网**（当时更名"能量弹药"但手册还写着旧名）——本轮一并清掉。

## 五、验证

- [x] **反证（各自精准 · 复原即全绿）**：把物品名临时退回「动能弹」⇒ 体检只报
      `items.ts:458（'动能弹'…）→ 旧弹药名（2026-09-16 起：动能弹药）` **1 处**，
      全仓其余「**动能弹药**」**未被误报** ⇒ 负向断言生效；
- [x] `npm run content:check` ✅ —— **146 个源文件无已改判旧称（黑名单 16 组 · 白名单 1 条专名）**；
- [x] `npm run typecheck` 四包 **0 错**；
- [x] `npm run test -w @whale/core` **156 文件 / 1684 例全绿**；
- [x] `docs:index` · 桌面 + 网页双 build ✅（产物实证：见 §六 收尾记录）。

## 六、残留旧名的分布（如实登记）

改完后全仓仍有旧名，**逐条核过，全部落在"非玩家可见"处**，故有意保留：

1. **源码注释里的沿革记录**（如 `items.ts` 段头「高爆弹更名…」「动能弹 → 动能弹药」、
   `modules.ts` 段头、`onboarding.ts` 两行注释、`state.ts` 序章注释、`combat.ts` 的 per-gun 注释）——
   契约只扫文本节点，注释是合法的历史说明；将来谁把它抄进界面，黑名单会拦；
2. **测试标题/注释**（`v18b.test.ts` · `t24.test.ts` 等）——内部语言；
3. **历史文档**（`docs/archive/*` · 各版设计稿 `v10*`/`v17b`/`v18b-missiles` · `docs/test-saves/README.md`）——
   按 §8 历史卷宗不改写；**现行词典与 roadmap 的旧名走归档那一步**（见 §七）。

## 七、归档待办（船长验收 + 合入后当批做）

1. `docs/glossary.md`（**现行口径，必须改**）：
   - line 165「弹药键」条：`（动能弹/爆破导弹/能量弹药）`→ **（动能弹药/爆破弹药/能量弹药）**；
   - line 167「换装卡的弹伤倍率」条：「动能弹 6 / 爆破导弹 7」→ 新名；
   - line 168「弹药 MK2 / 弹药档位」条：「动能弹 MK2/爆破导弹 MK2」→ 新名；
   - line 249「价格权威关系」条读数行「最小 0.857（动能弹 6/7）」→ 新名；
   - 弹药词条补一句 2026-09-16 定名口径（《系+弹药》· id 不变）；
2. `docs/roadmap.md`：本批一条（映射 + 契约 + 反证）；
3. 本工作文档删除 + 重跑 `npm run docs:index`。
