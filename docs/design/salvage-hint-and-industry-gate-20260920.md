# 打捞门槛提示（＋去装配入口）与工业页解锁改挂「第一次操作精炼炉」（2026-09-20）

> **状态：进行中**（已实现，闸门全绿，待船长验收）
>
> **船长原话（照抄）**：「**打捞需要打捞器的提示，添加让玩家去装配的提示。并且解锁工业界面要和
> 第一次精炼的任务挂钩一起解锁。**」
>
> **本件范围**：两件——① 打捞页在"没装打捞器"时给一行提示 ＋ 一个**直接落到装配页**的按钮，并把相关三处
> 文案点到「装配」；② 工业页的解锁口径改成**跟「第一次操作精炼炉」一起开**。
> **不做**：打捞门槛本身（core 判据不动）· 装配页/工业页的其它内容 · 任务顺序（见 §四 待你定）。

## 一、打捞需要打捞器 ⇒ 提示 ＋ 去装配

**改了什么**

| 文件 | 改动 |
|---|---|
| `apps/desktop/src/renderer/src/pages/MapPage.tsx` | 新增可选 prop `onGotoFit(shipId)`；`SalvageTab` 在**驾驶船一台打捞器都没装**时（判据 = core 的 `salvagerCyclesOf(...).length === 0`，与出发门槛同一把尺）在页首插一行提示 ＋「装配」按钮 |
| `apps/desktop/src/renderer/src/App.tsx` | 给 `MapPage` 接上 `onGotoFit`（`setFitShipId(shipId) + changePage('fit')`，与舰船页那枚「装配」按钮同一落点） |
| `packages/data/src/l10n/table.ts` | 新增 `ui.MapPage.118`（提示正文，中英）；`ui.MapPage.049`「未装配打捞器（…）」由"舰船页"改指**「装配」页** |
| `packages/core/src/salvaging.ts` | 出发被拒的文案 `core.salvaging.003` 由"请先在舰船高槽装上打捞器"改成**"先到「装配」页给驾驶船的高槽装一台"**（`errorId` 不变，中英两列同步） |

**外观口径**：按钮与舰船页那枚「装配」同款（`app-btn is-small` ＋ `nav-fit` 字形 ＋ `ui.App.003`），
悬停沿用 `ui.FirstTasks.029`「前往「装配」」；提示行复用页内既有的 `app-dim app-inv-empty` 行样式（未自造新样式）。

## 二、工业页解锁改挂「第一次操作精炼炉」

**改了什么**（`packages/core/src/firstTasks.ts`）

- 新增一张表 **`UNLOCK_AT_TASK`**（跟某条「第一次」任务**轮到**一起解锁的页面）＝ `{ industry: 'first-refine' }`；
  `unlocked()` / `unlockNeedTitle()` 改成**两种口径**：先看 `UNLOCK_AT_TASK`（轮到即开），再看 `FIRST_UNLOCKS`（完成才开）；
  `FIRST_UNLOCKS` 里的 `industry: 'first-mine'` **删除**。
- 判据 = `FIRST_TASKS` 里排在 `first-refine` **前面全部**已完成（顺序解锁下"轮到它"的等价说法）。
- 提示文案（未解锁时点导航的那句 `ui.App.110`「尚未解锁：先完成「{p1}」。」）给的是**前一条**：
  即「第一次完成悬赏」——玩家当下真要做的下一件活。

**为什么不能按"该任务完成"解锁**：精炼炉就在工业页里 ⇒ 按完成后解锁会**死锁**（页面永远打不开、任务也做不完）。

**作废的旧口径**：2026-09-17 船长定的三页前置表里那句「**工业界面和相关任务则需要玩家先完成第一次采集矿物**」
（`FIRST_UNLOCKS` 头注原文）——它把"哪条任务"写死在表里；现在改成**由任务序推导**（跟精炼任务走）。

### 玩家可见变化

| 项 | 改前 | 改后 |
|---|---|---|
| 任务序（前 6 条） | 扫描 → 采矿 → **打捞 → 维修 → 悬赏 → 精炼** | 扫描 → 采矿 → **精炼 → 打捞 → 维修 → 悬赏**（船长三选②：精炼前移到采矿之后） |
| 工业页何时亮起 | 「第一次采集原矿」完成 | **同一刻**（采矿做完 ⇒ 精炼轮到 ⇒ 页面与那张卡**同时**出现）；判据改为"跟精炼任务走"，不再写死任务 id |
| 未解锁提示 | 「先完成「第一次采集原矿」」 | 同上（精炼的前一条＝采矿）——文案没变，含义变成"精炼那张卡快了" |
| 打捞页 | 只写"未装配打捞器" | 多一行提示 ＋「装配」按钮 |
| 「第一次打捞残骸」到步时 | 工业页已开（回收炉可用） | **同前**（精炼提前 ⇒ 工业页在打捞之前就开着，那条任务正文/情报信提到的回收炉不再"锁着"） |

## 三、验证

- `npm run typecheck` 四包 **0 错** · `npm run content:check` ✅ · `npm run l10n:check` ✅（新增 1 条 id，无死引用）·
  `npm run ui:rot-check` ✅ · `npm run build` ✓；
- `npm run test -w @whale/core` **180 文件 / 2,019 用例全绿**：
  - `first-steps-gate.test.ts` 解锁表用例按新口径重写：开局全锁 → 扫描后星图四项开 → **采矿做完工业页与
    「第一次操作精炼炉」一起开** → 市场另按"第一次生产"；提示文案钉「第一次采集原矿」，再往后的任务不影响工业页；
  - `first-tasks.test.ts`「顺序解锁」第 ③ 步改钉"采矿之后当前这条 = **第一次操作精炼炉**"（三选② 的任务序）。
- `npm run flow:newgame` **✅ 流程全部通过**：④ 采矿后两条新读数（「工业页解锁（与「第一次操作精炼炉」一起开）」＋
  「当前这条轮到「第一次操作精炼炉」」）· ⑤ 处注明**本脚本顺序 = 玩家顺序**（精炼前移后不再有"工具顺序≠玩家顺序"
  的偏差）· 收尾再钉一次"工业页仍是解锁态"。
- ⚠ 观感（打捞页那一行提示 ＋「装配」按钮的排布）**请船长过目**——我不自评。

## 四、待你定

1. ~~工业页要不要更早开~~ ⇒ **你已选②**：「把「第一次操作精炼炉」在任务序里前移到采矿之后」——**已实现**：
   `FIRST_TASKS` 里精炼从第 6 条移到第 3 条（打捞/维修/悬赏各顺延一位）。
   由此**一石三鸟**：① 工业页与精炼卡同刻出现；② 打捞那条到步时回收炉已可用（正文/情报信不再自相矛盾）；
   ③ `flow-newgame` 的脚本顺序变回**玩家真实路径**（采矿 → 精炼，无需再声明"本工具顺序≠玩家顺序"）。
2. 打捞的情报信/任务正文里那句"回收炉…"**现在没问题了**（工业页在打捞之前就开）⇒ 本条关闭，无需改动。
3. AI 打捞那条被拒的文案（`core.ai.008`「该副船没有打捞器…」）要不要也补一句"去装配"？（未做，等你一句话）

## 五、归档待办（§8）

- `docs/glossary.md`：① 打捞门槛那条补"面板给去装配的入口"；② 教程/「第一次」任务族的**页面前置表**改成
  "工业 ← 第一次操作精炼炉（轮到即开）"，旧句标作废；
- roadmap 一条（并进 2026-09-17 教程重做批，作为后续改判）。

---

_维护：本件是工作文档，验收后按 AGENTS §8 归档（关键内容并入 roadmap/词典 → 删文件 → 重跑 docs:index）。_
