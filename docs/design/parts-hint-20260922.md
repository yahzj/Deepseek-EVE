/**
 * **组装机 / 造船厂缺料提示：零件指去组装机**（**2026-09-22 船长报障 · 已落码**）。
 *
 * > **船长原话（照抄）**：
 * > ①「**组装机和造船厂需要零件时，提示不是去组装机，而是去市场。**」
 * > ②「**我的意思是，希望提示玩家去组装机生产零件，不要提示去市场。**」
 * > ③「**关于零件，高级零件依旧去相应的组装机。**」
 *
 * **范围**：只改「缺料那一行」的**提示与跳转**——不动任何数值、配方、蓝图获取方式、存档结构。
 * **不做**：`docs/roadmap.md` 的批次入册（等船长验收 ＋ 归档时一并做，见 §十五）。
 *
 * ## 一、改动
 *
 * | 位置 | 改前 | 改后 |
 * |---|---|---|
 * | 缺料行提示（组装机与造船厂**共用** `BlueprintCard`） | 两支：有精炼源 ⇒「⚒ 去精炼」；否则 ⇒「🛒 去市场」 | **三支**：有精炼源 ⇒「⚒ 去精炼」· **有产出蓝图（零件 14 种等）⇒「🏭 去组装机」** · 其余 ⇒「🛒 去市场」 |
 * | 点击落点（`IndustryPage.handleNeedMineral`） | 有精炼源 ⇒ 精炼炉该资源卡；否则 ⇒ 市场页 | **中间插一支**：有产出蓝图 ⇒ `setSec('craft')` ＋ `setCraftFocus(蓝图 id)`（零件图都在组装机那一栏） |
 * | 新增 core 单点 | — | `manufacturing.blueprintProducingItem(ctx, itemId)`：产物 → 蓝图反查，**按 `ctx` 用 `WeakMap` 缓存**（工业页上百张卡 × 每卡数行材料，逐行扫全表会变成 O(卡×行×蓝图)，见 §十四） |
 *
 * **高级零件按船长第 ③ 条：照样去组装机**（不管玩家手上有没有那张零件图——卡上本来就会给"求购图纸→市场"
 * 的下一步，不在这里另开分支）。
 *
 * ## 二、文案调整台账（§十三 · 按船长报障/裁定调整过的玩家可见文案）
 *
 * | 日期 | 位置（文件/元素） | 原 → 新 | 缘由 |
 * |---|---|---|---|
 * | 2026-09-22 | `packages/data/src/l10n/table.ts` `ui.Industry.154`（新增） | （新）→「🏭 去组装机」/ `🏭 To the assembly machine` | 船长②：缺料是零件时要指去组装机 |
 * | 2026-09-22 | 同上 `ui.Industry.155`（新增） | （新）→「「{matName}」由组装机造出（图纸：{p2}）——点击跳到那张零件卡」 | 同上（悬停说明；括号只放规格、不写原因，§十三.5） |
 * | 2026-09-22 | 同上 `ui.Industry.110`（改写） | 「「{matName}」**无法经精炼炉产出**——点击到市场购买」→「「{matName}」要到市场购买——点击跳转」 | 船长②：**去掉原因解释**（§十三.5）；该支现在只在"既炼不出也造不出"时出现 |
 *
 * 改动处均留可检索记号 `⟪文案调整 2026-09-22⟫`（`l10n/table.ts` 对应条目上方）。
 *
 * ## 三、验证
 *
 * - `typecheck` 四包 0 错 · core **183 文件 / 2092 例全绿**（新增 1 例：产物→蓝图反查与缓存）
 * - `content:check` ✅ · `l10n:check` ✅（新条目中英齐）· `ui:rot-check` ✅ · `build` ✅
 * - ⚠ **观感与手感请船长过目**（本条属界面行为改动，未自行截图当结论）
 */
