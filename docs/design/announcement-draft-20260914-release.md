# 更新公告 · 待审稿：本次推送批次（2026-09-14 一号 起草）

> **状态：待船长审核** —— 按 AGENTS.md §7 与约定第十二章：开发/商讨内容不得直接写进
> `packages/data/src/announcements.ts`；**船长逐条批准后**才落库，并**与代码一起推送一次**。
> 写法自查：全文第三人称「玩家」· 无第一/第二人称代词 · 无开发/验收话语（复测/校准/对齐/批次/落码等）。

## 〇、本批推送的范围（事实）

- 本地 `main` 领先 `origin/main`（上次抓到的远端 = `f8509d2f`）**277 个提交**。
- 已入库的最后一条公告 = **`2026-09-13-haul-security-reward`**（长途运输重定）⇒ 它随本批一起上线。
- 之后（09-13 / 09-14）的**玩家可见**改动尚未公告 —— 即下面 7 张卡。
- ⚠ **虫洞本体仍处施工期**（入口走调试开关 + 数据走未上线闸门）⇒ 默认**不进本批公告**，见 §三。
- ⚠ **网络**：起草时 `git fetch origin` 失败（`Recv failure: Connection was reset`）⇒ **推送前需先确认能连上远端**。

---

## 一、拟落库的 7 张卡（请逐条裁量：发 / 不发 / 改口径）

### 卡 1 · 金钱栏与货币单位

| 字段 | 值 |
|---|---|
| `id` | `2026-09-14-wallet-credits` |
| `title` | 金钱栏移到左侧出港上方，货币单位统一为「信用点」 |
| `tag` | 系统 |

1. 金钱栏从顶栏移到**左侧栏「出港」上方**，与舰船状态窗同族样式，一眼可见；
2. 货币单位全面改为「**信用点**」（原 ISK 字样全数替换，数值不变）；
3. 金额按**位数分级显示**：一万以下千分位全写、到万显示「万」、到亿显示「亿」，**精确值悬停可见**；
4. 数字改等宽字体并收紧字距 —— 金额跳动时列宽稳定，界面不再跟着晃。

### 卡 2 · 战列舰「巨齿鲨级」入列 + 价位与工期重排（沿用既有待审稿，已把 ISK 改成信用点）

| 字段 | 值 |
|---|---|
| `id` | `2026-09-14-megalodon` |
| `title` | 战列舰「巨齿鲨级」入列，战列舰档价位与全舰制造工期重排 |
| `tag` | 舰船 |

1. 全新**战列舰「巨齿鲨级」**加入船坞：十四处装配位、三层防护合计 1,273，护盾对动能伤害抗性更高，火力加成 0.85——敢站编队最前，代价是转身与起步偏慢；
2. 该舰**不售成品**：可在深空工业协会市场购入「巨齿鲨级舰船蓝图」（需协会声望 11）后自行筹备材料总装——三钛合金 4,809,750 · 类银超金属 1,434,750 · 类晶体胶体 810,000 · 超噬矿 157,500 · 同位聚晶 276,000，制造耗时 20 小时；
3. **战列舰档舰船价格上调**：玄武级重装旗舰 2,200,000 → 16,500,000 · 蝠鲼级重载货舰 1,900,000 → 13,500,000 · 剑鱼级大型货舰 1,250,000 → 4,800,000 **信用点**；对应蓝图书价同步调整，修船费用随舰价档位同步；
4. **舰船制造工期按档位重排**：护卫舰 15~25 分钟 · 驱逐舰 51~86 分钟 · 巡洋舰 3~5 小时 · 战列舰 9~20 小时 · 旗舰 45 小时（此前巡洋舰制造耗时统一封顶在 4 小时）。

> 裁量提示（不进公告）：第 3、4 条是**涨价与加时**，对玩家敏感；可弱化为只写"按档位分层"不给具体数。

### 卡 3 · 护盾回充改判 + 新装备「护盾充能装置」

| 字段 | 值 |
|---|---|
| `id` | `2026-09-14-shield-charger` |
| `title` | 护盾回充改为按当前护盾比例结算，新增「护盾充能装置」 |
| `tag` | 数值 |

1. 护盾的**被动回充改按"当前护盾剩余比例"结算**：盾越满回得越快，**盾被打空即完全不回**——破盾后必须靠装备或撤离，而不是原地等盾；
2. 新增中槽装备「**护盾充能装置**」三档，单次恢复护盾上限的 **24% / 40% / 64%**；价格与市场投放照「船体维修装置」同档设置；
3. MK1 / MK2 可从稀有订单买到、也有蓝图可自制；MK3 只在奇货出现、无蓝图。

### 卡 4 · 索敌统合改判「炮台命中 +2%/级」

| 字段 | 值 |
|---|---|
| `id` | `2026-09-14-targeting-integration` |
| `title` | 索敌统合改为直接提升炮台与导弹架的命中率 |
| `tag` | 数值 |

1. 「索敌统合」的效果从"提升舰船的命中加成"改为**直接提升炮台与导弹架的命中率：每级 +2%（满级 +10%）**；
2. 该效果与「火控阵列学」**乘算叠加**；激光必中与无人机条目不受影响；
3. 装配台与舰船信息里的「命中加成」一行自此**只显示舰船自身数值**，不再被技能放大——面板读数与实际一致。

### 卡 5 · 战斗入场窗口与阵亡演出

| 字段 | 值 |
|---|---|
| `id` | `2026-09-14-battle-arrival-grace` |
| `title` | 入场动画播完前双方不开火；阵亡单位保留尸骸与爆炸演出 |
| `tag` | 修复 |

1. **敌方（含中途增援）的入场动画播完之前，双方都不开火**：不会再出现"敌人刚出现就被打成 0 血、血条与舰影留在场上不消失"的情况；
2. 洞外洞内一致：玩家首波出击不受影响（玩家自己飞入那段照旧可开火）；
3. 阵亡单位的**爆炸与淡出演出**在所有场景下都会播放（含洞内），沉没的舰影保留在原位只转灰，不再让其余舰影跳动。

### 卡 6 · 界面与文案批（市场页 / 提示 / 排版）

| 字段 | 值 |
|---|---|
| `id` | `2026-09-14-ui-text-pass` |
| `title` | 市场页与全站界面梳理：布局修复、提示统一、内容宽度上限 |
| `tag` | 修复 |

1. **市场详情**：行情折线图不再压住买卖盘，其高度随面板高度伸缩（有下限，不过度压缩）；买卖盘档数随窗口高度自适应显示 **5~8 档**，只显示整行、不裁半行；「我的挂单」可见高度对齐整行，不再被切掉半条；窗口过窄时整页滚动、各子窗口完整显示（常驻供应目录限高，详情不再被推到很远的下方）；
2. **悬停提示**：全站原生提示改为自绘（限宽 300 像素、多行、悬停 200 毫秒后出现、跟随鼠标）；手机端下拉选项改为**抬手才弹**（最短 200 毫秒），避免误触；
3. **排版与阅读**：标题行字号与标题后的圆形感叹号（ⓘ）按同一组比例联动放大；原先常驻在页面上的机制说明收进标题后的 ⓘ，页面更清爽；**超宽屏给内容加最大宽度**，长屏不再把内容拉成一条；
4. **文案梳理**：货币单位与术语统一，清掉玩家可见文案里的现实词汇、英文标记与冗长表述（数值与机制不变）。

### 卡 7 · 维修技能与修理组件口径统一

| 字段 | 值 |
|---|---|
| `id` | `2026-09-14-repair-skills` |
| `title` | 修理效果加成统一为每级 5%，修船两条路径口径一致 |
| `tag` | 数值 |

1. 修理相关的效果加成**统一为每级 +5%**；「维修工程学」难度升为 3，效果与其它修理技能同源；
2. **修理组件回血口径统一**：手动修理与自动修理两条路径给出同样的回血量（此前不一致）；
3. 中槽「船体维修装置」现在也吃「舰体快修学」加成。

---

## 二、落库格式（批准后照此写入 `packages/data/src/announcements.ts` 顶部，新的在上）

```ts
{
  id: '…',
  title: '…',
  date: '2026-09-14',
  tag: '…',
  bullets: ['…', '…', '…'],
}
```

写完后跑四闸门（`typecheck` / `test -w @whale/core` / `content:check` / 两个 build），
`content:check` 里有公告字段与条数的既有契约会一并复核。

---

## 三、⚠ 待船长裁决：虫洞要不要随这批上线？

本批 277 个提交里**绝大多数是虫洞本体**（施工期：入口只在调试模式下出现 + 数据走未上线闸门）。
三种走法：

1. **一起上线**（"大更"）：需要先做**上线动作清单**——① 撤掉 `unreleased` 闸门（虫洞专属装备/舰船/图纸/无人机/
   谜质物品与市场卡）② 开放「安全货柜拆解」的调试开关 ③ 处理虚空晶的市场供给行 ④ 词典补虫洞术语
   ⑤ 公告用既有待审稿 `announcement-draft-20260913-wormhole.md`（我已抽查，第 5 条里"虚空母矿 → 虚空晶"
   这条来源链要不要写请你定）；**风险**：上线前需要一次端到端验收（入口可见性、经济读数、四个地点的产出）。
2. **本批只推"非虫洞"内容**（推荐）：虫洞代码照样进远端仓库，但**闸门一个都不动** ⇒ 玩家看不到、也拿不到
   虫洞物品；公告只发 §一 的 7 张卡。等你拍板做上线动作时再推一次（并可只推那一次）。
3. **折中**：先推代码 + 7 张卡，虫洞单独排一次上线（含公告与闸门），我先把上线动作清单按现场核对好交你。

---

## 四、"准备推送"清单（**未执行**，等船长批准）

1. **闸门**：`npm run typecheck` · `npm run test -w @whale/core` · `npm run content:check` ·
   `npm run build` · `npm run build --prefix web` —— 全绿才推。
2. **工作区**：`git status` 干净；`tools/` 下无 `_` 前缀探针残留；产物目录不入库。
3. **公告**：§一 的卡经船长批准后写入 `announcements.ts`（含已入库的 09-13 长途运输那条，一起上线）。
4. **网络**：先 `git fetch origin` 确认连通（起草时失败过一次），再 `git push origin main`（**一次**，
   代码与公告同一批）。
5. **推送后**：核对网页版 Pages 部署产物；`docs/roadmap.md` 顶部给本批记一条"已推送（远端 `<sha>`）"。
