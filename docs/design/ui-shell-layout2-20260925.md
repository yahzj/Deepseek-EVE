# 工作文档 · 外壳布局第二轮（ui-redesign-2）（2026-09-25 · 三号）

> **状态：进行中**
> **船长原话（照抄）**：
> ①「**你自己改完就没有自己看过吗，允许你使用截图检查。**」
> ②「**首先将存档管理，重置档案，保存移动到设置内。**」
> ③「**其次钱包显示移动到顶部玩家名字的右侧。**」
> ④「**SVG舰船动画小窗口依旧是在左上角，活动页面的上方。**」
> ⑤「**事件还是在屏幕右侧，但是采用和现在一样的弹出式（弹出后覆盖在主区上方）。**」
> ⑥「**然后你的宽度是不是有问题？游戏屏幕最下方有左右的滚动条。**」

## 一、船长逐条要求的落地口径

| # | 要求 | 落地 |
| --- | --- | --- |
| ⑥ | 底部横向滚动条 | **已修**：`.app-nav-side` 漏了 `box-sizing: border-box` ⇒ `width:100%` 是内容宽、再加 padding 24px ⇒ 底栏 1288 > 视口 1264（实测 `hOverflow: 24`） |
| ② | 保存/存档管理/重置档案 → 设置内 | 从顶栏右侧三个按钮**移入设置弹窗**，设置里新增一组"存档" |
| ③ | 钱包 → 顶部玩家名右侧 | `MoneyFit` 从左栏移到 `.app-header-left` 的 `app-pilot` 之后 |
| ④ | SVG 舰船窗在**左上角、活动栏上方** | 回到**左侧列**：舰船窗在上、活动栏在下（`app-workspace` 保持横向：左列 + 右主区） |
| ⑤ | 事件改成**右侧弹出、覆盖主区** | 日志坞从纵向流里移出，改为**浮层**（右侧、覆盖主区上方），触发按钮留在右侧 |

## 二、第二轮结构（目标）

```
┌───────────────────────────────────────────────────────────┐
│ 顶栏：大鲸鱼-深空放置  玩家名 · 信用点 ……… 在线/公告/手册/设置 │
├──────────┬────────────────────────────────────────────────┤
│ 左列      │ 主区（星图 / 各页）                             │
│ ├舰船窗   │                                                │
│ └活动栏   │                                   ┌──────────┐ │
│          │                                   │ 事件日志 │ │← 右侧浮层
│          │                                   │（覆盖主区）│ │
├──────────┴────────────────────────────────────────────────┤
│ 底栏：物品 装配 舰船 工业 │ 出港 │ 市场 技能 任务 成就 通讯    │
└───────────────────────────────────────────────────────────┘
```

## 三、教训（本批已踩，写下来避免再犯）
1. **改完结构必须量"每段的实际占用"**——只顾"活动栏在左"而没量"主窗口还剩多少"，
   导致主窗口被挤到 218px（船长反馈"主窗口不存在"）；
2. **`width:100%` + padding 必须配 `box-sizing: border-box`**（本仓 header/workspace 早有此注释，加底栏时漏抄）；
3. **每项要求单独验收**：改完对着船长原话逐条核，别"整体感觉对了"就交。

## 四、新旧界面切换（船长令：「新旧界面能否允许玩家在设置内切换？」⇒ 裁定**甲：两套 DOM 并存**）

### 4.1 落地形态
- **`apps/desktop/src/renderer/src/ui/AppShell.tsx`（新文件，~500 行）**：两套外壳**同文件、同状态源**。
  - `modern`（新）：顶栏 +（**左列**：舰船窗 + 活动栏）+ 右主区 → **底部横栏**导航 + 右侧日志浮层。
  - `classic`（旧）：**取自 `main` 分支原文冻结**——顶栏（含保存/存档管理/重置档案）+ 左侧竖栏
    （舰船窗 + 钱包 + 导航项）+（右主区 + 日志坞）。
  - 两套**共用同一个 `ShellCtx`**（`state` / `engine` / 各回调 / `pageMain` / `logDock` 插槽）
    ⇒ 切换不丢进度、不丢页面状态；`pageMain` 与 `logDock` 仍归 App，不搬进外壳。
- **偏好**：`localStorage['whale-idle:layout']` = `modern`（缺省）/ `classic`；根节点挂
  `is-layout-modern` / `is-layout-classic` 类，便于 CSS 针对性微调。**本机偏好、不进存档**。
- **入口**：设置弹层新增「界面布局」一行（`ui.App.146/147/148`），与「语言」同款左右按钮组。

### 4.2 ⚠ 已知差异（船长此前已接受）
`classic` 是**冻结的历史形态**：本轮及后续新增（副AI活动组、计时中组、AI 迷你卡、
底栏图标放大、存档按钮移入设置…）**只在 modern 出现**。切到 classic 即回到 2026-09-25 之前的样子。

### 4.3 验证（读数型，非观感结论）
`_probe-layout2.mjs` / `_probe-layouttoggle.mjs`（1280×860，测试档 `save-20260920-164600`）：

| 项 | modern | classic |
| --- | --- | --- |
| 根类 | `is-layout-modern` | `is-layout-classic` |
| 导航形态 | **底部横栏**（1280×87，子块 = 左组/出港/右组） | **左侧竖栏**（含舰船窗 + 钱包 + 10 项） |
| 活动栏 | 左列 150×610，**可滚**（scrollH 1039 / clientH 608） | 无（旧版本就没有） |
| 钱包落点 | `.app-header-left`（玩家名右侧，x 193 > 名 x 166） | `nav.app-nav-side`（原文位置） |
| 顶栏按钮 | 7 枚、**无「保存」**（已入设置） | 10 枚、**含保存/存档管理/重置档案** |
| 日志坞 | `position: fixed` 浮层 | `position: fixed` 浮层 |
| 横向/纵向溢出 | 0 / 0 | 0 / 0 |

切换链实测：默认 `modern` → 设置里点「旧版」⇒ `localStorage` 变 `classic`、根类变
`is-layout-classic`、导航由底部变侧栏；**刷新后仍为 classic** ⇒ 偏好确实记住了。

### 4.4 本轮踩坑（写下来避免再犯）
- **取块一律按锚点/配平算，别手数行号**：`App.tsx` 与生成脚本之间曾差 1 行，
  加上"JSX 注释块内花括号自平衡会把花括号配平骗过去"，前后白改 6 轮。
- **`<main>` 之后那段主区收口**：原文里 `app-page-content` → `</main>` → `app-workspace-body`
  是**三层**收口，日志坞是它们的**兄弟**（原文 1545/1547 行），少一层多一层都会让
  TS 报"几百行之外"的位置。定稿判据 = 全文件 `<div` 74 / `</div>` 76（净差 −2 = root 那层）。
- **两套 DOM 并存**必须让外壳**零改写**地复用原文：做法是"一个结构化 `ctx` 在组件顶部解构"，
  而不是"抽 34 个 props 再逐个加前缀"（后者改坏了 JSX 标签名/属性名/`=>` 三次）。

_维护：本件按 §8 归档；归档前不得合入 main（船长令：本工作区隔离）。_
