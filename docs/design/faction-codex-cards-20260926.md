# 势力图鉴：卡片化改造（去切换 · 敌人舰船形象 · 专属件用图鉴卡 · 族徽角标）

- **状态**：已实现，四闸门待跑，**待船长验收**
- **经办**：三号 · 工作树 `H:\大鲸鱼\Deepseek-EVE-verify`（分支 `verify`）
- **日期**：2026-09-26

## 一、船长原话（照抄）

> 手册内，势力图鉴的图标和列表切换是多余的。而且有问题。内容中的敌人窗口，每个敌人需要显示敌人的舰船SVG。专属装备也是过于简陋。装备和舰船以及蓝图这些应该使用装备图鉴舰船图鉴中的痛苦卡片。顺便给所有位置势力专属的舰船和装备的图标卡片的左上角标注势力族徽

（"痛苦卡片"按上下文＝**图鉴的图标卡片** `app-hand-cell`；下方裁决按此理解。）

## 二、四处追问与船长裁定（照抄）

| 追问 | 船长选定 |
|---|---|
| 敌人卡"未遭遇"档要不要显示舰船剪影？ | **不显示剪影，改为类似信号不良的通用图形** |
| 专属装备/舰船/蓝图卡片可不可点开？ | **和其他图鉴中一样，可以点开** |
| 图纸段的族徽按什么标？ | **按照所属势力标**（= 按它产出的专属装备所属势力） |

## 三、改后口径（逐处）

| # | 处 | 改法 |
|---|---|---|
| 1 | 势力图鉴页 | **去掉图标/列表切换**（该页恒走卡片网格；其余图鉴页的切换不动）；该页的"列表"档同时暴露了排版 bug（卡片被当列表行渲染）⇒ 一并消失 |
| 2 | 敌人种类窗口 | 每型敌人 = 一张**图标卡**：`ShipSprite`（敌舰资产表，与战斗画面同一张）＋ 舰名 ＋ 舰种；**未遭遇** = 通用"信号不良"图形（`ico-antenna` 线稿 ＋ 暗淡色调及噪点装饰），名字与介绍仍按旧令打「？？？」 |
| 3 | 专属装备 / 舰船 / 图纸 | 三段都改用 **`IconGrid` 那套卡片**（`app-hand-cell`：图标 ＋ 名称 ＋ 副行 ＋ 稀有度）——与装备图鉴/舰船图鉴**同一份卡片构造**（走共用 builder，避免两处漂移） |
| 4 | 全局族徽角标 | **凡"势力专属"的装备/舰船卡，左上角标该势力族徽**（`fam-x` glyph ＋ 势力色）：装备图鉴 / 舰船图鉴 / 势力图鉴详情**三处都标**；判据走单一入口 `factionOfExclusive(id)`，蓝图按**产物**判 |

## 四、涉及文件

- `apps/desktop/src/renderer/src/panels/Handbook.tsx`：
  - `GridCell` 加 `crest` 字段；`tab` 类型放宽为 `DetailTab`（= `Tab` ＋ `'foe'`，只给详情窗用）
  - `IconGrid` 渲染族徽角标（左上角，`fam-x` ＋ 势力色）与「信号不良」图形分支
  - 卡片构造抽成**模块作用域共用 builder**：`moduleCellOf` / `shipCellOf` / `blueprintCellOf` /
    `shipBlueprintCellOf`（族徽在 builder 里按 `factionOfExclusive` 加）⇒ 图鉴页与势力详情**唯一构造点**
  - `SignalLostIcon`（新 SVG 线稿：天线 ＋ 扫描弧 ＋ 斜划 ＋ 噪点）
  - `FoeBody`（敌舰详情：舰影 ＋ 势力/火力/盾甲结构/命中/回避/射程/单发/射速/动力/战术/特殊装置）
  - `FactionDetailPanel`：三段改 `IconGrid`、敌人卡、容器内详情窗（`CellDetail`）
  - 势力页去掉图标/列表切换（其余图鉴页保留）
- `packages/core/tests`：无（本次无 core 改动）；测试数 2548 → **2566** 是他人在途批带入

## 五、验证（真机复读 · 无头 CDP · 势力图鉴）

| 项 | 读数 |
|---|---|
| ① 切换按钮 | 势力页**已隐藏**（`.app-hand-viewbar` 不存在） |
| ② 势力卡 | 海盗舰系 / 异形生物 / 守墓古舰 / 泰坦巨构 / 鱿烬亡军 / 墨潮帮（卡片网格） |
| ③ 敌人卡 | 每型一张卡、图标 = **舰影 SVG**（已遭遇档）；副行 = 舰种 · 战术 · 伤害构成 |
| ④ 未遭遇档 | 卡片参数读：名字「？？？」· 副行「未遭遇」· **无 shipId ⇒ 走信号不良图形** |
| ⑤ 专属装备/舰船/图纸 | 三段都成了卡片：`掠袭折射涂层 / 掠袭破片炮 / …`（装备）、`掠袭电子舰 / 掠袭炮舰 / …`（舰船）、`…图纸（一次性）`（图纸） |
| ⑥ 族徽角标 | 共 **18 枚**落在专属件卡上，位置 **@4,4**（左上角），类名 `app-map-famchip is-fam-A` |
| ⑦ 点开详情 | 点「海盗快艇」→ 详情窗标题 = 海盗快艇 · **头部有舰影** · 行 = 定位/档次 · 火力 · 盾/甲/结构 · 命中加成 · 回避率 · 锁定范围 · 单发伤害 · 射速支援 · 动力 · 战术 近战缠斗…（**射程/速度两行当晚按船长追加令改口径，见 §七**） |

闸门：typecheck 四包 0 错 · core **2566/2566（234 文件）** · `content:check` ✅ · `l10n:check` ✅ · `ui:rot-check` ✅ · web 构建 ✅。

## 六、待裁决 / 已知取舍

1. **族徽角标只在有专属件的卡上出现**：普通件（非势力专属）不标——船长令说的是"势力专属的舰船和装备"。
2. 角标走 `app-map-famchip`（星图势力色块的既有类）＋ 内联定位；若要更强的"徽章感"（底圈/描边）请点名。
3. 敌舰详情里的数值是**敌舰自身定义的原值**（单发/装填/射程带/速度比/血占比）；实战值按威胁缩放，未在此换算。

## 七、追加：敌人详情两行改口径（2026-09-26 晚 · 船长令）

船长原话：「**在手册内的敌人，还会显示其基础速度和攻击范围。**」

| 行 | 改前 | 改后 |
|---|---|---|
| 攻击范围 | 标签用「**锁定范围**」（`ui.shipInfo.012`）配 `rangeMinM – rangeMaxM` —— 语义不对（那是"能锁多远"） | 标签换既有词条「**射程带**」（`ui.shipInfo.040`），数值不变 |
| 速度 | 只有「**动力** = `1.15×`」一行（动力已按 2026-09-26 前批船长令退役）；中途误用 `ui.shipInfo.009` 曾印出裸 id | 「**机动速度** = `Math.round(balance.battle.hullClassBaseSpeedMps[舰种档] × speedRatio) m/s（倍率）`」，与 `combat.createFoeSpecsFromShips` **同源同式**（基准表 `{1:340, 2:295, 3:258, 4:205, 5:155}` 读 balance，不另存数字） |
| 单发伤害 | 显示舰级裸值 `shotDmg`（如 28） | **整行撤掉**——船长 2026-09-26 同日再令：「**敌人的单发伤害不要显示，因为实际上并没有这个伤害**」（`shotDmg` 是舰级裸值，实战单发还经 `dmgMul × 多舰船补偿 × 越线折扣 × 逐卡缩放`；火力大小由「伤害构成占比」那行表达，**装填行保留**＝射速口径） |

- 只改 `panels/Handbook.tsx` 的 `FoeBody`（＋ 传 `engine` 以取 balance）；**标签一律复用既有 `ui.*` 词条，不新造 id**；行数 9 → 10（加两行）→ **9**（撤单发伤害行）。
- 实测（无头 CDP · 合并后构建 · 势力图鉴 → 海盗快艇）：`射程带 = 1 – 2600 m` · `机动速度 = 391 m/s（1.15×）` · **单发行 0 条** · 裸 id 命中 **0** · 行数 **9**。
- 闸门：typecheck / `content:check` / `l10n:check` / `ui:rot-check` / core 测试 全绿（合并 main 后复跑一遍）。
- 待船长定：① 标签是否要**「基础速度」**四个字（现复用「机动速度」，若要新词条需新增 id）；② 数值里的倍率括号（`（1.15×）`）留不留。

