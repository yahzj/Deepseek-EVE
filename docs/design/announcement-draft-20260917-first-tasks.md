# 公告待审稿：任务重做 ·「第一次」任务系列（2026-09-17 · 2026-09-18 修订）

> **状态：待船长审核**（未合入 `packages/data/src/announcements.ts`）。
> 口径来源：船长 2026-09-17 教程重做批（工作文档 `docs/design/first-tasks-rework-20260917.md`）
> ＋ **2026-09-18 的奖励表与扫描时长裁定**（母港扫描 10 秒 · 链奖金按档位递增 · 13 条奖励逐条改定）。
> 写法按约定第十二章：第三人称「玩家」、无第一/第二人称代词、不含开发/验收话语、不写倍率数字与公式。
> **批准后**：按本文逐字写入 `announcements.ts` 顶部新条目，与代码同批推送。

## 拟发布条目

- **id**：`ann-first-tasks-20260917`
- **title**：任务重做：「第一次」任务系列
- **date**：2026-09-18
- **tag**：系统
- **bullets**（5 条）：

1. 新档开场不再走线性教程：序章演出结束后直接开工，开场第一步「扫描母港」只需十来秒；此后行动清单改由任务中心的「第一次」系列发布——扫描星系、采集原矿、打捞残骸、维修舰船、完成悬赏、操作精炼炉、生产、挂单销售、造船、学习技能、指派 AI 副船、长途运输、探索虫洞，想先做哪一件都行。
2. 每完成一条「第一次」，舰载信息库会送来一封相关情报，任务卡上可直接打开；同一张任务卡随后变成长期次数目标（例如「宇宙探索家」），每达成一个档位都能领取奖金，档位越靠后奖金越高。
3. 部分页面随进度解锁：星图的矿带开采、常驻悬赏、残骸打捞与长途运输要先完成一次扫描；工业页要先采到原矿；市场页要先完成一次生产。未解锁的页面与任务不会显示。
4. 多条「第一次」带实物奖励：采集原矿给采集器、打捞残骸给打捞器、维修舰船给民用修理组件、操作精炼炉给「动能弹药生产线」蓝图、完成悬赏给一艘鲣鱼级护卫舰、造出第一条船给民用船体维修装置、学习技能给基础 AI 核心、长途运输给飞鱼级快运舰、解锁虫洞给两处虫洞坐标。
5. 已有存档不受影响：读档时按既有进度一次性记为已完成，对应的情报信补入收件箱，已有资产不变；旧的线性教程、顶部引导条与「跳过教程」一并下线（序章演出保留，右上角可跳过演出）。

## 断言出处（备核）

- 13 条任务与清单内容 → `packages/core/src/firstTasks.ts` 的 `FIRST_TASKS`
- 实物奖励逐条 → 同文件各条 `reward`（发放逻辑 `packages/core/src/firstRewards.ts`：蓝图书/仓库/装备库/机库/核心账本/虫洞库存六个口袋）
- 母港扫描十来秒 → `packages/core/src/explore.ts` 的 `HOME_SCAN_WINDOW_MS = 10_000`（`scanWindowMsFor` 对母港直接返回）
- 完成一条送一封情报信 → `packages/data/src/firstTaskMessages.ts`（13 封，触发器 `{ kind: 'firstTask' }`）
- 档位奖金递增 → `firstTasks.ts` 的 `chainLevelRewardIsk`（第 N 级 = 基准 × N⁵；公告按 §12 **不写公式**）
- 星图四项 ← 一次扫描 · 工业 ← 原矿 · 市场 ← 一次生产 → 同文件 `FIRST_UNLOCKS`
- 老档一次性判定＋补发情报信、资产不变 → `packages/core/src/save.ts` 的 v25→v26 迁移（只置 `done`、不发奖励）

## ⚠ 待裁决（第 4 条要点里**暂缺一项**）

「第一次生产」的奖励，船长裁定为「沙猫级采矿艇蓝图」，但**沙猫级没有舰船蓝图**（开局白送的船）——
三选一后可能需要在第 4 条里补一句：① 改成现有的 T1 采矿艇蓝图 · ② 新增一张沙猫蓝图 ·
③ 改发沙猫舰船本体。**裁定前公告第 4 条不写「生产」那一项**（避免公告与实现不一致）。
