# 公告待审稿：任务重做 ·「第一次」任务系列（2026-09-17）

> **状态：待船长审核**（未合入 `packages/data/src/announcements.ts`）。
> 口径来源：船长 2026-09-17 教程重做批（工作文档 `docs/design/first-tasks-rework-20260917.md`）。
> 写法按约定第十二章：第三人称「玩家」、无第一/第二人称代词、不含开发/验收话语、数字断言须能在代码里找到出处。
> **批准后**：按本文逐字写入 `announcements.ts` 顶部新条目，与代码同批推送。

## 拟发布条目

- **id**：`ann-first-tasks-20260917`
- **title**：任务重做：「第一次」任务系列
- **date**：2026-09-17
- **tag**：系统
- **bullets**（5 条）：

1. 新档开场不再走线性教程：序章演出结束后直接开工，行动清单改由任务中心的「第一次」系列发布——扫描星系、采集原矿、打捞残骸、维修舰船、完成悬赏、操作精炼炉、生产、挂单销售、造船、学习技能、指派 AI 副船、长途运输、探索虫洞，想先做哪一件都行。
2. 每完成一条「第一次」，舰载信息库会送来一封相关情报，任务卡上可直接打开；同一张任务卡随后变成长期次数目标（例如「宇宙探索家」），每达成一个档位即可领取奖金 2,500 信用点。
3. 部分页面随进度解锁：星图的矿带开采、常驻悬赏、残骸打捞与长途运输要先完成一次扫描；工业页要先采到原矿；市场页要先完成一次生产。未解锁的页面与任务不会显示。
4. 两条「第一次」带实物奖励：「第一次学习技能」发基础 AI 核心 ×1，「第一次采集原矿」发动能弹药生产线蓝图 ×1。
5. 已有存档不受影响：读档时按既有进度一次性记为已完成，对应的情报信补入收件箱，已有资产不变；旧的线性教程、顶部引导条与「跳过教程」一并下线（序章演出保留，右上角可跳过演出）。

## 数字与断言出处（备核）

| 公告断言 | 代码出处 |
|---|---|
| 13 条「第一次」与清单内容 | `packages/core/src/firstTasks.ts` 的 `FIRST_TASKS` |
| 每档奖金 2,500 信用点 | 同文件 `CHAIN_REWARD_ISK = 2_500`（领奖入口 `claimChainReward`） |
| 完成一条送一封情报信 | `packages/data/src/firstTaskMessages.ts`（13 封，触发器 `{ kind: 'firstTask' }`） |
| 星图四项 ← 一次扫描 · 工业 ← 原矿 · 市场 ← 一次生产 | 同文件 `FIRST_UNLOCKS`（界面 `unlocked()`；未解锁的导航项与页签不渲染） |
| 「第一次学习技能」发基础 AI 核心 ×1 | `FIRST_TASKS` 中 `first-skill.reward.aiCores`（船长 2026-09-17 裁决） |
| 「第一次采集原矿」发动能弹药生产线蓝图 ×1 | `first-mine.reward.items`＝`bp-ammo-kinetic` |
| 老档一次性判定＋补发情报信、资产不变 | `packages/core/src/save.ts` 的 v25→v26 迁移（只置 `done`、不发奖励） |
| 线性教程/引导条/跳过教程下线 | 删 `data/src/tutorialSteps.ts`、`panels/TutorialGuide.tsx`、`core/onboarding.ts` 步骤机与 `skipTutorial` |
