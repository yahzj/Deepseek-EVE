# 报障分析：入侵活动里「没有判定击杀」（2026-09-26 晚 · 二号 · d2）

状态：**根因已定位 · 缺失的那一掷已修**；**存量补偿待船长裁决**。归档按 AGENTS.md §8。

## 船长原话（照抄）

> 「玩家在入侵活动中依旧没有判定击杀，存档我已经导入本地，分析下原因」

取证档 = 船长导入的 `docs/test-saves/save-20260926-230119.json.json`（后端存档时刻 2026-09-26 23:01 +08:00）。

## 一、档里到底发生了什么（读数）

| 读数 | 值 | 换算 / 说明 |
| --- | --- | --- |
| `weekendEvent.flagshipHpMax` | 150,000 | 母舰共享血池 |
| `weekendEvent.flagshipHpDone` | **146,625** | 玩家累计打进去的伤害 = **97.75%** |
| `weekendEvent.octopusHpDone` | **3,376** | 章鱼人累计削掉 = **2.25%** |
| `weekendEvent.flagshipDown` | **`octopus`** | **判给章鱼人**（谁把共享血条清空谁得） |
| `weekendEvent.flagshipBlackBox` | **缺省** | **那一掷从没发生**（见第三节） |
| `weekendLastResult.blackBox` | **0** | 结算快照：一个黑匣都没发 |
| `weekendLastResult.standing` / `isk` / `wreck` | 13 / 76,000,000 / 360 | 参与奖照发（贡献占比 85% ⇒ A 档） |
| `octopusHoldUntilWallMs` | 2026-09-26 22:12:44.903 +08:00 | 章鱼停工终点 = 玩家最后一场旗舰战结束 + 60 秒 |
| `endedAtWallMs` | 2026-09-26 22:12:45.424 +08:00 | 本场结束 —— **停工刚过 0.52 秒** |

**时间线**：玩家最后一场旗舰战在 **22:11:44.903** 结束（此刻血条只剩 3,375 没被章鱼认领）；
按船长 2026-09-26 的令章鱼停工 60 秒；22:12:44.903 停工一过，章鱼恢复削血，**0.52 秒后**它那份累计
（3,376）就越过了剩余需要（3,375）⇒ `flagshipDown = 'octopus'`、本场结束。
弹药库读数：玩家的出战编队 `weekendPrepSquad` = 3 艘鹦鹉螺级测绘巡洋舰 ＋ 1 艘巨齿鲨级战列舰
（舰队 14 艘、无损失记录）。

**结论一：击杀判定没有"漏判"** —— 按现行口径「共享血条谁清空谁得」，最后那 1 点血确实是章鱼削掉的，
本场判给它是对的。玩家拿到的是"没抢到最后一下"那一档的待遇，不是"玩家击沉"。

## 二、玩家真正的损失：**没有抢到最后一下**那一档

船长 2026-09-25 定的爆率表（`weekendBlackBoxChanceOf`）：

| 情形 | 爆率 |
| --- | --- |
| 抢到最后一下 ＋ p > 50% | **100%** |
| 抢到最后一下 ＋ p ≤ 50% | 100% → 10% 线性 |
| **没抢到最后一下** | **25% × p** |

该玩家 p = 97.75% ⇒ 爆率 = **24.438%**。**按同一条确定性子流重算，那一掷是 0.015648 < 24.438% ⇒ 本该命中**
（本该拿到 1 个黑匣）。

## 三、真根因：**这一掷根本没有发生**（我们的 bug）

`weekendRollBlackBox` 只在**公开收口** `weekendEvent.weekendTick` 的"章鱼得手"分支里被调用；
而**引擎每拍走的是另一个收口** `weekendTickBoss`（`engine.advanceGame` → `weekendTickBoss`，
2026-09-26「章鱼人冷却 60 秒」那批新加的第 4 参闸就在它里面）。那个分支里**没有掷骰**：

```
if (weekendOctopusTick(state, ev, dt, inBattle)) {
  ev.flagshipDown = 'octopus'
  endWeekendEvent(state, nowWallMs)   // ← 原先这里漏了 weekendRollBlackBox
  return { down: 'octopus' }
}
```

一旦它在这里结束本场，随后公开收口 `weekendTick` 就因"本场已结束"直接返回 ⇒ **那一掷永远不发生**
⇒ `flagshipBlackBox` 缺省 ⇒ 结算补发判据 `ev.flagshipBlackBox === true` 为假 ⇒ **玩家一个黑匣都拿不到**。

**这不是个例**：引擎心跳里"章鱼把血条削空"这一类**全部**走本收口 ⇒ 所有"母舰被章鱼收走"的场次
都丢了这一掷（玩家按规则该得的黑匣被吞）。报障玩家只是其中一例，而且他的那一掷**本该命中**。

## 四、已修（本次落码）

`weekendEvent.weekendTickBoss` 的"章鱼得手"分支补上 `weekendRollBlackBox(state, ev, false)`（与公开收口
逐字同款，掷骰本身幂等、走同一条确定性子流）⇒ 掷中时结算会照旧补发黑匣（`boxAtSettle` 那条路）。

**新增用例 2 条**（`octopus-hold-20260926.test.ts`）：⑧ 章鱼得手必须留下掷骰结果；⑧b 与公开收口同源、
同种子同场次同结果。**反向验证**：把这一行删掉 ⇒ 两例当场转红，装回即绿。

验证：`typecheck` 四包 0 错 · core **241 文件 / 2637 用例全绿** · `content:check` ✅ · `l10n:check` ✅ ·
`ui:rot-check` ✅。

## 五、存量补偿：**船长已裁定 —— 截止调到现在**（本次落码）

**船长令**：「**将之前的补发时间调到现在截止。然后推送更新**」
⇒ `WEEKEND_BOX_COMPENSATION_CUTOFF_WALL_MS` 由 **21:33** 改为 **2026-09-26 23:11 +08:00**
（`weekendComms.ts`）⇒ 21:33~23:11 之间结束的场次一并进补偿面。

**选的是"沿用既有补发判据 ＋ 推截止"，不是另立"补掷"**：既有判据本就写着"打完入侵没拿到黑匣就补 1 枚"，
与本次的漏掷指纹同源 ⇒ 不必再加一条。
**本场结束于 22:12 ⇒ 现在落在补偿范围内。**

**验收读数**（临时探针，跑完即删；只读导入档、**不回写**）：
截止 = `2026-09-26T15:11:00Z` · 本场结束 `2026-09-26T14:12:45.424Z`（在截止前 ✓）· 结算信在 ✓ ·
快照实发黑匣 0 ✓ · 见过黑匣 false、仓库/货舱 0、插件实物 0 ✓
⇒ **`compensateMissingWeekendBlackBox` 返回 true，仓库黑匣 0 → 1**，末尾日志「入侵补偿：补发旗舰黑匣 ×1
（已存入物品仓库）。」（`core.weekend.040`）。

## 六、另一个可选项（机制，本批不动）

本场玩家 97.75% vs 章鱼 2.25%，最后 1 点血被章鱼收走 ⇒ 体验上"打了 97% 却算章鱼击沉"。
若船长认为该改，可选：
- **甲**：章鱼只有削掉 ≥ 总血量的某个比例（如 50%）才允许认领击沉，否则血条清空仍记玩家击沉；
- **乙**：保持归属，但"没抢到最后一下"那条爆率在 p 很高时抬起来（如 p ≥ 90% 必爆）；
- **丙**：延长停工时间（60 秒 → N 分钟）或只在玩家离线/长时间不接战时允许章鱼收尾；
- **丁**：不改机制，只补提示（界面写明"章鱼将在 X 秒后恢复削血"——当时那批船长明令"不做界面/倒计时改动"）。
**本次不动机制**，等船长点名。
