# 交接文档 · 大鲸鱼深空工业 · 2026-09-20 早（一号 → 下一任一号）

> **给零上下文的接手人**：本卡一页读完即可开工。**§0 一分钟开工 → §1 三树现状 → §2 本会话（一号）办的批次 →
> §3 关键口径速查 → §4 挂账（按优先级）→ §5 铁律与坑**。
> 目录 = `docs/INDEX.md`（机器生成 · `--check` 校验过期）· 指路 = `docs/catalog.md`（手写）· 更早批次原文 = `docs/archive/roadmap-<日期>.md`。
> 上一张一号卡 = `docs/design/archive/handoff-20260915-to-new-pilot1.md`（那一轮的批次/口径，本卡不重复）。
> **同一轮还有一张卡**：`docs/design/handoff-20260919-matter-tech-d2.md`（**二号 · 谜质科技批**，已并入 main）——
> 「谜质科技」那棵树的逐批施工细节与 d2 侧待办在那张卡里；本卡管**一号侧批次 ＋ 总基线 ＋ 全部待裁**。**两份一起读。**
> ⚠ 本卡是**交接件**：下一任开工后把它并入自己的交接卡，或按 AGENTS.md §8（约定 §十五）归档。

## 0. 一分钟开工

1. **工作区** = `H:\大鲸鱼\Deepseek-EVE`（主树 · 分支 `main` · **一号与船长独占**）；二号 `H:\大鲸鱼\Deepseek-EVE-d2`（`d2/workspace`）；三号 `H:\大鲸鱼\Deepseek-EVE-verify`（`verify40`）。
2. **基线（2026-09-20 08:45 实测）**：`main` 上**最后一个代码提交** = **`50714210`**（仓库两条报障修）；
   其上只有文档提交（二号续接卡 `1a97d6d9` · **本卡**）· **工作树干净** · **比 `origin/main`（`7f3871e1`）超前 7 个提交**
   （未推送）。**本卡自身的哈希不在此处写死**（改一次就变）⇒ 要基线就 `git log -1 --oneline`。
3. **开跑五连**（改任何东西都要）：
   ```
   npm run typecheck
   npm run test -w @whale/core
   npm run content:check
   npm run ui:rot-check
   npm run build
   ```
   文案批再加 `npm run l10n:check`；结构/存档改动加 `npm run save:migrate` 与 `npm run flow:newgame`；文档批加 `npm run docs:index -- --check`。
4. **当前读数（全绿 · 08:31 就在 HEAD 上复跑）**：typecheck 四包 **0 错** · core **173 文件 / 1,896 用例全绿** · `content:check` ✅ ·
   `ui:rot-check` ✅ · `l10n:check` ✅ · 桌面 **build ✅** · `flow:newgame` ✅ · `save:migrate` **71/71** ✅。
5. **本地产物已是最新**：`apps/desktop/out/renderer` 已重建（08:31 那次 `npm run build`）。**船长验收视图默认 = 本地**，
   改完代码记得重建并提醒他 Ctrl+F5 / 重启 `npm run dev`。
6. **未推送（6 条 · `origin/main` = `7f3871e1`）**：`31bc75b0` 本卡 · `c0fcea5b` 合并 · `1a97d6d9` 二号的谜质科技续接卡 ·
   `50714210` 我：仓库两条报障修 · `c9f63199` 合并 · `a640b72b` 二号：资源任务显示均价。
   **推送要船长一句话**（他 2026-09-19 因我未问就推过一次而重申该闸门）；他最近的用词：「合并后推送」「小修改不用发公告」。
7. **⚠ 推送通道**：本机 `git` **直连 GitHub 不通**（`Failed to connect to github.com:443`），但本机有代理
   `127.0.0.1:7897`（在听；注册表 `ProxyEnable=0` ⇒ 没设成系统级）⇒ 用一次性参数推：
   `git -c http.proxy=http://127.0.0.1:7897 push origin main`（**别改仓库/全局 git 配置**）。
8. **别动别人的东西**：二号已合入（`d2` 与 main 差 0）；三号 `verify40` 领先 main **51 条**（本地化工程），
   他卡里照抄着船长令「**继续翻译工作，翻译完成（前）不要合并**」⇒ **不合、只读观察**（详见 §1）。

## 1. 三个工作树现状（2026-09-20 08:45 实测）

| 工作树 | 分支 | HEAD | 与 main 的关系 | 在途 |
|---|---|---|---|---|
| `Deepseek-EVE`（主树） | `main` | **`50714210`** ＋ 文档提交 | — | **干净**（可合入/可推） |
| `Deepseek-EVE-d2`（二号） | `d2/workspace` | `c0fcea5b` | **差 0**（全部已并入 main） | 干净 |
| `Deepseek-EVE-verify`（三号） | `verify40` | `cdcdb915` | **领先 51 条 · 落后若干** | 本地化工程（P1b ID 制 + P2/P3/P5；表 2,816 条 · 未译 308）——**船长令：翻完前不合入** |

其它本地分支（都不是活线）：`verify` `b025397b` · `wip/wormhole` `5dcee26f` · `d3-ship-art-backup` `1b207ddd`（舰船美术草稿）。

## 2. 本会话（一号）办的批次

### 2.1 已推送（在 `origin/main` 之内）

| 批次 | 船长要点 | 提交 |
|---|---|---|
| **筛选统一（四遍 + 戊基线）** | 全仓筛选标签盘点 → 甲（物品与装备）/ 乙（舰船）/ 丙（蓝图）/ 丁（工业与其它）逐遍收敛；判定单点（`itemBucketPasses` / `rackPasses` / `itemSubPasses` / `shipRolePasses` / `shipTierPasses` / `moduleSubKeyOf` / `bpFilterKeysOf`）；对拍 **11,176 ＋ 39,605 ＋ 484 条 · 0 不一致**；**4 型船换类别**（原武装舰 ⇒ 装甲舰：`sh-bullshark` / `sh-wh-e-frigate` / `sh-wh-e-destroyer` / `sh-wh-e-carrier`） | `d26d80a7` · `86dcbf76` · `0a5298c9` · `0edb0f97` · `5317d0b5` 等（`d5fb629a..0f6c09a5` 一段） |
| **谜质科技树（23 节点 3 线）** | 消耗谜质 + 信用点、不耗时；入口 = 「扫描虫洞」拆「虫洞探索 / 谜质科技」；探索/战斗/工业三线；行价 70 万 | 引擎/数据/界面多批（二号主体 ＋ 我收尾），见 `docs/design/matter-tech-20260919.md` |
| **报障：拾取谜质吃掉科技回合（138 → 38）** | 根因 `wormholeSyncMatterTurns` 的 `want` 漏了"当前科技整份"（`turnsBase` 本就不含科技）⇒ 一同步就丢；老档分支还多减一次。修法 = `want = turnsBase ＋ 装置 ＋ 当前科技整份`，老档首次同步自愈 | `6bc31ee0` |
| **读数补齐「谜质科技」那一份（四处）** | ① 准备页回合预算不传 `techTurnBonus` ② 扫描进度分母不吃 `matterTechScanCut` ⇒ 进度条走不满 ③ 入洞「谜质 N 台」不带装置就整格消失 ④ 打捞/采集效率读数缺失（抽 `wormholeWorkEfficiencyOfFleet` 让准备页与入洞同尺） | `90d14ec7` · `8d90e1db` · `b0da2452` |
| **自动探索吃科技（甲案 · 不折减）** | 船长四条：「用实际回合」「完成度 ⇒ 损伤最多减半」「货仓接」「AI 核心吃」。新增 `wormholeAutoTechFactors`（读数与结算同一函数）；不接扫描间隔/战斗倍速/洞外工业三件；只算科技那一份（装配档位效率不进） | `5a6579a7` · `7f3871e1` |
| **谜质科技公告入库** | 船长批准稿 `ann-matter-tech-20260919`（筛选整理批公告**不发**，稿留档） | `be861452` |

> 同期**二号**也有多批并入 main（取消固定种子 `7e481ec0` · 谜质科技契约读数 `440eebad` · 导弹残段／战场远端／纯科技增益修复等），
> 本卡不逐条列；以 `git log --oneline` 为准。

### 2.2 未推送（**待船长验收** · 6 条）

| 批次 | 内容提要 | 提交 |
|---|---|---|
| **仓库两条报障修（船长 09-19 验收中发现）** | ① **装备二级「槽类」三档全筛空**：键空间错配（槽类键喂给了认功能分组键的入口）⇒ 改走 `rackPasses`；探针：142 种模块 高槽 0→55 · 中槽 0→39 · 低槽 0→48 ② **一级「全部」时装备库整块不显示**：丁组把一级键改成 `'all'` 时漏改相邻 `showMods` ③ **悬停改富卡**：装备行 + 图标模式全部卡片原先是纯文本 `title` ⇒ 改走 `hoverTipProps`（`ItemGridCell` 新增 `hover`；`InfoHover` 补 `extra`、`ModuleHover` 补 `hint`；货仓页同族对齐） | `50714210` |
| 二号：资源任务显示每单位均价（已并入 main） | 「报酬后面显示『均价 N 信用点/单位』」 | `a640b72b` · `c9f63199`（合并） |
| 二号：谜质科技批**续接卡**（船长要求「准备交接卡」） | `docs/design/handoff-20260919-matter-tech-d2.md` ＋ INDEX | `1a97d6d9` · `c0fcea5b`（合并） |
| **本卡**（一号 → 下一任一号） | 本文件 ＋ INDEX（哈希见 `git log`） | 文档提交 |

## 3. 关键口径速查（现行 · 细节先查 `docs/glossary.md` 词条）

- **虫洞回合**：基数 **80**（`WORMHOLE_TURN_BASE`）· 质量系数 **0.53** · `turnsBase = 入场预算 − 科技加成`（**不含科技**）、
  `turnsTotal/Left = turnsBase ＋ 装置 ＋ 当前科技整份`；时序锚定器 **+10/级（永久，最多 10 级 = +100）**；装置「时序核心」**+10/台**。
- **谜质科技**：三线 23 节点（探索 6 / 战斗 14 / 工业 3）· 费用 = 虫洞谜质 **枚** ＋ 信用点（逐级表）· **研究不耗时**；
  效果在 core **按 `effect` 关键字聚合**（不认节点 id）；节点表住 `packages/data/src/matterTech.ts`，经 `ctx.matterTech` 供给；
  等级存 `state.research.levels`（`CURRENT_STATE_VERSION ≥ 29`，`MIGRATIONS[28]` 老档补空树）。
- **打捞 / 采集效率**：`Σ 各台档位基础效率`（民用 0 / MK1 20 / MK2 40 / MK3 60 / 异星 80 %）＋ 科技加成（吊臂/钻头各 +20%/级）；
  消费点 = 每次动作的**额外堆** = `floor(效率) ＋ 按余数掷一次`（确定性哈希、不占 RNG 流）；**效率 ≤ 0 不掷骰**。
- **自动探索吃科技**（`wormholeAutoTechFactors`）：`total = (1＋回合加成÷本队实际基础回合) × (1＋货仓加成÷本队基础货仓格)`；
  残骸线（普通/稀有/货柜/**AI 核心**）= `total × (1＋打捞效率)`、母矿线 = `total × (1＋采集效率)`；损伤 ×`(1 − 50%×战斗线完成度)`；
  **不接**：谐振信号滤波阵列（扫描间隔）· 时间压缩矩阵（战斗播放倍速）· 洞外工业三件；
  **只算科技那一份**（舰上装配的档位效率不进）；**未点科技 ⇒ 全 1（零行为变化）**；编队不成立 ⇒ 返回中性系数。
  满树读数（4×长尾鲨）：普通残骸 ≈29 堆 · 稀有残骸顶 95% 旧上限 · 母矿 ≈6.9 堆 · 货柜 80% · **AI 核心 34.6%/趟** · 损伤 −20%~−40%。
- **筛选单点**（改动一律只碰 `apps/desktop/src/renderer/src/ui/itemSubs.ts`）：一级 `itemBucketPasses` · 二级槽类 `rackPasses` ·
  二级/三级子维 `itemSubPasses` · 舰船 `shipRolePasses` / `shipTierPasses` · 装备功能分组 `moduleSubKeyOf` · 蓝图书架/组装机 `bpFilterKeysOf`（住 `panels/Industry.tsx`）。
  **键口径**：一级选择器「全部」= `'all'`，**下级/维度选择器「全部」= `SUB_ALL`**。
- **悬停**（AGENTS §6）：HTML 写 `title`、SVG 写 `data-tip`；**富内容一律 `hoverTipProps(content)` 或 `HoverTip`**（同延迟 `TIP_DELAY_MS=500`、同单例层）；
  同一个元素**禁** `title` 与 `hoverTipProps` 并存；富卡内容工厂 = `itemHoverContent` / `moduleHoverContent` / `shipInfoLines`（`ui/shipInfo.tsx`）。
- **玩家可见文案**：**中英双语**（2026-09-19 船长改判）——界面串走 `useL10n().t('中文源串')` ＋ `i18n/dict.en.ts`；数据层走 `packages/data/src/l10n.ts` 覆盖层；`npm run l10n:check` 校验。
  ⚠ `Wormhole.tsx` / `Handbook.tsx` / `Expedition.tsx` 等大页**尚未接**（三号 `verify40` 上在推 ID 制方案，未合入）。

## 4. 挂账（按优先级 · 每条都给了入口）

1. **推送闸门未过（最高优先）**：**6 条**本地提交待船长一句话（§0.6 / §2.2）；他上次的口径是「合并后推送」＋「小修改不用发公告」。
   **推送时用代理一次性参数**（§0.7）；推完按老规矩核远端头与本地产物。
2. **待船长实测验收（四条实测 ＋ 两条报障观感）**：
   - 筛选统一四遍（甲/乙/丙/丁）的**观感与手感**——工作文档 `docs/design/filters-audit-20260919.md` §七~§十二 逐遍列了"看什么"；
   - **仓库两条报障修**的观感（`filters-audit-20260919.md` §十三）：装备筛选三档出不出货、悬停富卡好不好用；
   - **谜质科技**整棵树的实机手感（实验档 `docs/test-saves/test-save-mt-lab-*.json`，二号造）。
3. **待船长裁决（都是一句话的事）**：
   - **满树自动探索的两个读数偏高**（我已在 `matter-tech-20260919.md` 标出）：AI 核心 **34.6%/趟**（一枚核心是稀缺货币）·
     稀有残骸/货柜**顶到既有 `min(0.95, …)` 旧上限**。要压就给个数（如核心单独封 15%/趟）。
   - **筛选「动态性」基线**（`filters-audit-20260919.md` §五.5 / §三⑦）：是否把"只列真有内容的档"（目前**只有蓝图书架**现算）推广到其余页面，
     或统一改成"静态全列 ＋ 空态文案"。
   - **A5 弹药（弹种/档位）· A6 无人机（机型）**两个子维度需要**新建分类表**，等船长口径（`filters-audit-20260919.md` §八/§九）。
   - **模块目录的 `workEfficiency` 是否也进自动探索**（现口径：只算科技那一份，装配档位效率不进）——要接另起一批。
   - **手册图鉴的图标网格**悬停仍是纯文本 `title`（与仓库/货仓已改的富卡不一致）——要不要一起统一。
4. **归档待办（船长验收 + 合入后当批做 · AGENTS §8 三步）**：
   - `docs/design/matter-tech-20260919.md`（**状态：进行中**）→ 关键内容并入 roadmap/词典 → **删文件** → 重跑 `npm run docs:index`；
     其中两条随归档一并改：`docs/glossary.md` 的「**三联读数**」⇒「**编队读数**」· `docs/test-saves/README.md` 里同一处旧称；
     `EN_MATTER_TECH` 未接进 `ctx.matterTech`（`packages/data/src/context.ts` 一处）——三号接线时一并处理。
   - `docs/design/filters-audit-20260919.md`（**状态：进行中**）→ 同上（结论/数值必须先并入再删）。
   - 公告稿两份：`announcement-draft-20260919-matter-tech.md`（**已批准、已入库、已推送** ⇒ 归档）·
     `announcement-draft-20260919-filters.md`（**船长定不发** ⇒ 留档或随归档处理）。
5. **三号侧（`verify40`，不合入）**：本地化工程 51 条提交（P1b ID 制 · P2 内容 100% · P3 界面进行中 · P5 长文批 10）＋
   他续接卡里「**英文名待船长选词**」（`docs/design/l10n-en-20260919.md`，注意：该文件在 main 上是旧版，新版在 `verify40`）。
   **两条已知冲突面**：① 他改了大量 `t()` 接线，我这边也在改 `Wormhole.tsx` 等页 ⇒ **谁先合谁解冲突**；
   ② 他 `verify40` 上删了 `i18n/dict.en.ts`（ID 制单表）⇒ 若我这边新增中文串，合并时要按他的新制补条目。
6. **工具与环境坑（会在你手上复现）**：`npm run wormhole:econ` 的模拟政策**不处理「临时空间」** ⇒ 深趟收益读数不可信（老账，见上一张卡 §5.4）；
   `git` 直连 GitHub 不通（§0.7）；`tools/*.ts` **不在 typecheck 覆盖内**（改工具要手动跑一次）；
   `docs:index -- --check` 在新增文档后会报"过期"⇒ 跑一次 `npm run docs:index`。

## 5. 铁律与坑（一条都没变 · 全文见 `AGENTS.md`）

- 中文汇报**结论先行**（改了哪些文件 ＋ 行为变化 ＋ 验证结果 ＋ 已知取舍）· **不启动任何子代理/分叉/编排**（§5.1）·
  **一律不许关浏览器进程**（§6）· **观感审查权在船长**（不许自己截图当"改好了"依据）·
  施工/开发语不进玩家可见范围（§5）· 四步闸门（§2：集中提问 → 中文设计总结 → 等显式确认 → 实现）·
  **新提议撞旧裁定先停下摆冲突**（§5.2）· 推送闸门（§4）· 工作期间**只改工作文档**（§8）。
- **本会话新增/重申的裁定**：① 「**小修改不用发公告**」（2026-09-19，与 §7/§12"大的系统更新才发"一致）·
  ② **推送只在船长说「推送」时做**；「合并/合入」= 仅本地 main 提交 · ③ 玩家可见文案**中英双语**（09-19 改判）·
  ④ 「说明文案 ≤30 字」**已废止**（09-18，长度上限/`copyLen`/基线文件全部删除）· ⑤ 改过的文案要**留痕**（代码侧 `⟪文案调整 YYYY-MM-DD⟫` 记号 ＋ 工作文档台账）。
- **PowerShell / 工具坑**（本会话反复命中）：`git` 多行提交信息用 `git commit -F <tempfile>`（heredoc/多行 `-m` 会炸）·
  `edit` 偶发 `ReplaceFileW EIO (Win32 32)`（重试一次即可）· `edit`/`write` 之后**必须重新 read** 再编辑（否则报 "file changed since it was read"）·
  `write` 出 LF ⇒ 中文文档写完用 .NET `UTF8Encoding($false)` 校一遍 **CRLF / 无 BOM** · `Get-Content` 在控制台会把中文弄花（读文件用 read/grep 工具）·
  反引号/`$` 在双引号串里会炸（用 `edit` 工具写代码，别用命令行拼）· 后台任务每 2 分钟无新输出主动查一次，收尾 `job_kill`。
- **临时探针一律 `_` 前缀**，收尾二选一（转正式入库 / 删除）；本会话用过三个（筛选对拍 · 仓库筛选取证），**都已删**（§6 合规）。

---

_维护：本卡是交接件，只在"交接给下一任"时更新；下一任开工后把它并入自己的交接卡，或按 AGENTS.md §8 归档。_
