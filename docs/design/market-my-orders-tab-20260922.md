# 市场「我的挂单」提升为同级子页面（2026-09-22 · 船长令）

> 状态：**已实现并读数核对 · 待船长验收（观感）**
> 船长原话（任务）：「**市场内，将我的挂单放入和常驻订单同级的子页面内，位置排在第一位，但是玩家进入市场时，默认还是打开常驻订单。**」
> 船长原话（两处细节裁定）：「**甲：让行情详情卡独占时撑满右栏**」＋「**钱包删除，托管挪到子标签页内的标题里**」
> 经办：二号 · 工作树 `H:\大鲸鱼\Deepseek-EVE-d2`（分支 `d2/workspace`）

---

## 一、改了什么

| # | 改动 | 位置 |
|---|---|---|
| 1 | 子标签新增「**我的挂单**」并排**第一位**（▤）；`mktTab` 初值**仍是 `'common'`** ⇒ 进市场默认还是常驻订单，只是它在行里变第二位 | `pages/MarketPage.tsx` |
| 2 | 「我的挂单」面板从**右栏**搬进**左栏标签页**（原来的 `<MyOrders>` 组件与逻辑一字不动） | 同上 |
| 3 | 面板**标题行右侧**显示**托管**读数（「托管在售/在途 N 件」）；**钱包读数删除**（顶栏本来就常显余额，页内不再重复） | 同上 |
| 4 | 右栏只剩详情卡 ⇒ **详情卡吃满右栏**（下限仍保 400px，窗口矮到一定程度由面板体内滚兜底，绝不裁内容） | `styles.css` |
| 5 | 「整行对齐」那套测量的**订阅容器**从 `.app-mkt-right` 改成 `.app-mkt-left`（面板换栏了，不改就量不出来） | `pages/MarketPage.tsx` |
| 6 | l10n：删掉 `ui.MarketPage.114`（余额）与 `ui.MarketPage.115`（信用点 · 托管在售/在途）两条**已无调用点**的条目；新增 `ui.MarketPage.179`（托管在售/在途，尾巴沿用 `ui.MarketPage.135` 的「 件」） | `packages/data/src/l10n/table.ts` |

**没动**：搜索结果合并那条路径（`filterActive`：有搜索词或选了类型时的跨档合并列表）——挂单行内跳转仍照旧走它；
商品列表、详情卡、窄屏浮窗、托管/撤单等任何账目逻辑，一律未改。

## 二、与旧口径的关系（**同日新令取代**）

- `MarketPage.tsx` 里 2026-09-21 那条注释写的是「**「我的挂单」照旧留在栏内**（它不是"详情"，
  宽窄都要能一眼看到钱包与托管额）」——**本次船长令把"留在右栏"改成"搬进子标签页"**，
  同时把"钱包"从页内**删除**、把"托管"跟到「我的挂单」页的标题行 ⇒ 09-21 那条做法被同日新令取代，
  注释已就地改写（旧文保留在 git 历史里）。
- `styles.css` 里 2026-09-14／09-15 那套"右栏两块分富余"的规则（详情吃上限、挂单吃富余）
  **随之作废**（右栏此后只有一块）；注释里保留来龙去脉备查。

## 三、读数核对（`tools/_market-tab-check.ts`，视口 1600×900，真档 `save-20260920-164600`）

| 读点 | 实测 |
|---|---|
| 标签行 | `▤我的挂单 / ≡常驻订单 / ✦稀有订单 / ◈限定奇货` ⇒ **我的挂单排第一** ✅ |
| 进市场默认选中 | **≡常驻订单**（序号 1）✅ 默认没变 |
| 切「我的挂单」后 | 左栏面板标题「我的挂单」· 标题行右侧「**托管在售/在途 1,281,271 件**」· 挂单行 7 行 ✅ |
| 页内还有没有"余额" | **false** ✅（钱包读数已删） |
| 右栏 | 面板数 1 · **面板高 685 = 列高 685** ⇒ 详情卡吃满右栏 ✅ |
| 整页滚动条（宽屏一级页不滚） | **false** ✅ |
| 窄屏 1000×900 | 右栏面板 0（原样：窄屏不渲染详情、点订单弹浮窗）· 标签顺序与默认同上 ✅ |

⚠ 这是**读数**、不是观感结论（约定 §6）：好不好看、间距与手感请船长实测判定。

## 四、闸门

`npm run typecheck` ×4 全绿 · core **184 文件 2093 用例**全绿 · `content:check` ✅ ·
`ui:rot-check` ✅（UI/样式改动必跑） · `l10n:check` ✅（**无死引用**：删掉的两条确实已无调用点）

## 五、待船长

1. **观感**：四个标签的排布与「托管」落在标题行右侧是否合意？（要挪成"接在标题文字后面"也是一行的事）
2. 要不要现在合入 main 并推送（上一轮刚推过一次；本次改动只在市场页与文案表）。
