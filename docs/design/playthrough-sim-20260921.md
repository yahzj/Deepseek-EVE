# 全流程模拟工具 renovate（2026-09-21 · 船长令「更新下全流程工具，进行一次全量跑通测试」）

> 状态：**进行中**（工具已更新到能跑、可诊断；三条目标**已达成 1 条、虫洞 3/6 枚**）
> 船长原话（照抄）：「**更新下全流程工具，进行一次全量跑通测试，设置几个目标：完成所有悬赏，虫洞的成就完成，赚到10亿。**」
> 追问两答：虫洞成就 = **六枚虫洞里程碑**（层深 2/3/4/5 ＋ 击破层末守卫 2/4）；跑法 = **默认快进**（`debugQuick`）。
> 工具：`tools/playthrough-sim.ts`（约 2450 行）· 运行 `npx tsx tools/playthrough-sim.ts --goal bounties,whach,isk1b`。

## 一、三条目标的口径（已落码）

| 目标 | 判据（引擎里的唯一台账） | 现状（40~45 天档） |
|---|---|---|
| `bounties` 完成所有悬赏 | `state.completedBounties` 收全 **23 张可见悬赏卡**（`ctx.anomalies` 里 `hidden !== true` 的全部） | ⬜ **10/23** |
| `whach` 虫洞成就 | `firstStats.whMaxDepth ≥ 5` **且** `firstStats.whBossClears ≥ 4`（六枚里程碑全拿） | ⬜ **层深 2/5 · 守卫 6/4**（六枚中 3 枚 ✅） |
| `isk1b` 赚到 10 亿 | `state.wallet.isk ≥ 1,000,000,000` | ✅ **第 11.6~22.5 天达成**（多次复现） |

`--goal` 默认值已改为 `bounties,whach,isk1b`；旧的 `boss` / `tril` / `collect` / `all` 原样保留。

## 二、这一批修掉的**九处**缺陷（每处都有实测证据）

前四处是策略问题：
1. **`ensureFlagship` 换驾后不配装** —— 炮全装到**采矿艇**上（日志实证：「装配 mod-turret-kin-2（**沙猫级采矿艇**）」），
   真出去打仗的护卫舰**一直裸着** ⇒ 对威胁 ≥34 的卡预估胜率一律 2%。
2. **战力打分口径错** —— 原用 `tier + powerBonus`，而 **2026-09-17 起装甲舰 `powerBonus` 一律 0**
   ⇒ **T3 重装巡舰永远输给 T1 护卫舰**。改为 `shipPowerScore = tier×100 + 槽位总量×2`。
3. **升级流程跑不到** —— `buyShipAndGear` 只在 `homeLull()` 分支里调，模拟长期在远征/采矿循环里 ⇒ 整段跑不到。
   改为主循环**每拍**都调（函数内自带守卫）。
4. **只装驾驶船** —— 虫洞要**整队 4 艘**，而编队里三艘僚舰全是裸的 ⇒ 真跑门稳定"0 秒团灭"。
   改为 `autoFitGear(uid)` 支持指定船 + 每拍给最多 3 艘没炮的僚舰补装。

后五处是**虫洞专项**（按发现顺序）：
5. **没有整队战力快照** —— 引擎自带 `buildEvalState`（`packages/core/src/winEstimate.ts`）**只复制一艘船**（给单舰蒙特卡洛用的），
   拿它跑 `startFleetBattleFor` 时**编队另外 3 艘在快照里根本不存在**、我方只有 1 个单位参战
   （诊断实证：`我方残血 …（1 单位）· 编队 4 艘`）⇒ 4×T3 满配也稳定"第 1 层真跑打不过"、**一趟都进不去**。
   修法：新写 `buildFleetEvalState()`，把**整队**搬进同一份快照（口径照抄 `buildEvalState`）。
6. **没有"凑齐 4 艘 T3"的备战策略** —— `buyShipAndGear` 的判据是"**比驾驶船更强**才买"，
   天生只把主控那一艘往上换 ⇒ 终态永远是"1 艘 T3 + 一堆 T1"，而虫洞按 **4×T3 满配**配平。
   修法：新增 `ensureWhFleet()`（凑齐 4 艘 T3 战斗舰；**凑齐后只做升级到 T4**，且必须先卖掉编队里最弱那艘腾位）。
7. **进洞门只要"赢"不看"剩多少血"** —— 第 1 趟"赢了但只剩 4.1% 血" ⇒ 第 2 层当场团灭、四艘全沉。
   修法：`whFloor1Outcome()` 返回**战后残血比**，门要求 ≥ `WH_ENTRY_HP_MIN = 0.45`（与"撤退线"同一个数）。
8. **门不过时报告里什么都没有** —— 只写"进洞 0 次"，看不出是没编队/不够档/打不过/血不够。
   修法：`whGateReason()` + 「第 1 层真跑」逐次诊断（`ended`/步数/双方残血/单位数）+ 逐层读数 + 每日在洞体检。
9. **报告里程碑被 `slice(0, 150)` 截断** —— 排查"虫洞日志在 10.53 天之后消失"时，
   真因就是这 150 行上限把后半程全截了（探针一直在跑、模拟并未卡死）⇒ 提到 400 行并注明别调小。

## 三、当前真实卡点（下一步该做什么）

**卡点只有一个：钱不是问题（60 天 286 亿），"编队战力够不够打第 1 层"才是。**

60 天全量跑（`--goal bounties,whach,isk1b`，墙钟 131.5s）的实测链：

- **10.37~10.40d**：四艘 T3 一次凑齐（现货恰好连出四张）⇒ **编队 4/4**；
- **10.41d 第 1 趟进洞** ⇒ 第 1 层残血 100%（4/4 艘在场）⇒ 打掉守卫 ⇒ 第 1 层**入账**、立刻撤离；
- **10.52~10.53d 第 2、3 趟** ⇒ 层深 **2**（"初入深渊"✅）＋ 守卫账本 1；
- **11.01d 起**：`whCapableShips()` 掉到 **3/4** ⇒ 只能拿 3 艘去凑整套、第 1 层真跑不过；
  **12.01d 又回到 4/4**（10 艘船里 4 艘 T3、炮 5/5/3/5＝18 门），
  但**第 1 层真跑仍判"打不过"**（`ended=foe`）⇒ 门一直拦着，层深停在 2。
  ⇒ 结论：**4×T3 这档在第 1 层的余量已不足 45%**（不是"进不去"、也不是"没编队"）。

**所以要解决的是"编队战力再上一档"**，两条路（都没实现）：

1. **T4（玄武级）现货路走不通**：虫洞允许到 T4（`WORMHOLE_MAX_TIER = 4`；2×T4+2×T3 折算质量
   16000 正好压线、回合预算 40），但 T4 现货要**声望 20**，实测 60 天只到 15；
2. **用一次性舰船蓝图自造（推荐）**：`sbp-once-thresher` 等是 `singleUse` 蓝图，
   料价约 370 万/艘（现货 750~1550 万）、**不受"rare 抽取 1 艘/10 分钟"限速**；
   T4 也有 `sbp-once-xuanwu`（声望 25）。需在 `doLearnCraft` 上补一条
   "按虫洞编队缺口重复造同型"的策略（当前一张蓝图只造一次）。

**另外两处待办**：
- **悬赏那条**（10/23 卡住）：剩下的 13 张是威胁 58~96 的卡，与虫洞同一个"战力不足"根因；
- `ensureWhFleet` 与 AI 出勤调度的互斥要显式化（实测编队会在 3/4 与 4/4 之间来回跳）。

## 四、读数留档

| 配置 | 声望 | 首胜 | 层深 | 守卫 | 10 亿 | 阵亡 |
|---|---|---|---|---|---|---|
| 旧工具（默认 `boss`） | 10 | 9 | — | — | — | — |
| 修缺陷 1~4 后（35 天） | 9 | 8 | 0 | 0 | ✅ 22.5d | — |
| 加整队快照 + 备战（25 天） | 13 | 10 | 2 | 1 | ✅ 11.6d | 0 |
| 加"入账即撤" + 备战升级（45 天） | 15 | 10 | 2 | **6** | ✅ 11.96d | **0** |
| **60 天全量（本批终值）** | **15** | 10 | 2 | 2 | ✅ **11.81d** | **0** |

**六枚里程碑现状**：✅ 初入深渊(2) · ⬜ 深渊宿将(3) · ⬜ 深渊之主(4) · ⬜ 深渊彼岸(5) · ✅ 斩层者(2) · ✅ 守关终结者(4)。

## 五、验证与边界

- 本批**只改工具**（`tools/playthrough-sim.ts`）与本文档，未改 `packages/**`、`apps/**` ⇒ 游戏行为与四闸门不受影响。
- ⚠ 本工具是**读数型**验证，**不替代船长观感审查**；三张目标里 `whach` / `bounties` **都还没达成**，
  报告如实标注，未做任何"看起来达成"的粉饰。
- 报告里新增的诊断行（备战待购/进洞门/逐层读数）是**给排查用的**，不是游戏内文案。
