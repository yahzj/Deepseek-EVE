# 盘面阶梯回退（每 2 层 +1 环、不封顶）＋ 虫洞地图可拖动（2026-09-20 二批）

> **状态：进行中**（已实现，四闸门全绿，待船长验收）
>
> **船长原话（照抄，两句）**：
> ① 「**那还是改回「每 2 层 +1」的机制，不封顶**」
> ② 「**以及允许玩家拖动虫洞探索地图**」
>
> 两句都发生在同一天第一批（"每 1 层 +1 环、上不封顶"）落地并推送之后 ⇒ 本件是**当日二次改判**，
> 它同时**改写已归档的口径**（见 §五 归档待办）。

## 一、阶梯：改回"每 2 层 +1 环"、**不封顶**

| 项 | 第一批（作废） | **本批（现行）** | 旧旧口径（更早） |
|---|---|---|---|
| 半径 | `2 + (层−1)`（每层 +1） | **`2 + ⌊(层−1) ÷ 2⌋`（每 2 层 +1）** | `min(4, 2 + ⌊(层−1)÷2⌋)`（**封顶 R=4**） |

**差别只在"节拍"与"封顶"两点**：节拍回到每 2 层（= 更早那版的节奏），封顶**取消**（层 7 起继续长）。
⇒ 层 1~2 = R2 · 层 3~4 = R3 · 层 5~6 = R4 · 层 7~8 = R5 · 层 9~10 = R6 …（旧口径层 7 起恒 61 格，现在继续涨）。

**1~12 层明细（实测 · 生成器跑 200 个种子）**：

| 层 | 半径 | 盘面格数 | 空占比 | 空格数 | 可分配信号格 | 信标 | 星云 |
|---|---|---|---|---|---|---|---|
| 1 | R=2 | 19 | 50% | 10 | 9 | 1 | — |
| 2 | R=2 | 19 | 49% | 10 | 9 | 1 | — |
| 3 | R=3 | 37 | 48% | 19 | 18 | 2 | — |
| 4 | R=3 | 37 | 47% | 18 | 19 | 2 | 4（20%） |
| 5 | R=4 | 61 | 46% | 29 | 32 | 3 | 8（22%） |
| 6 | R=4 | 61 | 45% | 28 | 33 | 3~4 | 8（24%） |
| 7 | R=5 | 91 | 44% | 41 | 50 | 5 | 13（26%） |
| 8 | R=5 | 91 | 43% | 40 | 51 | 5 | 15（28%） |
| 9 | R=6 | 127 | 42% | 54 | 73 | 7 | 22（30%） |
| 10 | R=6 | 127 | 41% | 53 | 74 | 7 | 24（32%） |
| 11 | R=7 | 169 | 40% | 69 | 100 | 10 | 34（34%） |
| 12 | R=7 | 169 | 39% | 67 | 102 | 10 | 37（36%） |

- 上表里**只有信标数会浮 1**（层 6 = 3 或 4）：池子变小后，「内容原型」对四类信号权重的再分配会把
  **信标配额的取整落点**顶过 0.5 边界（与旧口径"层 9 起浮 1"同一性质，起点提前）。
- **舰船 / 残骸 / 资源 / 谜质 四列随该洞的「内容原型」浮动**（既有机制，不是本批引入），故不逐层钉死。
- **波数、遗迹/谜质保底、空占比 −1%/层、星云 +2%/层封顶 80%、回合预算**一律**不动**。

## 二、连带改动

- `wormholeGrid.ts`：`wormholeGridRadiusFor` 回退 + 注释改写（写明"同日两次改判"的来龙去脉，防再被回改）；
  遗迹保底那句注释恢复"两条阶梯同步（都是每 2 层 +1）"的事实。
- 测试重钉：`wormhole-grid.test.ts`（半径阶梯 → 层1/2=2 · 3/4=3 · 5=4 · 10=6 · 20=11 · 99=51；
  信标数改**区间**表达，层 6 = 3~4）· `wormhole-nebula.test.ts`（层 5 半径 6→4；"深层信号格增长"
  改取层 14（R8）与层 1 比 ⇒ 新节拍下仍成立）。
- **第一批的存档护栏不动**（`WORMHOLE_GRID_SAVE_MAX_R = 64`）：它是"坏档护栏"、与阶梯解耦，
  本批改阶梯**不需要**动它 —— 这正是上一批事故的教训所在。

## 三、地图可拖动（新功能）

**口径**：放大后（`zoom > 1`）**按住地图即可拖动**，拖的是整张盘（格子 / 舰影 / 路径预览线一起走）。

| 面 | 做法 |
|---|---|
| 手势 | `pointerdown` 记起点、**捕获指针**（拖出地图框也不断线）；`pointermove` 累加平移；`pointerup/cancel` 收尾 |
| 阈值 | 位移 **< 4px 当点击格子**（不进入拖动）⇒ 手抖不会把"点格前往"变成拖图 |
| 夹取 | `wormholeMapPanClamp`：每轴夹在 `±(z−1)·半幅` 内 ⇒ **怎么拖都不会把盘面拖丢**；`z = 1`（适应窗口）无余量 ⇒ 拖动自然不生效 |
| 与点击 | 拖过之后**吃掉紧随的那次 `click`**（`mapDragAteClickRef`，下一次 `pointerdown` 清）⇒ 拖完不会顺手走一步 |
| 与缩放 | 平移叠在**同一个缩放层 transform** 上（锚点仍以玩家格为中心）；拖动中加 `.is-panning` **关掉 220ms 过渡**（否则每帧追动画，手感发飘） |
| 复位 | 换层/进出洞与「适应」按钮都复位平移（与自动缩放同一处：`layerKeyForFx` 那条 effect） |
| 触屏 | `.app-wh-mapbox { touch-action: none; cursor: grab }` + `.is-panning { cursor: grabbing }` ⇒ 手指按在图上就是拖图，不滚页面 |
| 说明 | 图例 ⓘ 那句（`ui.Wormhole.145`）补"放大后按住地图可以拖动查看"（**改既有 id 的值**，不新增 id、中英同步） |

**不做**（本批）：双指捏合缩放（滚轮/按钮已够）· 拖动惯性/边界回弹 · 拖动时的小地图或"回到玩家"按钮。

## 四、验证

- `npm run typecheck` 四包 **0 错** · `npm run test -w @whale/core` **177 文件 / 1,975 用例全绿**
  （阶梯与信标数已按新节拍重钉）· `npm run content:check` ✅ · `npm run ui:rot-check` ✅ ·
  `npm run l10n:check` ✅ · `npm run build` ✓（渲染层构建通过）。
- 读数（1~12 层表）由临时探针实测（200 种子），**用后即删**（§6）。
- ⚠ 拖动是**界面行为**：几何量（夹取范围、阈值、复位时机）已按上面口径实现并自查；
  **手感与观感由船长验收**（§6「观感审查权在船长」——不自评）。

## 五、归档待办（§8：工作期不改旧文档）

- ⚠ **已归档口径要改写**：`docs/roadmap.md` 的 9-20 盘面条目与 `docs/glossary.md`
  「盘面半径（每层 +1 环 · 上不封顶）」写的是**第一批**的口径 ⇒ 归档时改写为"**每 2 层 +1 环、不封顶**"
  （1~10 层表同步换成本件 §一），并把「地图缩放配套」那句补上"可拖动"。
- `docs/design/wh-grid-deep-save-20260920.md`（深层盘读档那条工作件）里引用的半径数字按本件更新。
- roadmap 一条精简条目（可并进"盘面阶梯"那条，作为**当日二次改判**）。

---

_维护：本件是工作文档，验收后按 AGENTS §8 归档（关键内容并入 roadmap/词典 → 删文件 → 重跑 docs:index）。_
