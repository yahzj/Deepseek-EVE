# 护盾充能力场装置 MK2 / MK3 的 CPU 占用上调（2026-09-24）

> **⚠ 已并入（2026-09-26 标注）**：本稿（力场装置 MK2/MK3 的 CPU 55/80 → **63/91**）是护盾充能力场这条线的**前一阶段稿**，关键内容**已并入 `docs/roadmap.md` 2026-09-25/24 那条「护盾充能力场：回盾量改按装件舰满盾 ＋ MK2/MK3 的 CPU 上调到 63/91」**（与后一阶段 `shield-field-caster-20260925.md` 的施放者口径合并成一条）。本稿按船长选定**保留不删**、只作历史过程留档；CPU 现值 **63 / 91**、冷却 MK2 10 秒 / MK3 8 秒不变。

状态：**进行中** —— 改动**已落码**，四闸门 ＋ `balance` 全绿，**待船长验收**；
归档按 AGENTS.md §8（船长验收 + 合入 main 后当批做：关键内容并入 roadmap → 删本文件 → 重跑 `docs:index`）。

## 船长原话（照抄）

「**护盾充能力场装置 MK2和MK3的CPU占用提高到63/91**」（2026-09-24）

## 范围 / 不做

- **做**：只改这两件装备的 `cpuUse`——**MK2 55 → 63** · **MK3 80 → 91**（`packages/data/src/modules.ts`）。
- **不做**：不动冷却（MK2 10 秒 / MK3 8 秒）· 不动每跳比例（各 10%）· 不动叠加惩罚 ·
  不动稀有度（4 / 5）· 不动价格（400 万 / 2000 万）与一次性蓝图渠道 · 不动 MK1（本件**没有 MK1**）。

## 落码与留痕

- `packages/data/src/modules.ts` 两处 `cpuUse`，各带一行 **`⟪2026-09-24 船长令⟫`** 标注（照本仓改值留痕的既有手法）。
- 这是**纯数据数值改动**：零接口、零存档、零文案结构改动。

## 连带面（都已核实）

| 面 | 结论 |
| --- | --- |
| **内容工作台（第三份派生件）** | ⚠ **必须重导**：`content-csv/modules.csv` 与 `content-workbench.xlsx` 里原写着 55/80，不重导就会像 2026-09-15 那次「旧名称还在」一样，船长手上的工作台停在旧值；**且下次 `content:import` 会把 55/80 写回来**。已跑 `npm run content:export`（8 张表 956 条），核对两行 = **63 / 91** ✅ |
| **「蓝图说明契约」** | 不涉：该契约按说明里的 `CPU N` / `N 点 CPU` 与真实值核对，而本件蓝图与模块说明**都不含 CPU 数字**（说明只写"每 N 秒展开力场…"）⇒ 不会因改值而报红 |
| **用例** | 无断言真值：`shield-field.test.ts` 里那个 `cpuUse: 55` 是**合成夹具**（`moduleDef()` 造的假件），不是本装备 ⇒ 未动 |
| **逐型号账本 / 存档** | 无涉：力场账本 `shieldFieldBy` 记的是计时与跳数，**不含 CPU** |

## 验证读数

- `typecheck` 四包 **0 错** · core **201 文件 / 2272 用例全绿** ·
  `content:check` ✅（仅既有警告：两条 `bp-shieldfield` 书价偏离，**与 CPU 无关、改动前就在**）·
  `l10n:check` ✅ · `ui:rot-check` ✅ · `save:roundtrip-audit` ✅（84 份往返 · **丢键 0**）·
  桌面 `build` ✅（1.83 秒）· **`balance`** ✅（三策略 24h 对比照跑，读数表照出）
- 横向对照（供船长手感参考）：同族**中槽**「护盾充能装置」MK1/2/3 的 CPU 是 **25 / 45 / 70**；
  本件是高槽、**治全队**、冷却自带 ⇒ 63 / 91 落在"明显更贵但非装不上"的一档。

## 待裁决点

- **要不要发公告**：本件是既有装备的数值上调，按 §十二 判定可能不构成"大更"——**待船长定**
  （与同日「敌方挂载件要不要发公告」一并问过）。
