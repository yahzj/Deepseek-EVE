# 多波次敌人设定(低安顶段悬赏)设计稿(2026-09-09;**已确认并实现·待验收/待发布**)

> 背景:敌人强度复调轮(r4)real sim 终验打穿终局闸 → 船长定:**暂停单敌血量微调路线**,
> 引入多波次机制化解"后期单体血量过大",并作为**低安加强**的实现方式。
> **2026-09-11 状态更新**：本稿已按拍板落地——`anomalies.ts` 四张卡（噬口/坟场/虚海/穹顶）的 `waves`
> 均已写入生效，测试档 `test-save-wave-*` 已交付；原"待终审"标注作废（仍待的是**验收与随批发布**）。
> 船长拍板(2026-09-09):首批 = 深低安顶段 4 张(噬口 80 / 坟场 88 / 虚海 88 / 穹顶 96);
> 单场目标 **90~150s**、波间无喘息;胜率预估 **同步校准**(按总波战力折算 + 修正低估)。
> 复调轮 r4 数值保留,随本机制统一重构(每卡总血 = 现 r4 foeHpOverride,拆波不改总预算)。

## 一、机制(引擎)
- **波次 = 同场续刷**:战斗保持"一次悬赏整场结算"(声望/首胜/奖金/残骸注入按原卡,不按波发奖)。
  敌方单位清空且未到末波 → 引擎补刷下一波(同族敌舰,按每波构成生成;波间无喘息,
  不重置双方状态,护盾/回充照常);
- **数据结构(AnomalyDef 可选字段,零迁移)**:
  `waves?: Array<{ units: number; hpShare: number; escortMul?: number }>`
  —— units = 本波主舰数;hpShare = 占该卡总战力的血比例(Σ≈1);escortMul 预留(僚机随波)。
  缺省 = 单波(现行为,全量兼容);有 waves 时引擎按波生成:单波总血量 = 现敌血算法
  (foeHpOverride/曲线) × hpShare,单舰血 = 波血 ÷ units;
- **推进**:敌清空 → 波次+1 补刷(波间同 tick 续战);末波清空 → 正常胜利结算;
  中途败北 = 原失利规则;撤退/召回沿用现状(整场语义不变);
- **UI/日志**:战斗横幅/日志提示「第 N/总 波来袭」;结算与现状一致。

## 二、首批 4 卡波表建议(总血 = r4 值;按目测可调,终审时可改)
| 卡 | 总血(r4) | 建议波表(units × hpShare) | 意图 |
|---|---|---|---|
| 噬口猎杀令 80 | 4000 | 2 波:2×0.5 → 1×0.5 | 先双舰压阵、再单体强敌收尾 |
| 坟场守墓人 88 | 3600 | 2 波:2×0.55 → 1×0.45 | 守墓编队前哨 + 主墓卫 |
| 虚海守望者 88 | 4300 | 3 波:2×0.35 → 2×0.35 → 1×0.30 | 守望巡弋层层递进 |
| 穹顶守卫 96 | 5600 | 3 波:2×0.35 → 2×0.35 → 1×0.30 | 终局"穹顶卫队"压迫感 |

## 三、胜率预估/AI(同步校准)
- `winEstimate`/preview:按卡总战力口径折算多波(每波 hpShare×units 计入敌方总持续火力/血量
  近似),并**校准已知低估**(09-06 决策点 5:实测穹顶 100% 而 preview ~87%)——校准目标 =
  预估向 battle:calibrate 实战收敛(±5pp 内),玩家悬赏列表胜率展示、AI 85% 闸、real sim
  最终验证闸同源受益;
- 校准杠杆(设计时定):logit 扩散系数/敌方火力折算(不在本稿展开,实现时按矩阵拟合)。

## 四、范围与边界
- 首批仅上述 4 张悬赏(主控与 AI 同源跟随,引擎自然支持);低安伏击遭遇=当地悬赏卡,
  若抽中多波卡 → 伏击战同样多波(结算语义一致,无需单独实现);
- 其余悬赏/中低安维持单波;复调轮 r4 数值保留至本机制统一接管(单卡拆波后旧 foeHpOverride
  即为其总预算);
- 时长:单波卡不受影响;多波卡单场目标 90~150s(实测校准,不再硬卡 40~90s 带)。

## 五、验收
- battle:calibrate 新增"多波卡实战时长/胜率/残血"行(锤头鲨/S2/S4 对照,3 技能档);
- real sim `--real-training --goal boss` 通关(终局预估 ≥85%,复跑不再黑洞);
- 新增核心回归(波次续刷/末波结算/中途败北/零迁移缺省单波);
- core 全量 + typecheck ×4 + content:check + desktop build 绿;
- 文档:本稿转"已确认/已实现"、power-ladder §六 状态同步、roadmap 变更记录;
- 公告:待船长决定(低安加强+战斗体验变化,建议发)。

## 六、实施状态(2026-09-09 晚,引擎已落地)
- **引擎**:`AnomalyDef.waves`(units 小队 × hpShare,零迁移)+ `BattleState.waveIdx`(存档透传);
  `createFoeSpecs` 波化(首波沿用 foe-0 旧 tag,后续 `w{n}-` 前缀);`advanceBattleFor` 波循环
  (敌清空同 tick 续刷下一波、无喘息、波次日志);`stepBattle` 末波清空才判胜;
- **敌侧属性口径(船长拍板维持现状)**:总血量预算不变(份额切分),**单舰输出保持整卡威胁满规格
  (hpShare 只进血量不进伤害)**——拆波后峰值火力反而高于单波(前两波双舰同场),无任何削弱;
- **波表形态(船长拍板维持小队流)**:噬口 2+1 / 坟场 2+1 / 虚海 2+2+1 / 穹顶 2+2+1;
- **实测矩阵(锤头鲨中位)**:噬口 83s/残血97% · 坟场 71s/112% · 虚海 118s/91% · 穹顶 138s/70%
  (单波 r4 对照 33~72s/109%);时长×1.9 主因 = 多单位"过杀浪费"(单发大伤打小分舰)+ 随机目标
  均匀磨血(敌全程满员输出)+ 双舰峰值承伤;上一档(S2 灰鲭鲨MK2)穹顶 0%、无技能锤头鲨穹顶 0%
  ——低安加强强度目标达成(设计稿原"可磨/无技能宽容线"在顶卡按新难度失效,已在 §五 记录);
- **预估校准(落地路线 A)**:steady 稳态对长盘多波系统失真(实测 100% vs 预估 11~37%,两轮物理
  修正(多波峰值火力/减员平均/回充窗口)不足以收敛)→ playthrough-sim 的 boss 判定/多波卡选择
  改用**与实战完全同构的蒙特卡洛**(winEstimate,9 局,满耐久口径,配置指纹缓存);
  单波卡维持 battleWinPreview;steady 修正保留(活动远征视图/AI 展示口径方向性改善);
- **终验**:`--real-training --goal boss` **15.66d 通关**(穹顶连打 5/5,MC 预估 89%),黑洞 0,
  无引擎异常;首轮 run 第 5 局概率性战败(89%^5≈55%)自动重试后全通,机制符合预期;
- 全量:core 571/571 + typecheck ×4 + content:check + desktop build 绿【待验收/待发布】。

### 六·补 1(2026-09-09 晚,船长实测反馈二轮:转场演出)【已确认·待合入】
- **引擎演出窗口(替代"同 tick 无喘息续刷")**:波全灭瞬间记 `BattleState.waveClearAt`(存档透传,
  零迁移)= 战斗时钟 + `balance.battle.waveEnterGapMs`;窗口内推进只停表等待(**战斗时钟不推进**——
  与击杀慢镜同语义,演出时间不计入 maxBattleMs 超时),窗口结束才补刷下一波 + 回拉距离
  (waveReopenFrac 见前 commit);大步长/离线推进越过窗口即立刻续刷,无额外等待;
- **窗口长度口径 = UI 残骸演出全长**:致死弹道(≤760ms 导弹)+ 爆炸环演出(1700ms)+ 灰舰淡出
  (520ms)+ 一拍的检测延迟 → `waveEnterGapMs 900 → 3300`(0=立即,可随时调);
- 波全灭新增日志「第 N/M 波已全灭…敌方增援正在从远处入场」;战斗界面窗口内显示
  「第 N/M 波增援正在接近…」提示(引擎 waveClearAt 驱动,玩家可见文案无开发口径);
- **UI 残骸演出重构(V18B-3 撤队+锚点层 → 原位占槽整段演出)**:尸骸不撤队,boomAt(致死弹道
  着弹)前原样停留 → 灰化 + 爆炸环 → 原位淡出(内联透明度压子层,避入场动画 fill 覆盖);
  撤出只允许「淡出完成且右侧无演出期尸骸」——整批演完才一次收拢,存活舰补位不再压着
  爆炸/淡出动画走;弹道几何按视觉行序(含占位尸骸)计算,主/僚舰艏偏移按 tag 判定
  (多波主舰 w{n}-foe-* 非队列首位同样大舰艏);
- **预算头**:单次大预算推进(胜率 MC/校准工具)预算外加全波窗口总时长
  (`waveGapTotalMs` helper;winEstimate/`battle-calibrate`/`bounty-econ` 接线),末段跨窗口
  不再提前耗尽预算;远征战斗进度条按战斗时钟算(间隙/慢镜不空耗进度);
- 回归:wave-battle +1(白盒清波→窗口冻结不刷→跨窗口补刷,时钟豁免断言);
  core 582/582 + typecheck ×4 + content:check + desktop build 绿【待验收/待发布】。
