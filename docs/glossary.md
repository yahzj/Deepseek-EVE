# 术语词典（Glossary）

> **仓库级术语权威**（约定见 `docs/development-conventions.md` 第十一章）。
> 用途：跨会话沟通 / 写文档 / 写注释前查词，避免压缩上下文后名词漂移。
> 维护：新系统定稿词由经办人当天登记（定义/公式/出处/状态）；与 `docs/design/` 冲突以本词典为准并回头修订。

---

## 一、领域与流程

| 术语 | 含义 | 备注 |
|---|---|---|
| 船长（旧记"老大"） | 用户本人；游戏设计与数值最终拍板人 | 全程只用"船长"这一个称呼（"老大"为早期写法，作废） |
| 大鲸鱼一号 / 二号 / 三号 | 并行工作 agent 代号（**一号** = 主树 `main`：UI/renderer/引擎壳与技能文案域为主；**二号** = `…-d2` 工作树：战斗/数值/系统设计域为主；**三号** = `…-verify` 工作树：核验与收尾） | 分工与边界见 `AGENTS.md` §4、`docs/development-conventions.md` §三 |
| **抽取单点（`pickOne` / `pickWeighted`）** | **2026-09-12 审计 B2/B3 单点化**（`packages/core/src/rng.ts`，从 `index.ts` 导出）：把原先散在 6 个文件的 **18 处**"均匀取一个"（`arr[Math.floor(nextRandom(rng) * arr.length)]`）与 **6 套**"按权重取一个"（各自手写 `r -= w` / `acc += w` 循环）收成两个函数。**关键契约 = "抽几次随机数"也一致**：`nextRandom` 的消耗次数与手写式逐一相同（**含空表与总权重 ≤ 0 的路径也先抽再判**——`count` 进存档，序列一挪位离线结算结果就全变）；权重按 `max(0, w)` 夹紧；`bound: 'lt' \| 'lte'` 只用于逐字复刻各处原算术形式（两者只在"精确命中累计边界"时不同，概率 ≈ 2⁻³²，不可观测）。守卫 = `tests/rng-pick.test.ts` 6 例（与手写参照实现逐位对照、含 `rng.count`）+ 既有种子确定性用例全量 | `rng.ts` · 替换点：`salvage` / `combat` / `encounters` / `expedition` / `market` / `mining` / `salvaging` / `events` |
| 四步闸门 | 设计/数值改动流程：集中提问 → 中文设计总结 → 等显式确认 → 实现 | 最高优先级约定 |
| 主仓库 / 工作树 | 主仓库 = 本 git 仓库，主工作树 `H:\大鲸鱼\Deepseek-EVE`（分支 `main`）；另有**同仓 `git worktree`**：二号 `H:\大鲸鱼\Deepseek-EVE-d2`（分支 `d2/*`）、三号 `H:\大鲸鱼\Deepseek-EVE-verify`（分支 `verify`） | 早期「副本 = 一号为流程验证复制出的独立工程」**已废弃**（2026-09-05 起改 worktree 模型） |
| 真档 / 门槛档 | 真档 = `%APPDATA%\whale-idle\save.json` 船长真实进度；门槛档 = tools/make-test-save.ts 注入的验收存档（docs/test-saves/） | |
| 高安 / 中安 / 低安 | `security ≥ 0.5` = 高安（太平）；`0 < sec < 0.5` = 中安（常态）；`security ≤ 0` = **低安**（**含 0**——2026-09-12 船长「0也算低安」）。**各系统用哪条线（已写明，不再是含糊的"低安"）**：**B1 遭遇暴露区** = 只此一档（中安与高安不掷）· **B3 低安残骸箱**（MK2 完好舰体层 + `recycleLoot.mk2` 主题件）同日并入本线 `sec ≤ 0` · **扫描窗口延长是另一条线（`sec < 0.5`，中安也慢）**——船长同日裁定「保留 0.5、只修文案」 | B1/B3 共用；边界出处 = `sideTasks.securityZoneOf` 与 `balance.encounter.lowSecMax` |
| 悬赏（卡/目标） | `AnomalyDef`；远征战斗目标，声望门槛 standingReq 解锁 | |
| 星图显示模式 | **2026-09-11 船长定**：「在星图上添加切换按钮，让玩家可以快速在：显示星系名称、显示安全等级、显示敌对派系 之间切换」——**三态互斥单选**（星图上方一行小按钮，复刻既有工具条同族做法），选择存本地 `whale-idle:starmap-label`、重启保留。①**名称**（默认）＝ 现状：节点下方星系名 + 安全色阶；②**安全等级**＝ 节点下方换成等级数字（`secText`：`+0.6` / `0.0` / `−1.0`），颜色仍走安全色阶；③**敌对派系**＝ 节点下方换成该星系的**敌族标签**（SVG 圆角标签、最多 2 枚、超出写「等 N 族」），**颜色按敌族取 `FOE_ACCENT`**（海盗锈红 / 拾荒警告黄 / 异形磷光绿 / 守墓磷光冰蓝 / 巨构残铁棕 / 巡逻琥珀 / 流亡聚落紫，与战场敌舰同源），并在**星系后方叠一层同族色的静态径向光晕**（"这块是它的势力范围"）。**族名短写**（船长认可）：海盗 / 拾荒 / 异形 / 守墓 / 巨构 / 巡逻 / 流亡（全称为「海盗舰系 / 武装拾荒者 / 异形生物 / 守墓古舰 / 泰坦巨构 / 制式巡逻 / 烬火流亡」）。**边界**：母港恒显「母港」（金色，不参与族色）；未探索剪影显示灰「未知」；已探索但无敌情显示灰「无敌情」；当日「敌对派系活跃」红框 + ✦ 全称**照旧保留**（"今天的目标"与"地盘归谁"两件事）；避让判定 `bandClear` 的标签带半宽随模式取实际宽度（名称 23 / 敌族 **34**，按实测字宽 11px/字）；**族标签文字字号必须与星系名一致（11px）**——2026-09-11 船长实机发现初版漏写字号（SVG 默认 16px）导致"字体不一样 + 文字撑出标签框"，修复见 roadmap 同日「星图族标签字体修复」条 | panels/Expedition.tsx（LabelMode / LABEL_MODES / familyChipsOf / FamChips / app-famglow-*）；ui/shipArt.tsx FOE_ACCENT / FOE_FAMILY / FOE_FAMILY_LABEL；styles.css .app-map-viewbar / .app-map-famchip / .app-map-famchip-text |
| 常驻悬赏 | **2026-09-10 船长定**：原「战斗悬赏」改名——星图页第一个战斗标签（悬赏卡列表，22 张常驻目标）。改名理由：与任务中心新增的「赏金任务」（临时战斗任务）区分——常驻 = 一直在星图上、随时可打；赏金 = 限时派发、过期作废 | 星图 MAP_TABS.bounty / BountyPanel；教程与重要任务文案同步 |
| 首胜 | `completedBounties` 含该卡 id：手动打赢后才能交给 AI 副船自动远征；重复打不再涨声望 | AI 远征已下线（2026-09-05） |
| 远征 | 去程已取消（2026-09-05）→ 即时开战(battle) → 返航(back，去程并入返航) 状态机；胜利与失利/撤退均自动返航（**胜利返航不可召回**、失利可召回，2026-09-06；**手动撤退 2026-09-11 起立刻回港、不走返航段**） | returnReason 兼容字段 |
| 悬赏（完成即返航） | 2026-09-06 起胜利/失利/撤退结算后不再停留——自动返航**最近已建成空间站**（2026-09-08 船长定：无建成副站 = 母港；2×单程按 目标↔落点 计）；**本地悬赏**（落点 = 目标星系）固定返港 2 分钟（120s，2026-09-08 定，防零航程白刷）。**2026-09-11 船长补定：手动撤退 = 立刻回港**（不再付返航航程；到港时刻 = 战斗停表时刻，下一拍即入港卸货）——**自动撤退（结构损失过半）与超时判负仍按原口径返航**（"被迫撤离，正在返航"） | 防"打完即召回家"的 09-06 口径随之更新 |
| 扫描探索 | 就地深空扫描窗口（**基准 10 分钟 × 技能系数 × 危险度系数**）；窗口完成 = 点亮星系 + **自动返航最近已建成站**（2026-09-08；无建成站 = 母港，2×单程不可终止）；手动终止保留进度并即时回港。**危险度系数（越危险扫得越久）**：目标星系 `sec < 0.5` 时 **×[1 + 0.8×(0.5 − sec)]**——sec 0 = ×1.4（14 分钟）、sec −1.0 = **×2.2（22 分钟）**，sec ≥ 0.5 不延长；技能 = 信号分析学 −8%/级 × 信号过滤学 −6%/级 × 星图测绘学 −6%/级（乘算）。窗口合法上限 = `maxScanWindowMs()` = 22 分钟（读档兜底用，见 roadmap 2026-09-11 修复条） | scanning.returning 兼容字段；explore.ts SCAN_WINDOW_MS / SCAN_LOWSEC_PENALTY / SEC_FLOOR / maxScanWindowMs / scanWindowMsFor |
| 巡回讨伐 | 连续出击新语义（2026-09-06）：胜利自动返航到港 → 冷却结束自动再出发，每趟 = 交火 + 2×单程；停环条件不变 | 原"停留站桩连打"作废 |
| 掩护巡逻 | 原"待命"；主控前往已探索星系即时驻留（awayGalaxy）——**唯一**野外停留来源（2026-09-06 起悬赏/扫描不再停留）；低安驻留入遭遇暴露 | location.goStandbyAt |
| 低安暴露 | B1：只属"停留/就地作业"（采掘中/打捞作业中/扫描窗口/驻留）；**移动状态（航行/返航/转场）不暴露**（2026-09-06） | encounters.collectExposures |
| 时效任务板 | 任务中心「资源任务/快递任务」板（v24）：与市场常驻补给同源 20 分钟整板刷新（寿命=整轮）、rng 每轮 2 条；候选池 = common 池商品 + 星图进度门槛（矿石/气/冰=产出带星系已探索，矿物反查精炼源，NPC 直供品与仓库已见恒刷） | state.sideTasks |
| 资源任务 | 时效任务板一类：协会限时收购，仓库足量交付即结（不接受船上货仓）；奖励 = 刷出时收购价锚 ×1.04 整百、强制 < 需量×供应价（市价买入必亏） | sideTasks.TaskKindResource；RESOURCE_TASK_MARGIN |
| 快递任务 | 时效任务板另一类：向**已建成**副站投送（建成任一站解锁）；「出发投送」= 仓库锁货 → 真实航程计时到站自动结算（整板刷新不清在途、在途与作业互斥）；报酬 ×1.30 运费补贴口径（2026-09-06 船长拍板**保持计时机制现状**：免费的是"去程就位"，返程类移动仍计时——快递为唯一跨站直达航程） | sideTasks courier/deliver；COURIER_TASK_MARGIN |
| 赏金任务 | **2026-09-10 船长定**：任务中心第四个子页（重要/资源/快递/赏金）——**临时战斗任务 = 独立日板**：24 小时一轮、**每天本地 0 点整板替换**、过期作废无惩罚（资源/快递仍是 20 分钟板，两者互不影响）；任务 = 指定一处的敌人窝点，必须**亲自出击**（AI 不能代劳）；打赢 = 窝点奖金 + 任务酬金入账 + 该星系留下稀有残骸 ×档位件数，该条下板；打输/撤退任务仍在板上（可再来，不投放）。**只记「主题悬赏 id + 档位」入档**，开战时现算强化卡。**声望门槛 = 接取条件（同日修订）**：不够也能在板上看见（卡面同时展示门槛与当前声望），但「出发」被拒——不再是"声望到了才刷" | state.sideTasks.bounty / bountyWindow；sideTasks.BOUNTY_BOARD_PERIOD_MS |
| 日板席位 | **2026-09-10 船长定**：每天抽 **5 个地点**作为赏金主体——按安全等级分区抽，**高安不派发**（排除后按候选数比例：**中安 2 席 + 低安 3 席**）；各区**独立抽**、抽不满就少发（不跨区补位）；同日不重复星系（同星系多张卡：**代表位随机抽取**——2026-09-12 船长「**派发代表位改为随机抽取**」，旧口径 = 取**地图级别最高**（并列取奖金最高）已作废；赏金日板走**蓄水池抽样**、派系活跃候选池**等概率抽一张**，两处都消费 `state.rng` 故同种子同结果），见「地图级别」）。**档位由该地点的地图级别封顶**：旧规则"随机发放 + 保证每天每个档位至少一张 + 档位与地点解耦"已于同日退役 | sideTasks.BOUNTY_ZONE_PLAN / securityZoneOf / BOUNTY_TASKS_PER_ROUND=5 / spawnBountyTasks |
| 地图级别 | **2026-09-10 船长定**：每张窝点卡标 `AnomalyDef.lairLevel`（1/2/3）= 该地图的**档位上限**——**1 级只出外围、2 级到核心、3 级全档**（硬封顶，不是加权），故**族内越低级的图出现高档位赏金的概率越低**（1 级图永远出不了核心/深层）。发档 = 在该卡 `[1..级别]` 内**均匀随机**、逐卡独立掷、**无三档保底**。级别**只按族内相对强度**（奖金为主、威胁为辅），**不与区划绑定**（A 族 6 张全在中安，若按区划封顶则海盗永远出不了深层）。契约：每张候选卡必须显式标级（缺省 3 只是"漏标不误伤"），且**每个敌族至少一张 3 级**（船长定） | AnomalyDef.lairLevel；lairs.lairLevelOf / lairTiersOf；sideTasks.rollLairTierFor；content-check「窝点级别契约」；tests/bounty-tasks.test.ts |
| 敌人窝点 | **2026-09-10 船长定**：赏金任务的目标——由该星系**主题悬赏按档位派生**的高难加强版（威胁 ×1.3/×1.6/×2.0、加僚机与波次，其余字段全部继承），敌人类型与星系特色一致；**档位由该地点的「地图级别」封顶发放**（旧规则"上限由声望决定"已退役，接取门槛只看卡面声望要求）；三档称呼按**敌族**定制且都是"地点"语义（小头目/藏货据点/隐蔽船坞…），窝点名 = 卡上核心词 + 档位词；**不入星图常驻悬赏列表**。**B 族（武装拾荒者）取消**：不入候选、不出赏金任务与稀有残骸（词表留档） | core/lairs.ts（常量集中可调）；FOE_LAIR_TIERS；isLairCandidate |
| 赏金倍率 | **2026-09-10 船长定（2/4/8）**：三档窝点奖金 = 主题悬赏奖金 ×**2 / 4 / 8**——**只管钱、不管难度**：威胁/波次/僚机仍走档位威胁系数 1.3/1.6/2.0，三档奖金差距拉成 2/4/8 倍（一档也明确高于常驻悬赏原值）。酬金仍 = 窝点奖金 ×档位比例 0.5/0.75/1.0 | LAIR_REWARD_MUL；lairBaseRewardIsk |
| 波次威胁后置 | 窝点波次的强度分配口径（2026-09-10 船长定）：首波只是试探、末波才是硬骨头——二档双波 0.35/0.65、三档三波 0.2/0.3/0.5（一档沿用主题悬赏原波次） | LAIR_WAVES |
| 敌对派系活跃 | **2026-09-10 船长定**：每天选**一个中安/低安星系**（已探索、有正经悬赏卡），在任务中心「赏金任务」页**置顶**一条；**额外一席**（当天 1 条派系 + 5 席常规 = 6 条），该星系从常规席位抽签池里**剔除**（避免同星系两条）。**只作用于该星系的常驻悬赏**（与赏金任务的窝点无关）：当天它的全部悬赏**奖金 +10%、敌人威胁 +10%**，战斗胜利后**按概率掉稀有残骸**（进该星系残骸场、打捞必得），**并有保底**——连刷 20 次未出必掉（见下一行）。**不因打赢而下板** → 当天可反复刷；逐日重选 | state.sideTasks.faction；lairs.FACTION_BOUNTY_REWARD_MUL / FACTION_BOUNTY_THREAT_MUL / FACTION_RARE_DROP_CHANCE / FACTION_RARE_DROP_PITY_ROLLS |
| 敌对派系活跃掉落概率（含保底） | **标称 2026-09-10 船长核定 5%（每次胜利 ×1 件）**——同日先由 10% 下调到 5%（口径：按"每天平均刷取次数"反推）。审数基线（母港出发、含重复冷却 10s）：每趟 = 交火 D(威胁) + 胜利返航 2×单程 + 冷却 → 近处中安卡 ≈5.6 趟/时、最远低安卡 ≈1.7 趟/时、**均值 ≈3.5 趟/时**（4 小时 ≈14 趟、8 小时 ≈28 趟）。**2026-09-11 船长加保底**（玩家反馈「刷了一天没看到稀有残骸」⇒「加一个每 20 次必定掉的保底」，口径裁决「**甲：只保底派系活跃那条掷骰链**」「**19 次未掉 → 第 20 次必掉**」）：`FACTION_RARE_DROP_PITY_ROLLS = 20`，**实际 ≈7.8%/趟**（`factionRareDropEffectiveRate()` 解析值，E[循环] ≈12.83 趟），空手尾巴封在 **19 趟**；8 小时专注刷 ≈2.2 件（原 ≈1.4 件）。**计数口径**：只统计派系活跃掷骰链的空手次数（别的星系不掷骰、不计数——口径甲）；**任何稀有残骸入库都清零**（窝点必掉的 1~3 件也清零，单点 `salvage.injectRareWreck`）；计数器 `GameState.rareWreckDryStreak` 随档保留（零迁移）。实测取证（2026-09-11 探针 300 趟）：加保底前 5.00%/最长空手 **64 趟** → 加保底后 **7.33%/最长空手 19 趟**。复跑 `npm run faction:audit`（打印实际率）与 `npm run bounty:econ`（日供给 9.1 → **9.5 件/天**）可对照 | FACTION_RARE_DROP_CHANCE=0.05（**船长 2026-09-10 核定**）；FACTION_RARE_DROP_PITY_ROLLS=20（**船长 2026-09-11**）；core/lairs.ts + core/expedition.ts + tests/rare-wreck-pity.test.ts |
| AI 远征（下线） | AI 副船打悬赏的任务类型；**已软下线（2026-09-05 船长定，引擎代码保留可恢复）**——指派一律拒绝，历史遗留任务自动善后 | ai.ts 开关 aiExpeditionOffline |
| 悬赏冷却 | 同目标重复出击冷却 `bountyCooldownRemainingMs`（T8；受扫描属性影响） | |
| 连击 / 连续出击 | `autoLoopAnomalyId`：完成→自动返航到港→冷却结束自动再出发（**巡回讨伐**，2026-09-06 起） | |
| 主控 / 驾驶船 | 玩家当前驾驶的船实例（state.shipId） | |
| AI 副船 | `aiAssignments` 指派任务（采矿/打捞/驻留待命；远征已下线）的闲置舰船 | |
| 声望 | standing（协会声望按星系/阵营累积） | |
| 精炼炉 | 母港的循环批处理工位（存档字段 `refineRuns` 工位表，v19 起；每资源一条，固定批量自动续批至料尽）：**主控亲自运转至多 1 台**（占主控工作位），其余工位各由一枚闲置 AI 核心驱动 | 残骸回收炉复用同一实现（含高级箱） |

## 二、战斗 / 敌方数值

| 术语 | 含义 | 备注 |
|---|---|---|
| 威胁等级 | 敌方卡面强度数字 = “对参考火力的击杀秒数刻度”D(T)（5s→90s），**不是单一装配胜率标尺**；驱动敌血/火力/速度/射程成长 | #4 B 方案：标注=叙事，真值=胜率% |
| 虚拟装配 | 敌舰生成口径：射程 = 战术带×成长系数 封顶 15km；速度 = 参考船速段×m_base×tactic 系数 封顶 1.2× | foeSpeedTacticMul brawl 1.28 / orbit 1.0 / kite 0.72 |
| 舰种名 | 敌舰显示名基础（战术 × 血型 9 类：突击/巡逻/狙击护卫舰、攻坚重甲舰、装甲巡逻舰、突击/环绕护航/狙击炮艇等）；满规格主体用本名 | combat FOE_CLASS（界面/引擎同源） |
| 轻装（敌舰规格词） | 敌舰显示名前缀，表示单舰规格 ≤ 本场最强档 ×0.6——覆盖僚机（份额 ×0.6）与明显低血波主舰；例：攻坚重甲舰 → 轻装攻坚重甲舰 | FOE_LIGHT_FRAC 0.6；combat.foeUnitNameOf（2026-09-09 船长拍板） |
| **精锐（敌舰规格词）** | 敌舰显示名前缀，表示该单位是**头目档**（舰级 `elite: true`）；2026-09-11 船长「⑤可以」实装——原为 2026-09-09 预留位 | `FOE_ELITE_WORD`；combat.foeUnitNameOf |
| **敌舰配置表（舰级表）** | 敌人属性改由**独立舰级表**供给：每行 = 一个舰级，血 / 血层分布 / 单发 / 装填 / 命中 / 射程带 / 远端衰减 / 近盲 / 伤害构成 / 战术全部为**绝对值**，不再从"威胁"推导。目的 = **改一艘船的影响面一眼可见、且只限引用它的卡**。**速度是唯一例外**（2026-09-11 追加裁决）：改登记**舰种档 + 倍率**（`hullClassTier` / `speedRatio`），**敌速 = 舰种基准 × 倍率**（见「舰种基准速度」条） | `packages/data/src/foe-ships.ts`；`FoeShipDef`（2026-09-11 船长提议 + 裁决「舰级给绝对值」） |
| **海盗族舰队构成（A 族·不配战列级）** | **2026-09-11 船长定**：「**海盗应该是护卫驱逐巡洋构成，战列级强力+维护成本大，不适合海盗的背景设定**」；同日追加「**劫掠护卫舰和劫掠狙击舰下落一档，只有头目是巡洋舰**」。⇒ A 族舰级**只登记 1 护卫舰 / 2 驱逐舰 / 3 巡洋舰三档**，**不得登记 4 战列舰 / 5 旗舰**（海盗舰队的顶格 = 头目舰登记 3 巡洋舰）。A 族四舰级登记：海盗快艇 **T1**、劫掠护卫舰 **T1**、劫掠狙击舰 **T2**、海盗头目舰 **T3** | `FoeShipDef.hullClassTier`（`packages/core/src/types.ts`）；`packages/data/src/foe-ships.ts`；守卫 = `content:check`「舰级契约」⑤（越界与"海盗配战列级"两条断言）+ `tests/foe-ship-path.test.ts` |
| **舰级路径 / 编成** | 悬赏卡不再逐卡写数值，只写**编成**（引用哪个舰级、几艘、第几波）与**修正**（血/伤害/速度/射程倍率、主系与命中覆写）；`AnomalyDef.ships` 有值即走舰级路径，**未写则走旧的威胁推导路径**（两者可并存） | `FoeShipSlot`；combat.createFoeSpecs 分支 |
| **混编** | 同一张卡引用**多个舰级**（例：海盗头目舰 ×1 + 快艇 ×3），各自按自己的舰级建档——这是"头目强大带一堆杂鱼"的落地手段（2026-09-11 船长「允许混编」） | **A 族 6 张卡自 2026-09-11 数值落地批起全部混编**（头目 ×1 + 同族杂鱼 ×3）；能力本身另有 `tests/foe-ship-path.test.ts` 直接覆盖 |
| **A 族编成口径（头目 ×1 + 杂鱼 ×3）** | **2026-09-11 数值落地批**：六张 A 族卡统一编成 = **海盗头目舰 ×1 + 本卡原有舰级 ×3**（共 **4** 个单位）；**血**守恒总额、头目 **60%**、每杂鱼 **40%÷3**；**名义火力**同样头目 60% / 每杂鱼 40%÷3。依据船长 A 族设定：「鱼龙混杂 / 装备较差数值偏低 / **靠数量弥补** / **一个头目强大带一堆杂鱼** / 速度都快（方便突袭）」 | 落卡 = `packages/data/src/anomalies.ts` 六张卡的 `ships`；守卫 = `content:check`「舰级契约」**⑥a/⑥b**（编成必须是头目 ×1 + 同族杂鱼 ×3、**头目血量占比 60% ±1%**）；数值稿 = `docs/design/foe-faction-a-numbers-20260911.md` |
| **多舰船补偿系数 `2N/(N+1)`** | **2026-09-11 船长确认**（「先按照你的提议实现」）。**动机**：一张卡有 N 个单位时，它们是**逐个被击毁**的——单位 1 打满全场、单位 2 只打 (N−1)/N 场……单位 N 只打 1/N 场，敌人整场累计输出 = 单舰基准 × **`(N+1)/(2N)`**（**N=4 时只剩 62.5%**），即"看得见的 N 倍火力，实际只打出不到 N 倍"。本系数**正好抵消这层阶梯衰减**（N=4 → **×1.6**、N=1 → ×1、N 越大越趋近 ×2）。**适用范围 = 舰级路径**（写了 `AnomalyDef.ships` 的卡）：引擎在建档时按"**本卡编成单位总数 N**"缩放 `shotDmg`（`core/combat.ts` `foeMultiShipCompMul`），故卡上 `dmgMul` 写的是**设计单发 ÷（舰级单发 × 补偿）**。⚠ **旧威胁推导路径暂未启用**（那里 N=1、系数天然为 1）——**已知口径不一致**，待旧卡迁入舰级路径时统一。**名义总 DPS = 威胁 × `foeDpsPerThreat` × 本系数** | `foeMultiShipCompMul`（`packages/core/src/combat.ts`）；`A_MULTI_SHIP_COMP = 1.6`（`packages/data/src/anomalies.ts`）；守卫 = `tests/foe-ship-path.test.ts`「多舰船补偿」3 条用例 |
| **编成条目** | 悬赏卡上一个 `FoeShipSlot`：`ship`（舰级）+ `count`（数量）+ `wave`（第几波）+ `escort`（是否僚机，**当前无卡使用**——船长 2026-09-11「**不保留僚机**」）+ 倍率（`hpMul`/`dmgMul`/`speedMul`/`rangeMul`）+ 覆写（`split`/`dmgMix`/`hitRate`/`tactic`/`rangeMinM`/`rangeMaxM`） | `packages/core/src/types.ts` |
| **标准复核配置集（A0~A3）** | **2026-09-11 船长采纳 = 正式口径**。四行固定装配 + 强制开火列，用于**判定敌人强弱与数值批验收**：**A0 标准中距**（4×动能MK2 `700~5740` ＋MK2推进器＋支援，全技能 L3 = 原参考行）· **A1 贴脸近战**（4×轻型炮MK1 `250~3220`，**无推进器**，全技能 L3）· **A2 远程风筝**（4×重型导弹架MK2 `900~11760` ＋推进器，全技能 L3）· **A3 弱技能行**（**A0 装配 + 6 项战斗技能 L3**：`gunnery`/`kinetic-gunnery`/`reload-drills`/`fire-control`/`targeting-integration`/`shield-operation`）。**纪律：判定敌人强弱必须至少跑满四行**——单跑 A0 得出的"残血/胜率"会被距离口径污染（A0 的 mid=3220m 恰在低威胁敌人射程外，会系统性偏袒玩家），本次"残血 100% 被误读成敌人软"即此根因 | `tools/battle-calibrate.ts` `STANDARD_ROWS` + **`--std`**；依据 = `docs/design/foe-baseline-audit-20260911.md` §四（已采纳） |
| **纸面单发 vs 实收输出（登记口径，2026-09-11 船长选「只登记口径」）** | **问题**：卡面/舰级上的"单发"是**纸面值**，与实际输出不同源——实战输出 = 单发 × **命中** × (装填节奏) × **远端衰减** × 多舰补偿后的分摊；于是"纸面看起来差很多、实收却相近"（例：赤潮头目 **beam 必中单发 21** ↔ 灰霾头目 **fixed 命中 0.9 单发 59**）。**已确立的判定口径（2026-09-11 A 族批）**：**难度守恒一律按「实收」判定，不按「纸面」**——比较两张卡/两族强弱时用 A0~A3 实测（胜率/时长/残血/开火），**不得拿单发数字直接对比**。**换算关系（登记备查）**：实收 ≈ 单发 × 命中 × (战斗时长 ÷ 装填) × 远端衰减系数（`beamPowerFactor`/`distFactor`）× 存活时长占比；**多舰编成另有阶梯衰减**：N 个单位逐个被击毁时整场累计输出 = 单舰基准 × `(N+1)/(2N)`（N=4 → 62.5%），故舰级路径按 **`2N/(N+1)`** 预补偿（见「多舰船补偿系数」）。**本口径只登记、不重标数值**（船长 2026-09-11 裁定） | `docs/design/foe-faction-a-numbers-20260911.md` §3.3 与 §六-6；`docs/glossary.md`「多舰船补偿系数」「开火列口径」 |
| **舰级按档重排（档基线 × 角色修正）** | **2026-09-11 船长裁定「甲案」**。**动机**：船长「为啥**舰种档有差距，基础属性却差不多**，虽然会有修正，但是**基础属性要符合舰种定位**。**巡洋舰和护卫舰一样强不对吧**」——原舰级基准取自"各卡迁移前的现状值"（**卡锚**），导致头目舰 T3 360/56 与护卫舰 T1 365/51 几乎同强。**口径**：**舰级基础属性 = 档基线 × 角色修正**；**档基线**（血 / 单发）= `T1 260/39.5 · T2 480/68 · T3 900/124`（跨档 ≈1.85×/1.88×；T4/T5 预留）；**角色修正**（同档内区分）= 数量型小艇 `0.60/0.71`（海盗快艇、拾荒武装艇 → **156/28**）· 标准护卫 `1.40/1.29`（劫掠护卫舰 → **364/51**）· 远程狙击 `1.00/0.88`（劫掠狙击舰 → **480/60**）· 中距火力舰 `1.00/0.95`（拾荒火力舰 → **480/65**）· 精锐主力 `1.00/1.09`（海盗头目舰 → **900/135**）。**卡片侧一律写成 `设计值 ÷（舰级值 × 多舰补偿）`** ⇒ 换舰级值自动反算、**卡片读数不变**（落码时对 9 张舰级路径卡逐字段验证：除三张重编成卡外**零差异**） | `packages/data/src/foe-ships.ts`（顶部口径表 + 6 个舰级）；`docs/design/foe-faction-a-numbers-20260911.md` 附二；守卫 = `content:check`「舰级契约」+ `tests/foe-ship-path.test.ts` |
| **无首领编成（A 族三张）** | **2026-09-11 船长裁定**：「边境海盗前哨 / 碎晶带劫匪通缉 / 信标猎手悬赏 **都设定无首领**」，并给定波次结构：**边境「1+2」**（首波 1 艘 + 次波 2 艘 = 3 单位）、**碎晶与信标「2+2」**（每波 2 艘 = 4 单位）。⇒ 这三张卡**全队同族同型、无 elite 头目**；总血/总火力**守恒**（150/43 · 285/72 · 365/80），但失去"一个单位扛 60% 血与火力"的集中结构、命中由 0.9 回到 0.85 ⇒ **略变轻**（A0 快 2~5 秒）。**波次靠 `slot.wave`（0/1）+ `anomaly.waves` 两波**驱动（舰级路径的波次来源；旧 `LAIR_WAVES` 波表对它不适用）。守卫 = `content:check`「A 族编成契约」的**无首领分支**（无 elite 条目 + 同型 + 单位数 + 波次结构 + 两波登记）。**另三张（灰霾/赤潮/蜃影）维持「头目 ×1 + 杂鱼 ×3、头目血量 60% ±1%」** | `packages/data/src/anomalies.ts`（三卡 `ships` + `waves`）；`tools/content-check.ts` |
| **无法交战（提前脱战）** | **2026-09-11 船长裁定「乙2，事件为 120 秒」**。**开战满 120 秒**仍满足三件 ⇒ 判负并按**撤退**结算（`autoEscaped = true`、`escapeReason = 'cannot-engage'`）：①我方**全程一炮未发**（`stats.meShots === 0`，开过一炮就永不触发）②当前距离仍**在我方最远射程之外**③**敌人已经开火**（`stats.foeShots > 0`）。**动机**：远程型（kite）敌把交战距离拉在自己带内时，短射程/无推进器装配会全程 0 开火并空转——灰霾 ②/⑤/③ 原为打满 600 秒触顶判负、蜃影/赤潮三行原在 140/373 秒**被击毁**。**数值一个字不动**：本机制只决定"什么时候收场 + 战报怎么写"，结算走与「超时判负」同源的**轻损撤退**（扣损 ×0.5、**不掷弃船骰**、下限 5% 保护）。**阈值** = `balance.battle.cannotEngageMs`（默认 120 000；设 `0` = 关闭判定）；⚠ 判定在**满 T 后的下一拍（+100ms）**生效（`advanceBattleFor` 在 `stepBattle` 之后才推进 `lastTickGameMs`，与超时判负同源）。**落码后实测（5 播种）**：蜃影 ②⑤③ 击毁 5/5 → **撤退 5/5/120s/残 14%**；赤潮 373s 击毁 → **120s 撤退/残 69%**；灰霾 600s 触顶 → **120s 撤退/残 96%**；胜率仍 0%。**边界**：只治"我够不着"（双方都没开火的接近段、我方开过火都不触发）；AI 副船与低安遭遇的结算路径本就不看 `autoEscaped`（与超时判负同待遇） | `combat.ts` `stepBattle` 结束判定段；`balance.ts` `cannotEngageMs`；`state.ts` `escapeReason`；`expedition.ts` `settleBattleRetreat('cannot-engage')`；用例 `tests/cannot-engage.test.ts`（5 例）；设计稿 `docs/design/p0-2-engagement-20260911.md` §八 |
| **开火列口径（强制列）** | **2026-09-11 船长采纳**：任何战斗复核表都必须报 **`我开火` / `敌开火(头目)` / `敌开火(杂鱼合计)`**（**分列**），并附 **`终局交距` / `最近交距`**。**头目 = 该波首个单位**（tag `foe-0` / `w1-foe-0`…），其余计入杂鱼合计。**动机**：区分"打出来的损失"与"敌人根本没开火"——残血 100% 可能是敌人全程打不到人；分列才能看出是头目没到位还是杂鱼没到位。**计数口径（2026-09-11 修正，务必照此）**：①**我方开火取 `BattleState.stats.meShots`**（引擎累计，权威）；②**敌方按 `BattleFx.seq` 续读**——`fx` 是 **48 条环**（`pushBattleFx` 超长丢最旧），**旧写法 `b.fx.slice(seen)` + `seen = b.fx.length` 以"环长度"作游标，环满后恒为空 ⇒ 长战全部漏计**（实测：灰霾 ⑤ 600s 真值 **584** 被读成 **48**、漏 92%；蜃影 A0 **54.0 → 43.2**、A1 **136 → 48**）；被裁掉的永远是旧事件，故"`seq` > 上次最大值"的事件一定还在环里 ⇒ 无损。**已修**：`tools/battle-calibrate.ts` 于 2026-09-11 改用 seq 游标，并**重测**受影响的敌开火读数 | `tools/battle-calibrate.ts`（`--std` / `--csv` 六列）；`BattleState.fx`/`fxSeq`/`stats`（`packages/core/src/state.ts`） |

| **开火列口径（强制列）** | **2026-09-11 船长采纳**：任何战斗复核表都必须报 **`我开火` / `敌开火(头目)` / `敌开火(杂鱼合计)`**（**分列**），并附 **`终局交距` / `最近交距`**。**头目 = 该波首个单位**（tag `foe-0` / `w1-foe-0`…），其余计入杂鱼合计。**动机**：区分"打出来的损失"与"敌人根本没开火"——残血 100% 可能是敌人全程打不到人；分列才能看出是头目没到位还是杂鱼没到位。计数必须**逐单位累计**（`BattleState.fx` 开火事件是 **48 条环形缓冲**，一次性大步长推进会把长战的早期事件裁掉 ⇒ 工具改为**分段推进**采样；引擎内部固定 100ms 步长，分段不改变结果） | `tools/battle-calibrate.ts`（`--std` / `--csv` 六列）；`BattleState.fx`/`stats`（`packages/core/src/state.ts`） |
| **能量·掷命中（`energyForm: 'spit'`）** | **2026-09-11 船长裁定⑤「立「能量·掷命中」档」**：给**能量主系**（`dmgMix` 主系 = `plasma`）两种**形态**——①缺省 / **`'beam'`** = **激光式光束必中**（`kind: 'beam'`：**开火必中、不掷命中、不消费 `hitRate`、守方回避也不生效**，远端用 `beamPowerFactor` 做**威力**衰减）；②**`'spit'`** = **能量掷命中（喷吐 / 投射）**（`kind: 'fixed'`：**掷命中 + 命中随距离衰减**（`distFactor`：近端 1 → 最远端 = `falloff`）、**消费 `hitRate` 且吃守方回避**）。**层位克制两者都按等离子行**（对盾 ×1.25 / 对甲 ×1 / 结构 ×1）——与光束"同系不同形态"，不是第二套伤害系。**只对能量主系生效**：动能/爆炸主系本来就是掷命中（`fixed`），本字段对它们无影响。**默认不写 = 现状（零行为变化）**。落码 = `FoeShipDef.energyForm` / `FoeShipSlot.energyForm`（条目 > 舰级 > 缺省）；`createFoeSpecsFromShips` 一处决定 `kind`/`hitRate`，胜率预估与实时结算读 `w.kind` 自动跟随 | 首用 = **C 族四舰级全 `'spit'`**（酸液喷吐：裂谷/星髓/噬口/深渊之门，2026-09-11 C 族落码批）；守卫 = `content:check`「敌方混伤契约」纯能量卡分支（须显式声明形态）+ `tests/foe-ship-path.test.ts`「形态覆写」2 条 |
| **C 族（异形生物）族格** | **2026-09-11 船长五条裁定**：①**深渊之门卫队改 brawl + 整套重标**（原 C 族唯一 13.3 km 光束点名，与"贴脸撕咬/酸液喷吐"设定硬冲突）②**C 族速度比 A 海盗还快**（同档实速须高于 A 族；倍率带 1.30~2.10）③**允许 T4 战列舰**（"生物巨兽"；**T4 例外允许慢**）④**分两步**（第一步 = 舰级路径迁移 + 族格口径 + 契约；第二批 = 虫群编成 + 稀有头目）⑤**立「能量·掷命中」档 + 主系统一为等离子**（酸液签名）。**第二批族级结构修正**（船长：「配了 2 个巡洋级…不符合我对 C 族的设计。**护卫舰级应该有 2 种，星髓幼虫和畸变幼虫。驱逐级一种，星髓成虫**」）⇒ **退掉两条 T3 巡洋**，现四舰级 = **星髓幼虫 T1·544** / **畸变幼虫 T1·544** / **星髓成虫 T2·398** / **噬口巨兽 T4·277**（巨兽白名单 + `elite` + 冲锋）；**四张卡全在舰级路径、编成一律主体（不写 `escort`）**；窝点级别 L1/L2/L3/L3 **保持不动**（四张仍是窝点候选） | 舰级 = `packages/data/src/foe-ships.ts`；白名单 = `ALIEN_BEAST_SHIP_IDS`；设定与落码记录 = `docs/design/foe-faction-c-alien-20260911.md`（§九 第一步基线 + §10.3~10.6 第二批落码/实测/连带项/挂账） |
| **虫群编成（C 族）** | **2026-09-11 船长第二批裁定**：「**虫群规模比海盗还要大，按 6~12 只算**」「**分波因此也更多**」「**每个悬赏单独设定**」+ 族级结构修正。**四张卡**：深渊之门卫队 = 星髓幼虫 ×6/2 波（3+3）；裂谷畸变体猎杀令 = 畸变幼虫 ×8/2 波（4+4）；星髓虫群 = 星髓幼虫 ×7 + **星髓成虫 ×3（末波）**/3 波（4+3+3）；噬口猎杀令 = 畸变幼虫 ×10 + **噬口巨兽 ×1（第 3 波）**/3 波（4→3→4）。**总血一律精确守恒**（1,000 / 1,815 / 1,530 / **2,766**），单发按 `2N/(N+1)` 反算（取整后偏差 ≤ ±1.8%）。⚠ **实测教训**：摊薄到 N 个单位后**实收输出下降**（多舰补偿没补回这一层）⇒ 三张卡 A1 贴脸行**普遍变简单**（深渊之门残血 35%→82%、裂谷 40%→100%、星髓 0%→100%），与「**纸面单发 vs 实收输出**」同一条教训 | `FoeShipSlot.count/wave` + `AnomalyDef.waves`；`packages/data/src/anomalies.ts` 四张卡；`ALIEN_COMP(n)`；用例 = `tests/foe-ship-path.test.ts` C 族组；读数 = 设计稿 §10.4 |
| **稀有头目（C 族 · 全族唯一）** | **2026-09-11 船长第二批裁定**：「**只有噬口有头目出现**」「在设定上，**出现稀少**，和海盗头目那种到处都是的不一样，**因此也更加强大**」「**首领血量占比提高到 80%**」。**落法**：头目 = **噬口巨兽**（T4 巨兽档，`elite: true` ⇒ 显示名「精锐噬口巨兽」）**×1 只、只在噬口猎杀令第 3 波**；「稀少」由**全族只此一条舰级 + 只此一张卡的一个单位**表达（**不采用** A 族"每张卡都有头目"的做法）；「更强」由**卡上 80% 血量 + 80% 火力**表达（噬口：首领血 2,212.8 / 单发 401，小虫每只 55.32 / 10）。⚠ **同批实测与三次当日修正**：巨兽一度是"射程 2,713→**4,000** + 冲锋（倍率外溢成全队飙车）+ 80% 占比"三件套叠加 ⇒ 噬口 A0/A2/A3 由 100% 翻成 **0%**；船长当日**三次回收**——①冲锋**倍率不外溢**（见「敌突进 / 冲锋」条）②「**回收射程加成**」（4,000 → 原值 **2,713**，不再覆盖玩家标准站位 3,220m）③「**首领速度单独上调 20 点**」（277 → **297**，冲锋期 594 已超过小虫 544 ⇒ 由巨兽领跑）⇒ **A0 = 80%｜69s｜残血 80%**（②之后曾回到 100%｜残血 100%，③把它压回 80%）；仍待裁 = A2（100%→20%）/ A3（100%→0%），剩余主因 = **首领独占 80% 火力 ⇒ 单发 401**（见设计稿 §10.4 读法 2 / §10.6） | `FOE_ALIEN_MAW`（`elite` + `foeCanCharge`）；`ALIEN_BEAST_SHIP_IDS`；`packages/data/src/anomalies.ts` 噬口卡（`MAW_BOSS_SHARE = 0.8`）；用例 = `tests/foe-ship-path.test.ts`「稀有头目唯一性」 |
| **守墓古舰（D 族）** | **2026-09-11 船长九条裁定（设定完善批 · 纯设定文档、零数据调整）**：基调 = **远古机械化的陵墓文明**（静滞陵寝 · 守卫体系 · **誓约驱动的意志** · 能量武器 · 沉睡与重启；**内部参照 = 《战锤》太空死灵基调**，船长原话「**按照战锤世界的太空排骨设定**」——**内部参照不进玩家可见文案**，产物一律用我们自己的词、不出现现实作品专名）。**族格**：**有意志的守誓者**（非无脑机械；**守而不猎**、越界即清除 = 「劝离 / 清除」两种响应）· **战术族规 = 全远程**（只允许 `kite` / `orbit`，**不许 `brawl`**）· **种族以能量武器为主**（能量炮／长炮为主武，古典实弹是舰体残留的"旧炮"——⚠ **与现状数据有差**，记为待落码差异）· 四张卡 **平级、只按地点分**（不设职能分层）。**命名口径**：族内统一「**守墓者**」（卡名「坟场守墓**人**」→「坟场守墓**者**」为**待审稿**，属数据未改）；地名词分层 = **坟场 / 墓园**（陵区地表统称）· **穹顶**（封存结构本体）· **守卫舰长 → 残舰泊地 → 隐秘陵寝**（纵深三层：外围指挥单位 / 中层停泊整备 / 最内层核心墓室）。**留白**：谁造的、守谁的墓、与人类有无关系**一概不写**（不接「人类早已离开」那条线）。**保留不动**：族色 `#9fd0f2` · 外形资产 · 三件专属件及其说明 · 据点三档词 · 副系签名 `FOE_SUB_DMG.D` · 四张卡全部数据 · 四段星系 lore 与卡面描述 | 设定卡 = `docs/design/foe-faction-d-gravekeeper-20260911.md`（标"已确认"）；族谱稿 = `docs/design/foe-faction-identity-20260910.md` §D（已定案）；落点 = `anomalies.ts`（四卡）/ `lairs.ts`（词表与专属件）/ `universe.ts`（四段星系 lore）/ `modules.ts`（三件专属件） |
| **泰坦巨构（E 族）** | **2026-09-11 船长十条裁定（设定完善批 · 纯设定文档、零数据调整）**：本质 = **无人的巨构残骸 + 残存自动程序**——**不是生命、没有意志**（「巨构不是生命，是还在跑的机器」；不追你、不恨你，只把靠近的东西当靶子）。**族格**：**战术 = 中距为主**（主体一律 `orbit`；`kite` 可作单卡变体；**`brawl` 不再是族特征**）· **火力 = 动能 + 爆炸为主**（主系动能、副系**爆炸**＝专属件巨构残骸炮的系）· **节奏 = 长战厚血**（1585/1740，慢、撑到最后）· **立「核心舱段级」代表位**（一具接近完好的巨构为目标）——✅ **2026-09-12 已落码**：代表卡 = 「**巨构核心勘探令**」`ano-core-section`（**威胁 84 · 奥罗荒环 · L3**（2026-09-12 由深渊之门迁入：E 族三卡集中奥罗荒环） · 精锐核心舱段 ×1 · 血 3,400／单发 560＝炮台 224＋机群 3 架 ×112 · 交距 5,545m · 奖金 100 万）· **警戒机群 = 巨构自带**（第二套火力，保留承诺、记为待落码）。⚠ **两处与现状数据有差**（现状两张卡均 `brawl`、卡面副系现取等离子）⇒ **✅ 2026-09-12 已落码**（E 族数据批 · 二号 · d2）：全族构成改 **50% 动能 + 50% 爆炸**（含赏金任务卡与窝点派生卡）· 全族**速度倍率 0**（静物残骸，靠机群打炮台射程外的敌人）· 主体战术改 `orbit` 且**作战距离经 `desireRangeM` 钉住不变** · 三条舰级齐备（巨构残段 T4 / 奥罗残骸段 T3 / 核心舱段 **T5 旗舰**，见第三修订）· **警戒机群已实装**（敌方机群系统）。**2026-09-12 第二修订**（船长「E 族射程按照最低 100，最高根据 7000~10000 设定，无人机射程设为 5000，添加新机制，受到攻击后大幅提高无人机射程（提高 400%）」）：**射程带 100~7,000/8,500/10,000**（三条舰级）· **警戒机射程 5,000m** · **受击增程 ×4**（见「受击增程（敌机）」）· **解除期望距离钉住** ⇒ 交距 = 带内插值 0.55 = **3,895 / 4,720 / 5,545m**。**2026-09-12 第三修订**（船长「**核心舱段，调为旗舰级**。①**泰坦残骸勘探，敌人数量缩减为 1**。然后将**三个悬赏整体火力上调 20%**，**衰减降低为 0.3**」）：**核心舱段 = T5 旗舰**（档基线 2,800/420 × 1.40/1.30 = **3,920/546**，卡侧反算 ⇒ 实收 3,400/672；旧「T4 上限、T5 不开」作废，E 族为敌舰唯一 T5）· **三卡整体火力 +20%**（泰坦 180→**216** / 奥罗 128→**154** / 核心 560→**672**）· **三条舰级远端衰减 0.5 → 0.3**（⚠ 方向 = 射程上限处威力系数 ⇒ **远端收得更紧**；警戒机机型仍 `falloff: 1`）· **泰坦残骸勘探缩为单舰**（血 **1,585 守恒**，机群 4 → **2 架**、每架 65）。**三个词的定位**：**巨构**＝类别统称 · **泰坦**＝其中**最大的一级**（"泰坦级巨构"）· **奥罗**＝**地名**（奥罗荒环，不是型号名）。**装备哲学（本节立在设定卡里）**：①带机库的移动基地（深层机库 +95 m³，无人机＝第二套火力）②拆解工具而非精度武器（巨构残骸炮：10 秒一发、远端 0.1，用法是"**贴到中距离换血**"）③撑到最后（巨构骨架：**结构 +60%**，全游戏唯一）；代价一律是 **CPU・机动・射速**。**留白**：谁造的、为何在此、与人类有无关系、**「旧战争」的双方**一概不写；残骸**散落在别族地盘不额外解释**；**与 D 族的"远古"各自留白、不勾连**。**保留不动**：族色 `#d9b98c` · 外形资产 · 三件专属件及说明文 · 据点三档词 · 副系签名 `FOE_SUB_DMG.E` · 两张卡全部数据（均 L3 窝点候选；**2026-09-12 数据批已按族规对齐构成与战术**：`dmgMix` 5:5 · 战术 `orbit` · 奥罗卡迁入舰级路径）· 两段星系 lore 与卡面描述 · 泰坦残骸勘探**命中 0.65**（"老化自动炮台"＝"无意志"设定的落点） | 设定卡 = `docs/design/foe-faction-e-titan-20260911.md`（标"已确认"）；族谱稿 = `docs/design/foe-faction-identity-20260910.md` §E（已定案）；落点 = `anomalies.ts`（两卡）/ `lairs.ts`（词表与专属件）/ `universe.ts`（深渊之门・奥罗荒环）/ `modules.ts`（三件专属件） |
| **制式巡逻（F 族 · 已废弃）** | **2026-09-11 船长裁定：「废弃F族，将F族融合进A族」**——⇒ **F 族不再作为一族存在**。**融合**：四张已退役的 hidden 遭遇模板（流窜海盗快艇 10 / 伏击劫掠队 22 / 狂徒巡逻编队 40 / 深空屠夫舰队 70）**归入 A 族**（补 `foeFamily: 'A'`，属数据批）——依据 = 它们的**名字本就是海盗语汇**、且缺族时签名表本就**回落到 `FOE_SUB_DMG.A`**（与 `FOE_SUB_DMG.F` 同序），**数据层早已按 A 族口径在跑**；"制式"只是它们的**外观来源、不是身份**；旧档兜底用途**照旧**。**退役留档（不删）**：族短名「巡逻」· 族色琥珀 `#ffab5e` · 制式巡逻形 · 据点三档词（巡逻队长/据点/隐蔽据点）· 签名表条目——全部标「已废弃·留档」（照 B 族先例）。**`'F'` 字母位永久保留为空位**（不再分配给新族；存档与导出都不落族字段，已实测）。**兜底政策**：改**强制显式登记**（新增契约：**每张敌军卡必须有 `foeFamily`**，未登记即体检报错）＋ `shipArt` 的**全仓唯一缺省点** `foeFamily ?? 'F'` **改指 A 形**（仅防崩保护）。**A 族一切不动**（族名/色/形/专属件/据点词/签名表/六张卡全不变）。⚠ **待落码 8 项**（数据 1 · 类型 1 · 数据表 1 · 美术 2 · 契约 1 · 工具 2）见 `foe-pending-index-20260911.md` **P-15** —— ✅ **8 项已全部落码（2026-09-12 二号 · d2）**：四张模板显式登记 A 族 · `'F'` 空位保留 · 三表与美术（色/形/短名/挂点）退役留档 · 兜底改指 A 形 · **新契约「敌族显式登记」**（实测 27 张全登记、`'F'` 无卡占用）· schema 枚举注记 · 工具 F 段收口 | 记录件 = `docs/design/foe-faction-f-retired-20260911.md`（标"已确认"）；族谱稿 §F = 已废弃；落点 = `shipArt.tsx`（族短名/色/形/兜底点）/ `lairs.ts`（三表）/ `types.ts`（`FoeFamily`）/ `anomalies.ts`（四张模板）/ `content-check.ts`（`!== 'F'` 特例）/ `content-schema.ts`（族枚举）/ `mixed-damage-review.ts`（F 段） |
| **武装拾荒者（B 族）** | **2026-09-11 船长七裁决**：「关于B组，**统一为武装拾荒者，移除靶机**。**确认为新手过渡种族**。**演习场讨伐令改为演习场驱逐令**，驱逐到演习场拾荒的拾荒者。**战术性格统一为 orbit**。**乱射不动**。**速度偏慢**。**舰种按照你推荐的来**」+ 同日追加「**B 族速度按 0.8 走**。**没有窝点，排除出赏金范围**」。⇒ 族全名 = **武装拾荒者**（族短名「**拾荒**」不变，`FOE_FAMILY_LABEL.B`；**全仓不再叫"靶机"**）；定位 = **新手过渡族**（教学 → 低威胁赏金）；**全族 orbit**（契约「族→战术契约」B = `['orbit']`）；**速度偏慢 = `speedRatio` `0.80` / `0.80`**（拾荒武装艇 **272** / 拾荒火力舰 **236**，均低于本档基准 340/295；**2026-09-11 船长裁决「B 族速落实 0.8」= 终值**；历史留档：本批曾一度取 `0.90`/`0.92` = 306/271，已作废）；**舰种只登记 T1 护卫舰 / T2 驱逐舰**（不配 T3+，契约「舰级契约」守卫）；**乱射（命中 0.55）是单卡特征、不是族级签名**（写在占港那张卡的编成条目上，舰级一律 0.85）；**无窝点、不参与赏金任务派发**（`isLairCandidate` 族规则排除 + 「派系活跃」候选池改按 `isLairCandidate` 判；B 族专属件 `FOE_LAIR_GEAR.B` 本就为空） | 舰级 = `foe-scav-skiff` / `foe-scav-armed`（`packages/data/src/foe-ships.ts`）；设定卡 = `docs/design/foe-faction-b-scavenger-20260911.md`（§六 窝点排除核查）；族档 = `docs/design/foe-faction-identity-20260910.md` §B（已定案） |
| **乱射（单卡特征）** | **打得重、准头差**：命中 0.55（敌缺省 0.85）。**只是「占港武装通缉」这一张卡的特征，不是 B 族族级签名**——船长 2026-09-11 七裁决明确「**乱射不动**」，即保持原样、**也不升格**为族级/舰级口径（该舰级 `hitRate` 仍是 0.85，0.55 写在卡的编成条目 `hitRate` 上）。全游戏另一处低命中特例是「远古残骸老化自动炮台」0.65（**E 族卡**：泰坦残骸勘探 `ano-titan-wreck`——按 E 族 2026-09-11 设定，它正是"**无人的巨构残骸 + 残存自动程序**"的落点；2026-09-11 更正：本条原写作"C 族卡"，经全仓核对为误记） | `FoeShipSlot.hitRate`；`packages/data/src/anomalies.ts`（占港卡）；`docs/design/foe-faction-b-scavenger-20260911.md` §一 裁决 5 |
| **血型随卡走（`split` 覆写）** | **2026-09-11 船长裁决**：「**头目血型随卡片走**，其实我们刚刚忘记讨论种族血型了，不过问题不大，**海盗设定鱼龙混杂，那么就什么血型都有**」。⇒ 条目可写 `split` 覆写三层血量比例，**有效 split = 条目覆写 ?? 舰级 split**；卡面 `defProfile` 是**卡**的口径，必须对**每一条主体编成**都成立（头目舰本体是装甲型，故卡面非装甲的卡要在头目条目上覆写）。**A 族不做族级血型约束**（鱼龙混杂 ⇒ 什么血型都有），血型**逐卡自定** | `FoeShipSlot.split`；`createFoeSpecsFromShips`；守卫 = `content:check`「舰级契约」（逐条主体按**有效值**判定血型与构成）+ core 用例 |
| **头目射程多重方案** | **2026-09-11 船长裁决**：「我记得之前说过，**头目可以多种战术选择，因此射程方案也是多重**」——同一头目舰按卡配射程：**近战卡不写覆写**；**头目打不到的卡**在其条目写 `rangeMinM`/`rangeMaxM` = **本卡杂鱼（主体）的有效射程带**（"同卡同带"，让那 60% 的火力真正落地）。动机实测：头目舰本体是近战带 1~2210m，而 A 族 kite 卡实战交战距离 3.2~3.8km ⇒ 覆写前**头目一次都没开火**（探针 0 次），覆写后 4~13 次 | `FoeShipSlot.rangeMinM`/`rangeMaxM`；`docs/design/foe-faction-a-numbers-20260911.md` §七 7.3 |
| **窝点派生按档缩放（舰级路径）** | **2026-09-11 船长裁决④「按甲处理」**：旧路径靠"威胁 → 血/火力曲线"自动随档变强，而**舰级路径是绝对值、不会自己涨** ⇒ 派生窝点卡时按「**原威胁 → 派生威胁**」的比例（= `派生威胁 ÷ 原威胁`）**同乘每个条目的 `hpMul` 与 `dmgMul`**（保住「威胁 = 战力标尺」的语义）。**波次**：舰级路径编队由 `slot.wave` 决定，**不套旧路径的 `LAIR_WAVES`**（套了会刷出 0 个单位）⇒ 舰级路径派生卡维持**单波** | `lairAnomalyOf`（`packages/core/src/lairs.ts`）；守卫 = `content:check`「**窝点派生契约**」（每波 ≥1 单位 + 总血/火力随 tier **严格递增**；负向验证：摘掉缩放 ⇒ 36 处错误）+ core `tests/foe-lair-scale.test.ts` |
| **头目多战术** | **同一条头目舰可配多种打法**（贴脸头目 / 狙击头目是同一条船）："强"由**档位**给（血厚、单发高），"怎么打"由**卡上**定（编成条目的 `tactic` 覆写）；**射程随之多重**（见「头目射程多重方案」条）——船长 2026-09-11：「头目可以多种战术选择，因此**射程方案也是多重**」 | 2026-09-11 船长「头目建议允许多个战术」；契约相应放宽为"卡面战术须落在**主体有效战术集合**内"（混编允许各主体不同战术） |
| **退役窝点卡白名单（`RETIRED_LAIR_CARD_IDS`）** | **2026-09-11 船长裁决「按方案 2 执行」**。**用途**：把"曾为窝点候选、后按裁决退役"的卡**显式保留在稀有残骸的注册集合里**（`data/context.ts` 的注册条件 = `hasLairCore(a) \|\| RETIRED_LAIR_CARD_IDS.has(a.id)`）。**为什么不能直接删字段就算完**：稀有残骸物品**只在注册后才存在**（`wreck-rare-<卡 id>`）；B 族两卡（新港商路护航令 / 占港武装通缉）的 `lairCore` 退役删除后，若不补白名单，**旧档里已获得的这两件会解析不到定义 ⇒ 玩家看到「未知物品」**（回收炉首行守卫就会报 `未知物品：wreck-rare-…。`）。**语义分层（务必分清）**：①**窝点候选**由 `isLairCandidate()` 判（含"B 族一律排除"族规则）⇒ 退役卡**不参与**派发；②**稀有残骸注册**由本白名单 + `hasLairCore` 判 ⇒ 退役卡**仍注册**（只为旧档兼容，不再新增产出）。**值 = 卡 id（`AnomalyDef.id`），不是物品 id**（物品 id = `wreck-rare-<卡 id>`，由 `rareWreckItemIdOf()` 推导）。**守卫** = `content-check`「**退役窝点卡契约**」4 条：白名单 id 必须真实存在于 `ANOMALIES` / 这些卡 `lairCore` 必须为空（防加回来）/ 必须仍被 `isLairCandidate()` 排除（防复活）/ 对应稀有残骸必须仍可注册（防白名单被摘掉） | `packages/data/src/retiredLairCards.ts`（常量 + 退役说明）；`packages/data/src/context.ts`（注册条件）；`packages/data/src/anomalies.ts`（两卡字段退役注释）；`tools/content-check.ts`（契约）；设定记录 = `docs/design/foe-faction-b-scavenger-20260911.md` §六（"B 族无窝点 = 三层结构"） |
| **退役窝点卡契约** | 见上条「守卫」——`content:check` 里防止白名单腐烂的三向护栏（**不是**编号契约，单独登记以便检索）：它同时防"白名单写错 id"（= 旧档照样认不出）、"字段被加回来"（= 退役没落地）、"白名单被从注册条件里摘掉"（= 旧档出现未知物品）。三种情形**均已实测会拦**（负向验证记录见 roadmap 同日条目） | `tools/content-check.ts`；守卫实现位置在「窝点契约」之后、「窝点级别契约」之前 |
| **远端衰减（falloff）** | **2026-09-11 船长定：三族统一语义 = 「最远端倍率」**（近端 ×1 → 最远端 = 该武器 `falloff`，线性、保底 0；动能/爆炸作用于**命中**、能量作用于**威力**）。现值：**我方动能炮台民用/MK1/MK2/MK3 一律 0.5**（原 0.30/0.28）、**我方激光四件一律 0.1**（原 0.30/0.35；同时**废除**旧的「命中衰减 ×0.8」换算，旧远端 ×0.44/×0.48 作废）、**敌方默认 `balance.battle.foeFalloff` 0.5**（原 0.3；逐卡 `foeFalloff` 可覆写）。窝点专属件按**逐件**定（**D 守墓者长炮 0.6**——比普通 MK3 的 0.5 更准；**A 劫掠者转管炮保持 0.35、E 巨构残骸炮保持 0.1**，后者设计就是"越远越打不中"）。**A/B 实测（3 种子）**：动能流打深渊之门残血 22~36% → **41~64%**、噬口 43s → **30s**；激光流全面变慢（灰霾 9→16s、噬口 25→32s），**穹顶守卫 100% → 67%**。⚠ **单一实现（2026-09-12 审计 B1）**：`beamPowerFactor` 与 `distFactor` 原本是**两份逐字相同**的实现（前者多一层 `Math.max(0, …)`，而该层在 `falloff ∈ [0,1]` 时**恒不生效**：`t` 已 clamp 到 [0,1] ⇒ `1 − t(1−falloff) ≥ falloff ≥ 0`）⇒ 已合并：**`beamPowerFactor` 改为委托 `distFactor`**（保留"威力衰减"的名字与语义标签），全仓"远端衰减"自此**只有一处实现**；前提由 `content:check`「**远端衰减取值域契约**」守住（56 处 `falloff` 全部 ∈ [0,1]，越界即报错）。✅ **已同步复核（2026-09-12）**：本行口径在 d2 工作树**已落地**（我方动能 0.5 · 我方激光 0.1 · `balance.battle.foeFalloff` 0.5 均已实核）；⚠ 但**基于旧口径测得的 A/B 标定读数不会自动更新**——要按新口径重标需另立批次。 | combat.ts `distFactor`（唯一实现）/ `beamPowerFactor`（委托）；data/modules.ts falloff；balance.ts foeFalloff；tools/battle-calibrate.ts |
| **彩头分层（内部叫法）** | ①基础常驻件直出（无蓝图门槛的 MK1 系民用件）；②低安（sec<0）残骸箱低概率 MK2；③蓝图碎片（高级件不直出，集齐逆向解锁蓝图；直出件不需要）。**⚠ 玩家可见文案禁用「彩头」「主题件」「掉池」等内部叫法**（2026-09-11 玩家反馈"事件日志里看到不符合游戏设定的名词"）——日志/卡面一律写 **「额外掉落」/「特色掉落」/「特色装备」/「常规掉落」**（「保底矿物」是船长已批准的玩家用词，可继续用） | 定稿方向；玩家用词见「特色掉落」条 |
| **射程 × 战术（弱相关）** | **射程与战术仅"弱相关"，不实时强绑定**（2026-09-11 船长）——舰级路径下射程带是**绝对值、不随战术推导**，换战术**不必**跟着改射程；`rangeMinM`/`rangeMaxM`/`rangeMul` 是**独立旋钮**，按设计需要单独调 | 反例（旧强绑定）：旧路径 `TACTIC_RANGE[战术] × 成长`；现口径只保留在**未写 `ships` 的旧路径卡**上 |
| **期望交距（舰级路径取自身射程带）** | **2026-09-11 船长裁决②**：敌人的"想站的距离"= **`自己的有效射程带.min + tacticDesireFactor[战术] × (自己的带.max − 带.min)`**——**战术只决定"带内的偏好位置"**（brawl 0.20 靠带内近端 / orbit 0.55 居中 / kite 0.85 贴远端，**系数沿用旧口径不改**），**带由单位自己决定**（含条目 `rangeMul`/`rangeMinM`/`rangeMaxM` 覆写后的有效值，建档写进 `UnitSpec.foeRangeBand`）。混编卡取 **`foes[0]`（卡上首个主体单位）**；要让头目与杂鱼同带就写覆写（"同卡同带"），**不做加权平均**。**旧威胁推导路径一字不动**（仍用全局 `TACTIC_RANGE`，那里"带与期望同源"本来自洽）。**动机**：旧口径下"敌人想站的位置"与"敌人打得到的距离"脱钩 ⇒ 教学卡双方 0 开火打满 600s 判负（裸船 15/25 艘）、弱技能行触顶、近战装配下玩家 0 开火——三类同源故障 | `foeDesiredRange()` / `UnitSpec.foeRangeBand` / `createFoeSpecsFromShips()`（`packages/core/src/combat.ts`）；守卫 = `tests/foe-ship-path.test.ts`「期望交距」4 条（旧路径 440/2688/8000 逐值锁定 + 舰级路径 441/1210/3550 + 全 9 卡"期望必落在自身带内"）；记录 = `docs/design/p0-foe-desired-range-20260911.md` §7 |
| 参考段火力 | foeRefFire 段表 2.1/9.7/12.8 dps（≤16 / 17-40 / 41+），foeHpOfThreat = 段火力×D(T) | |
| 近盲带 | 武器 minRange 以内。**敌我区分（2026-09-05 拍板）**：敌近盲内不停火、伤害×blindDmgMul(缺省0.3)；玩家近盲内不开火 | blindDmgMul 单卡可覆盖 |
| 胜率口径 | `battleWinPreview`（玩家手动，无 favor）与 `aiWinPreview`（AI：favor 0.3 修正 + logit 扩散）| AI 远征已停用并隐藏（2026-09-05 软下线、2026-09-08 UI 隐藏）；口径代码留存待恢复 |
| 带伤预警 | 悬赏展示胜率口径（2026-09-08）：原显示 − 预计装甲损耗×10pp − 预计结构损耗×25pp——只改展示，结算与 AI/模拟不变 | bountyWinPercentGuarded |
| 连续作战保险 | 巡回场次（2026-09-08）：战斗内结构剩余 <50% 自动撤退（轻损、停环、绝不弃船）；巡回自动再出发门槛提前到装甲 <50% 即修补至 60% | battle.hullEscapeFrac/autoEscaped |
| favor | AI 远征优势：模型胜率 0.8 局 → AI 命中×1.18/敌命中×0.82 | aiFavorStrength 0.3 |
| 承伤三层 | 护盾 s / 装甲 a / 结构 h：伤害按 盾→甲→结构 顺序扣 | P0 承伤持久化（2026-09-05） |
| 结构（=原耐久） | `fleetShip.durability` 与结构层合并：战斗中结构受损即扣耐久并**跨场保留**，归零弃船 | 仅港内维修/套件恢复 |
| 装甲残余 | `fleetShip.armorPct`（0~1）：战斗中装甲损伤**跨场保留** | 同上 |
| 护盾回充 | 护盾损失不保留：战中被动回充（shieldRegenPerSec，初值 2%/s；甲/结构打穿后停充防僵持），脱战/场间回满 | balance.battle |
| 修理组件 | kit 消耗品（repairkit-civ 0.35 / repairkit-mil 0.7）：使用一枚对结构+装甲各恢复上限百分比；市场直售+蓝图自制（材料≈市价55%）；自动链（连续出击前装甲或结构 <50% 自动修补至 60%，2026-09-08）与手动入口（舰船页/停留面板） | shipyard.useOneRepairKit |
| 支援件 | V18.1 support 件：稳定(伤害)/射速/索敌(命中)/陀螺(闪避) | **回调已取消（2026-09-05 定）** |
| 船体维修装置（战后消耗口径） | 2026-09-11 船长：「**船体修理装置不单独显示日志。只将消耗组件数量显示到战后总结。**」⇒ 战斗日志里关于本装置的**两条都取消**（开战「维修装置待命…」/缺料警示、战斗中「组件耗尽自动停机」）；消耗数只进**战后总结**，形如 `，船体维修装置消耗 军用修理组件 ×12`（多型按「、」连接；一枚没耗 → 不添尾巴；旧档无逐型账本 → `消耗修理组件 ×N` 只报总数）。**五处战报同源** `repairUsageText(battle, ctx)`：远征胜/败（含弃船）、遭遇战大捷/失利/自动脱离、AI 副船胜败。装置是否在跑由战斗页徽标表达（`维修装置运转中 · 组件 ×N` / `维修装置停机`，悬停含"开战时未备组件"），故删日志不丢"组件够不够"的判断依据。逐型记账 = `BattleState.repair.kitsUsedByType`（可选、零迁移） | core/combat.ts（pulseRepairs 累加 / `repairUsageText`）；core/expedition.ts、core/encounters.ts、core/ai.ts；state.ts；本稿 §八见 docs/design/hull-repair-module-20260909.md；tests（combat / expedition / b1） |

## 三、装备 / 制造 / 经济

| 术语 | 含义 | 备注 |
|---|---|---|
| CPU | 全船唯一硬装配约束：全位 cpuUse 合计 + 无人机放飞 ≤ 船体 cpu | CPU 类技能讨论挂起（B3 后） |
| 无人机体积档 | 无人机 unitM3 按机种大小分 5/10/20/40 四档（蜂鸟/赤鸢/猎鹰/雷鸥）——机巢可装架数由体积主导（小护卫带蜂鸟赤鸢、炮舰带猎鹰、母舰带雷鸥），每架 CPU 不变、甲板扩展 +m³ 有效（2026-09-09 船长拍板；此前 CPU 先卡死装载、大舱"装不满"） | items.ts DRONES |
| 无人机射程分类 | 无人机武器射程按机型入物品本体（2026-09-10 船长拍板：蜂鸟 2500 / 赤鸢 3000 / 猎鹰 3500 / **雷鸥哨戒 5000**——前三型压缩、哨戒独享远程兑现"航程极远"；combat 读 ItemDef.maxRangeM，旧值 2600 兜底）。配套**无人机中继天线**（高槽 drone-relay 装置族 MK1/2/3 = 射程 +20%/+45%/+80%、CPU 6/16/46、市场 15k/150k/400k；百分比求和乘入机型基础射程、多件线性可叠；市场现货+蓝图双渠道，M3 学习声望 4）——轻机带满配 ≈ 追平裸雷鸥、哨戒可深入激光/导弹带 | items.ts maxRangeM；modules.ts mod-drone-relay-* |
| 无人机分类（种类显示） | 无人机分类入物品本体 ItemDef.droneClass（2026-09-10 船长：归类子属性一起显示在**种类**中）：scout/combat/assault/sentry = **侦察机/战斗机/攻坚机/哨戒机**（与射程分类一一对应）；core 单点 `itemKindText()` 输出「无人机 · 侦察机」，市场行种类列、物品仓库/货仓/图鉴/手册详情与列表 chip 全站同源（残骸「残骸」特例保留） | labels.ts DRONE_CLASS_LABELS / itemKindText；content-check 分类契约护栏 |
| 无人机命中与衰减 | 无人机放飞命中下放机型本体（2026-09-10 船长拍板）：ItemDef.hitRate/falloff，combat 读本体（旧档缺字段兜底 0.6/0.35）。**侦察/战斗/攻坚三型 = 命中 75% 且不随距离衰减**（falloff **1** → 射程带内命中恒定）；**哨戒 = 命中 110%**（近距被 100% 上限截断，用意 = **抵掉部分衰减**）+ **保留正常衰减**（falloff 0.35，射程端点倍率）。命中公式不变（hitChance = (hitRate + 攻方加成) × 距离衰减 − 守方回避，clamp 0~1）。联动：中继天线拉长射程带时，三型同距离命中不变（无衰减）、哨戒同距离命中上升（衰减按 min→max 摊薄） | items.ts hitRate/falloff；combat.ts 无人机条目；tests/drone-hit.test.ts |
| 近防炮 / 机群战损 | 敌方**近防炮**（2026-09-10 船长拍板「无人机可被击落」）：**威胁 ≥ 60** 的敌舰才装，每舰**每 0.5 秒判定一次**（逐舰独立、多舰叠加；**未特意说明一律按此默认档：0.5 秒 / 命中 0.5 / 伤害 5**）——随机挑一架**正在攻击的放飞无人机**（**默认不理会哨戒机；非哨戒机全部被摧毁后转打哨戒机**），按 `pdAcc − 机型闪避` 掷命中、命中按 `pdDmg` 走机型三层抗性，打空即**击落**（该架停火、UI 机群计数递减）；**不看距离**（放飞出去就在威胁之下）、**不参与**敌舰对玩家的常规攻击、**战斗内可 100% 损坏**（无单场上限）。**战后回收**：总回收架数 = `round(总损坏 × 回收率)`（回收率 = `20% + 6%/级 无人机回收学`，满级 **50%**），**2026-09-11 船长定「优先回收高价值的」**——每型先 `floor(损坏×回收率)`，**余数名额按机型基准价从高到低补满**（哨戒机 9,500 ＞ 专属流亡蜂 6,000 ＞ 攻坚机 5,000 ＞ 战斗机 2,200 ＞ 侦察机 900；同价按 id 稳定排序），找回的**回无人机舱清单**继续服役，未回收部分才永久损失（净损失写日志 + 一次性提示）；**战报弹层**显示两行（汇总：损坏/回收/净损失 + 逐型明细：回收 ｜ 净损失，按价值降序），战报自动关闭 6 秒 → **12 秒**（船长同日：「战斗报告持续时间延长」）。**机群战损 = 永久损失（耗材流）**：净损失从无人机舱清单扣除（胜利/失利/撤退/AI 副船一律照扣），需回港重新购买或制造补充；重复清剿在本场净损失过半时自动停环提示补货。默认档实测（王鲭 D3 满编）：噬口/坟场 **0 战损**、硬卡穹顶 **1.4 架**、战损估值约该卡奖励 0.5%；重档（0.5/10、0.5/20）可**整队 10/10 损坏**（胜率随之下滑）——体现"取消上限"后的真实风险。工具 `npm run battle:pd-tune`（`--acc/--dmg/--period` 传参试档） | balance.ts pd*4 参数；combat.ts resolvePointDefense / settleDroneLosses / DRONE_SKILL；state.ts dronePools/droneLost/droneLossNotice/droneLossReport；BattleScreen.tsx 战报两行；tests/drone-loss.test.ts |
| 无人机线技能 | 2026-09-10 船长拍板新增五条（战斗组）：**无人机打击学** `drone-strike`（rank 4，单发 +4%/级，与作战学/导控/船体专属乘算）、**无人机耐久学** `drone-durability`（rank 2，三层血 +4%/级 = 满级 **+20%**，全血条同步放大）、**无人机强化学** `drone-reinforce`（rank 4，三层血再 +6%/级 = 满级再 **+30%**，**与耐久学乘算**，双满 **×1.56**）、**无人机回收学** `drone-recovery`（rank 3，回收率 +6%/级 → 基础 **20%** 至满级 50%；基础值 2026-09-10 船长定：随永久损失制由 10% 上调到 20%，满级仍 50%）、**无人机规避学** `drone-evasion`（rank 4，闪避 +2%/级 = 满级 +10%，相对乘算封顶 90%）；既有两条 = 无人机作战学（伤害 +5%/级）与无人机整备学（装填 −4%/级）。数值常量集中在 `combat.ts` 的 `DRONE_SKILL`，文案 ⟦…⟧ 在 `data/skills.ts`，测试 `tests/drone-skills.test.ts` | data/skills.ts；combat.ts DRONE_SKILL / droneRecoveryRate；tests/drone-skills.test.ts |
| 无消耗自愈 / 结构抗性 | 2026-09-10 船长（异形 C 族专属件引出）：①`ModuleDef.repairFree`（**无消耗自愈**）——战斗中按周期自动修复装甲/结构，**不吃修理组件、永不停机**；与 `repairKit`（消耗件）**互斥**，content:check 强制二选一。**同型多件的修复量按 EVE 曲线收敛**（权重 100%/87%/57%/28%/11%，见 `combat.preloadRepairFor`）——无组件成本兜底，线性叠装会失控。②`ModuleDef.hullResistAdd`（**结构层抗性**）——模块首次可给结构层加抗性（语义同 shieldResistAdd/armorResistAdd：按系缺口复合、上限 90%） | types.ts repairFree/hullResistAdd；combat.ts preloadRepairFor / pulseRepairs / resists.hull；state.ts BattleRepairUnit.free；tests/lair-gear-c.test.ts；content-check 互斥护栏 |
| 敌族专属装备 | 赏金任务·窝点的**专属掉落件**（2026-09-10 船长定：**每族 3 件**，`core/lairs.ts` FOE_LAIR_GEAR）：只在"打赢窝点 → 捞稀有残骸 → 精炼炉开高级箱"链路产出，**无蓝图、不上市场、不入常规掉落池**。**A/C/D/E/G 五族三件已全部逐件过审落地**（A 海盗：转管炮 / 掠袭导弹巢 / 赃物强化舱；C 异形：生体甲壳板 / 生体损管腔 / 酸液喷吐器；D 守墓古舰：陵墓护盾阵列 / 守墓者长炮 / 陵寝装甲层；E 泰坦巨构：巨构残骸炮 / 深层机库 / 巨构骨架；G 烬火流亡：**流亡蜂无人机（专属物品）** / 流亡蜂群导控 / 流亡中继桅）。**池内元素可以是模块或物品**（2026-09-10 船长：G 族第一件改为专属无人机"物品"——模块进装备库、无人机进物品仓库、一次 10 架）；**B 族（武装拾荒者）随窝点取消**，原「拾荒者拆解臂」已撤下；**F 族（制式巡逻）留空**（只有隐藏遭遇模板、无窝点成员）。**掉落规则 = 同族集齐前不重复**（开箱时若该族池里还有玩家当前未持有的件，只从"未持有"里抽；该族全部到手后恢复均匀随机、允许重复）；判定口径 = 当前持有（装备库 + 已装配位，`equipment.ownedModuleCount`），零存档迁移 | core/lairs.ts FOE_LAIR_GEAR；data/modules.ts mod-lair-*；salvage.ts rollRareBoxExtra；equipment.ts ownedModuleCount；tests/lair-gear-collect.test.ts / lair-gear-c.test.ts / lair-gear-d.test.ts / lair-gear-e.test.ts / lair-gear-g.test.ts |
| **乌贼人（G 族物种）** | **2026-09-11 船长裁定**（原话：「**是乌贼人残兵！在和章鱼人的战争中失败了！**」）：G 族的物种是**乌贼人**——与**章鱼人**（＝协会/官方，世界观铁律所定）**不同族的异种**；在与章鱼人的**战争中战败**，但**战败 ≠ 终战**：「**依旧处于战争敌对状态**」⇒ 协会**长年封锁**（天底静区）并**悬赏民间火力**清剿/增援。⚠ **留白（不写）**：乌贼人与章鱼人**是否同源**、战争**起因与年代**（烬火星区文案"百年前的星际火灾"**是否即战争痕迹——未定**）。⚠ **其它敌族的物种与来历均未定**（A 海盗 / B 拾荒者 / C 异形 / D 守墓古舰 / E 泰坦巨构），已登记为待办（总表 P-22）。**族名标记**：族名「鱿烬亡军」里的「**鱿**」是族名用字，与物种名「乌贼人」**同指** | 设定卡 = `docs/design/foe-faction-g-exiles-20260911.md` §二；世界观铁律（章鱼人）见 main 侧 `docs/design/npc-factions-20260911.md` §6 与 `handoff-20260911-to-new-pilot1.md`（**敌族关系留白：协会侧不编，指向敌族文档**——本族设定卡即那份文档） |
| **鱿烬亡军（G 族）**（原名「烬火流亡」· **已作废留档**） | **2026-09-11 船长命名裁定**：族全名 **烬火流亡 → 鱿烬亡军**（物种「鱿」+ 战火余烬「烬」+ 战败残军「亡军」）、族短名（星图「敌对派系」标签）**流亡 → 鱿烬**（取头部，与「海盗／异形／守墓」同款）。**为什么改**（船长质疑「既然是乌贼人，叫烬火流亡是不是有问题」）：原名是**地名＋处境**、不体现"异种敌人"、且「流亡」带**受害者色彩**——而它是**战败但仍在战争状态的残军**。**族格**：**乌贼人残兵 + 依附的流亡聚落**；**族规 = 全 orbit**（蜂群远距压制）；**火力 = 三系无人机**（动能/爆炸/等离子三系齐备；⚠ 现状三卡仍是炮类口径 `kinetic 8:2`、敌方无人机未实装 ⇒ 待落码）；**招牌 = 蜂群式无人机**（保留承诺 + 登记「僚机替代」候选：机群实装前先用 `escorts 1~2`）；**层次 = 外围围攻军 → 残响残舰 → 最后据点**；**「残舰复活」定死为残余自动化程序 + 亡军修修补补**（不讲超自然）。**外形 = 蜂窝舱段 + 补丁帆 + 拼装焊接痕**（"拼出来的舰队"）、族色 = **聚落紫 `#cd9fdd`**；**装备哲学 = "一架便宜、一群致命"**（三件专属件全无人机线：`drone-exile-bee` 流亡蜂无人机 / `mod-lair-drone-tac-g` 流亡蜂群导控 单发 +45% / `mod-lair-drone-relay-g` 流亡中继桅 射程 +65%）；窝点三档称呼 = **残兵头目 / 聚落营地 / 藏匿地**（不加分层所指）。三张卡 = 烬火围攻战 42（烬火星区·L1）· 回音残舰 52（回音荒区·L2）· 天底静区封锁 66（天底静区·L3），**均 L3 窝点候选、均单舰无僚机**。⚠ **玩家可见三处仍是旧名**（星图标签 / 两件专属件说明文 / 一条已发布公告）⇒ **待审稿**；另**顺带发现 14 处「（X 族）」内部编号**进玩家可见文案（A3/C3/D3/E3/G2），一并列待审稿 —— ✅ **均已落码（2026-09-12 二号 · 船长「允许修改文案」）**：星图短名「**鱿烬**」· 族全名「**鱿烬亡军**」· 14 处「（X 族）」内部编号**清零**（物品名保留「流亡」不动，P-36） | 设定卡 = `docs/design/foe-faction-g-exiles-20260911.md`（标"已确认"）；`anomalies.ts`（三卡）· `lairs.ts`（词表/专属件/签名）· `universe.ts`（烬火星区/回音荒区/天底静区）· `modules.ts` + `items.ts`（三件专属件）· `shipArt.tsx`（族短名/族色）· `announcements.ts`（待改那句）；旧词条原文按"历史条目不回改"另行留档。**舰级与蜂群（2026-09-12 二号 · d2 落码）**：四档舰级 = **围攻残兵舰 T1（357）· 残响残舰 T2（310）· 天底封锁舰 T3（271，挂蜂群机 **2 架**）· 亡军战列舰 T4（215，空置壳体）**；全族**速度定值 1.05×**（船长「速度口径按照1.05算」）；蜂群**无后备**（A3「打光为止不补充」对本族继续成立）；**蜂群机射程 = 7,000m**（船长 2026-09-12「敌方蜂群攻击范围提高到 7000」；G 族**无受击增程**）；三张卡编成 = 2 护卫 / 1 驱逐 2 护卫 / 1 巡洋 2 驱逐，**总血与炮台总单发逐字守恒**（见 G 卡 §十） |
| 武装拾荒者（B 族） | **2026-09-11 船长定案（九条族格裁决 + 四条补充裁决）**：族短名「**拾荒**」· 全名「**武装拾荒者**」（**移除"靶机"**）；**新手过渡族**（威胁 6~20 专用，不延长到中段）；舰种只登记 **T1 护卫舰 / T2 驱逐舰**（**不得 ≥ T3**）；**战术统一 orbit**；**速度偏慢 `speedRatio 0.80`**（拾荒武装艇 272 / 拾荒火力舰 236）；火力签名 **动能 8 : 2**（教学卡保持纯系）；**乱射（命中 0.55）是单卡特征、不升格族级**；**血型逐卡自定**；**没有窝点**——删 `lairCore`、退出赏金任务派发，窝点级别契约由「19 张 / 5 族」改为「**17 张 / 4 族**」；专属件**已是空表**（`FOE_LAIR_GEAR.B = []`，词表「已停用·留档」）⇒ 无需移除。三张卡 = 演习场**驱逐令** 6（教学，由"讨伐令"改名）· 新港商路护航令 10 · 占港武装通缉 16。**权威设定记录**：`docs/design/foe-faction-b-scavenger-20260911.md`（状态：设定已确认 · **已落码**） | docs/design/foe-faction-b-scavenger-20260911.md；data/anomalies.ts 三卡；data/foe-ships.ts 两舰级 FOE_SCAV_SKIFF / FOE_SCAV_ARMED；core/lairs.ts FOE_LAIR_GEAR.B；tests/foe-ship-path.test.ts |
| B 族期望射程修正（**已落码**） | **2026-09-11 船长裁决「单独给 B 族添加期望射程修正」**：修的是 **orbit 下"双方打不到"的 P0**——`foeDesiredRange()` 原按**旧全局战术射程表**算敌人期望交距（orbit ≈ 2.7~3.5 km），而拾荒武装艇射程只有 1~2200 m、新手基础舰炮只有 2500 m ⇒ 双方 **0 次开火、600 秒判满判负**（裸船零技能：改判前 100%/23s）。**落码**：`FoeShipDef.desireRangeM`（舰级）/ `FoeShipSlot.desireRangeM`（卡上覆写）→ `UnitSpec.foeDesireRangeM` → `foeDesiredRange()` **优先采纳**（优先级：条目 > 舰级 > 旧全局表；**未写该字段的单位零行为变化**，A 族实测仍为旧表 440 m）；现值 **拾荒武装艇 1300 m / 拾荒火力舰 1800 m**（均落在自身射程带内）。**实测（3 种子均值）**：演习场驱逐令 裸船零技能 **0%/600s → 100%/52s/残血 99%/敌开火 12 次**；新港护航 → **100%/165s/残血 54%**。护栏 = `content:check`「期望射程契约」（取值须落在本舰级射程带内 + **B 族每张舰级路径卡必须显式声明**） | docs/design/foe-faction-b-scavenger-20260911.md §四/§八；core/combat.ts foeDesiredRange；types.ts FoeShipDef.desireRangeM / FoeShipSlot.desireRangeM；tests/foe-ship-path.test.ts |
| 机动代价（速度代价） | 2026-09-10 船长（陵寝装甲层引出）：`ModuleDef.speedPenaltyPct`——**重甲件的常驻战斗速度代价**（值域 (0, 0.9]，content:check 只允许挂在装甲**容量件**上）。进战斗时 `speedMps` 乘 `1 − 该值`；**多件不叠加、只取最重一件**（与推进器失稳 `hitPenalty` 同口径：重甲不会叠成静止）。**只影响接敌/拉开距离的距离机动，不改命中**；不涉跃迁与工厂循环 | types.ts speedPenaltyPct；combat.ts worstSpeedPen（createPlayerSpec.speedMps）；shipInfo.tsx「机动代价」行；content-check 护栏；tests/lair-gear-d.test.ts |
| 目标距离（按星系独立保存）· 胜率预估口径 | 2026-09-11 船长：「**玩家每个星系设定的目标距离独立保存，预估胜率的战斗按照那个距离决定。如果没有，采用射程中段距离。**」（起因 = 船长问「预估战斗中采用的是默认的预期距离吗」——答：是，旧口径只认单一全局值、且预估恒用中段）。**口径**：`state.expedition.desirePrefByGalaxy`（键 = 星系 id，值 = 米；**可选字段、零迁移、随档保留**，`save.ts` 只收"键非空 + 值正有限"）；**单点读写** = `combat.desirePrefOf(state, galaxyId)` / `combat.setDesirePrefOf(state, galaxyId, m)`。**写入**：战斗内拖距离条 / 点「贴脸·中距·风筝」（`setBattleDesire` 写**本场所在星系**＝远征目标卡星系）· 出发时显式 `opts.desireM`（写**目标星系**）。**读取**：①**实战开战** `startBattleFor`（远征 / AI 副船按卡星系；遭遇战显式传 `desirePrefOf(state, enc.galaxyId)`——遭遇模板自带 galaxyId 是模板产地、不是玩家所在星系）；②**玩家可见胜率**——**MC N=21 预估**（`buildEvalState` 把该表带进评估快照 ⇒ 每一局模拟都按该星系设定打）与**稳态解析**（派系活跃卡 / 窝点卡的 `steadyPreview`：设定过就用设定距离、钳到开战距离内）。**回落**：该星系没设过 ⇒ **战斗**用 `desiredRangeFor(me,'mid')`＝主武器有效射程中点；**解析口径**沿用模型基准「双方期望距离中点」（`steadyDistance`）。**作废**：旧全局字段 `desirePrefM`（老档不再沿用、一律回落；类型里留兼容占位，读档不回填）。**已知取舍**：**只有主控吃**（船长 2026-09-11 追加裁决）——AI 副船开战显式传 `desireM = null`＝强制射程中段；主控远征、低安遭遇与胜率预估才读该星系设定；战斗界面暂无「本星系目标距离」提示（船长同日裁「不用更改」） | core/combat.ts `desirePrefOf`/`setDesirePrefOf`/`startBattleFor`/`steadyPreview`；core/expedition.ts `setBattleDesire`/`beginBattleAt`/`startExpedition`；core/encounters.ts；core/winEstimate.ts `buildEvalState`；core/state.ts；core/save.ts；tests/desire-per-galaxy.test.ts（6 例）+ expedition.test.ts |
| 主武器（战术距离口径） | **2026-09-12 船长裁定「甲」**：**主武器 = 射程最远的武器**（并列射程取名义火力大的，再并列保持武器表顺序）——单点 = `combat.mainWeaponOf`。**读这一处的有四处**：三条战术距离（贴脸 = 主武器近端×0.6 / 中距 = 主武器带中点 / 风筝 = 主武器最远×0.95；战斗中三按钮与出发前战术同源）、战斗界面「我方射程带」（`battleZonesFor`）、战场弧的主武器米数刻度（`battleArcsFor` 的 `isMain`）、无星系偏好时的预估回落中距（`desiredRangeFor` → `steadyDistance`）。**作废旧口径** `weapons.find(w => w.kind === 'gun') ?? weapons[0]`（2026-09-04 初版）：**激光是 `beam`、基础舰炮（恒在的兜底武器）是 `fixed`** ⇒ 纯激光/无人机船会回落到基础舰炮（2,500m）；**近防炮是 `turret` 槽（`kind = gun`，2,500m）** ⇒ 装了它的船被近防炮抢位（与该件自己的设定「占一个高槽、**不是主炮替代品**」相冲）。触发时机 = 近防炮上线后玩家开始装它。**实测（真数据真引擎）**：激光船 200/1250/2375 → **200/2300/4370**（射程带「基础舰炮 0~2500」→「轻型激光炮 MK1 0~4600」）；近防炮 + 导弹架 MK2 200/1251/2375 → **540/6330/11172**；炮台船与纯导弹船**一字不变**（旧口径恰好取对） | core/combat.ts `mainWeaponOf` / `desiredRangeFor` / `battleZonesFor` / `battleArcsFor`（`isMain`）/ `steadyDistance`；renderer `panels/BattleScreen.tsx`（三按钮 + 弧端米数刻度）；tests/main-weapon-range.test.ts（7 例） |
| 推进器周期点火（加力推进） | 2026-09-10 船长：矢量推进器**不再常驻提速**——改为**点火 60 秒 → 冷却 60 秒**、**开场即点火**；点火期战斗机动 ×(1+爆发倍率)（**MK1 +30% / MK2 +60% / MK3 +100%**，多件按 EVE 曲线收敛），冷却期回基础速度。周期由**战斗时钟推导**（`thrusterPhase`，不占存档字段），引擎与战斗界面**同源**；战斗界面底部「装填冷却」行显示倒计时。失稳代价 `hitPenalty` **也只在点火期生效**（2026-09-10 船长追加：冷却期不失效稳，`stepBattle` 用 `meAtk` 把 `hitMul` 置 1）。动机 = 原常驻加成让玩家全程快于近战敌 → 距离拔河停在玩家期望距离而近战敌够不着（复核 §9）。**周期口径文案单点（2026-09-11 船长：「推进器现在有持续时间和冷却时间，这点希望在推进器的说明内讲清」）**：秒数**唯一出处 = `balance.battle.thrusterBoostMs/thrusterCooldownMs`**，界面一律经 core `thrusterCycleText()`（短缀「点火 60 秒 / 冷却 60 秒」）/ `thrusterCycleFullText()`（全缀「60 秒点火 / 60 秒冷却，开场即点火」）取值，**不许写死 60**；落点 = 换装卡与装备库/手册网格短行（`moduleShortEffect`）、模块信息卡「加力推进」行（`PROP_TAIL`）、装配页「机动速度」行（基础值与点火期值并列）。 | combat.ts thrusterPhase / createPlayerSpec.thrusterBoost / stepBattle 距离步进；balance.ts thrusterBoostMs/thrusterCooldownMs；labels.ts thrusterCycleSeconds/thrusterCycleText/thrusterCycleFullText；shipInfo.tsx moduleShortEffect / PROP_TAIL；FitPage.tsx 机动速度行 + diffSegs 点火期速度段；modules.ts mod-prop-*；BattleScreen.tsx 底部冷却格；tests/thruster-charge.test.ts |
| 敌速基准（速度中位线船只） | 2026-09-10 船长裁决：「采取固定锚定，参考按照速度中位线的船只进行参考」（并选「不压反提」）——**敌速不再按威胁段参考船分档锚定，改为对齐单一基准船**：船池 25 艘按 `maxSpeedMps` 排序取中位 = **长尾鲨级导弹巡洋舰（船速 272 / 敏捷 0.54）→ 基准战斗机动 165.2 m/s**（= 272 ×0.6×(1+(0.54−0.5)×2×0.15)，无技能、不装推进器）。**比率 = 敌战斗机动 ÷ 165.2**，其中敌战斗机动 = `foeSpeedMps` ×0.6×0.94（敌敏捷固定 0.3）。**全 26 张敌军卡逐卡显式给 `foeSpeedMps`**（brawl 低段缓坡 1.10/1.15/1.20 → 1.29~1.48×；orbit 0.96~1.20×；kite 维持 0.69~0.80×），`balance.foeSpeedCapMul` 1.2→1.55；**旧公式（段参考船 × m_base × 战术系数）降级为"未逐卡标定的新卡"兜底默认**。⚠ 口径换算说明：旧表分母是**阶梯**（低段 220/250 比中位船低、高段 300/320 比中位船高），换单一 275.3 后低段敌速被抬高、高段被压低——**"高段变慢"只是换分母，不是数值被削**（穹顶守卫同一份 356：对旧分母 1.05×、对新分母 1.22×）。护栏 = `content:check` **「敌速口径契约」**（每卡必须显式声明 + 比率落在战术带 brawl 1.05~1.55 / orbit 0.90~1.25 / kite 0.60~0.85 内） | data/anomalies.ts `foeSpeedMps`；balance.ts foeRefSpeedTable / foeSpeedCapMul；tools/content-check.ts；tools/battle-calibrate.ts `--proposal`；docs/design/enemy-speed-retune-20260910.md |
| 敌方基础单发（威胁→伤害推导链） | 敌方单发完整推导（`combat.ts` `createFoeSpecs`）：**①份额** `uThreat = 威胁 ÷ (1+0.6×僚机数)` → **②火力** `dps = uThreat × foeDpsPerThreat(0.8)` → **③单发** `round(dps × 装填4000ms/1000 × [0.62÷有效命中])`，**命中补偿只对非光束**（能量光束 `effHit=1` → ×1）。**⚠ 原推导链第三项的「逐卡伤害倍率口」已于 2026-09-11 按船长「先移除所有 `foeDmgMul`，按照实际算」整体退休**——该字段已从类型上删除，单发一律按实际推导值算（赤潮 0.27 / 蜃影 0.30 的逐卡压制、深渊之门与星髓各 0.35 的等效回退，均已并入实际值或撤除）。**两个逐卡口**（2026-09-10 船长「直接调整基础伤害不行吗」）：**`foeShotDmg`＝基础单发直写**（写了就短路整条链，首例＝深渊之门 45：推得 90 → 直写 45）；**`foeFalloff`＝逐卡远端衰减覆写**（动能/爆炸走 `distFactor` 命中衰减；**能量走 `beamPowerFactor` 威力衰减**——2026-09-11 起两者同算法「近端 ×1 → 最远端 = 本值」，**值越大衰减越轻**；缺省 `balance.battle.foeFalloff` **0.5**（同日 0.3 → 0.5），首例＝深渊之门 0.1 → 远端威力 ×0.10）。**⚠ 标定纪律（船长追问「有无考虑堆抗性」「不考虑推进器吗」后补，2026-09-10）**：伤害/血量类标定**必须跑三组两端**——①**只堆主系抗 vs 堆对应抗性**；②**不带推进器 vs 带推进器**；③**单系 vs 双抗**。实测差距极大：深渊之门「只堆主系 0% / 堆能量抗 100%」「**不带推进器 0% / 带 MK2 40%**」；星髓虫群「**不带推进器 残血 81% / 带推进器残血 100%**」；噬口猎杀令**推进器反而更慢**（65s→78s：敌人够不着时 +60% 机动无价值、只剩失稳代价） | combat.ts createFoeSpecs / beamPowerFactor / distFactor；balance.ts foeDpsPerThreat / foeReloadMs / foeHitCompMul；types.ts foeShotDmg / foeFalloff；tools/battle-calibrate.ts `--dmg-sweep` / `--resist-sweep` / `--prop-sweep`；docs/design/faction-tactic-20260910.md |
| 族系战术改判（2026-09-10） | 船长「敌人战术尽量贴近族群特色」→「推进下一批」：**坟场守墓人 88 brawl→orbit**（D 族＝残破古典长舰 + 12 km 必中点名炮，不该在 2.8 km 贴脸）、**噬口猎杀令 80 orbit→brawl**（C 族＝有机曲线 + 螯颚 + 酸液喷吐器，应贴脸吞噬）；**射程带 / 速度 / 血量三件套一起动**。**难度守恒**：噬口由「全程输出的墙」变「要爬进来的贴脸怪」（中位 S2 **0%→100%/78s**）、坟场由「贴脸怪」变「墙」（**100%→0%**），E 段中位通过卡数不变。**族谱权威 = `shipArt.tsx` `FOE_FAMILY`**——未登记卡**缺省 F 制式巡逻**（4 张 `enc-pirate-*` 即此例，非 A 海盗） | anomalies.ts（tactic / foeSpeedMps / foeHpOverride）；apps/desktop/.../ui/shipArt.tsx FOE_FAMILY；docs/design/faction-tactic-20260910.md |
| **敌编队接近速度（2026-09-11 改判：取平均）** | 距离模型是**单标量**（`b.distanceM` + 拔河公式），故敌侧需要一个"编队速度"。**原口径 = 存活单位的 `max`**（最快那条船决定整队）；**船长 2026-09-11 改判：「能否敌舰移动速度按照敌方是**所有船的平均值**算」** ⇒ 改为**存活单位战斗机动的算术平均**（`combat.foeV`，`stepBattle` 内一处）。**动机**：混编卡里"最快单位拖快整队"会失真——穹顶守卫 = 2 静滞卫舰（129）+ 1 守墓长舰（232）原按 **232** 走，与"静滞卫舰是半速炮台"的设定相冲；改平均后该队按 **163**（战斗机动 92）走。**影响面**：**同速编成逐字不变**（单舰卡、同型多舰卡如 A 族头目+同族杂鱼 386.75 vs max 391、C 族虫群、D 族两张单型卡）；**混编卡**才有差（实测：D 穹顶 E1 行交距 7,459 → **6,878**；A/C 两族混编卡读数基本不动） | `packages/core/src/combat.ts`（`stepBattle` 的编队速度段）；用例 = `tests/thruster-charge.test.ts`「冲锋倍率不外溢」（按平均值口径重写） |
| **敌突进 / 冲锋（2026-09-11 起实装于 C 族巨兽）** | 2026-09-10 船长定：高威胁近战敌在**够不着**时机动 ×`foeChargeMul`(=2) 逼近；**船长同日追加「暂时先取消实装，仅实现功能」** ⇒ 由 `balance.foeChargeEnabled`（默认 `false`）总开关控制。**2026-09-11 两处改判（C 族第二批）**：①**资格扩为两条互相独立的来源**——**舰级级 opt-in**（`FoeShipDef.foeCanCharge`，**无条件放行、不看总开关与威胁门槛**，用于"慢而硬、追不上"的重型单位；首用 = **噬口巨兽**，实速 277）与**老路**（威胁 ≥ `foeChargeThreatFloor` 60 且 战术 = brawl，受总开关约束）；②**结束条件由「进入自己武器射程后再维持 `foeChargeMaxHoldMs`(2 秒)」改为「到达目标距离」**（船长：「冲锋结束条件修改，改为到达目标距离，冷却时间延长至 20 秒」）——判据 = 距离压到**敌方期望交距** `foeDesire`；**冷却 `foeChargeCooldownMs` = 20 秒**。另修正一处判据：距离测试由 `foes[0].weapons[0]` 改为**该冲锋单位自己的武器**（原写法只在"冲锋者恰好是编队首位"时与"够不着 = 在**自己**射程外"等价）。⚠ **两条实况须知**：**只在有该单位的波次生效**（`stepBattle` 收到的是当前波编制；噬口巨兽在第 3 波）；**加速只乘在冲锋者自己身上**（2026-09-11 船长：「冲锋还是按照**巨兽自己的速度**算…哪怕是冲锋也是按照巨兽速度」）——编队接近速度 = `max(编队速度, 冲锋者速度 × 倍率)`，**倍率不外溢**（⚠ 原实现是 `if (b.foeChargeOn) foeV *= 倍率`，把编队速度整体翻倍；C 族噬口第 3 波因此全编队 613.6 m/s 飙车，实测把该卡打成四行全败；改后该波按编队速度走）；**结束条件改为"到达"后，若玩家恰好把距离卡在目标距离略外侧，冲锋会长期不结束**（实测僵持 630m vs 目标 440m）——这是改判的直接推论、不是缺陷。**已停用但保留**：`foeChargeMaxHoldMs`、`BattleState.foeChargeEnteredAtMs`（不动存档形状、便于回退）。**2026-09-10 收尾销账**（船长「已经确认过了，可以移除」）＝ **代码 / 开关 / 参数 / 用例一律原样保留**（启用依旧是改一个开关 + 复跑标定）；「近战短战够不着」按 **D 口径**承认：**短战卡 = 高血沙包**，威胁由时长与消耗承担 | balance.ts `foeCharge*`（5 项）；combat.ts `updateFoeCharge` / `createFoeSpecsFromShips`（`ship.foeCanCharge`）；types.ts `FoeShipDef.foeCanCharge`；state.ts `foeChargeOn`/`foeChargeEnteredAtMs`/`foeChargeCdUntilMs`；BattleScreen.tsx「敌突进中」标记；tests/thruster-charge.test.ts（含"到达目标距离才结束"确定性用例）；读数与实况见 `docs/design/foe-faction-c-alien-20260911.md` §10.3~10.4 |
| **波次转场距离回拉（⚙ 暂时关闭）** | **2026-09-09 船长建议**：多波次转场时把交战距离**向开战距离回拉** `waveReopenFrac` 比例（原值 0.5）——增援"从更远的接战距离"入场、双方**重新接近**（重演接近期）。**2026-09-11 船长：「将敌人增援波次距离会后退的惩罚暂时关闭」** ⇒ 新增**总开关** `BattleBalance.waveReopenEnabled`（**现值 `false` = 关闭**）：关闭时下一波在**当前交战距离原地入场**，`waveReopenFrac 0.5` **保留不动**（改回 `true` 即恢复旧口径）；玩家可见日志同步改中性表述（关闭时「敌方增援入场。」，开启时才说「自远处入场，重新接近中」）。⚠ **实测影响面 = 只有 4 张多波卡**（噬口猎杀令 80 / 坟场守墓人 88 / 虚海守望者 88 / 穹顶守卫 96），其余 **22 张零变化**（26 卡校准矩阵 + 标准复核 A0~A3 双向对账）；方向**不是单向变简单**——**时长普遍变短**（均值 **−3.3s**、最多 **−44s**）、但**残血平均 −9pp**、胜率 **8 格降 / 3 格升**：**近战装配受益**（不必再"重新接近"），**远程装配反而更疼**（第二波直接贴脸入场、敌人不吃远端衰减）。与「敌突进」「单波次内增援」同款总开关形态 | `packages/core/src/balance.ts`（开关 + 比例）· `types.ts`（`BattleBalance.waveReopenEnabled`）· `combat.ts`（波次转场块 + 两句日志）· 用例 = `packages/core/tests/wave-battle.test.ts`「波次转场距离回拉 · 总开关」3 例（默认锁定 / 关闭⇒原地 / 打开⇒回拉 50%）；口径记录 = `docs/design/wave-battles-20260909.md` §六·补 2 |
| **单波次内增援（⚙ 已实现未启用）** | **船长 2026-09-11 裁决：「先完成相应的系统机制，不使用。用作后续机制。」** ——「敌突进」同款形态：机制/参数/总开关/契约/用例全部就位，**任何战斗都不触发**。**机制**：编成条目写 `enterAt` 后，该单位**开战不进战场**，由 `advanceBattleFor` **每拍**检查、条件命中才入场（入场走 `enterReload`＝一次装填的哑火窗口）；**三种触发（任一满足即入场，可写多个）**：① `sec` 开战满 N 秒（战斗时钟口径）② `afterKills` 本场已击毁敌单位数 ≥ N ③ `hpBelow` **己方（本波敌方编成）**存活剩余总血比例 ≤ N（"残部呼救"；要"玩家残血才来援"须另加字段，不改本语义）。**未写 / 条件全无效 = 开战即在（= 现状，逐字不变）**——刻意不产生"永不入场"的沉默副作用。**总开关** = `BattleBalance.foeReinforceEnabled`，**默认 `false`**（建档期即按开关过滤：关着就不写字段）；**距离重开** = `foeReinforceReopenFrac`（语义同 `waveReopenFrac`，**缺省 0 = 原地入场不重开**）。**存档零迁移**：不新增任何存档字段——"是否已入场"**由 `battle.units` 里有没有该 tag 反推**（尸体也留着，故"在场过"永远是"存在"）；读档续战用同一份编成重新推导。**仅舰级路径**（`enterAt` 长在 `FoeShipSlot` 上）；**未到触发点的增援不参与判胜**（在场者全灭即胜＝"援军来不及赶到"）。⚠ **与多舰补偿系数 `2N/(N+1)` 不可叠加**：那是**数值层补偿**、本机制是**结构解法**（N 个单位不再"逐个被击毁"而是分批到场），**若将来启用增援，补偿系数必须下调或退场**。守卫：`content:check`「**增援机制未启用契约**」在开关关闭期间**禁止任何卡写 `enterAt`**（让"未使用"由代码守住，而不是靠人记）；用例 `tests/foe-reinforce.test.ts`（13 项，直接构造、不依赖任何真实卡使用） | `FoeShipSlot.enterAt` / `FoeReinforceTrigger`（types.ts）；`balance.foeReinforceEnabled` / `foeReinforceReopenFrac`；combat.ts `normReinforceTrigger` / `reinforceTriggered` / `resolveReinforcements` / `createBattleState` 跳过未入场者；契约 = tools/content-check.ts；设计稿 = `docs/design/foe-reinforce-20260911.md` |
| 结构容量（hullHpBonus） | 2026-09-10 船长（E 族巨构骨架引出）：`ModuleDef.hullHpBonus`——**结构层血量加成**（0.6 = 结构 +60%；值域 (0, 2]，content:check 护栏）。结构层此前是全游戏**唯一没有容量模块**的一层（只有 C 族的"结构抗性"与船体加固理论技能能碰），本字段把它补上。多件加算求和（与 armorHpBonus 同口径），生效处 `createPlayerSpec` 的 `hp.h`，技能（船体加固理论 / 重装舰操作 +4%/级）再乘于其上；与 hullResistAdd（结构抗性）互不替代 | types.ts hullHpBonus；combat.ts hullHpMult；shipInfo.tsx「结构容量」行与短效尾缀；content-check 护栏；tests/lair-gear-e.test.ts |
| 悬赏奖金口径（展示=到账） | 2026-09-10 船长按玩家反馈拍板（"卡片赏金与实际到账不一样"）：**取消奖金浮动**（`balance.rewardJitter 0.15 → 0`）——卡片展示值即到账值；**唯一随机奖励 = 情报彩蛋 +10%**（15% 概率触发，触发时写事件日志说明）。展示公式与结算公式统一为 `基础奖金 × 赏金猎手学系数（1+8%/级，满级 1.4）`：常驻悬赏卡用 `anomaly.rewardIsk`、赏金任务（窝点）卡用 `lairBaseRewardIsk(卡, 档位)`（**此前赏金任务卡漏乘技能系数，已修**）。AI 副船远征同口径 | balance.ts rewardJitter；expedition.ts resolveBattleOutcome；ai.ts；Expedition.tsx 卡片展示；tests/bounty-reward-display.test.ts |
| 无人机舱（机舱联动） | 装载清单制（2026-09-08）：战斗只放飞清单内已装入的无人机，舱容 = 船体 `droneBayM3` + 已装**甲板扩展** bonus，与装配共用 CPU（装入受舱容 + CPU 双重校验）。**机舱变小的联动（2026-09-10 船长）**：卸下甲板扩展（或旧档槽位对齐裁掉甲板件）导致舱容不足时，**超出容量的无人机自动按清单逆序卸下并退回仓库**（与 createPlayerSpec 的超量裁剪同口径），写事件日志 + 一次性提示；`repairDeprecatedModules` 载入时同样整理 | equipment.ts droneBayCapOf / trimDroneLoadToBay / unfitAt(ctx) / adjustDroneLoad；tests/drone-bay-trim.test.ts |
| 无人机专属加成 | 船体级 `ShipDef.droneDmgBonus`（2026-09-10 船长拍板）：无人机舰的"专属加成"，只乘入**放飞无人机单发**，与战术导控、无人机作战学**乘算**；对称于其它巡洋舰的武器族加成（+12% 档）。**背景**：无人机舰原有的 `powerBonus` 只喂炮台、对机群无效（王鲭 0.6 与长尾鲨同级却吃不到机群），等于把母舰往炮舰方向推——故按船长口径"削火力 + 加专属"：**王鲭 powerBonus 0.6→0.3 且 +12%**、**梭鱼 0.45→0.25 且 +8%**（T2 档）。实测：王鲭 D3 名义 220→**245**、同一艘船炮流 183→**149**（−19%）；梭鱼 D2b 132→142、炮流 126→109 | ships.ts droneDmgBonus/powerBonus；combat.ts 无人机单发乘算；tests/drone-ship-bonus.test.ts |
| 无人机四型定位 | 四型无人机的 role 契约（2026-09-10 船长拍板）：**侦察机**闪避最高装甲最薄（闪避 0.45 / 总血 19 / 单发 1× / 近程）、**战斗机**全属性平均（0.25 / 40 / 2× / 中程）、**攻坚机**血量最厚单发高闪避最低（0.10 / 97 / 4× / 中远·装甲向抗性）、**哨戒机**射程最远单发最高（狙击）且血量与侦察机相仿（0.18 / 23 / 6.7× / 最远·保留命中衰减）。**新增制式机型必须落在契约区间内**——契约可执行版在 `data/droneRoles.ts`（`DRONE_ROLE_SPECS`），由 `content:check`（逐机 + 跨类阶梯）与 `tests/drone-roles.test.ts` 双重守卫。**2026-09-10 扩展（专属无人机引出）**：①**锚点机型** `DRONE_ROLE_ANCHORS`（蜂鸟/赤鸢/猎鹰/雷鸥）显式化，单发基准与跨类阶梯**只比锚点**；②**专属型号（`exclusive`）豁免四型区间校验**（仍守硬边界：闪避 0~0.9、三层血 1~500、单发 1~200、射程 500~20000、命中 0.2~2）→ "专属强化型 + 制式型"同类共存合法，四型定位本身不变 | items.ts DRONES；data/droneRoles.ts；docs/design/drone-roles-20260910.md / g-exile-bee-drone-20260910.md |
| 专属型号 / 专属物品（exclusive） | 2026-09-10 船长（G 族「流亡蜂无人机」引出）：`ItemDef.exclusive`——**只能从敌族窝点·高级箱产出**的物品（无蓝图不可造、不上市场、不入常规掉落池；与模块侧「来源唯一契约」同一套断言）。首批 = **流亡蜂无人机**（侦察机档专属强化型：单发 6 / 闪避 0.55 / 血 15 / CPU 5）。**无人机是消耗品**（会被点防击落、永久损失），故专属无人机**一次掉 10 架**（`RARE_BOX_DRONE_UNITS`），持有判定 = **物品仓库 + 各船机舱架数**（`equipment.ownedItemCount`）——手上还有就不重复掉、**打光之后重新进池**（打光再刷即可补货） | types.ts ItemDef.exclusive；items.ts drone-exile-bee；salvage.ts RARE_BOX_DRONE_UNITS / rollRareBoxExtra；equipment.ts ownedItemCount；content-check 来源唯一契约 |
| **机群火力占比（`droneFireShare`）** | **2026-09-11 船长：「允许调整敌舰的无人机/炮台火力比例。这个要根据每个悬赏卡制定」**——**逐卡旋钮**，语义 = **守恒拆分**：这张卡的**总火力不动**，机群与炮台**此消彼长**。**层级**：`FoeShipSlot.droneFireShare`（**编成条目级**，卡上逐条写）＞ `FoeShipDef.droneFireShare`（舰级缺省）；**不写 = 旧算法**（机群与炮台同吃 `dmgMul`，占比隐式决定 ⇒ **零行为变化**）。**算法**（`combat.droneFireSplitOf`）：①基准 `T` = 该条目**现口径实收总单发**（含 `dmgMul`/多舰补偿/取整）；②机群 `D = clamp(round(T×s), 架数, …)`、炮台 `G = T − D`（**两侧各保底 1/架 · 1/单位** ⇒ 无 0 伤害条目）；③均摊：机群余数补**前面的架次**、炮台余数补**前面的单位**（Σ 精确）。**范围** = 只含「机群 vs 母舰武器组」（敌侧近防炮照 B3 不动）；命中/射程/装填/血型/交距**一律不动**；**不显示给玩家**。守卫 = `content:check`「机群与防空契约」⑥（0~1 · 须有机群 · **总单发守恒实测**）+ `tests/foe-drone.test.ts` 5 条 | 落点：types.ts（两处字段）· combat.ts（`droneFireSplitOf` + 建档取用）· tools/content-check.ts 契约⑥ · docs/design/foe-drone-system-20260911.md §五·补 |
| **总火力锚点（`firepowerAnchor`）** | **2026-09-12 船长：「架数变多、总火力不动」**——条目级 `FoeShipSlot.firepowerAnchor`（**须与 `droneFireShare` 成对写**）：写了它 ⇒ 本**条目**（该 slot 所有单位合计）的实收总单发**以锚点为准**，再按占比拆机群/炮台。**为什么需要**：机群架数是**舰级**属性，而 A5 口径下"每架无人机都是一门炮" ⇒ 架数一变总量就变，且**取整步长 = 架数**（7 架时总量只能按 7 递增 ⇒ 216 这类目标取不到）。**口径**：写了锚点 ⇒ 该条目的 `dmgMul` 不再参与总量；**窝点派生时锚点随档位同乘 `scale`**（漏了会"血涨火力不涨"）。守卫 = `content:check`「机群与防空契约」⑥ 第二口径（**实收总单发 = 锚点合计**）+ `lairs.lairAnomalyOf` | types.ts · combat.ts（`droneFireSplitOf` 的 T 取值）· lairs.ts（派生成倍）· E 族三卡 = 216 / 72+82 / 672 |
| **受击增程（炮台）** | **2026-09-12 船长：「给 D 族静滞卫舰加入类似 E 族挨打加炮台射程的效果，不过「仅影响所有静滞卫舰」。挨打后射程增加 50%」**——落码 = `FoeShipDef.gunRangeMulOnHit`（数据现值只登记 D 族「静滞卫舰」= **1.5**）→ `UnitSpec.foeGunRangeMulOnHit` → **标量** `BattleState.foeGunRangeBuff`（**一次触发、本场永久**、值即倍率）。**三条口径**（船长当日选定）：①**甲** 任一静滞卫舰**本体被命中** ⇒ **全体静滞卫舰**吃倍率（打机群 / 未命中不算；**只推一条**画面提示「静滞阵列解除限幅：静滞卫舰炮台射程 +50%」）；②**乙** **只延长最远射程**（12,000 → **18,000m**）、近界 2,062 不动，**原射程内的命中/伤害折减一字不变**、延长段**按同斜率**继续线性衰减（18 km 处 ≈ ×0.20，而不是趴在 `falloff` 平台上）；③**只作用于带该字段的敌舰** ⇒ 同场的守墓长舰**不受影响**（与 E 族那条"整支敌队的机群 ×4"是**两套独立状态**，互不覆盖）。**单一出处** = `foeGunRangeMulOf` / `foeGunMaxRangeOf` / `foeGunPowerFactorOf`：**开火射程门 · 命中与伤害衰减 · 战斗界面底部射程标签**三处共用。守卫 = `content:check`「机群与防空契约」⑤c-2（只允许 D 族静滞卫舰 · 倍率 ≤ 2 · **必须真出现在某张卡编成里**）+ `tests/foe-gun-range.test.ts` 6 例 | `packages/core/src/{combat,state,types}.ts` · `packages/data/src/foe-ships.ts`（FOE_D_STASIS）· D 族设定稿 §十 |
| **受击增程（敌机）** | **2026-09-11 船长：「添加新机制，受到攻击后，大幅提高无人机射程（提高 400%）」**（E 族泰坦巨构专属）：**母舰本体被命中一次** ⇒ **整支敌队**机群射程 **×4**（警戒机 5,000 → **20,000m**），**本场永久**、**不封顶**；**打机群不触发**、未命中不触发。⚠ **只触发一次、对所有敌舰生效**（船长三次裁定：「每个敌人都会单独触发一次射程增加的文字提示，理论上应该**只触发一次**，**对所有敌舰生效**」）⇒ 状态是**标量** `BattleState.foeDroneRangeBuff`，提示**整队只一条**。读法 = 巨构平时只铺 5km 警戒幕，**挨打之后才把长臂伸出来**（族格「依靠无人机攻击炮台射程外的敌人」的兑现）。落码 = `FoeShipDef.droneRangeMulOnHit`（舰级）→ `UnitSpec.foeDroneRangeMulOnHit` → `BattleState.foeDroneRangeBuff`（按敌单位 tag 记倍率）；射程**只有一处算法** `foeDroneRangeOf(b, tag, w)`（开火判定与界面同源）；表现 = **战斗画面提示**（**不写日志**——船长二次裁定「日志内不用显示提示，将该提示放入战斗画面内显示」，与「**敌方增援**」**统一下系统**：同一处提示位、**战场窗口正上方**、限时自动消失）+ 机群阵位沿"我舰→敌舰"外推（`foeDroneStation`，55% / 上限 260px；**该函数是敌机位置的唯一算法**——机体、弹道起飞点、击落爆炸点三处共用）。守卫 = `content:check`「机群与防空契约」⑤（只允许带机群的 E 族舰级、倍率 ≤ 4）+ `tests/foe-drone.test.ts` 五条 | 落点：types.ts（`FoeShipDef.droneRangeMulOnHit`）· state.ts（`foeDroneRangeBuff`）· combat.ts（`foeDroneRangeOf` / `markFoeDroneRangeBuff` / 开火射程门）· data/foe-ships.ts（E 族三条 ×4）· desktop BattleScreen（阵位外推） |
| **敌方混伤（dmgMix）** | 2026-09-10 船长：「给所有赏金任务的敌人添加额外攻击的副伤害类型，让其攻击造成混伤（主类型占大部分），并需要在任务中告知玩家」+「常驻悬赏也改，比例约 8:2」。`AnomalyDef.dmgMix` 从"只用来挑主系"变成**真正的伤害构成**：**正权重键 = 参战系**（≥2 系 = 混伤，缺口归一化），**常驻悬赏 / 低安遇袭 = 主 8 : 副 2（80/20）**、**窝点派生 = 主 6 : 副 4（60/40）**、**教学卡不写 = 纯系**；**E 族（泰坦巨构）为唯一特例 = 50% 动能 + 50% 爆炸**（**常驻卡与窝点派生卡同值**——2026-09-11 船长「**E 族单独调整，包括 E 族赏金任务的伤害比例**」⇒ 逐族份额覆写 `LAIR_SUB_DMG_SHARE_BY_FAMILY.E = 0.5`）；副系按**敌族签名序** `FOE_SUB_DMG` 取第一个 ≠ 主系的系（撞主系顺延）。**敌总伤不变**（只改构成），武器形态/命中/近盲/衰减仍按**主系**（主系是能量仍光束必中）。各系各自吃层位克制与层抗 → **只堆主系抗会被副系穿透**。战斗结算（`applyFoeShot` 逐系）、胜率预估（份额加权）、卡面显示（「敌火力 ⚫动能 60% · ⚫爆炸 40%」，`ui/shipInfo.FoeDamageMix`）三处同源。⚠ 旧口径"没写的系缺省权重 1"作废 | types.ts dmgMix；data/anomalies.ts（25 张卡）；core/lairs.ts FOE_SUB_DMG / LAIR_SUB_DMG_SHARE / lairAnomalyOf；core/combat.ts foeDamageComposition / splitShotByComposition / applyFoeShot；tools/content-check.ts 混伤契约；tests/mixed-damage.test.ts |
| 支援件作用面 | 中/低槽支援件（伤害稳定器 / 射速计算机 / 索敌阵列）**只作用于炮台三族武器**，无人机**不吃**（2026-09-10 船长口径：支援件本就是为炮台设计的，无人机走自己的专属装备——甲板扩展 / 战术导控 / 中继天线）；姿态陀螺（闪避缺口）与盾容/甲容属全船生效，与武器无关。基础舰炮（兜底武器）同样不吃支援件。**新增支援件时默认继承本口径，除非文案特意说明** | combat.ts createPlayerSpec（dmgBonus/reloadDiv/hitEq 仅进炮台分支） |
| 无人机专用舰槽位 | 2026-09-10 船长拍板：**无人机专用舰高槽 −2**——梭鱼级 sh-swarm 5→**3**、王鲭级 sh-sentinel 6→**4**（中/低槽与其余带机巢舰船不动）。旧档由 repair 链「位对齐」把超长位尾件退回装备库（不丢装备、无人机清单保留、幂等）。**代价**：王鲭炮流 6 门→4 门、混装路线收窄（4 炮+甲板+导控 不可行）；**注意**：该改动对"无人机流 vs 炮流"的相对关系是**净利好无人机流**（炮流火力随槽位线性下降、无人机人口来自机群），若要削弱无人机流应动用 CPU / 舱容 / 战术导控 | ships.ts slots；equipment.ts repairDeprecatedModules；tests/drone-ship-slots.test.ts |
| 武器族加成 | 船体级"本族武器"加成（EVE 式族加成；2026-09-09 船长拍板四族巡洋分型）：ShipDef.weaponFamilyBonus 按伤害类型键，只乘入该弹型三族武器（炮台/导弹/激光）单发，装别族武器无加成但仍可用；无人机与基础舰炮豁免。现挂 4 艘巡洋 ×12%（长尾鲨=高爆 / 电鳐=能量 / 锤头·牛鲨=动能） | ShipDef.weaponFamilyBonus；createPlayerSpec |
| 锁定装置 / 集火 | 高槽 target-lock 家族（2026-09-09 船长拍板三档 MK1/2/3 = 受击加深 8/12/20%，多件 EVE 曲线收敛）：装上即进入**集火模式**——本舰全部武器不再随机分散（V18B 口径被装备取代），改打存活编队首位（主舰优先、击毁自动接力）；被锁定目标受本舰伤害 ×(1+加深)；无装备 = 随机目标不变。AI 副船/遭遇/胜率 MC 同源 | ModuleDef.lockDmgBonus；combat.firstAliveFoe |
| 收敛 | V18.1 防超模三类：缺口复合 1−Π(1−x)（抗/闪避）、EVE 曲线（命中/速度）、加算（伤害/射速/容量） | |
| 数字稀有度 | 物品本体隐藏档（2026-09-09 船长拍板：稀有度入物品本体 data `RARITY_TIER` 表，市场/图鉴/掉落统一查）：**1** = 常驻层、**2/3** = 稀有订单层（2 大众 / 3 高阶）、**4** = 奇货层；与供给渠道字符串（common/rare/exotic）**分离**——只驱动稀有订单渠道刷新权重（rare 卖单抽取 + NPC 收购窗；tier3 乘子 = balance.market.rareTier3Weight = **0.15**（2026-09-10 船长定：0.25 → 0.15），模拟校准），**奇货渠道出率与数字不挂钩**；未来加更高档时奇货低稀有度行下滑到 3 由稀有订单层承接；content:check 护栏 = 表键与市场卡全集一致、值 1~4、渠道一致（common→1、rare→2/3、exotic→3/4）。**2026-09-10 另定：档位与价格解耦**（同批 12 件 MK3 装备随"装备价对齐同级武器价"由 2 升 3；日后调价不再自动改档） | data/rarityTier.ts；market.ts rareTierWeight；tools/market-rarity-sim.ts |
| 武器三族 | 动能炮台(turret/kinetic)、导弹架(missile/explosive)、激光(laser/plasma 必中光束) | V18B 形态分家 |
| 远端衰减（falloff） | **2026-09-11 船长定：三族统一语义 = 「最远端倍率」**（近端 ×1 → 最远端 = 该武器 `falloff`，线性、保底 0；动能/爆炸作用于**命中**、能量作用于**威力**）。现值：**我方动能炮台民用/MK1/MK2/MK3 一律 0.5**（原 0.30/0.28）、**我方激光四件一律 0.1**（原 0.30/0.35；同时**废除**旧的「命中衰减 ×0.8」换算，旧远端 ×0.44/×0.48 作废）、**敌方默认 `balance.battle.foeFalloff` 0.5**（原 0.3；逐卡 `foeFalloff` 可覆写）。窝点专属件按**逐件**定（船长 2026-09-11 回调后口径：**D 守墓者长炮 0.45 → 0.6**——比普通 MK3 的 0.5 更准；**A 劫掠者转管炮保持 0.35、E 巨构残骸炮保持 0.1**，后者设计就是"越远越打不中"）。**A/B 实测（3 种子）**：动能流打深渊之门残血 22~36% → **41~64%**、噬口 43s → **30s**；激光流全面变慢（灰霾 9→16s、噬口 25→32s），**穹顶守卫 100% → 67%** | combat.ts beamPowerFactor / distFactor；data/modules.ts falloff；balance.ts foeFalloff；tools/battle-calibrate.ts |
| 弹药键 | ammo.kin/exp/pla ↔ ammo-kinetic-l/explosive-l/plasma-l（动能弹/爆破导弹/能量弹药） | |
| 齐射扣弹（门数） | **2026-09-11 修复（船长：「多个武器开火只消耗一次弹药」）**：同 id 同参炮台/激光在构建期合并为「×N 齐射」**一条**武器条目（避免 UI 弧线爆炸），该条目带 `WeaponSpec.count` = **门数**；**一轮齐射按门数扣弹**（炮台扣 `count` 发本弹型、激光扣 `count` 发能量弹药）。修复前每轮只扣 1 发 → 多门武器等于白嫖弹药。**余弹不足一轮齐射 → 该武器停火**（不发射、余弹留存退还）；**出发预载同步按门数放大**（`ammoLoadTotals` = 每键轮数 × 该键各条目门数之和）。实测（4 卡 × 5 种子中位）：4×动能MK2 一场 **252 发**、5×动能MK3 一场 **130 发** | combat.ts createPlayerSpec / stepBattle / ammoLoadTotals；tests/ammo-volley.test.ts（4 例）；tools/liquidity-audit.ts 消耗品侧 |
| 换装卡的弹伤倍率与克制（2026-09-11 船长补正） | 船长原话：「装配界面更换炮台时，只简略的显示了火力变化，无法看到武器的弹药伤害倍率」。**数据链路**：单发 = **弹 dmg**（动能弹 6 / 爆破导弹 7 / 能量弹药 9；MK2 = 8/9/12）× **模块 `dmgMult`**（炮台档位差主要在此：民用 1.0、轻型 MK1 1.25、重型 MK2 3.73、攻坚 MK3 5.13）× 炮术 × 船体加成；弹种另吃**层位克制**（`typeLayerMult`：动能 盾×1.5/甲×0.5、高爆反之、能量 盾×1.25 其余×1）。**界面落点**：换装浮层每张武器卡在弹种 chip 旁补一枚**克制 chip**（复用 `DmgChip`，标签 = `layerShortOf()` 如「盾×1.5·甲×0.5」、悬停给三层全串），文本行补 **`弹伤 ×N`**；换装**对比段**（`diffSegs`）在原有「火力 ±%」之外，再点明 **`弹伤 ×旧→×新`** 与 **`弹种 旧→新`**（船长要看的正是这两项） | panels/FitPage.tsx（`layerShortOf` / `mulText` / `weaponDamageTypeOf` / `diffSegs(weapon)`）；ui/shipInfo.tsx DmgChip；core/combat.ts typeLayerMult |
| 弹药 MK2 / 弹药档位 | 三族高级弹（2026-09-09 船长拍板）：动能弹 MK2/爆破导弹 MK2/能量弹药 MK2（dmg 8/9/12 vs 基础 6/7/9，克制表同基础；市场 45/60/80；**蓝图书奇货**（exotic，船长追加拍板 rare→exotic；**书价维持 2026-09-09「补给线 ×1.5」批的落账原值 9k/12.75k/18k**——2026-09-11 船长复核「弹药蓝图回滚到 1.5 倍，其他保持不变」⇒ 弹药 6 张书价**不随档位系数漂移**，逐条登记在 `BLUEPRINT_PRICE_OVERRIDES`））。**出战前选档**：装配页按弹族设档（FleetShip.ammoPref，旧档缺省 = 基础弹），开战预载按档消耗、配置档库存不足整族回退基础弹 + 日志；连打/离线与手动同源——玩家配置什么档就消耗什么档；战斗内不换弹（battle.ammoIds 记实装 id，退还按 id 原样退回） | items.ts AMMO；combat.loadAmmoTier/ammoIdFor；docs/design/ammo-mk2-20260909.md |
| 装备/模块库 | moduleBay（装备库）与市场 goods；蓝图 = BlueprintDef（市场现货 + 制造门槛） | |
| C2 成品价规则 | 市场现货 ≥ 材料成本（蓝图不计；每次制造费 2026-09-08 船长拍板取消）；现货阶梯常驻 ×1.15 / 稀有 MK2 ×1.4 / MK3 ×1.7；自制 ≈ 现货 0.17~0.61×（材料计，2026-09-08 重估） | 掉落入市须守此护栏 |
| 产出倍率（精炼） | 精炼炉的玩家口径产出系数（**旧称「收率」，已全量废止**）：基础 **120%** + 精炼学 +6%/级 + 高级回收处理 +3%/级，满级合计 **165%**（2026-09-08 工业工位收益体检再定：无技能净率由≈持平抬到 **≈+20%**；技能本身每级加成在原值上下调约 20%（原 +8%/+4%））；每批产物 = floor(批 × perOre × 倍率) | balance.ts refining.baseRate=1.2、ratePerLevel 0.06 / secondRatePerLevel 0.03 |
| 制造技能加成（收益体检） | 2026-09-08 船长定（组装机同精炼口径，技能本身加成下调约 20%，就近取整）：工业理论/批量生产学（制造时间）−5%/−4% → **−4%/−3%** 每级；材料学/组件标准化（材料消耗）−2%/−1% → **−1.5%/−0.8%** 每级；产线节拍学（周期）不变 | balance.manufacturing.timePerLevel 0.04；manufacturing.ts 常量 |
| 工业工位（收益口径） | 2026-09-08 工业收益体检口径：精炼炉/回收炉/组装机每占用 1 工位（手动位或 1 枚 AI 核心）按「卖站净得 ISK/h」「周转收益率 = 净/h ÷ 材料投入/h（无技能，下限 ≥20%）」两把尺评估；组装机 7 行低周转只升不砍调至净率≥20%；舰船/修理组件等高收益行保留（稀缺工位/长周期/吸收流约束）。**2026-09-10 补锚**：组装机配方再加一条「**料/价 = 材料成本 ÷ 成品现货价**」尺，同族锚 = **45%**（MK1/民用/弹药/修理组件实测 44.8~51%）——同日"装备价对齐武器价"后 46 件掉到 3.0~18.3%（毛利 446~2625%），已按锚补齐料耗（46 张非武器件，料/价回 44.8~45.4%、毛利回 ≈123%）；武器族 33~42% 属族内一致带不动；**舰船蓝图 24 张同批处理**（23 张 22.0% → 44.3~45.2%、毛利回 121~122%；`sbp-colossal` 61% 高于锚保持不动） | docs/design/industry-ai-slot-expansion.md；docs/design/manufacture-refine-worker-econ.md §六；tools/manufacture-econ.ts（含「料/价 %」列） |
| 精炼配平护栏 | 14 条配方按「无技能（倍率 100%）每批产值 ≈ 每批耗料价值（双方站内收价）」标定——零技能不吃亏（实际净 +0.4%~+4.6%），满技能（160%）净 ≈ +60%~+79%（2026-09-08 定稿；C2 旧保底「100% 收率 ≥ ×1.11~1.35」作废） | tools/content-check.ts 护栏 98%~108% |
| 站内让利吸收 | 卖单挂价低于收购价线时，站内按"每窗吸收额（F×折价倍率，封顶 ×5）"保底消化余量：先吃簿、不够才站内补差、不叠加（2026-09-08 船长定；文档 market-sell-absorb.md） | 日志"让利售出" |
| 吸收额 F | 卖出侧每窗保底件数：池商品 = supplyFlow、common 单件 = 1、rare = 0.3、奇货 = 0.1（件/窗）。**2026-09-10 船长定：池商品的 `supplyFlow` 按玩家产能标定**——基准 = 掘洞级 + 满采矿技能 + 2×强化采集器 MK1（采矿侧）／主控精炼炉满技能（矿物侧），覆盖比按单价分层 **≤20 → ×15 ／ ≤200 → ×6 ／ ≤400 → ×3 ／ >400 → ×2**（保留"越低阶需求越大"，最低档仍 >1 = 卖得掉）；改前 17 个矿带里 15 个覆盖比 <1（气冰类仅 0.02×）。文档 `docs/design/market-pool-liquidity-20260910.md`；工具 `npm run liquidity:audit` | 折 1% 提速 40%、折 10% 封顶 |
| 收购档位 | NPC 收玩家货的价线 = 档位 × L（2026-09-08 船长定）：原料池 1.0L / 池耗材（弹药·修理件·无人机）0.6L / common 单件 0.6L / rare 0.65L / 奇货 1.0L（全价回收）；供应价 ≈1.0~1.06L 恒高于收购 → 倒卖亏税防套利 | demandMultiplier = 档位值；文档 market-acquisition-rework.md |
| 巡游抢单 | 越线挂单每 60s 窗小概率成交：卖单挂价 > 收购价线 → 30%·e^(−6r)；买单挂价 < 供应价线 → 20%·e^(−14s)（r/s = 相对价线偏移比例）；**单次件数随贴线度放大且与池订单生成量挂钩（2026-09-08 方案 B）：池商品贴线 ≤1% = round(0.5×supplyFlow)、≤3% = 0.25×、≤8% = 0.1×、更远 1 件（例：氖云气 1,899/窗 —— 2026-09-10 按产能标定后 —— → 贴线 ~950 件；矿石大池自动放大）；单件商品 5/3/2/1**——概率与数量双重"越接近价线惩罚越小"，但任何档位仍远慢于平价簿+让利吸收（价线纪律保留）；买侧维持 1 件/窗 | 日志"巡游采购/巡游供货" |
| 市场蓝图出现率 | 蓝图现货供给的稀有度权重：蓝图书走稀有渠道、`blueprintWeight = 0.05`（原 50% → 5%）、单张寿命 6 小时；碎片门槛同日下调为 **MK2 25 / MK3 250 片**（见「蓝图碎片阈值」）。**2026-09-10 已落地**（原「待实施」标注作废） | market.ts；docs/design/blueprint-rate-20260910.md |

## 四、B3 残骸 / 打捞 / 回收（2026-09-05 逐步定稿，状态见每行）

| 术语 | 定义 | 状态 |
|---|---|---|
| 残骸密度 | 每星系一个标量（当前值 vs base），击杀注入、打捞消耗、闲置漂移。**2026-09-10 船长拍板改口径**：①**基础密度 base = 该星系全部可见悬赏「完成 20 次」的注入量之和**（两卡求和；无可见卡回退旧安全等级曲线 10~40 兜底）——实测 58~1536；②击杀注入 Δ = **威胁 ×0.4 × (1 + 0.2×敌人数)**（敌人数 = 主舰+僚机+多波全部单位；单敌 ×1.2 / 三敌 ×1.6 / 五敌 ×2.0）；③低安遇袭注入 = 该星系**最强悬赏卡**注入量 ×0.5；④回收档位阈值**等比上调**：险 428 / 危 642（系数 21.4 = 新/旧 base 均值比）——5 张卡随之变档（红环两卡 险→危、烬火 危→常、天底/裂谷 危→险、碎晶带 险→常），其特色池已按新档位重校（content-check 21 张全绿）；⑤**保底线 5 → 10**（≤10 不扣密度、进入保底稳态；mul 分母仍为 10 → 稳态系数 = 1.0，船长定"不做修改"）；⑥**自然恢复速度大幅下调：回升 4h → 96h 回满**（>base 衰减仍 48h） | salvage.ts；b3-salvage.md |
| 基础密度（base） | `round(10 + 15×(1 − security))` ∈ [10,40]；新手 10 / 最危险 40；只与危险度挂钩 | **已作废（2026-09-10 改口径：base = 该星系可见悬赏「完成 20 次」注入量之和，见上行「残骸密度」①；旧曲线仅作无可见卡时的兜底）** |
| 保底点 | 全图固定 **5**：密度 ≤5 打捞不扣密度、进入保底稳态（不随 base 浮动） | **已作废（2026-09-10：保底线改 10，见上行⑤）** |
| 击杀注入 | 该星系战役胜利（主控/AI）`Δ = threat×0.4`，**无上限** | **已作废（2026-09-10：改 `threat×0.4×(1+0.2×敌人数)`，见上行②）** |
| 漂移 | 星系**无打捞进行中**才结算：>base 衰减 48h 线性回 base；<base 回升 4h 线性回 base（离线照算） | **已作废（2026-09-10：<base 回升改 **96h** 回满，>base 衰减仍 48h，见上行⑥）** |
| 保底稳态产出 | 密度 ≤**10**（`WRECK_FLOOR`）时每轮**不扣密度**、仍产 1 份（体积当量 `mul = max(0.5, 密度/10)`，故密度 = 10 时 mul = 1.0 = **满效**）——可持续保底捞 | **2026-09-10 定**（原「≤5 = 满效的一半」随保底线 5→10 作废） |
| 打捞器 | 高槽新族（无伤害，与炮/矿枪/无人机件争槽）；MK1/2/3；**CPU 2/6/15**；**周期 10s/8s/6s**，每台每轮结算一次捞取；升级 = 减周期不增产 | 定稿 |
| 残骸记账 | **计数 = 体积（乙案，2026-09-05 船长拍板）**：残骸 item unitM3 = 1，数量即 m³；型号（按敌群）只决定回收池/低安/碎片档 | 定稿 |
| 捞取体积 | 单轮捞取 m³ = 该型号单份体积（威胁×0.06） × max(0.5, 密度/10)——密度 5 = ×0.5（半效保底）、10 = ×1、越高越肥；每轮放干扣减（超出的 2%），≤5 停扣 | 定稿 |
| 残骸型号 | 按**敌群（悬赏卡/低安遭遇群）注册**，每卡/群一种；体积量级默认随威胁自动派生（威胁×0.06），可覆盖；回收产出 ∝ 体积 × 型号系数（不成 1:1） | 定稿 |
| 满仓自动返港 | 打捞与采矿同构：满仓自动返航 → 到港卸入仓库（自动循环默认开 → 同星系自动续捞） | 定稿（2026-09-09 更新） |
| 打捞自动循环 | 2026-09-09 船长定（玩家反馈"打捞只一趟"）：主控与 AI 打捞均采矿式自动循环——满舱返航卸货后同星系自动续捞，直到手动停止/取消；主控可勾「本次返航卸货后停止」做单趟（autoCycle/stopAfterTrip，默认开、零迁移） | 定稿 2026-09-09 |
| 回收 = 开箱 | 精炼炉新增“残骸回收”批；**批 = 10 m³ / 25s**（劳动者 100%；残骸计数即体积）= 保底矿物（确定性）+ 彩头（概率） | 定稿方向 |
| 彩头分层（**内部叫法**） | ①基础常驻件直出（无蓝图门槛的 MK1 系民用件）；②低安（sec<0）残骸箱低概率 MK2；③蓝图碎片（高级件不直出，集齐逆向解锁蓝图；直出件不需要）。**⚠ 玩家可见文案禁用「彩头」「主题件」「掉池」等内部叫法**（2026-09-11 玩家反馈"事件日志里看到不符合游戏设定的名词"）——日志/卡面一律写 **「额外掉落」/「特色掉落」/「特色装备」/「常规掉落」**（「保底矿物」是船长已批准的玩家用词，可继续用） | 定稿方向；玩家用词见「特色掉落」条 |
| 蓝图碎片阈值 | **2026-09-10 船长定（下调）**：MK2 蓝图 = **25 片** / MK3 = **250 片**（原 100 / 1000；异星原型 10000 片仍为预留） | 定稿 |
| 碎片集齐前不重复 | **2026-09-10 船长定**：碎片命中后从**该档还没拿到蓝图的书**里等概率抽 1 张，**已学会（含市场买书）或碎片已集齐门槛的模块移出池子**——重复碎片是死库存（碎片不可出售、无市场卡）。三张书全部到手后该档池为空、不再出碎片。效果：凑齐该档三张书恰好 3×门槛片（MK2 75 片 / MK3 750 片），尾部消失（原"纯随机"要 +17%/+5.7% 的集邮损耗）。回收开箱与完好舰体彩头共用同一池 | `salvage.fragmentPoolOf(state, ctx, tier)`；tests/recycle.test.ts「碎片池」4 项 |
| 保底矿物（残骸回收） | 回收卡按**该敌群矿池**逐项列出的必得矿物（**2026-09-10 船长定：仿精炼卡格式**——`♨ 保底矿物：` 标题 + 每矿物缩进一行「矿物名 ×约 N（仓库 M）」，M 与精炼卡同源）。每批只按池权重出**一种**、产量 = 批体积 × 档位单方产量 ×(1+8%×残骸提纯学)×池权重占比（±10% 抖动）；矿池取引擎单点 `recycleMineralPoolOf(profile)`（敌群特色池优先、缺省回落档位基础池），界面不复写回落逻辑。原「产出倾向」一句话（`recycleNote`）**降为矿物块悬停说明**（星图打捞页仍作独立行显示） | salvage.ts recycleMineralPoolOf / RECYCLE_POOLS / RECYCLE_YIELD_PER_M3；ui/wreckFlavor.tsx mineralRowsOf |
| 产出倾向（B3.1） | 敌群主题矿物侧重一句话（`recycleNote`）；背后 = 该卡按同档 2~3 主矿求解的权重池。**2026-09-10 起不再作工业页回收卡的独立行**（改由「保底矿物」块承载，原文进该块悬停）；星图「残骸打捞」星系卡仍保留独立行 | 定稿 2026-09-08（2026-09-10 改展示位） |
| 特色掉落（B3.1；原称「低出率掉落」，2026-09-10 船长改名） | 回收卡/打捞页星系卡的彩头清单，**2026-09-10 船长定"说明精简"后只讲特色**：**具名** = 该敌群主题追加件（中安 `modules` 组 / 低安 `mk2` 组）；**稀有残骸只介绍族专属装备**（船长同日追加：不列主题件、不列系列装备与蓝图碎片，整行最强强调色 + 加粗）；**泛化不具名**（仅普通残骸） = 通用 MK2 池「另有 MK2 系列装备」（低安）、直出基础池「另有民用与 MK1 系列装备」、蓝图碎片「高威胁另有蓝图碎片（MK2 集 25 片 / MK3 集 250 片）」（威胁 ≥17 / ≥41，与引擎同源）。**卡面配色**（船长：「特色掉落的内容也换个更显眼的颜色」）：具名特色件用 `.app-belt-feat`（最强强调色 `#ffe08a`，与公告标题/战报标题同族），泛化兜底内嵌 `.app-dim` 保持次级灰；无具名特色件时标签退为「其他掉落」。背后机制不变：主题件在默认池上"追加"（默认池一件不少）；武器不入主题，唯一白名单 = 穹顶守卫三把 MK3 武器直出 | ui/wreckFlavor.tsx recycleFeatureOf / FlavorTip；styles.css .app-belt-feat；salvageFlavors.ts；content-check 主题件/区划白名单断言 |
| 特色池乘数 m（B3.1） | 保底目标 = m = mSec(1+0.45×max(0,−sec)，≤1.45) × mThreat(1+0.004×威胁，≤1.30)，顶格 ≈1.89（≈155k ISK/h）；content-check 断言池均价 ÷ 档基数 ∈ m ±3% | 定稿 2026-09-08 |
| 稀有残骸 | **2026-09-10 船长定：已实现并已解禁**（同日追加：二号五族专属装备齐备后开放回收炉）——赏金任务（敌人窝点）战利品。打赢窝点 → 该星系残骸场按敌群记账留下稀有残骸 ×档位件数（外围 1 / 核心 2 / 深层 3），单件 30 m³；打捞时**必捞**（捞完为止，再回常规池）；回站精炼炉「残骸回收」当**高级箱**开：常规保底照有，**另必给**额外掉落（先掷该敌群专属装备 → 未出则给主题件，再附一批高阶矿物）。**一件 = 一箱（2026-09-11 船长定「甲：起炉即预占」修复）**：起炉时把这 1 件（30 m³）**从货仓/仓库扣出**进本炉私有料账 `claimedUnits`，每批从私有账扣、停炉把未用完的退回物品仓库；**开箱资格 = 该型残骸"未开箱存量 ≥ 一件"**（未开箱存量 = 总存量 − `state.rareOpenedUnits`），开箱后本件记入该账本（退还的余料仍能炼矿物、**不再产箱**）。两道锁合起来：一件残骸**只能开一箱**——多台 AI 并行拿不到同一件（后开的起炉直接被拒）、"跑一批就停再起"也刷不动（修前 1 件最多被 3 台炉各开 1 箱）。**一炉一箱**（按炉结算、不按件累积）语义保留。命中率外围 **5%** / 核心 **8%** / 深层 **10%**。**无市场卡**：唯一变现 = 开箱（`wreck-rare-<悬赏 id>`；rareBy 归族记账）。**炉内料账对玩家可见（2026-09-11 玩家反馈「稀有残骸空了精炼炉还在运转」后补）**：预占的这批料已从货仓/仓库扣走、玩家看到的存量会立刻变 0，故卡面与活动栏一律**改读料账**——工业页残骸卡「运转 N 台 · 合计余 X m³（其中炉内料账 Y m³、货仓/仓库 Z m³）」+ 运转名册行「本炉料余 Z m³」、顶部活动栏「本炉料余 ×Z」；**料账吃完的那一批当场收工**（不再空转一个批周期），停炉提示写明"未用完的预占料退回物品仓库"。**落盘口径**：`claimedUnits` / `lockUnits` / `rareBoxEligible` **一律随档保留（含 0 与 false）**——"字段在不在"本身是语义（有字段 = 该炉吃私有料账；缺失 = 老档/公共料），丢掉归零字段会让炉子转吃公共库存、丢掉 `false` 会让已开箱的余料再开一箱。**界面落点（2026-09-11 船长：「稀有残骸能否在星图的星系详细里看到？」）**：战果**两处同源**、共用 `pages/common.rareWreckRefsOf`（口径 = 数量 + 来源窝点名，如「稀有残骸 ×5（噬口猎杀令 ×3、穹顶守卫 ×2）」）——① **星图「星系行动」弹窗**（＝星系详细；`app-map-taskline.is-rare`，未探索星系不显示、无战果不占位）② 「残骸打捞」星系卡的 `app-chip.is-rare` 徽标（悬浮给"打捞必得 / 每件 30 m³ / 回站开高级箱"） | 已实现（salvage.ts / lairs.ts / industry.ts / activity.ts / save.ts；content-check 窝点契约；tests/recycle.test.ts「一件 = 一箱」7 例，含料账收工时机与读档往返 3 例；界面同源口径 = `pages/common.ts` `rareWreckRefsOf`） |
| 小剧场残骸层 | BattleScreen 被击毁单位的视觉残骸（爆炸淡出），**不是** B3 的打捞残骸 | 区分！ |

## 五、全局数值速查（已定稿锚点）

| 数值 | 值 | 出处 |
|---|---|---|
| foeSpeedTacticMul | brawl 1.28 / orbit 1.0 / kite 0.72 | balance.ts |
| foeHpCurve | D = 5+85×((T−6)/90)^1.6；foeRefFire 2.1/9.7/12.8 | balance.ts |
| 敌人近盲伤害 | blindDmgMul 缺省 0.3 | types.ts/combat.ts |
| 支援件回调 | **不执行** | C4 第二批收口 |
| 威胁标注 | B 方案（叙事标注 + 胜率%真值；副船下拉按其装配显示成功率） | b1e2e55 |
| AI 副船远征门槛 | aiWinPreview ≥ 0.8 | ai.ts |
| 富矿脉（红利窗口） | 基础 **3%/分钟** × 富矿勘探学（1+0.2×级，满级 ×2）；命中 = **连续 2 循环 ×3**（含触发当轮）；窗口绑定矿带（换带/停止清零，自动循环返航卸货回原带保留）、窗口内不掷点、只在触发写日志；主控与 AI 每船独立计数（rvLeft，旧档缺失按 0 零迁移） | 卷B2⑥（2026-09-08 船长定稿）；balance.richVeinChance |
| 完好舰体（当场直发） | 打捞彩头事件：命中率按**打捞分钟**语义（balance.intactHullRatePerMin 0.08%/分钟 × 残骸富集识别学 1.2/级）；命中 = 不再翻体积，**当场直发**该敌群回收彩头（基础件必中 1 件·主题优先 → 装备库；低安按 intactMk2Chance 掷 MK2 档；威胁 ≥17/≥41 掷蓝图碎片）——主控/AI 同源，产物不占货仓 | 卷B3⑨（2026-09-08 船长定稿）；salvage.ts rollIntactHullLoot |
| AI 核心调度学 | 首个 **rank5** 技能（工程组）：核心驱动的全部作业效率在核心档位之上**每级 +2 个百分点累加**（如基础核心 40% → 满级 50%；伽马 60%/贝塔 70%/阿尔法 85%，封顶 100%）——副船采矿/打捞/掩护与站内精炼炉/回收炉/制造线共源；返航腿不 ÷eff 故不受影响 | 卷B3⑩（2026-09-08 船长定稿）；ai-core-dispatch |
| AI 核心操作学 | AI 核心启用**共用上限**技能（2026-09-08 船长定：由「人工智能专家」更名并降 rank4→**rank2** 入门向）：每级 +1 枚可同时启用的 AI 核心（AI 副船任务与站内精炼炉/回收炉/制造线共用；Lv0 = 0 不可启用） | ai-expert（id 不变，仅更名/降档） |
| 工业自动化（工业扩容） | rank4 新技能（2026-09-08 船长定，命名承接原技能名）：AI 核心驱动的站内精炼炉/回收炉/制造线在共用上限之外**每级 +2 枚工业专用工位**（满级 +10；**仅对站内产业生效，不增加 AI 副船任务上限**；每个工位仍占用一枚实体 AI 核心）。**2026-09-11 起系数由平衡表逐技能给出**（见下条），与「工业自动化基础」叠加 | industrial-ai-cap；ai.ts industryAiBonus / aiCoreCapBlock(scope)；balance aiCore.industrySkillSlots |
| 工业自动化基础（工业扩容） | **rank3 新技能（2026-09-11 船长定：「添加一个 RANK3 的工业自动化下级技能，满级 +5 AI 工作位」）**：工业自动化的**基础课程**（同组低阶阶梯，**不做硬前置**——技能系统本无前置字段），AI 核心驱动的站内精炼炉/回收炉/制造线在共用上限之外**每级 +1 枚工业专用工位**（满级 +5；同样**仅站内生效、不加副船上限**、每个工位仍占一枚实体核心）。**满技能站内工位 = 共用 5（AI 核心操作学）+ 工业自动化 10 + 本技能 5 = 20**；平衡表 `aiCore.industrySkillSlots`（技能 id → 每级工位数）为该族唯一入口，新增扩容技能只加一行，`content:check` 有「AI 扩容技能契约」双向护栏（表里的 id 必须真实存在；说明里承诺工业专用工位的技能必须在表里） | industrial-ai-cap-basic；ai.ts industryAiBonus；aiSlots.tsx 逐技能构成提示 |
| 产线节拍学 | 原「工业自动化」（id industrial-automation 不变）2026-09-08 更名以符合效果：精炼炉与组装机的作业周期每级 −5%（满级 −25%，手动与 AI 核心驱动同享） | skills rank3 |
| 淘货折扣（二手市场学） | 市场**稀有**商品供给单价格每级 −2%（满级 −10%）；闸内 ×4 走私价同乘；**奇货不参与**（收购 1.0L 全价档 → 防"折价买入→全价回购"套利） | 卷B3⑪（2026-09-08 船长定稿）；market.ts secondhandMul |
| 残骸（市场出售） | 残骸可到市场出售（2026-09-08 船长定）：22 张悬赏残骸**收购卡**、**只收不卖**（playerBuyable=false，防"低价买残骸→拆解"套利）；收价按回收档 = 常 30 / 险 40 / 危 **50 ISK/m³**——**严格低于无技能拆解保底（三档齐平 ≈57/m³）**，任何档直接卖都不如拆解（锚不击穿；对"该档典型特色回收"约 50%~56%）；市场过滤「残骸」独立分类；拆解（彩头/碎片/技能乘区）仍更赚，卖站 = 折价清仓通道（原 45/60 为 79%/105% 会击穿最低锚，2026-09-08 当日修正并加 content-check 护栏） | marketCatalog WRECK_BUY_GOODS（收价 = basePrice，档由星系安全等级推基础密度；content-check 锚断言） |

## 六、UI 层级术语（2026-09-08 定）

| 术语 | 含义 | 出处/规则 |
|---|---|---|
| 一级页 | 导航栏直系页 = 星图 / 舰船 / 装配 / 物品 / 市场 / 工业 / 技能（App NAV_ITEMS）；**整页无滚动条、高度 = 主窗口内容区高** | conventions 第九章「一级页不滚」 |
| 二级窗口 | 一级页内的功能窗/标签页内容区（如 星图·远征/矿带、工业·精炼炉/组装机、市场左列/详情右栏、物品·仓库/货仓 等）；**滚动条只出现在这些内部窗口**（overflow-y:auto + min-height:0），样式复用既有列表/网格族 | 同上 |
| 一级不滚前提 | 一级页内容必须能在主窗口内**完整装下**才去掉页滚动；放不下且无法拆分进二级窗口的页面 → 单独提出与船长讨论方案，禁止硬裁内容 | 同上（红线） |

## 七、副空间站与建站交付（2026-09-08 定稿）

| 术语 | 定义 | 出处/规则 |
|---|---|---|
| 工地现场 | 建站点所在星系的野外停留即"现场"：可提交建材（交付前置 = playerAtSite——停靠该站或野外停留于站点星系） | station.ts playerAtSite |
| 未建成不视为任何站点 | 2026-09-08 船长定（彻底收口）：副站完成全部档位（建成）前**不提供停靠/泊位/卸货/维修/换驾驶等任何站内功能**——引擎停靠落点只认已建成站；advanceGame 逐 tick `reconcileDockSanity` 把旧档残留的未建成/未知停靠纠正为"工地现场野外停留"（幂等、只记一次日志）；维修/换驾驶对未建成停靠一律拒办 | location.ts reconcileDockSanity；stationSites stage ≥ tiers.length；shipyard.ts |
| 建站交付航线 | 2026-09-08 船长定（同日 v2 修订：物理载货模型）：「前往工地交付」指令（星图该星系行动区 + 建站任务卡双入口）——出发时把货仓装满建材（仓库按名单顺序装载，上限 = min(空闲 m³, 全站剩余需求)，保留船上原货），按**真实航程**前往工地星系（transit.delivery.phase = to-site，loaded 携带本趟装载明细）；到点**只清空本趟装载**（cargoOnly：不补扣仓库，白跑场景结构性消除）并**自动返航**（to-station）；仓库还有建材就**自动续趟**，直到①全部档位建成（就地停靠新站）或②仓库建材耗尽（终止：事件日志 + 一次性弹窗提示）；**随时可取消**（= 停止整个循环，无惩罚、立即返航停靠最近已建成站、本趟装载随进港卸回仓库） | location.ts startSiteDeliverTrip / cancelSiteDeliverTrip / arriveDeliverSite / continueDeliverLoop；ShipTransitState.delivery.loaded |
| 成交静默 | 2026-09-08 船长定：站内让利吸收与巡游抢单的成交日志及市场 UI 提示全部与普通簿面成交同模板（「挂单成交/挂单买入成交」），**不向玩家披露让利/巡游通道**（机制与价线纪律保留，仅玩家可见层静默） | market.ts settleStationTake/settleSnatch*；MarketPage 价格指引 |
| 承伤口径单点（`hullDamage.ts`：按敌火扣装甲/结构） | 2026-09-11 船长先定低安遇袭、随后「**关于战斗中撤退…希望能将其应用到战斗中撤退**」⇒ 同一套算法抽出单点 `core/hullDamage.ts`，两个使用方共用：① **低安遇袭受损档**（一口 = 敌群火力 × `encounter.hitFirepowerSec` = **0.3 秒**）；② **战斗撤退/自动撤退/超时判负**（一口 = 敌群火力 × `combat.retreatHitFirepowerSec` = **1 秒**，船长定「三档同一 K」、钱包维修费不变）。口径 = **敌群火力（威胁 × `battle.foeDpsPerThreat` 0.8）× 秒数**，**先扣装甲、吸完再进结构**，层满值走 `hullLayerCaps`（与维修同尺），**结构底线 5%（绝不弃船）**；导出 `foeFirepowerDps` / `firepowerHitHp` / `applyArmorFirstDamage` / `hitDamageText` / `HULL_FLOOR_FRAC`。**撤退旧口径作废**：「轻损 = 失利扣损骰 ×0.5 = 结构 −7.5%~15%（与敌人强弱无关、装甲不动）」。战败（远征失利 / AI 副船失利 / 遭遇判负附加档）仍走各自既有口径 | core/hullDamage.ts；core/encounters.ts；core/expedition.ts settleBattleRetreat；core/balance.ts `retreatHitFirepowerSec`；tests/retreat.test.ts（5 例） |
| 冲击动量（倾销惩罚 · 砸得越狠买家越多） | **2026-09-11 船长三裁一追加**：①「**提高倾销惩罚**」⇒ 每次触发 **−5% → −8% → −10%**（`shockPerTrigger`；衰减半程仍 6 分钟）；②「**砸得越狠越卖来买的人越多**」＋「每层提高 8% 买单数量」「所有商品都适用」⇒ **每层未衰减惩罚把 NPC 收购单量 +8%**（`dumpBuyVolumePerLayer`）；③「**移除压力项**」→ 追问后定「**甲：只从收购量里移除**」⇒ **池商品收购阶梯量不再乘库存压力**（`qty = flow × 阶梯系数 × 补偿倍率`），**价格仍保留压力项**（淤积压价/缺货涨价照旧，V9 那条规则继续成立）；④追加「**价格钳制下限下调到 0.1**」（`minPriceRatio` 0.2 → 0.1）＋「**砸盘时挂卖单的量同步削减**」⇒ **每层未衰减惩罚把 NPC 供应单量 −8%**（`dumpSellVolumePerLayer`，下限 10%，只作用于池商品供应阶梯）。**口径**：层数 = `\|shock\| ÷ 每层幅度`（**不取整**，随衰减平滑回落）；**两侧倍率都只在砸盘方向生效**（`shock < 0`；玩家买入顶价时不动）。**实测（真引擎 40 窗连续倾销 ore-veldspar，基准 12 ISK、阈值 4,992/窗）**：温和倾销（池量 1.20× 目标、shock −0.805）⇒ 均衡价 **2 ISK = 0.167×**（旧口径 0.333×；已跌破旧 0.2× 地板 ⇒ 地板下调生效）、本窗新铺**收购单 ×1.64**、**供应单 ×0.43**；巨量倾销（池量 2.07× 目标、shock −0.701）⇒ 价格同 0.167×、**收购单 ×1.56**、供应单 ×0.66 —— **深砸不再把买家压少**（改前深砸会掉到 ×0.80）。停手后仍 6 分钟半衰、30 分钟回基准 | core/market.ts（`dumpBuyVolumeMul` / `dumpSellVolumeMul` / `priceLevel` / `refreshGoodOrders` / `seedCommonBook`）；core/balance.ts `shockPerTrigger` / `dumpBuyVolumePerLayer` / `dumpSellVolumePerLayer` / `minPriceRatio`；core/types.ts MarketBalance；docs/design/v9-market.md §二；tests/market-dump-penalty.test.ts（5 例）+ market.test.ts 衰减窗口同步 |
| **价格权威关系（真源 = `marketCatalog`）** | **2026-09-12 审计 A2（船长裁定「丁：只写文档，不改护栏」）**——**行情真源 = `packages/data/src/marketCatalog.ts` 的 `basePrice`（272 处）**：市场买卖、工业估值、事件定价都读它。**其余价格字段与它的关系（逐条登记）**：① `blueprints.ts priceIsk`（94 处，书价）**必须 = 市场蓝图行 `basePrice`** ⇒ ✅ **硬契约**；② `ships.ts priceIsk`（25 处）**必须 = 市场舰船行 `basePrice`**（定制船必须 0 = 仅可造）⇒ ⚠ **仅预警、不阻断**（2026-09-11 船长「3 改为预警」）；③ `shipBlueprints.ts priceIsk`（24 处）= 市场价 × **档位系数**（系数单点见 `BLUEPRINT_PRICE_*`）⇒ ⚠ **仅预警**；④ **`items.ts baseSellPriceIsk`（35 处）是另一个概念**（基础卖价/保底，进采掘结算 · 打捞定价 · 工业估值共 14 个读取点）——**与行情锚本就允许不等**，且**目前没有任何对账**；⑤ `content-csv/*.csv`（items / market / ships 三张表）是**事实上的第三份**（工作台导出/导入往返 ⇒ **导入后必须跑 `content:check`**）。**基线读数（2026-09-12 实测，供日后判断是否漂移）**：物品基础卖价 ÷ 行情锚 = 最小 **0.857**（动能弹 6/7）· 中位 **1.000** · 最大 **1.083**（富凡晶石 13/12）· **28/34 件恰好 1.000** · 越出 `[0.6, 1.6]` **0 件**。**本次未做（船长选定）**：不升级为阻断 · 不加比例带契约 · 不改单一真源派生 | 真源 `data/marketCatalog.ts`；副本 `data/{blueprints,ships,shipBlueprints,items}.ts`；契约 = `content:check`「舰船价格口径（预警）」「蓝图价格口径」「残骸卡价格」；设计稿 `v9-market.md` §十二 |
| 挂买单预扣（`PlayerOrder.escrowIsk`） | **2026-09-11 船长追问「当玩家挂单购买时，是否有冻结玩家金钱？」→ 裁决「甲：改成 EVE 式预扣冻结」**。**改前口径（已作废）**：买单**不冻结**——`placeBuyOrder` 只在成交时扣钱，钱不够就"挂着等"（`settleBuy` 的 `wallet.isk < value` 早退）⇒ 可**零资金挂满全市场**，且**玩家挂单不过期**（`expiresAtGameMs` 只对 NPC 单生效）＝对未来供给的免费期权；卖侧则**一直冻结货物**（`escrowItems`/`escrowShips`）。**现行口径**：挂单即从钱包扣下 `挂价 × 实际挂量`（记在订单 `escrowIsk` 上）——**余额不足按余额缩量**、连 1 件都挂不起则拒绝（`buyOrderBlockedReason` 给出「ISK 不足：挂 1 件需预扣 X，钱包 Y」）；成交（`settleBuy`／越线抢单 `settleSnatchBuy`）**从预扣里核销**，挂价高于实际成交价时**价差退回钱包**；**撤单全额退回**（界面回执写明「预扣 X ISK 已退回钱包」）；成交完毕若有残留预扣一并退回（防"单子移出挂单表后钱失踪"）。**旧档零迁移**：`escrowIsk` 缺省 0 ＝改动前挂的**遗留单**，仍按旧口径（成交时看钱包），不追溯补扣。界面：挂单回执带「（已预扣 X ISK，撤单退回）」，「我的挂单」行显示「已预扣 X ISK」 | core/market.ts（`placeBuyOrder` / `buyOrderBlockedReason` / `settleBuy` / `settleSnatchBuy` / `cancelOrder`）；core/state.ts `PlayerOrder.escrowIsk`；core/save.ts 订单归一化；renderer MarketPage（挂单回执 / 挂单行 / 撤单回执）；tests/market-buy-escrow.test.ts（7 例）+ market-cross.test.ts 口径同步 |
| 市价买入失败四类原因（`buyAtMarket.blocked`） | **2026-09-11 船长实测反馈修复**：「市场订单有货时点市价买入，报『供应簿只剩 0 件』、无法购买」——根因是 core 里三条**静默**分支（钱包不够 `break` / 暗市闸跳单 `continue` / 只收不卖早退）都表现为 `bought = 0`，而界面把 `bought < qty` **一律**说成"没现货"。实测影响面：钱包不足时 **104/141** 个"有供应"商品会这样误报、声望不足时 3 个 MK3 会这样误报。**现行口径**：`buyAtMarket` 返回 `blocked ∈ {no-stock, insufficient-isk, standing, not-buyable}`（部分成交归 `no-stock`），界面据此分别措辞——**钱不够**「ISK 不足：最低一张 X，钱包 Y——减少数量，或用「挂买单」低价排队」· **声望**「暂不能买入：需「深空工业协会」声望 N（当前 M）」（`bmStanding` 暗市闸**对玩家按同一口径措辞**，不泄露暗市机制）· **只收不卖**「市场不出售现货（可等二手挂单或自己制造）」· **真没货**「当前没有现货：用「挂买单」等 NPC 补给」· **部分成交**「供应簿只够 N 件（已买入 …）」。界面侧：市场详情「市价买入」在**无可吃单 / 买不起最低一张**时**禁用**并把原因写进 title | core/market.ts `buyAtMarket`；renderer/game/engine.ts `buyGoodAt`；pages/MarketPage.tsx 买入按钮门控；tests/market-buy-reason.test.ts（5 例） |
| 协处理器（低槽 CPU 预算扩容件） | 2026-09-11 船长：「**新增低槽配件，效果是增加舰船CPU**」→ 追问「**玩家是否会通过装卸这个配件'偷'CPU容量**」→ 四条裁定：「自身不占用，单纯增加10/15/20CPU」·「甲：新开一族『协处理器』」·「市场只有MK1稀有2，MK2稀有3。无MK3蓝图，MK3稀有4放入奇货」·「甲：双向校验」。**口径**：新族 `slot='cpu'`（低槽，与装甲/货舱/支援件抢槽）；**MK1/MK2/MK3 = 装配 CPU 上限 +25/+35/+45（2026-09-11 船长追加「之前的CPU装备，增加的CPU数值上调至25/35/45」——原定 +10/+15/+20 作废；价与稀有度未随之调整）**，本件 `cpuUse = 0`（自身不占）；多件全额叠加（上限 = 低槽位数）；**装配校验与无人机放飞共用这一份预算**。**渠道**：稀有度 2 / 3（稀有订单层）与 4（**奇货**，无蓝图）；价 388,000 / 1,940,000 / 2,600,000（落本档既有价带）；配方锚 材料≈产物价×0.45。**防套利 = 双向校验（单点 `equipment.cpuOverloadText`）**：预算随件走 ⇒ **卸下/换装都按"最终状态"预演，超载则拒绝卸下**（否则"装扩容→塞满→卸扩容"净偷扩容值；无人机清单同一路径）；装配页换装改走 **`swapModuleAt`（一次成型）**，避免中间态把合法换装卡死；旧档超载不自动卸件、只拒绝新增。**契约放宽（船长裁决甲）**：V10.5b 的「每件 `cpuUse ≥ 1`」改为「必填整数 ≥0，**只有带 `cpuBonus` 的件允许为 0**」 | core/equipment.ts（`cpuBudgetOf` / `cpuOverloadText` / `swapModuleAt` / fitModule / unfitAt / adjustDroneLoad）；core/combat.ts createPlayerSpec；types.ts `ModuleDef.cpuBonus` + `ModuleSlot 'cpu'`；data/modules.ts `mod-cpu-1/2/3`；data/blueprints.ts `bp-cpu-1/2`；marketCatalog + rarityTier；renderer FitPage（CPU 条走 cpuBudgetOf、换装走 swapModuleTo）/ shipInfo（信息卡「CPU 扩容 + 自身占用」行）/ Glyphs（`cpu` 图标 + 色调）/ itemSubs（`cpu` 子分类）；content-check 两处契约；tests/cpu-coprocessor.test.ts（6 例）；设计稿 `docs/design/cpu-coprocessor-20260911.md` |
| 位数组（槽位布局对齐） | **V18 位数组 = 每类槽位的定长数组**（`FittedModules.high/mid/low`），长度语义 = **船型布局**（`ShipDef.slots`）；`emptyFitted()` 缺省造的是 **1/1/1**、与船型无关 ⇒ **任何"按位号"的命令都必须先补齐**（`equipment.ensureRackBays` / `wantedBaysOf`：只补空位、不动已装件、幂等），载入修复链 `repairDeprecatedModules` 负责持久对齐（超长裁尾退库）。**2026-09-12 玩家实测**：装配页装入改走 `swapModuleAt` 后漏了补齐，界面按 `Math.max(船型布局, 数组长度)` 画格子 ⇒ **每类槽位只有第 1 位能装**，报「该 X 槽位不可用（第 Y 位）」（新造/新买的船 + 未读到存档的新档必中；**重开一次游戏即恢复**；船长裁定「按推荐来」= 甲 + 丙 已修） | equipment.ts `ensureRackBays` / `wantedBaysOf` / `fitModule` / `swapModuleAt` / `repairDeprecatedModules`；labels.ts `emptyFitted`；engine.ts `start`（新档也跑修复链）；tests/fit-bay-align.test.ts |
| CPU 剩余显示口径（刚好装满 / 超载） | **2026-09-12 船长（玩家反馈「装备的 CPU 大于剩余 CPU 时，卡片上只显示到 0，看不到还差多少，也没有变红警示」）→ 裁定「甲」**：装配页 CPU 相关的显示**允许剩余为负**——**超载**（预算 − 占用 < 0，装配/放飞会被拒）⇒ 候选卡差异段 `CPU 剩 11→−9（差 9）`**红**（复用 `.dseg.is-down` `#ff9288`）、顶部 CPU 条 `−15 / 130` + 右侧「超 N」**红**（`is-full`）；**刚好装满**（= 0，**合法、装得进**，只是加不了新件）⇒ 顶条右侧「刚好装满」**琥珀**（`is-warn`，**不再红**）；有余量照旧按百分比（>15% 青绿 / ≤15% 琥珀）。**判据 = "装后是否超预算"，不是"数值有没有变化"**：已超载的船换同耗件照红、换成更轻的件但仍然超载也照红；两栏各用**自己的预算**（装卸协处理器时前后预算不同）。同页无人机装入弹层的「余 CPU」同样给原值（可为负），而"还能装几架"的功能钳制不变 | `apps/desktop/src/renderer/src/pages/FitPage.tsx`（`minus()` / `diffSegs` 的 CPU 段 / `CpuStrip` / 无人机弹层）；样式沿用 `styles.css` 的 `.dseg.is-down` 与 `.app-fit-cpustrip.is-warn/is-full`；引擎判定不变（`equipment.cpuOverloadText`） |
| 我的舰队检索（状态 / **级别** / **类别** 三级） | 2026-09-10 船长：舰队「排序 + 筛选 + 搜索」。**2026-09-11 船长：「我的舰队中，移除排序选项，改为按照舰船级别划分的子筛选」** ⇒ **排序下拉（默认排序 / 按名称 / 耐久〔待修优先〕/ 按舰船族）整条退场**，列表顺序**固定为「机库序 + 已标记置顶」**（＝原「默认排序」口径，「收藏船置顶」规则继续成立，不再有"选其它排序时置顶失效"的分支）。**同日追加：「级别筛选单列一行，当选择级别后，出现舰船类别（工业战斗那些）筛选」** ⇒ 工具条由单行 flex 改**多行**（每行一个 `.app-fleet-row`）：**第 1 行** 搜索 + **状态**（全部 / 驾驶中 / AI 执勤 / 空闲 / 待维修）；**第 2 行** **级别**独占一行（复用组装机那张单点表 `SHIP_TIER_SUBS`：全部 / T1 护卫舰 / T2 驱逐舰 / T3 巡洋舰 / T4 主力舰 / T5 旗舰）；**第 3 行** **舰船类别**（角色，取市场/手册同源的 `SHIP_SUBS`：全部 / 采矿舰 / 货运舰 / 武装舰 / 重装舰）——**仅在选中具体级别后出现**（级别回「全部」即收走）。**级联口径**：换级别 → 类别复位「全部」；级别 = 全部时类别不参与判定。**每行各带灰字前缀**（「状态：」「级别：」「类别：」，前缀写法同星图页「矿带排序：」），避免多个「全部」混淆；各维筛选与搜索词一律「与」关系。**同日再追加：「将搜索栏移到标题内，目前不够美观」** ⇒ **搜索框与计数整体上移到面板标题行**（复刻仓库页 / 技能目录同一套写法：`.app-head-search-wrap` + `.app-head-search` + 灰字计数），工具条只留各维筛选；标题行计数随状态切换——无筛选 = 「N 艘」、任一维筛选或搜索生效 = 「**匹配 N / 共 M 艘**」，后接「· 当前驾驶：<船名>」。**实测**（无头 Chrome + 六艘跨五档档：沙猫 T1 采矿 / 鲣鱼 T1 武装 / 鲸吞 T2 采矿 / 鲸王 T3 采矿〔打伤〕/ 蝠鲼 T4 货运 / 皇带鱼 T5 货运）：初始 **2 行**（状态行 top **259px** / 级别行 top **302px** ⇒ 级别确实自占一行）、选 T1 后 **3 行**（类别行 top **345px**，标签 = 全部 / 采矿舰 / 货运舰 / 武装舰 / 重装舰）；读数链——T1 = **2 艘** → 叠加「武装舰」= **1**（鲣鱼）→ 叠加「采矿舰」= **1**（沙猫）；**切 T4 后类别自动复位**（T4 = **1 艘**；若未复位应为 0，"采矿舰"残留会归零）→ 叠加「货运舰」= **1**（蝠鲼）；再切 T3（类别再次复位）+ 状态「待维修」= **1 艘**（鲸王）；**级别回「全部」行数回到 2 行**（类别行收走，状态筛选仍生效）；列表容器 **612/612**（内滚正常、无裁切）。**搜索入标题行后三档窗口实测**（1920×1080 / 1366×768 / 1024×768）：搜索框均**在标题行内**（`head.contains(input) = true`）、工具条内已无搜索框；标题行宽 **1341/1341 · 787/787 · 445/445**（client=scroll，**零溢出**）、搜索框与右栏都不越界；搜索「鲸」→ **2 张** 且计数变「**匹配 2 / 共 6 艘** · 当前驾驶：…」；级别 T4 → **1 张** 且计数「匹配 1 / 共 6 艘」 | apps/desktop/src/renderer/src/pages/ShipPage.tsx（`FLEET_FILTER_TABS` / `fleetTier` / `fleetRole` / `fleetTotal` / `fleetFiltered` / `pinMarked`）；styles.css `.app-fleet-toolbar`（改纵向）+ `.app-fleet-row`；ui/itemSubs.ts `SHIP_TIER_SUBS` / `SHIP_SUBS` |
| 蓝图二次分类（组装机子筛选 · 舰船按级别） | 2026-09-11 船长：「**对组装机的蓝图添加子筛选，根据产物的类型进行二次分类。舰船部分按舰船级别划分。弹药蓝图改为消耗品蓝图。**」（集中提问后定「甲：装备蓝图按**产物功能**分组」＋「甲：市场页蓝图子分类**同步改**」）。**①组装机一级标签** = 全部 / 装备蓝图 / 舰船蓝图 / **消耗品蓝图**（原「弹药蓝图」——该档实际含**弹药 6 + 修理组件 2**，"弹药"是旧误称）。**②二级子筛选**（按当前一级标签出候选，**全部取自 `itemSubs.ts` 单点**）：装备 = **产物功能九组**（`MODULE_SUBS`：采集与货舱 8 / 武器 10 / 护盾 12 / 装甲 12 / 推进器 3 / 无人机装置 9 / 支援件 21 / 打捞器 3 / 目标锁定 3 = 81 张）；舰船 = **舰船级别五档**（`SHIP_TIER_SUBS`，键 `t<级别>`、名走 core `shipSizeLabel`：T1 护卫舰 4 / T2 驱逐舰 7 / T3 巡洋舰 9 / T4 主力舰 3 / T5 旗舰 1 = 24 张）；消耗品 = 产物大类（`CONSUME_SUBS`：弹药 6 / 修理组件 2 / 无人机 0）。**「全部」标签不带子筛选**；**切一级标签自动回「全部子类」**（与市场页 `changeKind` 同款）；子筛选中面板头补「当前 N 张」；空子类（如无人机）给空态提示。**③市场页同步**：`BLUEPRINT_SUBS` 由 5 项 → **9 项**（保留高/中/低槽装备蓝图三档、舰船蓝图由 1 档拆成 **T1~T5 五档**、原「补给蓝图（弹药·修理组件）」改名「**消耗品蓝图（弹药·修理组件）**」）；`subPasses` 舰船档改按 `t<级别>` 判定。**④手册「蓝图图鉴」**随同一张表：舰船由 1 组变 5 个级别组（全表 9 组），且**物品蓝图条目的产物门类改取产物自身大类**——修掉此前把 2 张修理组件蓝图错标成「弹药 · …」的文案 bug。**⚠ 粒度差异（有意）**：组装机装备档按**产物功能**分九组（槽类三分收不窄 81 张），市场/手册仍按**产物槽类**分三档（2026-09-10 船长定，未改） | apps/desktop/src/renderer/src/ui/itemSubs.ts（`SHIP_TIER_KEYS` / `SHIP_TIER_SUBS` / `BLUEPRINT_SUBS` / `subPasses`）；panels/Industry.tsx（`MANU_TABS` / `manuSubsOf` / 子筛选行）；pages/MarketPage.tsx（子下拉走 `SUBS_OF_KIND`）；panels/Handbook.tsx（蓝图图鉴分组） |
| 市场类型筛选（四层：物品／消耗品／残骸／装备） | 2026-09-11 船长：「市场的筛选中，物品选项文字**建议换成消耗品**」→ 当场更正为「**不对，应该将消耗品独立出来**」⇒ **消耗品独立成一级类型**，并从「物品」剔除（与 2026-09-08「残骸」独立成类同口径）。**现行类型下拉**：全部类型 / **物品**（矿石·矿物·气体·冰矿 + 动态碎片）/ **消耗品**（**弹药·修理组件·无人机**）/ 残骸 / 高槽装备 / 中槽装备 / 低槽装备 / 舰船 / 蓝图 / 核心。消耗品子分类 = 弹药/修理组件/无人机（`CONSUME_SUBS`）；键集合单点 `CONSUME_KIND_KEYS`，剔除判定在 `subPasses`（子分类）与 `MarketPage.kindPasses`（一级类型）两处。**实测**（无头 Chrome）：消耗品 12 行 = 弹药 ×6 + 修理组件 ×2 + 无人机 ×4；子筛选「弹药」6 行；「物品」22 行只剩矿石 7/矿物 8/气体 4/冰矿 3。**手册图鉴不受影响**（它按 core `ITEM_KIND_ORDER` 的物品大类分组） | apps/desktop/src/renderer/src/ui/itemSubs.ts（CONSUME_SUBS / CONSUME_KIND_KEYS / SUBS_OF_KIND）；pages/MarketPage.tsx（KIND_TEXT / KIND_OPTIONS / kindPasses） |
| 长途运输 | 2026-09-09 船长定稿（原称「运输任务」，同日独立改名定稿）：星图页独立标签（残骸打捞之后），**至少建成一座副空间站解锁**（母港之外的端点）；任意两座已建成站点间真实航程往返循环（虚拟货物占满货仓、不产生真实物品、到站自动结算续段）；可随时停止（即时返港出发站，无惩罚）；换驾驶 = 立即终止 | hauling.ts；HaulingPanel（星图「长途运输」标签）；HAUL_RATE_PER_M3_MIN |
| 舰船价格三口径（市场行 = 锚） | **三处价格必须同源**：① `marketCatalog` 的市场行 `basePrice` = **玩家真正付的价**（`buyShip` 走市场报价），是**锚**；② `ships.ts priceIsk` 必须等于该市场行价（**定制船 = 0**：无市场行、或市场行 `playerBuyable:false`〔只收不卖〕⇒ 图鉴显示「定制 / 仅可制造」，维修费档位退回"层容量"兜底）；③ `shipBlueprints priceIsk` = **市场价 × 档位系数**（≤30 万 ×2 / 30~100 万 ×2.5 / 100~400 万 ×3 / >400 万 ×4，±1 万整万取整余量）。**为什么重要**：`repairTierWeight` 读 `priceIsk`（<20 万 ×0.4 / <80 万 ×0.7 / <150 万 ×1.0 / 其余 ×1.4），两处不一致 ⇒ **维修费与船价脱节**；平衡工具（`balance-check` / `playthrough-sim`）也按 `priceIsk` 决定买船与进度。**2026-09-09 事故**：「巡洋价位定档 9/11/13/15M」的重放（提交 `6148f0d`，自称"此前被并行提交交叉吞失"）按**旧数值**匹配，把阶梯写到陆龟/玳瑁/飞鱼/旗鱼头上（330k/760k/210k/480k → 9M/11M/13M/15M），4 艘巡洋反而没改 ⇒ 4 艘维修费贵一倍、座头鲸贵 40%。**2026-09-11 船长裁决**：「按你推荐的来（对齐市场行），3 改为预警，将舰船蓝图价格也同步」⇒ 9 处对齐 + 2 张蓝图取整对齐（鲣鱼 8→8.4 万、陆龟 83→82.5 万）+ `content-check` 新增**价格口径预警**（`warn` 不阻断，扫 priceIsk／蓝图价两路）；同日**追加裁决「甲」**：皇带鱼级旗舰货舰补齐「只收不卖」（`playerBuyable: false`，与开拓/鲸王同口径——它是 2026-09-09「蓝图船成品现货下架」那次清扫的漏网），至此**三艘定制船 = 开拓 / 鲸王 / 皇带鱼**（图鉴「仅可制造」，二手可卖） | data/ships.ts；data/marketCatalog.ts；data/shipBlueprints.ts；core/shipyard.ts repairTierWeight；core/industry.ts buyShip；tools/content-check.ts「舰船价格口径（预警）」 |
| 蓝图价格口径（装备/物品 · 档位系数 + 个例覆盖） | 2026-09-11 船长：「**调整所有蓝图到合适价格**」→ 裁决「**甲**」；同日复核补正「**蓝图价格排除奇货档。奇货档默认 4 倍。所有蓝图按系数都是默认值。如果有单独调整过的则允许覆盖默认值。**」⇒ **装备/物品蓝图书价 = 产物现货价 × 档位系数**——**奇货 ×4**（市场稀有度 `exotic`，**不参与** MK 阶梯）· `民用/基础/MK1 ×2` · `MK2 ×2.5` · `MK3 ×3`，**取整到 500 ISK**；产物现货价 = 市场行 `basePrice`（item 类 × 单次产出数量 `outputUnits`）。**系数即默认值**；个例走 `BLUEPRINT_PRICE_OVERRIDES`（键 = 蓝图 id，值 = 覆盖价 + 理由；**现登记 6 条 = 弹药线**，见下「两条复核」）。**两处必须同值**：`blueprints.ts priceIsk` = `marketCatalog` 蓝图行 `basePrice`（玩家付款读市场行、图鉴/工业页读书价）。**单点 = `blueprintTierCoefOf(bpId, rarity)` / `blueprintBookPriceOf(bpId, 产物价, rarity)` / `BLUEPRINT_PRICE_STEP`（`data/blueprints.ts`，已由 `data/index.ts` 导出；`rarity` **必填**，漏传会算错奇货档 ⇒ 由类型强制）**；`content-check` 据此出「蓝图价格口径」契约（**硬**：两处同值 + 有市场行 + 覆盖表键必须是真实蓝图 + 覆盖价三处同值；**预警**：与规则值差 >500 ISK、料/价出 30%~60% 带）。**背景（实证）**：2026-09-10「MK2/MK3 装备价对齐同级武器价」只改成品价、书价没跟 ⇒ 91 张里 **56 张**的「书价 ÷ 产物价」漂到 **0.18~1.88**。**本批全量对齐**：涨 56 / 不变 35 / 降 0，书价总额 **76,203,900 → 199,794,500**（后按下列两条复核调整：弹药线回滚、奇货 ×4）；档位分布 民用·基础·MK1 **37** / MK2 **28** / MK3 **25** / **奇货 4**（共 **94** 张），其中**单独覆盖 6 张**；料/价全部落在 30%~60%（锚 45%）；按料/价 45%、现货自用算，**回本 ≈ 3.6 / 4.5 / 5.5 次制造**。**两条复核（同日）**：**(a) 奇货档单列 ×4 恢复**（船长「蓝图价格排除奇货档。奇货档默认 4 倍」）；**(b) 弹药线书价不随系数走**（船长「弹药蓝图回滚到 1.5 倍，其他保持不变」→ 6 张弹药书维持 2026-09-09 原值 1,350/1,650/1,950 与 9,000/12,750/18,000，**登记为覆盖项**；修理组件 2 张按「其他」走系数默认 33,000/138,500 不动）。**（c）巨构近防炮三张补对齐（2026-09-12 船长裁定「乙」）**：近防炮 MK1/MK2/MK3 的书原为「照抄产物现货价」（系数 ×1）+ 料/价 15.8~17.6%，是**全表仅剩的 3 处系数漂移 + 3 处料/价出带**（`content:check` 常驻 6 条预警，源自 2026-09-11 机群批 S4 补档 `011f8e3` 未走本口径）⇒ 本批书价改 **236,000 / 787,500 / 2,760,000**（= 118,000×2 / 315,000×2.5 / 690,000×4），材料抬到料/价 **44.7% / 44.9% / 44.6%**（材料总价 52,800 / 141,400 / 308,000）⇒ 全表 **94/94 合规、预警 0**（硬契约 0 · 系数不符 0 · 料/价出带 0）。三张书的 `buildCostIsk`（24,000/52,000/96,000）为 2026-09-08 起**不再收取的历史遗留字段**，本次未动。**不在本口径内**：舰船蓝图 24 张（另走市场价档位系数，见「舰船价格三口径」）——**船长 2026-09-11 明确裁决「不动」**：13 张奇货舰船蓝图维持船价档位带（其中 7 张 ×3 不上调为 ×4），**「奇货 ×4」只适用于装备/物品蓝图**。 | data/blueprints.ts（`blueprintTierCoefOf`/`blueprintBookPriceOf`/`BLUEPRINT_PRICE_STEP`/`BLUEPRINT_PRICE_OVERRIDES`）；data/marketCatalog.ts；data/index.ts；tools/content-check.ts「蓝图价格口径」；tools/manufacture-econ.ts（料/价体检）；docs/design/ammo-mk2-20260909.md（旧 ×1.5 条款已批注作废） |
| 长途运输·时间与行情倍率（2026-09-11 船长两次定案·终版） | **① 时间**：标称航程分钟 ×**15**（`HAUL_LEG_TIME_MUL`，含"就位段"；面板/日志/真实毫秒同步变，航行技能照旧缩短实际时长，现下限系数 0.35）。**② 报酬**：**基准 = 货仓 × `HAUL_RATE_PER_M3_MIN`（0.6，改前原值）× 标称航程分钟**；每**趟**（一趟往返）掷一次**行情倍率 r ∈ [5,10]**（`HAUL_TRIP_MUL_MIN/MAX`，均匀、保留 1 位小数），**同一趟两段同价**；到站结算入账并写日志（含「本趟行情 ×N」）。**③ 面板只显示区间**（`haulRewardRange`：单段 min~max + 往返 + 时薪区间），**不预告本趟实际掷值**——船长原话「重新定位 5~10 倍的价格波动，并要求在长途运输任务内显示」。**均值口径** = 改前单段的 7.5 倍，时薪均值 = 改前的 7.5/15 = **0.5 倍**；仍与航线长短无关（不挑短线刷钱）。**沿革**：船长先定「时间 ×15、收益 ×10」（固定倍率，费率一度改 0.4），随后「**价格回调**，重新定位 5~10 倍的价格波动」⇒ **固定 ×10 作废、费率回 0.6**，改为每趟掷价。**存档**：`hauling.tripMul` / `tripLegsLeft` 随档保留（旧档缺字段 = 0 → 结算按区间下限兜底、下一段起重新掷） | core/hauling.ts（`haulLegReward` / `haulBaseReward` / `haulRewardRange`）；state.ts HaulingState；save.ts；panels/Hauling.tsx；tests/hauling.test.ts |

## 七之二、通讯（收件箱）（2026-09-11 船长定：NPC 发消息补充剧情与任务提示）

| 术语 | 定义 | 出处/规则 |
|---|---|---|
| 通讯（收件箱） | 2026-09-11 船长定（设计稿 `docs/design/comms-20260911.md`，六条裁决全为「甲」）：左侧导航新增一级页**「通讯」**（末位，**邮件形** SVG 线稿图标 `nav-mail`）。NPC（协会各部门、合作方、航线上的熟人）以**发消息**的形式补充剧情或发布任务提示；玩家在此查看**历史通讯记录** | pages/CommsPage.tsx；ui/Glyphs.tsx `nav-mail`；App.tsx NAV_ITEMS |
| 通讯消息来源与触发 | **数据表 + 触发条件**：`packages/data/src/messages.ts` 一张 `COMMS_MESSAGES`（**发件势力/部门** + 内容类型 + 主题/正文/`trigger`）；core `advanceComms` 每次时间推进廉价判定、**条件满足一次即送达（幂等）**，送达即记账 `state.commsDelivered[id] = 游戏内毫秒`。触发器八类：开局（序章引导走完才送）/ 天数 / 已探明星系数 / 指定星系 / 技能等级 / 现金 / 副站建成 / **教程步骤**（`{ kind: 'tutorial', step }`，判定 `onboarding.step >= step`，跳过教程后补送） | core/comms.ts commsTriggerMet / advanceComms；data/messages.ts |
| 教程融入通讯 | 2026-09-11 船长定：**序章七步教程的全文改由通讯承载**——`data/src/tutorialSteps.ts` 是七步文案与跳转的**唯一出处**，`messages.ts` 按它生成 `tut-1..tut-7`（发件方 = **信息库 · 检索重启**，内容类型 = **教程**）；**右下角引导卡取消**；教程期**通讯页始终可开**（其余页面仍按 `TUT_LOCK` 白名单锁）；顶部引导条 = **「步骤 N/7」** + 当前动作一句话 + 「前往…」+ **「看详情」**（切通讯页并选中该封，经 App 的 `commsFocus{id,seq}`）+ **「跳过教程」**。**睁眼动画结束后先进简报态 `ONB_BRIEFING = 0.5`**（不立刻开始教程）：此时只开通讯页、顶栏提示「先看通讯」，玩家读 `tut-0` 简报并点其中的「开始教程：采集富凡晶石」（消息自带动作 `action` → `core runCommsAction` → `startTutorialFromBriefing`）才进第 1 步 | data/tutorialSteps.ts；core/{comms,onboarding}.ts；App.tsx tutCanOpen/commsFocus；panels/TutorialGuide.tsx |
| 训前简报（`tut-0`） | 2026-09-11 船长定：简报**标题简化为「训前简报」**（原「训前简报：先看这封，再开工」作废）；正文改用**公文条目体例**——`【区块】` 小标题 + 逐行陈述 + `01/02/03` 编号，自称一律「**本舰**」，区块依次为【自检记录｜本舰】【唤醒状态】【任务链｜协会新登记舰顺路流程｜共 7 步】【同步规则】【执行确认】（末句「本舰的活，从第一船矿石开始。」）。**七步教程正文同体例**（`【当前执行项｜第 N 步】` + 一句话 + 【执行步骤】编号 + 可选【系统设定】/【结算】/【备注】）；`goal` 仍是顶部引导条那句口语短句，`title` 只用于通讯主题 | data/tutorialSteps.ts BRIEFING_INTRO / TUTORIAL_STEPS |
| 演习场驱逐令（旧称"讨伐令"作废） | **2026-09-11 船长裁决**「**演习场讨伐令改为演习场驱逐令**」（`foe-ships.ts:158`）；游戏内一致口径 = **演习场驱逐令**（`anomalies.ts` 卡面、重要任务卡、系统日志、悬赏列表）。同日教程文案复核时发现简报/第 5 步仍写旧称，船长裁决**统一改为官方现名**⇒ 教程简报第 05 条、第 5 步标题/目标/正文、顶部引导条关键字全部改「演习场驱逐令」。**历史旧称"演习场讨伐令"不得再进玩家可见文案** | data/anomalies.ts；data/tutorialSteps.ts；panels/{ImportantTasks,TutorialGuide}.tsx |
| 通讯消息自带动作 | 2026-09-11：`CommsMessageDef.action = { label, command }`（`CommsActionCommand` 现只有 `startTutorial`）——通讯页在正文下方渲染这枚按钮，点击经 `engine.runCommsActionAt` → core `runCommsAction` 分发；未知命令报错返回、界面只弹提示不崩。**注意 `commsInbox` 必须透传 `action`**（漏传过，界面就不显示按钮） | core/comms.ts runCommsAction；pages/CommsPage.tsx |
| 低安遇袭受损与撤退（2026-09-11 船长重做） | 玩家反馈「在低安活动，遇袭耐久大幅下降后，不会自动维修」（船长裁定：**是副船的问题**）。三条定案：① 受损档**旧口径「结构 −5%~15%」作废**——一口伤害 = **敌群火力 × `encounter.hitFirepowerSec`（0.3 秒）**（敌群火力 = 威胁 × `battle.foeDpsPerThreat` 0.8，与敌方总 DPS 同一常量），施加时**先扣装甲、吸完再进结构**（旧实现直接扣结构、装甲不动，与既有日志"被咬下一块装甲"不符——真 BUG）；结构底线 5% 保留。② **结构 <50%（`encounter.retreatHullFrac`）即撤退**：遭遇了结（击退/受损/被抢/应战胜负**全判**）后被袭船收手返港**待命**——主控停低安作业（采矿/打捞/扫描）并即时返航最近已建成站，副船中止 AI 任务召回；**不自动维修、不自动再派**（回港等玩家决定；副船 <50% 本就不能再派）。③ **迎战也挂 50% 自动脱离保险**（`fightEncounter` 写 `hullEscapeFrac`；此前只有悬赏巡回场次有）＝轻损退出、无缴获、无额外扣损。保险三项自本日起**随档保留**（此前 `cleanBattle` 未收录，战中重载会凭空失效） | core/encounters.ts；core/balance.ts `encounter`；core/shipyard.ts hullLayerCaps；core/save.ts cleanBattle；tests/b1.test.ts（+6 例）；Handbook「低安安全」 |
| 教程跳转（页内／内层标签 + 步骤内元素焦点） | 2026-09-11 船长实测两修：①「**步骤 2/7 跳任务中心不切指定标签页**」——星图「任务中心」是**两级标签**（外层 `MapTab 'task'` + 内层 `TaskTabKey` 重要/资源/快递/赏金），内层**会记住玩家上次选择**（`localStorage['whale-idle:task-tab']`），原来只带外层标签故落错页；现由 `TutorialStepDef.taskTab`（步骤 2 = `important`）→ 通讯 `hint.taskTab`（契约：仅 page=map 且 tab=task，键白名单四档）→ `CommsPage.onGoto` / `GuideGo.taskTab` → App `taskFocus{tab,seq}` → `MapPage` → `TaskPanel.focusTab`（seq 变化即切并回写），步骤切换（含读档归位）同样带上。②「**步骤 7/7 建议先指引玩家选择副船（高亮对应下拉框）**」——光圈原先只扫 `button`，扫不到「AI 指挥中心」的舰船下拉框；新增 `StepPlan.selectFirst`（步骤内元素级焦点），步骤 7 在**未选船时**指 `select[data-ai-ship]`（气泡「点这里：选择空闲舰船」），选好后回落「指派任务」——与既有"步骤内分阶段"（S1 先切沙猫再出击）同一语言 | data/tutorialSteps.ts taskTab；core/types.ts hint.taskTab；content-check「通讯消息契约」；pages/{CommsPage,MapPage,ShipPage}.tsx；panels/{TutorialGuide,Expedition}.tsx；App.tsx taskFocus |
| 通讯正文强调行（`highlight`） | 2026-09-11 船长：「**将训前简报的任务链内的文字高亮**」⇒ 新增 `CommsMessageDef.highlight?: readonly string[]`：列出要强调的正文段落，**必须与 `body` 某段逐字相等**（`content:check`「强调行契约」盯着，不等直接报错——否则界面上静默不高亮）。数据侧把简报的任务链七行抽成 `BRIEFING_CHAIN`（`lines` 与 `highlight` 同源，只改一处）；界面给命中的段落加**既有**强调样式 `.app-report-highlight`（离线报告那套：暗底 `rgba(15,26,40,.7)` + 左侧 2px 强调竖条——约定第六章"先复刻同级既有样式"，通讯页只把内边距收紧）。`commsInbox` 透传该字段（与 `action` 同一类"漏传就没反应"的坑，已有用例守） | core/types.ts highlight；data/tutorialSteps.ts BRIEFING_CHAIN；pages/CommsPage.tsx；styles.css |
| 敌方射程带（战斗界面 legend） | 2026-09-11 船长：「战斗场景内，假如敌方的舰船射程不一致，只会显示其中一个的射程」——指战场底部那条「敌方 X~Ym」chip。根因：`battleArcsFor` 把敌方**全编队**武器取 min/max 合成**一条**并集带（`foe:{minM,maxM,type}`），编队里短射程的船（如快速艇 1~1932 对头目舰 1~2210）被并集吃掉。修法：新增 `foeBands`（按 `(min,max,弹种)` 去重、**外圈在前**，`count` = 用该带的**敌舰艘数**、`names` = 舰名），界面照「我方逐武器一条」的同款做法**逐带各出一条** chip（多条带时带「×N 艘」与逐舰悬停说明；**只有一条带时文本与旧版逐字一致**）。聚合带 `foe` 保留（距离尺色带与战场弧仍用它）。实测：可见 32 个编队变体里**只有 2 张卡**有两套射程（边境海盗前哨 2210/1883、碎晶带劫匪通缉 2210/1932），其余 30 个单一带零变化 | core/combat.ts `foeBands`；BattleScreen.tsx legend；tests/expedition.test.ts（多带/单带各一例） |
| 两套认知口径（对外/对内） | 2026-09-11 船长定：「**外部认知中，我们是飞行员。但是内部系统中，我们知道自己是舰载 AI**」。**对外**：章鱼人 NPC 眼里玩家是「飞行员」（职务称呼，与物种无关），他们**不追问船里是谁**；**对内**：船自己的系统（信息库等）**知道玩家就是舰载 AI**，可以直说。落地 = `content-check` 的 NPC 文案扫描**按发件方分级**：NPC 来源不豁免「舰载 AI / 船载 AI / 舰船 AI」，船内系统来源（`alignment: '系统'`）额外豁免这三类自我认知表述 | tools/content-check.ts loreHits；data/commsFactions.ts；docs/design/npc-factions-20260911.md §2 第 8 条 |
| 信息库（舰载系统来源） | 2026-09-11 船长：「消息来源修改为信息库检索重启方案」——新增**船内系统**来源 `archive`（名称「信息库」、`species: '舰载系统'`、`alignment: '系统'`、`tone: #ff8ab5`、部门 `dept-recall`「检索重启」），教程与简报以它的名义送达；**它不是 NPC**，故不受"NPC 势力必须章鱼人"的铁律约束（契约只对官方/民间/中立强制），头像用**核心形** `nav-ai` | data/commsFactions.ts；content-check「通讯消息契约」 |
| 势力头像（两种） | 2026-09-11 船长：「头像换成类似核心的SVG」→ 裁定**按发件方分两种**：**船内系统（信息库）用核心形** `nav-ai`（粉色调）、**NPC 势力用官方章鱼头** `faction-octopus`（只靠 `tone` 区分行会）。映射表 = `FACTION_AVATARS`（新增势力时沿用）；契约按该表逐势力校验，写别的图标直接报错 | data/commsFactions.ts FACTION_AVATARS；ui/Glyphs.tsx |
| 通讯与剧本**合并** | T9 建站剧本（介绍/庆贺通话）**照旧**在抵达时弹通讯器浮层（既有体验不变），同时**镜像进通讯页**成为一条历史消息（键 `dlg:<剧本 id>`）⇒ 通讯页 = 玩家侧**唯一**的 NPC 通信记录。剧本 `DialogueScriptDef` 增可选 `subject`（缺省回落 title）与可选挂靠 `commsFactionId`/`commsDeptId`/`commsSigner`（未挂靠回落 `title` 原文） | station.ts deliverDialogueToComms（挂起待播与播放两处都调，幂等）；data/dialogues.ts |
| 通讯里的任务口径 | **只给提示 + 跳转**（裁决③）：消息可带 `hint = { text, page, tab? }`，通讯页给一枚「前往」按钮切到对应一级页（星图可带页面内标签）；**不在通讯页里接取、推进或交付任何任务** | CommsPage onGoto → App.changePage/changeMapTab |
| 未读与已读 | **逐条已读**（裁决④）：未读 = 已送达且未读；**点开即已读**（含默认展开的那一封），另有「全部标记已读」；未读时导航图标**闪烁**（`app-nav-blink`，只动 transform/opacity；`prefers-reduced-motion` 退化为静态）+ 右上角**未读数字徽标** `.app-nav-badge`（>9 显示 `9+`） | core/comms.ts commsUnreadCount / markCommsRead / markAllCommsRead；styles.css |
| 回复选项（预留未启用） | 裁决⑥：类型（`CommsReplyDef`）、数据字段（`replies`）与界面分支都留好，开关常量 **`COMMS_REPLIES_ENABLED = false`** ⇒ 玩家侧**不出现**任何回复控件（不做空按钮） | core/comms.ts COMMS_REPLIES_ENABLED；CommsPage |
| 通讯器造型（机身 + 内嵌屏幕） | 右列通讯器 = **机身式大圆角外框 + 内嵌屏幕圆角框**（2026-09-11 v2 船长按参考图**只借边框线条/圆角**）：**机身** = SVG 线稿 `rect`（`vector-effect="non-scaling-stroke"`，跟随面板边框色）；**内嵌屏幕的圆角框由屏幕容器自己画**（`border-radius` + CSS `outline` 线段描边，只描边不改盒形，不算 CSS 拼形状）⇒ "文字容器 = 圆角框"，**窗口任意尺寸都不错位**（几何口径：机身内工作区上 15 / 侧 19 / 下 54，屏幕内缩与窗口高度无关） | CommsPage CommsDeviceFrame；styles.css `.app-comms-frame` / `.app-comms-screen` |
| 通讯页版式 | 裁决⑤：**双列**（左列消息列表 = 本页唯一滚动区，右列通讯器：屏幕自身滚 + 屏幕下方**通栏胶囊「前往」**），一级页**整页不滚**（`PAGE_NO_SCROLL` 含 `comms`）；屏幕内三层信息 = 大标题（右侧内容类型小片）→ 细分隔条（发件人 + 立场小片 + 送达时间）→ 正文 | styles.css `.app-comms-*`；App.tsx PAGE_NO_SCROLL |
| 通讯存档字段 | `state.commsDelivered`（id → 送达时的游戏内毫秒；负数/非数值丢弃 = 视作未送达，下次按条件补送）与 `state.commsRead`（id → true，只记 true）；两字段**可选、零迁移**，老档读入 = 空收件箱。**势力档案不入档**（静态数据，界面按当前数据表解析） | save.ts normalizeState；state.ts GameState |
| NPC 势力档案 | 2026-09-11 通讯 v2 船长定：`packages/data/src/commsFactions.ts` 一张 `COMMS_FACTIONS`（`id / name / species / alignment(官方·民间·中立) / tone / glyph / brief / kinds[] / departments[]`）。本期 2 个势力：**深空工业协会**（8 部门：航行管制 / 基建部 / 测绘处 / 工业部 / 冶炼组 / 训练处 / 财务处 / 航线安全）+ **打捞队工会**（1 队）。铁律：**所有 NPC 势力都是章鱼人**（`species` 契约强制）；色调与既有系统同源（协会 `#9fd8ff` 同 `nav-mail`、打捞队工会 `#6fe3f0` 同 `nav-salvage`） | data/commsFactions.ts；core/types.ts CommsFactionDef |
| 通讯发件方解析 | **玩家看到的发件人写法由「势力 + 部门」拼出**（`势力名 · 部门名`，缺部门 = 只显示势力名），消息表不再写自由文本发件人；剧本挂靠同一套（`commsFactionId`/`commsDeptId`/`commsSigner`）。收件箱视图 `CommsEntryView` 带 `from`（发件人写法）/ `factionName` / `alignment` / `kind` / `tone` / `glyph` / `fromBrief`（悬停"这是谁"）；**解析失败降级显示原文 id 且不崩**；内容类型走两级白名单（势力 + 部门） | core/comms.ts resolveCommsSender / commsInbox；content-check「通讯消息契约」 |
| 官方章鱼人头像 | 2026-09-11 船长定：**绘制章鱼头代表官方，该头像之后都用于代表官方章鱼人**。图形 = `ui/Glyphs.tsx` 的 **`faction-octopus`**（圆钝外套膜头 + 顶端小凸起 + 两只实心眼 + 外套膜线 + 6 条腕足；24×24 / currentColor，与 `nav-*` 同语言），色调与协会同为 `#9fd8ff`。**复用规则**：所有 NPC 势力物种都是章鱼人 ⇒ 头像一律用这一枚（数据常量 `FACTION_OCTOPUS_GLYPH`），**只靠 `tone` 色调区分行会/立场**，不再另画新头像；契约强制势力 `glyph` 必须等于 `faction-octopus`。出场：通讯页消息界面**左上角**（标题与来源左侧，头像盒 56/66/76px + 盒内图形 50px）+ 列表行内 18px。**线宽口径**：本组几何按 1.4× 铺满视框、`strokeWidth="0.7"` 覆盖默认 1.7（船长："盒内图形线条过粗"） | data/commsFactions.ts；ui/Glyphs.tsx；pages/CommsPage.tsx |
| 星系机制通讯（触发器 `lowSec` / `foeFamily`） | **2026-09-12 船长定**：「罗列下目前的特殊机制，并在玩家探索到该具备特殊机制的星系后在通讯界面发一封通讯给玩家，和玩家讲解下对应机制」⇒ **只发星系级机制**四封（奥罗荒环＝巨构残骸带 / 鱿烬亡军三星系＝无人机群 / **低安空域（sec ≤ 0，含 0）** / 红环航道＝前哨站选址），**一个触发面一封**；全局机制（近防炮集火 · 窝点 · 回收池）不发信，只在无人机相关的两封里点到近防炮与防空属性武器。**两条新触发器都不写死星系清单**：`{ kind: 'lowSec' }` = 已点亮星系里存在**低安**（**sec ≤ 0，含 0**——与伏击掷骰**同一出处** `balance.encounter.lowSecMax`，船长同日「将伏击掷骰阈值降低为0」＋「0也算低安」⇒ **安全等级改判时自动跟随**；缺省安全等级按高安 `?? 1`，与 `secOf` 同口径）；`{ kind: 'foeFamily', family }` = 已点亮星系里存在**任意一张该族敌卡**（读 `AnomalyDef.foeFamily + galaxyId` ⇒ **敌卡搬家自动跟随**）。**老档补送**（只读 `exploredGalaxies`，零新存档字段）。守卫 = `content:check`「星系机制通讯登记契约」（四封必须都在 · 四个触发面互不相同）+ 两条死触发器守卫 + `tests/comms.test.ts` 4 例 | core/{types,comms}.ts；data/messages.ts；docs/design/comms-20260911.md §17 |

## 八、舰船尺寸大分类（2026-09-09 船长定：全船统一 5 档，按等效质量落档；armored ×0.65 修正）

| 术语 | 定义 | 出处/规则 |
|---|---|---|
| 护卫舰 T1 | 舰船尺寸大分类最低档（等效质量 0.4~2.4M）：轻型舰体，入门与轻型作业 | labels.ts SHIP_SIZE_CLASS；等效质量 = massKg×(armored?0.65:1) |
| 驱逐舰 T2 | 第二档（2.4~5.8M）：炮位/系统显著多于护卫，玩家中坚 | 同上 |
| 巡洋舰 T3 | 第三档（5.8~13M）：主力舰体；**武装巡洋级内容 = 奇货渠道**（2026-09-09 船长定） | 同上；ship-size-tier-rework.md |
| 主力舰 T4 | 第四档（13~29M） | 同上 |
| 旗舰 T5 | 最高档（≥29M） | 同上 |
| 子分类（规格词） | 船名内旧规格词（武装艇/护卫舰/驱逐舰/炮舰/巡舰/母舰/艇/矿舰/货舰等）= 同大分类下的子分类/型号称号，不参与档位判定；UI 展示大分类 + 船名 | ship-size-tier-rework.md |
| 奇货精装（同型内上调） | 稀有度在同尺寸类型内上调（不改档）：如 T2 驱逐·炮舰型的奇货精装（大白鲨级），价值/价格对标高一档普通船 | 船长 2026-09-09：稀有驱逐舰与巡洋舰同价值级 |
| 舰种（质量分级·**敌我共用**） | **2026-09-11 船长定案：舰种收敛为 5 档**（原 7 档口径作废）——**1 = 护卫舰 / 2 = 驱逐舰 / 3 = 巡洋舰 / 4 = 战列舰 / 5 = 旗舰**。**「重型巡洋」归入巡洋舰档、「战列巡洋」归入战列舰档**（它们是**称号**不是独立档；子分类本轮不做）。**档位来源 = 现有 `ShipDef.tier`**，按**等效质量**落档（等效质量 = `massKg × (role === 'armored' ? 0.65 : 1)`；区间 T1 0.4~2.4M / T2 2.4~5.8M / T3 5.8~13M / T4 13~29M / T5 ≥29M）——沿用仓库 2026-09-09 既有规则，**本轮不改任何船的 tier、不重切区间、不改任何数值**。**敌我共用**：玩家的船与敌人的**舰级**归入同一套舰种、参考同一基准速度。单一出处 = `packages/data/src/hullClass.ts`（`HULL_CLASS_NAME` / `HULL_CLASS_MASS_RANGE` / `equivalentMassOf` / `hullClassTierOfTier` / `hullClassOf`）；`content:check`「舰种契约」逐船校验 tier 与等效质量落档一致（含 core `tests/hull-class.test.ts`） | `packages/data/src/hullClass.ts`；`ShipDef.tier`；labels.ts SHIP_SIZE_CLASS |
| 舰种基准速度 | **2026-09-11 船长本轮给定**：**1 护卫舰 340 / 2 驱逐舰 295 / 3 巡洋舰 258 / 4 战列舰 205 / 5 旗舰 155**（m/s）。口径：这是**敌我共用**的基准（玩家的船与敌人的舰级都归入舰种后参考同一基准）；**实际速度 = 基准 × 倍率**。**已落地到敌人侧**（2026-09-11 追加裁决「劫掠护卫舰和劫掠狙击舰下落一档，只有头目是巡洋舰」）：**敌人的舰级也登记舰种档，敌速 = 舰种基准 × 倍率**（倍率**按舰级给定**，写在 `FoeShipDef.speedRatio`、**精确分数式**如 `1.15`；编成条目还有 `speedMul` 时再乘）**后取整**。A 族四舰级 = T1/T1/T2/T3，**2026-09-11 数值落地批按 A 族设定「速度都快（方便突袭）」提速为 391 / 374 / 325 / 374**（**每档都高于本档舰种基准**；改造前读数 351/291/201/377 已随本批故意作废）；玩家的船仍走"船速 × 推进器"，本批不动。来由 = 各档**武装舰基线**（该档战斗舰体在正常推进器配备下的实速量级，轻档快、重档慢的单调阶梯） | **数值字面量唯一出处 = core `BattleBalance.hullClassBaseSpeedMps`**（core 不能 import data 包，引擎建单位时随 `bal` 读）；data `packages/data/src/hullClass.ts` 的 `HULL_CLASS_BASE_SPEED` 读它（**同源引用，不存第二份**）；`content:check`「舰种契约」校验每档恰一个基准速度且落在合理值域 100~500；A 族提速口径由同文件「敌速口径契约」的**全族提速带 1.00~1.60× + "实速高于本档基准"**守卫（旧"战术比率带"对 A 族不适用，见该契约注释） |
| **舰种体积（战斗画面）（⚙ 暂时关闭·默认还原）** | **2026-09-11 船长定：「战斗动画的舰身体积与舰种挂钩」**——战斗画面里舰身显示宽 = **T1 110 / T2 140 / T3 170 / T4 205 / T5 240**（px），**T3 = 170 是改造前的统一主尺寸（锚点）**；**僚机 / 轻装 = 同档 × 0.53**（＝改造前 90/170 的既有比例）。**敌我双向、同一套阶梯**：玩家舰取 `ShipDef.tier`，敌舰取编成条目所引**舰级的舰种档**（界面经 `core/foeShipTierOf(卡, tag)` 反查，与 `foeUnitNameOf` 同源）。舰艏（枪口回退锚）与舰身宽**线性**：`round(112 × 宽 / 240)`（170→79、90→42）。**旧威胁推导路径的卡没有舰级 ⇒ 回落 170/90**（船长裁定「这批卡**延后**，等各族舰级在二号处补完再生效」）。⚠ **2026-09-11 船长：「因为放大，导致各种距离不准了。能否先将各个级别舰船图形大小还原，系统保留进行测试」** ⇒ **总开关 `SIZE_BY_TIER`（默认 `false` = 还原）**：**关** = 显示回到改造前的「**主舰 170 / 其余 90**」，且**锚点也回到改造前的槽位算法**（首舰 `MAIN`、其余 `ESC`，与实际舰宽无关）⇒ 全 **26 张卡 × 5 个距离与改造前逐像素一致**；**开** = 启用阶梯，锚点改为**跟着逐舰实际体积**（顺带修掉"多舰卡锚点不跟舰宽"这处既有错位）。为什么观感"距离不准"：距离尺是**米 → 百分比**（与舰体无关），但**同一米数对应的视觉缺口**是按统一 170/90 舰体标定的。**测试启用（免重建）**：DevTools 置 `localStorage['whale-idle:ship-size-tier'] = '1'` 后刷新（与 V15 调试模式 `whale-idle:debug` 同款）。溢出**等比收缩**（下限 0.4）仅启用态生效 | `apps/desktop/.../panels/battleViewCore.tsx`（`TIER_SIZE`/`ESCORT_MUL`/`sizeByTierEnabled`/`sizeOfUnit`/`noseOf`/`layout`）· `BattleScreen.tsx`（取值与接线）· `packages/core/src/combat.ts`（`foeShipTierOf`）· 用例 = core `tests/foe-ship-path.test.ts`「舰种档反查」3 例；口径与读数 = `docs/design/ship-battle-art/battle-sprite-tier-20260911.md` |
| **敌列阵形（斜向菱形）** | **2026-09-12 船长定：「我想让敌舰排成 2X2 的菱形样式」→「斜向菱形，第二排向右偏移」→「我看了下 是丙」→（竖向两路里）「丙② 第一排不动 / 第二排下移 78」**——**取代** 2026-09-11 的「错列雁阵」口径（那份**作废**：`RANK_STAGGER`／`RANK_BY_WAVE`／`foeRaiseOf`／"前排＝首舰或头目档"判据均已删除；`core/foeShipEliteOf` 保留为只读查询）。**规则**：按**视觉行序交替分两排**（第 1/3/5… 艘＝第一排；第 2/4/6… 艘＝第二排），同排内按列从左到右（列宽＝该列最宽舰、列间距 4px）；**第二排右移半个列间距**（`shift = round((列宽₀+4)/2)`＝170 舰宽时 **87px**）、**下移一个舰高**（`drop = round(max(舰高))`＝**78px**）；**第一排严格原位**（`y = TOP + rowH − 舰高/2`＝改动前逐像素）、**单舰编成只有一排 ⇒ 逐像素不变**。⚠ **两条硬约束**（数值只能这么取）：① 舰体是实心的 ⇒ 两排水平错开 < 一舰宽时必须竖向拉开 ≥ 一舰高（右移 87＋竖向仅 54 ⇒ 会实打实叠住）；② 上方只有 `LAY.TOP`(54) 留白 ⇒ 第二排不能上抬 78（溢出被裁）故**往下**排（好处＝第一排完全不动，船长 2026-09-11 明确不喜欢"前排被顶下去"）。**DOM**：外层定宽块（宽＝编队总宽）＋两个 `.app-bts-shipRow`（排高固定 `minHeight = rowH`；第二排 `marginLeft = shift`；逐舰 `marginLeft = dx` 窄舰列内居中），**没有** `top` 偏移。**血条**：绝对定位在本舰正下方，宽 `min(185, 同排间距−6)` 下限 140；间距 <146 的排整组竖排到**编队最低舰底之下**（堆叠基准必须用**真实舰底**，用抬升量会算高一整排压到第二排——探针抓过此坑）。**第二排的浮空舰名**改由该舰**血条标签**承载（其上方是第一排舰体）。**残留取舍**：编队向下多占一个舰高（窄窗可能裁第二排血条）· 尸骸收拢时幸存舰**可能换排**（与"整批演完一起收拢"同源） | `apps/desktop/.../panels/battleViewCore.tsx`（`foeFormationOf`/`foeColLeft`/`foeBarGeom`/`layout`）· `BattleScreen.tsx`（两排 DOM + 血条入舰）· `styles.css`（`.is-unitBar` 一条）· 口径与读数 = `docs/design/ship-battle-art/battle-formation-diamond-20260912.md`（旧口径存档 = `battle-formation-20260911.md`，顶部已标作废） |

## 九、术语修订与交互用语（2026-09-08 船长定稿）

| 术语 | 定义 | 出处/规则 |
|---|---|---|
| 重复清剿 | 2026-09-08 船长定稿用词（原玩家向「连续出击/连击」；09-06 设计稿曾拟「巡回讨伐」，以本条为准）：悬赏打完自动回港、冷却结束自动再出发（每趟真实往返）；**返航途中也可在活动栏停止**（不影响本趟返航）；货仓不足/耐久不足/战败自动暂停。**2026-09-10 补**：①「退出战场」= 仅关闭全屏观看界面——战斗后台照常推进、清剿照常继续（中止请用战场内「⚑ 撤退」，撤退才停环）；②停环文案一律带当前 **装甲 X% / 结构 Y%**，并写入一次性提示 `autoLoopStopNotice` → 心跳读取即清并弹窗（玩家在线除事件日志外可见） | autoLoopAnomalyId；activity.ts loop 行；engine bountyLoopAt |
| 循环制造（原「连续生产」） | **2026-09-10 船长定：由逐条制造线「上移到整张生产卡」**（界面标签随之从「循环」改为「循环制造」）——一张卡（= 一个蓝图）只有**一个开关 + 一个目标件数 + 一个「本轮全卡合计」**：作用于该卡**全部制造线（含主控亲自那条）**，开关打开后新开的线自动继承；目标件数 = **全卡合计**，达标即不再续做，此刻在跑的件**跑完再停**（最多超产 = 同时在跑线数 − 1）；达成目标/材料不足 → **自动关开关**并在卡片上标停因（日志一条卡片级汇总）；「关→开」= 开一批新循环（合计与停因清零），开关开着时改目标**不动**已累计合计；手动关 = 完成当前件即止（不写停因）。老档逐线 `autoRepeat/repeatGoal/produced` 读入时归并到卡片（on = 任一为真、produced = 各线之和、只要有一条「无目标」则卡片也无目标）。逐线旧口径已作废 | `state.manufacturingLoops[blueprintId]`；`manufacturing.ts` setManufacturingLoop / manufacturingLoopOf；`GameEngine.setManufacturingLoopAt(blueprintId, on, goal)`；见 `docs/design/assembly-loop-card-level-20260910.md` |
| 防空（属性） | **2026-09-11 船长 A1「玩家武器通常不可打，需要带有防空属性的武器（为近防炮做铺垫）」**：**只有带本属性的武器能打敌方机群**（`WeaponSpec.canHitDrones` / `ModuleDef.canHitDrones`）；其余武器（**含我方无人机**，船长 B1）**按构造看不到机群**。玩家侧现只此一条线：**近防炮 MK1/MK2/MK3**（动能 · 高槽 · 射程 2500m · 可造可上市场） | 引擎 `combat.ts` + 数据 `modules.ts`；契约见 `content:check`「机群与防空契约」（防空武器射程 ≤ 2500m） |
| **备用机库（敌机）** | **2026-09-12 船长：「能否限制敌机单次出击数量**或者**给敌机添加备用机库（损坏后补充敌机）」→「**先都做，但是本轮只采用乙。甲留作后续其他机制。**」**（**乙** = 本条）：舰级 `droneReserve: { count, respawnMs }` ⇒ 机库在常备架数之外**多带 `count` 架同机型备用机**；备用机开局**在库**（不算出战架数、不可被防空选中、不出战）；前线战损 ⇒ **排期一架**、`respawnMs` 后**满血**从机库放出（同一时刻只排一架；母舰阵亡则整群停，含备用机）。**账目口径**：备用机条目带 `WeaponSpec.reserve`，**不属于常驻齐射**而是**库存深度**——总火力/守恒核对一律排除它，界面单列 `foeDrones[].hangar`。⚠ 与 A3 裁定「**打光为止不补充**」**相反 = 一次改判**（新裁定优先；未写该字段的族行为一字不变）。E 族实测：**常态出击数按舰种档**（船长 2026-09-12：「巡洋 **5** / 战列 **7** / 旗舰 **10**」）⇒ 奥罗 5 / 巨构 7 / 核心 10，**备用机库 = 出击数的 100%**（同数）；补位 10s / 10s / 12s；**总火力锚点**（见「机群火力占比」）钉住齐射 **216 / 154 / 672**（架数变动不影响总量）| `types.ts`（`FoeShipDef.droneReserve`）· `state.ts`（`DronePoolEntry.inHangar/readyAtMs/maxS~maxH`）· `combat.ts`（建池 / 排期 / 到位翻牌）· `data/foe-ships.ts`（E 三条）· 契约 = `content:check`「机群出击契约」· 用例 = `tests/foe-drone.test.ts` |
| **单次出击上限（敌机 · 机制待启用）** | **同日船长的"甲"**：舰级 `droneLaunch: { maxAloft, cycleMs?, keepDps? }` ⇒ **同时在空最多 `maxAloft` 架**，其余在机库按**批次窗口**轮换（每 `cycleMs` 换一批，缺省 = 机型装填）；`keepDps: true` ⇒ 在空架次有效装填 ×(k ÷ 在场架数) ⇒ **平均 DPS 守恒**（只摊平爆发），缺省 `false` ⇒ 持续输出按 k/N 下降。引擎 = `combat.aloftDroneSet`（**纯由战斗时钟推导**，不新增存档字段）。⚠ **本轮不采用**（船长「**甲留作后续其他机制**」）⇒ `content:check` **禁止任何舰级写它**（与"敌突进/单波增援"同款"机制实现、不启用"纪律） | `types.ts` · `combat.ts`（`aloftDroneSet` + 敌方机群开火门）· 契约 = `content:check`「机群出击契约」· 用例 = `tests/foe-drone.test.ts`（合成舰级锁住语义） |
| 敌方机群（警戒机 / 蜂群机） | **2026-09-11 机群批**：敌舰**自带的第二套火力**——挂**舰级**上（`FoeShipDef.drones`，船长 A3），机库存量、分批放飞、~~**打光为止不补充**~~（**2026-09-12 改判**：见「备用机库（敌机）」——写了 `droneReserve` 的族会补位，未写的族仍不补充）；演出**直接复刻我方出击制**（船长 A4：从敌舰机库口放出 → 抵阵位 → 开火 → 返航）；**火力计入卡的总火力**（船长 A5：机群吃同一条 `dmgMul`、**不吃多舰补偿**）。机型：E 族**警戒机**（命中 0.65＝"老化失准"· 中距 4.5km→5km）· G 族**蜂群机**三系（"一架便宜、一群致命"） | `packages/data/src/foe-drones.ts`；设计稿 `docs/design/foe-drone-system-20260911.md` |
| 近防炮反击（反击制 · 消费制） | **2026-09-11 船长重新定义**：①**出击型**无人机（侦察/战斗/攻坚）**每攻击一次 ⇒ 换近防炮一次无视射程的反击**（消费制：那一次"被打"用掉就没了，等它再打过来）；②**哨戒机**不适用"被攻击换反击"，而是**进入近防炮射程（`PD_SENTRY_RANGE_M` = 2500m）内就会被反击**（它常驻自己母舰旁 ⇒ 平时安全、两舰贴近才暴露）。**每发只打一个目标**（打舰与打机群互斥） | 引擎 `combat.resolvePointDefense` / `pickFoeDroneTarget`；`BattleState.droneHitAt` |
| **近防炮 · 2026-09-12 改版（船长八条裁决 · 以本条为准）** | 船长原话：「**敌人防空火力受舰船级别影响。越大的舰船防空火力越强。改为集火制度。优先攻击哨戒和攻坚无人机，给攻坚无人机添加 25% 全抗性。侦查和普通战机相同权重抽取。允许命中下限 10%。基础命中率提高到 70%**」。**现行口径**：①**命中** = `clamp(pdHitFloor 0.10, 1, pdAcc **0.7** − 机型闪避)`（原 0.5/无下限 ⇒ **专属机闪避 0.55 曾永久免疫**，已修）②**伤害** = `pdDmg 5 × 舰种档系数 pdTierMul [1.0 / 1.5 / 2.0 / 3.0 / 4.0]`（T1…T5，**越大的船防空越强**；单位档位来自新增的 `UnitSpec.hullClassTier`）③**集火**：锁定一架直到击落才换靶（新增 `BattleState.pdFocus`；原口径每拍随机换靶 ⇒ 伤害摊薄、几乎打不掉）④**选靶优先级**：**哨戒机 → 攻坚机 → 其余等权**（侦察机与战斗机同权）——⚠ **前提：哨戒机必须在近防炮射程（`PD_SENTRY_RANGE_M` = 2,500m）内**（船长 2026-09-12 追加：「**优先攻击哨戒机的前提是哨戒在射程内**」）：哨戒机只有两舰贴近时才进候选池，拉开距离时**既不可选也不占优先级**，优先级自然落到"攻坚机 → 其余"，集火锁同步解除⑤**攻坚机 25% 全抗性**（三层 × 三系全 0.25）⑥**判定频率 500ms 保留**：反击制只管"能不能开火"，频率管"开火后多久掷一次"（= 还手速率上限 2 次/秒；实测令牌速率 ≈0.23 次/秒 ⇒ 当前不是瓶颈）。**⚠ 同批修一处真 bug**：旧代码把"出击型反击令牌"与"哨戒机在射程内"用 **AND** 卡在一起（`if (!sentryInRange) return`）⇒ 出击型机群**永远不会被反击**（与"攻击一次换一次无视射程的反击"直接矛盾）⇒ 已改为**两条独立许可（OR）**。**改后读数**（`battle:pd-tune` 王鲭 D3）：噬口猎杀令 **每场平均损失 3.2 架（全为哨戒机）· 战损估值 30,400 ISK · 占卡奖励 3.6%**（改前 0 架 / 0.0%）；坟场/穹顶仍 0 架（玩家 0% 胜率、17~18 秒被打死，战斗太短） | 引擎 `balance.ts`（`pdAcc/pdHitFloor/pdTierMul/pdDmg`）· `combat.resolvePointDefense` · `state.pdBattle.pdFocus` · 数据 `items.ts`（攻坚机抗性）；守卫 = `tests/pd-rules.test.ts`（5 条）+ `tools/pd-tune.ts` 读数 |
| 远征善后返航（换船） | 返航（expedition.phase='back'）中切换驾驶：本趟返航转为旧船自动账本（shipReturns reason='expedition'，按剩余航程到港自动卸货），并立即停止重复清剿；出击/交火中仍不可换船 | shipyard.ts changeShip；mining.ts advanceShipReturns |
